import type { DocsManifest } from "../../types";

export const manifest = {
  "siteName": "Reactor Docs Preview",
  "productName": "Microsoft.UI.Reactor",
  "description": "Unofficial Fluent-style documentation shell for Microsoft.UI.Reactor.",
  "sourceDescription": "Declarative UI framework for native Windows desktop apps in pure C#.",
  "repoUrl": "https://github.com/microsoft/microsoft-ui-reactor",
  "docsDir": "docs/guide",
  "generatedAt": "2026-05-29T21:24:17.575Z",
  "nav": [
    {
      "title": "Home",
      "path": "index.md",
      "slug": "/"
    },
    {
      "title": "Get Started",
      "children": [
        {
          "title": "Getting Started",
          "path": "getting-started.md",
          "slug": "/getting-started/"
        },
        {
          "title": "Thinking in Reactor",
          "path": "thinking-in-reactor.md",
          "slug": "/thinking-in-reactor/"
        },
        {
          "title": "Reactor for XAML Developers",
          "path": "xaml-developers.md",
          "slug": "/xaml-developers/"
        },
        {
          "title": "Reactor vs XAML",
          "path": "reactor-vs-xaml.md",
          "slug": "/reactor-vs-xaml/"
        }
      ]
    },
    {
      "title": "Learn the Framework",
      "children": [
        {
          "title": "Components",
          "path": "components.md",
          "slug": "/components/"
        },
        {
          "title": "Hooks",
          "path": "hooks.md",
          "slug": "/hooks/"
        },
        {
          "title": "Effects and Lifecycle",
          "path": "effects.md",
          "slug": "/effects/"
        },
        {
          "title": "Context",
          "path": "context.md",
          "slug": "/context/"
        },
        {
          "title": "Commanding",
          "path": "commanding.md",
          "slug": "/commanding/"
        },
        {
          "title": "Advanced Patterns",
          "path": "advanced.md",
          "slug": "/advanced/"
        }
      ]
    },
    {
      "title": "UI Surface",
      "children": [
        {
          "title": "Layout",
          "path": "layout.md",
          "slug": "/layout/"
        },
        {
          "title": "Flex Layout",
          "path": "flex-layout.md",
          "slug": "/flex-layout/"
        },
        {
          "title": "Styling and Theming",
          "path": "styling.md",
          "slug": "/styling/"
        },
        {
          "title": "Animation",
          "path": "animation.md",
          "slug": "/animation/"
        },
        {
          "title": "Input and Gestures",
          "path": "input-and-gestures.md",
          "slug": "/input-and-gestures/"
        }
      ]
    },
    {
      "title": "Controls",
      "children": [
        {
          "title": "Controls Catalog",
          "path": "controls.md",
          "slug": "/controls/"
        },
        {
          "title": "Forms and Input",
          "path": "forms.md",
          "slug": "/forms/"
        },
        {
          "title": "Collections",
          "path": "collections.md",
          "slug": "/collections/"
        },
        {
          "title": "Text and Media",
          "path": "text-and-media.md",
          "slug": "/text-and-media/"
        },
        {
          "title": "Status and Info",
          "path": "status-and-info.md",
          "slug": "/status-and-info/"
        },
        {
          "title": "Dialogs and Flyouts",
          "path": "dialogs-and-flyouts.md",
          "slug": "/dialogs-and-flyouts/"
        },
        {
          "title": "Data System",
          "path": "data-system.md",
          "slug": "/data-system/"
        },
        {
          "title": "Charting",
          "path": "charting.md",
          "slug": "/charting/"
        }
      ]
    },
    {
      "title": "App Architecture",
      "children": [
        {
          "title": "Navigation",
          "path": "navigation.md",
          "slug": "/navigation/"
        },
        {
          "title": "Windows",
          "path": "windows.md",
          "slug": "/windows/"
        },
        {
          "title": "Docking Windows",
          "path": "docking.md",
          "slug": "/docking/"
        },
        {
          "title": "Async Resources",
          "path": "async-resources.md",
          "slug": "/async-resources/"
        },
        {
          "title": "Persistence",
          "path": "persistence.md",
          "slug": "/persistence/"
        },
        {
          "title": "Localization",
          "path": "localization.md",
          "slug": "/localization/"
        },
        {
          "title": "Accessibility",
          "path": "accessibility.md",
          "slug": "/accessibility/"
        }
      ]
    },
    {
      "title": "Patterns & Recipes",
      "children": [
        {
          "title": "Recipes",
          "path": "recipes/index.md",
          "slug": "/recipes/"
        },
        {
          "title": "Command Palette",
          "path": "recipes/command-palette.md",
          "slug": "/recipes/command-palette/"
        },
        {
          "title": "Drag Reorder",
          "path": "recipes/drag-reorder.md",
          "slug": "/recipes/drag-reorder/"
        },
        {
          "title": "Login",
          "path": "recipes/login.md",
          "slug": "/recipes/login/"
        },
        {
          "title": "Master Detail",
          "path": "recipes/master-detail.md",
          "slug": "/recipes/master-detail/"
        },
        {
          "title": "Modal Dialog",
          "path": "recipes/modal-dialog.md",
          "slug": "/recipes/modal-dialog/"
        },
        {
          "title": "Multi-step Form",
          "path": "recipes/multi-step-form.md",
          "slug": "/recipes/multi-step-form/"
        },
        {
          "title": "Paginated List",
          "path": "recipes/paginated-list.md",
          "slug": "/recipes/paginated-list/"
        },
        {
          "title": "Search with Suggestions",
          "path": "recipes/search-with-suggestions.md",
          "slug": "/recipes/search-with-suggestions/"
        },
        {
          "title": "Settings Page",
          "path": "recipes/settings-page.md",
          "slug": "/recipes/settings-page/"
        },
        {
          "title": "Cheat Sheet",
          "path": "cheat-sheet.md",
          "slug": "/cheat-sheet/"
        },
        {
          "title": "Rules of Reactor",
          "path": "rules-of-reactor.md",
          "slug": "/rules-of-reactor/"
        },
        {
          "title": "Theming Tokens",
          "path": "theming-tokens.md",
          "slug": "/theming-tokens/"
        }
      ]
    },
    {
      "title": "Tooling & Process",
      "children": [
        {
          "title": "Dev Tooling",
          "path": "dev-tooling.md",
          "slug": "/dev-tooling/"
        },
        {
          "title": "Testing",
          "path": "testing.md",
          "slug": "/testing/"
        },
        {
          "title": "Performance",
          "path": "performance.md",
          "slug": "/performance/"
        },
        {
          "title": "Packaging",
          "path": "packaging.md",
          "slug": "/packaging/"
        },
        {
          "title": "Diagnostics",
          "path": "diagnostics.md",
          "slug": "/diagnostics/"
        }
      ]
    },
    {
      "title": "Interop",
      "children": [
        {
          "title": "WinForms Interop",
          "path": "winforms-interop.md",
          "slug": "/winforms-interop/"
        },
        {
          "title": "WPF Interop",
          "path": "wpf-interop.md",
          "slug": "/wpf-interop/"
        }
      ]
    },
    {
      "title": "Under the Hood",
      "children": [
        {
          "title": "Architecture Overview",
          "path": "architecture-overview.md",
          "slug": "/architecture-overview/"
        },
        {
          "title": "Reactivity Model",
          "path": "reactivity-model.md",
          "slug": "/reactivity-model/"
        },
        {
          "title": "Hooks Internals",
          "path": "hooks-internals.md",
          "slug": "/hooks-internals/"
        },
        {
          "title": "Reconciliation",
          "path": "reconciliation.md",
          "slug": "/reconciliation/"
        },
        {
          "title": "Control Reconciler Protocol",
          "path": "control-reconciler-protocol.md",
          "slug": "/control-reconciler-protocol/"
        },
        {
          "title": "Extending Reactor Controls",
          "path": "extending-reactor-controls.md",
          "slug": "/extending-reactor-controls/"
        },
        {
          "title": "Element Pool",
          "path": "element-pool.md",
          "slug": "/element-pool/"
        },
        {
          "title": "Effects Scheduling",
          "path": "effects-scheduling.md",
          "slug": "/effects-scheduling/"
        },
        {
          "title": "Threading and Dispatch",
          "path": "threading-and-dispatch.md",
          "slug": "/threading-and-dispatch/"
        },
        {
          "title": "Source Mapping",
          "path": "source-mapping.md",
          "slug": "/source-mapping/"
        },
        {
          "title": "Modifier System",
          "path": "modifier-system.md",
          "slug": "/modifier-system/"
        },
        {
          "title": "Analyzer Architecture",
          "path": "analyzer-architecture.md",
          "slug": "/analyzer-architecture/"
        },
        {
          "title": "DevTools Internals",
          "path": "devtools-internals.md",
          "slug": "/devtools-internals/"
        },
        {
          "title": "Animation Pipeline",
          "path": "animation-pipeline.md",
          "slug": "/animation-pipeline/"
        },
        {
          "title": "Focus and Input Internals",
          "path": "focus-and-input-internals.md",
          "slug": "/focus-and-input-internals/"
        },
        {
          "title": "Perf Instrumentation",
          "path": "perf-instrumentation.md",
          "slug": "/perf-instrumentation/"
        }
      ]
    },
    {
      "title": "API Reference",
      "children": [
        {
          "title": "Hooks",
          "path": "reference/hooks/index.md",
          "slug": "/reference/hooks/"
        }
      ]
    }
  ],
  "pages": [
    {
      "title": "Home",
      "path": "index.md",
      "slug": "/",
      "html": "<h1 id=\"reactor\">Reactor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactor\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor) is a declarative UI framework for building native Windows desktop apps in\npure C#. No XAML. No data binding. No view models. You describe your UI as a\nfunction of state, and Reactor keeps the screen in sync.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>If you want to...</th><th>Start here</th></tr></thead><tbody><tr><td>Build your first Reactor app</td><td><a href=\"/getting-started/\">Getting Started</a></td></tr><tr><td>Understand the mental model</td><td><a href=\"/thinking-in-reactor/\">Thinking in Reactor</a></td></tr><tr><td>Port a XAML/WinUI/WPF app</td><td><a href=\"/xaml-developers/\">Reactor for XAML Developers</a></td></tr><tr><td>Look up a hook or modifier</td><td><a href=\"/reference/hooks/\">API Reference</a></td></tr><tr><td>Read how the runtime works</td><td><a href=\"/architecture-overview/\">Architecture Overview</a></td></tr></tbody></table>\n<h2 id=\"why-reactor\">Why Reactor?<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#why-reactor\">#</a></h2>\n<p><strong>Pure C# from top to bottom.</strong> Your entire app — layout, styling, state,\nlogic — lives in <code>.cs</code> files. No markup languages, no code-behind split, no\ndesigner files.</p>\n<p><strong>Declarative rendering.</strong> You write a <code>Render()</code> method that returns your UI.\nWhen state changes, Reactor diffs the old and new element trees and patches only\nwhat changed in the native WinUI controls.</p>\n<p><strong>Hooks for state management.</strong> <code>UseState</code>, <code>UseReducer</code>, <code>UseEffect</code>, and\nfriends give you React-style state management without the JavaScript.</p>\n<p><strong>Native performance.</strong> Reactor renders to real WinUI 3 controls. Your app is a\nstandard Windows desktop app — no web views, no Electron, no interpretation\nlayer.</p>\n<h2 id=\"quick-look\">Quick Look<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#quick-look\">#</a></h2>\n<p>A complete Reactor app in one file:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HelloWorld</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Hello from Reactor!\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n            TextBlock(<span class=\"hljs-string\">\"No XAML. No data binding. Just C#.\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/readme/hello-world.png\" alt=\"Hello world component\"></p>\n<p>State and interactivity in a few lines:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">QuickCounter</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">8</span>,\n            Button(<span class=\"hljs-string\">\"- 1\"</span>, () => setCount(count - <span class=\"hljs-number\">1</span>)),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{count}</span>\"</span>).FontSize(<span class=\"hljs-number\">20</span>).SemiBold().Width(<span class=\"hljs-number\">40</span>)\n                .HAlign(HorizontalAlignment.Center),\n            Button(<span class=\"hljs-string\">\"+ 1\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/readme/counter.png\" alt=\"Counter demo\"></p>\n<p>Built-in text styling:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StyledText</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Heading(<span class=\"hljs-string\">\"Heading element\"</span>),\n            SubHeading(<span class=\"hljs-string\">\"SubHeading element\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Regular text with modifiers\"</span>)\n                .FontSize(<span class=\"hljs-number\">14</span>).Foreground(<span class=\"hljs-string\">\"#0078D4\"</span>),\n            Caption(<span class=\"hljs-string\">\"Caption for fine print\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/readme/styled-text.png\" alt=\"Styled text elements\"></p>\n<h2 id=\"how-it-works\">How It Works<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#how-it-works\">#</a></h2>\n<ol>\n<li>You define <a href=\"/components/\"><strong>components</strong></a> — classes with a <code>Render()</code> method that returns\nan element tree.</li>\n<li>You manage state with <a href=\"/hooks/\"><strong>hooks</strong></a> — <code>UseState</code>, <code>UseReducer</code>, <code>UseEffect</code>,\nand more — called inside <code>Render()</code>.</li>\n<li>When state changes, Reactor calls <code>Render()</code> again, diffs the result, and\nupdates only the WinUI controls that changed.</li>\n</ol>\n<p>That's it. No event subscriptions to manage, no property-changed notifications\nto wire up, no dispatcher threading to worry about.</p>\n<h2 id=\"documentation\">Documentation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#documentation\">#</a></h2>\n<p>The docset is organized as ten sections, working from \"first app on screen\" to\n\"how the runtime is built\". XAML developers should read sections 1, 8, and 9\nfirst; everyone else can follow the order.</p>\n<h3 id=\"1-get-started\">1. Get Started<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#1-get-started\">#</a></h3>\n<ul>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — Create your first app, manage state, build a todo list</li>\n<li><strong><a href=\"/thinking-in-reactor/\">Thinking in Reactor</a></strong> — Mental-model essay: UI as a function of state</li>\n<li><strong><a href=\"/xaml-developers/\">Reactor for XAML Developers</a></strong> — Migration cookbook: XAML, bindings, MVVM, navigation</li>\n<li><strong><a href=\"/reactor-vs-xaml/\">Reactor vs XAML</a></strong> — Architectural essay: DependencyProperty → modifier, Binding → closure</li>\n</ul>\n<h3 id=\"2-learn-the-framework\">2. Learn the framework<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#2-learn-the-framework\">#</a></h3>\n<ul>\n<li><strong><a href=\"/components/\">Components</a></strong> — Component classes, props, function components, composition</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — UseState, UseReducer, UseEffect, UseMemo, UseRef, UseCallback</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — UseEffect patterns, cleanup, async work, timers</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — Share state across the component tree without prop drilling</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Commands, keyboard shortcuts, async actions</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — ErrorBoundary, Memo, observable binding, performance tuning</li>\n</ul>\n<h3 id=\"3-ui-surface\">3. UI surface<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#3-ui-surface\">#</a></h3>\n<ul>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — VStack, HStack, Grid, ScrollView, Border, responsive patterns</li>\n<li><strong><a href=\"/flex-layout/\">Flex Layout</a></strong> — Flexible box layout for adaptive UIs</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Colors, typography, dark/light themes, custom styles</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — Transitions, keyframes, interaction states, choreography</li>\n<li><strong><a href=\"/input-and-gestures/\">Input and Gestures</a></strong> — Pointer events, taps, gestures, access keys</li>\n</ul>\n<h3 id=\"4-controls-catalog\">4. Controls catalog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#4-controls-catalog\">#</a></h3>\n<ul>\n<li><strong><a href=\"/controls/\">Controls</a></strong> — Thumbnail-indexed catalog of every control</li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — Text fields, checkboxes, sliders, validation, data entry</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — ListView, LazyVStack, VirtualList for large datasets</li>\n<li><strong><a href=\"/text-and-media/\">Text and Media</a></strong> — TextBlock, MarkdownTextBlock, Image, MediaPlayerElement, WebView2, InkCanvas</li>\n<li><strong><a href=\"/status-and-info/\">Status and Info</a></strong> — InfoBar, InfoBadge, ProgressBar, TeachingTip, PipsPager</li>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a></strong> — ContentDialog, MenuFlyout, CommandBarFlyout, Popup</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — DataGrid with sort, filter, search, inline editing</li>\n<li><strong><a href=\"/charting/\">Charting</a></strong> — Line, bar, area, pie charts with the ReactorCharting library</li>\n</ul>\n<h3 id=\"5-app-architecture\">5. App architecture<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#5-app-architecture\">#</a></h3>\n<ul>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — NavigationView, TabView, multi-page apps, routing</li>\n<li><strong><a href=\"/windows/\">Windows</a></strong> — Top-level windows, tray icons, OpenWindow, shutdown policy</li>\n<li><strong><a href=\"/docking/\">Docking Windows</a></strong> — Rearrangeable dock panes: splits, tabs, side pins, floating tear-outs, persistence</li>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — <code>UseResource</code>, <code>UseInfiniteResource</code>, <code>UseMutation</code>, <code>Pending</code></li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — UsePersisted, scopes, migration</li>\n<li><strong><a href=\"/localization/\">Localization</a></strong> — Multi-language support, resource strings, RTL layouts</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Screen readers, keyboard navigation, focus trapping, runtime scanning</li>\n</ul>\n<h3 id=\"6-patterns--recipes\">6. Patterns &#x26; recipes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#6-patterns--recipes\">#</a></h3>\n<ul>\n<li><strong><a href=\"/recipes/\">Recipes</a></strong> — Gallery of end-to-end patterns (login, master-detail, paginated list, command palette, …)</li>\n<li><strong><a href=\"/cheat-sheet/\">Cheat Sheet</a></strong> — Single-page reference card</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — Hook rules, render purity, anti-patterns</li>\n<li><strong><a href=\"/theming-tokens/\">Theming Tokens</a></strong> — Full token catalog with swatches</li>\n</ul>\n<h3 id=\"7-tooling--process\">7. Tooling &#x26; process<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#7-tooling--process\">#</a></h3>\n<ul>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — <code>mur</code> CLI, MCP server, VS Code panel, dotnet watch, in-app dev menu</li>\n<li><strong><a href=\"/testing/\">Testing</a></strong> — Headless renderer, snapshot tests, async test patterns</li>\n<li><strong><a href=\"/performance/\">Performance</a></strong> — ETW, EventDispatch, flame graphs</li>\n<li><strong><a href=\"/packaging/\">Packaging</a></strong> — MSIX, single-file, ARM64, AOT considerations</li>\n</ul>\n<h3 id=\"8-interop--integration\">8. Interop &#x26; integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#8-interop--integration\">#</a></h3>\n<ul>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — Host Reactor components inside WinForms apps via XAML Islands</li>\n<li><strong><a href=\"/wpf-interop/\">WPF Interop</a></strong> — Host Reactor components inside WPF apps</li>\n</ul>\n<p>(XAML migration lives in <a href=\"#1-get-started\">Section 1</a> — XAML developers are a primary entry path.)</p>\n<h3 id=\"9-under-the-hood\">9. Under the hood<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#9-under-the-hood\">#</a></h3>\n<p>The runtime explained — for readers who want to know <em>why</em> it works the way it\ndoes, not just how to use it.</p>\n<ul>\n<li><strong><a href=\"/architecture-overview/\">Architecture Overview</a></strong> — Declarative shell → element records → reconciler → WinUI tree</li>\n<li><strong><a href=\"/reactivity-model/\">Reactivity Model</a></strong> — setState → re-render; why hooks not INotifyPropertyChanged</li>\n<li><strong><a href=\"/reactor-vs-xaml/\">Reactor vs XAML</a></strong> — (also in §1) the architectural essay</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Hook slot table, dispatcher, closure capture</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Element-record diff, identity, the three phases</li>\n<li><strong><a href=\"/control-reconciler-protocol/\">Control Reconciler Protocol</a></strong> — The per-control handler protocol: <code>IElementHandler</code>, descriptors, children strategies, pooling</li>\n<li><strong><a href=\"/extending-reactor-controls/\">Extending Reactor Controls</a></strong> — Cookbook: add a native control end-to-end with a descriptor</li>\n<li><strong><a href=\"/element-pool/\">Element Pool</a></strong> — Allocation reduction under scroll-heavy lists</li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — When effects run; dep semantics; cleanup ordering</li>\n<li><strong><a href=\"/threading-and-dispatch/\">Threading and Dispatch</a></strong> — UI-thread invariants, trampoline, batched renders</li>\n<li><strong><a href=\"/source-mapping/\">Source Mapping</a></strong> — How stack traces and devtools attribute work back to user source</li>\n<li><strong><a href=\"/modifier-system/\">Modifier System</a></strong> — How <code>.FontSize(24).Bold()</code> actually works</li>\n<li><strong><a href=\"/analyzer-architecture/\">Analyzer Architecture</a></strong> — Roslyn analyzers shipped with Reactor; authoring your own</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — Dev menu, reconcile-highlight, layout-cost overlay, MCP protocol</li>\n<li><strong><a href=\"/animation-pipeline/\">Animation Pipeline</a></strong> — Composition API end-to-end; 4 animation systems</li>\n<li><strong><a href=\"/focus-and-input-internals/\">Focus and Input Internals</a></strong> — <code>UseFocus</code> dispatcher, <code>FocusTrap</code> container, pointer event flow</li>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — ETW sources, frame-aligned sampling, layout-cost attribution</li>\n</ul>\n<h3 id=\"10-api-reference\">10. API Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#10-api-reference\">#</a></h3>\n<p>Auto-generated from XML doc comments in <code>src/Reactor*/</code>. Each member gets a\nuniform Summary / Parameters / Returns / Discussion / Examples / See Also page.</p>\n<ul>\n<li><strong><a href=\"/reference/hooks/\">Hooks</a></strong> — every <code>Use*</code> hook</li>\n<li><strong>Factories</strong> — every element factory (coming soon)</li>\n<li><strong>Modifiers</strong> — every chainable modifier (coming soon)</li>\n<li><strong>Elements</strong> — every Element record type (coming soon)</li>\n<li><strong>System</strong> — App, Window, Navigation, Context, Command (coming soon)</li>\n</ul>\n<h2 id=\"minimal-project-setup\">Minimal Project Setup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#minimal-project-setup\">#</a></h2>\n<p>Create a console project, then edit the <code>.csproj</code>:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Project</span> <span class=\"hljs-attr\">Sdk</span>=<span class=\"hljs-string\">\"Microsoft.NET.Sdk\"</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PropertyGroup</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">OutputType</span>></span>WinExe<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">OutputType</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TargetFramework</span>></span>net10.0-windows10.0.22621.0<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">TargetFramework</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">UseWinUI</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">UseWinUI</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">WindowsPackageType</span>></span>None<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">WindowsPackageType</span>></span>\n  <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">PropertyGroup</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ItemGroup</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PackageReference</span> <span class=\"hljs-attr\">Include</span>=<span class=\"hljs-string\">\"Microsoft.WindowsAppSDK\"</span> <span class=\"hljs-attr\">Version</span>=<span class=\"hljs-string\">\"2.0.*\"</span> /></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ProjectReference</span> <span class=\"hljs-attr\">Include</span>=<span class=\"hljs-string\">\"..\\Reactor\\Reactor.csproj\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ItemGroup</span>></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">Project</span>></span>\n</code></pre>\n<!-- /ai:lock -->\n<p>Replace <code>App.cs</code> with a component and a <code>ReactorApp.Run&#x3C;T>()</code> call, and run with\n<code>dotnet run</code>. That's your first Reactor app.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start with function components.</strong> For quick experiments, use\n<code>ReactorApp.Run(\"Title\", ctx => { ... })</code> — no class needed.</p>\n<p><strong>Read the <a href=\"/hooks/\">hooks page</a>.</strong> Hooks are the core of Reactor. Understanding <code>UseState</code>\nand <code>UseEffect</code> unlocks everything else.</p>\n<p><strong>Keep components small.</strong> Extract pieces into their own components early.\nComposition is always easier to reason about than a single giant <code>Render()</code>.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — Build your first app, manage state, ship a todo list.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — <code>UseState</code>, <code>UseEffect</code>, <code>UseMemo</code>, and the rest of the core state primitives.</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Component classes, props, and function components in detail.</li>\n<li><strong><a href=\"/xaml-developers/\">Reactor for XAML Developers</a></strong> — Migration cookbook for XAML, bindings, MVVM, and navigation.</li>\n</ul>",
      "text": "Reactor # Microsoft.UI.Reactor (Reactor) is a declarative UI framework for building native Windows desktop apps in pure C#. No XAML. No data binding. No view models. You describe your UI as a function of state, and Reactor keeps the screen in sync. If you want to... Start here Build your first Reactor app Getting Started Understand the mental model Thinking in Reactor Port a XAML/WinUI/WPF app Reactor for XAML Developers Look up a hook or modifier API Reference Read how the runtime works Architecture Overview Why Reactor? # Pure C# from top to bottom. Your entire app — layout, styling, state, logic — lives in .cs files. No markup languages, no code-behind split, no designer files. Declarative rendering. You write a Render() method that returns your UI. When state changes, Reactor diffs the old and new element trees and patches only what changed in the native WinUI controls. Hooks for state management. UseState , UseReducer , UseEffect , and friends give you React-style state management without the JavaScript. Native performance. Reactor renders to real WinUI 3 controls. Your app is a standard Windows desktop app — no web views, no Electron, no interpretation layer. Quick Look # A complete Reactor app in one file: class HelloWorld : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Hello from Reactor!\" ).FontSize( 24 ).Bold(), TextBlock( \"No XAML. No data binding. Just C#.\" ) ).Padding( 24 ); } } State and interactivity in a few lines: class QuickCounter : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return HStack( 8 , Button( \"- 1\" , () => setCount(count - 1 )), TextBlock( $\" {count} \" ).FontSize( 20 ).SemiBold().Width( 40 ) .HAlign(HorizontalAlignment.Center), Button( \"+ 1\" , () => setCount(count + 1 )) ).Padding( 24 ); } } Built-in text styling: class StyledText : Component { public override Element Render () { return VStack( 8 , Heading( \"Heading element\" ), SubHeading( \"SubHeading element\" ), TextBlock( \"Regular text with modifiers\" ) .FontSize( 14 ).Foreground( \"#0078D4\" ), Caption( \"Caption for fine print\" ) ).Padding( 24 ); } } How It Works # You define components — classes with a Render() method that returns an element tree. You manage state with hooks — UseState , UseReducer , UseEffect , and more — called inside Render() . When state changes, Reactor calls Render() again, diffs the result, and updates only the WinUI controls that changed. That's it. No event subscriptions to manage, no property-changed notifications to wire up, no dispatcher threading to worry about. Documentation # The docset is organized as ten sections, working from \"first app on screen\" to \"how the runtime is built\". XAML developers should read sections 1, 8, and 9 first; everyone else can follow the order. 1. Get Started # Getting Started — Create your first app, manage state, build a todo list Thinking in Reactor — Mental-model essay: UI as a function of state Reactor for XAML Developers — Migration cookbook: XAML, bindings, MVVM, navigation Reactor vs XAML — Architectural essay: DependencyProperty → modifier, Binding → closure 2. Learn the framework # Components — Component classes, props, function components, composition Hooks — UseState, UseReducer, UseEffect, UseMemo, UseRef, UseCallback Effects and Lifecycle — UseEffect patterns, cleanup, async work, timers Context — Share state across the component tree without prop drilling Commanding — Commands, keyboard shortcuts, async actions Advanced Patterns — ErrorBoundary, Memo, observable binding, performance tuning 3. UI surface # Layout — VStack, HStack, Grid, ScrollView, Border, responsive patterns Flex Layout — Flexible box layout for adaptive UIs Styling and Theming — Colors, typography, dark/light themes, custom styles Animation — Transitions, keyframes, interaction states, choreography Input and Gestures — Pointer events, taps, gestures, access keys 4. Controls catalog # Controls — Thumbnail-indexed catalog of every control Forms and Input — Text fields, checkboxes, sliders, validation, data entry Collections — ListView, LazyVStack, VirtualList for large datasets Text and Media — TextBlock, MarkdownTextBlock, Image, MediaPlayerElement, WebView2, InkCanvas Status and Info — InfoBar, InfoBadge, ProgressBar, TeachingTip, PipsPager Dialogs and Flyouts — ContentDialog, MenuFlyout, CommandBarFlyout, Popup Data System — DataGrid with sort, filter, search, inline editing Charting — Line, bar, area, pie charts with the ReactorCharting library 5. App architecture # Navigation — NavigationView, TabView, multi-page apps, routing Windows — Top-level windows, tray icons, OpenWindow, shutdown policy Docking Windows — Rearrangeable dock panes: splits, tabs, side pins, floating tear-outs, persistence Async Resources — UseResource , UseInfiniteResource , UseMutation , Pending Persistence — UsePersisted, scopes, migration Localization — Multi-language support, resource strings, RTL layouts Accessibility — Screen readers, keyboard navigation, focus trapping, runtime scanning 6. Patterns &#x26; recipes # Recipes — Gallery of end-to-end patterns (login, master-detail, paginated list, command palette, …) Cheat Sheet — Single-page reference card Rules of Reactor — Hook rules, render purity, anti-patterns Theming Tokens — Full token catalog with swatches 7. Tooling &#x26; process # Dev Tooling — mur CLI, MCP server, VS Code panel, dotnet watch, in-app dev menu Testing — Headless renderer, snapshot tests, async test patterns Performance — ETW, EventDispatch, flame graphs Packaging — MSIX, single-file, ARM64, AOT considerations 8. Interop &#x26; integration # WinForms Interop — Host Reactor components inside WinForms apps via XAML Islands WPF Interop — Host Reactor components inside WPF apps (XAML migration lives in Section 1 — XAML developers are a primary entry path.) 9. Under the hood # The runtime explained — for readers who want to know why it works the way it does, not just how to use it. Architecture Overview — Declarative shell → element records → reconciler → WinUI tree Reactivity Model — setState → re-render; why hooks not INotifyPropertyChanged Reactor vs XAML — (also in §1) the architectural essay Hooks Internals — Hook slot table, dispatcher, closure capture Reconciliation — Element-record diff, identity, the three phases Control Reconciler Protocol — The per-control handler protocol: IElementHandler , descriptors, children strategies, pooling Extending Reactor Controls — Cookbook: add a native control end-to-end with a descriptor Element Pool — Allocation reduction under scroll-heavy lists Effects Scheduling — When effects run; dep semantics; cleanup ordering Threading and Dispatch — UI-thread invariants, trampoline, batched renders Source Mapping — How stack traces and devtools attribute work back to user source Modifier System — How .FontSize(24).Bold() actually works Analyzer Architecture — Roslyn analyzers shipped with Reactor; authoring your own DevTools Internals — Dev menu, reconcile-highlight, layout-cost overlay, MCP protocol Animation Pipeline — Composition API end-to-end; 4 animation systems Focus and Input Internals — UseFocus dispatcher, FocusTrap container, pointer event flow Perf Instrumentation — ETW sources, frame-aligned sampling, layout-cost attribution 10. API Reference # Auto-generated from XML doc comments in src/Reactor*/ . Each member gets a uniform Summary / Parameters / Returns / Discussion / Examples / See Also page. Hooks — every Use* hook Factories — every element factory (coming soon) Modifiers — every chainable modifier (coming soon) Elements — every Element record type (coming soon) System — App, Window, Navigation, Context, Command (coming soon) Minimal Project Setup # Create a console project, then edit the .csproj : &#x3C; Project Sdk = \"Microsoft.NET.Sdk\" > &#x3C; PropertyGroup > &#x3C; OutputType > WinExe &#x3C;/ OutputType > &#x3C; TargetFramework > net10.0-windows10.0.22621.0 &#x3C;/ TargetFramework > &#x3C; UseWinUI > true &#x3C;/ UseWinUI > &#x3C; WindowsPackageType > None &#x3C;/ WindowsPackageType > &#x3C;/ PropertyGroup > &#x3C; ItemGroup > &#x3C; PackageReference Include = \"Microsoft.WindowsAppSDK\" Version = \"2.0.*\" /> &#x3C; ProjectReference Include = \"..\\Reactor\\Reactor.csproj\" /> &#x3C;/ ItemGroup > &#x3C;/ Project > Replace App.cs with a component and a ReactorApp.Run&#x3C;T>() call, and run with dotnet run . That's your first Reactor app. Tips # Start with function components. For quick experiments, use ReactorApp.Run(\"Title\", ctx => { ... }) — no class needed. Read the hooks page . Hooks are the core of Reactor. Understanding UseState and UseEffect unlocks everything else. Keep components small. Extract pieces into their own components early. Composition is always easier to reason about than a single giant Render() . Next Steps # Getting Started — Build your first app, manage state, ship a todo list. Hooks — UseState , UseEffect , UseMemo , and the rest of the core state primitives. Components — Component classes, props, and function components in detail. Reactor for XAML Developers — Migration cookbook for XAML, bindings, MVVM, and navigation.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/index.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/index.md",
      "headings": [
        {
          "depth": 2,
          "id": "why-reactor",
          "text": "Why Reactor?"
        },
        {
          "depth": 2,
          "id": "quick-look",
          "text": "Quick Look"
        },
        {
          "depth": 2,
          "id": "how-it-works",
          "text": "How It Works"
        },
        {
          "depth": 2,
          "id": "documentation",
          "text": "Documentation"
        },
        {
          "depth": 3,
          "id": "1-get-started",
          "text": "1. Get Started"
        },
        {
          "depth": 3,
          "id": "2-learn-the-framework",
          "text": "2. Learn the framework"
        },
        {
          "depth": 3,
          "id": "3-ui-surface",
          "text": "3. UI surface"
        },
        {
          "depth": 3,
          "id": "4-controls-catalog",
          "text": "4. Controls catalog"
        },
        {
          "depth": 3,
          "id": "5-app-architecture",
          "text": "5. App architecture"
        },
        {
          "depth": 3,
          "id": "6-patterns--recipes",
          "text": "6. Patterns &#x26; recipes"
        },
        {
          "depth": 3,
          "id": "7-tooling--process",
          "text": "7. Tooling &#x26; process"
        },
        {
          "depth": 3,
          "id": "8-interop--integration",
          "text": "8. Interop &#x26; integration"
        },
        {
          "depth": 3,
          "id": "9-under-the-hood",
          "text": "9. Under the hood"
        },
        {
          "depth": 3,
          "id": "10-api-reference",
          "text": "10. API Reference"
        },
        {
          "depth": 2,
          "id": "minimal-project-setup",
          "text": "Minimal Project Setup"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Getting Started",
      "path": "getting-started.md",
      "slug": "/getting-started/",
      "html": "<p>A Microsoft.UI.Reactor (Reactor) app is a tree of <a href=\"/components/\">components</a> driven by\n<a href=\"/hooks/\">hooks</a>, hosted in a WinUI window that the framework opens and\nmanages for you. You write one C# file, call <code>ReactorApp.Run&#x3C;T></code>, and your\ncomponent's <code>Render()</code> method returns the element tree that becomes the\nnative control tree. State lives in <code>UseState</code> and friends; every setter\ninvocation re-runs <code>Render()</code>; the reconciler diffs the new tree against\nthe previous one and patches the WinUI controls in place. This page is\nthe bootstrap walkthrough — installing the framework, scaffolding a\nproject, and growing from hello-world to a todo list and a calculator.\nBy the end you will have run code, seen a screenshot of each step, and\nrecognized the <a href=\"/layout/\">layout</a> primitives and <a href=\"/hooks/\">hooks</a> that\nthe rest of the docset elaborates.</p>\n<h1 id=\"getting-started-with-reactor\">Getting Started with Reactor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#getting-started-with-reactor\">#</a></h1>\n<!-- ai:lock -->\n<blockquote>\n<p><strong>Prerequisites:</strong> .NET 10+ and the Windows App SDK.</p>\n</blockquote>\n<!-- /ai:lock -->\n<blockquote>\n<p><strong>Heads up — no signed NuGet yet.</strong> Reactor doesn't ship a signed NuGet\npackage today, so you build the framework, the <code>mur</code> CLI, and the project\ntemplate from source. <code>bootstrap.ps1</code> automates all of that — one command,\n~3 minutes per machine. The signed distribution is tracked in\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/022-packaging-and-distribution.md\">spec 022</a>.</p>\n</blockquote>\n<p>Reactor is a declarative UI framework for building native Windows apps in pure C#.\nNo XAML, no data binding, no view models. You describe your UI as a function of\nstate and Reactor keeps the screen in sync.</p>\n<h2 id=\"setup-one-time\">Setup (one-time)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#setup-one-time\">#</a></h2>\n<pre><code class=\"hljs language-powershell\">git clone https://github.com/microsoft/microsoft-ui-reactor.git\ncd microsoft-ui-reactor\n./bootstrap.ps1\n</code></pre>\n<p>That's it. <code>bootstrap.ps1</code> packs and installs <code>mur</code> as a <code>dotnet tool</code> global\ninstall (so it's on PATH cross-shell with no manual <code>$env:Path</code> edits), runs\n<code>mur pack-local</code> to produce <code>local-nupkgs/Microsoft.UI.Reactor.0.0.0-local.nupkg</code>\nand the matching <code>ProjectTemplates</code> nupkg, registers the <code>dotnet new reactorapp</code>\ntemplate, and drops the Reactor agent plugin under <code>~/.claude/plugins/reactor</code>\n(symlink when allowed, copy otherwise).</p>\n<p>When it finishes you can immediately run:</p>\n<pre><code class=\"hljs language-powershell\">dotnet new reactorapp -n MyApp\ncd MyApp\ndotnet run\n</code></pre>\n<h3 id=\"after-git-pull\">After <code>git pull</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#after-git-pull\">#</a></h3>\n<p>The framework changes — your local NuGet snapshot and <code>dotnet new</code> template do\nnot, unless you repack them. Two options:</p>\n<pre><code class=\"hljs language-powershell\">mur upgrade           # repacks the framework + templates and refreshes plugin\n./bootstrap.ps1       # same, plus updates the `mur` global tool itself\n</code></pre>\n<p><code>mur upgrade</code> is the lightweight path. Re-run <code>bootstrap.ps1</code> when you want to\npick up CLI changes (a <code>mur</code> process can't replace its own binary mid-run).</p>\n<h3 id=\"verify-the-install\">Verify the install<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#verify-the-install\">#</a></h3>\n<pre><code class=\"hljs language-powershell\">mur doctor\n</code></pre>\n<p>Lists every dependency the rest of this guide assumes — .NET 10+ SDK, <code>mur</code> on\nPATH, current <code>local-nupkgs/</code> feed, the <code>reactorapp</code> template registration, and\nthe optional Claude plugin. Each line is PASS / WARN / FAIL with a one-line\nremediation for anything broken.</p>\n<blockquote>\n<p><strong>What this gets you.</strong> A globally-resolvable <code>mur</code> (via <code>~/.dotnet/tools</code>),\na local NuGet feed at <code>&#x3C;repo>/local-nupkgs/</code> that apps in any folder can\nconsume via <code>&#x3C;PackageReference Include=\"Microsoft.UI.Reactor\" Version=\"0.0.0-local\" /></code>, and an agent plugin so AI assistants generate\nagainst the real factories (<code>mur --skill</code> / <code>mur --api</code> print the same\ncontent). Run <code>mur upgrade</code> whenever you pull new framework changes.</p>\n</blockquote>\n<blockquote>\n<p><strong>Already have a signed package?</strong> Skip the bootstrap. Reference the\npublished <code>Microsoft.UI.Reactor</code> package directly and run the consumer-side\n<code>install-skill-kit.ps1</code> shipped in the release archive (covered in\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/022-packaging-and-distribution.md\">spec 022</a> §4.4).\nUntil that release ships, the bootstrap above is the supported path.</p>\n</blockquote>\n<h3 id=\"manual-setup\">Manual setup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#manual-setup\">#</a></h3>\n<p>If you'd rather not run <code>bootstrap.ps1</code>, here is exactly what it does, step\nby step. Every command is a stock <code>dotnet</code> or <code>git</code> invocation — no Reactor\ntooling required until the very last step, where you build <code>mur</code> from source\nand install it as a global tool. Each block below corresponds to a numbered\nphase in <code>bootstrap.ps1</code>, so the script is a useful cross-reference if\nanything goes wrong.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>#</th><th>Step</th><th>What it produces</th></tr></thead><tbody><tr><td>1</td><td><code>dotnet --list-sdks</code></td><td>Confirms .NET 10+ is installed</td></tr><tr><td>2</td><td><code>git clone</code> + <code>cd</code></td><td>Local source checkout</td></tr><tr><td>3</td><td><code>dotnet pack src/Reactor.Cli</code></td><td><code>Microsoft.UI.Reactor.Cli.&#x3C;ver>.nupkg</code> in <code>local-nupkgs/</code></td></tr><tr><td>4</td><td><code>dotnet tool install -g</code></td><td><code>mur</code> resolvable cross-shell from <code>~/.dotnet/tools</code></td></tr><tr><td>5</td><td><code>mur pack-local</code></td><td>Framework + <code>ProjectTemplates</code> 0.0.0-local nupkgs</td></tr><tr><td>6</td><td><code>dotnet new uninstall</code> + <code>install</code></td><td><code>dotnet new reactorapp</code> template registered</td></tr><tr><td>7</td><td>Symlink/copy <code>plugins/reactor</code></td><td>Reactor agent kit under <code>~/.claude/plugins/reactor</code> (optional)</td></tr><tr><td>8</td><td><code>mur doctor</code></td><td>Verification that 1–7 all stuck</td></tr></tbody></table>\n<p><strong>1. Confirm prerequisites.</strong> Reactor requires .NET 10 or later. The Windows\nApp SDK is pulled in transitively when the framework builds.</p>\n<pre><code class=\"hljs language-powershell\">dotnet --list-sdks\n# Expect at least one entry starting with \"10.\"\n</code></pre>\n<p><strong>2. Clone and enter the repo.</strong> Everything below assumes the working\ndirectory is the repo root.</p>\n<pre><code class=\"hljs language-powershell\">git clone https://github.com/microsoft/microsoft-ui-reactor.git\ncd microsoft-ui-reactor\n</code></pre>\n<p><strong>3. Pack <code>mur</code> as a global-tool nupkg.</strong> <code>Reactor.Cli.csproj</code> sets\n<code>PackAsTool=true</code>, so <code>dotnet pack</code> produces a tool package. The\n<code>-p:Platform</code> flag matters because the build step runs the SignaturesGen\napphost to refresh <code>skills/reactor.api.txt</code>; that apphost must match the\nhost architecture.</p>\n<pre><code class=\"hljs language-powershell\">$hostArch = if ($env:PROCESSOR_ARCHITECTURE -eq 'ARM64') { 'ARM64' } else { 'x64' }\ndotnet pack src/Reactor.Cli/Reactor.Cli.csproj `\n    -c Release `\n    \"-p:Platform=$hostArch\" `\n    -o local-nupkgs `\n    --nologo\n# Produces local-nupkgs/Microsoft.UI.Reactor.Cli.&#x3C;version>.nupkg\n</code></pre>\n<p><strong>4. Install <code>mur</code> as a dotnet global tool.</strong> This is what puts <code>mur</code> on\nPATH cross-shell with no <code>$env:Path</code> edits. If a previous install exists,\nuse <code>update</code> instead of <code>install</code>.</p>\n<pre><code class=\"hljs language-powershell\">dotnet tool install -g `\n    --add-source ./local-nupkgs `\n    Microsoft.UI.Reactor.Cli `\n    --no-cache --ignore-failed-sources\n\n# If `mur` was already installed:\n# dotnet tool update -g --add-source ./local-nupkgs Microsoft.UI.Reactor.Cli --no-cache --ignore-failed-sources\n</code></pre>\n<p><code>dotnet tool install -g</code> adds <code>~/.dotnet/tools</code> to the <strong>user</strong> PATH, which\nthe current shell does not automatically inherit. For this session only,\nprepend it manually so the next step finds <code>mur</code>:</p>\n<pre><code class=\"hljs language-powershell\">$env:Path = \"$env:USERPROFILE\\.dotnet\\tools;$env:Path\"\n</code></pre>\n<p>New PowerShell windows pick up the user-PATH change on their own.</p>\n<p><strong>5. Pack the framework and project templates.</strong> This is what produces the\ntwo <code>0.0.0-local</code> nupkgs that the <code>dotnet new reactorapp</code> template\nreferences.</p>\n<pre><code class=\"hljs language-powershell\">mur pack-local\n# Produces:\n#   local-nupkgs/Microsoft.UI.Reactor.0.0.0-local.nupkg\n#   local-nupkgs/Microsoft.UI.Reactor.ProjectTemplates.0.0.0-local.nupkg\n</code></pre>\n<p>If you'd rather not depend on the freshly-installed <code>mur</code>, you can invoke\nthe source project directly:</p>\n<pre><code class=\"hljs language-powershell\">dotnet run --project src/Reactor.Cli/Reactor.Cli.csproj `\n    -c Release \"-p:Platform=$hostArch\" -- pack-local\n</code></pre>\n<p><strong>6. Install the <code>dotnet new reactorapp</code> template.</strong> The template engine\ncaches by package id, so a same-version repack can lose to the cached copy.\nAlways uninstall first.</p>\n<pre><code class=\"hljs language-powershell\">dotnet new uninstall Microsoft.UI.Reactor.ProjectTemplates 2>$null\ndotnet new install local-nupkgs/Microsoft.UI.Reactor.ProjectTemplates.0.0.0-local.nupkg\n</code></pre>\n<p><strong>7. (Optional) Install the Reactor agent plugin.</strong> If you use Claude Code\nor another agent and want it to author Reactor code with the right\nfactories, drop the in-repo plugin folder into your agent's plugin path. A\nsymlink is preferred so edits in the checkout are immediately visible; a\ncopy works when you can't create symlinks (Developer Mode off + non-admin\nshell).</p>\n<pre><code class=\"hljs language-powershell\">$pluginSrc = (Resolve-Path \"plugins/reactor\").Path\n$pluginDst = \"$env:USERPROFILE\\.claude\\plugins\\reactor\"\nNew-Item -ItemType Directory -Path (Split-Path $pluginDst) -Force | Out-Null\nif (Test-Path $pluginDst) { Remove-Item $pluginDst -Recurse -Force }\ntry {\n    New-Item -ItemType SymbolicLink -Path $pluginDst -Target $pluginSrc -ErrorAction Stop | Out-Null\n} catch {\n    Copy-Item $pluginSrc $pluginDst -Recurse -Force\n}\n</code></pre>\n<p>For Copilot CLI or other agents, follow that tool's plugin-install path and\npoint it at <code>&#x3C;repo>/plugins/reactor</code>.</p>\n<p><strong>8. Verify.</strong> <code>mur doctor</code> performs the same checks the bootstrap script's\nfinal stage relies on — SDK version, <code>mur</code> resolvability, both <code>0.0.0-local</code>\nnupkgs present, template registered, and plugin installed.</p>\n<pre><code class=\"hljs language-powershell\">mur doctor\n</code></pre>\n<h4 id=\"refreshing-after-git-pull\">Refreshing after <code>git pull</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#refreshing-after-git-pull\">#</a></h4>\n<p>Without the bootstrap script, repeat <strong>steps 5 and 6</strong> after every pull —\nthe framework nupkg and the template both need to be regenerated against\nthe new source. Repeat <strong>steps 3 and 4</strong> only when <code>src/Reactor.Cli/</code>\nitself changes (a running <code>mur</code> process cannot replace its own binary, so\nthe install must happen from a shell that isn't already running <code>mur</code>).</p>\n<blockquote>\n<p><strong>Why a global tool instead of just running from <code>bin/&#x3C;arch>/</code>?</strong> Both\nwork. The repo's CLI csproj still mirrors <code>mur.exe</code> to <code>bin/&#x3C;arch>/</code> after\nevery build for the legacy PATH layout. The global-tool install is just\nthe friendliest default — it puts <code>mur</code> on PATH cross-shell, cross-CWD,\nwith no arch-aware PATH munging, and <code>dotnet tool update -g</code> becomes the\nupgrade verb.</p>\n</blockquote>\n<blockquote>\n<p><strong>Caveat:</strong> The bootstrap is the supported developer path <em>only</em> until the signed public\nNuGet ships under spec 022. If you skip <code>bootstrap.ps1</code> and try <code>dotnet new reactorapp</code> anyway, the scaffolder fails with <code>NU1101: Unable to find package Microsoft.UI.Reactor.ProjectTemplates</code> because nothing has produced\n<code>local-nupkgs/</code> yet — <code>dotnet</code> looks at the configured feeds and the package\nisn't on nuget.org. The fix is to re-run <code>bootstrap.ps1</code> (or <code>mur upgrade</code>)\nafter every <code>git pull</code>; the snapshot is regenerated from the current branch,\nnot cached. The template installer caches by package id, so a same-version\nrepack can lose to the cached copy — <code>mur upgrade</code> handles this by running\n<code>dotnet new uninstall</code> first.</p>\n</blockquote>\n<h2 id=\"creating-a-project\">Creating a Project<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#creating-a-project\">#</a></h2>\n<p>With the template installed, scaffold a new app from anywhere on disk:</p>\n<pre><code class=\"hljs language-powershell\">dotnet new reactorapp -n MyApp\ncd MyApp\ndotnet run\n</code></pre>\n<p>The template wires up the <code>Microsoft.UI.Reactor</code> package reference, the\nWinUI 3 target framework, and a working <code>App.cs</code> that mounts a single\nReactor component. No <code>App.xaml</code>, no <code>MainWindow.xaml.cs</code> — just one C#\nfile.</p>\n<blockquote>\n<p><strong>Why a custom template?</strong> A <code>dotnet new console</code> does not produce a WinUI\napp — it builds a console target with no UI thread, no <code>OutputType=WinExe</code>,\nno WindowsAppSDK reference, and no <code>[STAThread]</code> entry point. <code>reactorapp</code>\nsets all of those plus the Reactor package reference and a backdrop-aware\nroot component, so you get a window on first <code>dotnet run</code> instead of a\nconsole-host stub.</p>\n</blockquote>\n<h2 id=\"your-first-app\">Your First App<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#your-first-app\">#</a></h2>\n<p>The template's <code>App.cs</code> is the canonical hello-world. Replace its contents\nwith the snippet below to match the rest of this guide (the template\ndefaults to a slightly richer starter; the simpler form is easier to walk\nthrough):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor.Core;\n<span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">static</span> Microsoft.UI.Reactor.Factories;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Xaml;\n\nReactorApp.Run&#x3C;GettingStartedApp>(<span class=\"hljs-string\">\"Getting Started\"</span>, width: <span class=\"hljs-number\">600</span>, height: <span class=\"hljs-number\">400</span>, devtools: <span class=\"hljs-literal\">true</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GettingStartedApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"World\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Hello, <span class=\"hljs-subst\">{name}</span>!\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Enter your name\"</span>).Width(<span class=\"hljs-number\">250</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Run it with <code>dotnet run</code> and you'll see this:</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/getting-started/hello-world.png\" alt=\"Hello World app running\"></p>\n<p>Here's what's happening:</p>\n<ul>\n<li><strong><code>ReactorApp.Run&#x3C;T></code></strong> launches a window and mounts your root component.</li>\n<li><strong><code>devtools: true</code></strong> enables the in-app dev menu and screenshot capture. In a\nreal app you'd normally guard this under <code>#if DEBUG</code> so release builds don't\nship the dev surface; we skip the conditional here for brevity.</li>\n<li><strong><a href=\"/hooks/\"><code>UseState</code></a></strong> returns the current value and a setter. When you call the\nsetter, Reactor re-renders the component with the new value.</li>\n<li><strong><a href=\"/layout/\"><code>VStack</code></a></strong> stacks children vertically. The number <code>16</code> is the pixel spacing.</li>\n<li><strong><code>Text(...).FontSize(24).Bold()</code></strong> is the fluent modifier pattern — every\nelement supports chainable modifiers for styling and layout.</li>\n</ul>\n<p>Type in the text box and the greeting updates instantly. There's no event\nwiring or property notification — just state in, UI out.</p>\n<h2 id=\"understanding-state\">Understanding State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#understanding-state\">#</a></h2>\n<p>Every interactive UI needs state. In Reactor, <code>UseState</code> is the primary hook for\nmanaging values that change over time.</p>\n<h3 id=\"counter-example\">Counter Example<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#counter-example\">#</a></h3>\n<p>Here's a counter that tracks a single number:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Launch with:</span>\n<span class=\"hljs-comment\">//   ReactorApp.Run&#x3C;CounterExample>(\"Counter\", width: 600, height: 400);</span>\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CounterExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>).FontSize(<span class=\"hljs-number\">20</span>).SemiBold(),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"- 1\"</span>, () => setCount(count - <span class=\"hljs-number\">1</span>)),\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () => setCount(<span class=\"hljs-number\">0</span>)),\n                Button(<span class=\"hljs-string\">\"+ 1\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/getting-started/counter.png\" alt=\"Counter with buttons\"></p>\n<p>Each call to <code>setCount</code> triggers a re-render. Reactor diffs the old and new\nelement trees and updates only the WinUI controls that actually changed.</p>\n<h3 id=\"multiple-state-values\">Multiple State Values<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#multiple-state-values\">#</a></h3>\n<p>Components can call <code>UseState</code> multiple times — each call tracks an independent\nvalue:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Launch with:</span>\n<span class=\"hljs-comment\">//   ReactorApp.Run&#x3C;MultipleStateExample>(\"Multiple State\", width: 600, height: 400);</span>\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MultipleStateExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (firstName, setFirstName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (lastName, setLastName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (fontSize, setFontSize) = UseState(<span class=\"hljs-number\">16.0</span>);\n\n        <span class=\"hljs-keyword\">var</span> fullName = <span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(firstName) &#x26;&#x26; <span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(lastName)\n            ? <span class=\"hljs-string\">\"Anonymous\"</span>\n            : <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{firstName}</span> <span class=\"hljs-subst\">{lastName}</span>\"</span>.Trim();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Hello, <span class=\"hljs-subst\">{fullName}</span>!\"</span>).FontSize(fontSize).Bold(),\n            TextBox(firstName, setFirstName, placeholderText: <span class=\"hljs-string\">\"First name\"</span>).Width(<span class=\"hljs-number\">200</span>),\n            TextBox(lastName, setLastName, placeholderText: <span class=\"hljs-string\">\"Last name\"</span>).Width(<span class=\"hljs-number\">200</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Font size:\"</span>),\n                Slider(fontSize, <span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">40</span>, setFontSize).Width(<span class=\"hljs-number\">200</span>),\n                TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{fontSize:F0}</span>px\"</span>)\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The <code>fullName</code> variable is derived from <code>firstName</code> and <code>lastName</code> on every\nrender. In Reactor, you don't need computed properties or bindings — plain C#\nexpressions work because <code>Render()</code> runs every time state changes.</p>\n<h2 id=\"layout-basics\">Layout Basics<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#layout-basics\">#</a></h2>\n<p>Reactor provides a small set of layout primitives that compose together:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Launch with:</span>\n<span class=\"hljs-comment\">//   ReactorApp.Run&#x3C;LayoutBasicsExample>(\"Layout\", width: 600, height: 400);</span>\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LayoutBasicsExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            Heading(<span class=\"hljs-string\">\"Layout Demo\"</span>),\n\n            SubHeading(<span class=\"hljs-string\">\"Horizontal Stack\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"One\"</span>),\n                Button(<span class=\"hljs-string\">\"Two\"</span>),\n                Button(<span class=\"hljs-string\">\"Three\"</span>)\n            ),\n\n            SubHeading(<span class=\"hljs-string\">\"Nested Layout\"</span>),\n            HStack(<span class=\"hljs-number\">16</span>,\n                VStack(<span class=\"hljs-number\">4</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Left Column\"</span>).Bold(),\n                    TextBlock(<span class=\"hljs-string\">\"Item A\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Item B\"</span>)\n                ),\n                VStack(<span class=\"hljs-number\">4</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Right Column\"</span>).Bold(),\n                    TextBlock(<span class=\"hljs-string\">\"Item X\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Item Y\"</span>)\n                )\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/getting-started/layout-basics.png\" alt=\"Layout demo\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Purpose</th></tr></thead><tbody><tr><td><code>VStack</code></td><td>Vertical stack (children top to bottom)</td></tr><tr><td><code>HStack</code></td><td>Horizontal stack (children left to right)</td></tr><tr><td><code>Grid</code></td><td>Row/column grid with proportional sizing</td></tr><tr><td><code>ScrollView</code></td><td>Scrollable wrapper for overflow content</td></tr><tr><td><code>Border</code></td><td>Container with background, corner radius, stroke</td></tr></tbody></table>\n<p>All layout elements accept an optional spacing parameter as their first\nargument: <code>VStack(12, child1, child2)</code> adds 12px between children.</p>\n<h2 id=\"building-a-todo-app\">Building a Todo App<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#building-a-todo-app\">#</a></h2>\n<p>Let's put these pieces together into something real. A todo app needs a list\nof items, a way to add new ones, and checkboxes to mark them done.</p>\n<p>First, define a simple record for items:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">TodoItem</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Text</span>, <span class=\"hljs-title\">bool</span> <span class=\"hljs-title\">Done</span>);\n</code></pre>\n<p>Now the full component:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor.Core;\n<span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">static</span> Microsoft.UI.Reactor.Factories;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Xaml;\n\nReactorApp.Run&#x3C;TodoApp>(<span class=\"hljs-string\">\"Todo App\"</span>, width: <span class=\"hljs-number\">550</span>, height: <span class=\"hljs-number\">600</span>, devtools: <span class=\"hljs-literal\">true</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TodoApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;TodoItem>\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Learn Reactor basics\"</span>, <span class=\"hljs-literal\">true</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Build a todo app\"</span>, <span class=\"hljs-literal\">false</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Explore hooks\"</span>, <span class=\"hljs-literal\">false</span>),\n        });\n        <span class=\"hljs-keyword\">var</span> (newText, setNewText) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> doneCount = items.Count(i => i.Done);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            Heading(<span class=\"hljs-string\">\"Todo List\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{doneCount}</span>/<span class=\"hljs-subst\">{items.Count}</span> completed\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n\n            <span class=\"hljs-comment\">// Input row</span>\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(newText, setNewText, placeholderText: <span class=\"hljs-string\">\"What needs to be done?\"</span>)\n                    .Width(<span class=\"hljs-number\">300</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(newText))\n                    {\n                        updateItems(list => [.. list, <span class=\"hljs-keyword\">new</span> TodoItem(newText.Trim(), <span class=\"hljs-literal\">false</span>)]);\n                        setNewText(<span class=\"hljs-string\">\"\"</span>);\n                    }\n                }).IsEnabled(!(<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(newText)))\n            ),\n\n            <span class=\"hljs-comment\">// Item list</span>\n            VStack(<span class=\"hljs-number\">4</span>,\n                items.Select((item, index) =>\n                    HStack(<span class=\"hljs-number\">8</span>,\n                        CheckBox(item.Done, done =>\n                            updateItems(list =>\n                            {\n                                <span class=\"hljs-keyword\">var</span> copy = <span class=\"hljs-keyword\">new</span> List&#x3C;TodoItem>(list);\n                                copy[index] = item <span class=\"hljs-keyword\">with</span> { Done = done };\n                                <span class=\"hljs-keyword\">return</span> copy;\n                            }),\n                            label: item.Text\n                        ),\n                        Button(<span class=\"hljs-string\">\"Remove\"</span>, () =>\n                            updateItems(list =>\n                            {\n                                <span class=\"hljs-keyword\">var</span> copy = <span class=\"hljs-keyword\">new</span> List&#x3C;TodoItem>(list);\n                                copy.RemoveAt(index);\n                                <span class=\"hljs-keyword\">return</span> copy;\n                            })\n                        )\n                    ).WithKey(<span class=\"hljs-string\">$\"todo-<span class=\"hljs-subst\">{index}</span>\"</span>)\n                ).ToArray()\n            ),\n\n            <span class=\"hljs-comment\">// Clear completed button</span>\n            When(doneCount > <span class=\"hljs-number\">0</span>, () =>\n                Button(<span class=\"hljs-string\">$\"Clear completed (<span class=\"hljs-subst\">{doneCount}</span>)\"</span>, () =>\n                    updateItems(list => list.Where(i => !i.Done).ToList())\n                )\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/todo-app/todo-app.png\" alt=\"Todo app\"></p>\n<p>Key patterns to notice:</p>\n<ul>\n<li><strong><a href=\"/hooks/\"><code>UseReducer</code></a></strong> is like <code>UseState</code> but the setter receives a function\n<code>Func&#x3C;T, T></code> — you transform the previous value into the next value. This\nis the right tool when your new state depends on the old state (like\nappending to a list).</li>\n<li><strong><code>items.Select(...).ToArray()</code></strong> maps data into elements. Reactor reconciles\nthe list efficiently using keys.</li>\n<li><strong><code>WithKey</code></strong> gives each item a stable identity so Reactor can reorder, add,\nand remove items without rebuilding the entire list.</li>\n<li><strong><code>When(condition, () => element)</code></strong> conditionally renders content without\nan if/else cluttering the tree.</li>\n</ul>\n<h2 id=\"building-a-calculator\">Building a Calculator<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#building-a-calculator\">#</a></h2>\n<p>Here's a more complex example that manages multiple pieces of related state:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Reactor.Core;\n<span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">static</span> Microsoft.UI.Reactor.Factories;\n<span class=\"hljs-keyword\">using</span> Microsoft.UI.Xaml;\n\nReactorApp.Run&#x3C;CalculatorApp>(<span class=\"hljs-string\">\"Calculator\"</span>, width: <span class=\"hljs-number\">380</span>, height: <span class=\"hljs-number\">500</span>, devtools: <span class=\"hljs-literal\">true</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CalculatorApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (display, setDisplay) = UseState(<span class=\"hljs-string\">\"0\"</span>);\n        <span class=\"hljs-keyword\">var</span> (operand, setOperand) = UseState&#x3C;<span class=\"hljs-built_in\">double</span>?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> (op, setOp) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> (resetNext, setResetNext) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">PressDigit</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> digit</span>)</span>\n        {\n            <span class=\"hljs-keyword\">if</span> (resetNext || display == <span class=\"hljs-string\">\"0\"</span>)\n            {\n                setDisplay(digit);\n                setResetNext(<span class=\"hljs-literal\">false</span>);\n            }\n            <span class=\"hljs-keyword\">else</span>\n            {\n                setDisplay(display + digit);\n            }\n        }\n\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">PressOp</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> nextOp</span>)</span>\n        {\n            <span class=\"hljs-keyword\">var</span> current = <span class=\"hljs-built_in\">double</span>.Parse(display);\n            <span class=\"hljs-keyword\">if</span> (operand.HasValue &#x26;&#x26; op != <span class=\"hljs-literal\">null</span>)\n            {\n                <span class=\"hljs-keyword\">var</span> result = Calculate(operand.Value, current, op);\n                setDisplay(FormatResult(result));\n                setOperand(result);\n            }\n            <span class=\"hljs-keyword\">else</span>\n            {\n                setOperand(current);\n            }\n            setOp(nextOp);\n            setResetNext(<span class=\"hljs-literal\">true</span>);\n        }\n\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">PressEquals</span>()</span>\n        {\n            <span class=\"hljs-keyword\">if</span> (operand.HasValue &#x26;&#x26; op != <span class=\"hljs-literal\">null</span>)\n            {\n                <span class=\"hljs-keyword\">var</span> current = <span class=\"hljs-built_in\">double</span>.Parse(display);\n                <span class=\"hljs-keyword\">var</span> result = Calculate(operand.Value, current, op);\n                setDisplay(FormatResult(result));\n                setOperand(<span class=\"hljs-literal\">null</span>);\n                setOp(<span class=\"hljs-literal\">null</span>);\n                setResetNext(<span class=\"hljs-literal\">true</span>);\n            }\n        }\n\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">PressClear</span>()</span>\n        {\n            setDisplay(<span class=\"hljs-string\">\"0\"</span>);\n            setOperand(<span class=\"hljs-literal\">null</span>);\n            setOp(<span class=\"hljs-literal\">null</span>);\n            setResetNext(<span class=\"hljs-literal\">false</span>);\n        }\n\n        <span class=\"hljs-function\">Element <span class=\"hljs-title\">NumButton</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> digit</span>)</span> =>\n            Button(digit, () => PressDigit(digit))\n                .Width(<span class=\"hljs-number\">60</span>).Height(<span class=\"hljs-number\">48</span>);\n\n        <span class=\"hljs-function\">Element <span class=\"hljs-title\">OpButton</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> label, <span class=\"hljs-built_in\">string</span> opCode</span>)</span> =>\n            Button(label, () => PressOp(opCode))\n                .Width(<span class=\"hljs-number\">60</span>).Height(<span class=\"hljs-number\">48</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n            <span class=\"hljs-comment\">// Display</span>\n            TextBlock(display)\n                .FontSize(<span class=\"hljs-number\">32</span>).Bold()\n                .HAlign(HorizontalAlignment.Right)\n                .Padding(horizontal: <span class=\"hljs-number\">12</span>, vertical: <span class=\"hljs-number\">8</span>),\n\n            <span class=\"hljs-comment\">// Button grid</span>\n            HStack(<span class=\"hljs-number\">4</span>, Button(<span class=\"hljs-string\">\"C\"</span>, PressClear).Width(<span class=\"hljs-number\">60</span>).Height(<span class=\"hljs-number\">48</span>),\n                       NumButton(<span class=\"hljs-string\">\"7\"</span>), NumButton(<span class=\"hljs-string\">\"8\"</span>), NumButton(<span class=\"hljs-string\">\"9\"</span>)),\n            HStack(<span class=\"hljs-number\">4</span>, OpButton(<span class=\"hljs-string\">\"/\"</span>, <span class=\"hljs-string\">\"/\"</span>),\n                       NumButton(<span class=\"hljs-string\">\"4\"</span>), NumButton(<span class=\"hljs-string\">\"5\"</span>), NumButton(<span class=\"hljs-string\">\"6\"</span>)),\n            HStack(<span class=\"hljs-number\">4</span>, OpButton(<span class=\"hljs-string\">\"*\"</span>, <span class=\"hljs-string\">\"*\"</span>),\n                       NumButton(<span class=\"hljs-string\">\"1\"</span>), NumButton(<span class=\"hljs-string\">\"2\"</span>), NumButton(<span class=\"hljs-string\">\"3\"</span>)),\n            HStack(<span class=\"hljs-number\">4</span>, OpButton(<span class=\"hljs-string\">\"-\"</span>, <span class=\"hljs-string\">\"-\"</span>),\n                       NumButton(<span class=\"hljs-string\">\"0\"</span>), OpButton(<span class=\"hljs-string\">\"+\"</span>, <span class=\"hljs-string\">\"+\"</span>),\n                       Button(<span class=\"hljs-string\">\"=\"</span>, PressEquals).Width(<span class=\"hljs-number\">60</span>).Height(<span class=\"hljs-number\">48</span>))\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">double</span> <span class=\"hljs-title\">Calculate</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">double</span> a, <span class=\"hljs-built_in\">double</span> b, <span class=\"hljs-built_in\">string</span> op</span>)</span> => op <span class=\"hljs-keyword\">switch</span>\n    {\n        <span class=\"hljs-string\">\"+\"</span> => a + b,\n        <span class=\"hljs-string\">\"-\"</span> => a - b,\n        <span class=\"hljs-string\">\"*\"</span> => a * b,\n        <span class=\"hljs-string\">\"/\"</span> => b != <span class=\"hljs-number\">0</span> ? a / b : <span class=\"hljs-number\">0</span>,\n        _ => b,\n    };\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">string</span> <span class=\"hljs-title\">FormatResult</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">double</span> <span class=\"hljs-keyword\">value</span></span>)</span> =>\n        <span class=\"hljs-keyword\">value</span> == Math.Floor(<span class=\"hljs-keyword\">value</span>) ? <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">value</span>:F0}</span>\"</span> : <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">value</span>:G10}</span>\"</span>;\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/calculator/calculator.png\" alt=\"Calculator\"></p>\n<p>This demonstrates how plain C# control flow (methods, switch expressions,\nlocal functions) works naturally inside Reactor components. There's no special\ncommand pattern needed — just call <code>setDisplay(...)</code> and the UI updates.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"hot-reload-with-dotnet-watch\">Hot reload with <code>dotnet watch</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hot-reload-with-dotnet-watch\">#</a></h3>\n<p>The single fastest authoring loop is <code>dotnet watch run</code> from the project\ndirectory. Reactor's <a href=\"/dev-tooling/\">dev tooling</a> hooks watch's\nfile-change events so a save in <code>App.cs</code> re-runs <code>Render()</code> without\nrestarting the window. State that lives in <code>UseState</code> is preserved\nacross the patch (the hook slot table survives), so a counter at 42\nstays at 42 after a layout tweak. State held in static fields is\n<em>not</em> preserved — keep startup state in <code>UseState</code> if you want it to\nsurvive hot reload.</p>\n<h3 id=\"first-event-first-state--the-minimum-interactive-app\">First event, first state — the minimum interactive app<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#first-event-first-state--the-minimum-interactive-app\">#</a></h3>\n<p>Every Reactor app eventually has the same two ingredients: an event\nhandler that calls a setter, and a value rendered from the setter's\nstate slot. The hello-world snippet above wires <code>setName</code> to\n<code>TextBox</code>'s change handler and reads <code>name</code> back in the\n<code>Text(\"Hello, ...\")</code> line — that round trip is the entire reactivity\ncontract. Once it feels routine, every other <a href=\"/hooks/\">hook</a> is just\na specialization (<code>UseReducer</code> for derived updates, <code>UseEffect</code> for\nside effects, <code>UseRef</code> for non-rendering bookkeeping).</p>\n<h3 id=\"running-with-devtools\">Running with devtools<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#running-with-devtools\">#</a></h3>\n<p>Launch with <code>dotnet run -c Debug</code> and Reactor mounts the in-app dev\nmenu (Ctrl+Shift+D by default). The reconcile-highlight overlay flashes\non every commit, the layout-cost overlay attributes per-component time,\nand the <a href=\"/dev-tooling/\">dev tooling</a> page covers the full menu. The\noverlays are no-cost in Release builds — the dev menu compiles out\nunder <code>#if DEBUG</code>.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"editing-bin-artifacts-to-see-your-change\">Editing <code>bin/</code> artifacts to \"see your change\"<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#editing-bin-artifacts-to-see-your-change\">#</a></h3>\n<p>Reactor doesn't watch the build output. Edit the source files under\nyour project (<code>App.cs</code>, components in subdirectories) and rebuild —\neither via <code>dotnet run</code> or under <code>dotnet watch run</code>. The <code>bin/</code> tree\nis regenerated on every build; any hand-edit there is silently\noverwritten.</p>\n<h3 id=\"trying-to-use-reactor-inside-a-winui-page-or-usercontrol\">Trying to use Reactor inside a WinUI <code>Page</code> or <code>UserControl</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#trying-to-use-reactor-inside-a-winui-page-or-usercontrol\">#</a></h3>\n<p>Reactor expects to own the window. <code>ReactorApp.Run&#x3C;T></code> opens a\n<code>Window</code>, mounts your component tree directly, and drives the\nreconciler from that root. Mounting a Reactor component inside a\nWinUI <code>Page</code> (via <code>xmlns:reactor=...</code> markup) does not work — there\nis no XAML loader for Reactor elements. If you need Reactor inside an\nexisting WinUI/WinForms host, see <a href=\"/winforms-interop/\">WinForms interop</a>\nfor <code>XamlIslandControl</code> or use <code>ReactorHostControl</code> from\n<a href=\"/components/\">components</a> for the WinUI host case.</p>\n<h3 id=\"reaching-for-inotifypropertychanged-out-of-habit\">Reaching for <code>INotifyPropertyChanged</code> out of habit<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-inotifypropertychanged-out-of-habit\">#</a></h3>\n<p>XAML developers often try to back state with a view model. In Reactor,\nstate IS the binding — <code>UseState</code> returns <code>(value, setter)</code> and the\nsetter triggers the re-render. You can still bridge an existing\n<code>INotifyPropertyChanged</code> source with <code>UseObservable</code> (see\n<a href=\"/advanced/\">advanced</a>), but for new screens, hooks are the shorter\npath. The <a href=\"/xaml-developers/\">Reactor for XAML developers</a> page maps\neach XAML idiom to its Reactor equivalent.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Think in functions, not objects.</strong> Your <code>Render()</code> method is a pure function\nfrom state to UI. Every time state changes, it runs again from the top. Don't\ntry to mutate the UI imperatively.</p>\n<p><strong>Keep state as high as it needs to be, but no higher.</strong> If only one component\nuses a value, <code>UseState</code> in that component. If siblings need to share state,\nlift it to their parent.</p>\n<p><strong>Use records for data.</strong> C# records give you immutable data with value\nequality for free. Reactor uses this for efficient memoization — if your props\nhaven't changed structurally, the component skips re-rendering.</p>\n<p><strong>Prefer composition over inheritance.</strong> Build small components that each do\none thing, then compose them in parent components. You'll rarely need more than\n<code>Component</code> or <code>Component&#x3C;TProps></code> as a base class.</p>\n<p><strong>Fluent modifiers are your friend.</strong> Instead of wrapping elements in layout\ncontainers for simple styling, chain modifiers: <code>Text(\"hi\").Margin(8).Bold()</code>\nreads cleanly and avoids unnecessary nesting.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — Set up hot reload and preview mode for a faster development loop</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Break your app into reusable pieces with <code>Component&#x3C;TProps></code> and typed record props</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Deep dive into UseState, UseReducer, UseEffect, UseMemo, and more</li>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — Master VStack, HStack, Grid, and responsive patterns</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — Use <code>UseEffect</code> for side effects like timers, file I/O, and API calls</li>\n</ul>",
      "text": "A Microsoft.UI.Reactor (Reactor) app is a tree of components driven by hooks , hosted in a WinUI window that the framework opens and manages for you. You write one C# file, call ReactorApp.Run&#x3C;T> , and your component's Render() method returns the element tree that becomes the native control tree. State lives in UseState and friends; every setter invocation re-runs Render() ; the reconciler diffs the new tree against the previous one and patches the WinUI controls in place. This page is the bootstrap walkthrough — installing the framework, scaffolding a project, and growing from hello-world to a todo list and a calculator. By the end you will have run code, seen a screenshot of each step, and recognized the layout primitives and hooks that the rest of the docset elaborates. Getting Started with Reactor # Prerequisites: .NET 10+ and the Windows App SDK. Heads up — no signed NuGet yet. Reactor doesn't ship a signed NuGet package today, so you build the framework, the mur CLI, and the project template from source. bootstrap.ps1 automates all of that — one command, ~3 minutes per machine. The signed distribution is tracked in spec 022 . Reactor is a declarative UI framework for building native Windows apps in pure C#. No XAML, no data binding, no view models. You describe your UI as a function of state and Reactor keeps the screen in sync. Setup (one-time) # git clone https://github.com/microsoft/microsoft-ui-reactor.git cd microsoft-ui-reactor ./bootstrap.ps1 That's it. bootstrap.ps1 packs and installs mur as a dotnet tool global install (so it's on PATH cross-shell with no manual $env:Path edits), runs mur pack-local to produce local-nupkgs/Microsoft.UI.Reactor.0.0.0-local.nupkg and the matching ProjectTemplates nupkg, registers the dotnet new reactorapp template, and drops the Reactor agent plugin under ~/.claude/plugins/reactor (symlink when allowed, copy otherwise). When it finishes you can immediately run: dotnet new reactorapp -n MyApp cd MyApp dotnet run After git pull # The framework changes — your local NuGet snapshot and dotnet new template do not, unless you repack them. Two options: mur upgrade # repacks the framework + templates and refreshes plugin ./bootstrap.ps1 # same, plus updates the `mur` global tool itself mur upgrade is the lightweight path. Re-run bootstrap.ps1 when you want to pick up CLI changes (a mur process can't replace its own binary mid-run). Verify the install # mur doctor Lists every dependency the rest of this guide assumes — .NET 10+ SDK, mur on PATH, current local-nupkgs/ feed, the reactorapp template registration, and the optional Claude plugin. Each line is PASS / WARN / FAIL with a one-line remediation for anything broken. What this gets you. A globally-resolvable mur (via ~/.dotnet/tools ), a local NuGet feed at &#x3C;repo>/local-nupkgs/ that apps in any folder can consume via &#x3C;PackageReference Include=\"Microsoft.UI.Reactor\" Version=\"0.0.0-local\" /> , and an agent plugin so AI assistants generate against the real factories ( mur --skill / mur --api print the same content). Run mur upgrade whenever you pull new framework changes. Already have a signed package? Skip the bootstrap. Reference the published Microsoft.UI.Reactor package directly and run the consumer-side install-skill-kit.ps1 shipped in the release archive (covered in spec 022 §4.4). Until that release ships, the bootstrap above is the supported path. Manual setup # If you'd rather not run bootstrap.ps1 , here is exactly what it does, step by step. Every command is a stock dotnet or git invocation — no Reactor tooling required until the very last step, where you build mur from source and install it as a global tool. Each block below corresponds to a numbered phase in bootstrap.ps1 , so the script is a useful cross-reference if anything goes wrong. # Step What it produces 1 dotnet --list-sdks Confirms .NET 10+ is installed 2 git clone + cd Local source checkout 3 dotnet pack src/Reactor.Cli Microsoft.UI.Reactor.Cli.&#x3C;ver>.nupkg in local-nupkgs/ 4 dotnet tool install -g mur resolvable cross-shell from ~/.dotnet/tools 5 mur pack-local Framework + ProjectTemplates 0.0.0-local nupkgs 6 dotnet new uninstall + install dotnet new reactorapp template registered 7 Symlink/copy plugins/reactor Reactor agent kit under ~/.claude/plugins/reactor (optional) 8 mur doctor Verification that 1–7 all stuck 1. Confirm prerequisites. Reactor requires .NET 10 or later. The Windows App SDK is pulled in transitively when the framework builds. dotnet --list-sdks # Expect at least one entry starting with \"10.\" 2. Clone and enter the repo. Everything below assumes the working directory is the repo root. git clone https://github.com/microsoft/microsoft-ui-reactor.git cd microsoft-ui-reactor 3. Pack mur as a global-tool nupkg. Reactor.Cli.csproj sets PackAsTool=true , so dotnet pack produces a tool package. The -p:Platform flag matters because the build step runs the SignaturesGen apphost to refresh skills/reactor.api.txt ; that apphost must match the host architecture. $hostArch = if ($env:PROCESSOR_ARCHITECTURE -eq 'ARM64') { 'ARM64' } else { 'x64' } dotnet pack src/Reactor.Cli/Reactor.Cli.csproj ` -c Release ` \"-p:Platform=$hostArch\" ` -o local-nupkgs ` --nologo # Produces local-nupkgs/Microsoft.UI.Reactor.Cli.&#x3C;version>.nupkg 4. Install mur as a dotnet global tool. This is what puts mur on PATH cross-shell with no $env:Path edits. If a previous install exists, use update instead of install . dotnet tool install -g ` --add-source ./local-nupkgs ` Microsoft.UI.Reactor.Cli ` --no-cache --ignore-failed-sources # If `mur` was already installed: # dotnet tool update -g --add-source ./local-nupkgs Microsoft.UI.Reactor.Cli --no-cache --ignore-failed-sources dotnet tool install -g adds ~/.dotnet/tools to the user PATH, which the current shell does not automatically inherit. For this session only, prepend it manually so the next step finds mur : $env:Path = \"$env:USERPROFILE\\.dotnet\\tools;$env:Path\" New PowerShell windows pick up the user-PATH change on their own. 5. Pack the framework and project templates. This is what produces the two 0.0.0-local nupkgs that the dotnet new reactorapp template references. mur pack-local # Produces: # local-nupkgs/Microsoft.UI.Reactor.0.0.0-local.nupkg # local-nupkgs/Microsoft.UI.Reactor.ProjectTemplates.0.0.0-local.nupkg If you'd rather not depend on the freshly-installed mur , you can invoke the source project directly: dotnet run --project src/Reactor.Cli/Reactor.Cli.csproj ` -c Release \"-p:Platform=$hostArch\" -- pack-local 6. Install the dotnet new reactorapp template. The template engine caches by package id, so a same-version repack can lose to the cached copy. Always uninstall first. dotnet new uninstall Microsoft.UI.Reactor.ProjectTemplates 2>$null dotnet new install local-nupkgs/Microsoft.UI.Reactor.ProjectTemplates.0.0.0-local.nupkg 7. (Optional) Install the Reactor agent plugin. If you use Claude Code or another agent and want it to author Reactor code with the right factories, drop the in-repo plugin folder into your agent's plugin path. A symlink is preferred so edits in the checkout are immediately visible; a copy works when you can't create symlinks (Developer Mode off + non-admin shell). $pluginSrc = (Resolve-Path \"plugins/reactor\").Path $pluginDst = \"$env:USERPROFILE\\.claude\\plugins\\reactor\" New-Item -ItemType Directory -Path (Split-Path $pluginDst) -Force | Out-Null if (Test-Path $pluginDst) { Remove-Item $pluginDst -Recurse -Force } try { New-Item -ItemType SymbolicLink -Path $pluginDst -Target $pluginSrc -ErrorAction Stop | Out-Null } catch { Copy-Item $pluginSrc $pluginDst -Recurse -Force } For Copilot CLI or other agents, follow that tool's plugin-install path and point it at &#x3C;repo>/plugins/reactor . 8. Verify. mur doctor performs the same checks the bootstrap script's final stage relies on — SDK version, mur resolvability, both 0.0.0-local nupkgs present, template registered, and plugin installed. mur doctor Refreshing after git pull # Without the bootstrap script, repeat steps 5 and 6 after every pull — the framework nupkg and the template both need to be regenerated against the new source. Repeat steps 3 and 4 only when src/Reactor.Cli/ itself changes (a running mur process cannot replace its own binary, so the install must happen from a shell that isn't already running mur ). Why a global tool instead of just running from bin/&#x3C;arch>/ ? Both work. The repo's CLI csproj still mirrors mur.exe to bin/&#x3C;arch>/ after every build for the legacy PATH layout. The global-tool install is just the friendliest default — it puts mur on PATH cross-shell, cross-CWD, with no arch-aware PATH munging, and dotnet tool update -g becomes the upgrade verb. Caveat: The bootstrap is the supported developer path only until the signed public NuGet ships under spec 022. If you skip bootstrap.ps1 and try dotnet new reactorapp anyway, the scaffolder fails with NU1101: Unable to find package Microsoft.UI.Reactor.ProjectTemplates because nothing has produced local-nupkgs/ yet — dotnet looks at the configured feeds and the package isn't on nuget.org. The fix is to re-run bootstrap.ps1 (or mur upgrade ) after every git pull ; the snapshot is regenerated from the current branch, not cached. The template installer caches by package id, so a same-version repack can lose to the cached copy — mur upgrade handles this by running dotnet new uninstall first. Creating a Project # With the template installed, scaffold a new app from anywhere on disk: dotnet new reactorapp -n MyApp cd MyApp dotnet run The template wires up the Microsoft.UI.Reactor package reference, the WinUI 3 target framework, and a working App.cs that mounts a single Reactor component. No App.xaml , no MainWindow.xaml.cs — just one C# file. Why a custom template? A dotnet new console does not produce a WinUI app — it builds a console target with no UI thread, no OutputType=WinExe , no WindowsAppSDK reference, and no [STAThread] entry point. reactorapp sets all of those plus the Reactor package reference and a backdrop-aware root component, so you get a window on first dotnet run instead of a console-host stub. Your First App # The template's App.cs is the canonical hello-world. Replace its contents with the snippet below to match the rest of this guide (the template defaults to a slightly richer starter; the simpler form is easier to walk through): using Microsoft.UI.Reactor; using Microsoft.UI.Reactor.Core; using static Microsoft.UI.Reactor.Factories; using Microsoft.UI.Xaml; ReactorApp.Run&#x3C;GettingStartedApp>( \"Getting Started\" , width: 600 , height: 400 , devtools: true ); class GettingStartedApp : Component { public override Element Render () { var (name, setName) = UseState( \"World\" ); return VStack( 16 , TextBlock( $\"Hello, {name} !\" ).FontSize( 24 ).Bold(), TextBox(name, setName, placeholderText: \"Enter your name\" ).Width( 250 ) ).Padding( 24 ); } } Run it with dotnet run and you'll see this: Here's what's happening: ReactorApp.Run&#x3C;T> launches a window and mounts your root component. devtools: true enables the in-app dev menu and screenshot capture. In a real app you'd normally guard this under #if DEBUG so release builds don't ship the dev surface; we skip the conditional here for brevity. UseState returns the current value and a setter. When you call the setter, Reactor re-renders the component with the new value. VStack stacks children vertically. The number 16 is the pixel spacing. Text(...).FontSize(24).Bold() is the fluent modifier pattern — every element supports chainable modifiers for styling and layout. Type in the text box and the greeting updates instantly. There's no event wiring or property notification — just state in, UI out. Understanding State # Every interactive UI needs state. In Reactor, UseState is the primary hook for managing values that change over time. Counter Example # Here's a counter that tracks a single number: // Launch with: // ReactorApp.Run&#x3C;CounterExample>(\"Counter\", width: 600, height: 400); class CounterExample : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return VStack( 12 , TextBlock( $\"Count: {count} \" ).FontSize( 20 ).SemiBold(), HStack( 8 , Button( \"- 1\" , () => setCount(count - 1 )), Button( \"Reset\" , () => setCount( 0 )), Button( \"+ 1\" , () => setCount(count + 1 )) ) ).Padding( 24 ); } } Each call to setCount triggers a re-render. Reactor diffs the old and new element trees and updates only the WinUI controls that actually changed. Multiple State Values # Components can call UseState multiple times — each call tracks an independent value: // Launch with: // ReactorApp.Run&#x3C;MultipleStateExample>(\"Multiple State\", width: 600, height: 400); class MultipleStateExample : Component { public override Element Render () { var (firstName, setFirstName) = UseState( \"\" ); var (lastName, setLastName) = UseState( \"\" ); var (fontSize, setFontSize) = UseState( 16.0 ); var fullName = string .IsNullOrWhiteSpace(firstName) &#x26;&#x26; string .IsNullOrWhiteSpace(lastName) ? \"Anonymous\" : $\" {firstName} {lastName} \" .Trim(); return VStack( 12 , TextBlock( $\"Hello, {fullName} !\" ).FontSize(fontSize).Bold(), TextBox(firstName, setFirstName, placeholderText: \"First name\" ).Width( 200 ), TextBox(lastName, setLastName, placeholderText: \"Last name\" ).Width( 200 ), HStack( 8 , TextBlock( \"Font size:\" ), Slider(fontSize, 10 , 40 , setFontSize).Width( 200 ), TextBlock( $\" {fontSize:F0} px\" ) ) ).Padding( 24 ); } } The fullName variable is derived from firstName and lastName on every render. In Reactor, you don't need computed properties or bindings — plain C# expressions work because Render() runs every time state changes. Layout Basics # Reactor provides a small set of layout primitives that compose together: // Launch with: // ReactorApp.Run&#x3C;LayoutBasicsExample>(\"Layout\", width: 600, height: 400); class LayoutBasicsExample : Component { public override Element Render () { return VStack( 16 , Heading( \"Layout Demo\" ), SubHeading( \"Horizontal Stack\" ), HStack( 8 , Button( \"One\" ), Button( \"Two\" ), Button( \"Three\" ) ), SubHeading( \"Nested Layout\" ), HStack( 16 , VStack( 4 , TextBlock( \"Left Column\" ).Bold(), TextBlock( \"Item A\" ), TextBlock( \"Item B\" ) ), VStack( 4 , TextBlock( \"Right Column\" ).Bold(), TextBlock( \"Item X\" ), TextBlock( \"Item Y\" ) ) ) ).Padding( 24 ); } } Element Purpose VStack Vertical stack (children top to bottom) HStack Horizontal stack (children left to right) Grid Row/column grid with proportional sizing ScrollView Scrollable wrapper for overflow content Border Container with background, corner radius, stroke All layout elements accept an optional spacing parameter as their first argument: VStack(12, child1, child2) adds 12px between children. Building a Todo App # Let's put these pieces together into something real. A todo app needs a list of items, a way to add new ones, and checkboxes to mark them done. First, define a simple record for items: record TodoItem ( string Text , bool Done ); Now the full component: using Microsoft.UI.Reactor; using Microsoft.UI.Reactor.Core; using static Microsoft.UI.Reactor.Factories; using Microsoft.UI.Xaml; ReactorApp.Run&#x3C;TodoApp>( \"Todo App\" , width: 550 , height: 600 , devtools: true ); class TodoApp : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C;TodoItem> { new ( \"Learn Reactor basics\" , true ), new ( \"Build a todo app\" , false ), new ( \"Explore hooks\" , false ), }); var (newText, setNewText) = UseState( \"\" ); var doneCount = items.Count(i => i.Done); return VStack( 16 , Heading( \"Todo List\" ), TextBlock( $\" {doneCount} / {items.Count} completed\" ).Opacity( 0.6 ), // Input row HStack( 8 , TextBox(newText, setNewText, placeholderText: \"What needs to be done?\" ) .Width( 300 ), Button( \"Add\" , () => { if (! string .IsNullOrWhiteSpace(newText)) { updateItems(list => [.. list, new TodoItem(newText.Trim(), false )]); setNewText( \"\" ); } }).IsEnabled(!( string .IsNullOrWhiteSpace(newText))) ), // Item list VStack( 4 , items.Select((item, index) => HStack( 8 , CheckBox(item.Done, done => updateItems(list => { var copy = new List&#x3C;TodoItem>(list); copy[index] = item with { Done = done }; return copy; }), label: item.Text ), Button( \"Remove\" , () => updateItems(list => { var copy = new List&#x3C;TodoItem>(list); copy.RemoveAt(index); return copy; }) ) ).WithKey( $\"todo- {index} \" ) ).ToArray() ), // Clear completed button When(doneCount > 0 , () => Button( $\"Clear completed ( {doneCount} )\" , () => updateItems(list => list.Where(i => !i.Done).ToList()) ) ) ).Padding( 24 ); } } Key patterns to notice: UseReducer is like UseState but the setter receives a function Func&#x3C;T, T> — you transform the previous value into the next value. This is the right tool when your new state depends on the old state (like appending to a list). items.Select(...).ToArray() maps data into elements. Reactor reconciles the list efficiently using keys. WithKey gives each item a stable identity so Reactor can reorder, add, and remove items without rebuilding the entire list. When(condition, () => element) conditionally renders content without an if/else cluttering the tree. Building a Calculator # Here's a more complex example that manages multiple pieces of related state: using Microsoft.UI.Reactor; using Microsoft.UI.Reactor.Core; using static Microsoft.UI.Reactor.Factories; using Microsoft.UI.Xaml; ReactorApp.Run&#x3C;CalculatorApp>( \"Calculator\" , width: 380 , height: 500 , devtools: true ); class CalculatorApp : Component { public override Element Render () { var (display, setDisplay) = UseState( \"0\" ); var (operand, setOperand) = UseState&#x3C; double ?>( null ); var (op, setOp) = UseState&#x3C; string ?>( null ); var (resetNext, setResetNext) = UseState( false ); void PressDigit ( string digit ) { if (resetNext || display == \"0\" ) { setDisplay(digit); setResetNext( false ); } else { setDisplay(display + digit); } } void PressOp ( string nextOp ) { var current = double .Parse(display); if (operand.HasValue &#x26;&#x26; op != null ) { var result = Calculate(operand.Value, current, op); setDisplay(FormatResult(result)); setOperand(result); } else { setOperand(current); } setOp(nextOp); setResetNext( true ); } void PressEquals () { if (operand.HasValue &#x26;&#x26; op != null ) { var current = double .Parse(display); var result = Calculate(operand.Value, current, op); setDisplay(FormatResult(result)); setOperand( null ); setOp( null ); setResetNext( true ); } } void PressClear () { setDisplay( \"0\" ); setOperand( null ); setOp( null ); setResetNext( false ); } Element NumButton ( string digit ) => Button(digit, () => PressDigit(digit)) .Width( 60 ).Height( 48 ); Element OpButton ( string label, string opCode ) => Button(label, () => PressOp(opCode)) .Width( 60 ).Height( 48 ); return VStack( 4 , // Display TextBlock(display) .FontSize( 32 ).Bold() .HAlign(HorizontalAlignment.Right) .Padding(horizontal: 12 , vertical: 8 ), // Button grid HStack( 4 , Button( \"C\" , PressClear).Width( 60 ).Height( 48 ), NumButton( \"7\" ), NumButton( \"8\" ), NumButton( \"9\" )), HStack( 4 , OpButton( \"/\" , \"/\" ), NumButton( \"4\" ), NumButton( \"5\" ), NumButton( \"6\" )), HStack( 4 , OpButton( \"*\" , \"*\" ), NumButton( \"1\" ), NumButton( \"2\" ), NumButton( \"3\" )), HStack( 4 , OpButton( \"-\" , \"-\" ), NumButton( \"0\" ), OpButton( \"+\" , \"+\" ), Button( \"=\" , PressEquals).Width( 60 ).Height( 48 )) ).Padding( 16 ); } static double Calculate ( double a, double b, string op ) => op switch { \"+\" => a + b, \"-\" => a - b, \"*\" => a * b, \"/\" => b != 0 ? a / b : 0 , _ => b, }; static string FormatResult ( double value ) => value == Math.Floor( value ) ? $\" { value :F0} \" : $\" { value :G10} \" ; } This demonstrates how plain C# control flow (methods, switch expressions, local functions) works naturally inside Reactor components. There's no special command pattern needed — just call setDisplay(...) and the UI updates. Patterns # Hot reload with dotnet watch # The single fastest authoring loop is dotnet watch run from the project directory. Reactor's dev tooling hooks watch's file-change events so a save in App.cs re-runs Render() without restarting the window. State that lives in UseState is preserved across the patch (the hook slot table survives), so a counter at 42 stays at 42 after a layout tweak. State held in static fields is not preserved — keep startup state in UseState if you want it to survive hot reload. First event, first state — the minimum interactive app # Every Reactor app eventually has the same two ingredients: an event handler that calls a setter, and a value rendered from the setter's state slot. The hello-world snippet above wires setName to TextBox 's change handler and reads name back in the Text(\"Hello, ...\") line — that round trip is the entire reactivity contract. Once it feels routine, every other hook is just a specialization ( UseReducer for derived updates, UseEffect for side effects, UseRef for non-rendering bookkeeping). Running with devtools # Launch with dotnet run -c Debug and Reactor mounts the in-app dev menu (Ctrl+Shift+D by default). The reconcile-highlight overlay flashes on every commit, the layout-cost overlay attributes per-component time, and the dev tooling page covers the full menu. The overlays are no-cost in Release builds — the dev menu compiles out under #if DEBUG . Common Mistakes # Editing bin/ artifacts to \"see your change\" # Reactor doesn't watch the build output. Edit the source files under your project ( App.cs , components in subdirectories) and rebuild — either via dotnet run or under dotnet watch run . The bin/ tree is regenerated on every build; any hand-edit there is silently overwritten. Trying to use Reactor inside a WinUI Page or UserControl # Reactor expects to own the window. ReactorApp.Run&#x3C;T> opens a Window , mounts your component tree directly, and drives the reconciler from that root. Mounting a Reactor component inside a WinUI Page (via xmlns:reactor=... markup) does not work — there is no XAML loader for Reactor elements. If you need Reactor inside an existing WinUI/WinForms host, see WinForms interop for XamlIslandControl or use ReactorHostControl from components for the WinUI host case. Reaching for INotifyPropertyChanged out of habit # XAML developers often try to back state with a view model. In Reactor, state IS the binding — UseState returns (value, setter) and the setter triggers the re-render. You can still bridge an existing INotifyPropertyChanged source with UseObservable (see advanced ), but for new screens, hooks are the shorter path. The Reactor for XAML developers page maps each XAML idiom to its Reactor equivalent. Tips # Think in functions, not objects. Your Render() method is a pure function from state to UI. Every time state changes, it runs again from the top. Don't try to mutate the UI imperatively. Keep state as high as it needs to be, but no higher. If only one component uses a value, UseState in that component. If siblings need to share state, lift it to their parent. Use records for data. C# records give you immutable data with value equality for free. Reactor uses this for efficient memoization — if your props haven't changed structurally, the component skips re-rendering. Prefer composition over inheritance. Build small components that each do one thing, then compose them in parent components. You'll rarely need more than Component or Component&#x3C;TProps> as a base class. Fluent modifiers are your friend. Instead of wrapping elements in layout containers for simple styling, chain modifiers: Text(\"hi\").Margin(8).Bold() reads cleanly and avoids unnecessary nesting. Next Steps # Dev Tooling — Set up hot reload and preview mode for a faster development loop Components — Break your app into reusable pieces with Component&#x3C;TProps> and typed record props Hooks — Deep dive into UseState, UseReducer, UseEffect, UseMemo, and more Layout — Master VStack, HStack, Grid, and responsive patterns Effects and Lifecycle — Use UseEffect for side effects like timers, file I/O, and API calls",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/getting-started.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/getting-started.md",
      "headings": [
        {
          "depth": 2,
          "id": "setup-one-time",
          "text": "Setup (one-time)"
        },
        {
          "depth": 3,
          "id": "after-git-pull",
          "text": "After git pull"
        },
        {
          "depth": 3,
          "id": "verify-the-install",
          "text": "Verify the install"
        },
        {
          "depth": 3,
          "id": "manual-setup",
          "text": "Manual setup"
        },
        {
          "depth": 2,
          "id": "creating-a-project",
          "text": "Creating a Project"
        },
        {
          "depth": 2,
          "id": "your-first-app",
          "text": "Your First App"
        },
        {
          "depth": 2,
          "id": "understanding-state",
          "text": "Understanding State"
        },
        {
          "depth": 3,
          "id": "counter-example",
          "text": "Counter Example"
        },
        {
          "depth": 3,
          "id": "multiple-state-values",
          "text": "Multiple State Values"
        },
        {
          "depth": 2,
          "id": "layout-basics",
          "text": "Layout Basics"
        },
        {
          "depth": 2,
          "id": "building-a-todo-app",
          "text": "Building a Todo App"
        },
        {
          "depth": 2,
          "id": "building-a-calculator",
          "text": "Building a Calculator"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "hot-reload-with-dotnet-watch",
          "text": "Hot reload with dotnet watch"
        },
        {
          "depth": 3,
          "id": "first-event-first-state--the-minimum-interactive-app",
          "text": "First event, first state — the minimum interactive app"
        },
        {
          "depth": 3,
          "id": "running-with-devtools",
          "text": "Running with devtools"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "editing-bin-artifacts-to-see-your-change",
          "text": "Editing bin/ artifacts to \"see your change\""
        },
        {
          "depth": 3,
          "id": "trying-to-use-reactor-inside-a-winui-page-or-usercontrol",
          "text": "Trying to use Reactor inside a WinUI Page or UserControl"
        },
        {
          "depth": 3,
          "id": "reaching-for-inotifypropertychanged-out-of-habit",
          "text": "Reaching for INotifyPropertyChanged out of habit"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Thinking in Reactor",
      "path": "thinking-in-reactor.md",
      "slug": "/thinking-in-reactor/",
      "html": "<h1 id=\"thinking-in-reactor\">Thinking in Reactor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#thinking-in-reactor\">#</a></h1>\n<p>Coming soon — this page is the mental-model essay that frames Microsoft.UI.Reactor (Reactor)\nfor new readers: UI as a function of state, why there is no XAML, when\nto reach for hooks, and how components compose. See\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/_pipeline/ai-author-skill.md\">the AI Author Skill</a> for the planned\nsurface and the spec-041 §7.1 layout.</p>\n<p>In the meantime, <a href=\"/getting-started/\">Getting Started</a> covers the same\nground from the hands-on direction, and <a href=\"/components/\">Components</a> and\n<a href=\"/hooks/\">Hooks</a> cover the building blocks.</p>",
      "text": "Thinking in Reactor # Coming soon — this page is the mental-model essay that frames Microsoft.UI.Reactor (Reactor) for new readers: UI as a function of state, why there is no XAML, when to reach for hooks, and how components compose. See the AI Author Skill for the planned surface and the spec-041 §7.1 layout. In the meantime, Getting Started covers the same ground from the hands-on direction, and Components and Hooks cover the building blocks.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/thinking-in-reactor.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/thinking-in-reactor.md",
      "headings": []
    },
    {
      "title": "Reactor for XAML Developers",
      "path": "xaml-developers.md",
      "slug": "/xaml-developers/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s render-from-state model has a clean 1:1 translation table to XAML\nthat an experienced XAML developer can hold in their head. Where XAML\ndescribes <em>bindings</em> between view-model properties and control DPs and lets\nthe binding engine pull values on change-notification, Reactor describes the\n<em>current UI</em> directly and re-evaluates the entire component when state\nchanges. The control tree is the same WinUI control tree at runtime — only\nthe authoring surface differs. This page is the translation key: every\nXAML idiom you reach for (DataContext, Binding modes, DataTemplate,\nDependencyProperty, code-behind, ICommand, Frame.Navigate) maps to a\nspecific Reactor shape, and the body below walks each one in order.\nCompanion essay <a href=\"/reactor-vs-xaml/\"><code>reactor-vs-xaml</code></a> covers the\narchitectural <em>why</em> — read this page for the recipes, that one for the\nphilosophy.</p>\n<h1 id=\"reactor-for-xaml-developers\">Reactor for XAML Developers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactor-for-xaml-developers\">#</a></h1>\n<p>If you already know XAML, Reactor is not a different Windows UI stack. It still\nrenders real WinUI controls. The shift is in <strong>how</strong> you describe the UI: instead\nof splitting a page across XAML, bindings, converters, and code-behind, you\nreturn the UI directly from C# and let Reactor keep the native control tree in\nsync.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/xaml-developers/mental-model-shift.svg\" alt=\"From XAML page to Reactor component — mental-model shift\"></p>\n<h2 id=\"the-mental-model-shift\">The Mental Model Shift<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-mental-model-shift\">#</a></h2>\n<p>Think of Reactor as \"WinUI controls, but expressed like a function of state.\"</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>In XAML</th><th>In Reactor</th></tr></thead><tbody><tr><td><code>Page</code>, <code>UserControl</code>, <code>Window</code> markup</td><td>A <code>Component</code> with <code>Render()</code></td></tr><tr><td><code>{Binding Name}</code></td><td>Plain C# variable usage: <code>TextBlock(name)</code></td></tr><tr><td><code>Mode=TwoWay</code></td><td>Controlled input: <code>TextBox(name, setName)</code></td></tr><tr><td><code>DataContext</code></td><td>Local hook state, typed props, or <a href=\"/context/\">context</a></td></tr><tr><td><code>ICommand</code></td><td>Lambdas, methods, or <a href=\"/commanding/\">commands</a></td></tr><tr><td><code>StackPanel</code></td><td><a href=\"/layout/\"><code>VStack</code></a> or <a href=\"/layout/\"><code>HStack</code></a></td></tr><tr><td><code>Grid.Row</code>, <code>Grid.Column</code></td><td><code>.Grid(row: ..., column: ...)</code></td></tr><tr><td><code>StaticResource</code> / <code>ThemeResource</code></td><td><a href=\"/styling/\"><code>Theme</code></a> tokens and fluent modifiers</td></tr><tr><td><code>Frame.Navigate(...)</code></td><td><a href=\"/navigation/\"><code>UseNavigation</code></a> + <code>NavigationHost</code></td></tr></tbody></table>\n<p>The important difference is that Reactor does not ask you to describe <em>bindings</em>.\nIt asks you to describe the <strong>current UI</strong>. When the state changes, <code>Render()</code>\nruns again and Reactor updates only the native WinUI controls that changed.</p>\n<h2 id=\"rewriting-a-familiar-form\">Rewriting a Familiar Form<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rewriting-a-familiar-form\">#</a></h2>\n<p>Here is the kind of XAML page many WinUI developers start with:</p>\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">StackPanel</span> <span class=\"hljs-attr\">Spacing</span>=<span class=\"hljs-string\">\"12\"</span> <span class=\"hljs-attr\">Padding</span>=<span class=\"hljs-string\">\"24\"</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBlock</span> <span class=\"hljs-attr\">Text</span>=<span class=\"hljs-string\">\"Customer\"</span> <span class=\"hljs-attr\">FontSize</span>=<span class=\"hljs-string\">\"24\"</span> <span class=\"hljs-attr\">FontWeight</span>=<span class=\"hljs-string\">\"SemiBold\"</span> /></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBox</span> <span class=\"hljs-attr\">Header</span>=<span class=\"hljs-string\">\"Name\"</span>\n           <span class=\"hljs-attr\">Text</span>=<span class=\"hljs-string\">\"{Binding Name, Mode=TwoWay}\"</span> /></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBox</span> <span class=\"hljs-attr\">Header</span>=<span class=\"hljs-string\">\"Email\"</span>\n           <span class=\"hljs-attr\">Text</span>=<span class=\"hljs-string\">\"{Binding Email, Mode=TwoWay}\"</span> /></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">CheckBox</span> <span class=\"hljs-attr\">Content</span>=<span class=\"hljs-string\">\"Email me updates\"</span>\n            <span class=\"hljs-attr\">IsChecked</span>=<span class=\"hljs-string\">\"{Binding WantsUpdates, Mode=TwoWay}\"</span> /></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Button</span> <span class=\"hljs-attr\">Content</span>=<span class=\"hljs-string\">\"Save\"</span>\n          <span class=\"hljs-attr\">Command</span>=<span class=\"hljs-string\">\"{Binding SaveCommand}\"</span> /></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">StackPanel</span>></span>\n</code></pre>\n<p>In Reactor, the same screen becomes a component:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TutorialFormPage</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (wantsUpdates, setWantsUpdates) = UseState(<span class=\"hljs-literal\">true</span>);\n        <span class=\"hljs-keyword\">var</span> canSave = !<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(name) &#x26;&#x26; !<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(email);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Customer\"</span>),\n            TextBox(name, setName, header: <span class=\"hljs-string\">\"Name\"</span>),\n            TextBox(email, setEmail, header: <span class=\"hljs-string\">\"Email\"</span>),\n            CheckBox(wantsUpdates, setWantsUpdates, label: <span class=\"hljs-string\">\"Email me updates\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Save\"</span>, () => { }).IsEnabled(canSave),\n                TextBlock(canSave ? <span class=\"hljs-string\">\"Ready to save\"</span> : <span class=\"hljs-string\">\"Complete all required fields\"</span>)\n                    .Opacity(<span class=\"hljs-number\">0.7</span>)\n            )\n        ).Width(<span class=\"hljs-number\">360</span>);\n    }\n}\n</code></pre>\n<p>What changed:</p>\n<ul>\n<li><strong>Bindings became state variables.</strong> <code>Name</code>, <code>Email</code>, and <code>WantsUpdates</code> live in <code>UseState</code>.</li>\n<li><strong>Two-way input became explicit.</strong> <code>TextBox(name, setName)</code> makes data flow obvious.</li>\n<li><strong>The command became normal C#.</strong> The save button uses a lambda instead of XAML command wiring.</li>\n<li><strong>Derived UI stayed inline.</strong> <code>canSave</code> is just a local expression, not a converter or extra property.</li>\n</ul>\n<p>That is the Reactor pattern in one screen: <strong>state at the top, UI returned at the\nbottom</strong>.</p>\n<h2 id=\"layout-feels-familiar-but-smaller\">Layout Feels Familiar, but Smaller<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#layout-feels-familiar-but-smaller\">#</a></h2>\n<p>Most XAML layouts translate directly, but Reactor pushes you toward a smaller set\nof composition primitives.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>XAML</th><th>Reactor</th></tr></thead><tbody><tr><td><code>StackPanel Orientation=\"Vertical\"</code></td><td><code>VStack(...)</code></td></tr><tr><td><code>StackPanel Orientation=\"Horizontal\"</code></td><td><code>HStack(...)</code></td></tr><tr><td><code>Grid</code> with row/column definitions</td><td><code>Grid(columns: ..., rows: ..., ...)</code></td></tr><tr><td><code>Border</code></td><td><code>Border(child)</code></td></tr><tr><td><code>ScrollViewer</code> (classic)</td><td><code>ScrollViewer(child)</code></td></tr><tr><td><code>ScrollView</code> (modern)</td><td><code>ScrollView(child)</code></td></tr></tbody></table>\n<p>This XAML:</p>\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Grid</span> <span class=\"hljs-attr\">ColumnSpacing</span>=<span class=\"hljs-string\">\"12\"</span> <span class=\"hljs-attr\">RowSpacing</span>=<span class=\"hljs-string\">\"8\"</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Grid.ColumnDefinitions</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ColumnDefinition</span> <span class=\"hljs-attr\">Width</span>=<span class=\"hljs-string\">\"Auto\"</span> /></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ColumnDefinition</span> <span class=\"hljs-attr\">Width</span>=<span class=\"hljs-string\">\"*\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">Grid.ColumnDefinitions</span>></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Grid.RowDefinitions</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">RowDefinition</span> <span class=\"hljs-attr\">Height</span>=<span class=\"hljs-string\">\"Auto\"</span> /></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">RowDefinition</span> <span class=\"hljs-attr\">Height</span>=<span class=\"hljs-string\">\"Auto\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">Grid.RowDefinitions</span>></span>\n\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBlock</span> <span class=\"hljs-attr\">Grid.Row</span>=<span class=\"hljs-string\">\"0\"</span> <span class=\"hljs-attr\">Grid.Column</span>=<span class=\"hljs-string\">\"0\"</span> <span class=\"hljs-attr\">Text</span>=<span class=\"hljs-string\">\"First name\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBox</span> <span class=\"hljs-attr\">Grid.Row</span>=<span class=\"hljs-string\">\"0\"</span> <span class=\"hljs-attr\">Grid.Column</span>=<span class=\"hljs-string\">\"1\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBlock</span> <span class=\"hljs-attr\">Grid.Row</span>=<span class=\"hljs-string\">\"1\"</span> <span class=\"hljs-attr\">Grid.Column</span>=<span class=\"hljs-string\">\"0\"</span> <span class=\"hljs-attr\">Text</span>=<span class=\"hljs-string\">\"Last name\"</span> /></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">TextBox</span> <span class=\"hljs-attr\">Grid.Row</span>=<span class=\"hljs-string\">\"1\"</span> <span class=\"hljs-attr\">Grid.Column</span>=<span class=\"hljs-string\">\"1\"</span> /></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">Grid</span>></span>\n</code></pre>\n<p>becomes:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GridTranslationPage</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> Grid(\n            columns: [GridSize.Auto, GridSize.Star()],\n            rows: [GridSize.Auto, GridSize.Auto],\n            TextBlock(<span class=\"hljs-string\">\"First name\"</span>).Bold().Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">0</span>),\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }).Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">1</span>),\n            TextBlock(<span class=\"hljs-string\">\"Last name\"</span>).Bold().Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">0</span>),\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }).Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">1</span>)\n        ) <span class=\"hljs-keyword\">with</span>\n        {\n            ColumnSpacing = <span class=\"hljs-number\">12</span>,\n            RowSpacing = <span class=\"hljs-number\">8</span>\n        };\n    }\n}\n</code></pre>\n<p>The layout idea is the same. The difference is that the child placement lives in\nfluent modifiers instead of attached properties written in markup.</p>\n<h2 id=\"bindings-turn-into-state-props-or-plain-expressions\">Bindings Turn into State, Props, or Plain Expressions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#bindings-turn-into-state-props-or-plain-expressions\">#</a></h2>\n<p>XAML developers often look for the Reactor equivalent of <code>Binding</code>. There is no\nsingle replacement, because bindings usually solve several different problems.</p>\n<p>Use this rule of thumb instead:</p>\n<ul>\n<li><strong>Value owned by this component:</strong> <a href=\"/hooks/\"><code>UseState</code></a></li>\n<li><strong>Complex local updates:</strong> <a href=\"/hooks/\"><code>UseReducer</code></a></li>\n<li><strong>Input from a parent:</strong> typed props via <a href=\"/components/\"><code>Component&#x3C;TProps></code></a></li>\n<li><strong>Computed value:</strong> ordinary local C# expression</li>\n<li><strong>Shared app state:</strong> <a href=\"/context/\"><code>context</code></a> or a higher-level parent component</li>\n<li><strong>Existing MVVM object:</strong> <a href=\"/advanced/\"><code>UseObservable</code></a> or <code>UseObservableTree</code></li>\n</ul>\n<p>That is why Reactor code often looks simpler than XAML. A label like\n<code>TextBlock($\"{firstName} {lastName}\")</code> is already \"bound\" because <code>Render()</code>\nre-runs whenever the relevant state changes.</p>\n<blockquote>\n<p><strong>Caveat:</strong> A XAML <code>Binding</code> with <code>Mode=TwoWay</code> becomes a Reactor controlled-input\npattern (<code>TextBox(name, setName)</code>), not a binding-with-mode. There is\n<strong>no</strong> Reactor analogue to <code>Mode=OneWay</code> / <code>Mode=OneTime</code> / <code>Mode=TwoWay</code>\nbecause state IS the binding — every render re-reads from state, every\nedit calls a setter. If you write <code>TextBox(name, _ => { })</code> and never\ncall a setter, the field is read-only (effectively <code>Mode=OneWay</code>); if\nyou wire both, it round-trips (effectively <code>Mode=TwoWay</code>). The trap is\n\"reach for the binding mode for OneTime\" — there is no equivalent.\nCache the value in a <code>UseRef</code> and read <code>ref.Current</code> if you genuinely\nwant to capture-and-freeze, or call a function once in a\n<code>UseEffect(() => …, Array.Empty&#x3C;object>())</code> so it runs only on mount.\nThe Reactor analyzer doesn't emit a specific diagnostic for \"missing\nsetter\" — passing <code>null</code> to a <code>TextBox</code> change handler is a\n<code>CS8625</code> \"Cannot convert null literal\" instead.</p>\n</blockquote>\n<h2 id=\"events-and-commands-are-just-c\">Events and Commands Are Just C#<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#events-and-commands-are-just-c\">#</a></h2>\n<p>You do not need a special command layer for every button click.</p>\n<ul>\n<li><code>Button(\"Save\", Save)</code> is the direct equivalent of a button command.</li>\n<li><code>Button(\"Refresh\", async () => await ReloadAsync())</code> works for async actions.</li>\n<li><code>TextBox(text, setText)</code> replaces both <code>TextChanged</code> wiring and two-way binding.</li>\n</ul>\n<p>If you want richer busy/error behavior, Reactor also has a dedicated\n<a href=\"/commanding/\">Commanding</a> API. But the default is deliberately small: use a\nmethod or lambda first, then add command abstractions when they actually help.</p>\n<h2 id=\"events-xaml-attributes-become-fluents\">Events: XAML Attributes Become Fluents<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#events-xaml-attributes-become-fluents\">#</a></h2>\n<p>Every WinUI event attribute has a matching Reactor fluent. For most events\nthe rule is straightforward — drop the leading <code>On</code> from the Reactor\nproperty name. A handful of Reactor fluents normalize across WinUI's\nslightly different event shapes (e.g. <code>CheckBox</code> exposes three separate\n<code>Checked</code> / <code>Unchecked</code> / <code>Indeterminate</code> events in XAML; Reactor surfaces\na single <code>IsCheckedChanged</code> callback):</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>WinUI XAML event</th><th>Reactor fluent</th></tr></thead><tbody><tr><td><code>&#x3C;Button Click=\"OnClick\"/></code></td><td><code>Button(\"…\").Click(handler)</code></td></tr><tr><td><code>&#x3C;TextBox TextChanged=\"OnTextChanged\"/></code></td><td><code>TextBox(text, setText).Changed(handler)</code></td></tr><tr><td><code>&#x3C;ListView SelectionChanged=\"OnSelectionChanged\"/></code></td><td><code>ListView&#x3C;T>(...).SelectionChanged(handler)</code></td></tr><tr><td><code>&#x3C;ComboBox SelectionChanged=\"OnSelectionChanged\"/></code></td><td><code>ComboBox(...).SelectedIndexChanged(handler)</code> — Reactor reports the selected index, not the args</td></tr><tr><td><code>&#x3C;CheckBox Checked=\"…\" Unchecked=\"…\"/></code></td><td><code>CheckBox(value, setValue).IsCheckedChanged(handler)</code> — Reactor collapses the three XAML events into a single bool callback</td></tr></tbody></table>\n<p>The underlying init property keeps the <code>On</code> prefix, so existing\nproperty-init code continues to compile:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">EventsFluentExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        Action handler = () => { <span class=\"hljs-comment\">/* clicked */</span> };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            <span class=\"hljs-comment\">// Property-init still works:</span>\n            <span class=\"hljs-keyword\">new</span> ButtonElement(<span class=\"hljs-string\">\"Save\"</span>) { OnClick = handler },\n            <span class=\"hljs-comment\">// Preferred fluent:</span>\n            Button(<span class=\"hljs-string\">\"Save\"</span>).Click(handler)\n        );\n    }\n}\n</code></pre>\n<p>The fluent drops the <code>On</code> because C# binds <code>el.OnClick(arg)</code> to\ndelegate-as-property invocation (<code>Action?.Invoke(arg)</code>) and never falls back\nto extension methods — see\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §0.1 for the discovery\nand the naming decision. Passing <code>null</code> to any of these fluents clears a\npreviously-set handler.</p>\n<h2 id=\"navigation-without-frame\">Navigation Without <code>Frame</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigation-without-frame\">#</a></h2>\n<p>Reactor navigation is still WinUI navigation in spirit, but it is declared in the\ncomponent tree instead of being driven by imperative <code>Frame.Navigate(...)</code> calls.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TutorialNavigationPage</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(TutorialRoute.Home);\n\n        <span class=\"hljs-keyword\">return</span> Border(\n            NavigationView(\n                [\n                    NavItem(<span class=\"hljs-string\">\"Home\"</span>, icon: <span class=\"hljs-string\">\"Home\"</span>, tag: <span class=\"hljs-string\">\"Home\"</span>),\n                    NavItem(<span class=\"hljs-string\">\"Settings\"</span>, icon: <span class=\"hljs-string\">\"Setting\"</span>, tag: <span class=\"hljs-string\">\"Settings\"</span>),\n                    NavItem(<span class=\"hljs-string\">\"Account\"</span>, icon: <span class=\"hljs-string\">\"Contact\"</span>, tag: <span class=\"hljs-string\">\"Account\"</span>)\n                ],\n                content: NavigationHost(nav, route => route <span class=\"hljs-keyword\">switch</span>\n                {\n                    TutorialRoute.Home => VStack(<span class=\"hljs-number\">8</span>,\n                        Heading(<span class=\"hljs-string\">\"Home\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"This is the shell root.\"</span>),\n                        Button(<span class=\"hljs-string\">\"Go to Settings\"</span>, () => nav.Navigate(TutorialRoute.Settings))\n                    ).Padding(<span class=\"hljs-number\">24</span>),\n                    TutorialRoute.Settings => VStack(<span class=\"hljs-number\">8</span>,\n                        Heading(<span class=\"hljs-string\">\"Settings\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"Typed routes replace imperative Frame calls.\"</span>),\n                        Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())\n                    ).Padding(<span class=\"hljs-number\">24</span>),\n                    TutorialRoute.Account => VStack(<span class=\"hljs-number\">8</span>,\n                        Heading(<span class=\"hljs-string\">\"Account\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"A second page in the same shell.\"</span>)\n                    ).Padding(<span class=\"hljs-number\">24</span>),\n                    _ => TextBlock(<span class=\"hljs-string\">\"Not found\"</span>).Padding(<span class=\"hljs-number\">24</span>)\n                })\n            )\n        ).Height(<span class=\"hljs-number\">320</span>).Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">8</span>);\n    }\n}\n</code></pre>\n<p>Instead of keeping a <code>Frame</code> reference and pushing pages into it, you keep a typed\nnavigation handle in component state and render the current page through\n<code>NavigationHost</code>. That keeps navigation decisions in the same declarative flow as\nthe rest of the UI.</p>\n<h2 id=\"you-can-keep-mvvm-while-migrating\">You Can Keep MVVM While Migrating<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#you-can-keep-mvvm-while-migrating\">#</a></h2>\n<p>You do <strong>not</strong> have to throw away existing <code>INotifyPropertyChanged</code> view models on\nday one. Reactor has a bridge specifically for migration:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ObservableTreeDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> SettingsViewModel _vm = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> vm = UseObservableTree(_vm);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseObservableTree\"</span>),\n            TextBox(vm.UserName, v => vm.UserName = v,\n                header: <span class=\"hljs-string\">\"User Name\"</span>),\n            ToggleSwitch(vm.DarkMode, v => vm.DarkMode = v,\n                header: <span class=\"hljs-string\">\"Dark Mode\"</span>),\n            Slider(vm.FontSize, <span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">32</span>, v => vm.FontSize = (<span class=\"hljs-built_in\">int</span>)v),\n            TextBlock(<span class=\"hljs-string\">$\"Preview: <span class=\"hljs-subst\">{vm.UserName}</span>\"</span>)\n                .FontSize(vm.FontSize).Bold()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><code>UseObservableTree</code> subscribes to your existing view model and triggers a\nre-render when it changes. That lets you migrate screen-by-screen:</p>\n<ol>\n<li>Keep the current view model.</li>\n<li>Replace the XAML view with a Reactor component.</li>\n<li>Move simple screens from view-model state to hooks later, if you want.</li>\n</ol>\n<p>This is usually the least risky way to adopt Reactor in an existing codebase.</p>\n<h2 id=\"what-you-usually-stop-writing\">What You Usually Stop Writing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#what-you-usually-stop-writing\">#</a></h2>\n<p>Most XAML developers notice the same things disappear first:</p>\n<ul>\n<li><strong>No <code>DataContext</code> plumbing</strong> for ordinary screens</li>\n<li><strong>No value converters</strong> for simple formatting or visibility rules</li>\n<li><strong>No code-behind just to mirror control state</strong></li>\n<li><strong>No separate markup file</strong> for routine UI composition</li>\n<li><strong>Fewer tiny view models</strong> whose only job was exposing bindable properties</li>\n</ul>\n<p>The replacement is not \"more framework.\" It is usually just <strong>more ordinary C#</strong>.</p>\n<h2 id=\"a-practical-migration-path\">A Practical Migration Path<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#a-practical-migration-path\">#</a></h2>\n<p>If you are moving an existing WinUI app to Reactor, this tends to work well:</p>\n<ol>\n<li>Start with one leaf page, not the whole app shell.</li>\n<li>Rebuild the layout with <code>VStack</code>, <code>HStack</code>, <code>Grid</code>, and <code>Border</code>.</li>\n<li>Replace bindings with <code>UseState</code>, props, or <code>UseObservableTree</code>.</li>\n<li>Inline trivial converters as local expressions.</li>\n<li>Keep existing services and view models until the Reactor version is stable.</li>\n<li>Extract reusable UI into small components once the page works.</li>\n</ol>\n<p>The goal is not to \"port XAML syntax into C#.\" The goal is to adopt Reactor's\nstate-driven model while preserving your WinUI knowledge.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"dependencyproperty-becomes-a-hook\">DependencyProperty becomes a hook<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dependencyproperty-becomes-a-hook\">#</a></h3>\n<p>In XAML, every reactive value on a control sits on a <code>DependencyProperty</code>\n— a globally-registered slot with metadata, change callbacks, and\ninheritance rules. In Reactor, the analogue is a hook call inside\n<a href=\"/components/\"><code>Render()</code></a>: <code>var (count, setCount) = UseState(0)</code> is\nthe equivalent of a single instance-scoped DP, and the hook slot table\nbehind it (<a href=\"/hooks-internals/\">hooks-internals</a>) plays the role the\nDP system plays for XAML. The mental shift is \"I don't need a registry;\nI need a positional slot,\" and the implementation gets vastly smaller\nin the process.</p>\n<h3 id=\"usercontrol-becomes-a-component\"><code>UserControl</code> becomes a component<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usercontrol-becomes-a-component\">#</a></h3>\n<p>XAML developers reach for <code>UserControl</code> whenever a screen contains a\nreusable visual chunk with its own state. In Reactor, that chunk is a\n<a href=\"/components/\">component</a> — a class with a <code>Render()</code> method and\ntyped props. The XAML version has a <code>.xaml</code> markup file plus a\n<code>.xaml.cs</code> code-behind plus often a property dependency-injected via\nDataContext; the Reactor version is one C# class. The reuse story is\nthe same; the line count drops by ~70% on most controls.</p>\n<h3 id=\"datatemplate-becomes-a-render-function\"><code>DataTemplate</code> becomes a render function<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#datatemplate-becomes-a-render-function\">#</a></h3>\n<p>XAML <code>DataTemplate</code> declares how each item in a list renders. Reactor's\nequivalent is the third argument to <a href=\"/collections/\"><code>ListView&#x3C;T></code></a> /\n<a href=\"/collections/\"><code>GridView&#x3C;T></code></a> / <a href=\"/collections/\"><code>VirtualList</code></a>: a\n<code>Func&#x3C;T, Element></code> that returns the per-item Element tree. Item\nselection and editing are state in the parent component, not a\nhand-wired <code>SelectedItem</code> binding. The\n<a href=\"/recipes/master-detail/\">recipes/master-detail</a> walkthrough is the\ncanonical example.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"trying-to-compute-layout-from-a-dependencyproperty\">Trying to compute layout from a <code>DependencyProperty</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#trying-to-compute-layout-from-a-dependencyproperty\">#</a></h3>\n<p>Reactor has no DP system — there is no <code>DependencyProperty.Register</code>,\nno metadata callback, no <code>AffectsMeasure</code>. Computed values live as\nordinary local variables inside <code>Render()</code>; cached computations live in\n<a href=\"/hooks/\"><code>UseMemo</code></a>. If you find yourself looking for \"the equivalent\nof <code>AffectsArrange</code>,\" the answer is \"your <code>Render()</code> already ran and\nthe reconciler diffed the layout-affecting modifiers\" — see\n<a href=\"/reactor-vs-xaml/\">reactor-vs-xaml</a> for the longer explanation.</p>\n<h3 id=\"using-inotifypropertychanged-to-back-local-state\">Using <code>INotifyPropertyChanged</code> to back local state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-inotifypropertychanged-to-back-local-state\">#</a></h3>\n<p>A small \"binding view model\" that exposes <code>Name</code>, <code>Email</code>, etc. as\nINPC properties is the XAML idiom — in Reactor, those four lines\ncollapse to four <code>UseState</code> calls. The <code>INotifyPropertyChanged</code> bridge\nstill exists (<a href=\"/advanced/\"><code>UseObservable</code></a>), but reach for it only\nwhen you're migrating an existing view model. New code uses hooks.</p>\n<h3 id=\"writing-xaml-at-all\">Writing XAML at all<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#writing-xaml-at-all\">#</a></h3>\n<p>Reactor does not host a XAML loader. There is no <code>Application.LoadComponent</code>\nequivalent, no <code>xmlns:reactor</code>, no <code>*.reactor.xml</code> markup. Apps that\nneed part of the screen in XAML use a WinUI <code>Page</code> as the host and\nembed Reactor via <a href=\"/winforms-interop/\">winforms-interop</a> or\n<code>ReactorHostControl</code>; everything inside that host is C#. If a\ncontractor's \"Reactor XAML compiler\" appears in your search results,\nit doesn't exist — the framework's design eliminates the second\nauthoring language deliberately.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Think \"render the current truth.\"</strong> In XAML, you often describe relationships\nbetween properties. In Reactor, you usually compute the current value directly and\nreturn it.</p>\n<p><strong>Prefer state over control references.</strong> If changing something should update the\nscreen, store the value in <code>UseState</code> instead of reaching into a control instance.</p>\n<p><strong>Use small components where you used <code>UserControl</code>.</strong> The same decomposition\ninstinct still applies; the only change is that the reusable unit is a C#\ncomponent rather than a XAML file.</p>\n<p><strong>Keep MVVM only where it earns its keep.</strong> Existing observable objects migrate\nwell, but many small \"binding-only\" view models become unnecessary once your UI is\nalready C#.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/reactor-vs-xaml/\">Reactor vs XAML</a></strong> — the architectural essay: why the binding model is fundamentally different, not just syntactically</li>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — build your first Reactor app from scratch</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — break UI into reusable typed components</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — learn <code>UseState</code>, <code>UseReducer</code>, <code>UseEffect</code>, and the core render model</li>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — map more WinUI layout patterns into Reactor primitives</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — bridge existing MVVM state with <code>UseObservableTree</code></li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s render-from-state model has a clean 1:1 translation table to XAML that an experienced XAML developer can hold in their head. Where XAML describes bindings between view-model properties and control DPs and lets the binding engine pull values on change-notification, Reactor describes the current UI directly and re-evaluates the entire component when state changes. The control tree is the same WinUI control tree at runtime — only the authoring surface differs. This page is the translation key: every XAML idiom you reach for (DataContext, Binding modes, DataTemplate, DependencyProperty, code-behind, ICommand, Frame.Navigate) maps to a specific Reactor shape, and the body below walks each one in order. Companion essay reactor-vs-xaml covers the architectural why — read this page for the recipes, that one for the philosophy. Reactor for XAML Developers # If you already know XAML, Reactor is not a different Windows UI stack. It still renders real WinUI controls. The shift is in how you describe the UI: instead of splitting a page across XAML, bindings, converters, and code-behind, you return the UI directly from C# and let Reactor keep the native control tree in sync. The Mental Model Shift # Think of Reactor as \"WinUI controls, but expressed like a function of state.\" In XAML In Reactor Page , UserControl , Window markup A Component with Render() {Binding Name} Plain C# variable usage: TextBlock(name) Mode=TwoWay Controlled input: TextBox(name, setName) DataContext Local hook state, typed props, or context ICommand Lambdas, methods, or commands StackPanel VStack or HStack Grid.Row , Grid.Column .Grid(row: ..., column: ...) StaticResource / ThemeResource Theme tokens and fluent modifiers Frame.Navigate(...) UseNavigation + NavigationHost The important difference is that Reactor does not ask you to describe bindings . It asks you to describe the current UI . When the state changes, Render() runs again and Reactor updates only the native WinUI controls that changed. Rewriting a Familiar Form # Here is the kind of XAML page many WinUI developers start with: &#x3C; StackPanel Spacing = \"12\" Padding = \"24\" > &#x3C; TextBlock Text = \"Customer\" FontSize = \"24\" FontWeight = \"SemiBold\" /> &#x3C; TextBox Header = \"Name\" Text = \"{Binding Name, Mode=TwoWay}\" /> &#x3C; TextBox Header = \"Email\" Text = \"{Binding Email, Mode=TwoWay}\" /> &#x3C; CheckBox Content = \"Email me updates\" IsChecked = \"{Binding WantsUpdates, Mode=TwoWay}\" /> &#x3C; Button Content = \"Save\" Command = \"{Binding SaveCommand}\" /> &#x3C;/ StackPanel > In Reactor, the same screen becomes a component: class TutorialFormPage : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); var (email, setEmail) = UseState( \"\" ); var (wantsUpdates, setWantsUpdates) = UseState( true ); var canSave = ! string .IsNullOrWhiteSpace(name) &#x26;&#x26; ! string .IsNullOrWhiteSpace(email); return VStack( 12 , SubHeading( \"Customer\" ), TextBox(name, setName, header: \"Name\" ), TextBox(email, setEmail, header: \"Email\" ), CheckBox(wantsUpdates, setWantsUpdates, label: \"Email me updates\" ), HStack( 8 , Button( \"Save\" , () => { }).IsEnabled(canSave), TextBlock(canSave ? \"Ready to save\" : \"Complete all required fields\" ) .Opacity( 0.7 ) ) ).Width( 360 ); } } What changed: Bindings became state variables. Name , Email , and WantsUpdates live in UseState . Two-way input became explicit. TextBox(name, setName) makes data flow obvious. The command became normal C#. The save button uses a lambda instead of XAML command wiring. Derived UI stayed inline. canSave is just a local expression, not a converter or extra property. That is the Reactor pattern in one screen: state at the top, UI returned at the bottom . Layout Feels Familiar, but Smaller # Most XAML layouts translate directly, but Reactor pushes you toward a smaller set of composition primitives. XAML Reactor StackPanel Orientation=\"Vertical\" VStack(...) StackPanel Orientation=\"Horizontal\" HStack(...) Grid with row/column definitions Grid(columns: ..., rows: ..., ...) Border Border(child) ScrollViewer (classic) ScrollViewer(child) ScrollView (modern) ScrollView(child) This XAML: &#x3C; Grid ColumnSpacing = \"12\" RowSpacing = \"8\" > &#x3C; Grid.ColumnDefinitions > &#x3C; ColumnDefinition Width = \"Auto\" /> &#x3C; ColumnDefinition Width = \"*\" /> &#x3C;/ Grid.ColumnDefinitions > &#x3C; Grid.RowDefinitions > &#x3C; RowDefinition Height = \"Auto\" /> &#x3C; RowDefinition Height = \"Auto\" /> &#x3C;/ Grid.RowDefinitions > &#x3C; TextBlock Grid.Row = \"0\" Grid.Column = \"0\" Text = \"First name\" /> &#x3C; TextBox Grid.Row = \"0\" Grid.Column = \"1\" /> &#x3C; TextBlock Grid.Row = \"1\" Grid.Column = \"0\" Text = \"Last name\" /> &#x3C; TextBox Grid.Row = \"1\" Grid.Column = \"1\" /> &#x3C;/ Grid > becomes: class GridTranslationPage : Component { public override Element Render () { return Grid( columns: [GridSize.Auto, GridSize.Star()], rows: [GridSize.Auto, GridSize.Auto], TextBlock( \"First name\" ).Bold().Grid(row: 0 , column: 0 ), TextBox( \"\" , _ => { }).Grid(row: 0 , column: 1 ), TextBlock( \"Last name\" ).Bold().Grid(row: 1 , column: 0 ), TextBox( \"\" , _ => { }).Grid(row: 1 , column: 1 ) ) with { ColumnSpacing = 12 , RowSpacing = 8 }; } } The layout idea is the same. The difference is that the child placement lives in fluent modifiers instead of attached properties written in markup. Bindings Turn into State, Props, or Plain Expressions # XAML developers often look for the Reactor equivalent of Binding . There is no single replacement, because bindings usually solve several different problems. Use this rule of thumb instead: Value owned by this component: UseState Complex local updates: UseReducer Input from a parent: typed props via Component&#x3C;TProps> Computed value: ordinary local C# expression Shared app state: context or a higher-level parent component Existing MVVM object: UseObservable or UseObservableTree That is why Reactor code often looks simpler than XAML. A label like TextBlock($\"{firstName} {lastName}\") is already \"bound\" because Render() re-runs whenever the relevant state changes. Caveat: A XAML Binding with Mode=TwoWay becomes a Reactor controlled-input pattern ( TextBox(name, setName) ), not a binding-with-mode. There is no Reactor analogue to Mode=OneWay / Mode=OneTime / Mode=TwoWay because state IS the binding — every render re-reads from state, every edit calls a setter. If you write TextBox(name, _ => { }) and never call a setter, the field is read-only (effectively Mode=OneWay ); if you wire both, it round-trips (effectively Mode=TwoWay ). The trap is \"reach for the binding mode for OneTime\" — there is no equivalent. Cache the value in a UseRef and read ref.Current if you genuinely want to capture-and-freeze, or call a function once in a UseEffect(() => …, Array.Empty&#x3C;object>()) so it runs only on mount. The Reactor analyzer doesn't emit a specific diagnostic for \"missing setter\" — passing null to a TextBox change handler is a CS8625 \"Cannot convert null literal\" instead. Events and Commands Are Just C# # You do not need a special command layer for every button click. Button(\"Save\", Save) is the direct equivalent of a button command. Button(\"Refresh\", async () => await ReloadAsync()) works for async actions. TextBox(text, setText) replaces both TextChanged wiring and two-way binding. If you want richer busy/error behavior, Reactor also has a dedicated Commanding API. But the default is deliberately small: use a method or lambda first, then add command abstractions when they actually help. Events: XAML Attributes Become Fluents # Every WinUI event attribute has a matching Reactor fluent. For most events the rule is straightforward — drop the leading On from the Reactor property name. A handful of Reactor fluents normalize across WinUI's slightly different event shapes (e.g. CheckBox exposes three separate Checked / Unchecked / Indeterminate events in XAML; Reactor surfaces a single IsCheckedChanged callback): WinUI XAML event Reactor fluent &#x3C;Button Click=\"OnClick\"/> Button(\"…\").Click(handler) &#x3C;TextBox TextChanged=\"OnTextChanged\"/> TextBox(text, setText).Changed(handler) &#x3C;ListView SelectionChanged=\"OnSelectionChanged\"/> ListView&#x3C;T>(...).SelectionChanged(handler) &#x3C;ComboBox SelectionChanged=\"OnSelectionChanged\"/> ComboBox(...).SelectedIndexChanged(handler) — Reactor reports the selected index, not the args &#x3C;CheckBox Checked=\"…\" Unchecked=\"…\"/> CheckBox(value, setValue).IsCheckedChanged(handler) — Reactor collapses the three XAML events into a single bool callback The underlying init property keeps the On prefix, so existing property-init code continues to compile: class EventsFluentExample : Component { public override Element Render () { Action handler = () => { /* clicked */ }; return VStack( 8 , // Property-init still works: new ButtonElement( \"Save\" ) { OnClick = handler }, // Preferred fluent: Button( \"Save\" ).Click(handler) ); } } The fluent drops the On because C# binds el.OnClick(arg) to delegate-as-property invocation ( Action?.Invoke(arg) ) and never falls back to extension methods — see spec 039 §0.1 for the discovery and the naming decision. Passing null to any of these fluents clears a previously-set handler. Navigation Without Frame # Reactor navigation is still WinUI navigation in spirit, but it is declared in the component tree instead of being driven by imperative Frame.Navigate(...) calls. class TutorialNavigationPage : Component { public override Element Render () { var nav = UseNavigation(TutorialRoute.Home); return Border( NavigationView( [ NavItem( \"Home\" , icon: \"Home\" , tag: \"Home\" ), NavItem( \"Settings\" , icon: \"Setting\" , tag: \"Settings\" ), NavItem( \"Account\" , icon: \"Contact\" , tag: \"Account\" ) ], content: NavigationHost(nav, route => route switch { TutorialRoute.Home => VStack( 8 , Heading( \"Home\" ), TextBlock( \"This is the shell root.\" ), Button( \"Go to Settings\" , () => nav.Navigate(TutorialRoute.Settings)) ).Padding( 24 ), TutorialRoute.Settings => VStack( 8 , Heading( \"Settings\" ), TextBlock( \"Typed routes replace imperative Frame calls.\" ), Button( \"Back\" , () => nav.GoBack()) ).Padding( 24 ), TutorialRoute.Account => VStack( 8 , Heading( \"Account\" ), TextBlock( \"A second page in the same shell.\" ) ).Padding( 24 ), _ => TextBlock( \"Not found\" ).Padding( 24 ) }) ) ).Height( 320 ).Background(Theme.CardBackground).CornerRadius( 8 ); } } Instead of keeping a Frame reference and pushing pages into it, you keep a typed navigation handle in component state and render the current page through NavigationHost . That keeps navigation decisions in the same declarative flow as the rest of the UI. You Can Keep MVVM While Migrating # You do not have to throw away existing INotifyPropertyChanged view models on day one. Reactor has a bridge specifically for migration: class ObservableTreeDemo : Component { private static readonly SettingsViewModel _vm = new (); public override Element Render () { var vm = UseObservableTree(_vm); return VStack( 12 , SubHeading( \"UseObservableTree\" ), TextBox(vm.UserName, v => vm.UserName = v, header: \"User Name\" ), ToggleSwitch(vm.DarkMode, v => vm.DarkMode = v, header: \"Dark Mode\" ), Slider(vm.FontSize, 10 , 32 , v => vm.FontSize = ( int )v), TextBlock( $\"Preview: {vm.UserName} \" ) .FontSize(vm.FontSize).Bold() ).Padding( 24 ); } } UseObservableTree subscribes to your existing view model and triggers a re-render when it changes. That lets you migrate screen-by-screen: Keep the current view model. Replace the XAML view with a Reactor component. Move simple screens from view-model state to hooks later, if you want. This is usually the least risky way to adopt Reactor in an existing codebase. What You Usually Stop Writing # Most XAML developers notice the same things disappear first: No DataContext plumbing for ordinary screens No value converters for simple formatting or visibility rules No code-behind just to mirror control state No separate markup file for routine UI composition Fewer tiny view models whose only job was exposing bindable properties The replacement is not \"more framework.\" It is usually just more ordinary C# . A Practical Migration Path # If you are moving an existing WinUI app to Reactor, this tends to work well: Start with one leaf page, not the whole app shell. Rebuild the layout with VStack , HStack , Grid , and Border . Replace bindings with UseState , props, or UseObservableTree . Inline trivial converters as local expressions. Keep existing services and view models until the Reactor version is stable. Extract reusable UI into small components once the page works. The goal is not to \"port XAML syntax into C#.\" The goal is to adopt Reactor's state-driven model while preserving your WinUI knowledge. Patterns # DependencyProperty becomes a hook # In XAML, every reactive value on a control sits on a DependencyProperty — a globally-registered slot with metadata, change callbacks, and inheritance rules. In Reactor, the analogue is a hook call inside Render() : var (count, setCount) = UseState(0) is the equivalent of a single instance-scoped DP, and the hook slot table behind it ( hooks-internals ) plays the role the DP system plays for XAML. The mental shift is \"I don't need a registry; I need a positional slot,\" and the implementation gets vastly smaller in the process. UserControl becomes a component # XAML developers reach for UserControl whenever a screen contains a reusable visual chunk with its own state. In Reactor, that chunk is a component — a class with a Render() method and typed props. The XAML version has a .xaml markup file plus a .xaml.cs code-behind plus often a property dependency-injected via DataContext; the Reactor version is one C# class. The reuse story is the same; the line count drops by ~70% on most controls. DataTemplate becomes a render function # XAML DataTemplate declares how each item in a list renders. Reactor's equivalent is the third argument to ListView&#x3C;T> / GridView&#x3C;T> / VirtualList : a Func&#x3C;T, Element> that returns the per-item Element tree. Item selection and editing are state in the parent component, not a hand-wired SelectedItem binding. The recipes/master-detail walkthrough is the canonical example. Common Mistakes # Trying to compute layout from a DependencyProperty # Reactor has no DP system — there is no DependencyProperty.Register , no metadata callback, no AffectsMeasure . Computed values live as ordinary local variables inside Render() ; cached computations live in UseMemo . If you find yourself looking for \"the equivalent of AffectsArrange ,\" the answer is \"your Render() already ran and the reconciler diffed the layout-affecting modifiers\" — see reactor-vs-xaml for the longer explanation. Using INotifyPropertyChanged to back local state # A small \"binding view model\" that exposes Name , Email , etc. as INPC properties is the XAML idiom — in Reactor, those four lines collapse to four UseState calls. The INotifyPropertyChanged bridge still exists ( UseObservable ), but reach for it only when you're migrating an existing view model. New code uses hooks. Writing XAML at all # Reactor does not host a XAML loader. There is no Application.LoadComponent equivalent, no xmlns:reactor , no *.reactor.xml markup. Apps that need part of the screen in XAML use a WinUI Page as the host and embed Reactor via winforms-interop or ReactorHostControl ; everything inside that host is C#. If a contractor's \"Reactor XAML compiler\" appears in your search results, it doesn't exist — the framework's design eliminates the second authoring language deliberately. Tips # Think \"render the current truth.\" In XAML, you often describe relationships between properties. In Reactor, you usually compute the current value directly and return it. Prefer state over control references. If changing something should update the screen, store the value in UseState instead of reaching into a control instance. Use small components where you used UserControl . The same decomposition instinct still applies; the only change is that the reusable unit is a C# component rather than a XAML file. Keep MVVM only where it earns its keep. Existing observable objects migrate well, but many small \"binding-only\" view models become unnecessary once your UI is already C#. Next Steps # Reactor vs XAML — the architectural essay: why the binding model is fundamentally different, not just syntactically Getting Started — build your first Reactor app from scratch Components — break UI into reusable typed components Hooks — learn UseState , UseReducer , UseEffect , and the core render model Layout — map more WinUI layout patterns into Reactor primitives Advanced Patterns — bridge existing MVVM state with UseObservableTree",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/xaml-developers.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/xaml-developers.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-mental-model-shift",
          "text": "The Mental Model Shift"
        },
        {
          "depth": 2,
          "id": "rewriting-a-familiar-form",
          "text": "Rewriting a Familiar Form"
        },
        {
          "depth": 2,
          "id": "layout-feels-familiar-but-smaller",
          "text": "Layout Feels Familiar, but Smaller"
        },
        {
          "depth": 2,
          "id": "bindings-turn-into-state-props-or-plain-expressions",
          "text": "Bindings Turn into State, Props, or Plain Expressions"
        },
        {
          "depth": 2,
          "id": "events-and-commands-are-just-c",
          "text": "Events and Commands Are Just C#"
        },
        {
          "depth": 2,
          "id": "events-xaml-attributes-become-fluents",
          "text": "Events: XAML Attributes Become Fluents"
        },
        {
          "depth": 2,
          "id": "navigation-without-frame",
          "text": "Navigation Without Frame"
        },
        {
          "depth": 2,
          "id": "you-can-keep-mvvm-while-migrating",
          "text": "You Can Keep MVVM While Migrating"
        },
        {
          "depth": 2,
          "id": "what-you-usually-stop-writing",
          "text": "What You Usually Stop Writing"
        },
        {
          "depth": 2,
          "id": "a-practical-migration-path",
          "text": "A Practical Migration Path"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "dependencyproperty-becomes-a-hook",
          "text": "DependencyProperty becomes a hook"
        },
        {
          "depth": 3,
          "id": "usercontrol-becomes-a-component",
          "text": "UserControl becomes a component"
        },
        {
          "depth": 3,
          "id": "datatemplate-becomes-a-render-function",
          "text": "DataTemplate becomes a render function"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "trying-to-compute-layout-from-a-dependencyproperty",
          "text": "Trying to compute layout from a DependencyProperty"
        },
        {
          "depth": 3,
          "id": "using-inotifypropertychanged-to-back-local-state",
          "text": "Using INotifyPropertyChanged to back local state"
        },
        {
          "depth": 3,
          "id": "writing-xaml-at-all",
          "text": "Writing XAML at all"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Reactor vs XAML",
      "path": "reactor-vs-xaml.md",
      "slug": "/reactor-vs-xaml/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/winui/winui3/\">Winui3</a>.</p>\n</blockquote>\n<p>If you've spent years inside XAML, the question that keeps coming back\nis \"where did my bindings go?\" Microsoft.UI.Reactor (Reactor) renders the same WinUI controls\nyour XAML pages renders — <code>Button</code>, <code>TextBox</code>, <code>TabView</code>,\n<code>ItemsRepeater</code> — and lays them out through the same panels.\nWhat changes is the source of truth for property values. In XAML, a\ncontrol owns its dependency properties, and a binding expression\nlistens to a view-model property and pulls the new value when\n<code>PropertyChanged</code> raises. In Reactor, a hook owns the state, the\ncomponent re-renders to produce a new element tree, and the reconciler\nwrites the property values onto the existing control. The two models\nsolve the same problem (keep UI in sync with state) but locate the\nwork in opposite places. Read this page once and the rest of the\ncodebase falls into place: nothing in <a href=\"/architecture-overview/\">Architecture\nOverview</a>, <a href=\"/hooks-internals/\">Hooks Internals</a>,\nor <a href=\"/reconciliation/\">Reconciliation</a> hides a binding system.</p>\n<h1 id=\"reactor-vs-xaml\">Reactor vs XAML<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactor-vs-xaml\">#</a></h1>\n<p>This page is for the XAML developer who wants the <em>why</em>. The\n<a href=\"/xaml-developers/\">Reactor for XAML Developers</a> cookbook covers the\n<em>how</em> for common rewrite tasks (\"I have a <code>Page</code> with bindings; what\ndoes that become?\"). Indexed in two places: section 1 (Get Started)\nso a XAML developer hitting the docs cold finds it early, and section\n9 (Under the hood) so it sits next to the architecture pages it\ncomplements.</p>\n<h2 id=\"pull-vs-push-one-diagram\">Pull vs push, one diagram<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pull-vs-push-one-diagram\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/reactor-vs-xaml/push-vs-pull.svg\" alt=\"XAML pull-based binding versus Reactor push-from-state\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>XAML</th><th>Reactor</th></tr></thead><tbody><tr><td>State location</td><td>View-model property / dependency property</td><td>Hook slot inside <code>RenderContext</code></td></tr><tr><td>State signal</td><td><code>INotifyPropertyChanged.PropertyChanged</code></td><td>Setter closure from <code>UseState</code></td></tr><tr><td>Property write</td><td><code>BindingExpression.UpdateTarget</code> pulls VM getter</td><td>Reconciler writes after render</td></tr><tr><td>Style application</td><td><code>Style</code> + <code>Setter</code> + triggers</td><td>Modifier chain or <code>Use*</code> named-style hook</td></tr><tr><td>Animation</td><td><code>Storyboard</code> defined in XAML, started imperatively</td><td><code>.WithAnimation(...)</code> modifier resolved by reconciler</td></tr><tr><td>Layout state</td><td><code>VisualStateManager</code> + <code>VisualState</code>/Group XAML</td><td><code>.InteractionStates(...)</code> modifier on element</td></tr><tr><td>Resources</td><td><code>ResourceDictionary</code> / <code>ThemeDictionary</code></td><td><a href=\"/theming-tokens/\"><code>ThemeRef</code></a> record struct</td></tr><tr><td>Lists</td><td><code>ItemsControl</code> + <code>DataTemplate</code></td><td><code>ForEach(items, item => …)</code> builds element per item</td></tr><tr><td>MVVM</td><td>View-model class with INPC properties</td><td>Function or class component with hooks</td></tr></tbody></table>\n<p>Every row below explains the mapping in enough depth that the\nmechanical translation isn't a guess.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The pull/push contrast is a runtime distinction, not a feature\nchecklist. Reactor still uses dependency properties — every WinUI\ncontrol it materializes has its DPs set by the reconciler. What's gone\nis the binding <em>expression</em> that listens to a VM and pulls. If you\nattach a <code>{Binding}</code> to a control Reactor materialized (via an\n<a href=\"/hooks-internals/\">ElementRef</a> cast or by setting it imperatively in\nan effect), you'll create a parallel reactive edge the reconciler\ndoesn't know about, and the next reconcile pass will overwrite your\nproperty write. Drive properties through hook state and let the\nreconciler write them; do not bind onto controls Reactor owns.</p>\n</blockquote>\n<h2 id=\"dependencyproperty--element-record--modifier\">DependencyProperty → element record + modifier<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dependencyproperty--element-record--modifier\">#</a></h2>\n<p>XAML's dependency property system is the substrate for everything\nreactive in WinUI: bindings hang off DPs, styles set DPs, animations\nanimate DPs, the <code>VisualStateManager</code> flips DPs. The DP itself is a\nslot on a control, with a metadata-driven default value, type\ncoercion, change notifications, and inheritance through the visual\ntree. A <code>Button.Content</code> value is a DP; a <code>Grid.Row</code> attached\nproperty is a DP; theme colors come out of a DP-flavored resource\nsystem.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<p>Reactor replaces the DP-slot-on-the-control with the\n<code>Element</code> record. <code>ButtonElement</code> (a <code>record</code> deriving from\n<code>Element</code>) carries <code>Content</code>, <code>OnClick</code>, etc. as record fields. The\nmodifier system folds layout and styling concerns into a separate\n<a href=\"/modifier-system/\"><code>ElementModifiers</code></a> record threaded through\nrecord <code>with</code> updates. Attached properties (Grid.Row, Canvas.Left)\nlive in a type-keyed <code>Attached</code> dictionary on the same record.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Modify</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, ElementModifiers mods</span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    el <span class=\"hljs-keyword\">with</span> { Modifiers = el.Modifiers <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? el.Modifiers.Merge(mods) : mods };\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyA11y</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, AccessibilityModifiers a11y</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> existing = el.Modifiers?.Accessibility;\n    <span class=\"hljs-keyword\">var</span> merged = existing <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? existing.Merge(a11y) : a11y;\n    <span class=\"hljs-keyword\">return</span> Modify(el, <span class=\"hljs-keyword\">new</span> ElementModifiers { Accessibility = merged });\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyTheme</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, <span class=\"hljs-built_in\">string</span> property, ThemeRef theme</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> bindings = el.ThemeBindings <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>\n        ? <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef>(el.ThemeBindings) { [property] = theme }\n        : <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef> { [property] = theme };\n    <span class=\"hljs-keyword\">return</span> el <span class=\"hljs-keyword\">with</span> { ThemeBindings = bindings };\n}\n</code></pre>\n<p>The chain <code>Text(\"hi\").FontSize(24).Margin(8)</code> produces one\n<code>TextElement</code> record with merged <code>Modifiers</code>. The reconciler reads\nboth the typed fields and the modifiers when it patches the control.\nThere is no per-property change notification: the comparison is\nrecord-level on the next render, and only differences become property\nwrites.</p>\n<h2 id=\"binding--closure-over-state\">Binding → closure over state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#binding--closure-over-state\">#</a></h2>\n<p>A <code>{Binding Name}</code> in XAML installs a <code>BindingExpression</code> that holds\na reference to the view model and the property path, subscribes to\n<code>INotifyPropertyChanged</code>, and on each <code>PropertyChanged</code> raise reads\nthe source and writes the target DP. The binding is a long-lived\nobject; mode (<code>OneWay</code>, <code>TwoWay</code>, <code>OneTime</code>), update trigger, converter,\nand fallback all live on it.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>Reactor doesn't install anything. <code>TextBlock(name)</code> reads the current\nvalue of <code>name</code> (a plain C# variable that happens to come out of\n<code>UseState</code>) and produces a <code>TextElement</code> with <code>Text = name</code> as a\nrecord field. When <code>name</code> changes, the setter writes the slot, the\ncomponent re-renders, the new <code>TextElement</code> differs from the old, and\nthe reconciler writes the new <code>Text</code> value onto the existing\n<code>TextBlock</code>. There is no binding object — the closure that produces\nthe element is the binding.</p>\n<p><code>TwoWay</code> shows up as the controlled-input pattern. A <code>TextBox</code> takes\nthe current value and a setter callback; the user's input fires the\ncallback, which writes the slot, which re-renders, which writes the\nnew value back into the <code>TextBox</code>. Mode is implicit in whether you\npass a setter.</p>\n<h2 id=\"datatemplate--function-component\">DataTemplate → function component<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#datatemplate--function-component\">#</a></h2>\n<p>A <code>DataTemplate</code> is a recipe: given a data item, produce a control\ntree. It's authored in XAML and instantiated by the framework when\nitems appear in an <code>ItemsControl</code>. The template can name elements\nand bind to properties on the item; the runtime keeps a pool and\nrecycles.</p>\n<pre><code class=\"hljs language-csharp\">ForEach(items, item => Card(item))\n</code></pre>\n<p>The closure <code>item => Card(item)</code> is the template. It runs once per\nitem per render, produces an element tree, and the <a href=\"/reconciliation/\">child\nreconciler</a> matches the elements against the\nprevious render. Identity comes from position by default; <code>.WithKey(id)</code>\non the item element preserves identity across reorders.\n<a href=\"/collections/\"><code>ItemsRepeater</code></a> is still under the hood — the\n<code>ForEach</code> materializes through an <code>ElementFactory</code> that recycles the\nreal WinUI controls.</p>\n<h2 id=\"style--modifier-composition--named-style\">Style → modifier composition / named style<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#style--modifier-composition--named-style\">#</a></h2>\n<p><code>Style</code> in XAML applies a bag of property setters to every control\nthat matches a <code>TargetType</code> (or carries the style key). The bag is\nkeyed by DP, and <code>BasedOn</code> chains styles through inheritance.</p>\n<p>Reactor has two analogues. The most common is a plain method that\ntakes an element and applies modifiers:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">CardSurface</span>(<span class=\"hljs-params\">Element child</span>)</span> =>\n    child.Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">8</span>).Padding(<span class=\"hljs-number\">16</span>);\n</code></pre>\n<p>Calling <code>CardSurface(Text(\"hi\"))</code> returns the modified element. This\nis just composition — no framework involvement, no static\nregistration, no <code>Style.TargetType</code> check at runtime.</p>\n<p>For widely-reused style bundles, the <a href=\"/styling/\">styling</a> page\ndocuments the <code>UseNamedStyle</code> hook and the <code>.Named()</code> modifier, which\nlet you declare a named bag of modifiers once and apply it across the\napp. Theme-aware brush properties resolve through\n<a href=\"/theming-tokens/\"><code>ThemeRef</code></a> at reconcile time.</p>\n<h2 id=\"resourcedictionary--themeref--theme-tokens\">ResourceDictionary → ThemeRef + Theme tokens<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#resourcedictionary--themeref--theme-tokens\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-keyword\">struct</span> <span class=\"hljs-title\">ThemeRef</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> ResourceKey</span>)</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> <span class=\"hljs-built_in\">string</span> <span class=\"hljs-title\">ToString</span>()</span> => <span class=\"hljs-string\">$\"ThemeRef(<span class=\"hljs-subst\">{ResourceKey}</span>)\"</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Resolves this theme reference using the element's actual theme.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Walks the ThemeDictionaries in Application.Resources and MergedDictionaries</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> to find the brush matching the element's effective theme (which respects</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> per-element RequestedTheme overrides, not just the app-level theme).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">static</span> Brush? Resolve(<span class=\"hljs-built_in\">string</span> resourceKey, FrameworkElement fe)\n    {\n        <span class=\"hljs-keyword\">var</span> themeName = GetEffectiveThemeName(fe);\n        <span class=\"hljs-keyword\">return</span> ResolveForTheme(resourceKey, themeName);\n    }\n</code></pre>\n<p>A <code>ResourceDictionary</code> (especially merged into <code>Application.Resources</code>)\nis the XAML way to declare named resources — brushes, doubles, styles\n— and reference them via <code>{StaticResource Key}</code> or <code>{ThemeResource Key}</code>.\nThe latter resolves through the active <code>ThemeDictionaries[name]</code>.</p>\n<p>Reactor exposes the same WinUI theme dictionaries through\n<code>ThemeRef</code>, a readonly record struct holding a resource key. The\nstatic <a href=\"/theming-tokens/\"><code>Theme</code></a> class names the 35 most-used\ntokens (<code>Theme.Accent</code>, <code>Theme.CardBackground</code>, etc.); <code>Theme.Ref(key)</code>\nis the escape hatch for any other key. The reconciler walks\n<code>Application.Resources.ThemeDictionaries</code> exactly the way <code>{ThemeResource}</code>\ndoes, and re-resolves on <code>ActualThemeChanged</code> — so flipping the system\ntheme updates every <code>Theme.Accent</code> brush without a re-render.</p>\n<h2 id=\"visualstatemanager--interactionstates-modifier\">VisualStateManager → InteractionStates modifier<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#visualstatemanager--interactionstates-modifier\">#</a></h2>\n<p><code>VisualStateManager</code> in XAML drives state-machine animations: a control\ndeclares groups of named <code>VisualState</code>s (Normal / PointerOver /\nPressed / Disabled), and a state transition triggers the storyboard\nattached to the <code>VisualState</code>. Authors pick states by calling\n<code>VisualStateManager.GoToState</code>.</p>\n<p>Reactor's <code>.InteractionStates(...)</code> modifier on an element declares\nper-state property overrides directly. The reconciler installs WinUI\nvisual-state group plumbing on the materialized control and flips\nproperties when the visual state changes. There is no separate\nstoryboard authoring; transitions are described in C# alongside the\nelement. See the <a href=\"/animation/\">animation</a> page for the full surface\nand the <a href=\"/animation-pipeline/\">animation-pipeline</a> page for the\nreconciler-side detail.</p>\n<h2 id=\"storyboard--withanimation--animate-modifier\">Storyboard → WithAnimation / Animate modifier<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#storyboard--withanimation--animate-modifier\">#</a></h2>\n<p><code>Storyboard</code> is the WinUI animation primitive: a tree of timelines\nanimating dependency properties over time, started imperatively or\ndeclaratively via <code>VisualState</code>. The result is a long-lived\n<code>AnimationCollection</code> attached to the control.</p>\n<p>Reactor's <code>.WithAnimation(...)</code> and <code>.Animate(...)</code> modifiers on an\nelement describe the same animation as a record on the element. The\nreconciler unpacks the record into either an\n<code>ImplicitAnimationCollection</code> (composition-level transitions) or a\nkeyframe-driven property animation, depending on the mode. Stop the\nanimation by removing the modifier on the next render; the reconciler\ndetaches the collection.</p>\n<h2 id=\"inotifypropertychanged--hook-re-render\">INotifyPropertyChanged → hook re-render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#inotifypropertychanged--hook-re-render\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Observable</span>&#x3C;<span class=\"hljs-title\">T</span>> : <span class=\"hljs-title\">INotifyPropertyChanged</span>\n{\n    <span class=\"hljs-keyword\">private</span> T _value;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>() : <span class=\"hljs-title\">this</span>(<span class=\"hljs-params\"><span class=\"hljs-literal\">default</span>!</span>)</span> { }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>(<span class=\"hljs-params\">T initial</span>)</span> => _value = initial;\n\n    <span class=\"hljs-keyword\">public</span> T Value\n    {\n        <span class=\"hljs-keyword\">get</span> => _value;\n        <span class=\"hljs-keyword\">set</span>\n        {\n            <span class=\"hljs-keyword\">if</span> (EqualityComparer&#x3C;T>.Default.Equals(_value, <span class=\"hljs-keyword\">value</span>)) <span class=\"hljs-keyword\">return</span>;\n            _value = <span class=\"hljs-keyword\">value</span>;\n            PropertyChanged?.Invoke(<span class=\"hljs-keyword\">this</span>, _valueChangedArgs);\n        }\n    }\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> PropertyChangedEventHandler? PropertyChanged;\n</code></pre>\n<p><code>INotifyPropertyChanged</code> is XAML's contract for \"this property is\nobservable\". A binding subscribes to <code>PropertyChanged</code>; raising it\nre-pulls. Reactor's <code>UseObservable</code> hook is the migration bridge:\nhand it an existing <code>INotifyPropertyChanged</code> object, and the component\nsubscribes for the lifetime of the hook and re-renders on\n<code>PropertyChanged</code>. For pure Reactor state you never declare\n<code>INotifyPropertyChanged</code> — the hook setter is the signal.</p>\n<h2 id=\"mvvm-viewmodel--component-state--useobservable-bridge\">MVVM ViewModel → component state + UseObservable bridge<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mvvm-viewmodel--component-state--useobservable-bridge\">#</a></h2>\n<p>The MVVM ViewModel is the place XAML developers put \"things the page\nneeds to display and the commands the page can issue\". It implements\n<code>INotifyPropertyChanged</code>, holds <code>ICommand</code> instances, and stays alive\nacross navigations.</p>\n<p>Reactor doesn't separate VM from View. State lives in the component,\nin hooks. Commands are functions you close over. Pages persist via\n<a href=\"/persistence/\"><code>UsePersistedState</code></a>, not via a long-lived VM. When\nyou need a VM-shaped object — for shared logic across components,\nfor an existing service-layer model — author a plain class, raise\n<code>PropertyChanged</code> (or wrap a value in <a href=\"#\"><code>Observable&#x3C;T></code></a>), and let\neach consuming component subscribe via <code>UseObservable</code>. The component\nstill owns its rendered slice of state; the VM is the source.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"porting-a-xaml-page\">Porting a XAML page<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#porting-a-xaml-page\">#</a></h3>\n<p>The mechanical translation has four steps:</p>\n<ol>\n<li><strong>Identify VM properties used in the page.</strong> Each becomes a <code>UseState</code>\n(component-local) or a <code>UseObservable</code> over an existing INPC source.</li>\n<li><strong>Translate the XAML markup tree.</strong> Outer panel → <code>VStack</code> / <code>HStack</code>\n/ <code>Grid</code>; named elements stay positional inside the render; styles\nbecome modifier chains.</li>\n<li><strong>Translate bindings into closures.</strong> <code>{Binding Name}</code> → just read\nthe variable. <code>Mode=TwoWay</code> → controlled-input pair <code>(value, setter)</code>.</li>\n<li><strong>Translate commands.</strong> An <code>ICommand</code> becomes a regular function,\ntypically captured by the click handler closure. The\n<a href=\"/commanding/\">commanding</a> page documents the explicit shape if\nthe command must be sharable.</li>\n</ol>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>The <a href=\"/xaml-developers/\">Reactor for XAML Developers</a> cookbook has\nworked examples for the most common page shapes: settings, list-detail,\nmodal dialog. Use that as the recipe surface; this page is for when\nyou want to understand what's happening underneath.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"implementing-inpc-on-local-state-out-of-habit\">Implementing INPC on local state out of habit<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#implementing-inpc-on-local-state-out-of-habit\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ProfilePage</span> : <span class=\"hljs-title\">Component</span>, <span class=\"hljs-title\">INotifyPropertyChanged</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">string</span> _name = <span class=\"hljs-string\">\"\"</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span> Name\n    {\n        <span class=\"hljs-keyword\">get</span> => _name;\n        <span class=\"hljs-keyword\">set</span> { _name = <span class=\"hljs-keyword\">value</span>; PropertyChanged?.Invoke(<span class=\"hljs-keyword\">this</span>, <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-keyword\">nameof</span>(Name))); }\n    }\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> PropertyChangedEventHandler? PropertyChanged;\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>The framework doesn't subscribe to <code>PropertyChanged</code> on a component —\nre-renders come from <code>UseState</code> setters or parent re-renders. The\nfield is invisible to the render loop, so updating <code>Name</code> does\nnothing. Move the value into <code>UseState</code> and the setter is the signal;\nthe INPC ceremony goes away.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Bindings don't exist.</strong> When you find yourself wondering \"how do I\nbind this\", stop and ask \"what state does this property read from?\"\nThe answer is a <code>UseState</code>, a <code>UseObservable</code>, or a prop — and the\ncontrol just reads the variable.</p>\n<p><strong><code>ThemeRef</code> is your <code>{ThemeResource}</code>.</strong> Anywhere you'd reach for a\nthemed brush from <code>Application.Resources</code>, use <code>Theme.Accent</code> or\n<code>Theme.Ref(key)</code>. The reconciler resolves the brush per element and\nre-resolves on theme change.</p>\n<p><strong>Don't translate XAML 1:1.</strong> A page with seven <code>UserControl</code>s and\ntwelve bindings often becomes a single component with four hooks. The\nmarkup-to-code translation collapses a lot of XAML's structure\nbecause the C# function body holds it all in one place.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/xaml-developers/\">Reactor for XAML Developers</a></strong> — Cookbook with worked page rewrites.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture Overview</a></strong> — The runtime diagram that places everything here.</li>\n<li><strong><a href=\"/theming-tokens/\">Theming Tokens</a></strong> — Full <code>ThemeRef</code> surface.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — The state-handling API surface.</li>\n<li><strong><a href=\"/reactivity-model/\">Reactivity Model</a></strong> — Why the push direction is the way it is.</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Winui3 . If you've spent years inside XAML, the question that keeps coming back is \"where did my bindings go?\" Microsoft.UI.Reactor (Reactor) renders the same WinUI controls your XAML pages renders — Button , TextBox , TabView , ItemsRepeater — and lays them out through the same panels. What changes is the source of truth for property values. In XAML, a control owns its dependency properties, and a binding expression listens to a view-model property and pulls the new value when PropertyChanged raises. In Reactor, a hook owns the state, the component re-renders to produce a new element tree, and the reconciler writes the property values onto the existing control. The two models solve the same problem (keep UI in sync with state) but locate the work in opposite places. Read this page once and the rest of the codebase falls into place: nothing in Architecture Overview , Hooks Internals , or Reconciliation hides a binding system. Reactor vs XAML # This page is for the XAML developer who wants the why . The Reactor for XAML Developers cookbook covers the how for common rewrite tasks (\"I have a Page with bindings; what does that become?\"). Indexed in two places: section 1 (Get Started) so a XAML developer hitting the docs cold finds it early, and section 9 (Under the hood) so it sits next to the architecture pages it complements. Pull vs push, one diagram # Concern XAML Reactor State location View-model property / dependency property Hook slot inside RenderContext State signal INotifyPropertyChanged.PropertyChanged Setter closure from UseState Property write BindingExpression.UpdateTarget pulls VM getter Reconciler writes after render Style application Style + Setter + triggers Modifier chain or Use* named-style hook Animation Storyboard defined in XAML, started imperatively .WithAnimation(...) modifier resolved by reconciler Layout state VisualStateManager + VisualState /Group XAML .InteractionStates(...) modifier on element Resources ResourceDictionary / ThemeDictionary ThemeRef record struct Lists ItemsControl + DataTemplate ForEach(items, item => …) builds element per item MVVM View-model class with INPC properties Function or class component with hooks Every row below explains the mapping in enough depth that the mechanical translation isn't a guess. Caveat: The pull/push contrast is a runtime distinction, not a feature checklist. Reactor still uses dependency properties — every WinUI control it materializes has its DPs set by the reconciler. What's gone is the binding expression that listens to a VM and pulls. If you attach a {Binding} to a control Reactor materialized (via an ElementRef cast or by setting it imperatively in an effect), you'll create a parallel reactive edge the reconciler doesn't know about, and the next reconcile pass will overwrite your property write. Drive properties through hook state and let the reconciler write them; do not bind onto controls Reactor owns. DependencyProperty → element record + modifier # XAML's dependency property system is the substrate for everything reactive in WinUI: bindings hang off DPs, styles set DPs, animations animate DPs, the VisualStateManager flips DPs. The DP itself is a slot on a control, with a metadata-driven default value, type coercion, change notifications, and inheritance through the visual tree. A Button.Content value is a DP; a Grid.Row attached property is a DP; theme colors come out of a DP-flavored resource system. public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } Reactor replaces the DP-slot-on-the-control with the Element record. ButtonElement (a record deriving from Element ) carries Content , OnClick , etc. as record fields. The modifier system folds layout and styling concerns into a separate ElementModifiers record threaded through record with updates. Attached properties (Grid.Row, Canvas.Left) live in a type-keyed Attached dictionary on the same record. private static T Modify &#x3C; T >( T el, ElementModifiers mods ) where T : Element => el with { Modifiers = el.Modifiers is not null ? el.Modifiers.Merge(mods) : mods }; private static T ModifyA11y &#x3C; T >( T el, AccessibilityModifiers a11y ) where T : Element { var existing = el.Modifiers?.Accessibility; var merged = existing is not null ? existing.Merge(a11y) : a11y; return Modify(el, new ElementModifiers { Accessibility = merged }); } private static T ModifyTheme &#x3C; T >( T el, string property, ThemeRef theme ) where T : Element { var bindings = el.ThemeBindings is not null ? new Dictionary&#x3C; string , ThemeRef>(el.ThemeBindings) { [property] = theme } : new Dictionary&#x3C; string , ThemeRef> { [property] = theme }; return el with { ThemeBindings = bindings }; } The chain Text(\"hi\").FontSize(24).Margin(8) produces one TextElement record with merged Modifiers . The reconciler reads both the typed fields and the modifiers when it patches the control. There is no per-property change notification: the comparison is record-level on the next render, and only differences become property writes. Binding → closure over state # A {Binding Name} in XAML installs a BindingExpression that holds a reference to the view model and the property path, subscribes to INotifyPropertyChanged , and on each PropertyChanged raise reads the source and writes the target DP. The binding is a long-lived object; mode ( OneWay , TwoWay , OneTime ), update trigger, converter, and fallback all live on it. public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; Reactor doesn't install anything. TextBlock(name) reads the current value of name (a plain C# variable that happens to come out of UseState ) and produces a TextElement with Text = name as a record field. When name changes, the setter writes the slot, the component re-renders, the new TextElement differs from the old, and the reconciler writes the new Text value onto the existing TextBlock . There is no binding object — the closure that produces the element is the binding. TwoWay shows up as the controlled-input pattern. A TextBox takes the current value and a setter callback; the user's input fires the callback, which writes the slot, which re-renders, which writes the new value back into the TextBox . Mode is implicit in whether you pass a setter. DataTemplate → function component # A DataTemplate is a recipe: given a data item, produce a control tree. It's authored in XAML and instantiated by the framework when items appear in an ItemsControl . The template can name elements and bind to properties on the item; the runtime keeps a pool and recycles. ForEach(items, item => Card(item)) The closure item => Card(item) is the template. It runs once per item per render, produces an element tree, and the child reconciler matches the elements against the previous render. Identity comes from position by default; .WithKey(id) on the item element preserves identity across reorders. ItemsRepeater is still under the hood — the ForEach materializes through an ElementFactory that recycles the real WinUI controls. Style → modifier composition / named style # Style in XAML applies a bag of property setters to every control that matches a TargetType (or carries the style key). The bag is keyed by DP, and BasedOn chains styles through inheritance. Reactor has two analogues. The most common is a plain method that takes an element and applies modifiers: static Element CardSurface ( Element child ) => child.Background(Theme.CardBackground).CornerRadius( 8 ).Padding( 16 ); Calling CardSurface(Text(\"hi\")) returns the modified element. This is just composition — no framework involvement, no static registration, no Style.TargetType check at runtime. For widely-reused style bundles, the styling page documents the UseNamedStyle hook and the .Named() modifier, which let you declare a named bag of modifiers once and apply it across the app. Theme-aware brush properties resolve through ThemeRef at reconcile time. ResourceDictionary → ThemeRef + Theme tokens # public readonly record struct ThemeRef ( string ResourceKey ) { public override string ToString () => $\"ThemeRef( {ResourceKey} )\" ; /// &#x3C;summary> /// Resolves this theme reference using the element's actual theme. /// Walks the ThemeDictionaries in Application.Resources and MergedDictionaries /// to find the brush matching the element's effective theme (which respects /// per-element RequestedTheme overrides, not just the app-level theme). /// &#x3C;/summary> internal static Brush? Resolve( string resourceKey, FrameworkElement fe) { var themeName = GetEffectiveThemeName(fe); return ResolveForTheme(resourceKey, themeName); } A ResourceDictionary (especially merged into Application.Resources ) is the XAML way to declare named resources — brushes, doubles, styles — and reference them via {StaticResource Key} or {ThemeResource Key} . The latter resolves through the active ThemeDictionaries[name] . Reactor exposes the same WinUI theme dictionaries through ThemeRef , a readonly record struct holding a resource key. The static Theme class names the 35 most-used tokens ( Theme.Accent , Theme.CardBackground , etc.); Theme.Ref(key) is the escape hatch for any other key. The reconciler walks Application.Resources.ThemeDictionaries exactly the way {ThemeResource} does, and re-resolves on ActualThemeChanged — so flipping the system theme updates every Theme.Accent brush without a re-render. VisualStateManager → InteractionStates modifier # VisualStateManager in XAML drives state-machine animations: a control declares groups of named VisualState s (Normal / PointerOver / Pressed / Disabled), and a state transition triggers the storyboard attached to the VisualState . Authors pick states by calling VisualStateManager.GoToState . Reactor's .InteractionStates(...) modifier on an element declares per-state property overrides directly. The reconciler installs WinUI visual-state group plumbing on the materialized control and flips properties when the visual state changes. There is no separate storyboard authoring; transitions are described in C# alongside the element. See the animation page for the full surface and the animation-pipeline page for the reconciler-side detail. Storyboard → WithAnimation / Animate modifier # Storyboard is the WinUI animation primitive: a tree of timelines animating dependency properties over time, started imperatively or declaratively via VisualState . The result is a long-lived AnimationCollection attached to the control. Reactor's .WithAnimation(...) and .Animate(...) modifiers on an element describe the same animation as a record on the element. The reconciler unpacks the record into either an ImplicitAnimationCollection (composition-level transitions) or a keyframe-driven property animation, depending on the mode. Stop the animation by removing the modifier on the next render; the reconciler detaches the collection. INotifyPropertyChanged → hook re-render # public sealed class Observable &#x3C; T > : INotifyPropertyChanged { private T _value; public Observable () : this ( default ! ) { } public Observable ( T initial ) => _value = initial; public T Value { get => _value; set { if (EqualityComparer&#x3C;T>.Default.Equals(_value, value )) return ; _value = value ; PropertyChanged?.Invoke( this , _valueChangedArgs); } } public event PropertyChangedEventHandler? PropertyChanged; INotifyPropertyChanged is XAML's contract for \"this property is observable\". A binding subscribes to PropertyChanged ; raising it re-pulls. Reactor's UseObservable hook is the migration bridge: hand it an existing INotifyPropertyChanged object, and the component subscribes for the lifetime of the hook and re-renders on PropertyChanged . For pure Reactor state you never declare INotifyPropertyChanged — the hook setter is the signal. MVVM ViewModel → component state + UseObservable bridge # The MVVM ViewModel is the place XAML developers put \"things the page needs to display and the commands the page can issue\". It implements INotifyPropertyChanged , holds ICommand instances, and stays alive across navigations. Reactor doesn't separate VM from View. State lives in the component, in hooks. Commands are functions you close over. Pages persist via UsePersistedState , not via a long-lived VM. When you need a VM-shaped object — for shared logic across components, for an existing service-layer model — author a plain class, raise PropertyChanged (or wrap a value in Observable&#x3C;T> ), and let each consuming component subscribe via UseObservable . The component still owns its rendered slice of state; the VM is the source. Patterns # Porting a XAML page # The mechanical translation has four steps: Identify VM properties used in the page. Each becomes a UseState (component-local) or a UseObservable over an existing INPC source. Translate the XAML markup tree. Outer panel → VStack / HStack / Grid ; named elements stay positional inside the render; styles become modifier chains. Translate bindings into closures. {Binding Name} → just read the variable. Mode=TwoWay → controlled-input pair (value, setter) . Translate commands. An ICommand becomes a regular function, typically captured by the click handler closure. The commanding page documents the explicit shape if the command must be sharable. public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; The Reactor for XAML Developers cookbook has worked examples for the most common page shapes: settings, list-detail, modal dialog. Use that as the recipe surface; this page is for when you want to understand what's happening underneath. Common Mistakes # Implementing INPC on local state out of habit # // Don't: public class ProfilePage : Component , INotifyPropertyChanged { private string _name = \"\" ; public string Name { get => _name; set { _name = value ; PropertyChanged?.Invoke( this , new ( nameof (Name))); } } public event PropertyChangedEventHandler? PropertyChanged; } public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; The framework doesn't subscribe to PropertyChanged on a component — re-renders come from UseState setters or parent re-renders. The field is invisible to the render loop, so updating Name does nothing. Move the value into UseState and the setter is the signal; the INPC ceremony goes away. Tips # Bindings don't exist. When you find yourself wondering \"how do I bind this\", stop and ask \"what state does this property read from?\" The answer is a UseState , a UseObservable , or a prop — and the control just reads the variable. ThemeRef is your {ThemeResource} . Anywhere you'd reach for a themed brush from Application.Resources , use Theme.Accent or Theme.Ref(key) . The reconciler resolves the brush per element and re-resolves on theme change. Don't translate XAML 1:1. A page with seven UserControl s and twelve bindings often becomes a single component with four hooks. The markup-to-code translation collapses a lot of XAML's structure because the C# function body holds it all in one place. Next Steps # Reactor for XAML Developers — Cookbook with worked page rewrites. Architecture Overview — The runtime diagram that places everything here. Theming Tokens — Full ThemeRef surface. Hooks — The state-handling API surface. Reactivity Model — Why the push direction is the way it is.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/reactor-vs-xaml.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/reactor-vs-xaml.md",
      "headings": [
        {
          "depth": 2,
          "id": "pull-vs-push-one-diagram",
          "text": "Pull vs push, one diagram"
        },
        {
          "depth": 2,
          "id": "dependencyproperty--element-record--modifier",
          "text": "DependencyProperty → element record + modifier"
        },
        {
          "depth": 2,
          "id": "binding--closure-over-state",
          "text": "Binding → closure over state"
        },
        {
          "depth": 2,
          "id": "datatemplate--function-component",
          "text": "DataTemplate → function component"
        },
        {
          "depth": 2,
          "id": "style--modifier-composition--named-style",
          "text": "Style → modifier composition / named style"
        },
        {
          "depth": 2,
          "id": "resourcedictionary--themeref--theme-tokens",
          "text": "ResourceDictionary → ThemeRef + Theme tokens"
        },
        {
          "depth": 2,
          "id": "visualstatemanager--interactionstates-modifier",
          "text": "VisualStateManager → InteractionStates modifier"
        },
        {
          "depth": 2,
          "id": "storyboard--withanimation--animate-modifier",
          "text": "Storyboard → WithAnimation / Animate modifier"
        },
        {
          "depth": 2,
          "id": "inotifypropertychanged--hook-re-render",
          "text": "INotifyPropertyChanged → hook re-render"
        },
        {
          "depth": 2,
          "id": "mvvm-viewmodel--component-state--useobservable-bridge",
          "text": "MVVM ViewModel → component state + UseObservable bridge"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "porting-a-xaml-page",
          "text": "Porting a XAML page"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "implementing-inpc-on-local-state-out-of-habit",
          "text": "Implementing INPC on local state out of habit"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Components",
      "path": "components.md",
      "slug": "/components/",
      "html": "<p>A component in Microsoft.UI.Reactor (Reactor) is a pure function from (state + props) to an element\ntree. <code>Render()</code> is called every time the component's state changes or its\nparent's render produces new props, and its only job is to return what the\nUI should look like <em>right now</em>. Reactor takes that return value, diffs it\nagainst the previous render via the <a href=\"/reconciliation/\">reconciler</a>, and\npatches the underlying WinUI controls in place — only the properties and\nchildren that actually changed get touched. The \"function\" framing is\nload-bearing: a component must not mutate props, must not write to external\nstate during render, and must call <a href=\"/hooks/\">hooks</a> in the same positional\norder on every render. Side effects belong in <a href=\"/effects/\"><code>UseEffect</code></a>;\nderived values belong in <a href=\"/hooks/\"><code>UseMemo</code></a> or inline; subscriptions\nbelong in effects with cleanup. Get those four rules right and a component\nbecomes the unit of composition that the rest of the framework is built on\n— class components for stateful nodes, function components via <code>Memo</code> for\ninline shapes, both composed into trees by being instantiated inside their\nparent's <code>Render()</code> return value.</p>\n<h1 id=\"components\">Components<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#components\">#</a></h1>\n<p>Components are the building blocks of a Reactor app. Each component is a class\nwith a <code>Render()</code> method that returns an element tree describing its UI.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Shape</th><th>Use when</th><th>Re-renders when</th></tr></thead><tbody><tr><td><code>class C : Component</code></td><td>Stateful node with no input from parent.</td><td>Internal state changes (default <code>ShouldUpdate()</code> returns <code>false</code> for propless components — parent re-renders alone don't propagate).</td></tr><tr><td><code>class C : Component&#x3C;TProps></code></td><td>Stateful node that takes a <code>record</code> of inputs.</td><td>Props compare unequal (records use structural equality), or internal state changes.</td></tr><tr><td><code>Memo(ctx => …)</code></td><td>Inline function component with its own hook state.</td><td>Internal state changes; with <code>deps</code> argument, also when any dep changes.</td></tr><tr><td><code>RenderEachTime(ctx => …)</code></td><td>Inline component that should always re-render with its parent.</td><td>Every parent render — opts out of memoization.</td></tr><tr><td><code>Component&#x3C;C, TProps>(new(…))</code></td><td>Instantiate a typed-props class component inside another component's render.</td><td>Driven by the rules above.</td></tr></tbody></table>\n<p>Override <code>ShouldUpdate(TProps? old, TProps? new)</code> to make a <code>Component&#x3C;TProps></code>\nignore certain prop changes (cosmetic-only label updates, for example). The\ndefault <code>Equals()</code>-based check on records is the right call for most cases.\nThe auto-generated components reference covers every member (coming soon —\nsee the <a href=\"/reference/hooks/\">Hooks reference</a> for the existing\nprototype); the rest of this page is the narrative.</p>\n<h2 id=\"basic-component\">Basic Component<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#basic-component\">#</a></h2>\n<p>Extend <code>Component</code> and override <code>Render()</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Greeting</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"World\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Hello, <span class=\"hljs-subst\">{name}</span>!\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Your name\"</span>)\n                .Width(<span class=\"hljs-number\">200</span>)\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/components/basic-component.png\" alt=\"Basic component\"></p>\n<p><code>Render()</code> is called every time state changes. You call <a href=\"/hooks/\">hooks</a> like <code>UseState</code>\nat the top, then return an element tree. Reactor diffs the result against the\nprevious render and patches only the controls that changed.</p>\n<h2 id=\"props-with-records\">Props with Records<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#props-with-records\">#</a></h2>\n<p>When a component needs input from its parent, define a C# record for its\nprops and extend <code>Component&#x3C;TProps></code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">AlertProps</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Title</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Message</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Severity</span> = <span class=\"hljs-string\">\"info\"</span>);\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Alert</span> : <span class=\"hljs-title\">Component</span>&#x3C;<span class=\"hljs-title\">AlertProps</span>>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> bg = Props.Severity <span class=\"hljs-keyword\">switch</span>\n        {\n            <span class=\"hljs-string\">\"error\"</span> => <span class=\"hljs-string\">\"#FDE7E9\"</span>,\n            <span class=\"hljs-string\">\"warning\"</span> => <span class=\"hljs-string\">\"#FFF4CE\"</span>,\n            _ => <span class=\"hljs-string\">\"#DFF6DD\"</span>\n        };\n\n        <span class=\"hljs-keyword\">return</span> Border(\n            VStack(<span class=\"hljs-number\">4</span>,\n                TextBlock(Props.Title).Bold(),\n                TextBlock(Props.Message)\n            ).Padding(<span class=\"hljs-number\">12</span>)\n        ).Background(bg).CornerRadius(<span class=\"hljs-number\">4</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/components/props-component.png\" alt=\"Props component\"></p>\n<p>Records give you immutable data with value equality. Reactor uses this for\nautomatic memoization — if the parent re-renders but the props haven't\nchanged structurally, the child skips its <code>Render()</code> call.</p>\n<p>Access props via <code>Props.PropertyName</code> inside <code>Render()</code>. The parent sets\nprops by assigning the <code>Props</code> property when creating the component instance.</p>\n<h2 id=\"factory-helpers-for-cleaner-call-sites\">Factory Helpers for Cleaner Call Sites<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#factory-helpers-for-cleaner-call-sites\">#</a></h2>\n<p><code>Component&#x3C;T, TProps>(new(...))</code> reads heavily at the call site, especially\nwhen nested in an element tree. The idiomatic Reactor pattern is to wrap each\nclass component in a free-function factory that matches the rest of the DSL:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Components</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> ComponentElement <span class=\"hljs-title\">Alert</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> title, <span class=\"hljs-built_in\">string</span> message,\n        <span class=\"hljs-built_in\">string</span> severity = <span class=\"hljs-string\">\"info\"</span></span>)</span> =>\n        Component&#x3C;<span class=\"hljs-keyword\">global</span>::Alert, AlertProps>(<span class=\"hljs-keyword\">new</span>(title, message, severity));\n}\n</code></pre>\n<p>With <code>using static Components;</code> at the top of the consuming file, the call\nsite collapses to a normal function call:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>Component&#x3C;Alert, AlertProps>(new(\"Saved\", \"Done\"))</code></td><td><code>Alert(\"Saved\", \"Done\")</code></td></tr><tr><td><code>Component&#x3C;Alert, AlertProps>(new(\"Hi\", \"x\", \"warn\"))</code></td><td><code>Alert(\"Hi\", \"x\", \"warn\")</code></td></tr></tbody></table>\n<p>The class <code>Alert</code> and the helper method <code>Alert</code> coexist in the same scope —\nC# resolves <code>Alert(args)</code> as a method call and <code>Component&#x3C;Alert, ...></code> as a\ntype reference, based on syntactic position.</p>\n<p>Conventions:</p>\n<ul>\n<li><strong>Match the component name.</strong> Helper <code>Alert</code> for class <code>Alert</code> reads like\nJSX: <code>Alert(...)</code> instead of <code>&#x3C;Alert ... /></code>.</li>\n<li><strong>Group helpers in a single static class</strong> named <code>Components</code> (or per\nfeature). One <code>using static</code> import per consuming file is plenty.</li>\n<li><strong>Skip <code>Create</code> / <code>Of</code> / <code>New</code> prefixes.</strong> Reactor's built-in element\nfactories (<code>Button</code>, <code>TextBlock</code>, <code>FlexRow</code>) all read as bare functions;\nuser helpers should match that grammar.</li>\n</ul>\n<h2 id=\"custom-shouldupdate\">Custom ShouldUpdate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-shouldupdate\">#</a></h2>\n<p>Override <code>ShouldUpdate</code> to control when a component re-renders:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">ExpensiveProps</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Label</span>, <span class=\"hljs-title\">int</span> <span class=\"hljs-title\">Value</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ExpensiveDisplay</span> : <span class=\"hljs-title\">Component</span>&#x3C;<span class=\"hljs-title\">ExpensiveProps</span>>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">override</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>(<span class=\"hljs-params\">\n        ExpensiveProps? oldProps, ExpensiveProps? newProps</span>)</span>\n    {\n        <span class=\"hljs-comment\">// Only re-render when the Value changes, ignore Label</span>\n        <span class=\"hljs-keyword\">return</span> oldProps?.Value != newProps?.Value;\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Value: <span class=\"hljs-subst\">{Props.Value}</span>\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Bold();\n    }\n}\n</code></pre>\n<p>The default behavior for <code>Component&#x3C;TProps></code> uses <code>Equals()</code> — which with\nrecords means structural equality. Override <code>ShouldUpdate</code> when you want\ncoarser control, like ignoring cosmetic prop changes.</p>\n<p>For propless <code>Component</code>, <code>ShouldUpdate()</code> returns <code>false</code> by default,\nmeaning the component only re-renders from its own state changes. Override\nand return <code>true</code> to re-render whenever the parent re-renders.</p>\n<h2 id=\"function-components\">Function Components<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#function-components\">#</a></h2>\n<p>Not everything needs a class. Use <code>Memo</code> for lightweight inline components\nwith their own hook state:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FunctionComponentDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Function components\"</span>),\n            <span class=\"hljs-comment\">// Memo: render once + own state changes (the common case).</span>\n            Memo(ctx =>\n            {\n                <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">on</span>, setOn) = ctx.UseState(<span class=\"hljs-literal\">false</span>);\n                <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">8</span>,\n                    ToggleSwitch(<span class=\"hljs-keyword\">on</span>, setOn),\n                    TextBlock(<span class=\"hljs-keyword\">on</span> ? <span class=\"hljs-string\">\"Active\"</span> : <span class=\"hljs-string\">\"Inactive\"</span>)\n                );\n            }),\n            <span class=\"hljs-comment\">// Memo with a dep: skip re-render when deps haven't changed.</span>\n            Memo(ctx =>\n            {\n                <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"I only re-render when deps change\"</span>)\n                    .Opacity(<span class=\"hljs-number\">0.6</span>);\n            }, <span class=\"hljs-string\">\"stable-dep\"</span>)\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/components/function-component.png\" alt=\"Function component\"></p>\n<ul>\n<li><strong><code>Memo(ctx => { ... })</code></strong> — an inline component with its own hook state.\nWith no <code>deps</code> argument, it renders once and re-renders only on its own\nstate changes. The <code>ctx</code> parameter is a <code>RenderContext</code> that provides\n<code>UseState</code>, <code>UseEffect</code>, and all other hooks.</li>\n<li><strong><code>Memo(ctx => { ... }, deps)</code></strong> — same as above, but also re-renders when\nany value in <code>deps</code> changes. Use this for expensive subtrees that depend on\nexternal props (see <a href=\"/hooks/\">Hooks</a> for <code>UseMemo</code>).</li>\n<li><strong><code>RenderEachTime(ctx => { ... })</code></strong> — opts the component back into\nre-rendering on every parent render. Use sparingly — it defeats memoization\nand can amplify render storms. Pick this only when you've decided you\n<em>need</em> the always-re-render behavior.</li>\n</ul>\n<blockquote>\n<p>The older <code>Func(ctx => ...)</code> factory still works but is soft-deprecated\n(<code>CS0618</code>) — replace it with <code>Memo(ctx => ...)</code> for the common case or\n<code>RenderEachTime(ctx => ...)</code> when you specifically want the always-re-render\nshape.</p>\n</blockquote>\n<p>You can also use a function component as the app root:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\">ReactorApp.Run(<span class=\"hljs-string\">\"Title\"</span>, ctx => {\n    <span class=\"hljs-keyword\">var</span> (n, setN) = ctx.UseState(<span class=\"hljs-number\">0</span>);\n    <span class=\"hljs-keyword\">return</span> Text(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{n}</span>\"</span>);\n}, width: <span class=\"hljs-number\">400</span>, height: <span class=\"hljs-number\">300</span>);\n</code></pre>\n<!-- /ai:lock -->\n<h2 id=\"composition\">Composition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#composition\">#</a></h2>\n<p>Build complex UIs by nesting components:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ComponentsApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> ScrollView(\n            VStack(<span class=\"hljs-number\">16</span>,\n                Heading(<span class=\"hljs-string\">\"Component Patterns\"</span>),\n                Component&#x3C;Greeting>(),\n                Component&#x3C;Alert, AlertProps>(<span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Success\"</span>, <span class=\"hljs-string\">\"It works!\"</span>)),\n                Component&#x3C;Alert, AlertProps>(<span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Oops\"</span>, <span class=\"hljs-string\">\"Something broke\"</span>,\n                    <span class=\"hljs-string\">\"error\"</span>)),\n                HStack(<span class=\"hljs-number\">8</span>,\n                    Button(<span class=\"hljs-string\">\"+1\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>)),\n                    Component&#x3C;ExpensiveDisplay, ExpensiveProps>(\n                        <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Counter\"</span>, count))\n                ),\n                Component&#x3C;FunctionComponentDemo>()\n            ).Padding(<span class=\"hljs-number\">24</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/components/composition.png\" alt=\"Composed components\"></p>\n<p>Each component manages its own state independently. The parent creates child\ncomponents with <code>new</code> and sets their <code>Props</code>. Reactor handles the rest —\nmounting, updating, and unmounting as the tree changes.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Identity stability across renders is what lets a component preserve its\nstate, its hook slots, and its WinUI control instance between renders. The\n<a href=\"/reconciliation/\">reconciler</a> matches children by <strong>position</strong> by default —\nthe first child in render N matches the first child in render N+1 regardless\nof what type it is. That's fine when a list is append-only and order is\nstable; it's catastrophic when items reorder, because the second-position\nstate cell now belongs to a different item. Apply\n<code>.WithKey(item.Id)</code> (or any stable key) on every child of a <code>ForEach</code> /\ncollection render so the reconciler matches by <strong>identity</strong> instead. The\n<a href=\"/reconciliation/#child-reconciler--keyed-vs-positional\">Reconciliation</a>\nchapter walks the four-phase keyed algorithm in full. The classic failure\nmode: a todo list where deleting item 2 makes item 3's checkbox state\nappear on item 2 — because positionally, slot 2 is still occupied, and the\nreconciler updated the existing component instead of mounting a new one.\nThe fix is one <code>.WithKey(item.Id)</code> call per row.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"composition-with-children\">Composition with children<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#composition-with-children\">#</a></h3>\n<p>Component nesting is the primary way Reactor composes UI — there's no\n\"slot\" or <code>ContentPresenter</code> concept; the parent's <code>Render()</code> directly\nreturns child element trees:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Card</span> : <span class=\"hljs-title\">Component</span>&#x3C;<span class=\"hljs-title\">CardProps</span>>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        Border(\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(Props.Title).Bold(),\n                Props.Body                 <span class=\"hljs-comment\">// any Element, including child components</span>\n            ).Padding(<span class=\"hljs-number\">12</span>)\n        ).CornerRadius(<span class=\"hljs-number\">8</span>).WithBorder(Theme.Stroke);\n}\n</code></pre>\n<p>The caller decides what to put inside — a <code>TextBlock</code>, a <code>Component&#x3C;Form></code>,\neven a runtime-built tree from <code>ForEach</code>. This is the equivalent of\nReact's <code>children</code> prop or XAML's <code>ContentControl</code>, but expressed in plain\nC# without any binding boilerplate.</p>\n<h3 id=\"render-props-equivalent\">Render-props equivalent<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render-props-equivalent\">#</a></h3>\n<p>When a component needs to delegate part of its render to the caller, take\na <code>Func&#x3C;…, Element></code> prop. The function gets called inside the\ncomponent's <code>Render()</code> and its return value is spliced in — same as\nReact's render props or the <code>DataTemplate</code> selector pattern:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">ListProps</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-title\">IReadOnlyList</span>&#x3C;<span class=\"hljs-title\">T</span>> <span class=\"hljs-title\">Items</span>, <span class=\"hljs-title\">Func</span>&#x3C;<span class=\"hljs-title\">T</span>, <span class=\"hljs-title\">Element</span>> <span class=\"hljs-title\">Render</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">List</span>&#x3C;<span class=\"hljs-title\">T</span>> : <span class=\"hljs-title\">Component</span>&#x3C;<span class=\"hljs-title\">ListProps</span>&#x3C;<span class=\"hljs-title\">T</span>>>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        VStack(<span class=\"hljs-number\">4</span>, ForEach(Props.Items, item => Props.Render(item).WithKey(item)));\n}\n</code></pre>\n<p>The caller picks the row shape — <code>item => TextBlock(item.Name)</code> or a full\nnested component — without the list component knowing anything about the\nitem type beyond the key.</p>\n<h3 id=\"lifted-state\">Lifted state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lifted-state\">#</a></h3>\n<p>When two siblings need to coordinate, push the state up to their common\nparent and pass <code>(value, setter)</code> down. This is the\n<a href=\"/recipes/master-detail/\">recipes/master-detail</a> pattern: the master list\nand the detail panel both react to a shared <code>selectedId</code>, owned by the\nparent. The siblings stay pure — they take props in, call back out. See\n<a href=\"/hooks/\">hooks</a> for the <code>UseState</code> shape this pattern composes from.</p>\n<h3 id=\"errorboundary-integration\">ErrorBoundary integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#errorboundary-integration\">#</a></h3>\n<p>Wrap a subtree in <code>ErrorBoundary</code> to catch exceptions from any of its\nchildren's <code>Render</code> calls. The boundary swaps in your fallback element\ninstead of letting the exception bubble up to the host, which would\nunmount the whole tree:</p>\n<pre><code class=\"hljs language-csharp\">ErrorBoundary(\n    fallback: ex => TextBlock(<span class=\"hljs-string\">$\"Crash: <span class=\"hljs-subst\">{ex.Message}</span>\"</span>).Foreground(Theme.Error),\n    child: Component&#x3C;RiskyView>()\n)\n</code></pre>\n<p>The <a href=\"/advanced/\">Advanced Patterns</a> page covers the lifecycle and\nrecovery contract; the point at the components layer is that boundaries\nare themselves components, composed into the tree at the granularity you\nwant isolation.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"side-effects-in-render\">Side effects in render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#side-effects-in-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    File.AppendAllText(<span class=\"hljs-string\">\"log.txt\"</span>, <span class=\"hljs-string\">\"rendered\\n\"</span>);  <span class=\"hljs-comment\">// I/O during render</span>\n    Globals.RenderCount++;                          <span class=\"hljs-comment\">// mutation during render</span>\n    <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"hi\"</span>);\n}\n</code></pre>\n<p><code>Render()</code> may be called any number of times per user action — once per\nstate change, plus diagnostic renders the <a href=\"/dev-tooling/\">devtools</a> may\ntrigger. Side effects must move into <a href=\"/effects/\"><code>UseEffect</code></a> so they\nrun exactly once per commit. Diagnostic counters belong in\n<a href=\"/hooks/\"><code>UseRef</code></a>, which mutates without scheduling a re-render.</p>\n<h3 id=\"mutating-props\">Mutating props<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mutating-props\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    Props.Items.Add(newItem); <span class=\"hljs-comment\">// mutates the parent's collection</span>\n    <span class=\"hljs-keyword\">return</span> ListView(...);\n}\n</code></pre>\n<p>Props are inputs from the parent — read-only by contract. Mutating the\nunderlying list bypasses the parent's state setter, so the parent never\nsees the change and doesn't re-render. Worse, the next time the parent\n<em>does</em> re-render and pass <code>Items</code> down again, your local mutation has\nsilently changed the parent's snapshot. The fix is to call back through\nthe prop's setter: take an <code>Action&#x3C;Item> OnAdd</code> prop and have the parent\nown the list via <a href=\"/hooks/\"><code>UseState</code></a>.</p>\n<h3 id=\"creating-components-inside-render\">Creating components inside render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#creating-components-inside-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LocalComponent</span> : <span class=\"hljs-title\">Component</span> { ... }     <span class=\"hljs-comment\">// not even valid C#, but</span>\n    <span class=\"hljs-keyword\">var</span> inline = (Component)CreateLocal();        <span class=\"hljs-comment\">// any \"build a class per render\" shape</span>\n    <span class=\"hljs-keyword\">return</span> Component&#x3C;inline>();\n}\n</code></pre>\n<p>The reconciler matches by type identity — a fresh component class per\nrender compares unequal to last render's class, so the child unmounts and\nremounts every commit, losing all hook state and WinUI control instances.\nDefine component classes at the top level of a file (or use <code>Memo(ctx => …)</code> for inline shapes, which the reconciler treats as a stable function\nidentity). The same anti-pattern in React loses state for the same reason\nand for the same root cause.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use records for props.</strong> They give you immutable data, value equality, and\n<code>with</code> expressions for free. Reactor's memoization depends on <code>Equals()</code> working\ncorrectly.</p>\n<p><strong>Prefer composition over deep inheritance.</strong> <code>Component</code> and\n<code>Component&#x3C;TProps></code> are the only base classes you need. Build complexity\nthrough nesting, not class hierarchies.</p>\n<p><strong>Use <code>Memo</code> for one-off components.</strong> If a component is only used in one\nplace and has simple state, an inline <code>Memo(ctx => ...)</code> avoids the class\nboilerplate while still memoizing its render.</p>\n<p><strong>Keep Render() pure.</strong> Don't mutate external state or perform I/O inside\n<code>Render()</code>. Use <a href=\"/effects/\"><code>UseEffect</code></a> for side effects. <code>Render()</code> should be a pure\nfunction from (state + props) to elements.</p>\n<p><strong>Name components after what they display, not what they do.</strong> <code>Alert</code>,\n<code>UserCard</code>, <code>SettingsPanel</code> — not <code>AlertHandler</code>, <code>UserManager</code>,\n<code>SettingsProcessor</code>.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Next: deep dive into UseState, UseReducer, UseEffect, and all the hooks</li>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — Previous: hot reload and preview mode for faster iteration</li>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — Arrange components with VStack, HStack, Grid, and responsive patterns</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Apply colors, typography, and themes to your components</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — How the diff matches components across renders and why keys matter</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — ErrorBoundary, Memo, .Set, custom hooks</li>\n</ul>",
      "text": "A component in Microsoft.UI.Reactor (Reactor) is a pure function from (state + props) to an element tree. Render() is called every time the component's state changes or its parent's render produces new props, and its only job is to return what the UI should look like right now . Reactor takes that return value, diffs it against the previous render via the reconciler , and patches the underlying WinUI controls in place — only the properties and children that actually changed get touched. The \"function\" framing is load-bearing: a component must not mutate props, must not write to external state during render, and must call hooks in the same positional order on every render. Side effects belong in UseEffect ; derived values belong in UseMemo or inline; subscriptions belong in effects with cleanup. Get those four rules right and a component becomes the unit of composition that the rest of the framework is built on — class components for stateful nodes, function components via Memo for inline shapes, both composed into trees by being instantiated inside their parent's Render() return value. Components # Components are the building blocks of a Reactor app. Each component is a class with a Render() method that returns an element tree describing its UI. Reference # Shape Use when Re-renders when class C : Component Stateful node with no input from parent. Internal state changes (default ShouldUpdate() returns false for propless components — parent re-renders alone don't propagate). class C : Component&#x3C;TProps> Stateful node that takes a record of inputs. Props compare unequal (records use structural equality), or internal state changes. Memo(ctx => …) Inline function component with its own hook state. Internal state changes; with deps argument, also when any dep changes. RenderEachTime(ctx => …) Inline component that should always re-render with its parent. Every parent render — opts out of memoization. Component&#x3C;C, TProps>(new(…)) Instantiate a typed-props class component inside another component's render. Driven by the rules above. Override ShouldUpdate(TProps? old, TProps? new) to make a Component&#x3C;TProps> ignore certain prop changes (cosmetic-only label updates, for example). The default Equals() -based check on records is the right call for most cases. The auto-generated components reference covers every member (coming soon — see the Hooks reference for the existing prototype); the rest of this page is the narrative. Basic Component # Extend Component and override Render() : class Greeting : Component { public override Element Render () { var (name, setName) = UseState( \"World\" ); return VStack( 12 , TextBlock( $\"Hello, {name} !\" ).FontSize( 20 ).Bold(), TextBox(name, setName, placeholderText: \"Your name\" ) .Width( 200 ) ).Padding( 16 ); } } Render() is called every time state changes. You call hooks like UseState at the top, then return an element tree. Reactor diffs the result against the previous render and patches only the controls that changed. Props with Records # When a component needs input from its parent, define a C# record for its props and extend Component&#x3C;TProps> : record AlertProps ( string Title , string Message , string Severity = \"info\" ); class Alert : Component &#x3C; AlertProps > { public override Element Render () { var bg = Props.Severity switch { \"error\" => \"#FDE7E9\" , \"warning\" => \"#FFF4CE\" , _ => \"#DFF6DD\" }; return Border( VStack( 4 , TextBlock(Props.Title).Bold(), TextBlock(Props.Message) ).Padding( 12 ) ).Background(bg).CornerRadius( 4 ); } } Records give you immutable data with value equality. Reactor uses this for automatic memoization — if the parent re-renders but the props haven't changed structurally, the child skips its Render() call. Access props via Props.PropertyName inside Render() . The parent sets props by assigning the Props property when creating the component instance. Factory Helpers for Cleaner Call Sites # Component&#x3C;T, TProps>(new(...)) reads heavily at the call site, especially when nested in an element tree. The idiomatic Reactor pattern is to wrap each class component in a free-function factory that matches the rest of the DSL: static class Components { public static ComponentElement Alert ( string title, string message, string severity = \"info\" ) => Component&#x3C; global ::Alert, AlertProps>( new (title, message, severity)); } With using static Components; at the top of the consuming file, the call site collapses to a normal function call: Before After Component&#x3C;Alert, AlertProps>(new(\"Saved\", \"Done\")) Alert(\"Saved\", \"Done\") Component&#x3C;Alert, AlertProps>(new(\"Hi\", \"x\", \"warn\")) Alert(\"Hi\", \"x\", \"warn\") The class Alert and the helper method Alert coexist in the same scope — C# resolves Alert(args) as a method call and Component&#x3C;Alert, ...> as a type reference, based on syntactic position. Conventions: Match the component name. Helper Alert for class Alert reads like JSX: Alert(...) instead of &#x3C;Alert ... /> . Group helpers in a single static class named Components (or per feature). One using static import per consuming file is plenty. Skip Create / Of / New prefixes. Reactor's built-in element factories ( Button , TextBlock , FlexRow ) all read as bare functions; user helpers should match that grammar. Custom ShouldUpdate # Override ShouldUpdate to control when a component re-renders: record ExpensiveProps ( string Label , int Value ); class ExpensiveDisplay : Component &#x3C; ExpensiveProps > { protected override bool ShouldUpdate ( ExpensiveProps? oldProps, ExpensiveProps? newProps ) { // Only re-render when the Value changes, ignore Label return oldProps?.Value != newProps?.Value; } public override Element Render () { return TextBlock( $\"Value: {Props.Value} \" ).FontSize( 18 ).Bold(); } } The default behavior for Component&#x3C;TProps> uses Equals() — which with records means structural equality. Override ShouldUpdate when you want coarser control, like ignoring cosmetic prop changes. For propless Component , ShouldUpdate() returns false by default, meaning the component only re-renders from its own state changes. Override and return true to re-render whenever the parent re-renders. Function Components # Not everything needs a class. Use Memo for lightweight inline components with their own hook state: class FunctionComponentDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"Function components\" ), // Memo: render once + own state changes (the common case). Memo(ctx => { var ( on , setOn) = ctx.UseState( false ); return HStack( 8 , ToggleSwitch( on , setOn), TextBlock( on ? \"Active\" : \"Inactive\" ) ); }), // Memo with a dep: skip re-render when deps haven't changed. Memo(ctx => { return TextBlock( \"I only re-render when deps change\" ) .Opacity( 0.6 ); }, \"stable-dep\" ) ).Padding( 16 ); } } Memo(ctx => { ... }) — an inline component with its own hook state. With no deps argument, it renders once and re-renders only on its own state changes. The ctx parameter is a RenderContext that provides UseState , UseEffect , and all other hooks. Memo(ctx => { ... }, deps) — same as above, but also re-renders when any value in deps changes. Use this for expensive subtrees that depend on external props (see Hooks for UseMemo ). RenderEachTime(ctx => { ... }) — opts the component back into re-rendering on every parent render. Use sparingly — it defeats memoization and can amplify render storms. Pick this only when you've decided you need the always-re-render behavior. The older Func(ctx => ...) factory still works but is soft-deprecated ( CS0618 ) — replace it with Memo(ctx => ...) for the common case or RenderEachTime(ctx => ...) when you specifically want the always-re-render shape. You can also use a function component as the app root: ReactorApp.Run( \"Title\" , ctx => { var (n, setN) = ctx.UseState( 0 ); return Text( $\" {n} \" ); }, width: 400 , height: 300 ); Composition # Build complex UIs by nesting components: class ComponentsApp : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return ScrollView( VStack( 16 , Heading( \"Component Patterns\" ), Component&#x3C;Greeting>(), Component&#x3C;Alert, AlertProps>( new ( \"Success\" , \"It works!\" )), Component&#x3C;Alert, AlertProps>( new ( \"Oops\" , \"Something broke\" , \"error\" )), HStack( 8 , Button( \"+1\" , () => setCount(count + 1 )), Component&#x3C;ExpensiveDisplay, ExpensiveProps>( new ( \"Counter\" , count)) ), Component&#x3C;FunctionComponentDemo>() ).Padding( 24 ) ); } } Each component manages its own state independently. The parent creates child components with new and sets their Props . Reactor handles the rest — mounting, updating, and unmounting as the tree changes. Caveat: Identity stability across renders is what lets a component preserve its state, its hook slots, and its WinUI control instance between renders. The reconciler matches children by position by default — the first child in render N matches the first child in render N+1 regardless of what type it is. That's fine when a list is append-only and order is stable; it's catastrophic when items reorder, because the second-position state cell now belongs to a different item. Apply .WithKey(item.Id) (or any stable key) on every child of a ForEach / collection render so the reconciler matches by identity instead. The Reconciliation chapter walks the four-phase keyed algorithm in full. The classic failure mode: a todo list where deleting item 2 makes item 3's checkbox state appear on item 2 — because positionally, slot 2 is still occupied, and the reconciler updated the existing component instead of mounting a new one. The fix is one .WithKey(item.Id) call per row. Patterns # Composition with children # Component nesting is the primary way Reactor composes UI — there's no \"slot\" or ContentPresenter concept; the parent's Render() directly returns child element trees: class Card : Component &#x3C; CardProps > { public override Element Render () => Border( VStack( 8 , TextBlock(Props.Title).Bold(), Props.Body // any Element, including child components ).Padding( 12 ) ).CornerRadius( 8 ).WithBorder(Theme.Stroke); } The caller decides what to put inside — a TextBlock , a Component&#x3C;Form> , even a runtime-built tree from ForEach . This is the equivalent of React's children prop or XAML's ContentControl , but expressed in plain C# without any binding boilerplate. Render-props equivalent # When a component needs to delegate part of its render to the caller, take a Func&#x3C;…, Element> prop. The function gets called inside the component's Render() and its return value is spliced in — same as React's render props or the DataTemplate selector pattern: record ListProps &#x3C; T >( IReadOnlyList &#x3C; T > Items , Func &#x3C; T , Element > Render ); class List &#x3C; T > : Component &#x3C; ListProps &#x3C; T >> { public override Element Render () => VStack( 4 , ForEach(Props.Items, item => Props.Render(item).WithKey(item))); } The caller picks the row shape — item => TextBlock(item.Name) or a full nested component — without the list component knowing anything about the item type beyond the key. Lifted state # When two siblings need to coordinate, push the state up to their common parent and pass (value, setter) down. This is the recipes/master-detail pattern: the master list and the detail panel both react to a shared selectedId , owned by the parent. The siblings stay pure — they take props in, call back out. See hooks for the UseState shape this pattern composes from. ErrorBoundary integration # Wrap a subtree in ErrorBoundary to catch exceptions from any of its children's Render calls. The boundary swaps in your fallback element instead of letting the exception bubble up to the host, which would unmount the whole tree: ErrorBoundary( fallback: ex => TextBlock( $\"Crash: {ex.Message} \" ).Foreground(Theme.Error), child: Component&#x3C;RiskyView>() ) The Advanced Patterns page covers the lifecycle and recovery contract; the point at the components layer is that boundaries are themselves components, composed into the tree at the granularity you want isolation. Common Mistakes # Side effects in render # // Don't: public override Element Render () { File.AppendAllText( \"log.txt\" , \"rendered\\n\" ); // I/O during render Globals.RenderCount++; // mutation during render return TextBlock( \"hi\" ); } Render() may be called any number of times per user action — once per state change, plus diagnostic renders the devtools may trigger. Side effects must move into UseEffect so they run exactly once per commit. Diagnostic counters belong in UseRef , which mutates without scheduling a re-render. Mutating props # // Don't: public override Element Render () { Props.Items.Add(newItem); // mutates the parent's collection return ListView(...); } Props are inputs from the parent — read-only by contract. Mutating the underlying list bypasses the parent's state setter, so the parent never sees the change and doesn't re-render. Worse, the next time the parent does re-render and pass Items down again, your local mutation has silently changed the parent's snapshot. The fix is to call back through the prop's setter: take an Action&#x3C;Item> OnAdd prop and have the parent own the list via UseState . Creating components inside render # // Don't: public override Element Render () { class LocalComponent : Component { ... } // not even valid C#, but var inline = (Component)CreateLocal(); // any \"build a class per render\" shape return Component&#x3C;inline>(); } The reconciler matches by type identity — a fresh component class per render compares unequal to last render's class, so the child unmounts and remounts every commit, losing all hook state and WinUI control instances. Define component classes at the top level of a file (or use Memo(ctx => …) for inline shapes, which the reconciler treats as a stable function identity). The same anti-pattern in React loses state for the same reason and for the same root cause. Tips # Use records for props. They give you immutable data, value equality, and with expressions for free. Reactor's memoization depends on Equals() working correctly. Prefer composition over deep inheritance. Component and Component&#x3C;TProps> are the only base classes you need. Build complexity through nesting, not class hierarchies. Use Memo for one-off components. If a component is only used in one place and has simple state, an inline Memo(ctx => ...) avoids the class boilerplate while still memoizing its render. Keep Render() pure. Don't mutate external state or perform I/O inside Render() . Use UseEffect for side effects. Render() should be a pure function from (state + props) to elements. Name components after what they display, not what they do. Alert , UserCard , SettingsPanel — not AlertHandler , UserManager , SettingsProcessor . Next Steps # Hooks — Next: deep dive into UseState, UseReducer, UseEffect, and all the hooks Dev Tooling — Previous: hot reload and preview mode for faster iteration Layout — Arrange components with VStack, HStack, Grid, and responsive patterns Styling and Theming — Apply colors, typography, and themes to your components Reconciliation — How the diff matches components across renders and why keys matter Advanced Patterns — ErrorBoundary, Memo, .Set, custom hooks",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/components.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/components.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "basic-component",
          "text": "Basic Component"
        },
        {
          "depth": 2,
          "id": "props-with-records",
          "text": "Props with Records"
        },
        {
          "depth": 2,
          "id": "factory-helpers-for-cleaner-call-sites",
          "text": "Factory Helpers for Cleaner Call Sites"
        },
        {
          "depth": 2,
          "id": "custom-shouldupdate",
          "text": "Custom ShouldUpdate"
        },
        {
          "depth": 2,
          "id": "function-components",
          "text": "Function Components"
        },
        {
          "depth": 2,
          "id": "composition",
          "text": "Composition"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "composition-with-children",
          "text": "Composition with children"
        },
        {
          "depth": 3,
          "id": "render-props-equivalent",
          "text": "Render-props equivalent"
        },
        {
          "depth": 3,
          "id": "lifted-state",
          "text": "Lifted state"
        },
        {
          "depth": 3,
          "id": "errorboundary-integration",
          "text": "ErrorBoundary integration"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "side-effects-in-render",
          "text": "Side effects in render"
        },
        {
          "depth": 3,
          "id": "mutating-props",
          "text": "Mutating props"
        },
        {
          "depth": 3,
          "id": "creating-components-inside-render",
          "text": "Creating components inside render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Hooks",
      "path": "hooks.md",
      "slug": "/hooks/",
      "html": "<p>Hooks are the reactivity contract for a <a href=\"/components/\">component</a>. A hook is a\npositional slot — when <code>Render()</code> runs, Microsoft.UI.Reactor (Reactor) walks each <code>Use*</code> call in order\nand looks up the matching cell in a per-component slot table held on the\n<a href=\"/hooks-internals/\"><code>RenderContext</code></a>. The first <a href=\"/reference/hooks/UseState/\">UseState</a> call is\nslot 0, the second is slot 1, and so on; the setter the hook returns closes\nover its slot index and writes back to that cell when you call it. Hooks\nreplace what classic XAML / WPF apps build with <code>DependencyProperty</code>,\n<code>INotifyPropertyChanged</code>, view models, and lifecycle methods — instead of\nthose four mechanisms, you keep state with <code>UseState</code>, derive values with\n<code>UseMemo</code>, run side effects with <a href=\"/effects/\"><code>UseEffect</code></a>, share data\nwithout prop drilling via <a href=\"/context/\"><code>UseContext</code></a>, and persist values\nacross launches with <a href=\"/persistence/\"><code>UsePersisted</code></a>. Every hook on this\npage reads as a function call inside <code>Render()</code> and writes back through a\nsetter closure; understanding that single shape makes the rest of Reactor\nfall out as composition.</p>\n<h1 id=\"hooks\">Hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks\">#</a></h1>\n<p>Hooks are functions you call inside <a href=\"/components/\"><code>Render()</code></a> to manage state, side effects,\nand memoization. They replace the need for view models, event handlers, and\nlifecycle methods.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Returns</th><th>Purpose</th></tr></thead><tbody><tr><td><a href=\"/reference/hooks/UseState/\">UseState</a></td><td><code>(T value, Action&#x3C;T> set)</code></td><td>Reactive state — re-renders on <code>set</code>.</td></tr><tr><td><a href=\"/reference/hooks/UseReducer/\">UseReducer</a></td><td><code>(T value, Action&#x3C;Func&#x3C;T,T>> update)</code> <em>or</em> <code>(TState, Action&#x3C;TAction>)</code></td><td>Functional or Redux-style updates.</td></tr><tr><td><a href=\"/reference/hooks/UseEffect/\">UseEffect</a></td><td><code>void</code></td><td>Side effects after commit. With <code>Func&#x3C;Action></code> overload, runs the cleanup before the next effect and on unmount.</td></tr><tr><td><a href=\"/reference/hooks/UseMemo/\">UseMemo</a></td><td><code>T</code></td><td>Cached computation; re-runs when any <code>deps</code> entry compares unequal.</td></tr><tr><td><a href=\"/reference/hooks/UseRef/\">UseRef</a></td><td><code>Ref&#x3C;T></code> with mutable <code>.Current</code></td><td>Persists across renders <strong>without</strong> re-rendering on change.</td></tr><tr><td><a href=\"/reference/hooks/UseCallback/\">UseCallback</a></td><td><code>Action</code></td><td>Stable delegate identity across renders.</td></tr><tr><td><a href=\"/reference/hooks/UseContext/\">UseContext</a></td><td><code>T</code></td><td>Read the ambient <a href=\"/context/\">Context</a> value.</td></tr><tr><td><a href=\"/reference/hooks/UseObservable/\">UseObservable</a></td><td><code>T</code></td><td>Re-render when a tracked <code>INotifyPropertyChanged</code> source raises a change.</td></tr><tr><td><a href=\"/reference/hooks/UseResource/\">UseResource</a></td><td><code>AsyncValue&#x3C;T></code></td><td>Cached async read (see <a href=\"/async-resources/\">Async Resources</a>).</td></tr><tr><td><a href=\"/reference/hooks/UsePersisted/\">UsePersisted</a></td><td><code>(T, Action&#x3C;T>)</code></td><td><code>UseState</code> that survives app launches (see <a href=\"/persistence/\">Persistence</a>).</td></tr></tbody></table>\n<p>Every hook on this page is summarized again in the auto-generated\n<a href=\"/reference/hooks/\">hooks reference</a>; the rest of the page is the\nnarrative.</p>\n<h2 id=\"usestate\">UseState<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usestate\">#</a></h2>\n<p>The most common hook. Returns the current value and a setter function:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StateDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (color, setColor) = UseState(<span class=\"hljs-string\">\"#0078D4\"</span>);\n        <span class=\"hljs-keyword\">var</span> (size, setSize) = UseState(<span class=\"hljs-number\">20.0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseState\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Sample text\"</span>).FontSize(size).Foreground(color),\n            TextBox(color, setColor, placeholderText: <span class=\"hljs-string\">\"#hex color\"</span>)\n                .Width(<span class=\"hljs-number\">150</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Size:\"</span>),\n                Slider(size, <span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">48</span>, setSize).Width(<span class=\"hljs-number\">200</span>)\n            )\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/usestate.png\" alt=\"UseState demo\"></p>\n<p>Call <code>setColor(\"#FF0000\")</code> and Reactor re-renders the component with the new\nvalue. The setter also accepts a function: <code>setSize(s => s + 1)</code> — this is\nsafer when the update depends on the previous value.</p>\n<h2 id=\"usereducer-functional\">UseReducer (Functional)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usereducer-functional\">#</a></h2>\n<p>When your new state depends on the old state, <code>UseReducer</code> is cleaner than\n<code>UseState</code>. The updater receives a <code>Func&#x3C;T, T></code> — a function that transforms\nthe previous value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ReducerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n        <span class=\"hljs-keyword\">var</span> (input, setInput) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseReducer\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(input, setInput, placeholderText: <span class=\"hljs-string\">\"Add item\"</span>)\n                    .Width(<span class=\"hljs-number\">180</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(input)) <span class=\"hljs-keyword\">return</span>;\n                    updateItems(list =>\n                        <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>(list) { input });\n                    setInput(<span class=\"hljs-string\">\"\"</span>);\n                }),\n                Button(<span class=\"hljs-string\">\"Clear\"</span>, () =>\n                    updateItems(_ => <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>()))\n            ),\n            ForEach(items, item => TextBlock(<span class=\"hljs-string\">$\"  - <span class=\"hljs-subst\">{item}</span>\"</span>))\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/usereducer.png\" alt=\"UseReducer demo\"></p>\n<p><code>updateItems(list => new List&#x3C;string>(list) { input })</code> appends to the list\nby creating a new copy. This avoids mutation bugs — you always produce a new\nvalue from the old one.</p>\n<h2 id=\"usereducer-redux-style\">UseReducer (Redux-Style)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usereducer-redux-style\">#</a></h2>\n<p>For complex state with multiple action types, use the Redux-style overload.\nDefine a state record, action types, and a reducer function:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">CounterState</span>(<span class=\"hljs-title\">int</span> <span class=\"hljs-title\">Count</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">LastAction</span>);\n<span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">CounterAction</span>;\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Increment</span> : <span class=\"hljs-title\">CounterAction</span>;  <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Decrement</span> : <span class=\"hljs-title\">CounterAction</span>;\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Reset</span> : <span class=\"hljs-title\">CounterAction</span>;\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ReduxReducerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (state, dispatch) = UseReducer(\n            (CounterState s, CounterAction a) => a <span class=\"hljs-keyword\">switch</span> {\n                Increment => s <span class=\"hljs-keyword\">with</span> { Count = s.Count + <span class=\"hljs-number\">1</span>, LastAction = <span class=\"hljs-string\">\"+\"</span> },\n                Decrement => s <span class=\"hljs-keyword\">with</span> { Count = s.Count - <span class=\"hljs-number\">1</span>, LastAction = <span class=\"hljs-string\">\"-\"</span> },\n                Reset => <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">0</span>, <span class=\"hljs-string\">\"reset\"</span>), _ => s\n            }, <span class=\"hljs-keyword\">new</span> CounterState(<span class=\"hljs-number\">0</span>, <span class=\"hljs-string\">\"none\"</span>));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseReducer (Redux-style)\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{state.Count}</span>  (last: <span class=\"hljs-subst\">{state.LastAction}</span>)\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).Bold(),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"-\"</span>, () => dispatch(<span class=\"hljs-keyword\">new</span> Decrement())),\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () => dispatch(<span class=\"hljs-keyword\">new</span> Reset())),\n                Button(<span class=\"hljs-string\">\"+\"</span>, () => dispatch(<span class=\"hljs-keyword\">new</span> Increment()))\n            )\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/usereducer-redux.png\" alt=\"Redux-style reducer\"></p>\n<p>The reducer <code>(state, action) => newState</code> is a pure function. Each action type\nmaps to a state transformation. This pattern scales well — adding new actions\ndoesn't change existing logic.</p>\n<h2 id=\"useeffect\">UseEffect<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useeffect\">#</a></h2>\n<p>Run side effects (timers, subscriptions, async work) after a render. The\ndependencies array controls when the effect re-runs:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">EffectDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (seconds, updateSeconds) = UseReducer(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (running, setRunning) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">if</span> (!running) <span class=\"hljs-keyword\">return</span> () => { };\n            <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n            <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n            _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(cts.Token))\n                    updateSeconds(s => s + <span class=\"hljs-number\">1</span>);\n            });\n            <span class=\"hljs-keyword\">return</span> () => { cts.Cancel(); timer.Dispose(); };\n        }, running);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseEffect\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Elapsed: <span class=\"hljs-subst\">{seconds}</span>s\"</span>).FontSize(<span class=\"hljs-number\">18</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(running ? <span class=\"hljs-string\">\"Stop\"</span> : <span class=\"hljs-string\">\"Start\"</span>, () => setRunning(!running)),\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () => updateSeconds(_ => <span class=\"hljs-number\">0</span>))\n            )\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/useeffect.png\" alt=\"UseEffect timer\"></p>\n<p>Key details:</p>\n<ul>\n<li>The effect runs <strong>after</strong> the render completes, not during.</li>\n<li>Return a <strong>cleanup function</strong> to dispose resources. Reactor calls it before\nre-running the effect and when the component unmounts.</li>\n<li><strong>Empty dependencies</strong> <code>UseEffect(() => { ... })</code> — runs once on mount.</li>\n<li><strong>With dependencies</strong> <code>UseEffect(() => { ... }, running)</code> — runs when\n<code>running</code> changes.</li>\n</ul>\n<h2 id=\"usememo\">UseMemo<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usememo\">#</a></h2>\n<p>Cache an expensive computation so it only recalculates when its inputs change:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MemoDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (input, setInput) = UseState(<span class=\"hljs-string\">\"Hello, Reactor!\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> stats = UseMemo(() => <span class=\"hljs-keyword\">new</span>\n        {\n            Chars = input.Length,\n            Words = input.Split(<span class=\"hljs-string\">' '</span>,\n                StringSplitOptions.RemoveEmptyEntries).Length,\n            Upper = input.ToUpperInvariant()\n        }, input);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseMemo\"</span>),\n            TextBox(input, setInput).Width(<span class=\"hljs-number\">250</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Characters: <span class=\"hljs-subst\">{stats.Chars}</span>, Words: <span class=\"hljs-subst\">{stats.Words}</span>\"</span>),\n            Caption(<span class=\"hljs-string\">$\"Uppercased: <span class=\"hljs-subst\">{stats.Upper}</span>\"</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/usememo.png\" alt=\"UseMemo demo\"></p>\n<p><code>UseMemo</code> compares the dependency values between renders. If they haven't\nchanged, it returns the cached result. Use it for string processing, filtering\nlarge lists, or any computation you don't want to repeat every render.</p>\n<h2 id=\"useref\">UseRef<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useref\">#</a></h2>\n<p>Store a mutable value that persists across renders without triggering\nre-renders:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RefDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">value</span>, setValue) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> renderCount = UseRef(<span class=\"hljs-number\">0</span>);\n        renderCount.Current++;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseRef\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Render count: <span class=\"hljs-subst\">{renderCount.Current}</span>\"</span>).SemiBold(),\n            TextBox(<span class=\"hljs-keyword\">value</span>, setValue, placeholderText: <span class=\"hljs-string\">\"Type to trigger renders\"</span>)\n                .Width(<span class=\"hljs-number\">250</span>),\n            Caption(<span class=\"hljs-string\">\"UseRef persists across renders without causing them\"</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/useref.png\" alt=\"UseRef demo\"></p>\n<p><code>UseRef</code> returns a <code>Ref&#x3C;T></code> with a <code>.Current</code> property. Changing <code>.Current</code>\ndoes <strong>not</strong> cause a re-render. This is useful for:</p>\n<ul>\n<li>Counting renders</li>\n<li>Storing previous values for comparison</li>\n<li>Holding references to timers or cancellation tokens</li>\n</ul>\n<h2 id=\"usecallback\">UseCallback<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usecallback\">#</a></h2>\n<p>Stabilize a callback reference so child components don't re-render\nunnecessarily:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CallbackDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, updateCount) = UseReducer(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (label, setLabel) = UseState(<span class=\"hljs-string\">\"Click me\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> stableIncrement = UseCallback(\n            () => updateCount(c => c + <span class=\"hljs-number\">1</span>), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseCallback\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>).FontSize(<span class=\"hljs-number\">18</span>),\n            TextBox(label, setLabel, placeholderText: <span class=\"hljs-string\">\"Button label\"</span>)\n                .Width(<span class=\"hljs-number\">200</span>),\n            Button(label, stableIncrement),\n            Caption(<span class=\"hljs-string\">\"The callback identity stays stable across renders\"</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks/usecallback.png\" alt=\"UseCallback demo\"></p>\n<p>Without <code>UseCallback</code>, the lambda <code>() => setCount(c => c + 1)</code> would be a new\nobject every render. <code>UseCallback</code> returns the same delegate instance as long\nas the dependencies haven't changed. This matters when passing callbacks to\nmemoized <a href=\"/components/\">child components</a>.</p>\n<h2 id=\"updating-state-from-background-work\">Updating State From Background Work<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#updating-state-from-background-work\">#</a></h2>\n<p>Once the host is bootstrapped, <code>UseState</code> and <code>UseReducer</code> setters are safe to\ncall from any thread. When you invoke a setter from a background task — inside\n<code>Task.Run</code>, from a <code>PeriodicTimer</code> loop, from a network callback, or after\n<code>await ... ConfigureAwait(false)</code> — the setter automatically marshals the write\nand the resulting re-render onto the UI dispatcher. You write the same code\nyou'd write on the UI thread:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (seconds, setSeconds) = UseState(<span class=\"hljs-number\">0</span>);\n\n    UseEffect(() =>\n    {\n        <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n        _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n        {\n            <span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n            <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(cts.Token))\n                setSeconds(s => s + <span class=\"hljs-number\">1</span>);   <span class=\"hljs-comment\">// auto-marshals to the UI thread</span>\n        });\n        <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n    });\n\n    <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Elapsed: <span class=\"hljs-subst\">{seconds}</span>s\"</span>);\n}\n</code></pre>\n<!-- /ai:lock -->\n<p>Each cross-thread setter call costs one <code>DispatcherQueue.TryEnqueue</code> —\nmicroseconds, not free, but vastly cheaper than the bugs you'd hit writing the\nfield directly from a worker. If you need many concurrent setters to apply\nin-place rather than serialize through the UI thread (typical for ingest loops\nthat hammer the same hook from multiple producers), pass <code>threadSafe: true</code> to\nthe hook:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>, threadSafe: <span class=\"hljs-literal\">true</span>);\n<span class=\"hljs-keyword\">var</span> (sum, addToSum) = UseReducer(<span class=\"hljs-number\">0</span>, threadSafe: <span class=\"hljs-literal\">true</span>);\n</code></pre>\n<!-- /ai:lock -->\n<p><code>threadSafe: true</code> switches the hook to a per-cell lock: concurrent writers\nserialize on the lock instead of queuing through the UI dispatcher, and reads\ninside the setter (the <code>prev</code> argument of a reducer) see the latest committed\nwrite rather than a snapshot from the last UI tick.</p>\n<blockquote>\n<p><strong>When auto-marshal can't help.</strong> The setter needs a captured\n<code>ReactorApp.UIDispatcher</code> to marshal onto. In unit-test / headless contexts\nthat drive <code>RenderContext</code> directly, or before the first host has been\nbootstrapped, a cross-thread setter call throws <code>InvalidOperationException</code>\ninstead of silently racing. The setter also throws if the dispatcher refuses\nthe marshaled call (e.g., during shutdown). Cancel background producers in\nyour effect cleanup so they stop before the window closes.</p>\n</blockquote>\n<h2 id=\"hook-rules\">Hook Rules<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hook-rules\">#</a></h2>\n<p>Hooks must be called <strong>in the same order</strong> every render. Reactor tracks hooks by\ntheir position in the call sequence — the first <code>UseState</code> call always maps to\nthe first state slot, the second to the second, and so on. The internal walk\nof <code>_hookIndex</code> against <code>_hooks[currentIndex]</code> is described in\n<a href=\"/hooks-internals/\">Hooks Internals</a>.</p>\n<p><strong>Do:</strong></p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (a, setA) = UseState(<span class=\"hljs-number\">0</span>);     <span class=\"hljs-comment\">// always first</span>\n    <span class=\"hljs-keyword\">var</span> (b, setB) = UseState(<span class=\"hljs-string\">\"\"</span>);    <span class=\"hljs-comment\">// always second</span>\n    UseEffect(() => { ... }, a);     <span class=\"hljs-comment\">// always third</span>\n    <span class=\"hljs-keyword\">return</span> Text(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{a}</span> <span class=\"hljs-subst\">{b}</span>\"</span>);\n}\n</code></pre>\n<!-- /ai:lock -->\n<p><strong>Don't:</strong></p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (a, setA) = UseState(<span class=\"hljs-number\">0</span>);\n    <span class=\"hljs-keyword\">if</span> (a > <span class=\"hljs-number\">0</span>)\n        UseEffect(() => { ... }, a);  <span class=\"hljs-comment\">// WRONG: conditional hook</span>\n    <span class=\"hljs-keyword\">return</span> Text(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{a}</span>\"</span>);\n}\n</code></pre>\n<!-- /ai:lock -->\n<p>Put the condition <strong>inside</strong> the hook instead:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\">UseEffect(() => { <span class=\"hljs-keyword\">if</span> (a > <span class=\"hljs-number\">0</span>) { <span class=\"hljs-comment\">/* ... */</span> } }, a);\n</code></pre>\n<!-- /ai:lock -->\n<blockquote>\n<p><strong>Caveat:</strong> Calling a hook inside an <code>if</code>, <code>for</code>, <code>while</code>, <code>switch</code>, or <code>try</code> changes the\nslot index for every hook that follows on any render that takes the branch.\nThe next render then asks slot <code>N</code> for the type the <em>unbranched</em> call shape\nexpects — <code>ValueHookState&#x3C;int></code> vs. <code>EffectHookState</code>, say — and the slot\ntable guard at <code>RenderContext.UseState</code> throws\n<code>HookOrderException(\"Hook at index N is EffectHookState, expected ValueHookState&#x3C;Int32> (UseState). Hooks must be called in the same order every render.\")</code>. The Roslyn analyzer <code>REACTOR_HOOKS_001</code> flags the literal\npattern — <code>Use*</code> inside a control-flow construct in a <code>Render</code> override or\na <code>Use*</code>-prefixed custom hook — at compile time as a Warning. The analyzer\ncan't see calls through lambdas, helper functions whose names don't start\nwith <code>Use</code>, or pattern-matched dispatch, so the runtime guard is the\nbackstop. When you hit the exception, look for a <code>Use*</code> call that's\nconditionally reached — typically the new one you just added.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"custom-hooks\">Custom hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-hooks\">#</a></h3>\n<p>A custom hook is any method whose name starts with <code>Use</code> that calls other\nhooks inside it. The analyzer treats <code>Use*</code> methods as legitimate hook\ncontexts, so you can compose <code>UseState</code>, <code>UseEffect</code>, and friends into a\nnamed, reusable bundle without losing the rules.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">static</span> (<span class=\"hljs-built_in\">string</span> Value, Action&#x3C;<span class=\"hljs-built_in\">string</span>> Set) UseDebouncedText(<span class=\"hljs-built_in\">string</span> initial, <span class=\"hljs-built_in\">int</span> ms)\n{\n    <span class=\"hljs-keyword\">var</span> ctx = RenderContext.Current;\n    <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">value</span>, setValue) = ctx.UseState(initial);\n    <span class=\"hljs-keyword\">var</span> (debounced, setDebounced) = ctx.UseState(initial);\n\n    ctx.UseEffect(() =>\n    {\n        <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n        _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n        {\n            <span class=\"hljs-keyword\">try</span> { <span class=\"hljs-keyword\">await</span> Task.Delay(ms, cts.Token); setDebounced(<span class=\"hljs-keyword\">value</span>); }\n            <span class=\"hljs-keyword\">catch</span> (OperationCanceledException) { }\n        });\n        <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n    }, <span class=\"hljs-keyword\">value</span>);\n\n    <span class=\"hljs-keyword\">return</span> (debounced, setValue);\n}\n</code></pre>\n<p>The hook owns three slots — two <code>UseState</code> and one <code>UseEffect</code> — and the\ncaller still gets the simple <code>(value, setter)</code> shape they'd get from\n<code>UseState</code>. The compiled <a href=\"/rules-of-reactor/\">Rules of Reactor</a> page\ncatalogs the full set of custom-hook conventions.</p>\n<h3 id=\"lifted-state\">Lifted state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lifted-state\">#</a></h3>\n<p>When a parent and child both need to read the same value, hoist the\n<code>UseState</code> to the parent and pass <code>(value, setter)</code> down as a prop. The\n<a href=\"/recipes/master-detail/\">recipes/master-detail</a> walkthrough shows the\nclassic shape — the master list and the detail panel both react to the\nshared selection state. This is the same pattern XAML developers reach\nfor with shared view models; here the state lives in the parent\n<a href=\"/components/\">component</a>, the children are reactive consumers.</p>\n<h3 id=\"deferred-value-via-useref\">Deferred value via UseRef<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#deferred-value-via-useref\">#</a></h3>\n<p><code>UseRef</code> is the right tool when a value needs to survive renders but\nmust <strong>not</strong> trigger them. Storing the previous prop value for diffing,\nholding a <code>CancellationTokenSource</code>, or counting renders for diagnostics\nall belong in a ref:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> prev = UseRef&#x3C;<span class=\"hljs-built_in\">int</span>?>(<span class=\"hljs-literal\">null</span>);\nUseEffect(() => { <span class=\"hljs-comment\">/* compare prev.Current to current */</span> prev.Current = current; }, current);\n</code></pre>\n<p>The setter writes to <code>.Current</code> immediately without scheduling a render —\ncontrast with <code>UseState</code>, where every setter call queues a re-render\nthrough the dispatcher.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"hooks-inside-conditionals\">Hooks inside conditionals<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks-inside-conditionals\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n    <span class=\"hljs-keyword\">if</span> (open)\n    {\n        UseEffect(() => Subscribe(), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>()); <span class=\"hljs-comment\">// REACTOR_HOOKS_001</span>\n    }\n    <span class=\"hljs-keyword\">return</span> ...;\n}\n</code></pre>\n<p>The effect's slot index moves by one on every render where <code>open</code> flips.\nThe next render finds <code>EffectHookState</code> where it expected <code>ValueHookState</code>\nand throws <code>HookOrderException</code>. The fix is to call the hook\nunconditionally and put the condition <strong>inside</strong> it:</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() => { <span class=\"hljs-keyword\">if</span> (!open) <span class=\"hljs-keyword\">return</span> () => { }; <span class=\"hljs-keyword\">return</span> Subscribe(); }, open);\n</code></pre>\n<h3 id=\"stale-closures\">Stale closures<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stale-closures\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\nUseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> t = <span class=\"hljs-keyword\">new</span> Timer(_ => setCount(count + <span class=\"hljs-number\">1</span>), <span class=\"hljs-literal\">null</span>, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">1000</span>);\n    <span class=\"hljs-keyword\">return</span> () => t.Dispose();\n}, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>()); <span class=\"hljs-comment\">// captured `count` is forever 0</span>\n</code></pre>\n<p>The effect's empty deps array means it captures <code>count</code> once at mount.\nThe timer fires forever with the stale closure, so the counter sticks at</p>\n<ol>\n<li>The fix is the functional-setter pattern — <code>setCount(c => c + 1)</code> —\nwhich reads the live cell value instead of the captured variable. The\n<code>Func&#x3C;T,T></code> overload of <code>UseState</code> and the entire <code>UseReducer</code> API are\nbuilt around this.</li>\n</ol>\n<h3 id=\"setter-chain-that-should-be-setprev--\">Setter chain that should be <code>set(prev => ...)</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#setter-chain-that-should-be-setprev--\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"+3\"</span>, () => { setCount(count + <span class=\"hljs-number\">1</span>); setCount(count + <span class=\"hljs-number\">1</span>); setCount(count + <span class=\"hljs-number\">1</span>); });\n</code></pre>\n<p>The three setter calls all read the same captured <code>count</code> and all write\n<code>count + 1</code> — the button advances by one, not three. Use the functional\nform so each call sees the previous setter's result:</p>\n<pre><code class=\"hljs language-csharp\">Button(<span class=\"hljs-string\">\"+3\"</span>, () => { setCount(c => c + <span class=\"hljs-number\">1</span>); setCount(c => c + <span class=\"hljs-number\">1</span>); setCount(c => c + <span class=\"hljs-number\">1</span>); });\n</code></pre>\n<p>This is the same Reactor-wide rule as the <a href=\"#stale-closures\">stale closure</a>\npattern: when an update derives from the previous value, the functional\nsetter is the right shape. The auto-marshal path for cross-thread setters\ndescribed above relies on the same overload — every queued write reads\nthe latest committed value, not a snapshot.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use the functional setter for derived updates.</strong> <code>setCount(c => c + 1)</code> is\nsafer than <code>setCount(count + 1)</code> when multiple updates might batch together.</p>\n<p><strong>Always return cleanup from effects that create resources.</strong> Timers,\nsubscriptions, and event handlers must be disposed. The cleanup function is\nyour only chance to do it.</p>\n<p><strong>Don't overuse UseMemo.</strong> Simple expressions like <code>$\"{first} {last}\"</code> are\ncheap. Only memoize when the computation is genuinely expensive or the result\nis passed as a dependency elsewhere.</p>\n<p><strong>UseRef is not for UI values.</strong> If changing a value should update the screen,\nuse <code>UseState</code>. <code>UseRef</code> is for bookkeeping that doesn't affect rendering.</p>\n<p><strong>Keep effects focused.</strong> One effect per concern. Don't combine a timer and an\nAPI call in the same <code>UseEffect</code> — split them into separate hooks with their\nown dependency arrays. See <a href=\"/effects/\">Effects and Lifecycle</a> for advanced patterns.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — Next: arrange your UI with VStack, HStack, Grid, and responsive patterns</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Previous: component classes, props, and composition</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — Advanced UseEffect patterns, cleanup, and async work</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — Share state across the tree without prop drilling</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — How the slot table actually works under the surface</li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — <code>UsePersisted</code> for state that survives launches</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — Hook rules, idioms, and anti-patterns in one place</li>\n</ul>",
      "text": "Hooks are the reactivity contract for a component . A hook is a positional slot — when Render() runs, Microsoft.UI.Reactor (Reactor) walks each Use* call in order and looks up the matching cell in a per-component slot table held on the RenderContext . The first UseState call is slot 0, the second is slot 1, and so on; the setter the hook returns closes over its slot index and writes back to that cell when you call it. Hooks replace what classic XAML / WPF apps build with DependencyProperty , INotifyPropertyChanged , view models, and lifecycle methods — instead of those four mechanisms, you keep state with UseState , derive values with UseMemo , run side effects with UseEffect , share data without prop drilling via UseContext , and persist values across launches with UsePersisted . Every hook on this page reads as a function call inside Render() and writes back through a setter closure; understanding that single shape makes the rest of Reactor fall out as composition. Hooks # Hooks are functions you call inside Render() to manage state, side effects, and memoization. They replace the need for view models, event handlers, and lifecycle methods. Reference # Hook Returns Purpose UseState (T value, Action&#x3C;T> set) Reactive state — re-renders on set . UseReducer (T value, Action&#x3C;Func&#x3C;T,T>> update) or (TState, Action&#x3C;TAction>) Functional or Redux-style updates. UseEffect void Side effects after commit. With Func&#x3C;Action> overload, runs the cleanup before the next effect and on unmount. UseMemo T Cached computation; re-runs when any deps entry compares unequal. UseRef Ref&#x3C;T> with mutable .Current Persists across renders without re-rendering on change. UseCallback Action Stable delegate identity across renders. UseContext T Read the ambient Context value. UseObservable T Re-render when a tracked INotifyPropertyChanged source raises a change. UseResource AsyncValue&#x3C;T> Cached async read (see Async Resources ). UsePersisted (T, Action&#x3C;T>) UseState that survives app launches (see Persistence ). Every hook on this page is summarized again in the auto-generated hooks reference ; the rest of the page is the narrative. UseState # The most common hook. Returns the current value and a setter function: class StateDemo : Component { public override Element Render () { var (color, setColor) = UseState( \"#0078D4\" ); var (size, setSize) = UseState( 20.0 ); return VStack( 12 , SubHeading( \"UseState\" ), TextBlock( \"Sample text\" ).FontSize(size).Foreground(color), TextBox(color, setColor, placeholderText: \"#hex color\" ) .Width( 150 ), HStack( 8 , TextBlock( \"Size:\" ), Slider(size, 10 , 48 , setSize).Width( 200 ) ) ); } } Call setColor(\"#FF0000\") and Reactor re-renders the component with the new value. The setter also accepts a function: setSize(s => s + 1) — this is safer when the update depends on the previous value. UseReducer (Functional) # When your new state depends on the old state, UseReducer is cleaner than UseState . The updater receives a Func&#x3C;T, T> — a function that transforms the previous value: class ReducerDemo : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C; string >()); var (input, setInput) = UseState( \"\" ); return VStack( 12 , SubHeading( \"UseReducer\" ), HStack( 8 , TextBox(input, setInput, placeholderText: \"Add item\" ) .Width( 180 ), Button( \"Add\" , () => { if ( string .IsNullOrWhiteSpace(input)) return ; updateItems(list => new List&#x3C; string >(list) { input }); setInput( \"\" ); }), Button( \"Clear\" , () => updateItems(_ => new List&#x3C; string >())) ), ForEach(items, item => TextBlock( $\" - {item} \" )) ); } } updateItems(list => new List&#x3C;string>(list) { input }) appends to the list by creating a new copy. This avoids mutation bugs — you always produce a new value from the old one. UseReducer (Redux-Style) # For complex state with multiple action types, use the Redux-style overload. Define a state record, action types, and a reducer function: record CounterState ( int Count , string LastAction ); abstract record CounterAction ; record Increment : CounterAction ; record Decrement : CounterAction ; record Reset : CounterAction ; class ReduxReducerDemo : Component { public override Element Render () { var (state, dispatch) = UseReducer( (CounterState s, CounterAction a) => a switch { Increment => s with { Count = s.Count + 1 , LastAction = \"+\" }, Decrement => s with { Count = s.Count - 1 , LastAction = \"-\" }, Reset => new ( 0 , \"reset\" ), _ => s }, new CounterState( 0 , \"none\" )); return VStack( 8 , SubHeading( \"UseReducer (Redux-style)\" ), TextBlock( $\"Count: {state.Count} (last: {state.LastAction} )\" ) .FontSize( 18 ).Bold(), HStack( 8 , Button( \"-\" , () => dispatch( new Decrement())), Button( \"Reset\" , () => dispatch( new Reset())), Button( \"+\" , () => dispatch( new Increment())) ) ); } } The reducer (state, action) => newState is a pure function. Each action type maps to a state transformation. This pattern scales well — adding new actions doesn't change existing logic. UseEffect # Run side effects (timers, subscriptions, async work) after a render. The dependencies array controls when the effect re-runs: class EffectDemo : Component { public override Element Render () { var (seconds, updateSeconds) = UseReducer( 0 ); var (running, setRunning) = UseState( false ); UseEffect(() => { if (!running) return () => { }; var cts = new CancellationTokenSource(); var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); _ = Task.Run( async () => { while ( await timer.WaitForNextTickAsync(cts.Token)) updateSeconds(s => s + 1 ); }); return () => { cts.Cancel(); timer.Dispose(); }; }, running); return VStack( 8 , SubHeading( \"UseEffect\" ), TextBlock( $\"Elapsed: {seconds} s\" ).FontSize( 18 ), HStack( 8 , Button(running ? \"Stop\" : \"Start\" , () => setRunning(!running)), Button( \"Reset\" , () => updateSeconds(_ => 0 )) ) ); } } Key details: The effect runs after the render completes, not during. Return a cleanup function to dispose resources. Reactor calls it before re-running the effect and when the component unmounts. Empty dependencies UseEffect(() => { ... }) — runs once on mount. With dependencies UseEffect(() => { ... }, running) — runs when running changes. UseMemo # Cache an expensive computation so it only recalculates when its inputs change: class MemoDemo : Component { public override Element Render () { var (input, setInput) = UseState( \"Hello, Reactor!\" ); var stats = UseMemo(() => new { Chars = input.Length, Words = input.Split( ' ' , StringSplitOptions.RemoveEmptyEntries).Length, Upper = input.ToUpperInvariant() }, input); return VStack( 8 , SubHeading( \"UseMemo\" ), TextBox(input, setInput).Width( 250 ), TextBlock( $\"Characters: {stats.Chars} , Words: {stats.Words} \" ), Caption( $\"Uppercased: {stats.Upper} \" ) ); } } UseMemo compares the dependency values between renders. If they haven't changed, it returns the cached result. Use it for string processing, filtering large lists, or any computation you don't want to repeat every render. UseRef # Store a mutable value that persists across renders without triggering re-renders: class RefDemo : Component { public override Element Render () { var ( value , setValue) = UseState( \"\" ); var renderCount = UseRef( 0 ); renderCount.Current++; return VStack( 8 , SubHeading( \"UseRef\" ), TextBlock( $\"Render count: {renderCount.Current} \" ).SemiBold(), TextBox( value , setValue, placeholderText: \"Type to trigger renders\" ) .Width( 250 ), Caption( \"UseRef persists across renders without causing them\" ) ); } } UseRef returns a Ref&#x3C;T> with a .Current property. Changing .Current does not cause a re-render. This is useful for: Counting renders Storing previous values for comparison Holding references to timers or cancellation tokens UseCallback # Stabilize a callback reference so child components don't re-render unnecessarily: class CallbackDemo : Component { public override Element Render () { var (count, updateCount) = UseReducer( 0 ); var (label, setLabel) = UseState( \"Click me\" ); var stableIncrement = UseCallback( () => updateCount(c => c + 1 ), Array.Empty&#x3C; object >()); return VStack( 8 , SubHeading( \"UseCallback\" ), TextBlock( $\"Count: {count} \" ).FontSize( 18 ), TextBox(label, setLabel, placeholderText: \"Button label\" ) .Width( 200 ), Button(label, stableIncrement), Caption( \"The callback identity stays stable across renders\" ) ); } } Without UseCallback , the lambda () => setCount(c => c + 1) would be a new object every render. UseCallback returns the same delegate instance as long as the dependencies haven't changed. This matters when passing callbacks to memoized child components . Updating State From Background Work # Once the host is bootstrapped, UseState and UseReducer setters are safe to call from any thread. When you invoke a setter from a background task — inside Task.Run , from a PeriodicTimer loop, from a network callback, or after await ... ConfigureAwait(false) — the setter automatically marshals the write and the resulting re-render onto the UI dispatcher. You write the same code you'd write on the UI thread: public override Element Render () { var (seconds, setSeconds) = UseState( 0 ); UseEffect(() => { var cts = new CancellationTokenSource(); _ = Task.Run( async () => { using var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); while ( await timer.WaitForNextTickAsync(cts.Token)) setSeconds(s => s + 1 ); // auto-marshals to the UI thread }); return () => cts.Cancel(); }); return TextBlock( $\"Elapsed: {seconds} s\" ); } Each cross-thread setter call costs one DispatcherQueue.TryEnqueue — microseconds, not free, but vastly cheaper than the bugs you'd hit writing the field directly from a worker. If you need many concurrent setters to apply in-place rather than serialize through the UI thread (typical for ingest loops that hammer the same hook from multiple producers), pass threadSafe: true to the hook: var (count, setCount) = UseState( 0 , threadSafe: true ); var (sum, addToSum) = UseReducer( 0 , threadSafe: true ); threadSafe: true switches the hook to a per-cell lock: concurrent writers serialize on the lock instead of queuing through the UI dispatcher, and reads inside the setter (the prev argument of a reducer) see the latest committed write rather than a snapshot from the last UI tick. When auto-marshal can't help. The setter needs a captured ReactorApp.UIDispatcher to marshal onto. In unit-test / headless contexts that drive RenderContext directly, or before the first host has been bootstrapped, a cross-thread setter call throws InvalidOperationException instead of silently racing. The setter also throws if the dispatcher refuses the marshaled call (e.g., during shutdown). Cancel background producers in your effect cleanup so they stop before the window closes. Hook Rules # Hooks must be called in the same order every render. Reactor tracks hooks by their position in the call sequence — the first UseState call always maps to the first state slot, the second to the second, and so on. The internal walk of _hookIndex against _hooks[currentIndex] is described in Hooks Internals . Do: public override Element Render () { var (a, setA) = UseState( 0 ); // always first var (b, setB) = UseState( \"\" ); // always second UseEffect(() => { ... }, a); // always third return Text( $\" {a} {b} \" ); } Don't: public override Element Render () { var (a, setA) = UseState( 0 ); if (a > 0 ) UseEffect(() => { ... }, a); // WRONG: conditional hook return Text( $\" {a} \" ); } Put the condition inside the hook instead: UseEffect(() => { if (a > 0 ) { /* ... */ } }, a); Caveat: Calling a hook inside an if , for , while , switch , or try changes the slot index for every hook that follows on any render that takes the branch. The next render then asks slot N for the type the unbranched call shape expects — ValueHookState&#x3C;int> vs. EffectHookState , say — and the slot table guard at RenderContext.UseState throws HookOrderException(\"Hook at index N is EffectHookState, expected ValueHookState&#x3C;Int32> (UseState). Hooks must be called in the same order every render.\") . The Roslyn analyzer REACTOR_HOOKS_001 flags the literal pattern — Use* inside a control-flow construct in a Render override or a Use* -prefixed custom hook — at compile time as a Warning. The analyzer can't see calls through lambdas, helper functions whose names don't start with Use , or pattern-matched dispatch, so the runtime guard is the backstop. When you hit the exception, look for a Use* call that's conditionally reached — typically the new one you just added. Patterns # Custom hooks # A custom hook is any method whose name starts with Use that calls other hooks inside it. The analyzer treats Use* methods as legitimate hook contexts, so you can compose UseState , UseEffect , and friends into a named, reusable bundle without losing the rules. static ( string Value, Action&#x3C; string > Set) UseDebouncedText( string initial, int ms) { var ctx = RenderContext.Current; var ( value , setValue) = ctx.UseState(initial); var (debounced, setDebounced) = ctx.UseState(initial); ctx.UseEffect(() => { var cts = new CancellationTokenSource(); _ = Task.Run( async () => { try { await Task.Delay(ms, cts.Token); setDebounced( value ); } catch (OperationCanceledException) { } }); return () => cts.Cancel(); }, value ); return (debounced, setValue); } The hook owns three slots — two UseState and one UseEffect — and the caller still gets the simple (value, setter) shape they'd get from UseState . The compiled Rules of Reactor page catalogs the full set of custom-hook conventions. Lifted state # When a parent and child both need to read the same value, hoist the UseState to the parent and pass (value, setter) down as a prop. The recipes/master-detail walkthrough shows the classic shape — the master list and the detail panel both react to the shared selection state. This is the same pattern XAML developers reach for with shared view models; here the state lives in the parent component , the children are reactive consumers. Deferred value via UseRef # UseRef is the right tool when a value needs to survive renders but must not trigger them. Storing the previous prop value for diffing, holding a CancellationTokenSource , or counting renders for diagnostics all belong in a ref: var prev = UseRef&#x3C; int ?>( null ); UseEffect(() => { /* compare prev.Current to current */ prev.Current = current; }, current); The setter writes to .Current immediately without scheduling a render — contrast with UseState , where every setter call queues a re-render through the dispatcher. Common Mistakes # Hooks inside conditionals # // Don't: public override Element Render () { var (open, setOpen) = UseState( false ); if (open) { UseEffect(() => Subscribe(), Array.Empty&#x3C; object >()); // REACTOR_HOOKS_001 } return ...; } The effect's slot index moves by one on every render where open flips. The next render finds EffectHookState where it expected ValueHookState and throws HookOrderException . The fix is to call the hook unconditionally and put the condition inside it: UseEffect(() => { if (!open) return () => { }; return Subscribe(); }, open); Stale closures # // Don't: var (count, setCount) = UseState( 0 ); UseEffect(() => { var t = new Timer(_ => setCount(count + 1 ), null , 0 , 1000 ); return () => t.Dispose(); }, Array.Empty&#x3C; object >()); // captured `count` is forever 0 The effect's empty deps array means it captures count once at mount. The timer fires forever with the stale closure, so the counter sticks at The fix is the functional-setter pattern — setCount(c => c + 1) — which reads the live cell value instead of the captured variable. The Func&#x3C;T,T> overload of UseState and the entire UseReducer API are built around this. Setter chain that should be set(prev => ...) # // Don't: Button( \"+3\" , () => { setCount(count + 1 ); setCount(count + 1 ); setCount(count + 1 ); }); The three setter calls all read the same captured count and all write count + 1 — the button advances by one, not three. Use the functional form so each call sees the previous setter's result: Button( \"+3\" , () => { setCount(c => c + 1 ); setCount(c => c + 1 ); setCount(c => c + 1 ); }); This is the same Reactor-wide rule as the stale closure pattern: when an update derives from the previous value, the functional setter is the right shape. The auto-marshal path for cross-thread setters described above relies on the same overload — every queued write reads the latest committed value, not a snapshot. Tips # Use the functional setter for derived updates. setCount(c => c + 1) is safer than setCount(count + 1) when multiple updates might batch together. Always return cleanup from effects that create resources. Timers, subscriptions, and event handlers must be disposed. The cleanup function is your only chance to do it. Don't overuse UseMemo. Simple expressions like $\"{first} {last}\" are cheap. Only memoize when the computation is genuinely expensive or the result is passed as a dependency elsewhere. UseRef is not for UI values. If changing a value should update the screen, use UseState . UseRef is for bookkeeping that doesn't affect rendering. Keep effects focused. One effect per concern. Don't combine a timer and an API call in the same UseEffect — split them into separate hooks with their own dependency arrays. See Effects and Lifecycle for advanced patterns. Next Steps # Layout — Next: arrange your UI with VStack, HStack, Grid, and responsive patterns Components — Previous: component classes, props, and composition Effects and Lifecycle — Advanced UseEffect patterns, cleanup, and async work Context — Share state across the tree without prop drilling Hooks Internals — How the slot table actually works under the surface Persistence — UsePersisted for state that survives launches Rules of Reactor — Hook rules, idioms, and anti-patterns in one place",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/hooks.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/hooks.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "usestate",
          "text": "UseState"
        },
        {
          "depth": 2,
          "id": "usereducer-functional",
          "text": "UseReducer (Functional)"
        },
        {
          "depth": 2,
          "id": "usereducer-redux-style",
          "text": "UseReducer (Redux-Style)"
        },
        {
          "depth": 2,
          "id": "useeffect",
          "text": "UseEffect"
        },
        {
          "depth": 2,
          "id": "usememo",
          "text": "UseMemo"
        },
        {
          "depth": 2,
          "id": "useref",
          "text": "UseRef"
        },
        {
          "depth": 2,
          "id": "usecallback",
          "text": "UseCallback"
        },
        {
          "depth": 2,
          "id": "updating-state-from-background-work",
          "text": "Updating State From Background Work"
        },
        {
          "depth": 2,
          "id": "hook-rules",
          "text": "Hook Rules"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "custom-hooks",
          "text": "Custom hooks"
        },
        {
          "depth": 3,
          "id": "lifted-state",
          "text": "Lifted state"
        },
        {
          "depth": 3,
          "id": "deferred-value-via-useref",
          "text": "Deferred value via UseRef"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "hooks-inside-conditionals",
          "text": "Hooks inside conditionals"
        },
        {
          "depth": 3,
          "id": "stale-closures",
          "text": "Stale closures"
        },
        {
          "depth": 3,
          "id": "setter-chain-that-should-be-setprev--",
          "text": "Setter chain that should be set(prev => ...)"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Effects and Lifecycle",
      "path": "effects.md",
      "slug": "/effects/",
      "html": "<p>Effects are commit-phase side effects with dependency tracking. After Microsoft.UI.Reactor (Reactor)\nfinishes <a href=\"/reconciliation/\">reconciling</a> the element tree and patching the\nWinUI controls for a render, it walks the effect queue for that render and\nruns every <a href=\"/reference/hooks/UseEffect/\">UseEffect</a> body whose dependency array has changed\nsince the previous commit. The body runs <strong>after</strong> the new UI has been\ncommitted — never during render — so an effect is the safe place to start a\ntimer, open a subscription, or kick off a <code>Task.Run</code> that will eventually\ncall a setter to re-render the <a href=\"/components/\">component</a>. The cleanup\nfunction you return is the mirror image: Reactor calls it before re-running\nthe effect with a new dep set, and again when the component unmounts. The\ndependency array is the contract between you and the scheduler — every value\nthe effect <em>reads</em> from state, props, or context belongs in it. Forget one\nand the effect captures a stale closure; allocate a new array or lambda each\nrender and the effect re-runs forever because reference identity churns.</p>\n<h1 id=\"effects-and-lifecycle\">Effects and Lifecycle<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#effects-and-lifecycle\">#</a></h1>\n<p><code>UseEffect</code> runs side effects — code that reaches outside the\n<a href=\"/components/\">component's</a> render function. Timers, data fetching,\nsubscriptions, and DOM manipulation all belong in effects, not in <code>Render()</code>.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Overload</th><th>When the body runs</th><th>When cleanup runs</th></tr></thead><tbody><tr><td><code>UseEffect(Action body, params object[] deps)</code></td><td>After every commit where any entry in <code>deps</code> compares unequal (<code>Array.Empty&#x3C;object>()</code> → mount only).</td><td>Never — no cleanup function.</td></tr><tr><td><code>UseEffect(Func&#x3C;Action> bodyWithCleanup, params object[] deps)</code></td><td>Same as above.</td><td>Before the next body run, and on unmount.</td></tr></tbody></table>\n<p>Both overloads accept a <code>params</code> deps argument; pass no values for \"run every\ncommit\" (rarely correct), <code>Array.Empty&#x3C;object>()</code> for \"run once on mount\",\nor one-or-more reactive values for \"re-run when any of these change\". Compare\nwith <a href=\"/reference/hooks/UseResource/\">UseResource</a> when the side effect is a cached async read —\nsee <a href=\"/async-resources/\">Async Resources</a> for the cached-fetch shape, which\nhandles cancellation, retry, and revalidation for you.</p>\n<h2 id=\"running-once-on-mount\">Running Once on Mount<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#running-once-on-mount\">#</a></h2>\n<p>Pass an empty dependency array to run an effect once when the component mounts:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MountEffectExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (loadedAt, setLoadedAt) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        UseEffect(() =>\n        {\n            setLoadedAt(DateTime.Now.ToString(<span class=\"hljs-string\">\"HH:mm:ss\"</span>));\n        }, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(<span class=\"hljs-string\">\"Component mounted at:\"</span>),\n            TextBlock(loadedAt).FontSize(<span class=\"hljs-number\">20</span>).Bold()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/effects/mount-effect.png\" alt=\"Mount effect with loaded timestamp\"></p>\n<p>The empty <code>Array.Empty&#x3C;object>()</code> dependency array tells Reactor this effect has\nno external dependencies. It runs once after the first render and never again.</p>\n<h2 id=\"running-when-dependencies-change\">Running When Dependencies Change<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#running-when-dependencies-change\">#</a></h2>\n<p>Pass values in the dependency array to re-run the effect when those values\nchange:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DependencyEffectExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (query, setQuery) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (results, setResults) = UseState(<span class=\"hljs-string\">\"Type to search...\"</span>);\n\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(query))\n                setResults(<span class=\"hljs-string\">\"Type to search...\"</span>);\n            <span class=\"hljs-keyword\">else</span>\n                setResults(<span class=\"hljs-string\">$\"Found 3 results for \\\"<span class=\"hljs-subst\">{query}</span>\\\"\"</span>);\n        }, query);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(query, setQuery, placeholderText: <span class=\"hljs-string\">\"Search...\"</span>).Width(<span class=\"hljs-number\">300</span>),\n            TextBlock(results).Foreground(Theme.SecondaryText)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/effects/dependency-effect.png\" alt=\"Search with debounced query\"></p>\n<p>Every time <code>query</code> changes, the effect runs again. Reactor compares the current\ndependencies to the previous ones using structural equality. If nothing\nchanged, the effect is skipped.</p>\n<h2 id=\"cleanup-with-timers\">Cleanup with Timers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#cleanup-with-timers\">#</a></h2>\n<p>When an effect creates a resource (timer, subscription, event handler), return\na cleanup function. Reactor calls it before re-running the effect and when the\ncomponent unmounts:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TimerCleanupExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (seconds, updateSeconds) = UseReducer(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (isRunning, setIsRunning) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">if</span> (!isRunning) <span class=\"hljs-keyword\">return</span> () => { };\n            <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n            <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n            _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(cts.Token))\n                    updateSeconds(s => s + <span class=\"hljs-number\">1</span>);\n            });\n            <span class=\"hljs-keyword\">return</span> () => { cts.Cancel(); timer.Dispose(); };\n        }, isRunning);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Elapsed: <span class=\"hljs-subst\">{seconds}</span>s\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(isRunning ? <span class=\"hljs-string\">\"Stop\"</span> : <span class=\"hljs-string\">\"Start\"</span>, () => setIsRunning(!isRunning)),\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () => updateSeconds(_ => <span class=\"hljs-number\">0</span>))\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/effects/timer-cleanup.png\" alt=\"Timer counting up with start/stop\"></p>\n<p>The <code>Func&#x3C;Action></code> overload of <code>UseEffect</code> returns a cleanup function. Here\nthe cleanup disposes the timer, preventing leaks when the component unmounts\nor when <code>isRunning</code> changes.</p>\n<p>The timer body runs on a background thread (it's inside <code>Task.Run</code>) and calls\nthe <code>updateSeconds</code> reducer from there. Once the host is bootstrapped, that\nworks out of the box — every <code>UseState</code> / <code>UseReducer</code> setter automatically\nmarshals onto the captured UI dispatcher when called from a non-UI thread, so\ntimers, <code>PeriodicTimer</code>, network callbacks, and code after\n<code>await ... ConfigureAwait(false)</code> can all call the returned setter without any\nextra opt-in. Pass <code>threadSafe: true</code> to the hook only when you need many\nconcurrent setters to apply in-place (locked) instead of being queued one-by-one\nonto the UI thread. The setter throws <code>InvalidOperationException</code> if it's\ncalled cross-thread before any host has bootstrapped (no UI dispatcher\ncaptured) or after the dispatcher has begun shutting down — make sure the\neffect cleanup cancels the background producer so it stops with the component.</p>\n<h2 id=\"async-data-loading\">Async Data Loading<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-data-loading\">#</a></h2>\n<p>Use <code>UseEffect</code> with <a href=\"/hooks/\"><code>UseState</code></a> to load data asynchronously:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AsyncLoadingExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, setItems) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>[]?>(<span class=\"hljs-literal\">null</span>);\n\n        UseEffect(() =>\n        {\n            _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">await</span> Task.Delay(<span class=\"hljs-number\">1500</span>); <span class=\"hljs-comment\">// simulate network call</span>\n                setItems(<span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"Alice\"</span>, <span class=\"hljs-string\">\"Bob\"</span>, <span class=\"hljs-string\">\"Charlie\"</span> });\n            });\n        }, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n        <span class=\"hljs-keyword\">if</span> (items <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"Loading...\"</span>).Padding(<span class=\"hljs-number\">24</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Heading(<span class=\"hljs-string\">\"Loaded Users\"</span>),\n            VStack(<span class=\"hljs-number\">4</span>, items.Select(name => TextBlock(name)).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/effects/async-loading.png\" alt=\"Loading indicator then data\"></p>\n<p>The effect fires on mount, starts an async task, and updates state when it\ncompletes. The component re-renders automatically when <code>setItems</code> is called.</p>\n<h2 id=\"avoiding-infinite-loops\">Avoiding Infinite Loops<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#avoiding-infinite-loops\">#</a></h2>\n<p>A common mistake is calling a state setter unconditionally inside an effect\nwith that state in its dependency array:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InfiniteLoopWarning</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-comment\">// BAD: this creates an infinite loop!</span>\n        <span class=\"hljs-comment\">// UseEffect(() => { setCount(count + 1); }, count);</span>\n\n        <span class=\"hljs-comment\">// GOOD: guard with a condition</span>\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">if</span> (count &#x3C; <span class=\"hljs-number\">5</span>) setCount(count + <span class=\"hljs-number\">1</span>);\n        }, count);\n\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Count stopped at: <span class=\"hljs-subst\">{count}</span>\"</span>).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>This would re-render, re-run the effect, set state again, re-render, and\nloop forever. Always guard state updates with a condition, or remove the\nchanging value from the dependency array.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Cleanup runs <strong>before</strong> the next effect body, not after. When a dependency\nchanges, the sequence on the next commit is: cleanup of the previous effect\ninstance, then a render with the new dep set, then the new effect body\nrunning. So if your effect subscribes to channel A and the dep changes to\nchannel B, you'll see <code>unsubscribe(A) → subscribe(B)</code> in that order — never\ntwo simultaneous subscriptions, never a missing unsubscribe between them.\nThe same ordering applies on unmount: cleanup runs once, the effect body\nnever runs again. The concrete failure mode this prevents: a <code>PeriodicTimer</code>\nthat captures <code>cts</code> in the cleanup closure stops correctly when the dep\nchanges, but only if you return <code>() => { cts.Cancel(); timer.Dispose(); }</code>\nfrom the body. Forget the cleanup and the old timer keeps firing alongside\nthe new one. The scheduling internals — when each phase runs relative to\nthe reconciler — are documented in\n<a href=\"/effects-scheduling/\">effects-scheduling</a>.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"data-fetching-with-cancellation\">Data fetching with cancellation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data-fetching-with-cancellation\">#</a></h3>\n<p>When an effect kicks off an async fetch, the cleanup function must cancel\nit. Otherwise a stale request can land after the user has navigated away,\noverwriting state on the next component instance (or worse, throwing on a\ndisposed setter):</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n    _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">try</span>\n        {\n            <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">await</span> FetchAsync(query, cts.Token);\n            setItems(data);\n        }\n        <span class=\"hljs-keyword\">catch</span> (OperationCanceledException) { <span class=\"hljs-comment\">/* expected */</span> }\n    });\n    <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n}, query);\n</code></pre>\n<p>When <code>query</code> changes, cleanup cancels the in-flight fetch before the new\none starts. For the cached / retry / focus-revalidation shape of the same\npattern, reach for <a href=\"/async-resources/\"><code>UseResource</code></a> instead — it handles\nall of this plus a per-component cache key for free.</p>\n<h3 id=\"subscriptions-with-cleanup\">Subscriptions with cleanup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#subscriptions-with-cleanup\">#</a></h3>\n<p>The pattern is the same regardless of what you're subscribing to —\n<code>INotifyPropertyChanged</code>, an <code>IObservable</code>, a global event bus, a\nWinUI control's <code>RoutedEventHandler</code>. Subscribe in the body, return an\nunsubscribe in the cleanup:</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() =>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Handler</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span>? s, EventArgs e</span>)</span> => setTick(t => t + <span class=\"hljs-number\">1</span>);\n    Source.Changed += Handler;\n    <span class=\"hljs-keyword\">return</span> () => Source.Changed -= Handler;\n}, Source); <span class=\"hljs-comment\">// re-attach if Source identity changes</span>\n</code></pre>\n<p>If you only need to react to property changes on an <code>INotifyPropertyChanged</code>\nsource, use <a href=\"/hooks/\"><code>UseObservable</code></a> instead — it owns the\nsubscribe/unsubscribe pair so you don't have to.</p>\n<h3 id=\"useresource-for-async-data\">UseResource for async data<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useresource-for-async-data\">#</a></h3>\n<p>For data that lives outside the component (server fetch, file read, slow\ncomputation), reach for <a href=\"/async-resources/\"><code>UseResource</code></a> rather than\nhand-rolling <code>UseEffect</code> + <code>UseState</code>. It returns an <code>AsyncValue&#x3C;T></code> with\n<code>Pending</code> / <code>Value</code> / <code>Error</code> shape, handles cancellation on dep change,\nretries on <code>await Refresh()</code>, and shares a per-context query cache so two\ncomponents reading the same key reuse the in-flight task. The pattern\nto recognize: if the effect body would be \"kick off a fetch, write the\nresult to state, handle the loading flag, handle the error flag\", you're\nre-implementing <code>UseResource</code>.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"object-or-array-literals-in-the-deps-array\">Object or array literals in the deps array<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#object-or-array-literals-in-the-deps-array\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> options = <span class=\"hljs-keyword\">new</span> { Url = url, Limit = <span class=\"hljs-number\">10</span> };\nUseEffect(() => FetchAsync(options), options); <span class=\"hljs-comment\">// new instance every render</span>\n</code></pre>\n<p><code>options</code> is a fresh anonymous object on every render — its reference\ncompares unequal to the previous one, so the effect re-runs every commit\nand the fetch fires in a loop. The <code>REACTOR_HOOKS_004</code> analyzer warns\nwhen a hook's deps argument is a freshly-allocated object, array, or\nlambda. Pass the <em>primitives</em> instead:</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() => FetchAsync(url, <span class=\"hljs-number\">10</span>), url);\n</code></pre>\n<p>Or memoize the container with <a href=\"/hooks/\"><code>UseMemo</code></a> so its identity is\nstable across renders.</p>\n<h3 id=\"missing-cleanup\">Missing cleanup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#missing-cleanup\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nUseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n    _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync())\n            setTick(t => t + <span class=\"hljs-number\">1</span>);\n    });\n}, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>()); <span class=\"hljs-comment\">// no cleanup → timer fires forever after unmount</span>\n</code></pre>\n<p>The component unmounts, the timer keeps firing, the setter is called on a\ndead <a href=\"/hooks-internals/\"><code>RenderContext</code></a>, and the setter throws (or worse,\nsilently leaks the closure tree the timer captured). Always return a\ncleanup that cancels the producer:</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n    <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n    _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(cts.Token))\n            setTick(t => t + <span class=\"hljs-number\">1</span>);\n    });\n    <span class=\"hljs-keyword\">return</span> () => { cts.Cancel(); timer.Dispose(); };\n}, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n</code></pre>\n<h3 id=\"running-an-effect-that-should-be-a-memo\">Running an effect that should be a memo<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#running-an-effect-that-should-be-a-memo\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> (full, setFull) = UseState(<span class=\"hljs-string\">\"\"</span>);\nUseEffect(() => setFull(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{first}</span> <span class=\"hljs-subst\">{last}</span>\"</span>), first, last);\n</code></pre>\n<p>This pays for two extra renders (initial mount with empty <code>full</code>, then\nre-render after the effect writes) just to derive a string. The deriving\nshould happen <strong>in render</strong>, either inline or via\n<a href=\"/hooks/\"><code>UseMemo</code></a> for an expensive computation:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> full = <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{first}</span> <span class=\"hljs-subst\">{last}</span>\"</span>;                  <span class=\"hljs-comment\">// inline</span>\n<span class=\"hljs-keyword\">var</span> stats = UseMemo(() => Compute(input), input); <span class=\"hljs-comment\">// memoized when expensive</span>\n</code></pre>\n<p>Effects are for <em>side effects</em> — work that reaches outside the component.\nPure derivations of existing state belong in render.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use empty deps for one-time setup.</strong> <code>UseEffect(action, Array.Empty&#x3C;object>())</code>\nis the equivalent of \"run on mount.\" Use it for initial data fetching, event\nregistration, or logging.</p>\n<p><strong>Always clean up resources.</strong> If your effect creates a timer, subscription, or\nevent handler, return a cleanup function. Leaked resources cause bugs that are\nhard to trace.</p>\n<p><strong>Keep dependency arrays honest.</strong> Include every value the effect reads from\ncomponent state. Omitting a dependency doesn't prevent the read — it prevents\nthe re-run, leading to stale closures.</p>\n<p><strong>Don't call setState unconditionally in an effect that depends on that state.</strong>\nThis creates an infinite loop. Guard with a condition or restructure the logic.</p>\n<p><strong>Prefer <a href=\"/hooks/\"><code>UseReducer</code></a> for complex effect-driven state.</strong> When an\neffect needs to update multiple related values, a reducer keeps the logic in\none place and avoids cascading re-renders.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Previous: apply theme tokens, colors, and dark/light mode</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Next: bundle actions with labels, icons, and accelerators</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Deep dive into UseState, UseReducer, and other hooks used by effects</li>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — Cached async reads with <code>UseResource</code> and <code>UseMutation</code></li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — When effects run relative to render and commit</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — Combine effects with other hooks for complex scenarios</li>\n</ul>",
      "text": "Effects are commit-phase side effects with dependency tracking. After Microsoft.UI.Reactor (Reactor) finishes reconciling the element tree and patching the WinUI controls for a render, it walks the effect queue for that render and runs every UseEffect body whose dependency array has changed since the previous commit. The body runs after the new UI has been committed — never during render — so an effect is the safe place to start a timer, open a subscription, or kick off a Task.Run that will eventually call a setter to re-render the component . The cleanup function you return is the mirror image: Reactor calls it before re-running the effect with a new dep set, and again when the component unmounts. The dependency array is the contract between you and the scheduler — every value the effect reads from state, props, or context belongs in it. Forget one and the effect captures a stale closure; allocate a new array or lambda each render and the effect re-runs forever because reference identity churns. Effects and Lifecycle # UseEffect runs side effects — code that reaches outside the component's render function. Timers, data fetching, subscriptions, and DOM manipulation all belong in effects, not in Render() . Reference # Overload When the body runs When cleanup runs UseEffect(Action body, params object[] deps) After every commit where any entry in deps compares unequal ( Array.Empty&#x3C;object>() → mount only). Never — no cleanup function. UseEffect(Func&#x3C;Action> bodyWithCleanup, params object[] deps) Same as above. Before the next body run, and on unmount. Both overloads accept a params deps argument; pass no values for \"run every commit\" (rarely correct), Array.Empty&#x3C;object>() for \"run once on mount\", or one-or-more reactive values for \"re-run when any of these change\". Compare with UseResource when the side effect is a cached async read — see Async Resources for the cached-fetch shape, which handles cancellation, retry, and revalidation for you. Running Once on Mount # Pass an empty dependency array to run an effect once when the component mounts: class MountEffectExample : Component { public override Element Render () { var (loadedAt, setLoadedAt) = UseState( \"\" ); UseEffect(() => { setLoadedAt(DateTime.Now.ToString( \"HH:mm:ss\" )); }, Array.Empty&#x3C; object >()); return VStack( 8 , TextBlock( \"Component mounted at:\" ), TextBlock(loadedAt).FontSize( 20 ).Bold() ).Padding( 24 ); } } The empty Array.Empty&#x3C;object>() dependency array tells Reactor this effect has no external dependencies. It runs once after the first render and never again. Running When Dependencies Change # Pass values in the dependency array to re-run the effect when those values change: class DependencyEffectExample : Component { public override Element Render () { var (query, setQuery) = UseState( \"\" ); var (results, setResults) = UseState( \"Type to search...\" ); UseEffect(() => { if ( string .IsNullOrWhiteSpace(query)) setResults( \"Type to search...\" ); else setResults( $\"Found 3 results for \\\" {query} \\\"\" ); }, query); return VStack( 12 , TextBox(query, setQuery, placeholderText: \"Search...\" ).Width( 300 ), TextBlock(results).Foreground(Theme.SecondaryText) ).Padding( 24 ); } } Every time query changes, the effect runs again. Reactor compares the current dependencies to the previous ones using structural equality. If nothing changed, the effect is skipped. Cleanup with Timers # When an effect creates a resource (timer, subscription, event handler), return a cleanup function. Reactor calls it before re-running the effect and when the component unmounts: class TimerCleanupExample : Component { public override Element Render () { var (seconds, updateSeconds) = UseReducer( 0 ); var (isRunning, setIsRunning) = UseState( false ); UseEffect(() => { if (!isRunning) return () => { }; var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); var cts = new CancellationTokenSource(); _ = Task.Run( async () => { while ( await timer.WaitForNextTickAsync(cts.Token)) updateSeconds(s => s + 1 ); }); return () => { cts.Cancel(); timer.Dispose(); }; }, isRunning); return VStack( 12 , TextBlock( $\"Elapsed: {seconds} s\" ).FontSize( 24 ).Bold(), HStack( 8 , Button(isRunning ? \"Stop\" : \"Start\" , () => setIsRunning(!isRunning)), Button( \"Reset\" , () => updateSeconds(_ => 0 )) ) ).Padding( 24 ); } } The Func&#x3C;Action> overload of UseEffect returns a cleanup function. Here the cleanup disposes the timer, preventing leaks when the component unmounts or when isRunning changes. The timer body runs on a background thread (it's inside Task.Run ) and calls the updateSeconds reducer from there. Once the host is bootstrapped, that works out of the box — every UseState / UseReducer setter automatically marshals onto the captured UI dispatcher when called from a non-UI thread, so timers, PeriodicTimer , network callbacks, and code after await ... ConfigureAwait(false) can all call the returned setter without any extra opt-in. Pass threadSafe: true to the hook only when you need many concurrent setters to apply in-place (locked) instead of being queued one-by-one onto the UI thread. The setter throws InvalidOperationException if it's called cross-thread before any host has bootstrapped (no UI dispatcher captured) or after the dispatcher has begun shutting down — make sure the effect cleanup cancels the background producer so it stops with the component. Async Data Loading # Use UseEffect with UseState to load data asynchronously: class AsyncLoadingExample : Component { public override Element Render () { var (items, setItems) = UseState&#x3C; string []?>( null ); UseEffect(() => { _ = Task.Run( async () => { await Task.Delay( 1500 ); // simulate network call setItems( new [] { \"Alice\" , \"Bob\" , \"Charlie\" }); }); }, Array.Empty&#x3C; object >()); if (items is null ) return TextBlock( \"Loading...\" ).Padding( 24 ); return VStack( 8 , Heading( \"Loaded Users\" ), VStack( 4 , items.Select(name => TextBlock(name)).ToArray()) ).Padding( 24 ); } } The effect fires on mount, starts an async task, and updates state when it completes. The component re-renders automatically when setItems is called. Avoiding Infinite Loops # A common mistake is calling a state setter unconditionally inside an effect with that state in its dependency array: class InfiniteLoopWarning : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); // BAD: this creates an infinite loop! // UseEffect(() => { setCount(count + 1); }, count); // GOOD: guard with a condition UseEffect(() => { if (count &#x3C; 5 ) setCount(count + 1 ); }, count); return TextBlock( $\"Count stopped at: {count} \" ).Padding( 24 ); } } This would re-render, re-run the effect, set state again, re-render, and loop forever. Always guard state updates with a condition, or remove the changing value from the dependency array. Caveat: Cleanup runs before the next effect body, not after. When a dependency changes, the sequence on the next commit is: cleanup of the previous effect instance, then a render with the new dep set, then the new effect body running. So if your effect subscribes to channel A and the dep changes to channel B, you'll see unsubscribe(A) → subscribe(B) in that order — never two simultaneous subscriptions, never a missing unsubscribe between them. The same ordering applies on unmount: cleanup runs once, the effect body never runs again. The concrete failure mode this prevents: a PeriodicTimer that captures cts in the cleanup closure stops correctly when the dep changes, but only if you return () => { cts.Cancel(); timer.Dispose(); } from the body. Forget the cleanup and the old timer keeps firing alongside the new one. The scheduling internals — when each phase runs relative to the reconciler — are documented in effects-scheduling . Patterns # Data fetching with cancellation # When an effect kicks off an async fetch, the cleanup function must cancel it. Otherwise a stale request can land after the user has navigated away, overwriting state on the next component instance (or worse, throwing on a disposed setter): UseEffect(() => { var cts = new CancellationTokenSource(); _ = Task.Run( async () => { try { var data = await FetchAsync(query, cts.Token); setItems(data); } catch (OperationCanceledException) { /* expected */ } }); return () => cts.Cancel(); }, query); When query changes, cleanup cancels the in-flight fetch before the new one starts. For the cached / retry / focus-revalidation shape of the same pattern, reach for UseResource instead — it handles all of this plus a per-component cache key for free. Subscriptions with cleanup # The pattern is the same regardless of what you're subscribing to — INotifyPropertyChanged , an IObservable , a global event bus, a WinUI control's RoutedEventHandler . Subscribe in the body, return an unsubscribe in the cleanup: UseEffect(() => { void Handler ( object ? s, EventArgs e ) => setTick(t => t + 1 ); Source.Changed += Handler; return () => Source.Changed -= Handler; }, Source); // re-attach if Source identity changes If you only need to react to property changes on an INotifyPropertyChanged source, use UseObservable instead — it owns the subscribe/unsubscribe pair so you don't have to. UseResource for async data # For data that lives outside the component (server fetch, file read, slow computation), reach for UseResource rather than hand-rolling UseEffect + UseState . It returns an AsyncValue&#x3C;T> with Pending / Value / Error shape, handles cancellation on dep change, retries on await Refresh() , and shares a per-context query cache so two components reading the same key reuse the in-flight task. The pattern to recognize: if the effect body would be \"kick off a fetch, write the result to state, handle the loading flag, handle the error flag\", you're re-implementing UseResource . Common Mistakes # Object or array literals in the deps array # // Don't: var options = new { Url = url, Limit = 10 }; UseEffect(() => FetchAsync(options), options); // new instance every render options is a fresh anonymous object on every render — its reference compares unequal to the previous one, so the effect re-runs every commit and the fetch fires in a loop. The REACTOR_HOOKS_004 analyzer warns when a hook's deps argument is a freshly-allocated object, array, or lambda. Pass the primitives instead: UseEffect(() => FetchAsync(url, 10 ), url); Or memoize the container with UseMemo so its identity is stable across renders. Missing cleanup # // Don't: UseEffect(() => { var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); _ = Task.Run( async () => { while ( await timer.WaitForNextTickAsync()) setTick(t => t + 1 ); }); }, Array.Empty&#x3C; object >()); // no cleanup → timer fires forever after unmount The component unmounts, the timer keeps firing, the setter is called on a dead RenderContext , and the setter throws (or worse, silently leaks the closure tree the timer captured). Always return a cleanup that cancels the producer: UseEffect(() => { var cts = new CancellationTokenSource(); var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); _ = Task.Run( async () => { while ( await timer.WaitForNextTickAsync(cts.Token)) setTick(t => t + 1 ); }); return () => { cts.Cancel(); timer.Dispose(); }; }, Array.Empty&#x3C; object >()); Running an effect that should be a memo # // Don't: var (full, setFull) = UseState( \"\" ); UseEffect(() => setFull( $\" {first} {last} \" ), first, last); This pays for two extra renders (initial mount with empty full , then re-render after the effect writes) just to derive a string. The deriving should happen in render , either inline or via UseMemo for an expensive computation: var full = $\" {first} {last} \" ; // inline var stats = UseMemo(() => Compute(input), input); // memoized when expensive Effects are for side effects — work that reaches outside the component. Pure derivations of existing state belong in render. Tips # Use empty deps for one-time setup. UseEffect(action, Array.Empty&#x3C;object>()) is the equivalent of \"run on mount.\" Use it for initial data fetching, event registration, or logging. Always clean up resources. If your effect creates a timer, subscription, or event handler, return a cleanup function. Leaked resources cause bugs that are hard to trace. Keep dependency arrays honest. Include every value the effect reads from component state. Omitting a dependency doesn't prevent the read — it prevents the re-run, leading to stale closures. Don't call setState unconditionally in an effect that depends on that state. This creates an infinite loop. Guard with a condition or restructure the logic. Prefer UseReducer for complex effect-driven state. When an effect needs to update multiple related values, a reducer keeps the logic in one place and avoids cascading re-renders. Next Steps # Styling and Theming — Previous: apply theme tokens, colors, and dark/light mode Commanding — Next: bundle actions with labels, icons, and accelerators Hooks — Deep dive into UseState, UseReducer, and other hooks used by effects Async Resources — Cached async reads with UseResource and UseMutation Effects Scheduling — When effects run relative to render and commit Advanced Patterns — Combine effects with other hooks for complex scenarios",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/effects.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/effects.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "running-once-on-mount",
          "text": "Running Once on Mount"
        },
        {
          "depth": 2,
          "id": "running-when-dependencies-change",
          "text": "Running When Dependencies Change"
        },
        {
          "depth": 2,
          "id": "cleanup-with-timers",
          "text": "Cleanup with Timers"
        },
        {
          "depth": 2,
          "id": "async-data-loading",
          "text": "Async Data Loading"
        },
        {
          "depth": 2,
          "id": "avoiding-infinite-loops",
          "text": "Avoiding Infinite Loops"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "data-fetching-with-cancellation",
          "text": "Data fetching with cancellation"
        },
        {
          "depth": 3,
          "id": "subscriptions-with-cleanup",
          "text": "Subscriptions with cleanup"
        },
        {
          "depth": 3,
          "id": "useresource-for-async-data",
          "text": "UseResource for async data"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "object-or-array-literals-in-the-deps-array",
          "text": "Object or array literals in the deps array"
        },
        {
          "depth": 3,
          "id": "missing-cleanup",
          "text": "Missing cleanup"
        },
        {
          "depth": 3,
          "id": "running-an-effect-that-should-be-a-memo",
          "text": "Running an effect that should be a memo"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Context",
      "path": "context.md",
      "slug": "/context/",
      "html": "<p>A Microsoft.UI.Reactor (Reactor) <code>Context&#x3C;T></code> is namespaced dependency injection scoped to the\nelement tree, with reactivity. Define a context once as a static field,\npublish a value to any element's subtree with the <code>.Provide(ctx, value)</code>\nmodifier, and any descendant component reads the current value with\n<code>UseContext(ctx)</code>. The component does not need to know which ancestor\nprovided it; intermediate components do not need to forward the value as\na prop; and when the provided value changes, every consumer re-renders.\nThat is the same shape as React's <code>createContext</code> /\n<code>&#x3C;Provider value></code> / <code>useContext</code> triad, and the same shape as Compose's\n<code>CompositionLocal</code>. The most important property of the system is\n<em>value-identity awareness</em>: consumers re-render when the provided value\nis not equal to the previous value, so passing a fresh object literal\non every render thrashes every consumer in the subtree. Read this page\nwhen you have data that many distant components need (theme, current\nuser, locale, feature flags) and prop-threading would force every\nintermediate component to declare a prop it never uses.</p>\n<h1 id=\"context\">Context<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#context\">#</a></h1>\n<p>Context lets you pass data through the <a href=\"/components/\">component</a> tree without\nthreading it through every level of props. Define a <code>Context&#x3C;T></code>, provide a\nvalue at any level, and any descendant can read it with <code>UseContext</code>.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Shape</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Context&#x3C;T></code></td><td><code>new Context&#x3C;T>(T defaultValue)</code></td><td>The handle. Define once as a static field; <code>defaultValue</code> is returned when no provider is in scope.</td></tr><tr><td><code>.Provide(ctx, value)</code></td><td>Modifier on any <code>Element</code></td><td>Supplies <code>value</code> to descendants of this element. Multiple <code>.Provide(...)</code> chain into one merged dictionary.</td></tr><tr><td><code>UseContext(ctx)</code></td><td>Hook returning <code>T</code></td><td>Reads the current value from the nearest ancestor provider; returns <code>ctx.DefaultValue</code> if none.</td></tr></tbody></table>\n<p><code>Context&#x3C;T></code> is reactive: changing the provided <code>value</code> (by re-rendering\nthe provider with a different reference) re-renders every component in\nthe subtree that called <code>UseContext</code> on this context. Identity matters\n— see <a href=\"#stabilizing-the-provided-value\">memoizing the value</a>.</p>\n<h2 id=\"creating-a-context\">Creating a Context<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#creating-a-context\">#</a></h2>\n<p>A context is a static field with a default value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Contexts</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;<span class=\"hljs-built_in\">string</span>> ThemeMode = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"light\"</span>);\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;<span class=\"hljs-built_in\">string</span>> UserName = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Guest\"</span>);\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;<span class=\"hljs-built_in\">int</span>> FontScale = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p>Choose a sensible default — it makes components work standalone during\ndevelopment, in <a href=\"#testing-with-a-mock-provider\">test fixtures</a> without\nextra wiring, and in Storybook-style previews. A default of <code>null</code>\nforces every consumer to handle the unprovided case; a default that's a\nreal value is almost always nicer.</p>\n<h2 id=\"providing-and-consuming\">Providing and Consuming<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#providing-and-consuming\">#</a></h2>\n<p>Use <code>.Provide()</code> on any element to supply a value to its subtree. Use\n<code>UseContext()</code> in any descendant to read it:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ProvideConsumeExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Outside: no provider\"</span>),\n            VStack(<span class=\"hljs-number\">12</span>,\n                Component&#x3C;Greeting>()\n            ).Provide(Contexts.UserName, <span class=\"hljs-string\">\"Alice\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Greeting</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> name = UseContext(Contexts.UserName);\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Hello, <span class=\"hljs-subst\">{name}</span>!\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold();\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/provide-consume.png\" alt=\"Provider and consumer components\"></p>\n<p><code>.Provide()</code> is a modifier like <code>.Padding()</code> or <code>.Background()</code> (see\n<a href=\"/styling/\">styling</a>) — it works on any element and the value\npropagates to every descendant, no matter how deep. Components between\nthe provider and the consumer don't need to know the value exists.</p>\n<h2 id=\"theme-switching-with-context\">Theme Switching with Context<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#theme-switching-with-context\">#</a></h2>\n<p>A common use case is a theme toggle that affects an entire subtree. Here\nthe root component provides a theme value, and child components consume\nit:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemeSwitchExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (isDark, setIsDark) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> mode = isDark ? <span class=\"hljs-string\">\"dark\"</span> : <span class=\"hljs-string\">\"light\"</span>;\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            ToggleSwitch(isDark, setIsDark, onContent: <span class=\"hljs-string\">\"Dark\"</span>, offContent: <span class=\"hljs-string\">\"Light\"</span>),\n            VStack(<span class=\"hljs-number\">12</span>, Component&#x3C;ThemePanel>()).Provide(Contexts.ThemeMode, mode)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemePanel</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> theme = UseContext(Contexts.ThemeMode);\n        <span class=\"hljs-keyword\">var</span> elTheme = theme == <span class=\"hljs-string\">\"dark\"</span> ? ElementTheme.Dark : ElementTheme.Light;\n        <span class=\"hljs-keyword\">return</span> Border(\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">$\"Current theme: <span class=\"hljs-subst\">{theme}</span>\"</span>).Bold(),\n                TextBlock(<span class=\"hljs-string\">\"Panel adapts to context.\"</span>).Foreground(Theme.SecondaryText)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n        ).Background(Theme.CardBackground)\n         .CornerRadius(<span class=\"hljs-number\">8</span>)\n         .Set(b => b.RequestedTheme = elTheme);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/theme-switch.png\" alt=\"Theme toggle with light and dark panels\"></p>\n<p>The <code>ThemePanel</code> component reads the context and applies <code>ElementTheme</code>\naccordingly. When the toggle changes the provided value, all consumers\nre-render with the new theme. This is the same shape as Reactor's\n<a href=\"/styling/\">styling and theming</a> tokens use internally.</p>\n<h2 id=\"nested-context-overrides\">Nested Context Overrides<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#nested-context-overrides\">#</a></h2>\n<p>A child provider overrides its parent's value for its own subtree.\nSiblings keep the original value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NestedOverrideExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">16</span>,\n            VStack(<span class=\"hljs-number\">8</span>,\n                Caption(<span class=\"hljs-string\">\"Parent value\"</span>),\n                Component&#x3C;NameDisplay>()\n            ).Provide(Contexts.UserName, <span class=\"hljs-string\">\"Alice\"</span>),\n            VStack(<span class=\"hljs-number\">8</span>,\n                Caption(<span class=\"hljs-string\">\"Overridden child\"</span>),\n                VStack(<span class=\"hljs-number\">4</span>, Component&#x3C;NameDisplay>())\n                    .Provide(Contexts.UserName, <span class=\"hljs-string\">\"Bob\"</span>)\n            ).Provide(Contexts.UserName, <span class=\"hljs-string\">\"Alice\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NameDisplay</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> name = UseContext(Contexts.UserName);\n        <span class=\"hljs-keyword\">return</span> TextBlock(name).FontSize(<span class=\"hljs-number\">18</span>).SemiBold().Foreground(Theme.Accent);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/nested-override.png\" alt=\"Nested contexts with different values\"></p>\n<p>The inner <code>.Provide()</code> only affects descendants of that element. The\nsibling subtree still sees the parent's value. This lets you create\nlocal overrides — a single preview tile that always renders dark, an\naccent-color demo, a permission-denied subtree that re-scopes the\ncurrent-user context — without affecting the rest of the tree.</p>\n<h2 id=\"multiple-contexts\">Multiple Contexts<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#multiple-contexts\">#</a></h2>\n<p>Components can provide and consume multiple contexts simultaneously.\nEach context is independent:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MultipleContextsExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Component&#x3C;ProfileCard>()\n        ).Provide(Contexts.UserName, <span class=\"hljs-string\">\"Charlie\"</span>)\n         .Provide(Contexts.FontScale, <span class=\"hljs-number\">22</span>)\n         .Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ProfileCard</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> name = UseContext(Contexts.UserName);\n        <span class=\"hljs-keyword\">var</span> fontSize = UseContext(Contexts.FontScale);\n\n        <span class=\"hljs-keyword\">return</span> Border(\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(name).FontSize(fontSize).Bold(),\n                TextBlock(<span class=\"hljs-string\">$\"Font scale from context: <span class=\"hljs-subst\">{fontSize}</span>px\"</span>)\n                    .Foreground(Theme.SecondaryText)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n        ).Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">8</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/multiple-contexts.png\" alt=\"Component reading two contexts\"></p>\n<p>Each <code>Context&#x3C;T></code> is a separate channel. Providing one doesn't affect\nanother. A component can call <code>UseContext</code> as many times as it needs;\neach call subscribes that component to changes in its specific context.</p>\n<h2 id=\"current-user-pattern\">Current-user pattern<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#current-user-pattern\">#</a></h2>\n<p>A typed record at the app root, provided once, read from every page —\nthis is the canonical \"share state without prop-threading\" shape:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// A typed user-context record at the app root — every page reads the</span>\n<span class=\"hljs-comment\">// current user via UseContext rather than threading a User prop through</span>\n<span class=\"hljs-comment\">// every component along the way.</span>\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">CurrentUser</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">DisplayName</span>, <span class=\"hljs-title\">bool</span> <span class=\"hljs-title\">IsAdmin</span>);\n\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AppContexts</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;CurrentUser> User = <span class=\"hljs-keyword\">new</span>(\n        <span class=\"hljs-keyword\">new</span> CurrentUser(<span class=\"hljs-string\">\"guest\"</span>, <span class=\"hljs-string\">\"Guest\"</span>, IsAdmin: <span class=\"hljs-literal\">false</span>));\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">UserContextExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (user, setUser) = UseState(<span class=\"hljs-keyword\">new</span> CurrentUser(<span class=\"hljs-string\">\"u1\"</span>, <span class=\"hljs-string\">\"Alice\"</span>, IsAdmin: <span class=\"hljs-literal\">true</span>));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Sign in as Alice (admin)\"</span>,\n                    () => setUser(<span class=\"hljs-keyword\">new</span> CurrentUser(<span class=\"hljs-string\">\"u1\"</span>, <span class=\"hljs-string\">\"Alice\"</span>, IsAdmin: <span class=\"hljs-literal\">true</span>))),\n                Button(<span class=\"hljs-string\">\"Sign in as Bob (user)\"</span>,\n                    () => setUser(<span class=\"hljs-keyword\">new</span> CurrentUser(<span class=\"hljs-string\">\"u2\"</span>, <span class=\"hljs-string\">\"Bob\"</span>, IsAdmin: <span class=\"hljs-literal\">false</span>)))\n            ),\n            VStack(<span class=\"hljs-number\">8</span>,\n                Component&#x3C;AccountMenu>(),\n                Component&#x3C;AdminPanel>()\n            ).Provide(AppContexts.User, user)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AccountMenu</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> user = UseContext(AppContexts.User);\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Signed in as: <span class=\"hljs-subst\">{user.DisplayName}</span>\"</span>).SemiBold();\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AdminPanel</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> user = UseContext(AppContexts.User);\n        <span class=\"hljs-keyword\">return</span> user.IsAdmin\n            ? Border(TextBlock(<span class=\"hljs-string\">\"Admin tools available\"</span>).Padding(<span class=\"hljs-number\">8</span>))\n                .Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">4</span>)\n            : TextBlock(<span class=\"hljs-string\">\"(no admin tools)\"</span>).Foreground(Theme.SecondaryText);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/user-context.png\" alt=\"Account menu and admin panel driven by the same user context\"></p>\n<p>The signed-in user lives in <a href=\"/hooks/\"><code>UseState</code></a> at the app root.\nThe <code>.Provide(AppContexts.User, user)</code> modifier publishes it; the\n<code>AccountMenu</code> and <code>AdminPanel</code> components live anywhere in the tree\nand read it via <code>UseContext</code>. Sign in as a different user — both\npanels re-render because both subscribe to the same context.</p>\n<h2 id=\"stabilizing-the-provided-value\">Stabilizing the provided value<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stabilizing-the-provided-value\">#</a></h2>\n<p>The provided value's <em>identity</em> is what <code>UseContext</code> consumers compare\nagainst. Constructing a fresh object literal on every render — even\nwhen the underlying data hasn't changed — invalidates every consumer:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The value identity matters. Wrapping in UseMemo with explicit deps</span>\n<span class=\"hljs-comment\">// stops every consumer from re-rendering on every provider render —</span>\n<span class=\"hljs-comment\">// the inline-literal version below would create a fresh tuple every</span>\n<span class=\"hljs-comment\">// frame even when nothing changed.</span>\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">ThemeConfig</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Mode</span>, <span class=\"hljs-title\">int</span> <span class=\"hljs-title\">FontScale</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Accent</span>);\n\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemeContexts</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;ThemeConfig> Theme = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-keyword\">new</span> ThemeConfig(<span class=\"hljs-string\">\"light\"</span>, <span class=\"hljs-number\">14</span>, <span class=\"hljs-string\">\"#0078D4\"</span>));\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MemoizeContextValueExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (mode, setMode) = UseState(<span class=\"hljs-string\">\"light\"</span>);\n        <span class=\"hljs-keyword\">var</span> (scale, setScale) = UseState(<span class=\"hljs-number\">14</span>);\n\n        <span class=\"hljs-comment\">// GOOD — identity stable while inputs unchanged. Consumers only</span>\n        <span class=\"hljs-comment\">// re-render when mode or scale actually change.</span>\n        <span class=\"hljs-keyword\">var</span> theme = UseMemo(() => <span class=\"hljs-keyword\">new</span> ThemeConfig(mode, scale, <span class=\"hljs-string\">\"#0078D4\"</span>), mode, scale);\n\n        <span class=\"hljs-comment\">// BAD — every render creates a fresh ThemeConfig.</span>\n        <span class=\"hljs-comment\">// var theme = new ThemeConfig(mode, scale, \"#0078D4\");</span>\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Toggle mode\"</span>, () => setMode(mode == <span class=\"hljs-string\">\"light\"</span> ? <span class=\"hljs-string\">\"dark\"</span> : <span class=\"hljs-string\">\"light\"</span>)),\n                Button(<span class=\"hljs-string\">\"Bump scale\"</span>, () => setScale(scale + <span class=\"hljs-number\">2</span>))\n            ),\n            VStack(<span class=\"hljs-number\">8</span>, Component&#x3C;ThemedHeading>())\n                .Provide(ThemeContexts.Theme, theme)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemedHeading</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> t = UseContext(ThemeContexts.Theme);\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Headline @ <span class=\"hljs-subst\">{t.FontScale}</span>px (<span class=\"hljs-subst\">{t.Mode}</span>)\"</span>)\n            .FontSize(t.FontScale).Foreground(t.Accent);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/memoize-context-value.png\" alt=\"ThemeConfig context with UseMemo-stabilized identity\"></p>\n<p>The <code>UseMemo</code> call pins the <code>ThemeConfig</code> identity to its dependencies.\nConsumers only re-render when <code>mode</code> or <code>scale</code> actually changes. This\nis the same shape as React's\n<a href=\"https://react.dev/reference/react/useMemo#optimizing-re-rendering-of-components-that-use-context\"><code>useMemo</code> for context values</a>.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Context value-identity drives consumer re-renders. Passing a fresh\nobject literal on every render — <code>.Provide(ctx, new Config { ... })</code>\ninside <code>Render()</code> — causes every component that called <code>UseContext(ctx)</code>\nin the subtree to re-render every frame, even when the underlying data\ndidn't change. The classic failure mode: a theme provider at the app\nroot passes <code>new Theme(...)</code> inline; every page, every form field,\nevery text block re-renders on every state change anywhere in the\nparent tree. The fix is two-fold: (1) wrap the value in\n<a href=\"/hooks/\"><code>UseMemo</code></a> with explicit dependencies so identity is stable\nbetween updates, or (2) move the construction outside <code>Render()</code> so\nit's a singleton. The performance overlay (<code>mur docs perf-overlay</code>)\nmakes this visible — look for high \"context-driven re-render\" counts\nin the <a href=\"/dev-tooling/\">devtools</a> view.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"hoisting-state-to-the-nearest-common-ancestor\">Hoisting state to the nearest common ancestor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hoisting-state-to-the-nearest-common-ancestor\">#</a></h3>\n<p>The most-cited React tip — \"lift state up to the lowest ancestor that\nneeds to know about it\" — applies word-for-word in Reactor. Context is\nhow you propagate that lifted state back down without the boring\nmiddle. A two-pane editor with a shared toolbar that needs the selected\nitem: lift selection to the parent that owns both panes, provide it as\ncontext, both panes consume. The keyboard shortcut handler at the app\nroot also reads the same context — same pattern, no prop-threading.</p>\n<h3 id=\"testing-with-a-mock-provider\">Testing with a mock provider<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#testing-with-a-mock-provider\">#</a></h3>\n<p>Production wraps the component tree with a real user-fetch provider.\nTests render the <em>same</em> component tree wrapped with <code>.Provide(ctx, testStub)</code> — the consumer code is unchanged, and the test controls\nevery value via the stub:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Production root provides the real value; tests render the same</span>\n<span class=\"hljs-comment\">// component tree with a Provide(...) wrapper supplying a stub. The</span>\n<span class=\"hljs-comment\">// consumer code is unchanged — context lets you swap dependencies</span>\n<span class=\"hljs-comment\">// without re-plumbing props.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CartConsumer</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> user = UseContext(AppContexts.User);\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Cart for <span class=\"hljs-subst\">{user.DisplayName}</span> (<span class=\"hljs-subst\">{user.Id}</span>)\"</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MockProviderExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Two trees: one with the \"real\" production default, one with a</span>\n        <span class=\"hljs-comment\">// test stub supplied via .Provide(). Same CartConsumer in both.</span>\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">24</span>,\n            VStack(<span class=\"hljs-number\">8</span>,\n                Caption(<span class=\"hljs-string\">\"Production default\"</span>),\n                Component&#x3C;CartConsumer>()\n            ),\n            VStack(<span class=\"hljs-number\">8</span>,\n                Caption(<span class=\"hljs-string\">\"Test stub\"</span>),\n                VStack(<span class=\"hljs-number\">0</span>, Component&#x3C;CartConsumer>())\n                    .Provide(AppContexts.User, <span class=\"hljs-keyword\">new</span> CurrentUser(<span class=\"hljs-string\">\"test\"</span>, <span class=\"hljs-string\">\"Test User\"</span>, IsAdmin: <span class=\"hljs-literal\">false</span>))\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/context/mock-provider-test.png\" alt=\"Same CartConsumer rendered with default and a Provide-stub user\"></p>\n<p>This is the recommended pattern for unit tests that span more than one\ncomponent — see <a href=\"/testing/\">testing</a> for the renderer-fixture details\nand the snapshot-test surface.</p>\n<h3 id=\"scoped-feature-flags\">Scoped feature flags<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#scoped-feature-flags\">#</a></h3>\n<p>Read a feature flag from the nearest ancestor <code>Context&#x3C;FeatureFlags></code>,\noverride it for a single subtree to demo a beta surface, and the rest\nof the app stays on stable. The nested-override section above is the\nshape; the flag record is the payload.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"inline-literals-as-the-provided-value\">Inline literals as the provided value<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#inline-literals-as-the-provided-value\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">return</span> VStack(child)\n    .Provide(ctx, <span class=\"hljs-keyword\">new</span> ThemeConfig(mode, scale, <span class=\"hljs-string\">\"#0078D4\"</span>));\n</code></pre>\n<p>Every render constructs a fresh <code>ThemeConfig</code>, which compares unequal\nto the previous render's value, which re-renders every\n<code>UseContext(ctx)</code> consumer in the subtree. See the\n<a href=\"#stabilizing-the-provided-value\">stabilization caveat</a> above and use\n<a href=\"/hooks/\"><code>UseMemo</code></a> or hoist the construction.</p>\n<h3 id=\"reading-context-from-outside-any-provider\">Reading context from outside any provider<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-context-from-outside-any-provider\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Static field default of `null`.</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;CurrentUser?> User = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-literal\">null</span>);\n\n<span class=\"hljs-comment\">// In a component that's somehow rendered before the root provides:</span>\n<span class=\"hljs-keyword\">var</span> user = UseContext(AppContexts.User);\n<span class=\"hljs-keyword\">var</span> name = user.DisplayName;  <span class=\"hljs-comment\">// NullReferenceException</span>\n</code></pre>\n<p>When no ancestor has called <code>.Provide</code>, <code>UseContext</code> silently returns\n<code>ctx.DefaultValue</code> — there's no warning, no exception. The component\nruns against the default, which is rarely what the developer expected.\nEither pick a non-null default that's safe to render against, or treat\nthe default as an \"uninitialized\" sentinel and branch explicitly. The\n<a href=\"/recipes/login/\">init pattern</a> uses the latter — anonymous user is\nthe sentinel, the authentication flow swaps it out at the provider.</p>\n<h3 id=\"using-context-for-state-that-should-be-props\">Using context for state that should be props<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-context-for-state-that-should-be-props\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Context&#x3C;<span class=\"hljs-built_in\">int</span>> SelectedIndex = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">0</span>);\n\n<span class=\"hljs-comment\">// In a list component:</span>\n<span class=\"hljs-keyword\">var</span> index = UseContext(SelectedIndex);\n</code></pre>\n<p>If the value flows from one parent to one child, it's a prop. Context\nis for cross-cutting data — theme, locale, current user, feature flags\n— where many distant components at different depths need it. Forcing\na single-source-single-sink relationship through context obscures the\ndata flow and makes the component impossible to reuse with a different\nselection. See React's\n<a href=\"https://react.dev/learn/passing-data-deeply-with-context#before-you-use-context\">guidance on alternatives to context</a>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use context for cross-cutting concerns.</strong> Theme, locale, current\nuser, feature flags, dispatcher handles — anything that many\ncomponents at different depths need. Component-specific data is what\n<a href=\"/components/\">props</a> are for.</p>\n<p><strong>Always set a meaningful default.</strong> Components that read context\nshould work even without a provider — the default enables standalone\ntesting, Storybook-style previews, and ergonomic development against\nisolated components.</p>\n<p><strong>Memoize the provided value.</strong> Wrap the value in <a href=\"/hooks/\"><code>UseMemo</code></a>\nwith explicit dependencies. Inline literals on every render thrash\nevery consumer in the subtree.</p>\n<p><strong>Override locally where it helps.</strong> A debug-only <code>.Provide()</code> on a\nsingle test panel lets you preview an alternate theme, a different\nlocale, or a mocked user without rewiring the rest of the app. See the\n<a href=\"#nested-context-overrides\">nested overrides</a> section.</p>\n<p><strong>Compose context with <a href=\"/hooks/\"><code>UseState</code></a> for dynamic values.</strong>\nStore the value in state at the provider component, hand the setter\ndown through a separate command context if needed, and consumers\nupdate automatically when the setter is called.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Previous: bundle actions with labels, icons, and keyboard accelerators</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Next: focus traps, screen-reader announcements, ARIA roles</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — <code>UseContext</code>, <code>UseState</code>, <code>UseMemo</code>, and the rest of the hook surface</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Use context to propagate theme tokens across the app</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Props vs. context — when each shape is right</li>\n<li><strong><a href=\"/localization/\">Localization</a></strong> — <code>IntlContexts.Locale</code> is a real-world context in the framework</li>\n<li><strong><a href=\"/testing/\">Testing</a></strong> — Use <code>.Provide()</code> in test fixtures to inject stubs without re-plumbing props</li>\n</ul>",
      "text": "A Microsoft.UI.Reactor (Reactor) Context&#x3C;T> is namespaced dependency injection scoped to the element tree, with reactivity. Define a context once as a static field, publish a value to any element's subtree with the .Provide(ctx, value) modifier, and any descendant component reads the current value with UseContext(ctx) . The component does not need to know which ancestor provided it; intermediate components do not need to forward the value as a prop; and when the provided value changes, every consumer re-renders. That is the same shape as React's createContext / &#x3C;Provider value> / useContext triad, and the same shape as Compose's CompositionLocal . The most important property of the system is value-identity awareness : consumers re-render when the provided value is not equal to the previous value, so passing a fresh object literal on every render thrashes every consumer in the subtree. Read this page when you have data that many distant components need (theme, current user, locale, feature flags) and prop-threading would force every intermediate component to declare a prop it never uses. Context # Context lets you pass data through the component tree without threading it through every level of props. Define a Context&#x3C;T> , provide a value at any level, and any descendant can read it with UseContext . Reference # API Shape Purpose Context&#x3C;T> new Context&#x3C;T>(T defaultValue) The handle. Define once as a static field; defaultValue is returned when no provider is in scope. .Provide(ctx, value) Modifier on any Element Supplies value to descendants of this element. Multiple .Provide(...) chain into one merged dictionary. UseContext(ctx) Hook returning T Reads the current value from the nearest ancestor provider; returns ctx.DefaultValue if none. Context&#x3C;T> is reactive: changing the provided value (by re-rendering the provider with a different reference) re-renders every component in the subtree that called UseContext on this context. Identity matters — see memoizing the value . Creating a Context # A context is a static field with a default value: static class Contexts { public static Context&#x3C; string > ThemeMode = new ( \"light\" ); public static Context&#x3C; string > UserName = new ( \"Guest\" ); public static Context&#x3C; int > FontScale = new ( 16 ); } Choose a sensible default — it makes components work standalone during development, in test fixtures without extra wiring, and in Storybook-style previews. A default of null forces every consumer to handle the unprovided case; a default that's a real value is almost always nicer. Providing and Consuming # Use .Provide() on any element to supply a value to its subtree. Use UseContext() in any descendant to read it: class ProvideConsumeExample : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Outside: no provider\" ), VStack( 12 , Component&#x3C;Greeting>() ).Provide(Contexts.UserName, \"Alice\" ) ).Padding( 24 ); } } class Greeting : Component { public override Element Render () { var name = UseContext(Contexts.UserName); return TextBlock( $\"Hello, {name} !\" ).FontSize( 20 ).Bold(); } } .Provide() is a modifier like .Padding() or .Background() (see styling ) — it works on any element and the value propagates to every descendant, no matter how deep. Components between the provider and the consumer don't need to know the value exists. Theme Switching with Context # A common use case is a theme toggle that affects an entire subtree. Here the root component provides a theme value, and child components consume it: class ThemeSwitchExample : Component { public override Element Render () { var (isDark, setIsDark) = UseState( false ); var mode = isDark ? \"dark\" : \"light\" ; return VStack( 16 , ToggleSwitch(isDark, setIsDark, onContent: \"Dark\" , offContent: \"Light\" ), VStack( 12 , Component&#x3C;ThemePanel>()).Provide(Contexts.ThemeMode, mode) ).Padding( 24 ); } } class ThemePanel : Component { public override Element Render () { var theme = UseContext(Contexts.ThemeMode); var elTheme = theme == \"dark\" ? ElementTheme.Dark : ElementTheme.Light; return Border( VStack( 8 , TextBlock( $\"Current theme: {theme} \" ).Bold(), TextBlock( \"Panel adapts to context.\" ).Foreground(Theme.SecondaryText) ).Padding( 16 ) ).Background(Theme.CardBackground) .CornerRadius( 8 ) .Set(b => b.RequestedTheme = elTheme); } } The ThemePanel component reads the context and applies ElementTheme accordingly. When the toggle changes the provided value, all consumers re-render with the new theme. This is the same shape as Reactor's styling and theming tokens use internally. Nested Context Overrides # A child provider overrides its parent's value for its own subtree. Siblings keep the original value: class NestedOverrideExample : Component { public override Element Render () { return HStack( 16 , VStack( 8 , Caption( \"Parent value\" ), Component&#x3C;NameDisplay>() ).Provide(Contexts.UserName, \"Alice\" ), VStack( 8 , Caption( \"Overridden child\" ), VStack( 4 , Component&#x3C;NameDisplay>()) .Provide(Contexts.UserName, \"Bob\" ) ).Provide(Contexts.UserName, \"Alice\" ) ).Padding( 24 ); } } class NameDisplay : Component { public override Element Render () { var name = UseContext(Contexts.UserName); return TextBlock(name).FontSize( 18 ).SemiBold().Foreground(Theme.Accent); } } The inner .Provide() only affects descendants of that element. The sibling subtree still sees the parent's value. This lets you create local overrides — a single preview tile that always renders dark, an accent-color demo, a permission-denied subtree that re-scopes the current-user context — without affecting the rest of the tree. Multiple Contexts # Components can provide and consume multiple contexts simultaneously. Each context is independent: class MultipleContextsExample : Component { public override Element Render () { return VStack( 8 , Component&#x3C;ProfileCard>() ).Provide(Contexts.UserName, \"Charlie\" ) .Provide(Contexts.FontScale, 22 ) .Padding( 24 ); } } class ProfileCard : Component { public override Element Render () { var name = UseContext(Contexts.UserName); var fontSize = UseContext(Contexts.FontScale); return Border( VStack( 8 , TextBlock(name).FontSize(fontSize).Bold(), TextBlock( $\"Font scale from context: {fontSize} px\" ) .Foreground(Theme.SecondaryText) ).Padding( 16 ) ).Background(Theme.CardBackground).CornerRadius( 8 ); } } Each Context&#x3C;T> is a separate channel. Providing one doesn't affect another. A component can call UseContext as many times as it needs; each call subscribes that component to changes in its specific context. Current-user pattern # A typed record at the app root, provided once, read from every page — this is the canonical \"share state without prop-threading\" shape: // A typed user-context record at the app root — every page reads the // current user via UseContext rather than threading a User prop through // every component along the way. record CurrentUser ( string Id , string DisplayName , bool IsAdmin ); static class AppContexts { public static Context&#x3C;CurrentUser> User = new ( new CurrentUser( \"guest\" , \"Guest\" , IsAdmin: false )); } class UserContextExample : Component { public override Element Render () { var (user, setUser) = UseState( new CurrentUser( \"u1\" , \"Alice\" , IsAdmin: true )); return VStack( 12 , HStack( 8 , Button( \"Sign in as Alice (admin)\" , () => setUser( new CurrentUser( \"u1\" , \"Alice\" , IsAdmin: true ))), Button( \"Sign in as Bob (user)\" , () => setUser( new CurrentUser( \"u2\" , \"Bob\" , IsAdmin: false ))) ), VStack( 8 , Component&#x3C;AccountMenu>(), Component&#x3C;AdminPanel>() ).Provide(AppContexts.User, user) ).Padding( 24 ); } } class AccountMenu : Component { public override Element Render () { var user = UseContext(AppContexts.User); return TextBlock( $\"Signed in as: {user.DisplayName} \" ).SemiBold(); } } class AdminPanel : Component { public override Element Render () { var user = UseContext(AppContexts.User); return user.IsAdmin ? Border(TextBlock( \"Admin tools available\" ).Padding( 8 )) .Background(Theme.CardBackground).CornerRadius( 4 ) : TextBlock( \"(no admin tools)\" ).Foreground(Theme.SecondaryText); } } The signed-in user lives in UseState at the app root. The .Provide(AppContexts.User, user) modifier publishes it; the AccountMenu and AdminPanel components live anywhere in the tree and read it via UseContext . Sign in as a different user — both panels re-render because both subscribe to the same context. Stabilizing the provided value # The provided value's identity is what UseContext consumers compare against. Constructing a fresh object literal on every render — even when the underlying data hasn't changed — invalidates every consumer: // The value identity matters. Wrapping in UseMemo with explicit deps // stops every consumer from re-rendering on every provider render — // the inline-literal version below would create a fresh tuple every // frame even when nothing changed. record ThemeConfig ( string Mode , int FontScale , string Accent ); static class ThemeContexts { public static Context&#x3C;ThemeConfig> Theme = new ( new ThemeConfig( \"light\" , 14 , \"#0078D4\" )); } class MemoizeContextValueExample : Component { public override Element Render () { var (mode, setMode) = UseState( \"light\" ); var (scale, setScale) = UseState( 14 ); // GOOD — identity stable while inputs unchanged. Consumers only // re-render when mode or scale actually change. var theme = UseMemo(() => new ThemeConfig(mode, scale, \"#0078D4\" ), mode, scale); // BAD — every render creates a fresh ThemeConfig. // var theme = new ThemeConfig(mode, scale, \"#0078D4\"); return VStack( 12 , HStack( 8 , Button( \"Toggle mode\" , () => setMode(mode == \"light\" ? \"dark\" : \"light\" )), Button( \"Bump scale\" , () => setScale(scale + 2 )) ), VStack( 8 , Component&#x3C;ThemedHeading>()) .Provide(ThemeContexts.Theme, theme) ).Padding( 24 ); } } class ThemedHeading : Component { public override Element Render () { var t = UseContext(ThemeContexts.Theme); return TextBlock( $\"Headline @ {t.FontScale} px ( {t.Mode} )\" ) .FontSize(t.FontScale).Foreground(t.Accent); } } The UseMemo call pins the ThemeConfig identity to its dependencies. Consumers only re-render when mode or scale actually changes. This is the same shape as React's useMemo for context values . Caveat: Context value-identity drives consumer re-renders. Passing a fresh object literal on every render — .Provide(ctx, new Config { ... }) inside Render() — causes every component that called UseContext(ctx) in the subtree to re-render every frame, even when the underlying data didn't change. The classic failure mode: a theme provider at the app root passes new Theme(...) inline; every page, every form field, every text block re-renders on every state change anywhere in the parent tree. The fix is two-fold: (1) wrap the value in UseMemo with explicit dependencies so identity is stable between updates, or (2) move the construction outside Render() so it's a singleton. The performance overlay ( mur docs perf-overlay ) makes this visible — look for high \"context-driven re-render\" counts in the devtools view. Patterns # Hoisting state to the nearest common ancestor # The most-cited React tip — \"lift state up to the lowest ancestor that needs to know about it\" — applies word-for-word in Reactor. Context is how you propagate that lifted state back down without the boring middle. A two-pane editor with a shared toolbar that needs the selected item: lift selection to the parent that owns both panes, provide it as context, both panes consume. The keyboard shortcut handler at the app root also reads the same context — same pattern, no prop-threading. Testing with a mock provider # Production wraps the component tree with a real user-fetch provider. Tests render the same component tree wrapped with .Provide(ctx, testStub) — the consumer code is unchanged, and the test controls every value via the stub: // Production root provides the real value; tests render the same // component tree with a Provide(...) wrapper supplying a stub. The // consumer code is unchanged — context lets you swap dependencies // without re-plumbing props. class CartConsumer : Component { public override Element Render () { var user = UseContext(AppContexts.User); return TextBlock( $\"Cart for {user.DisplayName} ( {user.Id} )\" ); } } class MockProviderExample : Component { public override Element Render () { // Two trees: one with the \"real\" production default, one with a // test stub supplied via .Provide(). Same CartConsumer in both. return HStack( 24 , VStack( 8 , Caption( \"Production default\" ), Component&#x3C;CartConsumer>() ), VStack( 8 , Caption( \"Test stub\" ), VStack( 0 , Component&#x3C;CartConsumer>()) .Provide(AppContexts.User, new CurrentUser( \"test\" , \"Test User\" , IsAdmin: false )) ) ).Padding( 24 ); } } This is the recommended pattern for unit tests that span more than one component — see testing for the renderer-fixture details and the snapshot-test surface. Scoped feature flags # Read a feature flag from the nearest ancestor Context&#x3C;FeatureFlags> , override it for a single subtree to demo a beta surface, and the rest of the app stays on stable. The nested-override section above is the shape; the flag record is the payload. Common Mistakes # Inline literals as the provided value # // Don't: return VStack(child) .Provide(ctx, new ThemeConfig(mode, scale, \"#0078D4\" )); Every render constructs a fresh ThemeConfig , which compares unequal to the previous render's value, which re-renders every UseContext(ctx) consumer in the subtree. See the stabilization caveat above and use UseMemo or hoist the construction. Reading context from outside any provider # // Static field default of `null`. public static Context&#x3C;CurrentUser?> User = new ( null ); // In a component that's somehow rendered before the root provides: var user = UseContext(AppContexts.User); var name = user.DisplayName; // NullReferenceException When no ancestor has called .Provide , UseContext silently returns ctx.DefaultValue — there's no warning, no exception. The component runs against the default, which is rarely what the developer expected. Either pick a non-null default that's safe to render against, or treat the default as an \"uninitialized\" sentinel and branch explicitly. The init pattern uses the latter — anonymous user is the sentinel, the authentication flow swaps it out at the provider. Using context for state that should be props # // Don't: public static Context&#x3C; int > SelectedIndex = new ( 0 ); // In a list component: var index = UseContext(SelectedIndex); If the value flows from one parent to one child, it's a prop. Context is for cross-cutting data — theme, locale, current user, feature flags — where many distant components at different depths need it. Forcing a single-source-single-sink relationship through context obscures the data flow and makes the component impossible to reuse with a different selection. See React's guidance on alternatives to context . Tips # Use context for cross-cutting concerns. Theme, locale, current user, feature flags, dispatcher handles — anything that many components at different depths need. Component-specific data is what props are for. Always set a meaningful default. Components that read context should work even without a provider — the default enables standalone testing, Storybook-style previews, and ergonomic development against isolated components. Memoize the provided value. Wrap the value in UseMemo with explicit dependencies. Inline literals on every render thrash every consumer in the subtree. Override locally where it helps. A debug-only .Provide() on a single test panel lets you preview an alternate theme, a different locale, or a mocked user without rewiring the rest of the app. See the nested overrides section. Compose context with UseState for dynamic values. Store the value in state at the provider component, hand the setter down through a separate command context if needed, and consumers update automatically when the setter is called. Next Steps # Commanding — Previous: bundle actions with labels, icons, and keyboard accelerators Accessibility — Next: focus traps, screen-reader announcements, ARIA roles Hooks — UseContext , UseState , UseMemo , and the rest of the hook surface Styling and Theming — Use context to propagate theme tokens across the app Components — Props vs. context — when each shape is right Localization — IntlContexts.Locale is a real-world context in the framework Testing — Use .Provide() in test fixtures to inject stubs without re-plumbing props",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/context.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/context.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "creating-a-context",
          "text": "Creating a Context"
        },
        {
          "depth": 2,
          "id": "providing-and-consuming",
          "text": "Providing and Consuming"
        },
        {
          "depth": 2,
          "id": "theme-switching-with-context",
          "text": "Theme Switching with Context"
        },
        {
          "depth": 2,
          "id": "nested-context-overrides",
          "text": "Nested Context Overrides"
        },
        {
          "depth": 2,
          "id": "multiple-contexts",
          "text": "Multiple Contexts"
        },
        {
          "depth": 2,
          "id": "current-user-pattern",
          "text": "Current-user pattern"
        },
        {
          "depth": 2,
          "id": "stabilizing-the-provided-value",
          "text": "Stabilizing the provided value"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "hoisting-state-to-the-nearest-common-ancestor",
          "text": "Hoisting state to the nearest common ancestor"
        },
        {
          "depth": 3,
          "id": "testing-with-a-mock-provider",
          "text": "Testing with a mock provider"
        },
        {
          "depth": 3,
          "id": "scoped-feature-flags",
          "text": "Scoped feature flags"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "inline-literals-as-the-provided-value",
          "text": "Inline literals as the provided value"
        },
        {
          "depth": 3,
          "id": "reading-context-from-outside-any-provider",
          "text": "Reading context from outside any provider"
        },
        {
          "depth": 3,
          "id": "using-context-for-state-that-should-be-props",
          "text": "Using context for state that should be props"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Commanding",
      "path": "commanding.md",
      "slug": "/commanding/",
      "html": "<p>A Microsoft.UI.Reactor (Reactor) <code>Command</code> is the unit of \"what the user can do\" in a Reactor\napp. It bundles the action, its label, icon, keyboard accelerator,\ndescription, and enabled state into a single immutable record that you\nbind from multiple call sites simultaneously. The button that runs the\naction, the menu item in the toolbar overflow, the right-click flyout\nentry, and the Ctrl+S keyboard binding all reference the same record —\ndisable the command, and every surface disables in lock-step; change\nits label for <a href=\"/localization/\">localization</a>, and every surface re-\nlabels. The contract is <em>synchronized state across surfaces</em> — the\nopposite of the WPF <code>RoutedCommand</code> model where each surface registers\nits own binding and <code>CanExecuteChanged</code> events fan out invalidations.\nRead this page when you have an action that lives in more than one\nplace (toolbar + menu + keyboard), when you need async tracking with\ndisable-while-running semantics, or when you're wiring a\n<a href=\"/dialogs-and-flyouts/\">dialog's primary button</a> to a real action\nrather than an inline lambda.</p>\n<h1 id=\"commanding\">Commanding<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#commanding\">#</a></h1>\n<p>A <code>Command</code> bundles an action with its metadata. Define it once and use it\nacross buttons, menus, toolbars, and dialogs — the metadata stays consistent\neverywhere.</p>\n<h2 id=\"defining-a-command\">Defining a Command<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#defining-a-command\">#</a></h2>\n<p>Create a command with the properties you need:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BasicCommandExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"Hello, World!\"</span>);\n        <span class=\"hljs-keyword\">var</span> (saved, setSaved) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">var</span> saveCmd = <span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Save\"</span>,\n            Execute = () => setSaved(<span class=\"hljs-literal\">true</span>),\n            CanExecute = !saved,\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Save\"</span>),\n            Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(text, v => { setText(v); setSaved(<span class=\"hljs-literal\">false</span>); })\n                .Width(<span class=\"hljs-number\">400</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(saveCmd),\n                When(saved, () => TextBlock(<span class=\"hljs-string\">\"Saved!\"</span>).Foreground(Theme.SystemSuccess))\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/basic-command.png\" alt=\"Editor with save command\"></p>\n<p>Pass a <code>Command</code> to <code>Button()</code>, <code>MenuItem()</code>, or <code>AppBarButton()</code> and the\nlabel, icon, accelerator, and enabled state are wired automatically. You\ndo not set them individually on each control.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Member</th><th>Type</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Label</code></td><td><code>string</code> (required)</td><td>Text rendered on the button / menu item / tooltip.</td></tr><tr><td><code>Execute</code></td><td><code>Action?</code></td><td>Synchronous action. Mutually exclusive with <code>ExecuteAsync</code>.</td></tr><tr><td><code>ExecuteAsync</code></td><td><code>Func&#x3C;Task>?</code></td><td>Async action — pair with <a href=\"/hooks/\"><code>UseCommand</code></a> for tracking.</td></tr><tr><td><code>CanExecute</code></td><td><code>bool</code> (default <code>true</code>)</td><td>Whether the action can run right now.</td></tr><tr><td><code>IsExecuting</code></td><td><code>bool</code></td><td>Managed by <code>UseCommand</code> while async work is in flight.</td></tr><tr><td><code>Icon</code></td><td><code>IconData?</code></td><td><code>SymbolIcon(name)</code>, <code>FontIcon(...)</code>, or <code>BitmapIcon(uri)</code>.</td></tr><tr><td><code>Description</code></td><td><code>string?</code></td><td>Tooltip / accessibility description.</td></tr><tr><td><code>Accelerator</code></td><td><code>KeyboardAcceleratorData?</code></td><td>Keyboard binding (<code>Accelerator(VirtualKey.S, Control)</code>).</td></tr><tr><td><code>AccessKey</code></td><td><code>string?</code></td><td>Single-character Alt-prefix shortcut for menu items.</td></tr><tr><td><code>IsEnabled</code></td><td><code>bool</code> (computed)</td><td><code>CanExecute &#x26;&#x26; !IsExecuting</code> — what every surface reads.</td></tr></tbody></table>\n<p><code>Command&#x3C;T></code> exposes the same members with <code>Execute: Action&#x3C;T>?</code> and\n<code>ExecuteAsync: Func&#x3C;T, Task>?</code> — the action receives an argument from\nthe call site.</p>\n<h2 id=\"standard-commands\">Standard Commands<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#standard-commands\">#</a></h2>\n<p><code>StandardCommand</code> provides factory methods for the 16 most common application\nactions. Each comes with a label, icon, and keyboard accelerator preset:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StandardCommandsExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (log, updateLog) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n\n        <span class=\"hljs-keyword\">var</span> cut = StandardCommand.Cut(() => updateLog(l => [.. l, <span class=\"hljs-string\">\"Cut\"</span>]));\n        <span class=\"hljs-keyword\">var</span> copy = StandardCommand.Copy(() => updateLog(l => [.. l, <span class=\"hljs-string\">\"Copy\"</span>]));\n        <span class=\"hljs-keyword\">var</span> paste = StandardCommand.Paste(() => updateLog(l => [.. l, <span class=\"hljs-string\">\"Paste\"</span>]));\n        <span class=\"hljs-keyword\">var</span> undo = StandardCommand.Undo(\n            () => updateLog(l => [.. l, <span class=\"hljs-string\">\"Undo\"</span>]),\n            canExecute: log.Count > <span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            CommandBar(\n                primaryCommands: <span class=\"hljs-keyword\">new</span>[] { AppBarButton(cut), AppBarButton(copy),\n                    AppBarButton(paste), AppBarButton(undo) }\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"Actions: <span class=\"hljs-subst\">{<span class=\"hljs-built_in\">string</span>.Join(<span class=\"hljs-string\">\", \"</span>, log)}</span>\"</span>).Padding(<span class=\"hljs-number\">12</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/standard-commands.png\" alt=\"Command bar with standard commands\"></p>\n<p>Available: <code>Cut</code>, <code>Copy</code>, <code>Paste</code>, <code>Undo</code>, <code>Redo</code>, <code>Delete</code>, <code>SelectAll</code>,\n<code>Save</code>, <code>Open</code>, <code>Close</code>, <code>Share</code>, <code>Play</code>, <code>Pause</code>, <code>Stop</code>, <code>Forward</code>,\n<code>Backward</code>. Each factory takes the action as its first argument and\noptional <code>canExecute:</code> / <code>label:</code> overrides — the icon and accelerator\ncome from the preset.</p>\n<h2 id=\"one-command-many-surfaces\">One Command, Many Surfaces<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#one-command-many-surfaces\">#</a></h2>\n<p>The point of the model: one declaration drives every surface. Bind the\nsame <code>Command</code> to a <code>Button</code> and a <code>MenuFlyout</code> item — clicking either\nruns the action, disabling the command disables both, and the\n<a href=\"#keyboard-accelerators\">keyboard accelerator</a> routes through the same\ntarget:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ButtonAndMenuExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (saves, setSaves) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-comment\">// One Command. Two surfaces. Identical enabled-state, label, icon, accelerator.</span>\n        <span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Save\"</span>,\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Save\"</span>),\n            Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control),\n            Execute = () => setSaves(saves + <span class=\"hljs-number\">1</span>),\n            CanExecute = saves &#x3C; <span class=\"hljs-number\">3</span>,\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            <span class=\"hljs-comment\">// Button surface.</span>\n            Button(save),\n            <span class=\"hljs-comment\">// MenuFlyout surface — same Command record.</span>\n            MenuFlyout(\n                Button(<span class=\"hljs-string\">\"File…\"</span>),\n                MenuItem(save)),\n            TextBlock(<span class=\"hljs-string\">$\"Saved <span class=\"hljs-subst\">{saves}</span> time(s); CanExecute=<span class=\"hljs-subst\">{save.CanExecute}</span>\"</span>)\n                .Foreground(Theme.SecondaryText)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/button-and-menu.png\" alt=\"Button and MenuFlyout driven by the same Command\"></p>\n<p>The synchronized-state contract is the value. Without it, each surface\nwould compute its own <code>IsEnabled</code>, each would carry its own label\nduplicate, and the keyboard binding would have to be wired by hand.\nWith it, the <code>Command</code> record is the source of truth; the surfaces are\nprojections.</p>\n<h2 id=\"async-commands-and-usecommand\">Async Commands and UseCommand<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-commands-and-usecommand\">#</a></h2>\n<p>When a command has an <code>ExecuteAsync</code> action, wrap it with the\n<a href=\"/hooks/\"><code>UseCommand</code></a> hook to get automatic <code>IsExecuting</code> tracking\nand a re-entrance guard. The button disables itself while the async\noperation runs; a second click is dropped:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AsyncCommandExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (status, setStatus) = UseState(<span class=\"hljs-string\">\"Ready\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> saveCmd = UseCommand(<span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Save to Cloud\"</span>,\n            ExecuteAsync = <span class=\"hljs-keyword\">async</span> () =>\n            {\n                setStatus(<span class=\"hljs-string\">\"Saving...\"</span>);\n                <span class=\"hljs-keyword\">await</span> Task.Delay(<span class=\"hljs-number\">2000</span>);\n                setStatus(<span class=\"hljs-string\">\"Saved at \"</span> + DateTime.Now.ToString(<span class=\"hljs-string\">\"HH:mm:ss\"</span>));\n            },\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Save\"</span>)\n        });\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(saveCmd),\n                TextBlock(status).Foreground(Theme.SecondaryText)\n            ),\n            When(saveCmd.IsExecuting, () =>\n                ProgressRing().Width(<span class=\"hljs-number\">20</span>).Height(<span class=\"hljs-number\">20</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/async-command.png\" alt=\"Save button disabled during async operation\"></p>\n<p><code>UseCommand</code> sets <code>IsExecuting = true</code> <em>before</em> invoking the wrapped\naction and clears it in a <code>finally</code> block — so a throwing\n<code>ExecuteAsync</code> still unwinds the busy state. Read <code>command.IsExecuting</code>\nfrom any surface that wants to render progress.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AsyncWithProgressExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (progress, setProgress) = UseState(<span class=\"hljs-number\">0.0</span>);\n\n        <span class=\"hljs-keyword\">var</span> upload = UseCommand(<span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Upload\"</span>,\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Upload\"</span>),\n            ExecuteAsync = <span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">var</span> i = <span class=\"hljs-number\">0</span>; i &#x3C;= <span class=\"hljs-number\">100</span>; i += <span class=\"hljs-number\">10</span>)\n                {\n                    setProgress(i / <span class=\"hljs-number\">100.0</span>);\n                    <span class=\"hljs-keyword\">await</span> Task.Delay(<span class=\"hljs-number\">120</span>);\n                }\n            },\n        });\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(upload),\n                When(upload.IsExecuting, () =>\n                    TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{(<span class=\"hljs-built_in\">int</span>)(progress * <span class=\"hljs-number\">100</span>)}</span>%\"</span>)\n                        .Foreground(Theme.SecondaryText))\n            ),\n            When(upload.IsExecuting, () =>\n                Progress(progress * <span class=\"hljs-number\">100</span>).Width(<span class=\"hljs-number\">300</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/async-with-progress.png\" alt=\"ProgressBar driven by Command IsExecuting\"></p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>UseCommand</code> sets <code>IsExecuting = true</code> synchronously before the awaited\nbody runs, and clears it in a <code>finally</code> block after the awaited body\ncompletes — but a single <code>Command</code> record bound to two surfaces shares\none <code>IsExecuting</code> flag. That is intentional for the common case (one\nSave button + one Save menu item should <em>both</em> disable while saving,\npreventing double-submit) and surprising for the cross-page case: if\nthe same <code>Command</code> instance is bound from two\n<a href=\"/navigation/\">NavigationView</a> pages, the disable state crosses pages\ntoo — saving from page A leaves page B's Save button disabled until\nthe await completes. Hoist the <code>Command</code> to the navigation root and\nthe behavior is correct; recreate it per page (via\n<a href=\"/hooks/\"><code>UseMemo</code></a> keyed on page identity) when you want\nper-surface isolation.</p>\n</blockquote>\n<h2 id=\"parameterized-commands\">Parameterized commands<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#parameterized-commands\">#</a></h2>\n<p><code>Command&#x3C;T></code> lets a single command apply to every row of a list, every\nselected item in a grid, or any other \"do X with this thing\" pattern.\nThe action receives the argument from the call site:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">TodoItem</span>(<span class=\"hljs-title\">int</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Title</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ParameterizedCommandExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, setItems) = UseState&#x3C;IReadOnlyList&#x3C;TodoItem>>(\n            <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-keyword\">new</span> TodoItem(<span class=\"hljs-number\">1</span>, <span class=\"hljs-string\">\"Buy milk\"</span>), <span class=\"hljs-keyword\">new</span> TodoItem(<span class=\"hljs-number\">2</span>, <span class=\"hljs-string\">\"Walk dog\"</span>), <span class=\"hljs-keyword\">new</span> TodoItem(<span class=\"hljs-number\">3</span>, <span class=\"hljs-string\">\"Ship doc\"</span>) });\n\n        <span class=\"hljs-comment\">// One Command&#x3C;TodoItem> drives every row.</span>\n        <span class=\"hljs-keyword\">var</span> delete = <span class=\"hljs-keyword\">new</span> Command&#x3C;TodoItem>\n        {\n            Label = <span class=\"hljs-string\">\"Delete\"</span>,\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Delete\"</span>),\n            Execute = item => setItems(items.Where(i => i.Id != item.Id).ToList()),\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            ForEach(items, item =>\n                HStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(item.Title).Width(<span class=\"hljs-number\">180</span>),\n                    <span class=\"hljs-comment\">// Inline button — Command&#x3C;T> doesn't have a Button(cmd, arg) overload</span>\n                    <span class=\"hljs-comment\">// by design, so call .Execute(arg) directly from the click handler.</span>\n                    Button(delete.Label, () => delete.Execute?.Invoke(item))\n                        .IsEnabled(delete.IsEnabled)))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/parameterized-command.png\" alt=\"Single Command<TodoItem> driving Delete on every row\"></p>\n<p>For menu integration, the variadic <code>MenuItem(Command&#x3C;T>, T parameter)</code>\noverload binds the row's data to the menu item:</p>\n<pre><code class=\"hljs language-csharp\">MenuFlyout(rowContent,\n    MenuItem(deleteCommand, item),\n    MenuItem(renameCommand, item))\n</code></pre>\n<p>This is the same shape as the <a href=\"/dialogs-and-flyouts/\">right-click on a list row\npattern</a> — one parameterized command, one\ncontext-menu declaration, every row carries its own data.</p>\n<h2 id=\"command-bar-integration\">Command Bar Integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#command-bar-integration\">#</a></h2>\n<p><code>CommandBar</code> with <code>AppBarButton</code> is the canonical toolbar surface.\nPrimary commands render inline; secondary commands collapse into the\noverflow menu:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CommandBarExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"Edit me\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> save = StandardCommand.Save(() => { });\n        <span class=\"hljs-keyword\">var</span> copy = StandardCommand.Copy(() => { });\n        <span class=\"hljs-keyword\">var</span> delete = StandardCommand.Delete(\n            () => setText(<span class=\"hljs-string\">\"\"</span>), canExecute: text.Length > <span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n            CommandBar(\n                primaryCommands: <span class=\"hljs-keyword\">new</span>[] {\n                    AppBarButton(save), AppBarButton(copy) },\n                secondaryCommands: <span class=\"hljs-keyword\">new</span>[] {\n                    AppBarButton(delete) }\n            ),\n            TextBox(text, setText).Margin(<span class=\"hljs-number\">16</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/command-bar.png\" alt=\"Command bar with primary and secondary commands\"></p>\n<p>The <code>AppBarButton</code> variant of every button supports <code>Command</code> directly;\nthere is no second wiring step. Use <code>CommandBarFlyout</code> for the same\nshape rendered next to a selection — see\n<a href=\"/dialogs-and-flyouts/#commandbarflyout\">dialogs and flyouts</a>.</p>\n<h2 id=\"menu-integration\">Menu Integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#menu-integration\">#</a></h2>\n<p>Commands work in menu bars too. The accelerator text (like Ctrl+S)\nappears automatically next to the menu item:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MenuBarExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"Document text\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> save = StandardCommand.Save(() => { });\n        <span class=\"hljs-keyword\">var</span> close = StandardCommand.Close(() => setText(<span class=\"hljs-string\">\"\"</span>));\n        <span class=\"hljs-keyword\">var</span> undo = StandardCommand.Undo(() => { });\n        <span class=\"hljs-keyword\">var</span> redo = StandardCommand.Redo(() => { });\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n            MenuBar(\n                Menu(<span class=\"hljs-string\">\"File\"</span>, MenuItem(save), MenuItem(close)),\n                Menu(<span class=\"hljs-string\">\"Edit\"</span>, MenuItem(undo), MenuItem(redo))\n            ),\n            TextBlock(text).Padding(<span class=\"hljs-number\">16</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/commanding/menu-bar.png\" alt=\"Menu bar with File and Edit menus\"></p>\n<p><code>MenuItem(Command)</code> and <code>MenuItem&#x3C;T>(Command&#x3C;T>, T parameter)</code> are the\ntwo factory overloads. The latter is what powers per-row context\nmenus in <a href=\"/collections/\"><code>ListView</code></a> and <a href=\"/data-system/\"><code>DataGrid</code></a>.</p>\n<h2 id=\"keyboard-accelerators\">Keyboard accelerators<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyboard-accelerators\">#</a></h2>\n<p>The <code>Accelerator</code> property on <code>Command</code> is the source of truth for the\nkeyboard binding. The accelerator is wired at the WinUI level — the\nbinding lives on whichever surface is rendering the command (the\nbutton or the menu item), and Reactor inherits WinUI's \"keyboard\naccelerators are scoped to the focused element's ancestor chain\" rule.\nFor a global accelerator that fires regardless of focus, render the\ncommand from a <code>MenuBar</code> or <code>CommandBar</code> at the window root — both\nattach their accelerators to the window's <code>KeyboardAccelerators</code>\ncollection, which is window-scoped.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Save\"</span>,\n    Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control),\n    Execute = () => SaveDocument(),\n};\n\n<span class=\"hljs-comment\">// Window-scoped via MenuBar at the root.</span>\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n    MenuBar(Menu(<span class=\"hljs-string\">\"File\"</span>, MenuItem(save))),\n    content);\n</code></pre>\n<p>For a command palette-style global shortcut catalog, see\n<a href=\"/recipes/command-palette/\">recipes/command-palette</a>.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"async-confirmation-dialog\">Async confirmation dialog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-confirmation-dialog\">#</a></h3>\n<p>The classic delete confirmation — primary button runs an <code>ExecuteAsync</code>,\nthe dialog stays open until the await completes, the primary disables\nmid-flight. Wrap with <code>UseCommand</code>, bind <code>IsPrimaryButtonEnabled</code> to\n<code>command.IsEnabled</code>, and close the dialog from inside the async body:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> delete = ctx.UseCommand(<span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Delete\"</span>,\n    ExecuteAsync = <span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">await</span> api.DeleteAsync(id);\n        setOpen(<span class=\"hljs-literal\">false</span>);\n    },\n});\n\nContentDialog(<span class=\"hljs-string\">\"Delete?\"</span>, body, primaryButtonText: <span class=\"hljs-string\">\"Delete\"</span>) <span class=\"hljs-keyword\">with</span>\n{\n    IsOpen = open,\n    IsPrimaryButtonEnabled = delete.IsEnabled,\n    OnClosed = r =>\n    {\n        <span class=\"hljs-keyword\">if</span> (r == ContentDialogResult.Primary) delete.Execute?.Invoke();\n        <span class=\"hljs-keyword\">else</span> setOpen(<span class=\"hljs-literal\">false</span>);\n    },\n}\n</code></pre>\n<p>See <a href=\"/dialogs-and-flyouts/\">dialogs and flyouts</a> for the full pattern.</p>\n<h3 id=\"localized-commands\">Localized commands<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#localized-commands\">#</a></h3>\n<p><code>StandardCommand</code> presets ship with English labels. Override per render\nusing the <code>with { ... }</code> expression on the returned record:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> save = StandardCommand.Save(action) <span class=\"hljs-keyword\">with</span>\n{\n    Label = intl.Get(<span class=\"hljs-string\">\"save.button\"</span>),\n    Description = intl.Get(<span class=\"hljs-string\">\"save.tooltip\"</span>),\n};\n</code></pre>\n<p>See <a href=\"/localization/\">localization</a> for the <code>UseIntl</code> accessor.</p>\n<h3 id=\"one-command-three-surfaces-one-accelerator\">One command, three surfaces, one accelerator<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#one-command-three-surfaces-one-accelerator\">#</a></h3>\n<p>The shape that motivates the whole model — Ctrl+S triggers the same\naction whether the user pressed it from the editor body, opened the\nFile menu and clicked Save, or clicked the toolbar Save button:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Save\"</span>,\n    Icon = SymbolIcon(<span class=\"hljs-string\">\"Save\"</span>),\n    Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control),\n    ExecuteAsync = SaveDocumentAsync,\n};\n<span class=\"hljs-keyword\">var</span> saveWrapped = UseCommand(save);\n\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n    MenuBar(Menu(<span class=\"hljs-string\">\"File\"</span>, MenuItem(saveWrapped))),\n    CommandBar(primaryCommands: <span class=\"hljs-keyword\">new</span>[] { AppBarButton(saveWrapped) }),\n    editorBody);\n</code></pre>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"creating-commands-inside-render-without-memoization\">Creating Commands inside render without memoization<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#creating-commands-inside-render-without-memoization\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't: re-create the Command on every render — every surface that</span>\n<span class=\"hljs-comment\">// holds the previous reference sees a fresh identity each frame, which</span>\n<span class=\"hljs-comment\">// thrashes the WinUI keyboard-accelerator wiring and re-renders every</span>\n<span class=\"hljs-comment\">// consumer. Lift to a memo or hoist out of Render().</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DontCreateInRender</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// BAD — Command identity churns every render:</span>\n        <span class=\"hljs-comment\">// var save = new Command { Label = \"Save\", Execute = () => { } };</span>\n\n        <span class=\"hljs-comment\">// GOOD — UseMemo pins identity until deps change:</span>\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> save = UseMemo(() => <span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Save\"</span>,\n            Execute = () => setCount(count + <span class=\"hljs-number\">1</span>),\n        }, count);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>, Button(save), TextBlock(<span class=\"hljs-string\">$\"Saved <span class=\"hljs-subst\">{count}</span>\"</span>)).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Re-creating the <code>Command</code> record every frame creates a fresh\n<code>KeyboardAccelerator</code> and forces every consumer to re-render. The\nwindow's accelerator table grows without bound on rapid re-renders and\nthe analyzer fires <code>REACTOR_PERF_FUNCREF</code> for the inline lambda\nidentity. Wrap the construction in <a href=\"/hooks/\"><code>UseMemo</code></a> with the\ncorrect dependencies, or hoist the command above the component.</p>\n<h3 id=\"ignoring-canexecute-changes\">Ignoring CanExecute changes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#ignoring-canexecute-changes\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Save\"</span>,\n    Execute = () => { <span class=\"hljs-keyword\">if</span> (form.IsValid) Save(); },\n};\n</code></pre>\n<p>Pushing the guard inside <code>Execute</code> works but loses the synchronized\ndisable across surfaces — the toolbar button stays <em>enabled-looking</em>,\nthe menu item is still focusable, and the user clicks expecting an\naction. Promote the predicate to <code>CanExecute</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Save\"</span>,\n    Execute = Save,\n    CanExecute = form.IsValid,\n};\n</code></pre>\n<p>Now the button greys out, the menu item disables, and the accelerator\nno-ops — one decision, every surface.</p>\n<h3 id=\"awaiting-in-a-non-async-event-handler\">Awaiting in a non-async event handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#awaiting-in-a-non-async-event-handler\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, <span class=\"hljs-keyword\">async</span> () => { <span class=\"hljs-keyword\">await</span> api.SaveAsync(); setSaved(<span class=\"hljs-literal\">true</span>); })\n</code></pre>\n<p>The inline async lambda runs unmanaged — there is no <code>IsExecuting</code>,\nno re-entrance guard, no busy state to render against. The user\nclicks five times during the await and starts five concurrent saves.\nPromote the action to <code>ExecuteAsync</code> on a <code>Command</code>, wrap with\n<a href=\"/hooks/\"><code>UseCommand</code></a>, and bind the button to the command. The\nre-entrance guard drops duplicate clicks and <code>IsExecuting</code> lights up\nthe spinner.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use <code>StandardCommand</code> for common operations.</strong> It saves you from manually\nspecifying icons and keyboard accelerators for the 16 most common actions\nand keeps your surface consistent with WinUI conventions.</p>\n<p><strong>Always wrap async commands with <code>UseCommand</code>.</strong> It prevents\ndouble-execution via the re-entrance guard, tracks <code>IsExecuting</code>, and\nclears the busy flag in a <code>finally</code> so exceptions don't leave the UI\nstuck disabled.</p>\n<p><strong>Read <code>command.IsExecuting</code> for loading indicators.</strong> Any surface\nbound to a wrapped async command can render a spinner from the same\nflag — the <a href=\"/hooks/\"><code>UseCommand</code></a> wrapper guarantees the flag is\nlive across re-renders.</p>\n<p><strong>Commands are records — use <code>with</code> to customize.</strong> Override the label\nfor <a href=\"/localization/\">localization</a>:\n<code>StandardCommand.Save(action) with { Label = \"Guardar\" }</code>. The original\npreset stays intact for the next caller.</p>\n<p><strong>Define commands at the call site for one-offs; hoist them when shared.</strong>\nA single Save button can declare its command inline. A Save action used\nin the toolbar, the menu, the keyboard binding, and the dialog primary\nbelongs at the parent component or in a <code>UseMemo</code> so identity is stable.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — Previous: run side effects for timers, data loading, and cleanup</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — Next: share data across the component tree without prop drilling</li>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a></strong> — Wire dialog primaries and right-click menus to commands</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — <code>UseCommand</code> and the rest of the hook surface</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — Scope commands to a page vs. a window</li>\n<li><strong><a href=\"/recipes/command-palette/\">Recipes: Command palette</a></strong> — End-to-end recipe for a Ctrl+K palette</li>\n<li><strong><a href=\"/localization/\">Localization</a></strong> — Localize command labels and accelerator text</li>\n</ul>",
      "text": "A Microsoft.UI.Reactor (Reactor) Command is the unit of \"what the user can do\" in a Reactor app. It bundles the action, its label, icon, keyboard accelerator, description, and enabled state into a single immutable record that you bind from multiple call sites simultaneously. The button that runs the action, the menu item in the toolbar overflow, the right-click flyout entry, and the Ctrl+S keyboard binding all reference the same record — disable the command, and every surface disables in lock-step; change its label for localization , and every surface re- labels. The contract is synchronized state across surfaces — the opposite of the WPF RoutedCommand model where each surface registers its own binding and CanExecuteChanged events fan out invalidations. Read this page when you have an action that lives in more than one place (toolbar + menu + keyboard), when you need async tracking with disable-while-running semantics, or when you're wiring a dialog's primary button to a real action rather than an inline lambda. Commanding # A Command bundles an action with its metadata. Define it once and use it across buttons, menus, toolbars, and dialogs — the metadata stays consistent everywhere. Defining a Command # Create a command with the properties you need: class BasicCommandExample : Component { public override Element Render () { var (text, setText) = UseState( \"Hello, World!\" ); var (saved, setSaved) = UseState( false ); var saveCmd = new Command { Label = \"Save\" , Execute = () => setSaved( true ), CanExecute = !saved, Icon = SymbolIcon( \"Save\" ), Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control) }; return VStack( 12 , TextBox(text, v => { setText(v); setSaved( false ); }) .Width( 400 ), HStack( 8 , Button(saveCmd), When(saved, () => TextBlock( \"Saved!\" ).Foreground(Theme.SystemSuccess)) ) ).Padding( 24 ); } } Pass a Command to Button() , MenuItem() , or AppBarButton() and the label, icon, accelerator, and enabled state are wired automatically. You do not set them individually on each control. Reference # Member Type Purpose Label string (required) Text rendered on the button / menu item / tooltip. Execute Action? Synchronous action. Mutually exclusive with ExecuteAsync . ExecuteAsync Func&#x3C;Task>? Async action — pair with UseCommand for tracking. CanExecute bool (default true ) Whether the action can run right now. IsExecuting bool Managed by UseCommand while async work is in flight. Icon IconData? SymbolIcon(name) , FontIcon(...) , or BitmapIcon(uri) . Description string? Tooltip / accessibility description. Accelerator KeyboardAcceleratorData? Keyboard binding ( Accelerator(VirtualKey.S, Control) ). AccessKey string? Single-character Alt-prefix shortcut for menu items. IsEnabled bool (computed) CanExecute &#x26;&#x26; !IsExecuting — what every surface reads. Command&#x3C;T> exposes the same members with Execute: Action&#x3C;T>? and ExecuteAsync: Func&#x3C;T, Task>? — the action receives an argument from the call site. Standard Commands # StandardCommand provides factory methods for the 16 most common application actions. Each comes with a label, icon, and keyboard accelerator preset: class StandardCommandsExample : Component { public override Element Render () { var (log, updateLog) = UseReducer( new List&#x3C; string >()); var cut = StandardCommand.Cut(() => updateLog(l => [.. l, \"Cut\" ])); var copy = StandardCommand.Copy(() => updateLog(l => [.. l, \"Copy\" ])); var paste = StandardCommand.Paste(() => updateLog(l => [.. l, \"Paste\" ])); var undo = StandardCommand.Undo( () => updateLog(l => [.. l, \"Undo\" ]), canExecute: log.Count > 0 ); return VStack( 12 , CommandBar( primaryCommands: new [] { AppBarButton(cut), AppBarButton(copy), AppBarButton(paste), AppBarButton(undo) } ), TextBlock( $\"Actions: { string .Join( \", \" , log)} \" ).Padding( 12 ) ).Padding( 24 ); } } Available: Cut , Copy , Paste , Undo , Redo , Delete , SelectAll , Save , Open , Close , Share , Play , Pause , Stop , Forward , Backward . Each factory takes the action as its first argument and optional canExecute: / label: overrides — the icon and accelerator come from the preset. One Command, Many Surfaces # The point of the model: one declaration drives every surface. Bind the same Command to a Button and a MenuFlyout item — clicking either runs the action, disabling the command disables both, and the keyboard accelerator routes through the same target: class ButtonAndMenuExample : Component { public override Element Render () { var (saves, setSaves) = UseState( 0 ); // One Command. Two surfaces. Identical enabled-state, label, icon, accelerator. var save = new Command { Label = \"Save\" , Icon = SymbolIcon( \"Save\" ), Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control), Execute = () => setSaves(saves + 1 ), CanExecute = saves &#x3C; 3 , }; return VStack( 12 , // Button surface. Button(save), // MenuFlyout surface — same Command record. MenuFlyout( Button( \"File…\" ), MenuItem(save)), TextBlock( $\"Saved {saves} time(s); CanExecute= {save.CanExecute} \" ) .Foreground(Theme.SecondaryText) ).Padding( 24 ); } } The synchronized-state contract is the value. Without it, each surface would compute its own IsEnabled , each would carry its own label duplicate, and the keyboard binding would have to be wired by hand. With it, the Command record is the source of truth; the surfaces are projections. Async Commands and UseCommand # When a command has an ExecuteAsync action, wrap it with the UseCommand hook to get automatic IsExecuting tracking and a re-entrance guard. The button disables itself while the async operation runs; a second click is dropped: class AsyncCommandExample : Component { public override Element Render () { var (status, setStatus) = UseState( \"Ready\" ); var saveCmd = UseCommand( new Command { Label = \"Save to Cloud\" , ExecuteAsync = async () => { setStatus( \"Saving...\" ); await Task.Delay( 2000 ); setStatus( \"Saved at \" + DateTime.Now.ToString( \"HH:mm:ss\" )); }, Icon = SymbolIcon( \"Save\" ) }); return VStack( 12 , HStack( 8 , Button(saveCmd), TextBlock(status).Foreground(Theme.SecondaryText) ), When(saveCmd.IsExecuting, () => ProgressRing().Width( 20 ).Height( 20 )) ).Padding( 24 ); } } UseCommand sets IsExecuting = true before invoking the wrapped action and clears it in a finally block — so a throwing ExecuteAsync still unwinds the busy state. Read command.IsExecuting from any surface that wants to render progress. class AsyncWithProgressExample : Component { public override Element Render () { var (progress, setProgress) = UseState( 0.0 ); var upload = UseCommand( new Command { Label = \"Upload\" , Icon = SymbolIcon( \"Upload\" ), ExecuteAsync = async () => { for ( var i = 0 ; i &#x3C;= 100 ; i += 10 ) { setProgress(i / 100.0 ); await Task.Delay( 120 ); } }, }); return VStack( 12 , HStack( 8 , Button(upload), When(upload.IsExecuting, () => TextBlock( $\" {( int )(progress * 100 )} %\" ) .Foreground(Theme.SecondaryText)) ), When(upload.IsExecuting, () => Progress(progress * 100 ).Width( 300 )) ).Padding( 24 ); } } Caveat: UseCommand sets IsExecuting = true synchronously before the awaited body runs, and clears it in a finally block after the awaited body completes — but a single Command record bound to two surfaces shares one IsExecuting flag. That is intentional for the common case (one Save button + one Save menu item should both disable while saving, preventing double-submit) and surprising for the cross-page case: if the same Command instance is bound from two NavigationView pages, the disable state crosses pages too — saving from page A leaves page B's Save button disabled until the await completes. Hoist the Command to the navigation root and the behavior is correct; recreate it per page (via UseMemo keyed on page identity) when you want per-surface isolation. Parameterized commands # Command&#x3C;T> lets a single command apply to every row of a list, every selected item in a grid, or any other \"do X with this thing\" pattern. The action receives the argument from the call site: record TodoItem ( int Id , string Title ); class ParameterizedCommandExample : Component { public override Element Render () { var (items, setItems) = UseState&#x3C;IReadOnlyList&#x3C;TodoItem>>( new [] { new TodoItem( 1 , \"Buy milk\" ), new TodoItem( 2 , \"Walk dog\" ), new TodoItem( 3 , \"Ship doc\" ) }); // One Command&#x3C;TodoItem> drives every row. var delete = new Command&#x3C;TodoItem> { Label = \"Delete\" , Icon = SymbolIcon( \"Delete\" ), Execute = item => setItems(items.Where(i => i.Id != item.Id).ToList()), }; return VStack( 8 , ForEach(items, item => HStack( 8 , TextBlock(item.Title).Width( 180 ), // Inline button — Command&#x3C;T> doesn't have a Button(cmd, arg) overload // by design, so call .Execute(arg) directly from the click handler. Button(delete.Label, () => delete.Execute?.Invoke(item)) .IsEnabled(delete.IsEnabled))) ).Padding( 24 ); } } driving Delete on every row\"> For menu integration, the variadic MenuItem(Command&#x3C;T>, T parameter) overload binds the row's data to the menu item: MenuFlyout(rowContent, MenuItem(deleteCommand, item), MenuItem(renameCommand, item)) This is the same shape as the right-click on a list row pattern — one parameterized command, one context-menu declaration, every row carries its own data. Command Bar Integration # CommandBar with AppBarButton is the canonical toolbar surface. Primary commands render inline; secondary commands collapse into the overflow menu: class CommandBarExample : Component { public override Element Render () { var (text, setText) = UseState( \"Edit me\" ); var save = StandardCommand.Save(() => { }); var copy = StandardCommand.Copy(() => { }); var delete = StandardCommand.Delete( () => setText( \"\" ), canExecute: text.Length > 0 ); return VStack( 0 , CommandBar( primaryCommands: new [] { AppBarButton(save), AppBarButton(copy) }, secondaryCommands: new [] { AppBarButton(delete) } ), TextBox(text, setText).Margin( 16 ) ); } } The AppBarButton variant of every button supports Command directly; there is no second wiring step. Use CommandBarFlyout for the same shape rendered next to a selection — see dialogs and flyouts . Menu Integration # Commands work in menu bars too. The accelerator text (like Ctrl+S) appears automatically next to the menu item: class MenuBarExample : Component { public override Element Render () { var (text, setText) = UseState( \"Document text\" ); var save = StandardCommand.Save(() => { }); var close = StandardCommand.Close(() => setText( \"\" )); var undo = StandardCommand.Undo(() => { }); var redo = StandardCommand.Redo(() => { }); return VStack( 0 , MenuBar( Menu( \"File\" , MenuItem(save), MenuItem(close)), Menu( \"Edit\" , MenuItem(undo), MenuItem(redo)) ), TextBlock(text).Padding( 16 ) ); } } MenuItem(Command) and MenuItem&#x3C;T>(Command&#x3C;T>, T parameter) are the two factory overloads. The latter is what powers per-row context menus in ListView and DataGrid . Keyboard accelerators # The Accelerator property on Command is the source of truth for the keyboard binding. The accelerator is wired at the WinUI level — the binding lives on whichever surface is rendering the command (the button or the menu item), and Reactor inherits WinUI's \"keyboard accelerators are scoped to the focused element's ancestor chain\" rule. For a global accelerator that fires regardless of focus, render the command from a MenuBar or CommandBar at the window root — both attach their accelerators to the window's KeyboardAccelerators collection, which is window-scoped. var save = new Command { Label = \"Save\" , Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control), Execute = () => SaveDocument(), }; // Window-scoped via MenuBar at the root. return VStack( 0 , MenuBar(Menu( \"File\" , MenuItem(save))), content); For a command palette-style global shortcut catalog, see recipes/command-palette . Patterns # Async confirmation dialog # The classic delete confirmation — primary button runs an ExecuteAsync , the dialog stays open until the await completes, the primary disables mid-flight. Wrap with UseCommand , bind IsPrimaryButtonEnabled to command.IsEnabled , and close the dialog from inside the async body: var delete = ctx.UseCommand( new Command { Label = \"Delete\" , ExecuteAsync = async () => { await api.DeleteAsync(id); setOpen( false ); }, }); ContentDialog( \"Delete?\" , body, primaryButtonText: \"Delete\" ) with { IsOpen = open, IsPrimaryButtonEnabled = delete.IsEnabled, OnClosed = r => { if (r == ContentDialogResult.Primary) delete.Execute?.Invoke(); else setOpen( false ); }, } See dialogs and flyouts for the full pattern. Localized commands # StandardCommand presets ship with English labels. Override per render using the with { ... } expression on the returned record: var save = StandardCommand.Save(action) with { Label = intl.Get( \"save.button\" ), Description = intl.Get( \"save.tooltip\" ), }; See localization for the UseIntl accessor. One command, three surfaces, one accelerator # The shape that motivates the whole model — Ctrl+S triggers the same action whether the user pressed it from the editor body, opened the File menu and clicked Save, or clicked the toolbar Save button: var save = new Command { Label = \"Save\" , Icon = SymbolIcon( \"Save\" ), Accelerator = Accelerator(VirtualKey.S, VirtualKeyModifiers.Control), ExecuteAsync = SaveDocumentAsync, }; var saveWrapped = UseCommand(save); return VStack( 0 , MenuBar(Menu( \"File\" , MenuItem(saveWrapped))), CommandBar(primaryCommands: new [] { AppBarButton(saveWrapped) }), editorBody); Common Mistakes # Creating Commands inside render without memoization # // Don't: re-create the Command on every render — every surface that // holds the previous reference sees a fresh identity each frame, which // thrashes the WinUI keyboard-accelerator wiring and re-renders every // consumer. Lift to a memo or hoist out of Render(). class DontCreateInRender : Component { public override Element Render () { // BAD — Command identity churns every render: // var save = new Command { Label = \"Save\", Execute = () => { } }; // GOOD — UseMemo pins identity until deps change: var (count, setCount) = UseState( 0 ); var save = UseMemo(() => new Command { Label = \"Save\" , Execute = () => setCount(count + 1 ), }, count); return VStack( 8 , Button(save), TextBlock( $\"Saved {count} \" )).Padding( 24 ); } } Re-creating the Command record every frame creates a fresh KeyboardAccelerator and forces every consumer to re-render. The window's accelerator table grows without bound on rapid re-renders and the analyzer fires REACTOR_PERF_FUNCREF for the inline lambda identity. Wrap the construction in UseMemo with the correct dependencies, or hoist the command above the component. Ignoring CanExecute changes # // Don't: var save = new Command { Label = \"Save\" , Execute = () => { if (form.IsValid) Save(); }, }; Pushing the guard inside Execute works but loses the synchronized disable across surfaces — the toolbar button stays enabled-looking , the menu item is still focusable, and the user clicks expecting an action. Promote the predicate to CanExecute : var save = new Command { Label = \"Save\" , Execute = Save, CanExecute = form.IsValid, }; Now the button greys out, the menu item disables, and the accelerator no-ops — one decision, every surface. Awaiting in a non-async event handler # // Don't: Button( \"Save\" , async () => { await api.SaveAsync(); setSaved( true ); }) The inline async lambda runs unmanaged — there is no IsExecuting , no re-entrance guard, no busy state to render against. The user clicks five times during the await and starts five concurrent saves. Promote the action to ExecuteAsync on a Command , wrap with UseCommand , and bind the button to the command. The re-entrance guard drops duplicate clicks and IsExecuting lights up the spinner. Tips # Use StandardCommand for common operations. It saves you from manually specifying icons and keyboard accelerators for the 16 most common actions and keeps your surface consistent with WinUI conventions. Always wrap async commands with UseCommand . It prevents double-execution via the re-entrance guard, tracks IsExecuting , and clears the busy flag in a finally so exceptions don't leave the UI stuck disabled. Read command.IsExecuting for loading indicators. Any surface bound to a wrapped async command can render a spinner from the same flag — the UseCommand wrapper guarantees the flag is live across re-renders. Commands are records — use with to customize. Override the label for localization : StandardCommand.Save(action) with { Label = \"Guardar\" } . The original preset stays intact for the next caller. Define commands at the call site for one-offs; hoist them when shared. A single Save button can declare its command inline. A Save action used in the toolbar, the menu, the keyboard binding, and the dialog primary belongs at the parent component or in a UseMemo so identity is stable. Next Steps # Effects and Lifecycle — Previous: run side effects for timers, data loading, and cleanup Context — Next: share data across the component tree without prop drilling Dialogs and Flyouts — Wire dialog primaries and right-click menus to commands Hooks — UseCommand and the rest of the hook surface Navigation — Scope commands to a page vs. a window Recipes: Command palette — End-to-end recipe for a Ctrl+K palette Localization — Localize command labels and accelerator text",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/commanding.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/commanding.md",
      "headings": [
        {
          "depth": 2,
          "id": "defining-a-command",
          "text": "Defining a Command"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "standard-commands",
          "text": "Standard Commands"
        },
        {
          "depth": 2,
          "id": "one-command-many-surfaces",
          "text": "One Command, Many Surfaces"
        },
        {
          "depth": 2,
          "id": "async-commands-and-usecommand",
          "text": "Async Commands and UseCommand"
        },
        {
          "depth": 2,
          "id": "parameterized-commands",
          "text": "Parameterized commands"
        },
        {
          "depth": 2,
          "id": "command-bar-integration",
          "text": "Command Bar Integration"
        },
        {
          "depth": 2,
          "id": "menu-integration",
          "text": "Menu Integration"
        },
        {
          "depth": 2,
          "id": "keyboard-accelerators",
          "text": "Keyboard accelerators"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "async-confirmation-dialog",
          "text": "Async confirmation dialog"
        },
        {
          "depth": 3,
          "id": "localized-commands",
          "text": "Localized commands"
        },
        {
          "depth": 3,
          "id": "one-command-three-surfaces-one-accelerator",
          "text": "One command, three surfaces, one accelerator"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "creating-commands-inside-render-without-memoization",
          "text": "Creating Commands inside render without memoization"
        },
        {
          "depth": 3,
          "id": "ignoring-canexecute-changes",
          "text": "Ignoring CanExecute changes"
        },
        {
          "depth": 3,
          "id": "awaiting-in-a-non-async-event-handler",
          "text": "Awaiting in a non-async event handler"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Advanced Patterns",
      "path": "advanced.md",
      "slug": "/advanced/",
      "html": "<p>Advanced is the layer where Microsoft.UI.Reactor (Reactor)'s declarative ceiling meets the\nimperative WinUI runtime underneath. Most of the framework is shaped to\nmake the declarative path the right path — render is a function of\nstate, the reconciler turns the next element record into a control\ntree, you never new up a <code>Button</code> yourself. But every real app has a\nhandful of jobs that don't fit: focusing the password field after a\nlogin error, integrating an existing MVVM view model that already owns\nits INPC story, recovering from a crash inside a third-party\ncomponent's render, hand-tuning a 4,900-cell ticker that allocates\nduring scroll. The tools on this page — <code>ErrorBoundary</code>, <code>Memo</code>,\n<code>UseElementRef</code>, <code>.Set(...)</code>, <code>UseObservableTree</code>, custom hooks, direct\nrecord-initializer cell construction — are the escape hatches.\nReach for them when the declarative shape is genuinely wrong for the\njob; reach back for the declarative shape the moment the escape\nhatch's scope is fulfilled.</p>\n<h1 id=\"advanced-patterns\">Advanced Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#advanced-patterns\">#</a></h1>\n<h2 id=\"escape-hatch-reference\">Escape-hatch reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#escape-hatch-reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Tool</th><th>When it's the right hammer</th><th>When it isn't</th></tr></thead><tbody><tr><td><code>ErrorBoundary</code></td><td>Wrap a third-party subtree (plugin, marketplace component) that may throw during render.</td><td>App-wide error handlers — those belong to host code, not the render tree.</td></tr><tr><td><code>Memo(ctx => ..., deps)</code></td><td>Skip re-render of an expensive subtree when none of <code>deps</code> changed by reference.</td><td>Cheap subtrees — Memo's bookkeeping costs more than the render itself.</td></tr><tr><td><code>UseElementRef&#x3C;T>()</code></td><td>Imperative WinUI calls scoped to one element: focus, scroll, animation handles.</td><td>Reading state from the control — your component state is the source of truth.</td></tr><tr><td><code>.Set(control => ...)</code></td><td>A WinUI property Reactor doesn't expose.</td><td>Properties Reactor does expose — <code>.Set</code> bypasses reconciliation's property diff.</td></tr><tr><td><code>UseObservableTree</code></td><td>Bridging an existing INPC view model with nested objects.</td><td>New code — prefer <code>UseState</code> and <code>UseReducer</code> from <a href=\"/hooks/\">hooks</a>.</td></tr><tr><td><code>UseCollection</code></td><td>Wrapping an <code>ObservableCollection&#x3C;T></code> you don't own.</td><td>Owned lists — use <code>UseState&#x3C;ImmutableList&#x3C;T>></code> or <code>ImmutableArray&#x3C;T></code>.</td></tr><tr><td>Custom hook (<code>this RenderContext</code>)</td><td>Reusing a hook combination across components.</td><td>One-off compositions inside a single Render.</td></tr><tr><td>Direct record-initializer cells</td><td>Profiled hot loops (tickers, stress grids).</td><td>Ordinary screens. Keep them fluent.</td></tr></tbody></table>\n<h2 id=\"error-boundary\">Error Boundary<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#error-boundary\">#</a></h2>\n<p><code>ErrorBoundary</code> wraps a subtree and catches exceptions during render.\nInstead of crashing the whole app, the boundary displays a fallback\nelement:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ErrorBoundaryDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Error Boundary\"</span>),\n            ErrorBoundary(\n                Component&#x3C;BuggyComponent>(),\n                (Exception ex) => VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Something went wrong\"</span>).Bold()\n                        .Foreground(<span class=\"hljs-string\">\"#d13438\"</span>),\n                    TextBlock(ex.Message).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.7</span>)\n                ).Padding(<span class=\"hljs-number\">12</span>)\n                 .Background(<span class=\"hljs-string\">\"#fde7e9\"</span>)\n                 .CornerRadius(<span class=\"hljs-number\">8</span>)\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BuggyComponent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (crash, setCrash) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">if</span> (crash) <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> InvalidOperationException(<span class=\"hljs-string\">\"Oops!\"</span>);\n        <span class=\"hljs-keyword\">return</span> Button(<span class=\"hljs-string\">\"Click to crash\"</span>, () => setCrash(<span class=\"hljs-literal\">true</span>));\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/error-boundary.png\" alt=\"Error boundary with fallback\"></p>\n<p>The first argument is the child subtree. The second is a fallback —\neither a static element or a function that receives the caught\n<code>Exception</code>. When the boundary re-renders (e.g., its parent updates), it\nretries the child. That gives the user a chance to recover by changing\nthe state that caused the crash.</p>\n<h3 id=\"retrying-a-failed-subtree\">Retrying a failed subtree<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#retrying-a-failed-subtree\">#</a></h3>\n<p>The retry pattern uses <a href=\"/components/\"><code>WithKey</code></a> to assign a fresh\nidentity to the child each retry — same shape as React's\n<code>resetKeys</code>. Bumping the key drops the previous subtree's mounted\nstate, so a transient failure that left bad state behind doesn't\npersist:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ErrorBoundaryRetryDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (resetKey, setResetKey) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"ErrorBoundary with retry\"</span>),\n            ErrorBoundary(\n                Component&#x3C;FlakyComponent>().WithKey(<span class=\"hljs-string\">$\"flaky-<span class=\"hljs-subst\">{resetKey}</span>\"</span>),\n                ex => VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Couldn't load.\"</span>).Bold().Foreground(<span class=\"hljs-string\">\"#d13438\"</span>),\n                    TextBlock(ex.Message).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.7</span>),\n                    <span class=\"hljs-comment\">// Bumping resetKey reassigns identity to the child, so the</span>\n                    <span class=\"hljs-comment\">// ErrorBoundary mounts a fresh subtree on the next render.</span>\n                    Button(<span class=\"hljs-string\">\"Retry\"</span>, () => setResetKey(resetKey + <span class=\"hljs-number\">1</span>))\n                ).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#fde7e9\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>)\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FlakyComponent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (attempt, _) = UseState(Random.Shared.Next(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">3</span>));\n        <span class=\"hljs-keyword\">if</span> (attempt == <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> InvalidOperationException(<span class=\"hljs-string\">\"Service unavailable\"</span>);\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"Loaded.\"</span>).Foreground(<span class=\"hljs-string\">\"#107c10\"</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/error-boundary-retry.png\" alt=\"Retryable error boundary\"></p>\n<p>The Retry button bumps <code>resetKey</code>, the child gets a new key, and the\nreconciler mounts a fresh subtree on the next render. This is the same\nidentity-reset trick used by <a href=\"/navigation/\">navigation</a> cache\ninvalidation and by list-item-identity recovery in\n<a href=\"/collections/\">collections</a>.</p>\n<h2 id=\"memo\">Memo<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#memo\">#</a></h2>\n<p><code>Memo(ctx => ..., deps)</code> skips re-rendering an entire subtree when its\ndependencies have not changed by reference. The parent can re-render\nfreely — the memoized subtree stays frozen:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MemoSubtreeDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (label, setLabel) = UseState(<span class=\"hljs-string\">\"Expensive\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Memo\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Parent renders: click count = <span class=\"hljs-subst\">{count}</span>\"</span>),\n            Button(<span class=\"hljs-string\">\"Increment\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>)),\n            Memo(ctx =>\n            {\n                <span class=\"hljs-comment\">// This subtree only re-renders when label changes</span>\n                <span class=\"hljs-keyword\">return</span> Border(\n                    VStack(<span class=\"hljs-number\">4</span>,\n                        TextBlock(<span class=\"hljs-string\">$\"Memoized: <span class=\"hljs-subst\">{label}</span>\"</span>).Bold(),\n                        TextBlock(<span class=\"hljs-string\">\"Skips re-render when deps unchanged\"</span>)\n                            .FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">12</span>)\n                ).Background(<span class=\"hljs-string\">\"#f0f0f0\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>);\n            }, label)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/memo-subtree.png\" alt=\"Memo skipping re-render\"></p>\n<p><code>Memo</code> takes a render function and a list of dependencies. The subtree\nonly re-renders when at least one dependency changes by reference\nequality. Clicking \"Increment\" re-renders the parent, but the memoized\nborder stays untouched because <code>label</code> hasn't changed.</p>\n<p>Use Memo for expensive subtrees — large <a href=\"/collections/\">collections</a>,\ncomplex <a href=\"/charting/\">charts</a>, or deeply nested\n<a href=\"/components/\">component</a> trees that don't depend on frequently-changing\nstate. For per-cell memoization inside long lists, see\n<a href=\"#memoizing-list-cells\">Memoizing list cells</a>.</p>\n<h2 id=\"imperative-refs--useelementref\">Imperative refs — UseElementRef<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#imperative-refs--useelementref\">#</a></h2>\n<p><code>UseElementRef&#x3C;T>()</code> returns a stable <code>ElementRef&#x3C;T></code> whose <code>.Current</code>\nthe reconciler keeps in sync with the mounted control. Attach the ref\nto an element with <code>.Ref(ref)</code>, then call into <code>.Current</code> from an event\nhandler or <code>UseEffect</code> for the imperative operations Reactor doesn't\nwrap declaratively:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ElementRefFocusDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> fieldRef = <span class=\"hljs-keyword\">this</span>.UseElementRef&#x3C;TextBox>();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Imperative focus via ElementRef&#x3C;T>\"</span>),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Name\"</span>).Ref(fieldRef),\n            Button(<span class=\"hljs-string\">\"Focus the field\"</span>, () =>\n                fieldRef.Current?.Focus(FocusState.Programmatic))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The ref instance is identity-stable across re-renders (the hook returns\nthe same instance every time), so it's safe to use in dep arrays and\n<code>UseEffect</code> capture. <code>.Current</code> is null until the element mounts; null-\ncheck before dereferencing.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Holding a raw <code>FrameworkElement</code> (the WinUI control underneath, not the\nReactor <code>ElementRef&#x3C;T></code>) across renders is unsafe. The reconciler may\nunmount, pool, or replace the underlying control between renders — the\ncontrol you cached in a <code>static</code> field or in a captured local is\nno longer attached to the tree, and any property you set on it has no\neffect. <code>UseElementRef&#x3C;T></code> is the supported shape: the ref instance is\nstable, but <code>.Current</code> is <strong>re-read each time</strong> through a property the\nreconciler updates on mount and unmount. The analyzer\n<code>REACTOR_HOOKS_005</code> catches the common forms (caching a control in a\nfield, dereferencing without null-checking), but indirect caches\nthrough helper methods aren't visible to it — keep the rule in your\nhead: never store the control, always go through the ref.</p>\n</blockquote>\n<h2 id=\"set-escape-hatch\">.Set() escape hatch<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#set-escape-hatch\">#</a></h2>\n<p><code>.Set()</code> gives you direct access to the underlying WinUI control. Use\nit for the properties Reactor doesn't expose as modifiers:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SetEscapeHatchDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\".Set() Escape Hatch\"</span>),\n            Button(<span class=\"hljs-string\">\"Custom Tooltip\"</span>, () => { })\n                .Set(btn =>\n                {\n                    Microsoft.UI.Xaml.Controls.ToolTipService\n                        .SetToolTip(btn, <span class=\"hljs-string\">\"This is a native tooltip\"</span>);\n                    btn.Padding = <span class=\"hljs-keyword\">new</span> Thickness(<span class=\"hljs-number\">20</span>, <span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">20</span>, <span class=\"hljs-number\">10</span>);\n                }),\n            TextBlock(<span class=\"hljs-string\">\"Styled via .Set()\"</span>)\n                .Set(tb =>\n                {\n                    tb.TextWrapping = TextWrapping.WrapWholeWords;\n                    tb.CharacterSpacing = <span class=\"hljs-number\">80</span>;\n                    tb.IsTextSelectionEnabled = <span class=\"hljs-literal\">true</span>;\n                })\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/set-escape-hatch.png\" alt=\".Set() escape hatch\"></p>\n<p>Each element type has a typed <code>.Set()</code> overload. <code>Button</code> receives an\n<code>Action&#x3C;Microsoft.UI.Xaml.Controls.Button></code>, <code>Text</code> receives\n<code>Action&#x3C;TextBlock></code>, and so on. The callback runs at both mount and\nupdate, so set properties idempotently — don't accumulate event\nhandlers. Use <code>.OnMount(...)</code> instead when you need a one-shot\nattachment.</p>\n<p><code>.Set</code> bypasses the property-diff path in reconciliation — the\nreconciler doesn't know about state you write through the callback, so\nthe next render that touches a real modifier will not unwind it. Treat\n<code>.Set</code> as one-way: you're driving the control, not letting Reactor\nmanage it.</p>\n<h2 id=\"custom-hooks\">Custom hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-hooks\">#</a></h2>\n<p>Custom hooks compose existing hooks into reusable shapes. They're\nextension methods on <code>RenderContext</code> — the same shape as the built-in\nhooks, called from any function-component <code>Memo(ctx => ...)</code> or from\na class component's <code>Context</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Custom hook — composes UseState + UseEffect on RenderContext. Treated like</span>\n<span class=\"hljs-comment\">// a built-in hook from any function-component Render. Same hook-rules apply:</span>\n<span class=\"hljs-comment\">// call unconditionally, at the top of render, in the same order every time.</span>\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TogglerHook</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> (<span class=\"hljs-params\"><span class=\"hljs-built_in\">bool</span> IsOn, Action Toggle</span>) <span class=\"hljs-title\">UseToggler</span>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> RenderContext ctx, <span class=\"hljs-built_in\">bool</span> initial = <span class=\"hljs-literal\">false</span></span>)</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">on</span>, setOn) = ctx.UseState(initial);\n        <span class=\"hljs-keyword\">return</span> (<span class=\"hljs-keyword\">on</span>, () => setOn(!<span class=\"hljs-keyword\">on</span>));\n    }\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CustomHookDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => Memo(ctx =>\n    {\n        <span class=\"hljs-keyword\">var</span> (isOn, toggle) = ctx.UseToggler();\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Custom hook: UseToggler\"</span>),\n            Button(isOn ? <span class=\"hljs-string\">\"On\"</span> : <span class=\"hljs-string\">\"Off\"</span>, toggle),\n            TextBlock(isOn ? <span class=\"hljs-string\">\"State is on.\"</span> : <span class=\"hljs-string\">\"State is off.\"</span>)\n                .Foreground(isOn ? <span class=\"hljs-string\">\"#107c10\"</span> : <span class=\"hljs-string\">\"#666666\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    });\n}\n</code></pre>\n<p>The same hook rules apply (see <a href=\"/rules-of-reactor/\">rules-of-reactor</a>):\ncall unconditionally, at the top of render, in the same order every\ntime, never inside loops or conditionals. The analyzer\n<code>REACTOR_HOOKS_001</code> enforces these rules across custom hooks as well as\nbuilt-ins — it walks the call graph through extension methods.</p>\n<h2 id=\"observable-bridges--useobservabletree\">Observable bridges — UseObservableTree<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#observable-bridges--useobservabletree\">#</a></h2>\n<p><code>UseObservableTree</code> bridges <code>INotifyPropertyChanged</code> view models into\nthe declarative render cycle. It subscribes to property changes at\nevery depth and re-renders the component when any nested property\nfires.</p>\n<p>Define a standard INPC view model:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SettingsViewModel</span> : <span class=\"hljs-title\">INotifyPropertyChanged</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">string</span> _userName = <span class=\"hljs-string\">\"Alice\"</span>;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">bool</span> _darkMode;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">int</span> _fontSize = <span class=\"hljs-number\">14</span>;\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span> UserName\n    {\n        <span class=\"hljs-keyword\">get</span> => _userName;\n        <span class=\"hljs-keyword\">set</span> { _userName = <span class=\"hljs-keyword\">value</span>; Notify(<span class=\"hljs-keyword\">nameof</span>(UserName)); }\n    }\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">bool</span> DarkMode\n    {\n        <span class=\"hljs-keyword\">get</span> => _darkMode;\n        <span class=\"hljs-keyword\">set</span> { _darkMode = <span class=\"hljs-keyword\">value</span>; Notify(<span class=\"hljs-keyword\">nameof</span>(DarkMode)); }\n    }\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">int</span> FontSize\n    {\n        <span class=\"hljs-keyword\">get</span> => _fontSize;\n        <span class=\"hljs-keyword\">set</span> { _fontSize = <span class=\"hljs-keyword\">value</span>; Notify(<span class=\"hljs-keyword\">nameof</span>(FontSize)); }\n    }\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> PropertyChangedEventHandler? PropertyChanged;\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Notify</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> n</span>)</span> =>\n        PropertyChanged?.Invoke(<span class=\"hljs-keyword\">this</span>, <span class=\"hljs-keyword\">new</span>(n));\n}\n</code></pre>\n<p>Then bind it in a component with <code>UseObservableTree</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ObservableTreeDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> SettingsViewModel _vm = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> vm = UseObservableTree(_vm);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseObservableTree\"</span>),\n            TextBox(vm.UserName, v => vm.UserName = v,\n                header: <span class=\"hljs-string\">\"User Name\"</span>),\n            ToggleSwitch(vm.DarkMode, v => vm.DarkMode = v,\n                header: <span class=\"hljs-string\">\"Dark Mode\"</span>),\n            Slider(vm.FontSize, <span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">32</span>, v => vm.FontSize = (<span class=\"hljs-built_in\">int</span>)v),\n            TextBlock(<span class=\"hljs-string\">$\"Preview: <span class=\"hljs-subst\">{vm.UserName}</span>\"</span>)\n                .FontSize(vm.FontSize).Bold()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/observable-viewmodel.png\" alt=\"Observable view model\"></p>\n<p>The view model is a plain C# class with <code>INotifyPropertyChanged</code>. You\nmutate it directly (<code>vm.UserName = v</code>) and Reactor re-renders. This is\nthe migration bridge for teams arriving with existing MVVM view models\n— they work without modification.</p>\n<p><code>UseObservable(source)</code> is the shallow variant: it only subscribes to\nthe source object's own <code>PropertyChanged</code> events, not nested objects.\nPrefer it for flat view models — <code>UseObservableTree</code> walks the entire\ngraph on every render and the cost shows up on deep object trees.</p>\n<h2 id=\"usecollection\">UseCollection<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usecollection\">#</a></h2>\n<p><code>UseCollection</code> tracks an <code>ObservableCollection&#x3C;T></code> and re-renders the\ncomponent when items are added, removed, or the collection resets:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ObservableCollectionDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> ObservableCollection&#x3C;<span class=\"hljs-built_in\">string</span>> _tasks = <span class=\"hljs-keyword\">new</span>()\n        { <span class=\"hljs-string\">\"Review pull request\"</span>, <span class=\"hljs-string\">\"Update documentation\"</span> };\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> tasks = UseCollection(_tasks);\n        <span class=\"hljs-keyword\">var</span> (input, setInput) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"UseCollection\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(input, setInput, placeholderText: <span class=\"hljs-string\">\"New task\"</span>)\n                    .Width(<span class=\"hljs-number\">200</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () => {\n                    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(input))\n                    { _tasks.Add(input.Trim()); setInput(<span class=\"hljs-string\">\"\"</span>); }\n                })\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{tasks.Count}</span> tasks:\"</span>).SemiBold(),\n            VStack(<span class=\"hljs-number\">4</span>, tasks.Select((task, i) =>\n                HStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{i + <span class=\"hljs-number\">1</span>}</span>. <span class=\"hljs-subst\">{task}</span>\"</span>),\n                    Button(<span class=\"hljs-string\">\"Remove\"</span>, () => _tasks.RemoveAt(i))\n                ).WithKey(<span class=\"hljs-string\">$\"task-<span class=\"hljs-subst\">{i}</span>-<span class=\"hljs-subst\">{task}</span>\"</span>)\n            ).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/advanced/observable-collection.png\" alt=\"Observable collection\"></p>\n<p>The hook returns <code>IReadOnlyList&#x3C;T></code> — you read from it in render and\nmutate the original <code>ObservableCollection</code> in event handlers. Reactor\nsubscribes to <code>CollectionChanged</code> and triggers a re-render on any\nmodification. For owned lists (collections you created in the same\ncomponent), prefer <code>UseState&#x3C;ImmutableList&#x3C;T>></code> — the immutable shape\nfits the render-from-state model without an observable middleman.</p>\n<h2 id=\"hot-loops\">Hot loops<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hot-loops\">#</a></h2>\n<p>The fluent modifier chain is ergonomic but allocates an\n<code>ElementModifiers</code> clone per <code>with</code>-step. For ordinary UI that cost is\ninvisible — a button with five modifiers means two extra small records\nallocated on a click handler. For inner-loop cell construction in a\n4,900-cell grid that re-renders 30× per second, those clones dominate\nthe allocation profile.</p>\n<p>The escape hatch is to construct the <code>Element</code> and its\n<code>ElementModifiers</code> record directly, skipping the fluent chain entirely.\n<code>LayoutModifiers</code> and <code>VisualModifiers</code> are public types specifically\nso perf-critical code can build them once instead of having the fluent\nchain rebuild them step-by-step.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Fluent — five clones per cell. Right tool for ordinary UI.</span>\n<span class=\"hljs-keyword\">var</span> cell = TextBlock(label)\n    .FontSize(<span class=\"hljs-number\">8</span>)\n    .Foreground(item.IsUp ? GreenBrush : RedBrush)\n    .Padding(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">1</span>)\n    .Grid(row: r, column: c);\n\n<span class=\"hljs-comment\">// Direct record initializer — one TextBlockElement, one ElementModifiers,</span>\n<span class=\"hljs-comment\">// two bucket sub-records, one Attached dictionary. Use only when the</span>\n<span class=\"hljs-comment\">// allocation cost shows up in profiles.</span>\n<span class=\"hljs-keyword\">var</span> cell = <span class=\"hljs-keyword\">new</span> TextBlockElement(label)\n{\n    FontSize = <span class=\"hljs-number\">8</span>,\n    Modifiers = <span class=\"hljs-keyword\">new</span> ElementModifiers\n    {\n        Layout = <span class=\"hljs-keyword\">new</span> LayoutModifiers { Padding = <span class=\"hljs-keyword\">new</span> Thickness(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">1</span>) },\n        Visual = <span class=\"hljs-keyword\">new</span> VisualModifiers { Foreground = item.IsUp ? GreenBrush : RedBrush },\n    },\n    Attached = <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;Type, <span class=\"hljs-built_in\">object</span>>(<span class=\"hljs-number\">1</span>)\n    {\n        [<span class=\"hljs-meta\">typeof(GridAttached)</span>] = <span class=\"hljs-keyword\">new</span> GridAttached(r, c, <span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>),\n    },\n};\n</code></pre>\n<p><strong>Workload shape.</strong> Use this idiom in lists or grids with hundreds-plus\nelements per render — tickers, log tables, observability dashboards.\nDon't adopt it for ordinary screens. The fluent chain is the right tool\nfor everything except the inner cell loop.</p>\n<p><strong>Trade-offs.</strong> Roughly halves the allocation cost of cell construction\non the 4,900-cell stress grid, but loses fluent ergonomics. The direct\nform is more brittle to refactor — changing one field touches an\nexplicit initializer block instead of a chain step. Restrict it to the\nidentifiable hot loop and keep the rest of the file fluent. See\n<a href=\"/modifier-system/\">modifier-system</a> for the under-the-hood explanation\nof why the fluent chain allocates.</p>\n<p><strong>Reference implementation.</strong> The canonical before/after pair lives in\n<code>tests/stress_perf/StressPerf.Reactor</code> (naive — fluent chain, the shape\nunaware users write) and <code>tests/stress_perf/StressPerf.ReactorOptimized</code>\n(idiomatic perf-tuned variant). Same workload, side-by-side diffable.</p>\n<h2 id=\"memoizing-list-cells\">Memoizing list cells<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#memoizing-list-cells\">#</a></h2>\n<p><code>UseMemoCells</code> skips the cell-build for indices whose item value (and\ndeclared dependencies) haven't changed since the previous render. The\nreconciler's <code>ReferenceEquals</code> shortcut means a reused cell allocates\nnothing and skips diffing entirely.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> theme = ctx.UseTheme();\n<span class=\"hljs-keyword\">var</span> children = ctx.UseMemoCells(\n    stocks,\n    (item, i) => Cell(item, theme),\n    theme);   <span class=\"hljs-comment\">// ← deps; framework invalidates on change</span>\n</code></pre>\n<p><strong>When it's the right hammer.</strong> Tickers, log tables, file lists, large\nread-only grids — anywhere the cell content is a pure function of <code>T</code>\nplus a small set of declared deps.</p>\n<p><strong>When it's the wrong hammer.</strong> Rows whose chrome depends on focus,\ndrag, selection, or hover state that you aren't capturing in deps.\nMemo silently renders stale when an external state change isn't\ndeclared as a dep — the analyzer below catches the obvious cases, but\nindirect captures through helper methods aren't visible to it.</p>\n<p><strong>Three overloads:</strong></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Overload</th><th>Use when</th></tr></thead><tbody><tr><td><code>UseMemoCells&#x3C;T></code></td><td>Per-item value equality. Default choice.</td></tr><tr><td><code>UseMemoCellsByKey&#x3C;T, TKey></code></td><td>Items have stable identity but mutable interior. Hashes by key, value-compares for content. Reordered keys reuse cells via the reconciler's keyed-children path.</td></tr><tr><td><code>UseMemoCellsByIndex&#x3C;T></code></td><td>Data source already knows which indices changed. Skips the per-cell equality scan; only the named indices run the builder.</td></tr></tbody></table>\n<p><strong>gen2 caveat.</strong> Memo trades short-lived gen0 churn for longer-lived\ngen1/gen2 retention. Many memoized lists across an app can compound\ngen2 pressure even when bytes-per-tick drops. Profile before adopting\nacross the board — see\n<a href=\"/perf-instrumentation/\">perf-instrumentation</a> for ETW-driven sampling\nand <a href=\"/performance/\">performance</a> for the top-down walkthrough.</p>\n<p><strong>Compile-time safety net.</strong> The companion Roslyn analyzer\n<code>REACTOR_HOOKS_007</code> warns when a builder closure captures a value that\nisn't declared in the deps list. Codefix is \"add the missing capture to\ndeps\". Indirect captures through intermediate methods are a documented\nblind spot — the analyzer can't see through a method call without\nwhole-program analysis (same blind spot as React's\n<code>react-hooks/exhaustive-deps</code>).</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"custom-hook-authoring\">Custom hook authoring<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-hook-authoring\">#</a></h3>\n<p>Custom hooks let you bundle a hook combination behind a single name —\n<code>UseDebouncedValue</code>, <code>UseWindowScroll</code>, <code>UsePersistedToggle</code> — and reuse\nit across components. They're plain <code>RenderContext</code> extension methods.\nThe <a href=\"/rules-of-reactor/\">rules-of-reactor</a> page covers naming\nconventions (<code>Use*</code> prefix) and call-order discipline; the\n<a href=\"/hooks-internals/\">hooks-internals</a> page explains why custom hooks\nwork — Reactor doesn't distinguish them from built-ins, the slot table\njust counts hook invocations in order.</p>\n<h3 id=\"errorboundary-with-retry-key\">ErrorBoundary with retry key<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#errorboundary-with-retry-key\">#</a></h3>\n<p>The retry pattern shown above scales to async failures, transient\nnetwork errors, and any subtree whose state should reset on user\nacknowledgment. Pair the retry key with an <a href=\"/async-resources/\">async resource</a>\nthat holds the in-flight task — bumping the key cancels the previous\nattempt by reassigning identity, so a slow fetch from the failed\nattempt doesn't race with the new one.</p>\n<h3 id=\"imperative-focus-from-an-event-handler\">Imperative focus from an event handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#imperative-focus-from-an-event-handler\">#</a></h3>\n<p>A login form that focuses the password field when the username is\nvalid is the canonical <code>UseElementRef</code> shape — see the snippet above\nfor the call site. The same shape covers focusing a list item after a\nkeyboard shortcut, scrolling a <code>VirtualList</code> to a known index via\n<a href=\"/collections/\"><code>VirtualListRef</code></a>, and starting a Composition\nanimation on a known control. For the broader focus model (focus trap,\nenter-to-advance, automation tree), see\n<a href=\"/accessibility/\">accessibility</a> and\n<a href=\"/focus-and-input-internals/\">focus-and-input-internals</a>.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"caching-elements-or-controls-in-module-level-statics\">Caching elements (or controls) in module-level statics<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#caching-elements-or-controls-in-module-level-statics\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — the FrameworkElement underneath is pool-managed.</span>\n<span class=\"hljs-keyword\">static</span> TextBox? _passwordField;\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (pwd, setPwd) = UseState(<span class=\"hljs-string\">\"\"</span>);\n    <span class=\"hljs-keyword\">return</span> TextBox(pwd, setPwd).Set(c => _passwordField = c);\n}\n\n<span class=\"hljs-comment\">// Elsewhere:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">FocusPassword</span>()</span> => _passwordField?.Focus(FocusState.Programmatic);\n</code></pre>\n<p>The reconciler may unmount, swap, or pool the underlying control\nbetween renders. The static field outlives every one of those events\nand ends up pointing at a detached or recycled instance. Use\n<a href=\"#imperative-refs--useelementref\"><code>UseElementRef&#x3C;T></code></a> — the ref's\n<code>.Current</code> is null when the control isn't attached and is repopulated\nwhen it remounts.</p>\n<h3 id=\"calling-setstate-from-a-useeffect-cleanup\">Calling <code>setState</code> from a <code>UseEffect</code> cleanup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#calling-setstate-from-a-useeffect-cleanup\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — setState inside cleanup runs during unmount/dispose.</span>\nUseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> sub = Subscribe();\n    <span class=\"hljs-keyword\">return</span> () => { sub.Dispose(); setState(<span class=\"hljs-literal\">false</span>); };  <span class=\"hljs-comment\">// ← stale state, may throw</span>\n}, []);\n</code></pre>\n<p>Cleanup runs after the component has stopped being part of the tree;\ncalling <code>setState</code> from there either no-ops (the disposed render\ncontext refuses the call) or schedules a render against state that no\nlonger matters. Put state resets in the effect body or in an event\nhandler that explicitly transitions the parent. See\n<a href=\"/effects-scheduling/\">effects-scheduling</a> for cleanup ordering.</p>\n<h3 id=\"subscribing-to-events-from-a-parent-that-captures-the-wrong-this\">Subscribing to events from a parent that captures the wrong <code>this</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#subscribing-to-events-from-a-parent-that-captures-the-wrong-this\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — captures the parent's `this` inside the child component.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Parent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        Component&#x3C;Child>().Set(c => c.Loaded += (s, e) => <span class=\"hljs-keyword\">this</span>.OnChildLoaded());\n}\n</code></pre>\n<p><code>.Set(control => ...)</code> runs on every mount and update — every event\nsubscription accumulates. The <code>this</code> inside the lambda is the parent\ncomponent, captured at the lambda's closure site, which keeps a strong\nreference to a stale render's component instance after the parent has\nre-rendered. Two fixes: lift the handler to a stable <code>UseCallback</code>\nin the parent and pass it via <a href=\"/components/\">props</a>, or use\n<code>.OnMount(...)</code> for a one-shot subscription with explicit cleanup.</p>\n<h3 id=\"reaching-for-set-when-a-modifier-exists\">Reaching for <code>.Set</code> when a modifier exists<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-set-when-a-modifier-exists\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — Reactor exposes .RequestedTheme() as a modifier.</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, onSave).Set(c => c.RequestedTheme = ElementTheme.Dark);\n</code></pre>\n<p>The modifier participates in property diffing — the next render that\nremoves the modifier walks the property back to default. <code>.Set</code> is\nimperative and one-way; the next render won't undo it. Prefer the\nmodifier when one exists; reserve <code>.Set</code> for properties Reactor\ngenuinely doesn't expose. The <code>REACTOR_THEME_003</code> analyzer catches this\nspecific case for <code>RequestedTheme</code>; the same shape applies broadly.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Wrap third-party components in <code>ErrorBoundary</code>.</strong> If a plugin or\nmarketplace component throws during render, the boundary stops it from\ntaking down your entire app. Pair with a Retry button keyed by a\n<code>UseState</code> counter to let users recover without restarting the app.</p>\n<p><strong>Profile before memoizing.</strong> <code>Memo</code> adds bookkeeping cost and a\nsecond hook slot per subtree. Only reach for it when you have measured\na re-render bottleneck on a profiler. Most subtrees re-render fast\nenough without it.</p>\n<p><strong>Prefer declarative modifiers over <code>.Set()</code>.</strong> Every <code>.Set()</code> call is\nan escape hatch that bypasses Reactor's property diff. Reserve it for\nWinUI properties Reactor doesn't expose. If you find yourself reaching\nfor <code>.Set</code> frequently, the right next step is a Reactor feature\nrequest — the framework should grow a modifier for the common case.</p>\n<p><strong>Keep view models outside the component.</strong> Store them as <code>static</code>\nfields or inject them via <a href=\"/context/\">context</a>. Creating a new view\nmodel inside <code>Render()</code> would allocate a fresh object on every render\nand lose all state, plus break the <code>UseObservableTree</code> subscription on\nevery render.</p>\n<p><strong>Use <code>UseObservable</code> when depth is one level.</strong> <code>UseObservableTree</code>\nwalks the entire object graph on every change. For flat view models\nwith no nested INPC objects, <code>UseObservable</code> is cheaper and produces\nfewer event subscriptions.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — DataGrid with sort, filter, and inline editing</li>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — host Reactor components inside WinForms applications</li>\n<li><strong><a href=\"/charting/\">Charting</a></strong> — data visualization with line, bar, area, pie, and force-directed charts</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — review the hook system that <code>UseObservableTree</code>, <code>UseCollection</code>, and custom hooks build on</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — side-effect scheduling alongside observable data binding</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks internals</a></strong> — Under-the-hood: how hooks actually store their values</li>\n<li><strong><a href=\"/modifier-system/\">Modifier system</a></strong> — Under-the-hood: why the fluent chain allocates, and what the direct-initializer escape buys</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — hook rules, render purity, and the dos and don'ts of custom hook authoring</li>\n</ul>",
      "text": "Advanced is the layer where Microsoft.UI.Reactor (Reactor)'s declarative ceiling meets the imperative WinUI runtime underneath. Most of the framework is shaped to make the declarative path the right path — render is a function of state, the reconciler turns the next element record into a control tree, you never new up a Button yourself. But every real app has a handful of jobs that don't fit: focusing the password field after a login error, integrating an existing MVVM view model that already owns its INPC story, recovering from a crash inside a third-party component's render, hand-tuning a 4,900-cell ticker that allocates during scroll. The tools on this page — ErrorBoundary , Memo , UseElementRef , .Set(...) , UseObservableTree , custom hooks, direct record-initializer cell construction — are the escape hatches. Reach for them when the declarative shape is genuinely wrong for the job; reach back for the declarative shape the moment the escape hatch's scope is fulfilled. Advanced Patterns # Escape-hatch reference # Tool When it's the right hammer When it isn't ErrorBoundary Wrap a third-party subtree (plugin, marketplace component) that may throw during render. App-wide error handlers — those belong to host code, not the render tree. Memo(ctx => ..., deps) Skip re-render of an expensive subtree when none of deps changed by reference. Cheap subtrees — Memo's bookkeeping costs more than the render itself. UseElementRef&#x3C;T>() Imperative WinUI calls scoped to one element: focus, scroll, animation handles. Reading state from the control — your component state is the source of truth. .Set(control => ...) A WinUI property Reactor doesn't expose. Properties Reactor does expose — .Set bypasses reconciliation's property diff. UseObservableTree Bridging an existing INPC view model with nested objects. New code — prefer UseState and UseReducer from hooks . UseCollection Wrapping an ObservableCollection&#x3C;T> you don't own. Owned lists — use UseState&#x3C;ImmutableList&#x3C;T>> or ImmutableArray&#x3C;T> . Custom hook ( this RenderContext ) Reusing a hook combination across components. One-off compositions inside a single Render. Direct record-initializer cells Profiled hot loops (tickers, stress grids). Ordinary screens. Keep them fluent. Error Boundary # ErrorBoundary wraps a subtree and catches exceptions during render. Instead of crashing the whole app, the boundary displays a fallback element: class ErrorBoundaryDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"Error Boundary\" ), ErrorBoundary( Component&#x3C;BuggyComponent>(), (Exception ex) => VStack( 8 , TextBlock( \"Something went wrong\" ).Bold() .Foreground( \"#d13438\" ), TextBlock(ex.Message).FontSize( 12 ).Opacity( 0.7 ) ).Padding( 12 ) .Background( \"#fde7e9\" ) .CornerRadius( 8 ) ) ).Padding( 24 ); } } class BuggyComponent : Component { public override Element Render () { var (crash, setCrash) = UseState( false ); if (crash) throw new InvalidOperationException( \"Oops!\" ); return Button( \"Click to crash\" , () => setCrash( true )); } } The first argument is the child subtree. The second is a fallback — either a static element or a function that receives the caught Exception . When the boundary re-renders (e.g., its parent updates), it retries the child. That gives the user a chance to recover by changing the state that caused the crash. Retrying a failed subtree # The retry pattern uses WithKey to assign a fresh identity to the child each retry — same shape as React's resetKeys . Bumping the key drops the previous subtree's mounted state, so a transient failure that left bad state behind doesn't persist: class ErrorBoundaryRetryDemo : Component { public override Element Render () { var (resetKey, setResetKey) = UseState( 0 ); return VStack( 12 , SubHeading( \"ErrorBoundary with retry\" ), ErrorBoundary( Component&#x3C;FlakyComponent>().WithKey( $\"flaky- {resetKey} \" ), ex => VStack( 8 , TextBlock( \"Couldn't load.\" ).Bold().Foreground( \"#d13438\" ), TextBlock(ex.Message).FontSize( 12 ).Opacity( 0.7 ), // Bumping resetKey reassigns identity to the child, so the // ErrorBoundary mounts a fresh subtree on the next render. Button( \"Retry\" , () => setResetKey(resetKey + 1 )) ).Padding( 12 ).Background( \"#fde7e9\" ).CornerRadius( 8 ) ) ).Padding( 24 ); } } class FlakyComponent : Component { public override Element Render () { var (attempt, _) = UseState(Random.Shared.Next( 0 , 3 )); if (attempt == 0 ) throw new InvalidOperationException( \"Service unavailable\" ); return TextBlock( \"Loaded.\" ).Foreground( \"#107c10\" ); } } The Retry button bumps resetKey , the child gets a new key, and the reconciler mounts a fresh subtree on the next render. This is the same identity-reset trick used by navigation cache invalidation and by list-item-identity recovery in collections . Memo # Memo(ctx => ..., deps) skips re-rendering an entire subtree when its dependencies have not changed by reference. The parent can re-render freely — the memoized subtree stays frozen: class MemoSubtreeDemo : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); var (label, setLabel) = UseState( \"Expensive\" ); return VStack( 12 , SubHeading( \"Memo\" ), TextBlock( $\"Parent renders: click count = {count} \" ), Button( \"Increment\" , () => setCount(count + 1 )), Memo(ctx => { // This subtree only re-renders when label changes return Border( VStack( 4 , TextBlock( $\"Memoized: {label} \" ).Bold(), TextBlock( \"Skips re-render when deps unchanged\" ) .FontSize( 12 ).Opacity( 0.6 ) ).Padding( 12 ) ).Background( \"#f0f0f0\" ).CornerRadius( 8 ); }, label) ).Padding( 24 ); } } Memo takes a render function and a list of dependencies. The subtree only re-renders when at least one dependency changes by reference equality. Clicking \"Increment\" re-renders the parent, but the memoized border stays untouched because label hasn't changed. Use Memo for expensive subtrees — large collections , complex charts , or deeply nested component trees that don't depend on frequently-changing state. For per-cell memoization inside long lists, see Memoizing list cells . Imperative refs — UseElementRef # UseElementRef&#x3C;T>() returns a stable ElementRef&#x3C;T> whose .Current the reconciler keeps in sync with the mounted control. Attach the ref to an element with .Ref(ref) , then call into .Current from an event handler or UseEffect for the imperative operations Reactor doesn't wrap declaratively: class ElementRefFocusDemo : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); var fieldRef = this .UseElementRef&#x3C;TextBox>(); return VStack( 12 , SubHeading( \"Imperative focus via ElementRef&#x3C;T>\" ), TextBox(name, setName, placeholderText: \"Name\" ).Ref(fieldRef), Button( \"Focus the field\" , () => fieldRef.Current?.Focus(FocusState.Programmatic)) ).Padding( 24 ); } } The ref instance is identity-stable across re-renders (the hook returns the same instance every time), so it's safe to use in dep arrays and UseEffect capture. .Current is null until the element mounts; null- check before dereferencing. Caveat: Holding a raw FrameworkElement (the WinUI control underneath, not the Reactor ElementRef&#x3C;T> ) across renders is unsafe. The reconciler may unmount, pool, or replace the underlying control between renders — the control you cached in a static field or in a captured local is no longer attached to the tree, and any property you set on it has no effect. UseElementRef&#x3C;T> is the supported shape: the ref instance is stable, but .Current is re-read each time through a property the reconciler updates on mount and unmount. The analyzer REACTOR_HOOKS_005 catches the common forms (caching a control in a field, dereferencing without null-checking), but indirect caches through helper methods aren't visible to it — keep the rule in your head: never store the control, always go through the ref. .Set() escape hatch # .Set() gives you direct access to the underlying WinUI control. Use it for the properties Reactor doesn't expose as modifiers: class SetEscapeHatchDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \".Set() Escape Hatch\" ), Button( \"Custom Tooltip\" , () => { }) .Set(btn => { Microsoft.UI.Xaml.Controls.ToolTipService .SetToolTip(btn, \"This is a native tooltip\" ); btn.Padding = new Thickness( 20 , 10 , 20 , 10 ); }), TextBlock( \"Styled via .Set()\" ) .Set(tb => { tb.TextWrapping = TextWrapping.WrapWholeWords; tb.CharacterSpacing = 80 ; tb.IsTextSelectionEnabled = true ; }) ).Padding( 24 ); } } Each element type has a typed .Set() overload. Button receives an Action&#x3C;Microsoft.UI.Xaml.Controls.Button> , Text receives Action&#x3C;TextBlock> , and so on. The callback runs at both mount and update, so set properties idempotently — don't accumulate event handlers. Use .OnMount(...) instead when you need a one-shot attachment. .Set bypasses the property-diff path in reconciliation — the reconciler doesn't know about state you write through the callback, so the next render that touches a real modifier will not unwind it. Treat .Set as one-way: you're driving the control, not letting Reactor manage it. Custom hooks # Custom hooks compose existing hooks into reusable shapes. They're extension methods on RenderContext — the same shape as the built-in hooks, called from any function-component Memo(ctx => ...) or from a class component's Context : // Custom hook — composes UseState + UseEffect on RenderContext. Treated like // a built-in hook from any function-component Render. Same hook-rules apply: // call unconditionally, at the top of render, in the same order every time. static class TogglerHook { public static ( bool IsOn, Action Toggle ) UseToggler ( this RenderContext ctx, bool initial = false ) { var ( on , setOn) = ctx.UseState(initial); return ( on , () => setOn(! on )); } } class CustomHookDemo : Component { public override Element Render () => Memo(ctx => { var (isOn, toggle) = ctx.UseToggler(); return VStack( 8 , SubHeading( \"Custom hook: UseToggler\" ), Button(isOn ? \"On\" : \"Off\" , toggle), TextBlock(isOn ? \"State is on.\" : \"State is off.\" ) .Foreground(isOn ? \"#107c10\" : \"#666666\" ) ).Padding( 24 ); }); } The same hook rules apply (see rules-of-reactor ): call unconditionally, at the top of render, in the same order every time, never inside loops or conditionals. The analyzer REACTOR_HOOKS_001 enforces these rules across custom hooks as well as built-ins — it walks the call graph through extension methods. Observable bridges — UseObservableTree # UseObservableTree bridges INotifyPropertyChanged view models into the declarative render cycle. It subscribes to property changes at every depth and re-renders the component when any nested property fires. Define a standard INPC view model: class SettingsViewModel : INotifyPropertyChanged { private string _userName = \"Alice\" ; private bool _darkMode; private int _fontSize = 14 ; public string UserName { get => _userName; set { _userName = value ; Notify( nameof (UserName)); } } public bool DarkMode { get => _darkMode; set { _darkMode = value ; Notify( nameof (DarkMode)); } } public int FontSize { get => _fontSize; set { _fontSize = value ; Notify( nameof (FontSize)); } } public event PropertyChangedEventHandler? PropertyChanged; private void Notify ( string n ) => PropertyChanged?.Invoke( this , new (n)); } Then bind it in a component with UseObservableTree : class ObservableTreeDemo : Component { private static readonly SettingsViewModel _vm = new (); public override Element Render () { var vm = UseObservableTree(_vm); return VStack( 12 , SubHeading( \"UseObservableTree\" ), TextBox(vm.UserName, v => vm.UserName = v, header: \"User Name\" ), ToggleSwitch(vm.DarkMode, v => vm.DarkMode = v, header: \"Dark Mode\" ), Slider(vm.FontSize, 10 , 32 , v => vm.FontSize = ( int )v), TextBlock( $\"Preview: {vm.UserName} \" ) .FontSize(vm.FontSize).Bold() ).Padding( 24 ); } } The view model is a plain C# class with INotifyPropertyChanged . You mutate it directly ( vm.UserName = v ) and Reactor re-renders. This is the migration bridge for teams arriving with existing MVVM view models — they work without modification. UseObservable(source) is the shallow variant: it only subscribes to the source object's own PropertyChanged events, not nested objects. Prefer it for flat view models — UseObservableTree walks the entire graph on every render and the cost shows up on deep object trees. UseCollection # UseCollection tracks an ObservableCollection&#x3C;T> and re-renders the component when items are added, removed, or the collection resets: class ObservableCollectionDemo : Component { private static readonly ObservableCollection&#x3C; string > _tasks = new () { \"Review pull request\" , \"Update documentation\" }; public override Element Render () { var tasks = UseCollection(_tasks); var (input, setInput) = UseState( \"\" ); return VStack( 12 , SubHeading( \"UseCollection\" ), HStack( 8 , TextBox(input, setInput, placeholderText: \"New task\" ) .Width( 200 ), Button( \"Add\" , () => { if (! string .IsNullOrWhiteSpace(input)) { _tasks.Add(input.Trim()); setInput( \"\" ); } }) ), TextBlock( $\" {tasks.Count} tasks:\" ).SemiBold(), VStack( 4 , tasks.Select((task, i) => HStack( 8 , TextBlock( $\" {i + 1 } . {task} \" ), Button( \"Remove\" , () => _tasks.RemoveAt(i)) ).WithKey( $\"task- {i} - {task} \" ) ).ToArray()) ).Padding( 24 ); } } The hook returns IReadOnlyList&#x3C;T> — you read from it in render and mutate the original ObservableCollection in event handlers. Reactor subscribes to CollectionChanged and triggers a re-render on any modification. For owned lists (collections you created in the same component), prefer UseState&#x3C;ImmutableList&#x3C;T>> — the immutable shape fits the render-from-state model without an observable middleman. Hot loops # The fluent modifier chain is ergonomic but allocates an ElementModifiers clone per with -step. For ordinary UI that cost is invisible — a button with five modifiers means two extra small records allocated on a click handler. For inner-loop cell construction in a 4,900-cell grid that re-renders 30× per second, those clones dominate the allocation profile. The escape hatch is to construct the Element and its ElementModifiers record directly, skipping the fluent chain entirely. LayoutModifiers and VisualModifiers are public types specifically so perf-critical code can build them once instead of having the fluent chain rebuild them step-by-step. // Fluent — five clones per cell. Right tool for ordinary UI. var cell = TextBlock(label) .FontSize( 8 ) .Foreground(item.IsUp ? GreenBrush : RedBrush) .Padding( 2 , 1 , 2 , 1 ) .Grid(row: r, column: c); // Direct record initializer — one TextBlockElement, one ElementModifiers, // two bucket sub-records, one Attached dictionary. Use only when the // allocation cost shows up in profiles. var cell = new TextBlockElement(label) { FontSize = 8 , Modifiers = new ElementModifiers { Layout = new LayoutModifiers { Padding = new Thickness( 2 , 1 , 2 , 1 ) }, Visual = new VisualModifiers { Foreground = item.IsUp ? GreenBrush : RedBrush }, }, Attached = new Dictionary&#x3C;Type, object >( 1 ) { [ typeof(GridAttached) ] = new GridAttached(r, c, 1 , 1 ), }, }; Workload shape. Use this idiom in lists or grids with hundreds-plus elements per render — tickers, log tables, observability dashboards. Don't adopt it for ordinary screens. The fluent chain is the right tool for everything except the inner cell loop. Trade-offs. Roughly halves the allocation cost of cell construction on the 4,900-cell stress grid, but loses fluent ergonomics. The direct form is more brittle to refactor — changing one field touches an explicit initializer block instead of a chain step. Restrict it to the identifiable hot loop and keep the rest of the file fluent. See modifier-system for the under-the-hood explanation of why the fluent chain allocates. Reference implementation. The canonical before/after pair lives in tests/stress_perf/StressPerf.Reactor (naive — fluent chain, the shape unaware users write) and tests/stress_perf/StressPerf.ReactorOptimized (idiomatic perf-tuned variant). Same workload, side-by-side diffable. Memoizing list cells # UseMemoCells skips the cell-build for indices whose item value (and declared dependencies) haven't changed since the previous render. The reconciler's ReferenceEquals shortcut means a reused cell allocates nothing and skips diffing entirely. var theme = ctx.UseTheme(); var children = ctx.UseMemoCells( stocks, (item, i) => Cell(item, theme), theme); // ← deps; framework invalidates on change When it's the right hammer. Tickers, log tables, file lists, large read-only grids — anywhere the cell content is a pure function of T plus a small set of declared deps. When it's the wrong hammer. Rows whose chrome depends on focus, drag, selection, or hover state that you aren't capturing in deps. Memo silently renders stale when an external state change isn't declared as a dep — the analyzer below catches the obvious cases, but indirect captures through helper methods aren't visible to it. Three overloads: Overload Use when UseMemoCells&#x3C;T> Per-item value equality. Default choice. UseMemoCellsByKey&#x3C;T, TKey> Items have stable identity but mutable interior. Hashes by key, value-compares for content. Reordered keys reuse cells via the reconciler's keyed-children path. UseMemoCellsByIndex&#x3C;T> Data source already knows which indices changed. Skips the per-cell equality scan; only the named indices run the builder. gen2 caveat. Memo trades short-lived gen0 churn for longer-lived gen1/gen2 retention. Many memoized lists across an app can compound gen2 pressure even when bytes-per-tick drops. Profile before adopting across the board — see perf-instrumentation for ETW-driven sampling and performance for the top-down walkthrough. Compile-time safety net. The companion Roslyn analyzer REACTOR_HOOKS_007 warns when a builder closure captures a value that isn't declared in the deps list. Codefix is \"add the missing capture to deps\". Indirect captures through intermediate methods are a documented blind spot — the analyzer can't see through a method call without whole-program analysis (same blind spot as React's react-hooks/exhaustive-deps ). Patterns # Custom hook authoring # Custom hooks let you bundle a hook combination behind a single name — UseDebouncedValue , UseWindowScroll , UsePersistedToggle — and reuse it across components. They're plain RenderContext extension methods. The rules-of-reactor page covers naming conventions ( Use* prefix) and call-order discipline; the hooks-internals page explains why custom hooks work — Reactor doesn't distinguish them from built-ins, the slot table just counts hook invocations in order. ErrorBoundary with retry key # The retry pattern shown above scales to async failures, transient network errors, and any subtree whose state should reset on user acknowledgment. Pair the retry key with an async resource that holds the in-flight task — bumping the key cancels the previous attempt by reassigning identity, so a slow fetch from the failed attempt doesn't race with the new one. Imperative focus from an event handler # A login form that focuses the password field when the username is valid is the canonical UseElementRef shape — see the snippet above for the call site. The same shape covers focusing a list item after a keyboard shortcut, scrolling a VirtualList to a known index via VirtualListRef , and starting a Composition animation on a known control. For the broader focus model (focus trap, enter-to-advance, automation tree), see accessibility and focus-and-input-internals . Common Mistakes # Caching elements (or controls) in module-level statics # // Don't — the FrameworkElement underneath is pool-managed. static TextBox? _passwordField; public override Element Render () { var (pwd, setPwd) = UseState( \"\" ); return TextBox(pwd, setPwd).Set(c => _passwordField = c); } // Elsewhere: static void FocusPassword () => _passwordField?.Focus(FocusState.Programmatic); The reconciler may unmount, swap, or pool the underlying control between renders. The static field outlives every one of those events and ends up pointing at a detached or recycled instance. Use UseElementRef&#x3C;T> — the ref's .Current is null when the control isn't attached and is repopulated when it remounts. Calling setState from a UseEffect cleanup # // Don't — setState inside cleanup runs during unmount/dispose. UseEffect(() => { var sub = Subscribe(); return () => { sub.Dispose(); setState( false ); }; // ← stale state, may throw }, []); Cleanup runs after the component has stopped being part of the tree; calling setState from there either no-ops (the disposed render context refuses the call) or schedules a render against state that no longer matters. Put state resets in the effect body or in an event handler that explicitly transitions the parent. See effects-scheduling for cleanup ordering. Subscribing to events from a parent that captures the wrong this # // Don't — captures the parent's `this` inside the child component. class Parent : Component { public override Element Render () => Component&#x3C;Child>().Set(c => c.Loaded += (s, e) => this .OnChildLoaded()); } .Set(control => ...) runs on every mount and update — every event subscription accumulates. The this inside the lambda is the parent component, captured at the lambda's closure site, which keeps a strong reference to a stale render's component instance after the parent has re-rendered. Two fixes: lift the handler to a stable UseCallback in the parent and pass it via props , or use .OnMount(...) for a one-shot subscription with explicit cleanup. Reaching for .Set when a modifier exists # // Don't — Reactor exposes .RequestedTheme() as a modifier. Button( \"Save\" , onSave).Set(c => c.RequestedTheme = ElementTheme.Dark); The modifier participates in property diffing — the next render that removes the modifier walks the property back to default. .Set is imperative and one-way; the next render won't undo it. Prefer the modifier when one exists; reserve .Set for properties Reactor genuinely doesn't expose. The REACTOR_THEME_003 analyzer catches this specific case for RequestedTheme ; the same shape applies broadly. Tips # Wrap third-party components in ErrorBoundary . If a plugin or marketplace component throws during render, the boundary stops it from taking down your entire app. Pair with a Retry button keyed by a UseState counter to let users recover without restarting the app. Profile before memoizing. Memo adds bookkeeping cost and a second hook slot per subtree. Only reach for it when you have measured a re-render bottleneck on a profiler. Most subtrees re-render fast enough without it. Prefer declarative modifiers over .Set() . Every .Set() call is an escape hatch that bypasses Reactor's property diff. Reserve it for WinUI properties Reactor doesn't expose. If you find yourself reaching for .Set frequently, the right next step is a Reactor feature request — the framework should grow a modifier for the common case. Keep view models outside the component. Store them as static fields or inject them via context . Creating a new view model inside Render() would allocate a fresh object on every render and lose all state, plus break the UseObservableTree subscription on every render. Use UseObservable when depth is one level. UseObservableTree walks the entire object graph on every change. For flat view models with no nested INPC objects, UseObservable is cheaper and produces fewer event subscriptions. Next Steps # Data System — DataGrid with sort, filter, and inline editing WinForms Interop — host Reactor components inside WinForms applications Charting — data visualization with line, bar, area, pie, and force-directed charts Hooks — review the hook system that UseObservableTree , UseCollection , and custom hooks build on Effects and Lifecycle — side-effect scheduling alongside observable data binding Hooks internals — Under-the-hood: how hooks actually store their values Modifier system — Under-the-hood: why the fluent chain allocates, and what the direct-initializer escape buys Rules of Reactor — hook rules, render purity, and the dos and don'ts of custom hook authoring",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/advanced.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/advanced.md",
      "headings": [
        {
          "depth": 2,
          "id": "escape-hatch-reference",
          "text": "Escape-hatch reference"
        },
        {
          "depth": 2,
          "id": "error-boundary",
          "text": "Error Boundary"
        },
        {
          "depth": 3,
          "id": "retrying-a-failed-subtree",
          "text": "Retrying a failed subtree"
        },
        {
          "depth": 2,
          "id": "memo",
          "text": "Memo"
        },
        {
          "depth": 2,
          "id": "imperative-refs--useelementref",
          "text": "Imperative refs — UseElementRef"
        },
        {
          "depth": 2,
          "id": "set-escape-hatch",
          "text": ".Set() escape hatch"
        },
        {
          "depth": 2,
          "id": "custom-hooks",
          "text": "Custom hooks"
        },
        {
          "depth": 2,
          "id": "observable-bridges--useobservabletree",
          "text": "Observable bridges — UseObservableTree"
        },
        {
          "depth": 2,
          "id": "usecollection",
          "text": "UseCollection"
        },
        {
          "depth": 2,
          "id": "hot-loops",
          "text": "Hot loops"
        },
        {
          "depth": 2,
          "id": "memoizing-list-cells",
          "text": "Memoizing list cells"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "custom-hook-authoring",
          "text": "Custom hook authoring"
        },
        {
          "depth": 3,
          "id": "errorboundary-with-retry-key",
          "text": "ErrorBoundary with retry key"
        },
        {
          "depth": 3,
          "id": "imperative-focus-from-an-event-handler",
          "text": "Imperative focus from an event handler"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "caching-elements-or-controls-in-module-level-statics",
          "text": "Caching elements (or controls) in module-level statics"
        },
        {
          "depth": 3,
          "id": "calling-setstate-from-a-useeffect-cleanup",
          "text": "Calling setState from a UseEffect cleanup"
        },
        {
          "depth": 3,
          "id": "subscribing-to-events-from-a-parent-that-captures-the-wrong-this",
          "text": "Subscribing to events from a parent that captures the wrong this"
        },
        {
          "depth": 3,
          "id": "reaching-for-set-when-a-modifier-exists",
          "text": "Reaching for .Set when a modifier exists"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Layout",
      "path": "layout.md",
      "slug": "/layout/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/layout/\">Layout</a>.</p>\n</blockquote>\n<p>Every Microsoft.UI.Reactor (Reactor) layout is a composition of single-responsibility panels —\nyou pick by <em>direction</em> (<code>HStack</code> for a row, <code>VStack</code> for a column),\n<em>wrap behavior</em> (<code>WrapGrid</code> when items should flow into new rows), or\n<em>grid axes</em> (<code>Grid</code> when you want explicit rows × columns). The panels\ndo not overlap each other's job: a <code>Stack</code> is for one axis, a <code>Grid</code> is\nfor two, a <code>Canvas</code> is for absolute coordinates, and a <code>FlexPanel</code> (see\n<a href=\"/flex-layout/\">flex-layout</a>) is for the CSS-Flexbox feature set\nincluding <code>flex-grow</code>, <code>flex-shrink</code>, and <code>flex-basis</code>. The reason this\nmatters: the right panel keeps the layout cost linear in the child\ncount. The wrong panel — say, a <code>VStack</code> of <code>HStack</code>s where a <code>Grid</code>\nwould do — forces every nested measure-and-arrange pass through\nmultiple panels' code paths, which both costs more and reads worse.\nStart with <code>VStack</code> for a screen's top-level scaffold; switch to <code>Grid</code>\nthe moment you have row-and-column alignment; switch to\n<a href=\"/flex-layout/\"><code>FlexPanel</code></a> when you need ratio-based sizing or\nmulti-line wrapping with gaps.</p>\n<h1 id=\"layout\">Layout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#layout\">#</a></h1>\n<p>Reactor provides a small set of layout primitives that compose to build any\nscreen structure. Every layout element accepts children and optional spacing.</p>\n<h2 id=\"decision-table\">Decision table<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#decision-table\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Shape</th><th>Panel</th><th>Why</th></tr></thead><tbody><tr><td>Items in one direction</td><td><code>VStack(spacing, children)</code> / <code>HStack(spacing, children)</code></td><td>Single-axis stacking with built-in spacing.</td></tr><tr><td>Items in rows × columns</td><td><code>Grid(columns, rows, children)</code></td><td>Two-axis with explicit track sizing (<code>Auto</code>, <code>Star</code>, <code>Px</code>).</td></tr><tr><td>Items that should wrap</td><td><code>WrapGrid(maxRowsOrColumns, children)</code></td><td>Auto-flow after a row fills.</td></tr><tr><td>Items that need ratio sizing or multi-line wrap with gaps</td><td><code>FlexPanel(...)</code> (see <a href=\"/flex-layout/\">flex-layout</a>)</td><td>CSS-Flexbox semantics: grow/shrink/basis.</td></tr><tr><td>Absolute positioning</td><td><code>Canvas(children)</code></td><td>Free-form <code>Canvas.SetLeft</code> / <code>SetTop</code>.</td></tr><tr><td>Visual container</td><td><code>Border(child)</code> / <code>Card(child)</code></td><td>Background, corner radius, padding around a single child.</td></tr><tr><td>Overflow handling</td><td><code>ScrollView(child)</code></td><td>Wraps anything that may exceed its allotted space.</td></tr><tr><td>Collapsible group</td><td><code>Expander(header, content)</code></td><td>Header that toggles its content panel.</td></tr></tbody></table>\n<h2 id=\"vstack-and-hstack\">VStack and HStack<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#vstack-and-hstack\">#</a></h2>\n<p>The two most common layouts. VStack stacks children vertically, HStack\nhorizontally. The first argument is the pixel spacing between children:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StackDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"VStack and HStack\"</span>),\n            VStack(<span class=\"hljs-number\">4</span>,\n                TextBlock(<span class=\"hljs-string\">\"VStack: items top to bottom\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Item A\"</span>), TextBlock(<span class=\"hljs-string\">\"Item B\"</span>), TextBlock(<span class=\"hljs-string\">\"Item C\"</span>)\n            ),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"HStack:\"</span>),\n                Button(<span class=\"hljs-string\">\"One\"</span>), Button(<span class=\"hljs-string\">\"Two\"</span>), Button(<span class=\"hljs-string\">\"Three\"</span>)\n            )\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/vstack-hstack.png\" alt=\"VStack and HStack\"></p>\n<p><code>VStack(16, ...)</code> puts 16 pixels between each child, top to bottom.\n<code>HStack(8, ...)</code> puts 8 pixels between each child, left to right. Omit\nthe spacing argument for zero spacing: <code>VStack(child1, child2)</code>.</p>\n<p>Nest them freely — an HStack inside a VStack, VStacks inside HStacks,\nas deep as you need. <strong>But the moment you find yourself nesting more\nthan two deep, reach for <code>Grid</code> instead</strong> (see\n<a href=\"#common-mistakes\">Common Mistakes</a> for why).</p>\n<h2 id=\"grid\">Grid<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#grid\">#</a></h2>\n<p>For row-and-column layouts, use <code>Grid</code>. Define columns and rows with\n<code>GridSize</code> helpers, then place children with the <code>.Grid()</code> modifier:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GridDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Grid\"</span>),\n            Grid(\n                columns: [GridSize.Px(<span class=\"hljs-number\">120</span>), GridSize.Star(), GridSize.Auto],\n                rows: [GridSize.Auto, GridSize.Auto],\n                TextBlock(<span class=\"hljs-string\">\"Label\"</span>).Bold().Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">0</span>),\n                TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Input...\"</span>)\n                    .Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">1</span>),\n                Button(<span class=\"hljs-string\">\"Go\"</span>).Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">2</span>),\n                TextBlock(<span class=\"hljs-string\">\"Status\"</span>).Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">0</span>),\n                TextBlock(<span class=\"hljs-string\">\"Ready\"</span>).Foreground(<span class=\"hljs-string\">\"#0078D4\"</span>)\n                    .Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">1</span>, columnSpan: <span class=\"hljs-number\">2</span>)\n            ).Height(<span class=\"hljs-number\">80</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/grid-layout.png\" alt=\"Grid layout\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Helper</th><th>Meaning</th></tr></thead><tbody><tr><td><code>GridSize.Px(200)</code></td><td>Fixed 200 pixels</td></tr><tr><td><code>GridSize.Star()</code></td><td>Proportional — fills available space (weight 1)</td></tr><tr><td><code>GridSize.Star(2)</code></td><td>Proportional — twice the space of <code>GridSize.Star()</code></td></tr><tr><td><code>GridSize.Auto</code></td><td>Sizes to content</td></tr></tbody></table>\n<p>Place children with <code>.Grid(row: 0, column: 1)</code>. Use <code>columnSpan</code> or\n<code>rowSpan</code> to span multiple cells: <code>.Grid(row: 1, column: 0, columnSpan: 2)</code>.</p>\n<blockquote>\n<p>The older <code>Grid([\"120\", \"1*\", \"Auto\"], ...)</code> string-form overload still\nworks but is soft-deprecated (<code>CS0618</code>). Prefer the typed helpers — they\ncatch typos at compile time.</p>\n</blockquote>\n<h2 id=\"card\">Card<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#card\">#</a></h2>\n<p><code>Card(child)</code> wraps any element in the canonical WinUI card chrome — 8px\ncorner radius, 16px padding, theme-aware background and 1px stroke. Use\nit instead of hand-rolling a\n<code>Border(...).Background(Theme.CardBackground).WithBorder(...)</code> pipeline:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CardDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Card\"</span>),\n            Card(\n                VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Recent activity\"</span>).SemiBold(),\n                    TextBlock(<span class=\"hljs-string\">\"3 new messages, 2 mentions\"</span>)\n                        .Foreground(Theme.SecondaryText)\n                )\n            ).Width(<span class=\"hljs-number\">240</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/card.png\" alt=\"Card chrome around a summary block\"></p>\n<p>Override any preset by chaining further fluents:\n<code>Card(child).Padding(24).CornerRadius(16)</code>. The background and stroke\nresolve through <code>ThemeRef</code>, so light/dark/contrast switches re-paint\nwithout code changes — see <a href=\"/styling/\">styling</a>.</p>\n<h2 id=\"type-ramp-factories\">Type-Ramp Factories<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#type-ramp-factories\">#</a></h2>\n<p>For headings and body copy that match the WinUI 3 type ramp, use the\nnamed factories instead of repeating <code>.FontSize(...).Bold()</code> chains:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TypeRampDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Title(<span class=\"hljs-string\">\"Quarterly results\"</span>),\n            Subtitle(<span class=\"hljs-string\">\"Q3 2026 highlights\"</span>),\n            BodyLarge(<span class=\"hljs-string\">\"Revenue grew 18% year over year.\"</span>),\n            BodyStrong(<span class=\"hljs-string\">\"Net income reached an all-time high.\"</span>),\n            Body(<span class=\"hljs-string\">\"Full breakdown on the following pages.\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/type-ramp.png\" alt=\"WinUI 3 type ramp: Title / Subtitle / BodyLarge / BodyStrong / Body\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Factory</th><th>WinUI style</th></tr></thead><tbody><tr><td><code>Title(text)</code></td><td><code>TitleTextBlockStyle</code> — 28px Semibold</td></tr><tr><td><code>Subtitle(text)</code></td><td><code>SubtitleTextBlockStyle</code> — 20px Semibold</td></tr><tr><td><code>BodyLarge(text)</code></td><td><code>BodyLargeTextBlockStyle</code> — 18px regular</td></tr><tr><td><code>BodyStrong(text)</code></td><td><code>BodyStrongTextBlockStyle</code> — 14px Semibold</td></tr><tr><td><code>Body(text)</code></td><td><code>BodyTextBlockStyle</code> — 14px regular</td></tr></tbody></table>\n<p>These are factories (not fluents) by design — see\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §17.6. Layer\nfurther modifiers on the result (<code>Title(\"…\").Foreground(Theme.Accent)</code>)\nwhen you need to deviate.</p>\n<h2 id=\"scrollview-and-border\">ScrollView and Border<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#scrollview-and-border\">#</a></h2>\n<p><code>ScrollView</code> wraps content that might overflow. <code>Border</code> adds a visual\ncontainer with background, corner radius, and padding:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ScrollBorderDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"ScrollView and Border\"</span>),\n            Border(\n                ScrollView(\n                    VStack(<span class=\"hljs-number\">4</span>,\n                        ForEach(\n                            Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">20</span>),\n                            i => TextBlock(<span class=\"hljs-string\">$\"Scrollable item <span class=\"hljs-subst\">{i}</span>\"</span>))\n                    ).Padding(<span class=\"hljs-number\">8</span>)\n                ).Height(<span class=\"hljs-number\">120</span>)\n            ).CornerRadius(<span class=\"hljs-number\">4</span>).Background(<span class=\"hljs-string\">\"#F5F5F5\"</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/scrollview-border.png\" alt=\"ScrollView and Border\"></p>\n<p><code>ScrollView</code> takes a single child element. Wrap a <code>VStack</code> to scroll a\nvertical list. <code>Border</code> is purely visual — it renders a rounded\nrectangle behind its child and is useful for cards, panels, and\ngrouping.</p>\n<p><strong>For long lists, prefer <a href=\"/collections/\"><code>VirtualList</code></a> over\n<code>ScrollView(VStack(...))</code>.</strong> A <code>ScrollView</code> lays out every child even\nwhen off-screen, which is fast for ≤100 items and visibly slow above\n~500; <code>VirtualList</code> materializes only the items in view.</p>\n<h2 id=\"expander-and-canvas\">Expander and Canvas<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#expander-and-canvas\">#</a></h2>\n<p><code>Expander</code> shows a header and collapses/expands its content on click.\n<code>Canvas</code> positions children at absolute coordinates:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ExpanderCanvasDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Expander\"</span>),\n            Expander(<span class=\"hljs-string\">\"Click to expand\"</span>, VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Hidden content revealed!\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Expanders are great for optional details.\"</span>)\n            )),\n            SubHeading(<span class=\"hljs-string\">\"Canvas\"</span>),\n            Border(\n                Canvas(\n                    TextBlock(<span class=\"hljs-string\">\"Top-left\"</span>).Set(c => {\n                        Microsoft.UI.Xaml.Controls.Canvas.SetLeft((UIElement)c, <span class=\"hljs-number\">10</span>);\n                        Microsoft.UI.Xaml.Controls.Canvas.SetTop((UIElement)c, <span class=\"hljs-number\">10</span>);\n                    }),\n                    TextBlock(<span class=\"hljs-string\">\"Center\"</span>).Set(c => {\n                        Microsoft.UI.Xaml.Controls.Canvas.SetLeft((UIElement)c, <span class=\"hljs-number\">120</span>);\n                        Microsoft.UI.Xaml.Controls.Canvas.SetTop((UIElement)c, <span class=\"hljs-number\">40</span>);\n                    })\n                ).Height(<span class=\"hljs-number\">90</span>).Width(<span class=\"hljs-number\">300</span>)\n            ).Background(<span class=\"hljs-string\">\"#F5F5F5\"</span>).CornerRadius(<span class=\"hljs-number\">4</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/expander-canvas.png\" alt=\"Expander and Canvas\"></p>\n<p><code>Expander(header, content)</code> handles the expand/collapse state\ninternally. Pass <code>isExpanded: true</code> to start expanded, or use\n<code>onExpandedChanged</code> to track the state yourself.</p>\n<p><code>Canvas</code> positions children using the <code>.Set()</code> modifier to call\n<code>Canvas.SetLeft</code> and <code>Canvas.SetTop</code>. Use it for overlays, diagrams, or\nany layout that doesn't follow a stack or grid pattern.</p>\n<h2 id=\"responsive-layout\">Responsive Layout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#responsive-layout\">#</a></h2>\n<p>Switch between layouts based on window width. Use a state toggle or\n<a href=\"/hooks/\"><code>UseBreakpoint</code></a> to swap between HStack and VStack:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ResponsiveDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (wide, setWide) = UseState(<span class=\"hljs-literal\">true</span>);\n\n        <span class=\"hljs-keyword\">var</span> content = <span class=\"hljs-keyword\">new</span> Element[]\n        {\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel A\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(<span class=\"hljs-string\">\"#E3F2FD\"</span>).CornerRadius(<span class=\"hljs-number\">4</span>),\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel B\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(<span class=\"hljs-string\">\"#FFF3E0\"</span>).CornerRadius(<span class=\"hljs-number\">4</span>),\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel C\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(<span class=\"hljs-string\">\"#E8F5E9\"</span>).CornerRadius(<span class=\"hljs-number\">4</span>),\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Responsive Layout\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Simulate wide screen:\"</span>),\n                ToggleSwitch(wide, setWide)\n            ),\n            If(wide,\n                () => HStack(<span class=\"hljs-number\">12</span>, content),\n                () => VStack(<span class=\"hljs-number\">8</span>, content))\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/responsive.png\" alt=\"Responsive layout\"></p>\n<p>For real responsive layouts, use <a href=\"/hooks/\"><code>UseBreakpoint</code></a>\n<code>(window, minWidth)</code> which returns <code>true</code> when the window is at least\n<code>minWidth</code> pixels wide. Pair it with <code>If()</code> to swap layouts:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> wide = UseBreakpoint(window, <span class=\"hljs-number\">800</span>);\n<span class=\"hljs-keyword\">return</span> If(wide,\n    () => HStack(<span class=\"hljs-number\">12</span>, panelA, panelB),\n    () => VStack(<span class=\"hljs-number\">8</span>, panelA, panelB));\n</code></pre>\n<!-- /ai:lock -->\n<p><code>UseWindowSize(window)</code> returns <code>(Width, Height)</code> if you need exact\ndimensions for more granular control.</p>\n<h2 id=\"alignment-and-sizing\">Alignment and Sizing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#alignment-and-sizing\">#</a></h2>\n<p>Every element supports sizing and alignment modifiers:</p>\n<!-- ai:lock -->\n<pre><code class=\"hljs language-csharp\">Text(<span class=\"hljs-string\">\"Centered\"</span>).HAlign(HorizontalAlignment.Center)\nText(<span class=\"hljs-string\">\"Fixed width\"</span>).Width(<span class=\"hljs-number\">200</span>).Height(<span class=\"hljs-number\">40</span>)\nVStack(<span class=\"hljs-number\">8</span>, items).Margin(<span class=\"hljs-number\">24</span>).Padding(<span class=\"hljs-number\">16</span>)\n</code></pre>\n<!-- /ai:lock -->\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Modifier</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Width(n)</code></td><td>Fixed width in pixels</td></tr><tr><td><code>.Height(n)</code></td><td>Fixed height in pixels</td></tr><tr><td><code>.Margin(n)</code></td><td>Outer spacing (all sides)</td></tr><tr><td><code>.Padding(n)</code></td><td>Inner spacing (all sides)</td></tr><tr><td><code>.HAlign(alignment)</code></td><td>Horizontal alignment (Left, Center, Right, Stretch)</td></tr><tr><td><code>.VAlign(alignment)</code></td><td>Vertical alignment (Top, Center, Bottom, Stretch)</td></tr></tbody></table>\n<p>See <a href=\"/modifier-system/\">modifier-system</a> for how these chain\ninternally.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Grid</code> panels with <code>*</code> (star) sizing measure their children twice on\nthe first layout pass — once with <code>Available = Infinity</code> to determine\nthe natural content size, and once with <code>Available = ActualWidth</code> to\nresolve the star tracks. The double pass is fast for ≤100 children and\nvisible above ~500 (the layout-cost overlay reports the extra\nmeasure-pass cost — see <a href=\"/dev-tooling/\">dev-tooling</a>). For large\nlists rendered into a star-sized cell, wrap the content in\n<a href=\"/collections/\"><code>VirtualList</code></a> or <code>LazyVStack</code> so the panel only\nmaterializes the visible window; the host star track measures the\nviewport size once. Fixed (<code>Px</code>) and <code>Auto</code> tracks do not pay the\ndouble-measure cost.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"app-shell-scaffold\">App-shell scaffold<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#app-shell-scaffold\">#</a></h3>\n<p>The canonical admin-style app shell — title bar, sidebar, main content,\nstatus bar. One <code>Grid</code> declaration; one measure-and-arrange pass:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// App shell scaffold: title bar + sidebar + content using a single Grid</span>\n<span class=\"hljs-comment\">// declaration. The two-column / three-row layout is the canonical</span>\n<span class=\"hljs-comment\">// shape for an admin-style app and replaces hand-rolled nested</span>\n<span class=\"hljs-comment\">// HStacks/VStacks.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AppShellExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> Grid(\n            columns: [GridSize.Px(<span class=\"hljs-number\">220</span>), GridSize.Star()],\n            rows:    [GridSize.Px(<span class=\"hljs-number\">44</span>),  GridSize.Star(), GridSize.Px(<span class=\"hljs-number\">28</span>)],\n            <span class=\"hljs-comment\">// Top bar — spans both columns.</span>\n            Border(TextBlock(<span class=\"hljs-string\">\"My App\"</span>).Bold().Padding(<span class=\"hljs-number\">12</span>))\n                .Background(Theme.LayerFill)\n                .Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">0</span>, columnSpan: <span class=\"hljs-number\">2</span>),\n            <span class=\"hljs-comment\">// Sidebar.</span>\n            Border(VStack(<span class=\"hljs-number\">4</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Dashboard\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Reports\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Settings\"</span>))\n                .Padding(<span class=\"hljs-number\">12</span>))\n                .Background(Theme.CardBackground)\n                .Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">0</span>),\n            <span class=\"hljs-comment\">// Main content.</span>\n            ScrollView(VStack(<span class=\"hljs-number\">8</span>,\n                    Title(<span class=\"hljs-string\">\"Welcome\"</span>),\n                    Body(<span class=\"hljs-string\">\"Stack -> Grid for the shell, VStack inside the panes.\"</span>)))\n                .Padding(<span class=\"hljs-number\">16</span>)\n                .Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">1</span>),\n            <span class=\"hljs-comment\">// Status bar.</span>\n            TextBlock(<span class=\"hljs-string\">\"Ready\"</span>).Padding(<span class=\"hljs-number\">6</span>)\n                .Grid(row: <span class=\"hljs-number\">2</span>, column: <span class=\"hljs-number\">0</span>, columnSpan: <span class=\"hljs-number\">2</span>)\n        ).Width(<span class=\"hljs-number\">560</span>).Height(<span class=\"hljs-number\">360</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/app-shell.png\" alt=\"App shell with title bar, sidebar, content, and status bar\"></p>\n<p>A naive version with nested <code>HStack</code>/<code>VStack</code> panels works but couples\nthe rows to the column widths — resizing the sidebar requires editing\nevery row that intersects it. The <code>Grid</code> keeps the axes independent.</p>\n<h3 id=\"auto-grid\">Auto-grid<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#auto-grid\">#</a></h3>\n<p>A photo gallery, a tile dashboard, a chip cloud — anything that should\nflow into rows as the container width allows — uses <code>WrapGrid</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Auto-grid: WrapGrid wraps a sequence of items into columns once a</span>\n<span class=\"hljs-comment\">// row fills, no manual row/column placement. Picks \"max 4 per row\".</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AutoGridExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> WrapGrid(maxRowsOrColumns: <span class=\"hljs-number\">4</span>,\n            children: Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">11</span>)\n                .Select(i =>\n                    Border(TextBlock(<span class=\"hljs-string\">$\"Tile <span class=\"hljs-subst\">{i}</span>\"</span>).Padding(<span class=\"hljs-number\">12</span>))\n                        .Background(Theme.CardBackground)\n                        .CornerRadius(<span class=\"hljs-number\">6</span>)\n                        .Width(<span class=\"hljs-number\">110</span>).Height(<span class=\"hljs-number\">60</span>))\n                .Cast&#x3C;Element?>()\n                .ToArray()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/auto-grid.png\" alt=\"WrapGrid auto-flowing 11 tiles into 4-column rows\"></p>\n<p><code>maxRowsOrColumns</code> caps the major axis; the panel wraps once that\ncount is reached. For content that should wrap by available pixel\nwidth (rather than item count), use <a href=\"/flex-layout/\"><code>FlexPanel</code></a> with\n<code>FlexWrap.Wrap</code>.</p>\n<h3 id=\"responsive-switcher\">Responsive switcher<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#responsive-switcher\">#</a></h3>\n<p>The minimum responsive recipe — different shape at narrow vs. wide:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Responsive switcher: the same content renders as HStack at wide</span>\n<span class=\"hljs-comment\">// widths, VStack when the window narrows past 480px. UseBreakpoint is</span>\n<span class=\"hljs-comment\">// the canonical hook for this pattern.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ResponsiveSwitcherExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (simulated, setSimulated) = UseState(<span class=\"hljs-literal\">true</span>);  <span class=\"hljs-comment\">// toy demo without a window handle</span>\n\n        <span class=\"hljs-keyword\">var</span> panels = <span class=\"hljs-keyword\">new</span> Element[]\n        {\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel A\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">4</span>),\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel B\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">4</span>),\n            Border(TextBlock(<span class=\"hljs-string\">\"Panel C\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n                .Background(Theme.CardBackground).CornerRadius(<span class=\"hljs-number\">4</span>),\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Simulate wide window:\"</span>),\n                ToggleSwitch(simulated, setSimulated)),\n            If(simulated,\n                () => HStack(<span class=\"hljs-number\">12</span>, panels),\n                () => VStack(<span class=\"hljs-number\">8</span>, panels))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/responsive-switcher.png\" alt=\"Responsive switcher between HStack and VStack\"></p>\n<p>The same <code>panels</code> array drops into either container. The real\nproduction version reads <code>UseBreakpoint(window, 480)</code> rather than a\nmanual toggle — see the <a href=\"#responsive-layout\">Responsive Layout</a> section\nabove.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"nesting-deep-stack-hierarchies-where-grid-would-suffice\">Nesting deep stack hierarchies where Grid would suffice<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#nesting-deep-stack-hierarchies-where-grid-would-suffice\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// BAD — deeply nested VStack/HStack hierarchies obscure intent and</span>\n<span class=\"hljs-comment\">// produce a layout cost that Grid would absorb in one measure pass.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DontDeepStack</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">8</span>,\n            VStack(<span class=\"hljs-number\">4</span>,\n                TextBlock(<span class=\"hljs-string\">\"Label\"</span>).Bold(),\n                TextBlock(<span class=\"hljs-string\">\"Sublabel\"</span>).Foreground(Theme.SecondaryText)),\n            VStack(<span class=\"hljs-number\">0</span>,\n                HStack(<span class=\"hljs-number\">4</span>, TextBlock(<span class=\"hljs-string\">\"First:\"</span>), TextBlock(<span class=\"hljs-string\">\"Value 1\"</span>)),\n                HStack(<span class=\"hljs-number\">4</span>, TextBlock(<span class=\"hljs-string\">\"Second:\"</span>), TextBlock(<span class=\"hljs-string\">\"Value 2\"</span>)),\n                HStack(<span class=\"hljs-number\">4</span>, TextBlock(<span class=\"hljs-string\">\"Third:\"</span>), TextBlock(<span class=\"hljs-string\">\"Value 3\"</span>)))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Three levels of <code>HStack</code>/<code>VStack</code> to lay out a label-value list is\nharder to read than the row × column shape it really is. The same\nshape as a <code>Grid</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// GOOD — same shape as a 2-column Grid. Labels in column 0 are auto-</span>\n<span class=\"hljs-comment\">// sized to their content; column 1 stretches.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DoGridForForms</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> Grid(\n            columns: [GridSize.Auto, GridSize.Star()],\n            rows:    [GridSize.Auto, GridSize.Auto, GridSize.Auto],\n            TextBlock(<span class=\"hljs-string\">\"First:\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">0</span>),\n            TextBlock(<span class=\"hljs-string\">\"Value 1\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">0</span>, column: <span class=\"hljs-number\">1</span>),\n            TextBlock(<span class=\"hljs-string\">\"Second:\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">0</span>),\n            TextBlock(<span class=\"hljs-string\">\"Value 2\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">1</span>, column: <span class=\"hljs-number\">1</span>),\n            TextBlock(<span class=\"hljs-string\">\"Third:\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">2</span>, column: <span class=\"hljs-number\">0</span>),\n            TextBlock(<span class=\"hljs-string\">\"Value 3\"</span>).Margin(<span class=\"hljs-number\">4</span>).Grid(row: <span class=\"hljs-number\">2</span>, column: <span class=\"hljs-number\">1</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>).Width(<span class=\"hljs-number\">300</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/layout/do-grid-for-forms.png\" alt=\"Two-column Auto/Star Grid form\"></p>\n<p>The <code>Auto</code> column hugs the label width; the <code>Star</code> column stretches.\nAdding a row is one declaration. The nested-Stack version requires\nre-indenting every sibling. As a rule: <strong>if any two items in your\nlayout need to align across rows or columns, use <code>Grid</code>.</strong></p>\n<h3 id=\"missing--sizing-in-templates-that-should-fill\">Missing <code>*</code> sizing in templates that should fill<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#missing--sizing-in-templates-that-should-fill\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nGrid(\n    columns: [GridSize.Px(<span class=\"hljs-number\">220</span>), GridSize.Auto],   <span class=\"hljs-comment\">// content column is auto-sized</span>\n    rows: [GridSize.Star()],\n    Sidebar(), Content())  <span class=\"hljs-comment\">// Content shrinks to its intrinsic width</span>\n</code></pre>\n<p><code>Auto</code> is \"measure my content and use that\"; <code>Star</code> is \"take what's\nleft\". A main-content cell that should fill the remaining horizontal\nspace must declare <code>GridSize.Star()</code> — otherwise it shrinks to its\nintrinsic size and the surrounding empty cell appears. The\nanalyzer's <code>REACTOR_GRID_001</code> (when enabled) flags the common\n\"unused column\" symptom.</p>\n<h3 id=\"mixing-flexpanel-and-stack-on-the-same-axis\">Mixing FlexPanel and Stack on the same axis<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mixing-flexpanel-and-stack-on-the-same-axis\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nFlexPanel(\n    HStack(<span class=\"hljs-number\">8</span>, leftA, leftB),    <span class=\"hljs-comment\">// Stack inside Flex on the same axis</span>\n    HStack(<span class=\"hljs-number\">8</span>, rightA, rightB)\n).FlexDirection(FlexDirection.Row)\n</code></pre>\n<p><code>FlexPanel</code> and <code>Stack</code> both lay out along an axis, but <code>FlexPanel</code>\napplies CSS-Flexbox semantics (<code>flex-grow</code>, <code>flex-shrink</code>, <code>flex-basis</code>)\nand <code>Stack</code> applies a simpler \"give every child its natural size with\nfixed gaps\" rule. Mixing them on the same axis means the outer\n<code>FlexPanel</code>'s grow/shrink calculations operate on opaque <code>Stack</code>\nmeasurements — predictable enough for trivial cases, surprising for\nanything dynamic. Pick one per axis. See\n<a href=\"/flex-layout/\">flex-layout</a> for when each shape is right.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start with VStack.</strong> Most screens are a vertical stack of sections.\nAdd <code>HStack</code> for side-by-side content within those sections; reach for\n<code>Grid</code> when two items need to align across rows or columns.</p>\n<p><strong>Use Grid for forms.</strong> A two-column grid with\n<code>[GridSize.Auto, GridSize.Star()]</code> columns gives you aligned labels on\nthe left and stretching inputs on the right — see the\n<a href=\"#common-mistakes\">do-grid-for-forms snippet</a>.</p>\n<p><strong>Wrap long content in ScrollView; use VirtualList past ~500 items.</strong>\nA <code>ScrollView(VStack(...))</code> lays out every child even when off-screen.\n<a href=\"/collections/\"><code>VirtualList</code></a> materializes only the visible window.</p>\n<p><strong>Prefer spacing over margin.</strong> <code>VStack(12, ...)</code> gives consistent\nspacing between children. Adding <code>.Margin(12)</code> to each child\nindividually is harder to maintain and creates double-spacing at\nboundaries.</p>\n<p><strong>Use Card over hand-rolled Border for grouping.</strong> <code>Card(content)</code> is\n8px corner radius, 16px padding, theme-aware background, and a 1px\nstroke — the canonical WinUI surface. Reach for <code>Border</code> only when you\nneed a non-card chrome (a colored accent, a single-side border, a\nnon-standard radius).</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Previous: <code>UseState</code>, <code>UseMemo</code>, and the rest of the hook surface</li>\n<li><strong><a href=\"/flex-layout/\">Flex Layout</a></strong> — Next: <code>FlexPanel</code> for grow/shrink/basis and multi-line wrap with gaps</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Apply colors, typography, and themes to your layouts</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — <code>VirtualList</code> for large data sets that overflow <code>ScrollView</code></li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — Build data entry forms with text fields, checkboxes, and validation</li>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — Layout-cost overlay surfaces double-measure passes and other layout hot spots</li>\n<li><strong><a href=\"/modifier-system/\">Modifier System</a></strong> — How <code>.Width()</code>, <code>.Margin()</code>, and friends chain internally</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Layout . Every Microsoft.UI.Reactor (Reactor) layout is a composition of single-responsibility panels — you pick by direction ( HStack for a row, VStack for a column), wrap behavior ( WrapGrid when items should flow into new rows), or grid axes ( Grid when you want explicit rows × columns). The panels do not overlap each other's job: a Stack is for one axis, a Grid is for two, a Canvas is for absolute coordinates, and a FlexPanel (see flex-layout ) is for the CSS-Flexbox feature set including flex-grow , flex-shrink , and flex-basis . The reason this matters: the right panel keeps the layout cost linear in the child count. The wrong panel — say, a VStack of HStack s where a Grid would do — forces every nested measure-and-arrange pass through multiple panels' code paths, which both costs more and reads worse. Start with VStack for a screen's top-level scaffold; switch to Grid the moment you have row-and-column alignment; switch to FlexPanel when you need ratio-based sizing or multi-line wrapping with gaps. Layout # Reactor provides a small set of layout primitives that compose to build any screen structure. Every layout element accepts children and optional spacing. Decision table # Shape Panel Why Items in one direction VStack(spacing, children) / HStack(spacing, children) Single-axis stacking with built-in spacing. Items in rows × columns Grid(columns, rows, children) Two-axis with explicit track sizing ( Auto , Star , Px ). Items that should wrap WrapGrid(maxRowsOrColumns, children) Auto-flow after a row fills. Items that need ratio sizing or multi-line wrap with gaps FlexPanel(...) (see flex-layout ) CSS-Flexbox semantics: grow/shrink/basis. Absolute positioning Canvas(children) Free-form Canvas.SetLeft / SetTop . Visual container Border(child) / Card(child) Background, corner radius, padding around a single child. Overflow handling ScrollView(child) Wraps anything that may exceed its allotted space. Collapsible group Expander(header, content) Header that toggles its content panel. VStack and HStack # The two most common layouts. VStack stacks children vertically, HStack horizontally. The first argument is the pixel spacing between children: class StackDemo : Component { public override Element Render () { return VStack( 16 , SubHeading( \"VStack and HStack\" ), VStack( 4 , TextBlock( \"VStack: items top to bottom\" ), TextBlock( \"Item A\" ), TextBlock( \"Item B\" ), TextBlock( \"Item C\" ) ), HStack( 8 , TextBlock( \"HStack:\" ), Button( \"One\" ), Button( \"Two\" ), Button( \"Three\" ) ) ); } } VStack(16, ...) puts 16 pixels between each child, top to bottom. HStack(8, ...) puts 8 pixels between each child, left to right. Omit the spacing argument for zero spacing: VStack(child1, child2) . Nest them freely — an HStack inside a VStack, VStacks inside HStacks, as deep as you need. But the moment you find yourself nesting more than two deep, reach for Grid instead (see Common Mistakes for why). Grid # For row-and-column layouts, use Grid . Define columns and rows with GridSize helpers, then place children with the .Grid() modifier: class GridDemo : Component { public override Element Render () { return VStack( 8 , SubHeading( \"Grid\" ), Grid( columns: [GridSize.Px( 120 ), GridSize.Star(), GridSize.Auto], rows: [GridSize.Auto, GridSize.Auto], TextBlock( \"Label\" ).Bold().Grid(row: 0 , column: 0 ), TextBox( \"\" , _ => { }, placeholderText: \"Input...\" ) .Grid(row: 0 , column: 1 ), Button( \"Go\" ).Grid(row: 0 , column: 2 ), TextBlock( \"Status\" ).Grid(row: 1 , column: 0 ), TextBlock( \"Ready\" ).Foreground( \"#0078D4\" ) .Grid(row: 1 , column: 1 , columnSpan: 2 ) ).Height( 80 ) ); } } Helper Meaning GridSize.Px(200) Fixed 200 pixels GridSize.Star() Proportional — fills available space (weight 1) GridSize.Star(2) Proportional — twice the space of GridSize.Star() GridSize.Auto Sizes to content Place children with .Grid(row: 0, column: 1) . Use columnSpan or rowSpan to span multiple cells: .Grid(row: 1, column: 0, columnSpan: 2) . The older Grid([\"120\", \"1*\", \"Auto\"], ...) string-form overload still works but is soft-deprecated ( CS0618 ). Prefer the typed helpers — they catch typos at compile time. Card # Card(child) wraps any element in the canonical WinUI card chrome — 8px corner radius, 16px padding, theme-aware background and 1px stroke. Use it instead of hand-rolling a Border(...).Background(Theme.CardBackground).WithBorder(...) pipeline: class CardDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"Card\" ), Card( VStack( 8 , TextBlock( \"Recent activity\" ).SemiBold(), TextBlock( \"3 new messages, 2 mentions\" ) .Foreground(Theme.SecondaryText) ) ).Width( 240 ) ); } } Override any preset by chaining further fluents: Card(child).Padding(24).CornerRadius(16) . The background and stroke resolve through ThemeRef , so light/dark/contrast switches re-paint without code changes — see styling . Type-Ramp Factories # For headings and body copy that match the WinUI 3 type ramp, use the named factories instead of repeating .FontSize(...).Bold() chains: class TypeRampDemo : Component { public override Element Render () { return VStack( 8 , Title( \"Quarterly results\" ), Subtitle( \"Q3 2026 highlights\" ), BodyLarge( \"Revenue grew 18% year over year.\" ), BodyStrong( \"Net income reached an all-time high.\" ), Body( \"Full breakdown on the following pages.\" ) ).Padding( 24 ); } } Factory WinUI style Title(text) TitleTextBlockStyle — 28px Semibold Subtitle(text) SubtitleTextBlockStyle — 20px Semibold BodyLarge(text) BodyLargeTextBlockStyle — 18px regular BodyStrong(text) BodyStrongTextBlockStyle — 14px Semibold Body(text) BodyTextBlockStyle — 14px regular These are factories (not fluents) by design — see spec 039 §17.6. Layer further modifiers on the result ( Title(\"…\").Foreground(Theme.Accent) ) when you need to deviate. ScrollView and Border # ScrollView wraps content that might overflow. Border adds a visual container with background, corner radius, and padding: class ScrollBorderDemo : Component { public override Element Render () { return VStack( 8 , SubHeading( \"ScrollView and Border\" ), Border( ScrollView( VStack( 4 , ForEach( Enumerable.Range( 1 , 20 ), i => TextBlock( $\"Scrollable item {i} \" )) ).Padding( 8 ) ).Height( 120 ) ).CornerRadius( 4 ).Background( \"#F5F5F5\" ) ); } } ScrollView takes a single child element. Wrap a VStack to scroll a vertical list. Border is purely visual — it renders a rounded rectangle behind its child and is useful for cards, panels, and grouping. For long lists, prefer VirtualList over ScrollView(VStack(...)) . A ScrollView lays out every child even when off-screen, which is fast for ≤100 items and visibly slow above ~500; VirtualList materializes only the items in view. Expander and Canvas # Expander shows a header and collapses/expands its content on click. Canvas positions children at absolute coordinates: class ExpanderCanvasDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"Expander\" ), Expander( \"Click to expand\" , VStack( 8 , TextBlock( \"Hidden content revealed!\" ), TextBlock( \"Expanders are great for optional details.\" ) )), SubHeading( \"Canvas\" ), Border( Canvas( TextBlock( \"Top-left\" ).Set(c => { Microsoft.UI.Xaml.Controls.Canvas.SetLeft((UIElement)c, 10 ); Microsoft.UI.Xaml.Controls.Canvas.SetTop((UIElement)c, 10 ); }), TextBlock( \"Center\" ).Set(c => { Microsoft.UI.Xaml.Controls.Canvas.SetLeft((UIElement)c, 120 ); Microsoft.UI.Xaml.Controls.Canvas.SetTop((UIElement)c, 40 ); }) ).Height( 90 ).Width( 300 ) ).Background( \"#F5F5F5\" ).CornerRadius( 4 ) ); } } Expander(header, content) handles the expand/collapse state internally. Pass isExpanded: true to start expanded, or use onExpandedChanged to track the state yourself. Canvas positions children using the .Set() modifier to call Canvas.SetLeft and Canvas.SetTop . Use it for overlays, diagrams, or any layout that doesn't follow a stack or grid pattern. Responsive Layout # Switch between layouts based on window width. Use a state toggle or UseBreakpoint to swap between HStack and VStack: class ResponsiveDemo : Component { public override Element Render () { var (wide, setWide) = UseState( true ); var content = new Element[] { Border(TextBlock( \"Panel A\" ).Padding( 16 )) .Background( \"#E3F2FD\" ).CornerRadius( 4 ), Border(TextBlock( \"Panel B\" ).Padding( 16 )) .Background( \"#FFF3E0\" ).CornerRadius( 4 ), Border(TextBlock( \"Panel C\" ).Padding( 16 )) .Background( \"#E8F5E9\" ).CornerRadius( 4 ), }; return VStack( 8 , SubHeading( \"Responsive Layout\" ), HStack( 8 , TextBlock( \"Simulate wide screen:\" ), ToggleSwitch(wide, setWide) ), If(wide, () => HStack( 12 , content), () => VStack( 8 , content)) ); } } For real responsive layouts, use UseBreakpoint (window, minWidth) which returns true when the window is at least minWidth pixels wide. Pair it with If() to swap layouts: var wide = UseBreakpoint(window, 800 ); return If(wide, () => HStack( 12 , panelA, panelB), () => VStack( 8 , panelA, panelB)); UseWindowSize(window) returns (Width, Height) if you need exact dimensions for more granular control. Alignment and Sizing # Every element supports sizing and alignment modifiers: Text( \"Centered\" ).HAlign(HorizontalAlignment.Center) Text( \"Fixed width\" ).Width( 200 ).Height( 40 ) VStack( 8 , items).Margin( 24 ).Padding( 16 ) Modifier Effect .Width(n) Fixed width in pixels .Height(n) Fixed height in pixels .Margin(n) Outer spacing (all sides) .Padding(n) Inner spacing (all sides) .HAlign(alignment) Horizontal alignment (Left, Center, Right, Stretch) .VAlign(alignment) Vertical alignment (Top, Center, Bottom, Stretch) See modifier-system for how these chain internally. Caveat: Grid panels with * (star) sizing measure their children twice on the first layout pass — once with Available = Infinity to determine the natural content size, and once with Available = ActualWidth to resolve the star tracks. The double pass is fast for ≤100 children and visible above ~500 (the layout-cost overlay reports the extra measure-pass cost — see dev-tooling ). For large lists rendered into a star-sized cell, wrap the content in VirtualList or LazyVStack so the panel only materializes the visible window; the host star track measures the viewport size once. Fixed ( Px ) and Auto tracks do not pay the double-measure cost. Patterns # App-shell scaffold # The canonical admin-style app shell — title bar, sidebar, main content, status bar. One Grid declaration; one measure-and-arrange pass: // App shell scaffold: title bar + sidebar + content using a single Grid // declaration. The two-column / three-row layout is the canonical // shape for an admin-style app and replaces hand-rolled nested // HStacks/VStacks. class AppShellExample : Component { public override Element Render () { return Grid( columns: [GridSize.Px( 220 ), GridSize.Star()], rows: [GridSize.Px( 44 ), GridSize.Star(), GridSize.Px( 28 )], // Top bar — spans both columns. Border(TextBlock( \"My App\" ).Bold().Padding( 12 )) .Background(Theme.LayerFill) .Grid(row: 0 , column: 0 , columnSpan: 2 ), // Sidebar. Border(VStack( 4 , TextBlock( \"Dashboard\" ), TextBlock( \"Reports\" ), TextBlock( \"Settings\" )) .Padding( 12 )) .Background(Theme.CardBackground) .Grid(row: 1 , column: 0 ), // Main content. ScrollView(VStack( 8 , Title( \"Welcome\" ), Body( \"Stack -> Grid for the shell, VStack inside the panes.\" ))) .Padding( 16 ) .Grid(row: 1 , column: 1 ), // Status bar. TextBlock( \"Ready\" ).Padding( 6 ) .Grid(row: 2 , column: 0 , columnSpan: 2 ) ).Width( 560 ).Height( 360 ); } } A naive version with nested HStack / VStack panels works but couples the rows to the column widths — resizing the sidebar requires editing every row that intersects it. The Grid keeps the axes independent. Auto-grid # A photo gallery, a tile dashboard, a chip cloud — anything that should flow into rows as the container width allows — uses WrapGrid : // Auto-grid: WrapGrid wraps a sequence of items into columns once a // row fills, no manual row/column placement. Picks \"max 4 per row\". class AutoGridExample : Component { public override Element Render () { return WrapGrid(maxRowsOrColumns: 4 , children: Enumerable.Range( 1 , 11 ) .Select(i => Border(TextBlock( $\"Tile {i} \" ).Padding( 12 )) .Background(Theme.CardBackground) .CornerRadius( 6 ) .Width( 110 ).Height( 60 )) .Cast&#x3C;Element?>() .ToArray() ).Padding( 24 ); } } maxRowsOrColumns caps the major axis; the panel wraps once that count is reached. For content that should wrap by available pixel width (rather than item count), use FlexPanel with FlexWrap.Wrap . Responsive switcher # The minimum responsive recipe — different shape at narrow vs. wide: // Responsive switcher: the same content renders as HStack at wide // widths, VStack when the window narrows past 480px. UseBreakpoint is // the canonical hook for this pattern. class ResponsiveSwitcherExample : Component { public override Element Render () { var (simulated, setSimulated) = UseState( true ); // toy demo without a window handle var panels = new Element[] { Border(TextBlock( \"Panel A\" ).Padding( 16 )) .Background(Theme.CardBackground).CornerRadius( 4 ), Border(TextBlock( \"Panel B\" ).Padding( 16 )) .Background(Theme.CardBackground).CornerRadius( 4 ), Border(TextBlock( \"Panel C\" ).Padding( 16 )) .Background(Theme.CardBackground).CornerRadius( 4 ), }; return VStack( 8 , HStack( 8 , TextBlock( \"Simulate wide window:\" ), ToggleSwitch(simulated, setSimulated)), If(simulated, () => HStack( 12 , panels), () => VStack( 8 , panels)) ).Padding( 24 ); } } The same panels array drops into either container. The real production version reads UseBreakpoint(window, 480) rather than a manual toggle — see the Responsive Layout section above. Common Mistakes # Nesting deep stack hierarchies where Grid would suffice # // BAD — deeply nested VStack/HStack hierarchies obscure intent and // produce a layout cost that Grid would absorb in one measure pass. class DontDeepStack : Component { public override Element Render () { return HStack( 8 , VStack( 4 , TextBlock( \"Label\" ).Bold(), TextBlock( \"Sublabel\" ).Foreground(Theme.SecondaryText)), VStack( 0 , HStack( 4 , TextBlock( \"First:\" ), TextBlock( \"Value 1\" )), HStack( 4 , TextBlock( \"Second:\" ), TextBlock( \"Value 2\" )), HStack( 4 , TextBlock( \"Third:\" ), TextBlock( \"Value 3\" ))) ).Padding( 24 ); } } Three levels of HStack / VStack to lay out a label-value list is harder to read than the row × column shape it really is. The same shape as a Grid : // GOOD — same shape as a 2-column Grid. Labels in column 0 are auto- // sized to their content; column 1 stretches. class DoGridForForms : Component { public override Element Render () { return Grid( columns: [GridSize.Auto, GridSize.Star()], rows: [GridSize.Auto, GridSize.Auto, GridSize.Auto], TextBlock( \"First:\" ).Margin( 4 ).Grid(row: 0 , column: 0 ), TextBlock( \"Value 1\" ).Margin( 4 ).Grid(row: 0 , column: 1 ), TextBlock( \"Second:\" ).Margin( 4 ).Grid(row: 1 , column: 0 ), TextBlock( \"Value 2\" ).Margin( 4 ).Grid(row: 1 , column: 1 ), TextBlock( \"Third:\" ).Margin( 4 ).Grid(row: 2 , column: 0 ), TextBlock( \"Value 3\" ).Margin( 4 ).Grid(row: 2 , column: 1 ) ).Padding( 24 ).Width( 300 ); } } The Auto column hugs the label width; the Star column stretches. Adding a row is one declaration. The nested-Stack version requires re-indenting every sibling. As a rule: if any two items in your layout need to align across rows or columns, use Grid . Missing * sizing in templates that should fill # // Don't: Grid( columns: [GridSize.Px( 220 ), GridSize.Auto], // content column is auto-sized rows: [GridSize.Star()], Sidebar(), Content()) // Content shrinks to its intrinsic width Auto is \"measure my content and use that\"; Star is \"take what's left\". A main-content cell that should fill the remaining horizontal space must declare GridSize.Star() — otherwise it shrinks to its intrinsic size and the surrounding empty cell appears. The analyzer's REACTOR_GRID_001 (when enabled) flags the common \"unused column\" symptom. Mixing FlexPanel and Stack on the same axis # // Don't: FlexPanel( HStack( 8 , leftA, leftB), // Stack inside Flex on the same axis HStack( 8 , rightA, rightB) ).FlexDirection(FlexDirection.Row) FlexPanel and Stack both lay out along an axis, but FlexPanel applies CSS-Flexbox semantics ( flex-grow , flex-shrink , flex-basis ) and Stack applies a simpler \"give every child its natural size with fixed gaps\" rule. Mixing them on the same axis means the outer FlexPanel 's grow/shrink calculations operate on opaque Stack measurements — predictable enough for trivial cases, surprising for anything dynamic. Pick one per axis. See flex-layout for when each shape is right. Tips # Start with VStack. Most screens are a vertical stack of sections. Add HStack for side-by-side content within those sections; reach for Grid when two items need to align across rows or columns. Use Grid for forms. A two-column grid with [GridSize.Auto, GridSize.Star()] columns gives you aligned labels on the left and stretching inputs on the right — see the do-grid-for-forms snippet . Wrap long content in ScrollView; use VirtualList past ~500 items. A ScrollView(VStack(...)) lays out every child even when off-screen. VirtualList materializes only the visible window. Prefer spacing over margin. VStack(12, ...) gives consistent spacing between children. Adding .Margin(12) to each child individually is harder to maintain and creates double-spacing at boundaries. Use Card over hand-rolled Border for grouping. Card(content) is 8px corner radius, 16px padding, theme-aware background, and a 1px stroke — the canonical WinUI surface. Reach for Border only when you need a non-card chrome (a colored accent, a single-side border, a non-standard radius). Next Steps # Hooks — Previous: UseState , UseMemo , and the rest of the hook surface Flex Layout — Next: FlexPanel for grow/shrink/basis and multi-line wrap with gaps Styling and Theming — Apply colors, typography, and themes to your layouts Collections — VirtualList for large data sets that overflow ScrollView Forms and Input — Build data entry forms with text fields, checkboxes, and validation Dev Tooling — Layout-cost overlay surfaces double-measure passes and other layout hot spots Modifier System — How .Width() , .Margin() , and friends chain internally",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/layout.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/layout.md",
      "headings": [
        {
          "depth": 2,
          "id": "decision-table",
          "text": "Decision table"
        },
        {
          "depth": 2,
          "id": "vstack-and-hstack",
          "text": "VStack and HStack"
        },
        {
          "depth": 2,
          "id": "grid",
          "text": "Grid"
        },
        {
          "depth": 2,
          "id": "card",
          "text": "Card"
        },
        {
          "depth": 2,
          "id": "type-ramp-factories",
          "text": "Type-Ramp Factories"
        },
        {
          "depth": 2,
          "id": "scrollview-and-border",
          "text": "ScrollView and Border"
        },
        {
          "depth": 2,
          "id": "expander-and-canvas",
          "text": "Expander and Canvas"
        },
        {
          "depth": 2,
          "id": "responsive-layout",
          "text": "Responsive Layout"
        },
        {
          "depth": 2,
          "id": "alignment-and-sizing",
          "text": "Alignment and Sizing"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "app-shell-scaffold",
          "text": "App-shell scaffold"
        },
        {
          "depth": 3,
          "id": "auto-grid",
          "text": "Auto-grid"
        },
        {
          "depth": 3,
          "id": "responsive-switcher",
          "text": "Responsive switcher"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "nesting-deep-stack-hierarchies-where-grid-would-suffice",
          "text": "Nesting deep stack hierarchies where Grid would suffice"
        },
        {
          "depth": 3,
          "id": "missing--sizing-in-templates-that-should-fill",
          "text": "Missing * sizing in templates that should fill"
        },
        {
          "depth": 3,
          "id": "mixing-flexpanel-and-stack-on-the-same-axis",
          "text": "Mixing FlexPanel and Stack on the same axis"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Flex Layout",
      "path": "flex-layout.md",
      "slug": "/flex-layout/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s flex layout reasons about two axes at once. The main axis distributes\nchildren across available space — that's where <code>JustifyContent</code>,\n<code>Flex(grow:)</code>, <code>Flex(shrink:)</code>, and <code>Flex(basis:)</code> apply. The cross\naxis aligns each child against the container's other dimension — that's\nwhere <code>AlignItems</code> and per-child <code>AlignSelf</code> apply. The same <code>FlexPanel</code>\ncomposes both, and that combination is what unlocks the layouts that\n<code>VStack</code> and <code>HStack</code> cannot express. A toolbar with a flexible spacer,\nan app shell with a fixed sidebar and a fluid content area, a wrap-on-\noverflow tag row — all three are FlexPanels with different\nmain-axis / cross-axis rules. Read this page before reaching for <code>Grid</code>\non a problem that's really \"distribute these items along one axis with\nalignment on the other\" — that's the FlexPanel shape, and the moment a\ngrid feels overkill is the moment Flex is right.</p>\n<h1 id=\"flex-layout\">Flex Layout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#flex-layout\">#</a></h1>\n<p>Reactor's <code>FlexPanel</code> (exposed through the <a href=\"#direction\"><code>FlexRow</code></a> and\n<a href=\"#direction\"><code>FlexColumn</code></a> factories) is a Yoga-backed CSS Flexbox\nimplementation. Reach for it when the layout needs alignment control,\nwrapping, or proportional sizing that <a href=\"/layout/\"><code>VStack</code> and <code>HStack</code></a>\ncannot express. Add <code>using Microsoft.UI.Reactor.Layout;</code> (or import\n<code>FlexDirection</code>, <code>FlexJustify</code>, <code>FlexAlign</code>, <code>FlexWrap</code>) to access the\nenum types.</p>\n<h2 id=\"at-a-glance-reference\">At-a-glance reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#at-a-glance-reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Property</th><th>Set on</th><th>Effect</th></tr></thead><tbody><tr><td><code>JustifyContent</code></td><td>container (<code>with { ... }</code>)</td><td>Main-axis distribution: <code>FlexStart</code> / <code>Center</code> / <code>FlexEnd</code> / <code>SpaceBetween</code> / <code>SpaceAround</code> / <code>SpaceEvenly</code></td></tr><tr><td><code>AlignItems</code></td><td>container (<code>with { ... }</code>)</td><td>Cross-axis alignment: <code>Stretch</code> / <code>FlexStart</code> / <code>Center</code> / <code>FlexEnd</code> / <code>Baseline</code></td></tr><tr><td><code>AlignContent</code></td><td>container (<code>with { ... }</code>)</td><td>Cross-axis alignment of <strong>wrapped lines</strong> — only matters when <code>Wrap</code> is on and lines have leftover space</td></tr><tr><td><code>Wrap</code></td><td>container (<code>with { ... }</code>)</td><td><code>NoWrap</code> (single-line, overflow) / <code>Wrap</code> / <code>WrapReverse</code></td></tr><tr><td><code>ColumnGap</code> / <code>RowGap</code></td><td>container (<code>with { ... }</code>)</td><td>Spacing between children — applies between wrapped lines too</td></tr><tr><td><code>FlexPadding</code></td><td>container (<code>with { ... }</code>)</td><td>Inside-container padding measured by Yoga (distinct from the <code>.Padding(...)</code> modifier)</td></tr><tr><td><code>.Flex(grow:, shrink:, basis:)</code></td><td>child</td><td>Main-axis sizing</td></tr><tr><td><code>.Flex(minWidth:, minHeight:)</code></td><td>child</td><td>Override CSS §4.5 auto-min on the main axis (<code>null</code> = CSS <code>auto</code>; <code>0</code> = no floor; positive = hard floor). See the <a href=\"#min-sizing-css-45\">Min sizing</a> table for how <code>auto</code> resolves.</td></tr><tr><td><code>.Flex(alignSelf:)</code></td><td>child</td><td>Override the container's <code>AlignItems</code> for one child</td></tr></tbody></table>\n<h2 id=\"direction\">Direction<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#direction\">#</a></h2>\n<p><code>FlexRow</code> lays children along the horizontal main axis; <code>FlexColumn</code>\nlays them along the vertical main axis. The cross axis is whichever\ndirection is left. Set spacing through <code>ColumnGap</code> (between row\nchildren) or <code>RowGap</code> (between column children):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FlexDirectionDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"Row (default)\"</span>),\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"A\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"B\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"C\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>)\n            ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> },\n\n            SubHeading(<span class=\"hljs-string\">\"Column\"</span>),\n            FlexColumn(\n                TextBlock(<span class=\"hljs-string\">\"A\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"B\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"C\"</span>).Padding(<span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>)\n            ) <span class=\"hljs-keyword\">with</span> { RowGap = <span class=\"hljs-number\">8</span> }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/flex-direction.png\" alt=\"Row and column direction\"></p>\n<p>A <code>FlexRow</code> looks like <a href=\"/layout/\"><code>HStack</code></a> at first glance, but a\nFlexPanel additionally exposes justification, cross-axis alignment,\nwrapping, and proportional sizing — <code>HStack</code> does none of those.</p>\n<h2 id=\"justify-and-align\">Justify and Align<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#justify-and-align\">#</a></h2>\n<p><code>JustifyContent</code> distributes children along the <strong>main</strong> axis;\n<code>AlignItems</code> aligns them on the <strong>cross</strong> axis. These are independent\nknobs on the same container — read them as two separate sentences:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">JustifyAlignDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"JustifyContent: SpaceBetween\"</span>),\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"Left\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Center\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Right\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>)\n            ) <span class=\"hljs-keyword\">with</span> { JustifyContent = FlexJustify.SpaceBetween },\n\n            SubHeading(<span class=\"hljs-string\">\"AlignItems: Center\"</span>),\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"Short\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Tall\\nItem\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Med\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>)\n            ) <span class=\"hljs-keyword\">with</span> {\n                AlignItems = FlexAlign.Center,\n                ColumnGap = <span class=\"hljs-number\">8</span>\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>).Height(<span class=\"hljs-number\">300</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/justify-align.png\" alt=\"Justify and align demo\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th><code>JustifyContent</code></th><th>Effect (main axis)</th></tr></thead><tbody><tr><td><code>FlexStart</code></td><td>Pack to start (default)</td></tr><tr><td><code>Center</code></td><td>Center along main axis</td></tr><tr><td><code>FlexEnd</code></td><td>Pack to end</td></tr><tr><td><code>SpaceBetween</code></td><td>Equal space between items, none at edges</td></tr><tr><td><code>SpaceAround</code></td><td>Equal space around items</td></tr><tr><td><code>SpaceEvenly</code></td><td>Equal space between and at edges</td></tr></tbody></table>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th><code>AlignItems</code></th><th>Effect (cross axis)</th></tr></thead><tbody><tr><td><code>Stretch</code></td><td>Fill cross axis (default)</td></tr><tr><td><code>FlexStart</code></td><td>Align to cross-axis start</td></tr><tr><td><code>Center</code></td><td>Center on cross axis</td></tr><tr><td><code>FlexEnd</code></td><td>Align to cross-axis end</td></tr><tr><td><code>Baseline</code></td><td>Align text baselines</td></tr></tbody></table>\n<h2 id=\"wrap-and-gap\">Wrap and gap<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#wrap-and-gap\">#</a></h2>\n<p>When children would overflow the container on the main axis, set\n<code>Wrap = FlexWrap.Wrap</code> to flow them onto a second line. <code>ColumnGap</code> and\n<code>RowGap</code> apply consistently — between children on a line <strong>and</strong> between\nwrapped lines:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WrapGapDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> tags = <span class=\"hljs-keyword\">new</span>[] {\n            <span class=\"hljs-string\">\"C#\"</span>, <span class=\"hljs-string\">\"WinUI\"</span>, <span class=\"hljs-string\">\"Reactor\"</span>, <span class=\"hljs-string\">\".NET\"</span>, <span class=\"hljs-string\">\"XAML\"</span>,\n            <span class=\"hljs-string\">\"Flex\"</span>, <span class=\"hljs-string\">\"Layout\"</span>, <span class=\"hljs-string\">\"Desktop\"</span>, <span class=\"hljs-string\">\"Native\"</span>\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Wrapping Tags\"</span>),\n            FlexRow(\n                tags.Select(tag =>\n                    TextBlock(tag)\n                        .Padding(horizontal: <span class=\"hljs-number\">6</span>, vertical: <span class=\"hljs-number\">12</span>)\n                        .Background(<span class=\"hljs-string\">\"#e8e8e8\"</span>)\n                        .CornerRadius(<span class=\"hljs-number\">12</span>)\n                ).ToArray()\n            ) <span class=\"hljs-keyword\">with</span> {\n                Wrap = FlexWrap.Wrap,\n                ColumnGap = <span class=\"hljs-number\">8</span>,\n                RowGap = <span class=\"hljs-number\">8</span>\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/wrap-gap.png\" alt=\"Wrapping tags with gap\"></p>\n<p>Tag clouds, chip rows, breadcrumb overflow, filter pills — anywhere the\nitem count varies and the container width can shrink — are wrap-target\nshapes. For rendering long item collections with virtualization, see\n<a href=\"/collections/\">Collections</a>.</p>\n<h2 id=\"grow-shrink-basis\">Grow, shrink, basis<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#grow-shrink-basis\">#</a></h2>\n<p><code>.Flex(grow:, shrink:, basis:)</code> on a child controls how that child\nshares main-axis space with its siblings:</p>\n<ul>\n<li><strong><code>grow</code></strong> — share of the <strong>extra</strong> space the child claims after all\nbasis sizes are placed (default <code>0</code> — the child holds its content\nsize).</li>\n<li><strong><code>shrink</code></strong> — share of the <strong>deficit</strong> the child gives up when the\ntotal basis exceeds the container (default <code>1</code> — the child shrinks\nproportionally).</li>\n<li><strong><code>basis</code></strong> — the starting size in pixels before grow/shrink apply.\n<code>null</code> means \"measure my content first\" (Yoga's <code>auto</code>).</li>\n<li><strong><code>minWidth</code> / <code>minHeight</code></strong> — explicit floor for the child's main-axis\nsize. <code>null</code> (default) means <em>auto</em> — see \"Min sizing (CSS §4.5)\"\nbelow.</li>\n</ul>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GrowShrinkDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"Grow: sidebar + content\"</span>),\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"Sidebar\"</span>)\n                    .Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>)\n                    .Flex(basis: <span class=\"hljs-number\">200</span>, shrink: <span class=\"hljs-number\">0</span>),\n                TextBlock(<span class=\"hljs-string\">\"Main content area\"</span>)\n                    .Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#f0f0f0\"</span>)\n                    .Flex(grow: <span class=\"hljs-number\">1</span>)\n            ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> },\n\n            SubHeading(<span class=\"hljs-string\">\"Equal columns\"</span>),\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"Column 1\"</span>).Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>).Flex(grow: <span class=\"hljs-number\">1</span>),\n                TextBlock(<span class=\"hljs-string\">\"Column 2\"</span>).Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>).Flex(grow: <span class=\"hljs-number\">1</span>),\n                TextBlock(<span class=\"hljs-string\">\"Column 3\"</span>).Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>).Flex(grow: <span class=\"hljs-number\">1</span>)\n            ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/grow-shrink.png\" alt=\"Grow and shrink layout\"></p>\n<p>The sidebar uses <code>basis: 200, shrink: 0</code> to fix its width; the content\ncolumn uses <code>grow: 1</code> to absorb whatever remains. The equal-columns row\ngives every child <code>grow: 1</code> so the available space splits evenly.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Flex(grow: 0)</code> + <code>Flex(shrink: 1)</code> with no <code>basis</code> defaults to\n<code>basis: auto</code> — Yoga measures the child's content size in a first pass,\nthen runs distribution in a second. For a row of 200 list cells, that\nfirst measurement pass dominates layout cost: the panel asks each cell\nfor its preferred size before doing any distribution. Setting an\nexplicit <code>basis: 0</code> (or any pixel value) collapses the two passes into\none: distribution alone, no content measurement. The difference is\ninvisible on a 5-button toolbar and load-bearing on a 200-row table\nheader that rebuilds on every column resize.</p>\n</blockquote>\n<h2 id=\"min-sizing-css-45\">Min sizing (CSS §4.5)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#min-sizing-css-45\">#</a></h2>\n<p>By default, a flex child with <code>basis: auto</code> will not shrink below its\n<strong>min-content</strong> size — the smallest size it can occupy without\noverflowing its own content. This matches the <a href=\"https://www.w3.org/TR/css-flexbox-1/#min-size-auto\">CSS Flexbox\nspecification §4.5</a>\n(\"Automatic Minimum Size of Flex Items\") and is what most developers\nintuitively expect when laying out a row of cards or a column of\nsections: items don't squish smaller than their text or icons.</p>\n<p>Reactor's FlexPanel implements the auto-min rule as follows:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>basis</th><th>minWidth / minHeight</th><th>Resulting floor</th></tr></thead><tbody><tr><td><code>auto</code> (default)</td><td><code>null</code> (default)</td><td><code>min-content</code></td></tr><tr><td><code>0</code></td><td><code>null</code></td><td><code>0</code> (short-circuit — basis already opts out of content)</td></tr><tr><td>pixel value <code>N > 0</code></td><td><code>null</code></td><td><code>min(N, min-content)</code></td></tr><tr><td>any</td><td>explicit <code>0</code></td><td><code>0</code> (you opt out of the floor)</td></tr><tr><td>any</td><td>explicit <code>N > 0</code></td><td><code>N</code> (your hard floor)</td></tr></tbody></table>\n<p>The auto-min rule applies <strong>only to the main axis</strong> (width for\n<code>Direction.Row</code>, height for <code>Direction.Column</code>). On the cross axis,\nitems shrink freely to 0 unless you set <code>minWidth</code>/<code>minHeight</code>\nexplicitly.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Default behavior — items keep their min-content size:</span>\nFlexPanel(\n    Border(<span class=\"hljs-string\">\"Long text that won't truncate\"</span>).Flex(shrink: <span class=\"hljs-number\">1</span>),\n    Border(<span class=\"hljs-string\">\"Short\"</span>).Flex(shrink: <span class=\"hljs-number\">1</span>),\n).Direction(FlexDirection.Row);\n\n<span class=\"hljs-comment\">// Opt out — let items shrink below content (text will clip):</span>\nFlexPanel(\n    Border(<span class=\"hljs-string\">\"Long text that may be clipped\"</span>).Flex(shrink: <span class=\"hljs-number\">1</span>, minWidth: <span class=\"hljs-number\">0</span>),\n    Border(<span class=\"hljs-string\">\"Short\"</span>).Flex(shrink: <span class=\"hljs-number\">1</span>, minWidth: <span class=\"hljs-number\">0</span>),\n).Direction(FlexDirection.Row);\n\n<span class=\"hljs-comment\">// Explicit floor — never below 80px regardless of content:</span>\nBorder(content).Flex(shrink: <span class=\"hljs-number\">1</span>, minWidth: <span class=\"hljs-number\">80</span>)\n</code></pre>\n<blockquote>\n<p><strong>Caveat:</strong> <strong>Performance:</strong> computing <code>min-content</code> requires a WinUI\n<code>Measure(width: marginH, height: ∞)</code> pass on each affected child before\nthe main flex algorithm runs. This is fine for typical UIs but can\nmatter for very large lists. Two ways to skip the pre-measure:</p>\n<ul>\n<li>Set <code>basis: 0</code> explicitly — the auto-min collapses to 0 with no\nmeasurement.</li>\n<li>Set <code>minWidth: 0</code> (or <code>minHeight: 0</code>) explicitly — you've opted out\nof the floor.</li>\n</ul>\n<p>Children that are <code>ScrollView</code> or <code>ScrollViewer</code> always get a <code>0</code>\nfloor: their min-content would force their virtualized contents to\nrealize, which defeats the point of virtualization.</p>\n</blockquote>\n<blockquote>\n<p><strong>Caveat:</strong> <strong>CSS vs WinUI mismatch on explicit <code>Width</code>.</strong> In CSS, an item with\n<code>width: 200px; flex-shrink: 1; min-width: 0</code> is allowed to shrink\nbelow 200. In WinUI, <code>FrameworkElement.Width = 200</code> is a hard size\nconstraint — the control will report <code>DesiredSize.Width = 200</code>\nregardless of available space and will arrange at 200 inside its\nallocated slot. Inside a FlexPanel, prefer <code>.Flex(basis: 200, ...)</code>\nover <code>.Width(200)</code> for any item you want to shrink. Yoga reads the\nbasis; WinUI's <code>Width</code> doesn't participate in flex distribution the\nway CSS's <code>width</code> does.</p>\n</blockquote>\n<h2 id=\"practical-example-toolbar-with-flexible-spacer\">Practical example: toolbar with flexible spacer<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#practical-example-toolbar-with-flexible-spacer\">#</a></h2>\n<p>A toolbar with a left-aligned title and right-aligned buttons is the\ncanonical flex shape. Use <code>Empty().Flex(grow: 1)</code> as a flexible spacer —\nit absorbs every spare pixel and pushes the buttons to the trailing\nedge:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ToolbarDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (selected, setSelected) = UseState(<span class=\"hljs-string\">\"Home\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n            FlexRow(\n                TextBlock(<span class=\"hljs-string\">\"MyApp\"</span>).Bold().Flex(shrink: <span class=\"hljs-number\">0</span>),\n                Empty().Flex(grow: <span class=\"hljs-number\">1</span>),\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => setSelected(<span class=\"hljs-string\">\"Home\"</span>)),\n                Button(<span class=\"hljs-string\">\"Settings\"</span>, () => setSelected(<span class=\"hljs-string\">\"Settings\"</span>)),\n                Button(<span class=\"hljs-string\">\"About\"</span>, () => setSelected(<span class=\"hljs-string\">\"About\"</span>))\n            ) <span class=\"hljs-keyword\">with</span> {\n                AlignItems = FlexAlign.Center,\n                ColumnGap = <span class=\"hljs-number\">8</span>,\n                FlexPadding = <span class=\"hljs-keyword\">new</span> Thickness(<span class=\"hljs-number\">16</span>, <span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">16</span>, <span class=\"hljs-number\">8</span>)\n            },\n            TextBlock(<span class=\"hljs-string\">$\"Current page: <span class=\"hljs-subst\">{selected}</span>\"</span>)\n                .Padding(<span class=\"hljs-number\">24</span>).FontSize(<span class=\"hljs-number\">18</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/toolbar.png\" alt=\"Toolbar layout\"></p>\n<p>The spacer pattern composes — three spacers between three buttons\ndistribute them evenly without <code>SpaceBetween</code> on the container, which\nmatters when only some children should flex.</p>\n<h2 id=\"flex-vs-vstackhstack-vs-grid\">Flex vs VStack/HStack vs Grid<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#flex-vs-vstackhstack-vs-grid\">#</a></h2>\n<p>Pick a container by what its shape promises, not by what looks\nexpedient:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FlexVsStackDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"HStack (fixed spacing)\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"A\"</span>), Button(<span class=\"hljs-string\">\"B\"</span>), Button(<span class=\"hljs-string\">\"C\"</span>)\n            ),\n\n            SubHeading(<span class=\"hljs-string\">\"FlexRow (justify + align)\"</span>),\n            FlexRow(\n                Button(<span class=\"hljs-string\">\"A\"</span>), Button(<span class=\"hljs-string\">\"B\"</span>), Button(<span class=\"hljs-string\">\"C\"</span>)\n            ) <span class=\"hljs-keyword\">with</span> {\n                JustifyContent = FlexJustify.SpaceEvenly,\n                AlignItems = FlexAlign.Center\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/flex-layout/flex-vs-stack.png\" alt=\"Flex vs stack comparison\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Feature</th><th><code>VStack</code> / <code>HStack</code></th><th><code>FlexRow</code> / <code>FlexColumn</code></th><th><code>Grid</code></th></tr></thead><tbody><tr><td>Fixed inter-child spacing</td><td>yes</td><td>yes (<code>ColumnGap</code> / <code>RowGap</code>)</td><td>yes</td></tr><tr><td>Justify along main axis</td><td>no</td><td>yes</td><td>row/column at a time</td></tr><tr><td>Cross-axis alignment</td><td>no</td><td>yes</td><td><code>VAlign</code> per cell</td></tr><tr><td>Wrap on overflow</td><td>no</td><td>yes</td><td>implicit row/column matrix</td></tr><tr><td>Proportional sizing</td><td>no</td><td>yes (<code>Flex(grow:)</code>)</td><td>yes (<code>GridSize.Star</code>)</td></tr><tr><td>Two-dimensional layout</td><td>no</td><td>no</td><td>yes</td></tr></tbody></table>\n<p>If the layout has a row-and-column structure (a form, a settings grid,\nanything where columns line up across rows), reach for\n<a href=\"/layout/\"><code>Grid</code></a>. If it's \"distribute children along one axis with\nalignment on the other,\" FlexPanel is the right shape.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"app-shell--fixed-sidebar-fluid-content\">App shell — fixed sidebar, fluid content<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#app-shell--fixed-sidebar-fluid-content\">#</a></h3>\n<p>A two-column layout with a fixed-width nav rail and a content pane that\nabsorbs the rest is the most common flex shape in production apps. The\nkey is <code>basis: 220, shrink: 0</code> on the sidebar (locks the width and\nprevents collapse) paired with <code>grow: 1, basis: 0</code> on the content\n(grabs the remainder in a single distribution pass):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AppShellDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> FlexRow(\n            <span class=\"hljs-comment\">// Sidebar — fixed 220px, never shrinks below it.</span>\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Inbox\"</span>).Padding(<span class=\"hljs-number\">8</span>),\n                TextBlock(<span class=\"hljs-string\">\"Drafts\"</span>).Padding(<span class=\"hljs-number\">8</span>),\n                TextBlock(<span class=\"hljs-string\">\"Sent\"</span>).Padding(<span class=\"hljs-number\">8</span>)\n            ).Background(<span class=\"hljs-string\">\"#f3f3f3\"</span>)\n             .Flex(basis: <span class=\"hljs-number\">220</span>, shrink: <span class=\"hljs-number\">0</span>),\n\n            <span class=\"hljs-comment\">// Content — explicit basis: 0 + grow: 1 gives a single distribution</span>\n            <span class=\"hljs-comment\">// pass instead of measuring the inner text first.</span>\n            VStack(<span class=\"hljs-number\">12</span>,\n                Heading(<span class=\"hljs-string\">\"Inbox\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Three messages, one starred. The sidebar stays 220px wide; this column absorbs every spare pixel.\"</span>)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n             .Flex(grow: <span class=\"hljs-number\">1</span>, basis: <span class=\"hljs-number\">0</span>)\n        ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">1</span> };\n    }\n}\n</code></pre>\n<p>The explicit <code>basis: 0</code> on content matches the caveat above — no\ncontent-measurement pass, just distribution. The sidebar's <code>shrink: 0</code>\nkeeps the rail at 220 pixels even when the window narrows below the\ncontent's intrinsic minimum.</p>\n<h3 id=\"responsive-nav-rail-with-wrap-on-overflow\">Responsive nav rail with wrap-on-overflow<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#responsive-nav-rail-with-wrap-on-overflow\">#</a></h3>\n<p>Above a breakpoint, the nav rail is a single row; below the breakpoint,\nitems wrap onto a second line. <code>FlexWrap.Wrap</code> + <code>RowGap</code> + <code>ColumnGap</code>\nis the entire mechanism — no media-query equivalent needed:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ResponsiveNavDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Wrap kicks in when the narrow viewport can no longer fit one row.</span>\n        <span class=\"hljs-comment\">// RowGap and ColumnGap apply between wrapped lines too — no manual margin.</span>\n        <span class=\"hljs-keyword\">return</span> FlexRow(\n            TextBlock(<span class=\"hljs-string\">\"Home\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Catalog\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Pricing\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Docs\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"About\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Contact\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Status\"</span>).Padding(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e0e0ff\"</span>)\n        ) <span class=\"hljs-keyword\">with</span> {\n            Wrap = FlexWrap.Wrap,\n            ColumnGap = <span class=\"hljs-number\">8</span>,\n            RowGap = <span class=\"hljs-number\">8</span>,\n            AlignItems = FlexAlign.Center\n        };\n    }\n}\n</code></pre>\n<p>The same shape works for tag rows, breadcrumb overflow, and toolbar\noverflow. Pair with <a href=\"/hooks/\"><code>UseWindowSize</code></a> if the breakpoint\nshould drive a different layout entirely (e.g., collapsing to a\nhamburger menu), but for in-place wrap the Flex container alone is\nsufficient.</p>\n<h3 id=\"toolbar-with-flexible-spacer\">Toolbar with flexible spacer<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#toolbar-with-flexible-spacer\">#</a></h3>\n<p>Already shown above — repeating here because the pattern crystallizes\nthe main-axis vs cross-axis split. <code>AlignItems = FlexAlign.Center</code>\nvertically centers buttons of mixed heights; <code>Empty().Flex(grow: 1)</code>\npushes the trailing buttons to the right edge. Two unrelated knobs, one\ncontainer.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"using-flexpanel-for-grid-shape-layouts\">Using FlexPanel for grid-shape layouts<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-flexpanel-for-grid-shape-layouts\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — two-dimensional alignment between rows is not what Flex does.</span>\nFlexColumn(\n    FlexRow(TextBlock(<span class=\"hljs-string\">\"Name:\"</span>), TextBox(name, setName)) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> },\n    FlexRow(TextBlock(<span class=\"hljs-string\">\"Email:\"</span>), TextBox(email, setEmail)) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> },\n    FlexRow(TextBlock(<span class=\"hljs-string\">\"Phone:\"</span>), TextBox(phone, setPhone)) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> }\n)\n</code></pre>\n<p>The labels won't line up — each row's flex distribution is independent.\nUse <a href=\"/layout/\"><code>Grid</code></a> for any layout where columns align across rows\n(forms, settings grids, data tables). FlexPanel only knows one axis at a\ntime.</p>\n<h3 id=\"mixing-width-with-flexgrow\">Mixing <code>.Width(...)</code> with <code>.Flex(grow:)</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mixing-width-with-flexgrow\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WidthVsGrowWrong</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Don't: .Width(200) sets the WinUI Width, but inside a FlexPanel</span>\n        <span class=\"hljs-comment\">// child sizing is governed by Flex(basis/grow/shrink). The 200 is</span>\n        <span class=\"hljs-comment\">// silently ignored when grow > 0 fills available space.</span>\n        <span class=\"hljs-keyword\">return</span> FlexRow(\n            TextBlock(<span class=\"hljs-string\">\"Stays 200?\"</span>)\n                .Width(<span class=\"hljs-number\">200</span>)              <span class=\"hljs-comment\">// ignored — grow wins</span>\n                .Flex(grow: <span class=\"hljs-number\">1</span>)\n                .Background(<span class=\"hljs-string\">\"#ffe0e0\"</span>)\n        ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> };\n    }\n}\n</code></pre>\n<p><code>.Width(200)</code> sets the underlying <code>FrameworkElement.Width</code>, but inside a\nFlexPanel the child's main-axis size comes from <code>Flex(basis:, grow:, shrink:)</code> — and <code>grow: 1</code> will fill available space regardless of\n<code>Width</code>. The 200 is silently ignored. Encode the intended size as\n<code>basis</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WidthVsGrowRight</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Do: encode the intended size as basis with shrink: 0 — Flex</span>\n        <span class=\"hljs-comment\">// owns the sizing math, so no surprise overrides.</span>\n        <span class=\"hljs-keyword\">return</span> FlexRow(\n            TextBlock(<span class=\"hljs-string\">\"Exactly 200\"</span>)\n                .Flex(basis: <span class=\"hljs-number\">200</span>, shrink: <span class=\"hljs-number\">0</span>)\n                .Background(<span class=\"hljs-string\">\"#e0ffe0\"</span>)\n                .Padding(<span class=\"hljs-number\">8</span>)\n        ) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> };\n    }\n}\n</code></pre>\n<p>The general rule: inside a FlexPanel, prefer <code>.Flex(basis: N)</code> over\n<code>.Width(N)</code>. The basis is what Yoga's algorithm reads; the width is\nwhat WinUI's measure pass reads, and they only sometimes agree.</p>\n<h3 id=\"forgetting-wrap-then-overflowing-on-narrow-widths\">Forgetting <code>Wrap</code> then overflowing on narrow widths<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forgetting-wrap-then-overflowing-on-narrow-widths\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — a default FlexPanel is NoWrap. On a 600px window this</span>\n<span class=\"hljs-comment\">// row overflows and clips the trailing items.</span>\nFlexRow(<span class=\"hljs-comment\">/* 7 nav items */</span>) <span class=\"hljs-keyword\">with</span> { ColumnGap = <span class=\"hljs-number\">8</span> }\n</code></pre>\n<p>Default <code>FlexWrap.NoWrap</code> produces a single line that overflows the\ncontainer when total child width exceeds available space. Set\n<code>Wrap = FlexWrap.Wrap</code> for any row whose item count or window width can\nvary. The cost is zero on rows that don't actually need to wrap — Yoga\nshort-circuits when one line fits.</p>\n<h3 id=\"reaching-for-flexpadding-when-you-wanted-padding\">Reaching for <code>FlexPadding</code> when you wanted <code>.Padding</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-flexpadding-when-you-wanted-padding\">#</a></h3>\n<p><code>FlexPadding</code> is a property on the FlexPanel itself, measured by Yoga\ninside the same algorithm that distributes children. The <code>.Padding(...)</code>\nmodifier sets the underlying WinUI <code>FrameworkElement.Margin</code> /\n<code>Padding</code>, which WinUI applies in its own pass. For consistent\ninside-container spacing — especially when wrapping or <code>JustifyContent</code>\nis involved — use <code>FlexPadding</code>. Reserve <code>.Padding(...)</code> for elements\nthat aren't FlexPanels.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start with VStack/HStack.</strong> They produce simpler element trees and\ncover most layouts. Reach for FlexPanel when you specifically need\njustification, alignment, wrap, or proportional sizing.</p>\n<p><strong>Use <code>ColumnGap</code> and <code>RowGap</code> instead of per-child margins.</strong> Gap\napplies between children automatically — no first-child / last-child\nasymmetry, and it composes correctly with wrapping.</p>\n<p><strong>Set <code>shrink: 0</code> on every fixed-width item.</strong> A sidebar, icon column,\nor pinned action button without <code>shrink: 0</code> will collapse when the\nwindow narrows. Pair <code>shrink: 0</code> with <code>basis</code> to lock the exact size.</p>\n<p><strong>Use <code>Empty().Flex(grow: 1)</code> as a flexible spacer.</strong> This is the flex\nequivalent of a spring — it absorbs all remaining space and pushes\nsiblings to the opposite edge. Composes correctly with mixed-flex\nsiblings.</p>\n<p><strong>Remember <code>with { }</code> syntax for container props.</strong> <code>FlexElement</code> is a\nC# record, so set <code>JustifyContent</code>, <code>AlignItems</code>, <code>Wrap</code>, <code>ColumnGap</code>,\nand <code>RowGap</code> via <code>FlexRow(...) with { JustifyContent = FlexJustify.Center }</code>.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — Previous: VStack, HStack, Grid, ScrollView, and other core containers</li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — Next: controlled input controls, validation, and form composition</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — render dynamic lists and grids with virtualization</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — theme tokens, dark/light mode, and lightweight styling for flex containers</li>\n<li><strong><a href=\"/input-and-gestures/\">Input and Gestures</a></strong> — pointer and keyboard events on flex children</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s flex layout reasons about two axes at once. The main axis distributes children across available space — that's where JustifyContent , Flex(grow:) , Flex(shrink:) , and Flex(basis:) apply. The cross axis aligns each child against the container's other dimension — that's where AlignItems and per-child AlignSelf apply. The same FlexPanel composes both, and that combination is what unlocks the layouts that VStack and HStack cannot express. A toolbar with a flexible spacer, an app shell with a fixed sidebar and a fluid content area, a wrap-on- overflow tag row — all three are FlexPanels with different main-axis / cross-axis rules. Read this page before reaching for Grid on a problem that's really \"distribute these items along one axis with alignment on the other\" — that's the FlexPanel shape, and the moment a grid feels overkill is the moment Flex is right. Flex Layout # Reactor's FlexPanel (exposed through the FlexRow and FlexColumn factories) is a Yoga-backed CSS Flexbox implementation. Reach for it when the layout needs alignment control, wrapping, or proportional sizing that VStack and HStack cannot express. Add using Microsoft.UI.Reactor.Layout; (or import FlexDirection , FlexJustify , FlexAlign , FlexWrap ) to access the enum types. At-a-glance reference # Property Set on Effect JustifyContent container ( with { ... } ) Main-axis distribution: FlexStart / Center / FlexEnd / SpaceBetween / SpaceAround / SpaceEvenly AlignItems container ( with { ... } ) Cross-axis alignment: Stretch / FlexStart / Center / FlexEnd / Baseline AlignContent container ( with { ... } ) Cross-axis alignment of wrapped lines — only matters when Wrap is on and lines have leftover space Wrap container ( with { ... } ) NoWrap (single-line, overflow) / Wrap / WrapReverse ColumnGap / RowGap container ( with { ... } ) Spacing between children — applies between wrapped lines too FlexPadding container ( with { ... } ) Inside-container padding measured by Yoga (distinct from the .Padding(...) modifier) .Flex(grow:, shrink:, basis:) child Main-axis sizing .Flex(minWidth:, minHeight:) child Override CSS §4.5 auto-min on the main axis ( null = CSS auto ; 0 = no floor; positive = hard floor). See the Min sizing table for how auto resolves. .Flex(alignSelf:) child Override the container's AlignItems for one child Direction # FlexRow lays children along the horizontal main axis; FlexColumn lays them along the vertical main axis. The cross axis is whichever direction is left. Set spacing through ColumnGap (between row children) or RowGap (between column children): class FlexDirectionDemo : Component { public override Element Render () { return VStack( 16 , SubHeading( \"Row (default)\" ), FlexRow( TextBlock( \"A\" ).Padding( 12 ).Background( \"#e0e0ff\" ), TextBlock( \"B\" ).Padding( 12 ).Background( \"#ffe0e0\" ), TextBlock( \"C\" ).Padding( 12 ).Background( \"#e0ffe0\" ) ) with { ColumnGap = 8 }, SubHeading( \"Column\" ), FlexColumn( TextBlock( \"A\" ).Padding( 12 ).Background( \"#e0e0ff\" ), TextBlock( \"B\" ).Padding( 12 ).Background( \"#ffe0e0\" ), TextBlock( \"C\" ).Padding( 12 ).Background( \"#e0ffe0\" ) ) with { RowGap = 8 } ).Padding( 24 ); } } A FlexRow looks like HStack at first glance, but a FlexPanel additionally exposes justification, cross-axis alignment, wrapping, and proportional sizing — HStack does none of those. Justify and Align # JustifyContent distributes children along the main axis; AlignItems aligns them on the cross axis. These are independent knobs on the same container — read them as two separate sentences: class JustifyAlignDemo : Component { public override Element Render () { return VStack( 16 , SubHeading( \"JustifyContent: SpaceBetween\" ), FlexRow( TextBlock( \"Left\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Center\" ).Padding( 8 ).Background( \"#ffe0e0\" ), TextBlock( \"Right\" ).Padding( 8 ).Background( \"#e0ffe0\" ) ) with { JustifyContent = FlexJustify.SpaceBetween }, SubHeading( \"AlignItems: Center\" ), FlexRow( TextBlock( \"Short\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Tall\\nItem\" ).Padding( 8 ).Background( \"#ffe0e0\" ), TextBlock( \"Med\" ).Padding( 8 ).Background( \"#e0ffe0\" ) ) with { AlignItems = FlexAlign.Center, ColumnGap = 8 } ).Padding( 24 ).Height( 300 ); } } JustifyContent Effect (main axis) FlexStart Pack to start (default) Center Center along main axis FlexEnd Pack to end SpaceBetween Equal space between items, none at edges SpaceAround Equal space around items SpaceEvenly Equal space between and at edges AlignItems Effect (cross axis) Stretch Fill cross axis (default) FlexStart Align to cross-axis start Center Center on cross axis FlexEnd Align to cross-axis end Baseline Align text baselines Wrap and gap # When children would overflow the container on the main axis, set Wrap = FlexWrap.Wrap to flow them onto a second line. ColumnGap and RowGap apply consistently — between children on a line and between wrapped lines: class WrapGapDemo : Component { public override Element Render () { var tags = new [] { \"C#\" , \"WinUI\" , \"Reactor\" , \".NET\" , \"XAML\" , \"Flex\" , \"Layout\" , \"Desktop\" , \"Native\" }; return VStack( 12 , SubHeading( \"Wrapping Tags\" ), FlexRow( tags.Select(tag => TextBlock(tag) .Padding(horizontal: 6 , vertical: 12 ) .Background( \"#e8e8e8\" ) .CornerRadius( 12 ) ).ToArray() ) with { Wrap = FlexWrap.Wrap, ColumnGap = 8 , RowGap = 8 } ).Padding( 24 ); } } Tag clouds, chip rows, breadcrumb overflow, filter pills — anywhere the item count varies and the container width can shrink — are wrap-target shapes. For rendering long item collections with virtualization, see Collections . Grow, shrink, basis # .Flex(grow:, shrink:, basis:) on a child controls how that child shares main-axis space with its siblings: grow — share of the extra space the child claims after all basis sizes are placed (default 0 — the child holds its content size). shrink — share of the deficit the child gives up when the total basis exceeds the container (default 1 — the child shrinks proportionally). basis — the starting size in pixels before grow/shrink apply. null means \"measure my content first\" (Yoga's auto ). minWidth / minHeight — explicit floor for the child's main-axis size. null (default) means auto — see \"Min sizing (CSS §4.5)\" below. class GrowShrinkDemo : Component { public override Element Render () { return VStack( 16 , SubHeading( \"Grow: sidebar + content\" ), FlexRow( TextBlock( \"Sidebar\" ) .Padding( 16 ).Background( \"#e0e0ff\" ) .Flex(basis: 200 , shrink: 0 ), TextBlock( \"Main content area\" ) .Padding( 16 ).Background( \"#f0f0f0\" ) .Flex(grow: 1 ) ) with { ColumnGap = 8 }, SubHeading( \"Equal columns\" ), FlexRow( TextBlock( \"Column 1\" ).Padding( 16 ).Background( \"#ffe0e0\" ).Flex(grow: 1 ), TextBlock( \"Column 2\" ).Padding( 16 ).Background( \"#e0ffe0\" ).Flex(grow: 1 ), TextBlock( \"Column 3\" ).Padding( 16 ).Background( \"#e0e0ff\" ).Flex(grow: 1 ) ) with { ColumnGap = 8 } ).Padding( 24 ); } } The sidebar uses basis: 200, shrink: 0 to fix its width; the content column uses grow: 1 to absorb whatever remains. The equal-columns row gives every child grow: 1 so the available space splits evenly. Caveat: Flex(grow: 0) + Flex(shrink: 1) with no basis defaults to basis: auto — Yoga measures the child's content size in a first pass, then runs distribution in a second. For a row of 200 list cells, that first measurement pass dominates layout cost: the panel asks each cell for its preferred size before doing any distribution. Setting an explicit basis: 0 (or any pixel value) collapses the two passes into one: distribution alone, no content measurement. The difference is invisible on a 5-button toolbar and load-bearing on a 200-row table header that rebuilds on every column resize. Min sizing (CSS §4.5) # By default, a flex child with basis: auto will not shrink below its min-content size — the smallest size it can occupy without overflowing its own content. This matches the CSS Flexbox specification §4.5 (\"Automatic Minimum Size of Flex Items\") and is what most developers intuitively expect when laying out a row of cards or a column of sections: items don't squish smaller than their text or icons. Reactor's FlexPanel implements the auto-min rule as follows: basis minWidth / minHeight Resulting floor auto (default) null (default) min-content 0 null 0 (short-circuit — basis already opts out of content) pixel value N > 0 null min(N, min-content) any explicit 0 0 (you opt out of the floor) any explicit N > 0 N (your hard floor) The auto-min rule applies only to the main axis (width for Direction.Row , height for Direction.Column ). On the cross axis, items shrink freely to 0 unless you set minWidth / minHeight explicitly. // Default behavior — items keep their min-content size: FlexPanel( Border( \"Long text that won't truncate\" ).Flex(shrink: 1 ), Border( \"Short\" ).Flex(shrink: 1 ), ).Direction(FlexDirection.Row); // Opt out — let items shrink below content (text will clip): FlexPanel( Border( \"Long text that may be clipped\" ).Flex(shrink: 1 , minWidth: 0 ), Border( \"Short\" ).Flex(shrink: 1 , minWidth: 0 ), ).Direction(FlexDirection.Row); // Explicit floor — never below 80px regardless of content: Border(content).Flex(shrink: 1 , minWidth: 80 ) Caveat: Performance: computing min-content requires a WinUI Measure(width: marginH, height: ∞) pass on each affected child before the main flex algorithm runs. This is fine for typical UIs but can matter for very large lists. Two ways to skip the pre-measure: Set basis: 0 explicitly — the auto-min collapses to 0 with no measurement. Set minWidth: 0 (or minHeight: 0 ) explicitly — you've opted out of the floor. Children that are ScrollView or ScrollViewer always get a 0 floor: their min-content would force their virtualized contents to realize, which defeats the point of virtualization. Caveat: CSS vs WinUI mismatch on explicit Width . In CSS, an item with width: 200px; flex-shrink: 1; min-width: 0 is allowed to shrink below 200. In WinUI, FrameworkElement.Width = 200 is a hard size constraint — the control will report DesiredSize.Width = 200 regardless of available space and will arrange at 200 inside its allocated slot. Inside a FlexPanel, prefer .Flex(basis: 200, ...) over .Width(200) for any item you want to shrink. Yoga reads the basis; WinUI's Width doesn't participate in flex distribution the way CSS's width does. Practical example: toolbar with flexible spacer # A toolbar with a left-aligned title and right-aligned buttons is the canonical flex shape. Use Empty().Flex(grow: 1) as a flexible spacer — it absorbs every spare pixel and pushes the buttons to the trailing edge: class ToolbarDemo : Component { public override Element Render () { var (selected, setSelected) = UseState( \"Home\" ); return VStack( 0 , FlexRow( TextBlock( \"MyApp\" ).Bold().Flex(shrink: 0 ), Empty().Flex(grow: 1 ), Button( \"Home\" , () => setSelected( \"Home\" )), Button( \"Settings\" , () => setSelected( \"Settings\" )), Button( \"About\" , () => setSelected( \"About\" )) ) with { AlignItems = FlexAlign.Center, ColumnGap = 8 , FlexPadding = new Thickness( 16 , 8 , 16 , 8 ) }, TextBlock( $\"Current page: {selected} \" ) .Padding( 24 ).FontSize( 18 ) ); } } The spacer pattern composes — three spacers between three buttons distribute them evenly without SpaceBetween on the container, which matters when only some children should flex. Flex vs VStack/HStack vs Grid # Pick a container by what its shape promises, not by what looks expedient: class FlexVsStackDemo : Component { public override Element Render () { return VStack( 16 , SubHeading( \"HStack (fixed spacing)\" ), HStack( 8 , Button( \"A\" ), Button( \"B\" ), Button( \"C\" ) ), SubHeading( \"FlexRow (justify + align)\" ), FlexRow( Button( \"A\" ), Button( \"B\" ), Button( \"C\" ) ) with { JustifyContent = FlexJustify.SpaceEvenly, AlignItems = FlexAlign.Center } ).Padding( 24 ); } } Feature VStack / HStack FlexRow / FlexColumn Grid Fixed inter-child spacing yes yes ( ColumnGap / RowGap ) yes Justify along main axis no yes row/column at a time Cross-axis alignment no yes VAlign per cell Wrap on overflow no yes implicit row/column matrix Proportional sizing no yes ( Flex(grow:) ) yes ( GridSize.Star ) Two-dimensional layout no no yes If the layout has a row-and-column structure (a form, a settings grid, anything where columns line up across rows), reach for Grid . If it's \"distribute children along one axis with alignment on the other,\" FlexPanel is the right shape. Patterns # App shell — fixed sidebar, fluid content # A two-column layout with a fixed-width nav rail and a content pane that absorbs the rest is the most common flex shape in production apps. The key is basis: 220, shrink: 0 on the sidebar (locks the width and prevents collapse) paired with grow: 1, basis: 0 on the content (grabs the remainder in a single distribution pass): class AppShellDemo : Component { public override Element Render () { return FlexRow( // Sidebar — fixed 220px, never shrinks below it. VStack( 8 , TextBlock( \"Inbox\" ).Padding( 8 ), TextBlock( \"Drafts\" ).Padding( 8 ), TextBlock( \"Sent\" ).Padding( 8 ) ).Background( \"#f3f3f3\" ) .Flex(basis: 220 , shrink: 0 ), // Content — explicit basis: 0 + grow: 1 gives a single distribution // pass instead of measuring the inner text first. VStack( 12 , Heading( \"Inbox\" ), TextBlock( \"Three messages, one starred. The sidebar stays 220px wide; this column absorbs every spare pixel.\" ) ).Padding( 16 ) .Flex(grow: 1 , basis: 0 ) ) with { ColumnGap = 1 }; } } The explicit basis: 0 on content matches the caveat above — no content-measurement pass, just distribution. The sidebar's shrink: 0 keeps the rail at 220 pixels even when the window narrows below the content's intrinsic minimum. Responsive nav rail with wrap-on-overflow # Above a breakpoint, the nav rail is a single row; below the breakpoint, items wrap onto a second line. FlexWrap.Wrap + RowGap + ColumnGap is the entire mechanism — no media-query equivalent needed: class ResponsiveNavDemo : Component { public override Element Render () { // Wrap kicks in when the narrow viewport can no longer fit one row. // RowGap and ColumnGap apply between wrapped lines too — no manual margin. return FlexRow( TextBlock( \"Home\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Catalog\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Pricing\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Docs\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"About\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Contact\" ).Padding( 8 ).Background( \"#e0e0ff\" ), TextBlock( \"Status\" ).Padding( 8 ).Background( \"#e0e0ff\" ) ) with { Wrap = FlexWrap.Wrap, ColumnGap = 8 , RowGap = 8 , AlignItems = FlexAlign.Center }; } } The same shape works for tag rows, breadcrumb overflow, and toolbar overflow. Pair with UseWindowSize if the breakpoint should drive a different layout entirely (e.g., collapsing to a hamburger menu), but for in-place wrap the Flex container alone is sufficient. Toolbar with flexible spacer # Already shown above — repeating here because the pattern crystallizes the main-axis vs cross-axis split. AlignItems = FlexAlign.Center vertically centers buttons of mixed heights; Empty().Flex(grow: 1) pushes the trailing buttons to the right edge. Two unrelated knobs, one container. Common Mistakes # Using FlexPanel for grid-shape layouts # // Don't — two-dimensional alignment between rows is not what Flex does. FlexColumn( FlexRow(TextBlock( \"Name:\" ), TextBox(name, setName)) with { ColumnGap = 8 }, FlexRow(TextBlock( \"Email:\" ), TextBox(email, setEmail)) with { ColumnGap = 8 }, FlexRow(TextBlock( \"Phone:\" ), TextBox(phone, setPhone)) with { ColumnGap = 8 } ) The labels won't line up — each row's flex distribution is independent. Use Grid for any layout where columns align across rows (forms, settings grids, data tables). FlexPanel only knows one axis at a time. Mixing .Width(...) with .Flex(grow:) # class WidthVsGrowWrong : Component { public override Element Render () { // Don't: .Width(200) sets the WinUI Width, but inside a FlexPanel // child sizing is governed by Flex(basis/grow/shrink). The 200 is // silently ignored when grow > 0 fills available space. return FlexRow( TextBlock( \"Stays 200?\" ) .Width( 200 ) // ignored — grow wins .Flex(grow: 1 ) .Background( \"#ffe0e0\" ) ) with { ColumnGap = 8 }; } } .Width(200) sets the underlying FrameworkElement.Width , but inside a FlexPanel the child's main-axis size comes from Flex(basis:, grow:, shrink:) — and grow: 1 will fill available space regardless of Width . The 200 is silently ignored. Encode the intended size as basis : class WidthVsGrowRight : Component { public override Element Render () { // Do: encode the intended size as basis with shrink: 0 — Flex // owns the sizing math, so no surprise overrides. return FlexRow( TextBlock( \"Exactly 200\" ) .Flex(basis: 200 , shrink: 0 ) .Background( \"#e0ffe0\" ) .Padding( 8 ) ) with { ColumnGap = 8 }; } } The general rule: inside a FlexPanel, prefer .Flex(basis: N) over .Width(N) . The basis is what Yoga's algorithm reads; the width is what WinUI's measure pass reads, and they only sometimes agree. Forgetting Wrap then overflowing on narrow widths # // Don't — a default FlexPanel is NoWrap. On a 600px window this // row overflows and clips the trailing items. FlexRow( /* 7 nav items */ ) with { ColumnGap = 8 } Default FlexWrap.NoWrap produces a single line that overflows the container when total child width exceeds available space. Set Wrap = FlexWrap.Wrap for any row whose item count or window width can vary. The cost is zero on rows that don't actually need to wrap — Yoga short-circuits when one line fits. Reaching for FlexPadding when you wanted .Padding # FlexPadding is a property on the FlexPanel itself, measured by Yoga inside the same algorithm that distributes children. The .Padding(...) modifier sets the underlying WinUI FrameworkElement.Margin / Padding , which WinUI applies in its own pass. For consistent inside-container spacing — especially when wrapping or JustifyContent is involved — use FlexPadding . Reserve .Padding(...) for elements that aren't FlexPanels. Tips # Start with VStack/HStack. They produce simpler element trees and cover most layouts. Reach for FlexPanel when you specifically need justification, alignment, wrap, or proportional sizing. Use ColumnGap and RowGap instead of per-child margins. Gap applies between children automatically — no first-child / last-child asymmetry, and it composes correctly with wrapping. Set shrink: 0 on every fixed-width item. A sidebar, icon column, or pinned action button without shrink: 0 will collapse when the window narrows. Pair shrink: 0 with basis to lock the exact size. Use Empty().Flex(grow: 1) as a flexible spacer. This is the flex equivalent of a spring — it absorbs all remaining space and pushes siblings to the opposite edge. Composes correctly with mixed-flex siblings. Remember with { } syntax for container props. FlexElement is a C# record, so set JustifyContent , AlignItems , Wrap , ColumnGap , and RowGap via FlexRow(...) with { JustifyContent = FlexJustify.Center } . Next Steps # Layout — Previous: VStack, HStack, Grid, ScrollView, and other core containers Forms and Input — Next: controlled input controls, validation, and form composition Collections — render dynamic lists and grids with virtualization Styling and Theming — theme tokens, dark/light mode, and lightweight styling for flex containers Input and Gestures — pointer and keyboard events on flex children",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/flex-layout.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/flex-layout.md",
      "headings": [
        {
          "depth": 2,
          "id": "at-a-glance-reference",
          "text": "At-a-glance reference"
        },
        {
          "depth": 2,
          "id": "direction",
          "text": "Direction"
        },
        {
          "depth": 2,
          "id": "justify-and-align",
          "text": "Justify and Align"
        },
        {
          "depth": 2,
          "id": "wrap-and-gap",
          "text": "Wrap and gap"
        },
        {
          "depth": 2,
          "id": "grow-shrink-basis",
          "text": "Grow, shrink, basis"
        },
        {
          "depth": 2,
          "id": "min-sizing-css-45",
          "text": "Min sizing (CSS §4.5)"
        },
        {
          "depth": 2,
          "id": "practical-example-toolbar-with-flexible-spacer",
          "text": "Practical example: toolbar with flexible spacer"
        },
        {
          "depth": 2,
          "id": "flex-vs-vstackhstack-vs-grid",
          "text": "Flex vs VStack/HStack vs Grid"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "app-shell--fixed-sidebar-fluid-content",
          "text": "App shell — fixed sidebar, fluid content"
        },
        {
          "depth": 3,
          "id": "responsive-nav-rail-with-wrap-on-overflow",
          "text": "Responsive nav rail with wrap-on-overflow"
        },
        {
          "depth": 3,
          "id": "toolbar-with-flexible-spacer",
          "text": "Toolbar with flexible spacer"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "using-flexpanel-for-grid-shape-layouts",
          "text": "Using FlexPanel for grid-shape layouts"
        },
        {
          "depth": 3,
          "id": "mixing-width-with-flexgrow",
          "text": "Mixing .Width(...) with .Flex(grow:)"
        },
        {
          "depth": 3,
          "id": "forgetting-wrap-then-overflowing-on-narrow-widths",
          "text": "Forgetting Wrap then overflowing on narrow widths"
        },
        {
          "depth": 3,
          "id": "reaching-for-flexpadding-when-you-wanted-padding",
          "text": "Reaching for FlexPadding when you wanted .Padding"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Styling and Theming",
      "path": "styling.md",
      "slug": "/styling/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/style/\">Style</a>.</p>\n</blockquote>\n<p>Styling in Microsoft.UI.Reactor (Reactor) is a layered modifier chain. The bottom layer is the\nWinUI theme system — every visible color is ultimately a brush resource\nthat resolves against the effective <code>Light</code> / <code>Dark</code> / <code>HighContrast</code>\ntheme dictionary at render time. The next layer up is <code>ThemeRef</code>, a\nrecord-struct reference to one of those resources by key. On top of that\nsit two surfaces: typed accessors like <code>Theme.Accent</code>, <code>Theme.AccentText</code>,\n<code>Theme.CardBackground</code> (canonical, autocompleted, refactor-safe) and\n<code>Theme.Ref(\"AnyResourceKey\")</code> for the long tail of WinUI brushes Reactor\ndoesn't surface as a property. Above the tokens are the modifier\nshortcuts — <code>.AccentButton()</code>, <code>.SubtleButton()</code>, signal-severity\nfluents — that bake a small named-style choice into one call. Above\nthose is lightweight styling via <code>.Resources(r => r.Set(key, value))</code>,\nwhich overrides individual WinUI resource keys for a subtree without\nreplacing the control template. The whole stack is <strong>theme-aware by\ndefault</strong>: tokens, modifier shortcuts, and Resource overrides all\nre-resolve when the effective theme flips. The escape hatches are\nliteral hex strings (<code>\"#7B61FF\"</code>) and direct <code>.Set(control => ...)</code>\nproperty writes — both are theme-frozen by construction and are the\nright tool only for brand colors that should stay constant regardless\nof mode.</p>\n<h1 id=\"styling-and-theming\">Styling and Theming<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#styling-and-theming\">#</a></h1>\n<blockquote>\n<p><strong>WinUI reference:</strong> For the underlying brush resources, the Fluent\ndesign language, and the full styling surface, see\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/style/\">Design Windows apps overview on Microsoft Learn</a>.</p>\n</blockquote>\n<h2 id=\"at-a-glance-reference\">At-a-glance reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#at-a-glance-reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Layer</th><th>Use when</th></tr></thead><tbody><tr><td><code>Theme.Accent</code>, <code>Theme.PrimaryText</code>, etc.</td><td>Typed token</td><td>Always — these are the canonical brush references.</td></tr><tr><td><code>Theme.Ref(\"AnyResourceKey\")</code></td><td>String token</td><td>The brush isn't surfaced as a typed accessor (rare).</td></tr><tr><td><code>.Background(token)</code> / <code>.Foreground(token)</code></td><td>Color modifier</td><td>Apply a token to any element.</td></tr><tr><td><code>.Background(\"#RRGGBB\")</code></td><td>Color modifier</td><td>Brand colors that must stay constant across themes.</td></tr><tr><td><code>.AccentButton()</code>, <code>.SubtleButton()</code>, <code>.TextLink()</code></td><td>Named-style fluent</td><td>The four canonical button shapes. No <code>.Resources</code> needed.</td></tr><tr><td><code>.Informational()</code> / <code>.Success()</code> / <code>.Warning()</code> / <code>.Error()</code></td><td>Named-style fluent</td><td>InfoBar severity.</td></tr><tr><td><code>.Resources(r => r.Set(key, value))</code></td><td>Lightweight styling</td><td>Override individual WinUI resource keys for a subtree.</td></tr><tr><td><code>.RequestedTheme(ElementTheme.Dark)</code></td><td>Region root</td><td>Pin a subtree to a specific scheme.</td></tr><tr><td><code>UseColorScheme()</code> / <code>UseIsDarkTheme()</code></td><td>Hook</td><td>Branch logic (not just colors) on the effective theme.</td></tr><tr><td><code>.Set(control => control.Foreground = ...)</code></td><td>Escape hatch</td><td>The control has a property neither tokens nor <code>.Resources</code> reach.</td></tr></tbody></table>\n<h2 id=\"theme-tokens\">Theme tokens<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#theme-tokens\">#</a></h2>\n<p>Apply a typed token with <code>.Background()</code> or <code>.Foreground()</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemeTokensExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Primary Text\"</span>).Foreground(Theme.PrimaryText),\n            TextBlock(<span class=\"hljs-string\">\"Secondary Text\"</span>).Foreground(Theme.SecondaryText),\n            TextBlock(<span class=\"hljs-string\">\"Accent Text\"</span>).Foreground(Theme.AccentText).SemiBold(),\n            TextBlock(<span class=\"hljs-string\">\"On Accent Background\"</span>)\n                .Foreground(<span class=\"hljs-string\">\"#FFFFFF\"</span>)\n                .Padding(horizontal: <span class=\"hljs-number\">8</span>, vertical: <span class=\"hljs-number\">4</span>)\n                .Background(Theme.Accent)\n                .CornerRadius(<span class=\"hljs-number\">4</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/theme-tokens.png\" alt=\"Themed text and accent background\"></p>\n<p>Each token resolves to a WinUI brush at render time. When the user\nswitches between light and dark mode, every element bound to a <code>ThemeRef</code>\nupdates automatically — no manual rebinding. The full token catalog\nwith light/dark swatches lives in\n<a href=\"/theming-tokens/\">theming-tokens</a>.</p>\n<h2 id=\"building-cards\">Building cards<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#building-cards\">#</a></h2>\n<p>A card is a <code>Border</code> with <code>Theme.CardBackground</code>, rounded corners,\npadding, and <code>Theme.CardStroke</code>. Combine those modifiers with\n<a href=\"/layout/\">layout containers</a> for reusable card shapes:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CardLayoutExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            Heading(<span class=\"hljs-string\">\"Dashboard\"</span>),\n            HStack(<span class=\"hljs-number\">12</span>,\n                Card(<span class=\"hljs-string\">\"Users\"</span>, <span class=\"hljs-string\">\"1,204\"</span>, Theme.Accent),\n                Card(<span class=\"hljs-string\">\"Revenue\"</span>, <span class=\"hljs-string\">\"$48.2k\"</span>, Theme.SystemSuccess),\n                Card(<span class=\"hljs-string\">\"Errors\"</span>, <span class=\"hljs-string\">\"3\"</span>, Theme.SystemCritical)\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">Card</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> title, <span class=\"hljs-built_in\">string</span> <span class=\"hljs-keyword\">value</span>, ThemeRef accent</span>)</span> =>\n        Border(\n            VStack(<span class=\"hljs-number\">8</span>,\n                Caption(title).Foreground(Theme.SecondaryText),\n                TextBlock(<span class=\"hljs-keyword\">value</span>).FontSize(<span class=\"hljs-number\">28</span>).Bold().Foreground(accent)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n        ).Background(Theme.CardBackground)\n         .CornerRadius(<span class=\"hljs-number\">8</span>)\n         .WithBorder(Theme.CardStroke, <span class=\"hljs-number\">1</span>)\n         .Width(<span class=\"hljs-number\">160</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/card-layout.png\" alt=\"Card layout with themed backgrounds\"></p>\n<p><code>Theme.CardBackground</code> is the standard WinUI card surface;\n<code>Theme.CardStroke</code> is the matching border. Together they produce a card\nthat looks native in both light and dark mode.</p>\n<h2 id=\"color-modifiers\">Color modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#color-modifiers\">#</a></h2>\n<p><code>.Background()</code> and <code>.Foreground()</code> accept three input shapes:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ColorModifiersExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(<span class=\"hljs-string\">\"Theme token\"</span>).Background(Theme.SubtleFill).Padding(<span class=\"hljs-number\">8</span>),\n            TextBlock(<span class=\"hljs-string\">\"Hex string\"</span>).Background(<span class=\"hljs-string\">\"#E8F5E9\"</span>).Padding(<span class=\"hljs-number\">8</span>),\n            TextBlock(<span class=\"hljs-string\">\"Mixed\"</span>).Foreground(Theme.PrimaryText)\n                .Background(<span class=\"hljs-string\">\"#1E1E2E\"</span>).Padding(<span class=\"hljs-number\">8</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Overload</th><th>Example</th><th>Theme-aware?</th></tr></thead><tbody><tr><td>Typed token</td><td><code>.Background(Theme.Accent)</code></td><td>yes</td></tr><tr><td><code>Theme.Ref</code></td><td><code>.Background(Theme.Ref(\"MyCustomBrush\"))</code></td><td>yes</td></tr><tr><td>Hex string</td><td><code>.Background(\"#FF5733\")</code></td><td>no — frozen</td></tr><tr><td><code>Windows.UI.Color</code></td><td><code>.Background(Colors.Blue)</code></td><td>no — frozen</td></tr></tbody></table>\n<p>Tokens are the right default. Reserve hex and <code>Color</code> for brand colors\nthat should stay constant regardless of mode.</p>\n<h2 id=\"signal-colors\">Signal colors<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#signal-colors\">#</a></h2>\n<p>Status indicators use semantic signal colors:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SignalColorsExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">12</span>,\n            Badge(<span class=\"hljs-string\">\"Info\"</span>, Theme.SystemAttention),\n            Badge(<span class=\"hljs-string\">\"Success\"</span>, Theme.SystemSuccess),\n            Badge(<span class=\"hljs-string\">\"Warning\"</span>, Theme.SystemCaution),\n            Badge(<span class=\"hljs-string\">\"Error\"</span>, Theme.SystemCritical)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">Badge</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> label, ThemeRef color</span>)</span> =>\n        TextBlock(label)\n            .FontSize(<span class=\"hljs-number\">12</span>).SemiBold()\n            .Foreground(color)\n            .Padding(horizontal: <span class=\"hljs-number\">8</span>, vertical: <span class=\"hljs-number\">4</span>)\n            .Background(Theme.SubtleFill)\n            .CornerRadius(<span class=\"hljs-number\">4</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/signal-colors.png\" alt=\"Signal color badges\"></p>\n<p>Use these instead of hardcoded red/green/yellow — they meet\n<a href=\"/accessibility/\">accessibility</a> contrast requirements in both themes\nand tone-shift correctly under <code>HighContrast</code>.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Theme.Ref(\"SomeResourceKey\")</code> resolves through string lookup at every\nrender, walking the merged-dictionary chain to find the brush; the\ntyped accessors (<code>Theme.Accent</code>, <code>Theme.AccentText</code>, …) construct the\nsame <code>ThemeRef</code> record-struct with the canonical key string. The\ntyped-accessor path is the supported one for two reasons: (1) renaming\na WinUI resource is a compile error against typed accessors and a\nruntime null brush against <code>Theme.Ref</code> — the latter silently falls\nthrough to the system default; (2) the <code>REACTOR_THEME_001</code> analyzer\ntreats hex strings inside <code>.Background(...)</code> as warnings only when the\nhex matches a known token value, so <code>.Background(Theme.Ref(\"Foo\"))</code> is\ninvisible to the analyzer when <code>Foo</code> doesn't exist as a typed\naccessor. The rule: if a typed <code>Theme.X</code> exists, use it. Reserve\n<code>Theme.Ref(...)</code> for the long-tail brushes Reactor hasn't surfaced\nyet, and prefer filing a feature request to add the accessor over\nleaving a string reference in the codebase.</p>\n</blockquote>\n<h2 id=\"dark-and-light-mode\">Dark and light mode<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dark-and-light-mode\">#</a></h2>\n<p><code>.RequestedTheme(ElementTheme.Dark)</code> pins a subtree to a specific\ntheme. <code>UseColorScheme()</code> and <code>UseIsDarkTheme()</code> let you read the\neffective theme reactively:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DarkLightToggleExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (isDark, setIsDark) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> theme = isDark ? ElementTheme.Dark : ElementTheme.Light;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            ToggleSwitch(isDark, setIsDark, onContent: <span class=\"hljs-string\">\"Dark\"</span>, offContent: <span class=\"hljs-string\">\"Light\"</span>),\n            Border(\n                VStack(<span class=\"hljs-number\">12</span>,\n                    TextBlock(<span class=\"hljs-string\">\"This panel follows the toggle.\"</span>).Foreground(Theme.PrimaryText),\n                    TextBlock(<span class=\"hljs-string\">\"Background adapts automatically.\"</span>).Foreground(Theme.SecondaryText)\n                ).Padding(<span class=\"hljs-number\">16</span>)\n            ).Background(Theme.CardBackground)\n             .CornerRadius(<span class=\"hljs-number\">8</span>)\n             .RequestedTheme(theme)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/dark-light-toggle.png\" alt=\"Dark/light mode toggle\"></p>\n<p><code>.RequestedTheme(...)</code> sets the property on the underlying\n<code>FrameworkElement</code>. All <code>ThemeRef</code> bindings in descendants resolve\nagainst the new scheme. <code>UseIsDarkTheme()</code> returns <code>true</code> when the\neffective scheme is dark — branch on it when you need different\n<strong>behavior</strong> (different icons, different layouts), not just different\ncolors.</p>\n<h2 id=\"reactive-theme-hooks\">Reactive theme hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactive-theme-hooks\">#</a></h2>\n<p><code>UseColorScheme()</code> returns the effective scheme at the component's\nposition in the tree. <code>UseIsDarkTheme()</code> is a convenience wrapper that\nnarrows it to a boolean:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ColorSchemeHookExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> isDark = UseIsDarkTheme();\n        <span class=\"hljs-keyword\">var</span> scheme = UseColorScheme();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Color scheme: <span class=\"hljs-subst\">{scheme}</span>\"</span>).FontSize(<span class=\"hljs-number\">16</span>).SemiBold(),\n            TextBlock(isDark ? <span class=\"hljs-string\">\"Dark mode is active\"</span> : <span class=\"hljs-string\">\"Light mode is active\"</span>)\n                .Foreground(Theme.SecondaryText),\n            Border(\n                TextBlock(isDark ? <span class=\"hljs-string\">\"Dark content\"</span> : <span class=\"hljs-string\">\"Light content\"</span>)\n                    .Padding(<span class=\"hljs-number\">12</span>)\n            ).Background(Theme.CardBackground)\n             .CornerRadius(<span class=\"hljs-number\">8</span>)\n             .WithBorder(Theme.CardStroke, <span class=\"hljs-number\">1</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/color-scheme-hook.png\" alt=\"Color scheme hook\"></p>\n<p><code>ColorScheme</code> has three values: <code>Light</code>, <code>Dark</code>, and <code>HighContrast</code>.\nUse the hook when you need to branch logic (not just colors) on the\ntheme — choosing different icon assets, picking a different layout\ndensity, or running different analytics tags.</p>\n<h2 id=\"named-style-fluents\">Named-style fluents<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#named-style-fluents\">#</a></h2>\n<p>Reactor exposes the canonical WinUI named styles as fluent shortcuts.\nThe common cases never need a <code>.Resources(...)</code> block:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NamedStylesExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Save\"</span>, () => { }).AccentButton(),\n                Button(<span class=\"hljs-string\">\"Cancel\"</span>, () => { }).SubtleButton(),\n                HyperlinkButton(<span class=\"hljs-string\">\"Learn more\"</span>, <span class=\"hljs-keyword\">new</span> Uri(<span class=\"hljs-string\">\"https://example.com\"</span>))\n                    .TextLink()\n            ),\n            InfoBar(title: <span class=\"hljs-string\">\"Heads up\"</span>, message: <span class=\"hljs-string\">\"Backups run nightly.\"</span>)\n                .Informational(),\n            InfoBar(title: <span class=\"hljs-string\">\"Saved\"</span>, message: <span class=\"hljs-string\">\"All changes are persisted.\"</span>)\n                .Success(),\n            InfoBar(title: <span class=\"hljs-string\">\"Almost full\"</span>, message: <span class=\"hljs-string\">\"75% of quota used.\"</span>)\n                .Warning(),\n            InfoBar(title: <span class=\"hljs-string\">\"Sync failed\"</span>, message: <span class=\"hljs-string\">\"Check your network.\"</span>)\n                .Error()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Maps to</th></tr></thead><tbody><tr><td><code>.AccentButton()</code></td><td><code>AccentButtonStyle</code> — filled accent color</td></tr><tr><td><code>.SubtleButton()</code></td><td><code>SubtleButtonStyle</code> — text-only until hovered</td></tr><tr><td><code>.TextLink()</code></td><td>Hyperlink-style button (no chrome)</td></tr><tr><td><code>.Informational()</code></td><td><code>InfoBarSeverity.Informational</code></td></tr><tr><td><code>.Success()</code></td><td><code>InfoBarSeverity.Success</code></td></tr><tr><td><code>.Warning()</code></td><td><code>InfoBarSeverity.Warning</code></td></tr><tr><td><code>.Error()</code></td><td><code>InfoBarSeverity.Error</code></td></tr></tbody></table>\n<p>The button fluents also overload <code>DropDownButton</code>, <code>SplitButton</code>, and\n<code>ToggleSplitButton</code>. Reach for these before <code>.Resources(...)</code> — they\navoid the six-key hover/pressed/disabled dance and stay in sync with\nsystem theme updates. See\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §2.1, §2.2, and\n§2.4 for the inventory.</p>\n<h2 id=\"lightweight-styling\">Lightweight styling<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lightweight-styling\">#</a></h2>\n<p><code>.Resources(...)</code> overrides WinUI control resource keys without\nreplacing the control template. VisualStateManager states (hover,\npressed, disabled) automatically respect your overrides:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LightweightStylingExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Button(<span class=\"hljs-string\">\"Default Button\"</span>, () => { }),\n            Button(<span class=\"hljs-string\">\"Accent Button\"</span>, () => { })\n                .Resources(r => r\n                    .Set(<span class=\"hljs-string\">\"ButtonBackground\"</span>, Theme.Accent)\n                    .Set(<span class=\"hljs-string\">\"ButtonBackgroundPointerOver\"</span>, Theme.AccentSecondary)\n                    .Set(<span class=\"hljs-string\">\"ButtonBackgroundPressed\"</span>, Theme.AccentTertiary)\n                    .Set(<span class=\"hljs-string\">\"ButtonForeground\"</span>, <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n                    .Set(<span class=\"hljs-string\">\"ButtonForegroundPointerOver\"</span>, <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n                    .Set(<span class=\"hljs-string\">\"ButtonForegroundPressed\"</span>, <span class=\"hljs-string\">\"#FFFFFF\"</span>)),\n            Button(<span class=\"hljs-string\">\"Danger Button\"</span>, () => { })\n                .Resources(r => r\n                    .Set(<span class=\"hljs-string\">\"ButtonBackground\"</span>, Theme.SystemCritical)\n                    .Set(<span class=\"hljs-string\">\"ButtonBackgroundPointerOver\"</span>, <span class=\"hljs-string\">\"#C42B1C\"</span>)\n                    .Set(<span class=\"hljs-string\">\"ButtonForeground\"</span>, <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n                    .Set(<span class=\"hljs-string\">\"ButtonForegroundPointerOver\"</span>, <span class=\"hljs-string\">\"#FFFFFF\"</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/styling/lightweight-styling.png\" alt=\"Lightweight styling\"></p>\n<p><code>ResourceBuilder</code> supports <code>.Set(key, ThemeRef)</code> for theme-reactive\noverrides, <code>.Set(key, string)</code> for hex colors, <code>.Set(key, double)</code> for\nnumeric values, and <code>.Set(key, CornerRadius)</code> for corner radius.\nOverrides cascade to child elements through WinUI's resource dictionary\nhierarchy.</p>\n<h2 id=\"custom-resource-access\">Custom resource access<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-resource-access\">#</a></h2>\n<p>For brushes Reactor hasn't surfaced as a typed property, <code>Theme.Ref()</code>\ntakes the WinUI resource key:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CustomResourceExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Using a named WinUI resource:\"</span>)\n                .Foreground(Theme.PrimaryText),\n            TextBlock(<span class=\"hljs-string\">\"NavigationViewItemForeground\"</span>)\n                .Foreground(Theme.Ref(<span class=\"hljs-string\">\"NavigationViewItemForeground\"</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The key must exist in WinUI's resource dictionaries. See the caveat\nabove for why typed accessors are preferred when one exists.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"lightdark-adaptive-ui\">Light/dark adaptive UI<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lightdark-adaptive-ui\">#</a></h3>\n<p>Most UI adapts automatically through tokens — the same code runs in\nboth themes because every color is a <code>ThemeRef</code>. The remaining 5% needs\nbranching: choosing different glyphs, switching to a darker\nhero image, running a different background animation. That's where\n<code>UseIsDarkTheme()</code> fits — branch on the hook, return different\nelements. The full token catalog and swatch reference lives in\n<a href=\"/theming-tokens/\">theming-tokens</a>.</p>\n<h3 id=\"brand-color-override-at-app-root\">Brand color override at app root<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#brand-color-override-at-app-root\">#</a></h3>\n<p>A single <code>.Resources(...)</code> block at the app root re-skins every\ndescendant's accent. The override flows through every descendant that\nresolves <code>AccentFillColorDefaultBrush</code> — buttons with <code>.AccentButton()</code>,\ntext with <code>Theme.Accent</code>, anywhere the brush is referenced:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Brand color override at app root — every descendant that resolves</span>\n<span class=\"hljs-comment\">// AccentFillColorDefaultBrush picks up the brand color in both themes.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BrandOverrideExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Brand color cascades through descendants\"</span>),\n            Button(<span class=\"hljs-string\">\"Save\"</span>, () => { }).AccentButton(),\n            TextBlock(<span class=\"hljs-string\">\"Accented text\"</span>).Foreground(Theme.AccentText).SemiBold()\n        ).Padding(<span class=\"hljs-number\">24</span>)\n         <span class=\"hljs-comment\">// One Resources override at the root re-skins every descendant.</span>\n         <span class=\"hljs-comment\">// Cross-theme: set in both ThemeDictionaries if light vs. dark</span>\n         <span class=\"hljs-comment\">// should pick different brand hues.</span>\n         .Resources(r => r\n            .Set(<span class=\"hljs-string\">\"AccentFillColorDefaultBrush\"</span>, <span class=\"hljs-string\">\"#7B61FF\"</span>)\n            .Set(<span class=\"hljs-string\">\"AccentTextFillColorPrimaryBrush\"</span>, <span class=\"hljs-string\">\"#7B61FF\"</span>));\n    }\n}\n</code></pre>\n<p>For full brand support, set the override in both light and dark theme\ndictionaries — the values may need to be different hues to stay\nlegible. Pair with <code>Theme.AccentText</code> overrides if the brand contrast\ndiffers from the system pair.</p>\n<h3 id=\"per-element-theme-override-scope\">Per-element theme override scope<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#per-element-theme-override-scope\">#</a></h3>\n<p>Pin one panel to a different scheme while the rest of the app follows\nthe system. The key is to apply <code>.RequestedTheme(...)</code> to the\n<strong>region root</strong> — the container whose children should resolve against\nthe override — rather than to a leaf element:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Per-element theme override scope — a single panel forced to Dark</span>\n<span class=\"hljs-comment\">// inside an otherwise Light app, without app-wide RequestedTheme.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ScopedThemeOverrideExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            SubHeading(<span class=\"hljs-string\">\"Default scheme\"</span>),\n            Border(VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Default scheme — follows the app theme.\"</span>)\n                    .Foreground(Theme.PrimaryText),\n                TextBlock(<span class=\"hljs-string\">\"Card stroke and background also follow.\"</span>)\n                    .Foreground(Theme.SecondaryText)\n            ).Padding(<span class=\"hljs-number\">16</span>)).Background(Theme.CardBackground)\n             .WithBorder(Theme.CardStroke, <span class=\"hljs-number\">1</span>).CornerRadius(<span class=\"hljs-number\">8</span>),\n\n            SubHeading(<span class=\"hljs-string\">\"Dark scope — bound to a region root\"</span>),\n            Border(VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"This subtree is always dark.\"</span>)\n                    .Foreground(Theme.PrimaryText),\n                TextBlock(<span class=\"hljs-string\">\"ThemeRef descendants resolve against the override.\"</span>)\n                    .Foreground(Theme.SecondaryText)\n            ).Padding(<span class=\"hljs-number\">16</span>)).Background(Theme.CardBackground)\n             .WithBorder(Theme.CardStroke, <span class=\"hljs-number\">1</span>).CornerRadius(<span class=\"hljs-number\">8</span>)\n             <span class=\"hljs-comment\">// Region root carries the override — leaf-level overrides</span>\n             <span class=\"hljs-comment\">// are the anti-pattern called out in Common Mistakes.</span>\n             .RequestedTheme(ElementTheme.Dark)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The overridden subtree's <code>ThemeRef</code> resolutions walk up to the nearest\nexplicit <code>RequestedTheme</code>, so cards, buttons, and text inside the\nregion all pick up the dark scheme. This is the right shape for a\npreview pane, a dark-mode-only video player, or a marketing surface\nembedded in an otherwise-light app.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"hardcoded-color-literals-where-a-token-exists\">Hardcoded color literals where a token exists<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hardcoded-color-literals-where-a-token-exists\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — frozen across themes.</span>\nTextBlock(<span class=\"hljs-string\">\"Subtitle\"</span>).Foreground(<span class=\"hljs-string\">\"#888888\"</span>);\n</code></pre>\n<p>The grey looks correct in light mode and washes out in dark mode.\n<code>Theme.SecondaryText</code> is the right token — the brush resolves to the\nright grey in both themes. The <code>REACTOR_THEME_001</code> analyzer flags hex\nliterals that match a known token's resolved value with a code-fix to\nthe typed accessor.</p>\n<h3 id=\"using-themeref-when-a-typed-accessor-exists\">Using <code>Theme.Ref</code> when a typed accessor exists<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-themeref-when-a-typed-accessor-exists\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — works today, fragile to renames.</span>\n.Background(Theme.Ref(<span class=\"hljs-string\">\"AccentFillColorDefaultBrush\"</span>))\n\n<span class=\"hljs-comment\">// Do — refactor-safe and discoverable.</span>\n.Background(Theme.Accent)\n</code></pre>\n<p>The typed accessor produces the same <code>ThemeRef</code> record-struct as the\nstring lookup — at render time they're identical. The difference is at\nauthoring time: a renamed or removed WinUI resource fails at compile\ntime against the typed accessor and silently returns <code>null</code> (system\ndefault) against <code>Theme.Ref</code>. See the caveat above for the full\nrationale.</p>\n<h3 id=\"setting-requestedtheme-on-a-leaf-element\">Setting <code>RequestedTheme</code> on a leaf element<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#setting-requestedtheme-on-a-leaf-element\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — the leaf's theme override doesn't cascade because there are</span>\n<span class=\"hljs-comment\">// no descendants to receive it. The button's own brushes resolve, but</span>\n<span class=\"hljs-comment\">// the surrounding panel and any sibling controls do not.</span>\nBorder(VStack(<span class=\"hljs-number\">8</span>,\n    Button(<span class=\"hljs-string\">\"Click\"</span>).RequestedTheme(ElementTheme.Dark),  <span class=\"hljs-comment\">// leaf override</span>\n    TextBlock(<span class=\"hljs-string\">\"Caption\"</span>)                                <span class=\"hljs-comment\">// unaffected</span>\n))\n</code></pre>\n<p><code>RequestedTheme</code> is a <code>FrameworkElement</code> property that flows <strong>down</strong>\nthe visual tree. Apply it to the region root — the highest element\nwhose descendants should resolve against the override — not to a single\nleaf. The right shape is the per-element scope pattern above.</p>\n<h3 id=\"reaching-for-set-for-theme-properties\">Reaching for <code>.Set</code> for theme properties<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-set-for-theme-properties\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — the modifier exists.</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, onSave).Set(c => c.RequestedTheme = ElementTheme.Dark);\n</code></pre>\n<p>The <code>.RequestedTheme(...)</code> modifier participates in property diffing —\nthe next render that removes the modifier walks the property back to\ndefault. <code>.Set</code> is imperative and one-way; the next render won't undo\nit. The <code>REACTOR_THEME_003</code> analyzer catches this specific case with a\ncode-fix to the modifier. See <a href=\"/advanced/\">advanced</a> for the broader\n<code>.Set</code> discussion.</p>\n<h2 id=\"roslyn-analyzers\">Roslyn analyzers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#roslyn-analyzers\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Analyzer</th><th>Severity</th><th>Flags</th></tr></thead><tbody><tr><td><code>REACTOR_THEME_001</code></td><td>Warning</td><td>Hard-coded color string where a <code>Theme.*</code> token exists.</td></tr><tr><td><code>REACTOR_THEME_002</code></td><td>Info</td><td><code>.Set()</code> brush assignment that has a lightweight-styling key equivalent.</td></tr><tr><td><code>REACTOR_THEME_003</code></td><td>Info</td><td><code>.Set(fe => fe.RequestedTheme = ...)</code> — use the <code>.RequestedTheme(...)</code> modifier.</td></tr></tbody></table>\n<p>Each analyzer ships a code-fix that converts to the preferred pattern.\nEnable them by referencing the <code>Reactor.Analyzers</code> project — they're on\nby default in projects created from <code>mur new</code>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Prefer typed tokens over <code>Theme.Ref(string)</code>.</strong> Typed accessors are\ncompile-checked, refactor-safe, and discoverable in autocomplete.\nReserve <code>Theme.Ref</code> for the rare brush Reactor hasn't surfaced yet.</p>\n<p><strong>Apply <code>.RequestedTheme()</code> to region roots, not leaves.</strong> The override\nflows down the visual tree, so a leaf override doesn't reach siblings.\nThe right shape is one <code>RequestedTheme</code> per scope, on the scope's\ncontainer.</p>\n<p><strong>Use <code>.Resources()</code> for control color overrides.</strong> Lightweight styling\npreserves hover/pressed/disabled states. <code>.Set</code> forces a single value\nand breaks those states.</p>\n<p><strong>Use <code>UseIsDarkTheme()</code> for conditional logic.</strong> When you need\ndifferent <strong>behavior</strong> based on theme (different icons, different\nlayouts), read the hook instead of inspecting resources manually.</p>\n<p><strong>Test in both themes.</strong> Run your app, switch Windows to dark mode,\nand verify nothing becomes unreadable. The token system handles this\nif you avoid hardcoded colors — the analyzer catches the obvious\nomissions.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/theming-tokens/\">Theming tokens</a></strong> — Next: full token catalog with light/dark swatches</li>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — VStack, HStack, Grid, and core containers for the surfaces you're styling</li>\n<li><strong><a href=\"/flex-layout/\">Flex Layout</a></strong> — flex containers for adaptive alignment and wrap</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — ensure themed colors meet WCAG contrast in both schemes</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — pair theme-aware brushes with transitions and <code>.InteractionStates(...)</code></li>\n<li><strong><a href=\"/components/\">Components</a></strong> — compose styled elements into reusable function or class components</li>\n<li><strong><a href=\"/advanced/\">Advanced</a></strong> — <code>.Set</code> escape hatch and the analyzer rules that catch its misuse</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Style . Styling in Microsoft.UI.Reactor (Reactor) is a layered modifier chain. The bottom layer is the WinUI theme system — every visible color is ultimately a brush resource that resolves against the effective Light / Dark / HighContrast theme dictionary at render time. The next layer up is ThemeRef , a record-struct reference to one of those resources by key. On top of that sit two surfaces: typed accessors like Theme.Accent , Theme.AccentText , Theme.CardBackground (canonical, autocompleted, refactor-safe) and Theme.Ref(\"AnyResourceKey\") for the long tail of WinUI brushes Reactor doesn't surface as a property. Above the tokens are the modifier shortcuts — .AccentButton() , .SubtleButton() , signal-severity fluents — that bake a small named-style choice into one call. Above those is lightweight styling via .Resources(r => r.Set(key, value)) , which overrides individual WinUI resource keys for a subtree without replacing the control template. The whole stack is theme-aware by default : tokens, modifier shortcuts, and Resource overrides all re-resolve when the effective theme flips. The escape hatches are literal hex strings ( \"#7B61FF\" ) and direct .Set(control => ...) property writes — both are theme-frozen by construction and are the right tool only for brand colors that should stay constant regardless of mode. Styling and Theming # WinUI reference: For the underlying brush resources, the Fluent design language, and the full styling surface, see Design Windows apps overview on Microsoft Learn . At-a-glance reference # API Layer Use when Theme.Accent , Theme.PrimaryText , etc. Typed token Always — these are the canonical brush references. Theme.Ref(\"AnyResourceKey\") String token The brush isn't surfaced as a typed accessor (rare). .Background(token) / .Foreground(token) Color modifier Apply a token to any element. .Background(\"#RRGGBB\") Color modifier Brand colors that must stay constant across themes. .AccentButton() , .SubtleButton() , .TextLink() Named-style fluent The four canonical button shapes. No .Resources needed. .Informational() / .Success() / .Warning() / .Error() Named-style fluent InfoBar severity. .Resources(r => r.Set(key, value)) Lightweight styling Override individual WinUI resource keys for a subtree. .RequestedTheme(ElementTheme.Dark) Region root Pin a subtree to a specific scheme. UseColorScheme() / UseIsDarkTheme() Hook Branch logic (not just colors) on the effective theme. .Set(control => control.Foreground = ...) Escape hatch The control has a property neither tokens nor .Resources reach. Theme tokens # Apply a typed token with .Background() or .Foreground() : class ThemeTokensExample : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Primary Text\" ).Foreground(Theme.PrimaryText), TextBlock( \"Secondary Text\" ).Foreground(Theme.SecondaryText), TextBlock( \"Accent Text\" ).Foreground(Theme.AccentText).SemiBold(), TextBlock( \"On Accent Background\" ) .Foreground( \"#FFFFFF\" ) .Padding(horizontal: 8 , vertical: 4 ) .Background(Theme.Accent) .CornerRadius( 4 ) ).Padding( 24 ); } } Each token resolves to a WinUI brush at render time. When the user switches between light and dark mode, every element bound to a ThemeRef updates automatically — no manual rebinding. The full token catalog with light/dark swatches lives in theming-tokens . Building cards # A card is a Border with Theme.CardBackground , rounded corners, padding, and Theme.CardStroke . Combine those modifiers with layout containers for reusable card shapes: class CardLayoutExample : Component { public override Element Render () { return VStack( 16 , Heading( \"Dashboard\" ), HStack( 12 , Card( \"Users\" , \"1,204\" , Theme.Accent), Card( \"Revenue\" , \"$48.2k\" , Theme.SystemSuccess), Card( \"Errors\" , \"3\" , Theme.SystemCritical) ) ).Padding( 24 ); } static Element Card ( string title, string value , ThemeRef accent ) => Border( VStack( 8 , Caption(title).Foreground(Theme.SecondaryText), TextBlock( value ).FontSize( 28 ).Bold().Foreground(accent) ).Padding( 16 ) ).Background(Theme.CardBackground) .CornerRadius( 8 ) .WithBorder(Theme.CardStroke, 1 ) .Width( 160 ); } Theme.CardBackground is the standard WinUI card surface; Theme.CardStroke is the matching border. Together they produce a card that looks native in both light and dark mode. Color modifiers # .Background() and .Foreground() accept three input shapes: class ColorModifiersExample : Component { public override Element Render () { return VStack( 8 , TextBlock( \"Theme token\" ).Background(Theme.SubtleFill).Padding( 8 ), TextBlock( \"Hex string\" ).Background( \"#E8F5E9\" ).Padding( 8 ), TextBlock( \"Mixed\" ).Foreground(Theme.PrimaryText) .Background( \"#1E1E2E\" ).Padding( 8 ) ).Padding( 24 ); } } Overload Example Theme-aware? Typed token .Background(Theme.Accent) yes Theme.Ref .Background(Theme.Ref(\"MyCustomBrush\")) yes Hex string .Background(\"#FF5733\") no — frozen Windows.UI.Color .Background(Colors.Blue) no — frozen Tokens are the right default. Reserve hex and Color for brand colors that should stay constant regardless of mode. Signal colors # Status indicators use semantic signal colors: class SignalColorsExample : Component { public override Element Render () { return HStack( 12 , Badge( \"Info\" , Theme.SystemAttention), Badge( \"Success\" , Theme.SystemSuccess), Badge( \"Warning\" , Theme.SystemCaution), Badge( \"Error\" , Theme.SystemCritical) ).Padding( 24 ); } static Element Badge ( string label, ThemeRef color ) => TextBlock(label) .FontSize( 12 ).SemiBold() .Foreground(color) .Padding(horizontal: 8 , vertical: 4 ) .Background(Theme.SubtleFill) .CornerRadius( 4 ); } Use these instead of hardcoded red/green/yellow — they meet accessibility contrast requirements in both themes and tone-shift correctly under HighContrast . Caveat: Theme.Ref(\"SomeResourceKey\") resolves through string lookup at every render, walking the merged-dictionary chain to find the brush; the typed accessors ( Theme.Accent , Theme.AccentText , …) construct the same ThemeRef record-struct with the canonical key string. The typed-accessor path is the supported one for two reasons: (1) renaming a WinUI resource is a compile error against typed accessors and a runtime null brush against Theme.Ref — the latter silently falls through to the system default; (2) the REACTOR_THEME_001 analyzer treats hex strings inside .Background(...) as warnings only when the hex matches a known token value, so .Background(Theme.Ref(\"Foo\")) is invisible to the analyzer when Foo doesn't exist as a typed accessor. The rule: if a typed Theme.X exists, use it. Reserve Theme.Ref(...) for the long-tail brushes Reactor hasn't surfaced yet, and prefer filing a feature request to add the accessor over leaving a string reference in the codebase. Dark and light mode # .RequestedTheme(ElementTheme.Dark) pins a subtree to a specific theme. UseColorScheme() and UseIsDarkTheme() let you read the effective theme reactively: class DarkLightToggleExample : Component { public override Element Render () { var (isDark, setIsDark) = UseState( false ); var theme = isDark ? ElementTheme.Dark : ElementTheme.Light; return VStack( 16 , ToggleSwitch(isDark, setIsDark, onContent: \"Dark\" , offContent: \"Light\" ), Border( VStack( 12 , TextBlock( \"This panel follows the toggle.\" ).Foreground(Theme.PrimaryText), TextBlock( \"Background adapts automatically.\" ).Foreground(Theme.SecondaryText) ).Padding( 16 ) ).Background(Theme.CardBackground) .CornerRadius( 8 ) .RequestedTheme(theme) ).Padding( 24 ); } } .RequestedTheme(...) sets the property on the underlying FrameworkElement . All ThemeRef bindings in descendants resolve against the new scheme. UseIsDarkTheme() returns true when the effective scheme is dark — branch on it when you need different behavior (different icons, different layouts), not just different colors. Reactive theme hooks # UseColorScheme() returns the effective scheme at the component's position in the tree. UseIsDarkTheme() is a convenience wrapper that narrows it to a boolean: class ColorSchemeHookExample : Component { public override Element Render () { var isDark = UseIsDarkTheme(); var scheme = UseColorScheme(); return VStack( 12 , TextBlock( $\"Color scheme: {scheme} \" ).FontSize( 16 ).SemiBold(), TextBlock(isDark ? \"Dark mode is active\" : \"Light mode is active\" ) .Foreground(Theme.SecondaryText), Border( TextBlock(isDark ? \"Dark content\" : \"Light content\" ) .Padding( 12 ) ).Background(Theme.CardBackground) .CornerRadius( 8 ) .WithBorder(Theme.CardStroke, 1 ) ).Padding( 24 ); } } ColorScheme has three values: Light , Dark , and HighContrast . Use the hook when you need to branch logic (not just colors) on the theme — choosing different icon assets, picking a different layout density, or running different analytics tags. Named-style fluents # Reactor exposes the canonical WinUI named styles as fluent shortcuts. The common cases never need a .Resources(...) block: class NamedStylesExample : Component { public override Element Render () { return VStack( 12 , HStack( 8 , Button( \"Save\" , () => { }).AccentButton(), Button( \"Cancel\" , () => { }).SubtleButton(), HyperlinkButton( \"Learn more\" , new Uri( \"https://example.com\" )) .TextLink() ), InfoBar(title: \"Heads up\" , message: \"Backups run nightly.\" ) .Informational(), InfoBar(title: \"Saved\" , message: \"All changes are persisted.\" ) .Success(), InfoBar(title: \"Almost full\" , message: \"75% of quota used.\" ) .Warning(), InfoBar(title: \"Sync failed\" , message: \"Check your network.\" ) .Error() ).Padding( 24 ); } } Fluent Maps to .AccentButton() AccentButtonStyle — filled accent color .SubtleButton() SubtleButtonStyle — text-only until hovered .TextLink() Hyperlink-style button (no chrome) .Informational() InfoBarSeverity.Informational .Success() InfoBarSeverity.Success .Warning() InfoBarSeverity.Warning .Error() InfoBarSeverity.Error The button fluents also overload DropDownButton , SplitButton , and ToggleSplitButton . Reach for these before .Resources(...) — they avoid the six-key hover/pressed/disabled dance and stay in sync with system theme updates. See spec 039 §2.1, §2.2, and §2.4 for the inventory. Lightweight styling # .Resources(...) overrides WinUI control resource keys without replacing the control template. VisualStateManager states (hover, pressed, disabled) automatically respect your overrides: class LightweightStylingExample : Component { public override Element Render () { return VStack( 12 , Button( \"Default Button\" , () => { }), Button( \"Accent Button\" , () => { }) .Resources(r => r .Set( \"ButtonBackground\" , Theme.Accent) .Set( \"ButtonBackgroundPointerOver\" , Theme.AccentSecondary) .Set( \"ButtonBackgroundPressed\" , Theme.AccentTertiary) .Set( \"ButtonForeground\" , \"#FFFFFF\" ) .Set( \"ButtonForegroundPointerOver\" , \"#FFFFFF\" ) .Set( \"ButtonForegroundPressed\" , \"#FFFFFF\" )), Button( \"Danger Button\" , () => { }) .Resources(r => r .Set( \"ButtonBackground\" , Theme.SystemCritical) .Set( \"ButtonBackgroundPointerOver\" , \"#C42B1C\" ) .Set( \"ButtonForeground\" , \"#FFFFFF\" ) .Set( \"ButtonForegroundPointerOver\" , \"#FFFFFF\" )) ).Padding( 24 ); } } ResourceBuilder supports .Set(key, ThemeRef) for theme-reactive overrides, .Set(key, string) for hex colors, .Set(key, double) for numeric values, and .Set(key, CornerRadius) for corner radius. Overrides cascade to child elements through WinUI's resource dictionary hierarchy. Custom resource access # For brushes Reactor hasn't surfaced as a typed property, Theme.Ref() takes the WinUI resource key: class CustomResourceExample : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Using a named WinUI resource:\" ) .Foreground(Theme.PrimaryText), TextBlock( \"NavigationViewItemForeground\" ) .Foreground(Theme.Ref( \"NavigationViewItemForeground\" )) ).Padding( 24 ); } } The key must exist in WinUI's resource dictionaries. See the caveat above for why typed accessors are preferred when one exists. Patterns # Light/dark adaptive UI # Most UI adapts automatically through tokens — the same code runs in both themes because every color is a ThemeRef . The remaining 5% needs branching: choosing different glyphs, switching to a darker hero image, running a different background animation. That's where UseIsDarkTheme() fits — branch on the hook, return different elements. The full token catalog and swatch reference lives in theming-tokens . Brand color override at app root # A single .Resources(...) block at the app root re-skins every descendant's accent. The override flows through every descendant that resolves AccentFillColorDefaultBrush — buttons with .AccentButton() , text with Theme.Accent , anywhere the brush is referenced: // Brand color override at app root — every descendant that resolves // AccentFillColorDefaultBrush picks up the brand color in both themes. class BrandOverrideExample : Component { public override Element Render () { return VStack( 12 , SubHeading( \"Brand color cascades through descendants\" ), Button( \"Save\" , () => { }).AccentButton(), TextBlock( \"Accented text\" ).Foreground(Theme.AccentText).SemiBold() ).Padding( 24 ) // One Resources override at the root re-skins every descendant. // Cross-theme: set in both ThemeDictionaries if light vs. dark // should pick different brand hues. .Resources(r => r .Set( \"AccentFillColorDefaultBrush\" , \"#7B61FF\" ) .Set( \"AccentTextFillColorPrimaryBrush\" , \"#7B61FF\" )); } } For full brand support, set the override in both light and dark theme dictionaries — the values may need to be different hues to stay legible. Pair with Theme.AccentText overrides if the brand contrast differs from the system pair. Per-element theme override scope # Pin one panel to a different scheme while the rest of the app follows the system. The key is to apply .RequestedTheme(...) to the region root — the container whose children should resolve against the override — rather than to a leaf element: // Per-element theme override scope — a single panel forced to Dark // inside an otherwise Light app, without app-wide RequestedTheme. class ScopedThemeOverrideExample : Component { public override Element Render () { return VStack( 16 , SubHeading( \"Default scheme\" ), Border(VStack( 8 , TextBlock( \"Default scheme — follows the app theme.\" ) .Foreground(Theme.PrimaryText), TextBlock( \"Card stroke and background also follow.\" ) .Foreground(Theme.SecondaryText) ).Padding( 16 )).Background(Theme.CardBackground) .WithBorder(Theme.CardStroke, 1 ).CornerRadius( 8 ), SubHeading( \"Dark scope — bound to a region root\" ), Border(VStack( 8 , TextBlock( \"This subtree is always dark.\" ) .Foreground(Theme.PrimaryText), TextBlock( \"ThemeRef descendants resolve against the override.\" ) .Foreground(Theme.SecondaryText) ).Padding( 16 )).Background(Theme.CardBackground) .WithBorder(Theme.CardStroke, 1 ).CornerRadius( 8 ) // Region root carries the override — leaf-level overrides // are the anti-pattern called out in Common Mistakes. .RequestedTheme(ElementTheme.Dark) ).Padding( 24 ); } } The overridden subtree's ThemeRef resolutions walk up to the nearest explicit RequestedTheme , so cards, buttons, and text inside the region all pick up the dark scheme. This is the right shape for a preview pane, a dark-mode-only video player, or a marketing surface embedded in an otherwise-light app. Common Mistakes # Hardcoded color literals where a token exists # // Don't — frozen across themes. TextBlock( \"Subtitle\" ).Foreground( \"#888888\" ); The grey looks correct in light mode and washes out in dark mode. Theme.SecondaryText is the right token — the brush resolves to the right grey in both themes. The REACTOR_THEME_001 analyzer flags hex literals that match a known token's resolved value with a code-fix to the typed accessor. Using Theme.Ref when a typed accessor exists # // Don't — works today, fragile to renames. .Background(Theme.Ref( \"AccentFillColorDefaultBrush\" )) // Do — refactor-safe and discoverable. .Background(Theme.Accent) The typed accessor produces the same ThemeRef record-struct as the string lookup — at render time they're identical. The difference is at authoring time: a renamed or removed WinUI resource fails at compile time against the typed accessor and silently returns null (system default) against Theme.Ref . See the caveat above for the full rationale. Setting RequestedTheme on a leaf element # // Don't — the leaf's theme override doesn't cascade because there are // no descendants to receive it. The button's own brushes resolve, but // the surrounding panel and any sibling controls do not. Border(VStack( 8 , Button( \"Click\" ).RequestedTheme(ElementTheme.Dark), // leaf override TextBlock( \"Caption\" ) // unaffected )) RequestedTheme is a FrameworkElement property that flows down the visual tree. Apply it to the region root — the highest element whose descendants should resolve against the override — not to a single leaf. The right shape is the per-element scope pattern above. Reaching for .Set for theme properties # // Don't — the modifier exists. Button( \"Save\" , onSave).Set(c => c.RequestedTheme = ElementTheme.Dark); The .RequestedTheme(...) modifier participates in property diffing — the next render that removes the modifier walks the property back to default. .Set is imperative and one-way; the next render won't undo it. The REACTOR_THEME_003 analyzer catches this specific case with a code-fix to the modifier. See advanced for the broader .Set discussion. Roslyn analyzers # Analyzer Severity Flags REACTOR_THEME_001 Warning Hard-coded color string where a Theme.* token exists. REACTOR_THEME_002 Info .Set() brush assignment that has a lightweight-styling key equivalent. REACTOR_THEME_003 Info .Set(fe => fe.RequestedTheme = ...) — use the .RequestedTheme(...) modifier. Each analyzer ships a code-fix that converts to the preferred pattern. Enable them by referencing the Reactor.Analyzers project — they're on by default in projects created from mur new . Tips # Prefer typed tokens over Theme.Ref(string) . Typed accessors are compile-checked, refactor-safe, and discoverable in autocomplete. Reserve Theme.Ref for the rare brush Reactor hasn't surfaced yet. Apply .RequestedTheme() to region roots, not leaves. The override flows down the visual tree, so a leaf override doesn't reach siblings. The right shape is one RequestedTheme per scope, on the scope's container. Use .Resources() for control color overrides. Lightweight styling preserves hover/pressed/disabled states. .Set forces a single value and breaks those states. Use UseIsDarkTheme() for conditional logic. When you need different behavior based on theme (different icons, different layouts), read the hook instead of inspecting resources manually. Test in both themes. Run your app, switch Windows to dark mode, and verify nothing becomes unreadable. The token system handles this if you avoid hardcoded colors — the analyzer catches the obvious omissions. Next Steps # Theming tokens — Next: full token catalog with light/dark swatches Layout — VStack, HStack, Grid, and core containers for the surfaces you're styling Flex Layout — flex containers for adaptive alignment and wrap Accessibility — ensure themed colors meet WCAG contrast in both schemes Animation — pair theme-aware brushes with transitions and .InteractionStates(...) Components — compose styled elements into reusable function or class components Advanced — .Set escape hatch and the analyzer rules that catch its misuse",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/styling.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/styling.md",
      "headings": [
        {
          "depth": 2,
          "id": "at-a-glance-reference",
          "text": "At-a-glance reference"
        },
        {
          "depth": 2,
          "id": "theme-tokens",
          "text": "Theme tokens"
        },
        {
          "depth": 2,
          "id": "building-cards",
          "text": "Building cards"
        },
        {
          "depth": 2,
          "id": "color-modifiers",
          "text": "Color modifiers"
        },
        {
          "depth": 2,
          "id": "signal-colors",
          "text": "Signal colors"
        },
        {
          "depth": 2,
          "id": "dark-and-light-mode",
          "text": "Dark and light mode"
        },
        {
          "depth": 2,
          "id": "reactive-theme-hooks",
          "text": "Reactive theme hooks"
        },
        {
          "depth": 2,
          "id": "named-style-fluents",
          "text": "Named-style fluents"
        },
        {
          "depth": 2,
          "id": "lightweight-styling",
          "text": "Lightweight styling"
        },
        {
          "depth": 2,
          "id": "custom-resource-access",
          "text": "Custom resource access"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "lightdark-adaptive-ui",
          "text": "Light/dark adaptive UI"
        },
        {
          "depth": 3,
          "id": "brand-color-override-at-app-root",
          "text": "Brand color override at app root"
        },
        {
          "depth": 3,
          "id": "per-element-theme-override-scope",
          "text": "Per-element theme override scope"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "hardcoded-color-literals-where-a-token-exists",
          "text": "Hardcoded color literals where a token exists"
        },
        {
          "depth": 3,
          "id": "using-themeref-when-a-typed-accessor-exists",
          "text": "Using Theme.Ref when a typed accessor exists"
        },
        {
          "depth": 3,
          "id": "setting-requestedtheme-on-a-leaf-element",
          "text": "Setting RequestedTheme on a leaf element"
        },
        {
          "depth": 3,
          "id": "reaching-for-set-for-theme-properties",
          "text": "Reaching for .Set for theme properties"
        },
        {
          "depth": 2,
          "id": "roslyn-analyzers",
          "text": "Roslyn analyzers"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Animation",
      "path": "animation.md",
      "slug": "/animation/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/motion/\">Motion</a>.</p>\n</blockquote>\n<p>Microsoft.UI.Reactor (Reactor) exposes four animation systems and one rule about which to pick.\nThe rule first: the <strong>compositor</strong> can only animate five properties —\nOpacity, Offset (Translation), Scale, Rotation, and CenterPoint — and\nwhen it does, the managed render thread is not involved and the\nanimation runs at display refresh on the GPU. Everything that animates\nthose five properties (implicit transitions like <code>.OpacityTransition()</code>,\n<code>.Animate()</code> for persistent implicit, <code>.WithAnimation()</code> for an event-\nscoped batch, <code>.Transition()</code> for enter/exit, <code>.InteractionStates()</code>\nfor hover/press/focus, <code>.Keyframes()</code> for multi-step) is the same\nunderlying pipeline at different ergonomics. The other three systems\nexist for things outside that ceiling: <code>.LayoutAnimation()</code> animates\nposition changes from layout reflow (a sibling was added, the panel\nre-measured), <code>.ConnectedAnimation()</code> snapshots an element on unmount\nand plays the snapshot into the matching element on mount, and the\nWinUI <code>Storyboard</code> (reachable through <code>.Set(...)</code>) covers the rare\nproperties none of the above can touch. Read <a href=\"#animate-modifier\">Compositor animation</a>\nfirst if you're picking between <code>.Animate()</code> and <code>.WithAnimation()</code>,\nor <a href=\"#choreography\">Choreography</a> if you're trying to sequence steps.</p>\n<h1 id=\"animation\">Animation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animation\">#</a></h1>\n<p>Reactor animations are declarative. You set the target value (opacity, scale,\ntranslation) and attach a transition modifier. When the value changes on the\nnext render — driven by <a href=\"/hooks/\">hooks</a> and state — WinUI animates from\nthe old value to the new one automatically.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Animates</th><th>Trigger</th><th>Use when</th></tr></thead><tbody><tr><td><code>.OpacityTransition(duration?)</code></td><td>Opacity</td><td>Implicit, on every change</td><td>Show/hide a single element.</td></tr><tr><td><code>.ScaleTransition(transition?)</code></td><td>Scale</td><td>Implicit, on every change</td><td>Element resize feedback.</td></tr><tr><td><code>.TranslationTransition(transition?)</code></td><td>Translation (offset)</td><td>Implicit, on every change</td><td>Slide a single element.</td></tr><tr><td><code>.RotationTransition(duration?)</code></td><td>Rotation</td><td>Implicit, on every change</td><td>Spin a single element.</td></tr><tr><td><code>.BackgroundTransition(duration?)</code></td><td>Background brush</td><td>Implicit; <strong>panels only</strong></td><td>Color transitions on Grid / Stack.</td></tr><tr><td><code>.Animate(curve, props?)</code></td><td>Any compositor property</td><td>Implicit, persistent</td><td>One curve for every change to this element.</td></tr><tr><td><code>AnimationScope.WithAnimation(curve, action)</code></td><td>Compositor properties changed inside the action</td><td>Event-scoped</td><td>One state change drives a batched animation.</td></tr><tr><td><code>.Transition(t, curve?)</code></td><td>Enter / exit</td><td>When element enters or leaves the tree</td><td>Animate <code>When(...)</code> / ternary mount and unmount.</td></tr><tr><td><code>.InteractionStates(builder, curve?)</code></td><td>Compositor properties per state</td><td>Hover / press / focus</td><td>Zero-reconcile pointer-state feedback.</td></tr><tr><td><code>.Keyframes(name, trigger, builder)</code></td><td>Compositor properties at progress points</td><td>When <code>trigger</code> changes</td><td>Multi-step animation (pulse, shake, breathe).</td></tr><tr><td><code>.Stagger(delay, curve?)</code></td><td>Container's children</td><td>Sibling cascade</td><td>Cascade enter/exit and layout animations across a list.</td></tr><tr><td><code>.LayoutAnimation(duration?)</code> / <code>.SpringLayoutAnimation(...)</code></td><td>Element position from layout reflow</td><td>Layout pass</td><td>Animate position changes a sibling caused.</td></tr><tr><td><code>.ConnectedAnimation(key)</code></td><td>Snapshot from old position to new</td><td>On unmount/mount of the same key</td><td>List-to-detail hero animations.</td></tr><tr><td><code>AnimationScope.WithAnimationAsync(curve, action)</code></td><td>Compositor properties changed inside the action</td><td>Event-scoped, returns Task</td><td>Sequence steps with <code>await</code>.</td></tr></tbody></table>\n<h2 id=\"opacity-transition\">Opacity Transition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#opacity-transition\">#</a></h2>\n<p><code>.OpacityTransition()</code> animates opacity changes. Set <code>.Opacity()</code> to your\ntarget value and the transition handles the rest:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">OpacityDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (visible, setVisible) = UseState(<span class=\"hljs-literal\">true</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Opacity Transition\"</span>),\n            Button(visible ? <span class=\"hljs-string\">\"Fade Out\"</span> : <span class=\"hljs-string\">\"Fade In\"</span>,\n                () => setVisible(!visible)),\n            TextBlock(<span class=\"hljs-string\">\"This text fades in and out\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).Bold()\n                .Opacity(visible ? <span class=\"hljs-number\">1.0</span> : <span class=\"hljs-number\">0.0</span>)\n                .OpacityTransition(TimeSpan.FromMilliseconds(<span class=\"hljs-number\">500</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/opacity-transition.png\" alt=\"Opacity transition\"></p>\n<p>The optional <code>TimeSpan</code> parameter controls duration. The default is 300ms.\nUse this for fade-in/fade-out on showing and hiding elements.</p>\n<h2 id=\"scale-transition\">Scale Transition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#scale-transition\">#</a></h2>\n<p><code>.ScaleTransition()</code> animates scale changes. Set <code>.Scale()</code> to the target\nfactor:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ScaleDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (enlarged, setEnlarged) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Scale Transition\"</span>),\n            Button(enlarged ? <span class=\"hljs-string\">\"Shrink\"</span> : <span class=\"hljs-string\">\"Enlarge\"</span>,\n                () => setEnlarged(!enlarged)),\n            Border(\n                TextBlock(<span class=\"hljs-string\">\"Scales up and down\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Bold()\n            ).Padding(<span class=\"hljs-number\">12</span>)\n             .CornerRadius(<span class=\"hljs-number\">8</span>)\n             .Background(<span class=\"hljs-string\">\"#e8e8e8\"</span>)\n             .ScaleTransition()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/scale-transition.png\" alt=\"Scale transition\"></p>\n<p>Scale uses the element's center as the transform origin. A value of <code>1.0f</code> is\nnormal size, <code>1.5f</code> is 150%. You can pass a custom <code>Vector3Transition</code> to\ncontrol which axes animate.</p>\n<h2 id=\"translation-transition\">Translation Transition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#translation-transition\">#</a></h2>\n<p><code>.TranslationTransition()</code> animates position offsets. Set <code>.Translation()</code>\nto the target X, Y, Z offset in pixels:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TranslationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (moved, setMoved) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Translation Transition\"</span>),\n            Button(moved ? <span class=\"hljs-string\">\"Slide Back\"</span> : <span class=\"hljs-string\">\"Slide Right\"</span>,\n                () => setMoved(!moved)),\n            TextBlock(<span class=\"hljs-string\">\"Slides horizontally\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).Bold()\n                .Translation(moved ? <span class=\"hljs-number\">120f</span> : <span class=\"hljs-number\">0f</span>, <span class=\"hljs-number\">0f</span>, <span class=\"hljs-number\">0f</span>)\n                .TranslationTransition()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/translation-transition.png\" alt=\"Translation transition\"></p>\n<p>Translation offsets are relative to the element's layout position. Positive X\nmoves right, positive Y moves down. The element still occupies its original\nlayout space — only the visual position changes.</p>\n<h2 id=\"background-transition\">Background Transition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#background-transition\">#</a></h2>\n<p><code>.BackgroundTransition()</code> animates background color changes on <code>VStack</code>,\n<code>HStack</code>, and <code>Grid</code> elements:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BackgroundDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (warm, setWarm) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Background Transition\"</span>),\n            Button(warm ? <span class=\"hljs-string\">\"Cool Colors\"</span> : <span class=\"hljs-string\">\"Warm Colors\"</span>,\n                () => setWarm(!warm)),\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Background animates between colors\"</span>)\n                    .Foreground(<span class=\"hljs-string\">\"#ffffff\"</span>).Bold()\n            ).Padding(<span class=\"hljs-number\">16</span>)\n             .CornerRadius(<span class=\"hljs-number\">8</span>)\n             .Background(warm ? <span class=\"hljs-string\">\"#da3b01\"</span> : <span class=\"hljs-string\">\"#0078d4\"</span>)\n             .BackgroundTransition(TimeSpan.FromMilliseconds(<span class=\"hljs-number\">600</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/background-transition.png\" alt=\"Background transition\"></p>\n<p>Background transitions use WinUI's <code>BrushTransition</code>. They only work on\npanel elements (<code>StackPanel</code>, <code>Grid</code>) because WinUI restricts\n<code>BackgroundTransition</code> to those types.</p>\n<h2 id=\"combining-transitions\">Combining Transitions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#combining-transitions\">#</a></h2>\n<p>You can chain multiple transition modifiers on a single element. Each\nproperty animates independently:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CombinedDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (active, setActive) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Combined Transitions\"</span>),\n            Button(active ? <span class=\"hljs-string\">\"Reset\"</span> : <span class=\"hljs-string\">\"Animate\"</span>,\n                () => setActive(!active)),\n            Border(\n                TextBlock(<span class=\"hljs-string\">\"All at once\"</span>).FontSize(<span class=\"hljs-number\">16</span>).Bold()\n                    .Foreground(<span class=\"hljs-string\">\"#ffffff\"</span>)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n             .CornerRadius(<span class=\"hljs-number\">8</span>)\n             .Background(<span class=\"hljs-string\">\"#7b2ab5\"</span>)\n             .Opacity(active ? <span class=\"hljs-number\">1.0</span> : <span class=\"hljs-number\">0.4</span>)\n             .Translation(active ? <span class=\"hljs-number\">40f</span> : <span class=\"hljs-number\">0f</span>, <span class=\"hljs-number\">0f</span>, <span class=\"hljs-number\">0f</span>)\n             .OpacityTransition(TimeSpan.FromMilliseconds(<span class=\"hljs-number\">400</span>))\n             .TranslationTransition()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/combined-transitions.png\" alt=\"Combined transitions\"></p>\n<p>Each transition modifier is independent — <code>.OpacityTransition()</code> animates\nopacity while <code>.ScaleTransition()</code> animates scale simultaneously. Set the\ntarget values (<code>.Opacity()</code>, <code>.Scale()</code>, <code>.Translation()</code>) and the\ntransitions handle the animation for each property in parallel.</p>\n<h2 id=\"layout-animation\">Layout Animation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#layout-animation\">#</a></h2>\n<p><code>.LayoutAnimation()</code> animates elements when their position changes due to\nlayout reflow — items entering, leaving, or reordering in a <a href=\"/collections/\">collection</a>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LayoutAnimationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(\n            <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>> { <span class=\"hljs-string\">\"Apple\"</span>, <span class=\"hljs-string\">\"Banana\"</span>, <span class=\"hljs-string\">\"Cherry\"</span> });\n        <span class=\"hljs-keyword\">var</span> nextId = UseRef(<span class=\"hljs-number\">3</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Layout Animation\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Add Item\"</span>, () => {\n                    nextId.Current++;\n                    updateItems(l => [.. l, <span class=\"hljs-string\">$\"Item <span class=\"hljs-subst\">{nextId.Current}</span>\"</span>]);\n                }),\n                Button(<span class=\"hljs-string\">\"Remove Last\"</span>, () => updateItems(l =>\n                    l.Count > <span class=\"hljs-number\">0</span> ? l.Take(l.Count - <span class=\"hljs-number\">1</span>).ToList() : l))\n            ),\n            VStack(<span class=\"hljs-number\">4</span>, items.Select(item =>\n                TextBlock(item).Padding(horizontal: <span class=\"hljs-number\">8</span>, vertical: <span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#f0f0f0\"</span>)\n                    .CornerRadius(<span class=\"hljs-number\">4</span>).LayoutAnimation()\n                    .WithKey(<span class=\"hljs-string\">$\"item-<span class=\"hljs-subst\">{item}</span>\"</span>)\n            ).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/layout-animation.png\" alt=\"Layout animation\"></p>\n<p>Layout animation works at the Composition layer. When WinUI repositions an\nelement (e.g., a sibling is added or removed), Reactor animates from the old\nposition to the new one. Use <code>.WithKey()</code> on each element so the reconciler\ncan track identity across reorders.</p>\n<p>You can also use <code>.LayoutAnimation(TimeSpan)</code> for a custom duration or\n<code>.SpringLayoutAnimation()</code> for a bouncy feel.</p>\n<h2 id=\"connected-animation\">Connected Animation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#connected-animation\">#</a></h2>\n<p><code>.ConnectedAnimation(key)</code> creates a visual continuity effect between two\nviews. When an element with a key is unmounted and another with the same key\nis mounted, WinUI animates a snapshot from the old position to the new one:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ConnectedAnimationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (selected, setSelected) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n\n        <span class=\"hljs-keyword\">if</span> (selected <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n                Button(<span class=\"hljs-string\">\"Back to list\"</span>, () => setSelected(<span class=\"hljs-literal\">null</span>)),\n                TextBlock(selected)\n                    .FontSize(<span class=\"hljs-number\">28</span>).Bold()\n                    .ConnectedAnimation(<span class=\"hljs-string\">$\"title-<span class=\"hljs-subst\">{selected}</span>\"</span>)\n            ).Padding(<span class=\"hljs-number\">24</span>);\n\n        <span class=\"hljs-keyword\">var</span> items = <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"Photos\"</span>, <span class=\"hljs-string\">\"Music\"</span>, <span class=\"hljs-string\">\"Videos\"</span> };\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Connected Animation\"</span>),\n            VStack(<span class=\"hljs-number\">4</span>,\n                items.Select(item =>\n                    Button(item, () => setSelected(item))\n                        .ConnectedAnimation(<span class=\"hljs-string\">$\"title-<span class=\"hljs-subst\">{item}</span>\"</span>)\n                ).ToArray()\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/connected-animation.png\" alt=\"Connected animation\"></p>\n<p>Both the source and destination elements must use the same key string. The\nanimation runs automatically when the reconciler detects the transition.\nUse connected animations for list-to-detail <a href=\"/navigation/\">navigation</a>\nwhere an element \"flies\" from the list into the detail view.</p>\n<h2 id=\"transactional-animation--animationsanimate\">Transactional animation — <code>Animations.Animate(...)</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#transactional-animation--animationsanimate\">#</a></h2>\n<p><code>Animations.Animate(kind, action)</code> is Reactor's SwiftUI-style transactional\nanimation primitive. Wrap a state mutation, and any <strong>structural</strong> change to\na keyed list (insert, move, remove) that comes out of that mutation picks up\nthe kind — without a per-element modifier in sight.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TodoList</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, setItems) = UseState&#x3C;IReadOnlyList&#x3C;Todo>>(_seed);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Button(<span class=\"hljs-string\">\"Add\"</span>, () =>\n                Animations.Animate(AnimationKind.Spring, () =>\n                    setItems([.. items, <span class=\"hljs-keyword\">new</span> Todo(Guid.NewGuid().ToString(), <span class=\"hljs-string\">\"New\"</span>)]))),\n            ListView&#x3C;Todo>(items, (t, _) => TextBlock(t.Title).Padding(<span class=\"hljs-number\">8</span>))\n                .Height(<span class=\"hljs-number\">400</span>)\n        );\n    }\n}\n</code></pre>\n<p><code>AnimationKind</code> is the declarative knob — <code>Spring</code>, <code>EaseIn</code>, <code>EaseOut</code>,\n<code>EaseInOut</code>, <code>Default</code>, or <code>None</code>. The kind flows through an <code>AsyncLocal</code>\nambient: a setter invoked inside <code>Animate</code> snapshots the ambient before\nqueuing the render, and the reconciler re-pushes the snapshot around the\ndiff pass so <code>ListView</code>, <code>GridView</code>, <code>LazyVStack</code> (and hand-built\n<code>FlexColumn(items.Select(...).WithKey(...))</code> children) all animate the\nresulting insert / move / remove. (See spec 042 §6.)</p>\n<h3 id=\"what-animate-does-not-do\">What <code>Animate</code> does <em>not</em> do<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#what-animate-does-not-do\">#</a></h3>\n<p><code>Animate</code> is <strong>scoped to structural changes</strong>. A leaf <code>TextBlock</code> whose\n<code>Foreground</code> changes inside <code>Animate(.Spring)</code> does <strong>not</strong> animate the\nforeground — that remains the job of per-element modifiers like\n<code>.WithImplicitTransition(...)</code> or\n<a href=\"#withanimation-scope\"><code>AnimationScope.WithAnimation(...)</code></a>. The two\nchannels are deliberately independent so the SwiftUI \"<code>withAnimation</code> only\nanimates layout-shape ops\" contract holds; conflating them would surprise\nusers coming from that mental model.</p>\n<p>Per-element animation modifiers continue to win when set: declaring\n<code>.Transition(Fade)</code> on a row makes that row's enter / exit use Fade\nregardless of the ambient. The ambient is a default for the transactional\ncase, not a hammer for every change.</p>\n<h3 id=\"nesting-and-explicit-suppression\">Nesting and explicit suppression<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#nesting-and-explicit-suppression\">#</a></h3>\n<p>Nested <code>Animate</code> calls stack like <code>using</code> blocks — the inner kind wins for\nstate changes inside its scope, and the outer kind resumes after:</p>\n<pre><code class=\"hljs language-csharp\">Animations.Animate(AnimationKind.Spring, () =>\n{\n    <span class=\"hljs-comment\">// Insert: animates with Spring.</span>\n    setItems([.. items, x]);\n\n    Animations.Animate(AnimationKind.None, () =>\n    {\n        <span class=\"hljs-comment\">// Insert inside None: no animation, even though we're still inside</span>\n        <span class=\"hljs-comment\">// an outer Spring transaction. Useful when a child component needs</span>\n        <span class=\"hljs-comment\">// to opt out of the caller's implicit animation intent.</span>\n        setOtherItems([.. others, y]);\n    });\n});\n</code></pre>\n<h3 id=\"reduced-motion\">Reduced motion<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reduced-motion\">#</a></h3>\n<p><code>Animate</code> respects the system's reduced-motion preference <em>at the call\nsite</em>. Read <code>UseReducedMotion()</code> and skip the wrapper when the user has\nopted out:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> reduceMotion = UseReducedMotion();\nAction commit = () => setItems([.. items, x]);\n<span class=\"hljs-keyword\">if</span> (reduceMotion) commit();\n<span class=\"hljs-keyword\">else</span>              Animations.Animate(AnimationKind.Spring, commit);\n</code></pre>\n<h2 id=\"withanimation-scope\">WithAnimation Scope<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#withanimation-scope\">#</a></h2>\n<p><code>AnimationScope.WithAnimation()</code> wraps a state change so that every\ncompositor property modified during the scope animates with the given curve.\nCall it inside a button handler or effect:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WithAnimationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (opacity, setOpacity) = UseState(<span class=\"hljs-number\">1.0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"WithAnimation Scope\"</span>),\n            Button(opacity > <span class=\"hljs-number\">0.5</span> ? <span class=\"hljs-string\">\"Fade Out\"</span> : <span class=\"hljs-string\">\"Fade In\"</span>, () =>\n            {\n                Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimation(\n                    Microsoft.UI.Reactor.Animation.Curve.Ease(<span class=\"hljs-number\">300</span>, Microsoft.UI.Reactor.Animation.Easing.Decelerate), () =>\n                    {\n                        setOpacity(opacity > <span class=\"hljs-number\">0.5</span> ? <span class=\"hljs-number\">0.2</span> : <span class=\"hljs-number\">1.0</span>);\n                    });\n            }),\n            TextBlock(<span class=\"hljs-string\">\"Compositor-animated via WithAnimation scope\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).Bold()\n                .Opacity(opacity)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/with-animation.png\" alt=\"WithAnimation scope\"></p>\n<p><code>WithAnimation</code> captures the curve, triggers the state change, and any\n<code>.Opacity()</code>, <code>.Scale()</code>, <code>.Translation()</code>, or <code>.Rotation()</code> values that\nchange during the render animate on the compositor thread. The managed\nrender completes instantly — animation runs on the GPU.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>AnimationScope</code> stores its current curve in a <code>[ThreadStatic]</code> field,\nso the ambient scope is alive only on the call stack that opened it.\nThe classic failure: <code>WithAnimation(Curve.Spring(), async () => { await Task.Delay(100); setVisible(false); })</code>. The <code>setVisible</code> after the\n<code>await</code> runs with no ambient scope — the continuation is a fresh\nstack frame and <code>[ThreadStatic]</code> is empty — and the property change\napplies instantly with no animation. For sequenced animation,\n<code>WithAnimationAsync</code> (which uses a <code>CompositionScopedBatch</code> and\nreturns a task) is the right shape, not <code>await</code> inside <code>WithAnimation</code>.\nThe framework's render host uses internal <code>PushScope</code>/<code>PopScope</code> calls\nto re-establish the scope across its own async boundaries; user code\ndoes not have that hook.</p>\n</blockquote>\n<h2 id=\"animate-modifier\">.Animate() Modifier<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animate-modifier\">#</a></h2>\n<p><code>.Animate(curve)</code> attaches a persistent implicit animation to an element.\nEvery time a compositor property changes (opacity, offset, scale, rotation),\nit animates with the given curve — no scope needed:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AnimateDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (active, setActive) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\".Animate() Modifier\"</span>),\n            Button(active ? <span class=\"hljs-string\">\"Reset\"</span> : <span class=\"hljs-string\">\"Animate\"</span>, () => setActive(!active)),\n            Border(\n                TextBlock(<span class=\"hljs-string\">\"Spring-animated\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Bold()\n            ).Padding(<span class=\"hljs-number\">12</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Background(<span class=\"hljs-string\">\"#e8e8e8\"</span>)\n             .Opacity(active ? <span class=\"hljs-number\">0.5</span> : <span class=\"hljs-number\">1.0</span>)\n             .Animate(Microsoft.UI.Reactor.Animation.Curve.Spring(<span class=\"hljs-number\">0.65f</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/animate-modifier.png\" alt=\"Animate modifier\"></p>\n<p>Pass a <code>Curve.Spring()</code> for a bouncy feel or <code>Curve.Ease(ms)</code> for a timed\nease. You can restrict which properties animate with the <code>AnimateProperty</code>\nflags: <code>.Animate(Curve.Spring(), AnimateProperty.Opacity | AnimateProperty.Scale)</code>.\nThe compositor ceiling is <code>Opacity | Offset | Scale | Rotation | CenterPoint</code> — anything outside that flag set (<code>Width</code>, <code>Height</code>, layout\nslot, brush) is <em>not</em> covered by <code>.Animate()</code>. Use <code>.LayoutAnimation()</code>\nfor size changes driven by layout, or fall through to a WinUI\n<code>Storyboard</code> via <code>.Set(...)</code> for the unusual property.</p>\n<h2 id=\"interaction-states\">Interaction States<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#interaction-states\">#</a></h2>\n<p><code>.InteractionStates()</code> applies hover, press, and focus effects at the\ncompositor layer with zero reconciles. The visual feedback runs entirely on\nthe GPU — Reactor's render loop is never involved:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InteractionStatesDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"InteractionStates\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Hover and press — zero reconcile, compositor-driven.\"</span>),\n            HStack(<span class=\"hljs-number\">12</span>,\n                Border(\n                    TextBlock(<span class=\"hljs-string\">\"Hover me\"</span>).FontSize(<span class=\"hljs-number\">16</span>).Bold()\n                        .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)\n                ).Padding(<span class=\"hljs-number\">16</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Size(<span class=\"hljs-number\">150</span>, <span class=\"hljs-number\">60</span>).Background(<span class=\"hljs-string\">\"#50C878\"</span>)\n                 .InteractionStates(s => s\n                    .PointerOver(opacity: <span class=\"hljs-number\">0.85f</span>, scale: <span class=\"hljs-number\">1.05f</span>)\n                    .Pressed(scale: <span class=\"hljs-number\">0.95f</span>, opacity: <span class=\"hljs-number\">0.7f</span>)),\n                Border(\n                    TextBlock(<span class=\"hljs-string\">\"Press me\"</span>).FontSize(<span class=\"hljs-number\">16</span>).Bold()\n                        .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)\n                ).Padding(<span class=\"hljs-number\">16</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Size(<span class=\"hljs-number\">150</span>, <span class=\"hljs-number\">60</span>).Background(<span class=\"hljs-string\">\"#9B59B6\"</span>)\n                 .InteractionStates(s => s\n                    .PointerOver(scale: <span class=\"hljs-number\">1.03f</span>)\n                    .Pressed(scale: <span class=\"hljs-number\">0.97f</span>, opacity: <span class=\"hljs-number\">0.8f</span>),\n                    curve: Microsoft.UI.Reactor.Animation.Curve.Spring(<span class=\"hljs-number\">0.5f</span>))\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/interaction-states.png\" alt=\"Interaction states\"></p>\n<p>The builder supports <code>.PointerOver(...)</code>, <code>.Pressed(...)</code>, and\n<code>.Focused(...)</code>. Each accepts optional <code>opacity</code>, <code>scale</code>, <code>translation</code>,\n<code>rotation</code>, <code>background</code>, <code>foreground</code>, and <code>borderBrush</code> parameters. Pass\na <code>curve:</code> parameter for spring or ease transitions between states.</p>\n<h2 id=\"enterexit-transitions\">Enter/Exit Transitions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#enterexit-transitions\">#</a></h2>\n<p><code>.Transition()</code> animates an element when it enters or leaves the tree.\nCombine transitions with <code>+</code> (parallel) and <code>|</code> (asymmetric enter/exit):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TransitionDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (visible, setVisible) = UseState(<span class=\"hljs-literal\">true</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Enter/Exit Transition\"</span>),\n            Button(visible ? <span class=\"hljs-string\">\"Hide\"</span> : <span class=\"hljs-string\">\"Show\"</span>, () => setVisible(!visible)),\n            visible\n                ? Border(\n                    TextBlock(<span class=\"hljs-string\">\"Fade + Slide\"</span>).FontSize(<span class=\"hljs-number\">16</span>).Bold()\n                        .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)\n                ).Padding(<span class=\"hljs-number\">12</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Size(<span class=\"hljs-number\">200</span>, <span class=\"hljs-number\">60</span>).Background(<span class=\"hljs-string\">\"#E74C3C\"</span>)\n                 .Transition(Microsoft.UI.Reactor.Animation.Transition.Fade + Microsoft.UI.Reactor.Animation.Transition.Slide(Microsoft.UI.Reactor.Animation.Edge.Bottom))\n                : (Element)TextBlock(<span class=\"hljs-string\">\"(removed from tree)\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/enter-exit-transition.png\" alt=\"Enter/exit transition\"></p>\n<p>Built-in transitions:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Transition</th><th>Effect</th></tr></thead><tbody><tr><td><code>Transition.Fade</code></td><td>Fade in/out</td></tr><tr><td><code>Transition.Slide(edge)</code></td><td>Slide from an edge (Left, Top, Right, Bottom)</td></tr><tr><td><code>Transition.Scale(from)</code></td><td>Scale from a starting factor</td></tr><tr><td><code>a + b</code></td><td>Run both in parallel</td></tr><tr><td><code>enter | exit</code></td><td>Different transitions for enter and exit</td></tr></tbody></table>\n<p>The default curve is 300ms with Decelerate easing. Pass a custom <code>Curve</code>\nas the second parameter to override.</p>\n<h2 id=\"stagger\">Stagger<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stagger\">#</a></h2>\n<p><code>.Stagger(delay)</code> on a container adds an incremental delay to each child's\nenter transition and layout animation, creating a cascade effect:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StaggerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, setItems) = UseState(<span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"One\"</span>, <span class=\"hljs-string\">\"Two\"</span>, <span class=\"hljs-string\">\"Three\"</span>, <span class=\"hljs-string\">\"Four\"</span>, <span class=\"hljs-string\">\"Five\"</span> });\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Staggered Animation\"</span>),\n            Button(<span class=\"hljs-string\">\"Shuffle\"</span>, () => setItems(items.OrderBy(_ => Random.Shared.Next()).ToArray())),\n            VStack(<span class=\"hljs-number\">4</span>, items.Select(item =>\n                TextBlock(item).Padding(horizontal: <span class=\"hljs-number\">8</span>, vertical: <span class=\"hljs-number\">12</span>).Background(<span class=\"hljs-string\">\"#f0f0f0\"</span>)\n                    .CornerRadius(<span class=\"hljs-number\">4</span>).LayoutAnimation()\n                    .WithKey(item)\n            ).ToArray()).Stagger(TimeSpan.FromMilliseconds(<span class=\"hljs-number\">40</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/stagger.png\" alt=\"Staggered animation\"></p>\n<p>Each child's animation starts <code>delay</code> milliseconds after the previous one.\nCombine with <code>.LayoutAnimation()</code> on each child and <code>.WithKey()</code> for smooth\nreorder cascades.</p>\n<h2 id=\"keyframes\">Keyframes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyframes\">#</a></h2>\n<p><code>.Keyframes(name, trigger, configure)</code> runs a multi-property keyframe\nanimation whenever <code>trigger</code> changes. Define keyframes at progress points\nfrom 0.0 to 1.0:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">KeyframeDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Keyframe Animation\"</span>),\n            Button(<span class=\"hljs-string\">\"Pulse!\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>)),\n            Border(\n                TextBlock(<span class=\"hljs-string\">\"Pulse target\"</span>).FontSize(<span class=\"hljs-number\">16</span>).Bold()\n                    .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)\n            ).Padding(<span class=\"hljs-number\">12</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Size(<span class=\"hljs-number\">200</span>, <span class=\"hljs-number\">60</span>).Background(<span class=\"hljs-string\">\"#9B59B6\"</span>)\n             .Keyframes(<span class=\"hljs-string\">\"pulse\"</span>, count, kf => kf\n                .Duration(<span class=\"hljs-number\">600</span>)\n                .At(<span class=\"hljs-number\">0.0f</span>, scale: <span class=\"hljs-keyword\">global</span>::System.Numerics.Vector3.One)\n                .At(<span class=\"hljs-number\">0.4f</span>, scale: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-keyword\">global</span>::System.Numerics.Vector3(<span class=\"hljs-number\">1.3f</span>, <span class=\"hljs-number\">1.3f</span>, <span class=\"hljs-number\">1f</span>), easing: Microsoft.UI.Reactor.Animation.Easing.Decelerate)\n                .At(<span class=\"hljs-number\">0.7f</span>, scale: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-keyword\">global</span>::System.Numerics.Vector3(<span class=\"hljs-number\">0.95f</span>, <span class=\"hljs-number\">0.95f</span>, <span class=\"hljs-number\">1f</span>))\n                .At(<span class=\"hljs-number\">1.0f</span>, scale: <span class=\"hljs-keyword\">global</span>::System.Numerics.Vector3.One, easing: Microsoft.UI.Reactor.Animation.Easing.Accelerate))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/keyframes.png\" alt=\"Keyframe animation\"></p>\n<p>The builder supports <code>.Duration(ms)</code>, <code>.Loop()</code> for infinite repeat, and\n<code>.At(progress, opacity?, scale?, translation?, rotation?, easing?)</code> for\neach keyframe. Keyframes run on the compositor — no managed-code\ninvolvement during playback.</p>\n<h2 id=\"choreography\">Choreography<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#choreography\">#</a></h2>\n<p><code>AnimationScope.WithAnimationAsync()</code> returns a <code>Task</code> that completes when\nthe compositor animation finishes. Chain multiple calls with <code>await</code> to\nbuild sequenced animations:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ChoreographyDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (phase, setPhase) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Choreography (WithAnimationAsync)\"</span>),\n            Button(<span class=\"hljs-string\">\"Run Sequence\"</span>, <span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">await</span> Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimationAsync(\n                    Microsoft.UI.Reactor.Animation.Curve.Ease(<span class=\"hljs-number\">200</span>), () => setPhase(<span class=\"hljs-number\">1</span>));\n                <span class=\"hljs-keyword\">await</span> Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimationAsync(\n                    Microsoft.UI.Reactor.Animation.Curve.Spring(<span class=\"hljs-number\">0.7f</span>), () => setPhase(<span class=\"hljs-number\">2</span>));\n            }),\n            TextBlock(<span class=\"hljs-string\">$\"Phase: <span class=\"hljs-subst\">{phase}</span>\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Bold()\n                .Opacity(phase == <span class=\"hljs-number\">0</span> ? <span class=\"hljs-number\">1.0</span> : phase == <span class=\"hljs-number\">1</span> ? <span class=\"hljs-number\">0.3</span> : <span class=\"hljs-number\">1.0</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation/choreography.png\" alt=\"Choreography\"></p>\n<p>Each <code>await</code> waits for the <code>CompositionScopedBatch</code> to complete before\nstarting the next step. Use this for onboarding flows, multi-step reveals,\nor any animation that must happen in order.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"page-enterexit-on-route-change\">Page enter/exit on route change<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#page-enterexit-on-route-change\">#</a></h3>\n<p>Wrap the route-mapped page in a <code>.Transition(...)</code> so each new page\nslides or fades into view. Pair <code>Transition.Slide(Edge.Right) | Transition.Slide(Edge.Left)</code> (asymmetric — enter from right, exit to\nleft) with <a href=\"/navigation/\"><code>NavigationTransition.None</code></a> on the host so\nthe compositor animation runs without competing with the host's\ndefault <code>Slide</code>. See <a href=\"/navigation/\">Navigation</a> for the route\nplumbing.</p>\n<h3 id=\"skeleton-to-content-fade\">Skeleton-to-content fade<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#skeleton-to-content-fade\">#</a></h3>\n<p>When data is loading, render a skeleton element; when it arrives,\nswap to the real content. Apply <code>.Transition(Transition.Fade, Curve.Ease(150))</code>\non the content element and the skeleton element separately so the\ncrossfade looks intentional rather than a hard pop. Combine with\n<a href=\"/async-resources/\"><code>UseResource</code></a> for the pending-state plumbing.</p>\n<h3 id=\"reorder-list-with-stable-identity\">Reorder list with stable identity<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reorder-list-with-stable-identity\">#</a></h3>\n<p>Layout animations need <a href=\"/hooks/\"><code>UseState</code></a> updates that preserve\nidentity across reorders. Always set <code>.WithKey($\"item-{model.Id}\")</code>\non each list child — the reconciler matches keys to track which\nelement moved where, and <code>.LayoutAnimation()</code> reads the old → new\nposition from the matched element. Without keys the reconciler treats\nthe reorder as a destroy + create, and the animation falls back to a\nfade because there is no \"old position\" to animate from.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"animating-width-or-height-with-animate\">Animating Width or Height with <code>.Animate()</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animating-width-or-height-with-animate\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nBorder(content).Width(expanded ? <span class=\"hljs-number\">400</span> : <span class=\"hljs-number\">200</span>).Animate(Curve.Spring())\n</code></pre>\n<p><code>.Animate()</code> only covers compositor properties: Opacity, Offset\n(Translation), Scale, Rotation, CenterPoint. <code>Width</code> and <code>Height</code> are\nlayout properties; the compositor doesn't see them. The element will\nsnap from 200 to 400 with no animation. Two correct shapes: scale\ninstead (<code>Scale(expanded ? 2 : 1)</code> plus <code>.ScaleTransition()</code> — the\nelement renders at 200 and is <em>visually</em> 400), or animate via\n<code>.LayoutAnimation()</code> if a sibling-driven layout pass is what's\nchanging the size.</p>\n<h3 id=\"awaiting-inside-withanimation\">Awaiting inside <code>WithAnimation</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#awaiting-inside-withanimation\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nAnimationScope.WithAnimation(Curve.Ease(<span class=\"hljs-number\">300</span>), <span class=\"hljs-keyword\">async</span> () =>\n{\n    setStage(<span class=\"hljs-string\">\"loading\"</span>);\n    <span class=\"hljs-keyword\">await</span> api.SaveAsync();\n    setStage(<span class=\"hljs-string\">\"done\"</span>);   <span class=\"hljs-comment\">// No animation — scope is gone.</span>\n});\n</code></pre>\n<p><code>AnimationScope</code> is <code>[ThreadStatic]</code>; the <code>await</code> continuation runs on\na different stack frame with an empty scope. The second <code>setStage</code>\nruns unanimated. Use <code>WithAnimationAsync</code> (returns a Task tied to a\ncomposition batch) and one curve per phase, or sequence two separate\n<code>WithAnimation</code> calls between awaits.</p>\n<h3 id=\"re-running-keyframes-on-every-render\">Re-running keyframes on every render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#re-running-keyframes-on-every-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n.Keyframes(<span class=\"hljs-string\">\"pulse\"</span>, DateTime.Now, kf => ...)\n</code></pre>\n<p><code>.Keyframes</code> re-runs whenever its <code>trigger</code> value changes. Passing a\nvalue that changes every render (<code>DateTime.Now</code>, a freshly-allocated\nlist, an inline lambda) restarts the animation on every reconcile —\nthe element flickers as the keyframes constantly reset. Use a state\ncounter that increments only when you mean to retrigger\n(<code>setCount(c => c + 1)</code>).</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Keep durations short.</strong> 200--400ms feels responsive. Anything over 500ms\nfeels sluggish. Use the default duration unless you have a specific reason.</p>\n<p><strong>Use <code>.InteractionStates()</code> for hover/press effects.</strong> It runs entirely on\nthe compositor with zero reconciles — far cheaper than tracking pointer\nstate in <code>UseState</code> and re-rendering.</p>\n<p><strong>Use <code>.Transition()</code> for conditional rendering.</strong> When an element enters or\nleaves the tree via <code>When()</code> or ternary, <code>.Transition()</code> adds polish\nwithout manual mount/unmount tracking.</p>\n<p><strong>Combine transitions sparingly.</strong> One or two transitions per element is\nnatural. Three or more competing animations can feel chaotic.</p>\n<p><strong>Always set <code>.WithKey()</code> with layout animations.</strong> Without stable keys, the\nreconciler cannot track which element moved where, and the animation falls\nback to a simple fade.</p>\n<p><strong>Use <code>Curve.Spring()</code> for interactive feedback.</strong> Spring curves feel\nnatural for user-triggered animations. Use <code>Curve.Ease()</code> for timed,\nnon-interactive transitions.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/localization/\">Localization</a></strong> — previous topic: translate strings, format numbers/dates, and support RTL layouts</li>\n<li><strong><a href=\"/charting/\">Charting</a></strong> — next topic: data visualization with line, bar, area, and pie charts</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — pair connected animations with page transitions</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — animate list items as they enter, reorder, and leave</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — combine animations with theme-aware colors</li>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — pair compositor-batched animation with data load lifecycles</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Motion . Microsoft.UI.Reactor (Reactor) exposes four animation systems and one rule about which to pick. The rule first: the compositor can only animate five properties — Opacity, Offset (Translation), Scale, Rotation, and CenterPoint — and when it does, the managed render thread is not involved and the animation runs at display refresh on the GPU. Everything that animates those five properties (implicit transitions like .OpacityTransition() , .Animate() for persistent implicit, .WithAnimation() for an event- scoped batch, .Transition() for enter/exit, .InteractionStates() for hover/press/focus, .Keyframes() for multi-step) is the same underlying pipeline at different ergonomics. The other three systems exist for things outside that ceiling: .LayoutAnimation() animates position changes from layout reflow (a sibling was added, the panel re-measured), .ConnectedAnimation() snapshots an element on unmount and plays the snapshot into the matching element on mount, and the WinUI Storyboard (reachable through .Set(...) ) covers the rare properties none of the above can touch. Read Compositor animation first if you're picking between .Animate() and .WithAnimation() , or Choreography if you're trying to sequence steps. Animation # Reactor animations are declarative. You set the target value (opacity, scale, translation) and attach a transition modifier. When the value changes on the next render — driven by hooks and state — WinUI animates from the old value to the new one automatically. Reference # API Animates Trigger Use when .OpacityTransition(duration?) Opacity Implicit, on every change Show/hide a single element. .ScaleTransition(transition?) Scale Implicit, on every change Element resize feedback. .TranslationTransition(transition?) Translation (offset) Implicit, on every change Slide a single element. .RotationTransition(duration?) Rotation Implicit, on every change Spin a single element. .BackgroundTransition(duration?) Background brush Implicit; panels only Color transitions on Grid / Stack. .Animate(curve, props?) Any compositor property Implicit, persistent One curve for every change to this element. AnimationScope.WithAnimation(curve, action) Compositor properties changed inside the action Event-scoped One state change drives a batched animation. .Transition(t, curve?) Enter / exit When element enters or leaves the tree Animate When(...) / ternary mount and unmount. .InteractionStates(builder, curve?) Compositor properties per state Hover / press / focus Zero-reconcile pointer-state feedback. .Keyframes(name, trigger, builder) Compositor properties at progress points When trigger changes Multi-step animation (pulse, shake, breathe). .Stagger(delay, curve?) Container's children Sibling cascade Cascade enter/exit and layout animations across a list. .LayoutAnimation(duration?) / .SpringLayoutAnimation(...) Element position from layout reflow Layout pass Animate position changes a sibling caused. .ConnectedAnimation(key) Snapshot from old position to new On unmount/mount of the same key List-to-detail hero animations. AnimationScope.WithAnimationAsync(curve, action) Compositor properties changed inside the action Event-scoped, returns Task Sequence steps with await . Opacity Transition # .OpacityTransition() animates opacity changes. Set .Opacity() to your target value and the transition handles the rest: class OpacityDemo : Component { public override Element Render () { var (visible, setVisible) = UseState( true ); return VStack( 12 , SubHeading( \"Opacity Transition\" ), Button(visible ? \"Fade Out\" : \"Fade In\" , () => setVisible(!visible)), TextBlock( \"This text fades in and out\" ) .FontSize( 18 ).Bold() .Opacity(visible ? 1.0 : 0.0 ) .OpacityTransition(TimeSpan.FromMilliseconds( 500 )) ).Padding( 24 ); } } The optional TimeSpan parameter controls duration. The default is 300ms. Use this for fade-in/fade-out on showing and hiding elements. Scale Transition # .ScaleTransition() animates scale changes. Set .Scale() to the target factor: class ScaleDemo : Component { public override Element Render () { var (enlarged, setEnlarged) = UseState( false ); return VStack( 12 , SubHeading( \"Scale Transition\" ), Button(enlarged ? \"Shrink\" : \"Enlarge\" , () => setEnlarged(!enlarged)), Border( TextBlock( \"Scales up and down\" ).FontSize( 18 ).Bold() ).Padding( 12 ) .CornerRadius( 8 ) .Background( \"#e8e8e8\" ) .ScaleTransition() ).Padding( 24 ); } } Scale uses the element's center as the transform origin. A value of 1.0f is normal size, 1.5f is 150%. You can pass a custom Vector3Transition to control which axes animate. Translation Transition # .TranslationTransition() animates position offsets. Set .Translation() to the target X, Y, Z offset in pixels: class TranslationDemo : Component { public override Element Render () { var (moved, setMoved) = UseState( false ); return VStack( 12 , SubHeading( \"Translation Transition\" ), Button(moved ? \"Slide Back\" : \"Slide Right\" , () => setMoved(!moved)), TextBlock( \"Slides horizontally\" ) .FontSize( 18 ).Bold() .Translation(moved ? 120f : 0f , 0f , 0f ) .TranslationTransition() ).Padding( 24 ); } } Translation offsets are relative to the element's layout position. Positive X moves right, positive Y moves down. The element still occupies its original layout space — only the visual position changes. Background Transition # .BackgroundTransition() animates background color changes on VStack , HStack , and Grid elements: class BackgroundDemo : Component { public override Element Render () { var (warm, setWarm) = UseState( false ); return VStack( 12 , SubHeading( \"Background Transition\" ), Button(warm ? \"Cool Colors\" : \"Warm Colors\" , () => setWarm(!warm)), VStack( 8 , TextBlock( \"Background animates between colors\" ) .Foreground( \"#ffffff\" ).Bold() ).Padding( 16 ) .CornerRadius( 8 ) .Background(warm ? \"#da3b01\" : \"#0078d4\" ) .BackgroundTransition(TimeSpan.FromMilliseconds( 600 )) ).Padding( 24 ); } } Background transitions use WinUI's BrushTransition . They only work on panel elements ( StackPanel , Grid ) because WinUI restricts BackgroundTransition to those types. Combining Transitions # You can chain multiple transition modifiers on a single element. Each property animates independently: class CombinedDemo : Component { public override Element Render () { var (active, setActive) = UseState( false ); return VStack( 12 , SubHeading( \"Combined Transitions\" ), Button(active ? \"Reset\" : \"Animate\" , () => setActive(!active)), Border( TextBlock( \"All at once\" ).FontSize( 16 ).Bold() .Foreground( \"#ffffff\" ) ).Padding( 16 ) .CornerRadius( 8 ) .Background( \"#7b2ab5\" ) .Opacity(active ? 1.0 : 0.4 ) .Translation(active ? 40f : 0f , 0f , 0f ) .OpacityTransition(TimeSpan.FromMilliseconds( 400 )) .TranslationTransition() ).Padding( 24 ); } } Each transition modifier is independent — .OpacityTransition() animates opacity while .ScaleTransition() animates scale simultaneously. Set the target values ( .Opacity() , .Scale() , .Translation() ) and the transitions handle the animation for each property in parallel. Layout Animation # .LayoutAnimation() animates elements when their position changes due to layout reflow — items entering, leaving, or reordering in a collection : class LayoutAnimationDemo : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C; string > { \"Apple\" , \"Banana\" , \"Cherry\" }); var nextId = UseRef( 3 ); return VStack( 12 , SubHeading( \"Layout Animation\" ), HStack( 8 , Button( \"Add Item\" , () => { nextId.Current++; updateItems(l => [.. l, $\"Item {nextId.Current} \" ]); }), Button( \"Remove Last\" , () => updateItems(l => l.Count > 0 ? l.Take(l.Count - 1 ).ToList() : l)) ), VStack( 4 , items.Select(item => TextBlock(item).Padding(horizontal: 8 , vertical: 12 ).Background( \"#f0f0f0\" ) .CornerRadius( 4 ).LayoutAnimation() .WithKey( $\"item- {item} \" ) ).ToArray()) ).Padding( 24 ); } } Layout animation works at the Composition layer. When WinUI repositions an element (e.g., a sibling is added or removed), Reactor animates from the old position to the new one. Use .WithKey() on each element so the reconciler can track identity across reorders. You can also use .LayoutAnimation(TimeSpan) for a custom duration or .SpringLayoutAnimation() for a bouncy feel. Connected Animation # .ConnectedAnimation(key) creates a visual continuity effect between two views. When an element with a key is unmounted and another with the same key is mounted, WinUI animates a snapshot from the old position to the new one: class ConnectedAnimationDemo : Component { public override Element Render () { var (selected, setSelected) = UseState&#x3C; string ?>( null ); if (selected is not null ) return VStack( 12 , Button( \"Back to list\" , () => setSelected( null )), TextBlock(selected) .FontSize( 28 ).Bold() .ConnectedAnimation( $\"title- {selected} \" ) ).Padding( 24 ); var items = new [] { \"Photos\" , \"Music\" , \"Videos\" }; return VStack( 12 , SubHeading( \"Connected Animation\" ), VStack( 4 , items.Select(item => Button(item, () => setSelected(item)) .ConnectedAnimation( $\"title- {item} \" ) ).ToArray() ) ).Padding( 24 ); } } Both the source and destination elements must use the same key string. The animation runs automatically when the reconciler detects the transition. Use connected animations for list-to-detail navigation where an element \"flies\" from the list into the detail view. Transactional animation — Animations.Animate(...) # Animations.Animate(kind, action) is Reactor's SwiftUI-style transactional animation primitive. Wrap a state mutation, and any structural change to a keyed list (insert, move, remove) that comes out of that mutation picks up the kind — without a per-element modifier in sight. class TodoList : Component { public override Element Render () { var (items, setItems) = UseState&#x3C;IReadOnlyList&#x3C;Todo>>(_seed); return VStack( 12 , Button( \"Add\" , () => Animations.Animate(AnimationKind.Spring, () => setItems([.. items, new Todo(Guid.NewGuid().ToString(), \"New\" )]))), ListView&#x3C;Todo>(items, (t, _) => TextBlock(t.Title).Padding( 8 )) .Height( 400 ) ); } } AnimationKind is the declarative knob — Spring , EaseIn , EaseOut , EaseInOut , Default , or None . The kind flows through an AsyncLocal ambient: a setter invoked inside Animate snapshots the ambient before queuing the render, and the reconciler re-pushes the snapshot around the diff pass so ListView , GridView , LazyVStack (and hand-built FlexColumn(items.Select(...).WithKey(...)) children) all animate the resulting insert / move / remove. (See spec 042 §6.) What Animate does not do # Animate is scoped to structural changes . A leaf TextBlock whose Foreground changes inside Animate(.Spring) does not animate the foreground — that remains the job of per-element modifiers like .WithImplicitTransition(...) or AnimationScope.WithAnimation(...) . The two channels are deliberately independent so the SwiftUI \" withAnimation only animates layout-shape ops\" contract holds; conflating them would surprise users coming from that mental model. Per-element animation modifiers continue to win when set: declaring .Transition(Fade) on a row makes that row's enter / exit use Fade regardless of the ambient. The ambient is a default for the transactional case, not a hammer for every change. Nesting and explicit suppression # Nested Animate calls stack like using blocks — the inner kind wins for state changes inside its scope, and the outer kind resumes after: Animations.Animate(AnimationKind.Spring, () => { // Insert: animates with Spring. setItems([.. items, x]); Animations.Animate(AnimationKind.None, () => { // Insert inside None: no animation, even though we're still inside // an outer Spring transaction. Useful when a child component needs // to opt out of the caller's implicit animation intent. setOtherItems([.. others, y]); }); }); Reduced motion # Animate respects the system's reduced-motion preference at the call site . Read UseReducedMotion() and skip the wrapper when the user has opted out: var reduceMotion = UseReducedMotion(); Action commit = () => setItems([.. items, x]); if (reduceMotion) commit(); else Animations.Animate(AnimationKind.Spring, commit); WithAnimation Scope # AnimationScope.WithAnimation() wraps a state change so that every compositor property modified during the scope animates with the given curve. Call it inside a button handler or effect: class WithAnimationDemo : Component { public override Element Render () { var (opacity, setOpacity) = UseState( 1.0 ); return VStack( 12 , SubHeading( \"WithAnimation Scope\" ), Button(opacity > 0.5 ? \"Fade Out\" : \"Fade In\" , () => { Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimation( Microsoft.UI.Reactor.Animation.Curve.Ease( 300 , Microsoft.UI.Reactor.Animation.Easing.Decelerate), () => { setOpacity(opacity > 0.5 ? 0.2 : 1.0 ); }); }), TextBlock( \"Compositor-animated via WithAnimation scope\" ) .FontSize( 18 ).Bold() .Opacity(opacity) ).Padding( 24 ); } } WithAnimation captures the curve, triggers the state change, and any .Opacity() , .Scale() , .Translation() , or .Rotation() values that change during the render animate on the compositor thread. The managed render completes instantly — animation runs on the GPU. Caveat: AnimationScope stores its current curve in a [ThreadStatic] field, so the ambient scope is alive only on the call stack that opened it. The classic failure: WithAnimation(Curve.Spring(), async () => { await Task.Delay(100); setVisible(false); }) . The setVisible after the await runs with no ambient scope — the continuation is a fresh stack frame and [ThreadStatic] is empty — and the property change applies instantly with no animation. For sequenced animation, WithAnimationAsync (which uses a CompositionScopedBatch and returns a task) is the right shape, not await inside WithAnimation . The framework's render host uses internal PushScope / PopScope calls to re-establish the scope across its own async boundaries; user code does not have that hook. .Animate() Modifier # .Animate(curve) attaches a persistent implicit animation to an element. Every time a compositor property changes (opacity, offset, scale, rotation), it animates with the given curve — no scope needed: class AnimateDemo : Component { public override Element Render () { var (active, setActive) = UseState( false ); return VStack( 12 , SubHeading( \".Animate() Modifier\" ), Button(active ? \"Reset\" : \"Animate\" , () => setActive(!active)), Border( TextBlock( \"Spring-animated\" ).FontSize( 18 ).Bold() ).Padding( 12 ).CornerRadius( 8 ).Background( \"#e8e8e8\" ) .Opacity(active ? 0.5 : 1.0 ) .Animate(Microsoft.UI.Reactor.Animation.Curve.Spring( 0.65f )) ).Padding( 24 ); } } Pass a Curve.Spring() for a bouncy feel or Curve.Ease(ms) for a timed ease. You can restrict which properties animate with the AnimateProperty flags: .Animate(Curve.Spring(), AnimateProperty.Opacity | AnimateProperty.Scale) . The compositor ceiling is Opacity | Offset | Scale | Rotation | CenterPoint — anything outside that flag set ( Width , Height , layout slot, brush) is not covered by .Animate() . Use .LayoutAnimation() for size changes driven by layout, or fall through to a WinUI Storyboard via .Set(...) for the unusual property. Interaction States # .InteractionStates() applies hover, press, and focus effects at the compositor layer with zero reconciles. The visual feedback runs entirely on the GPU — Reactor's render loop is never involved: class InteractionStatesDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"InteractionStates\" ), TextBlock( \"Hover and press — zero reconcile, compositor-driven.\" ), HStack( 12 , Border( TextBlock( \"Hover me\" ).FontSize( 16 ).Bold() .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center) ).Padding( 16 ).CornerRadius( 8 ).Size( 150 , 60 ).Background( \"#50C878\" ) .InteractionStates(s => s .PointerOver(opacity: 0.85f , scale: 1.05f ) .Pressed(scale: 0.95f , opacity: 0.7f )), Border( TextBlock( \"Press me\" ).FontSize( 16 ).Bold() .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center) ).Padding( 16 ).CornerRadius( 8 ).Size( 150 , 60 ).Background( \"#9B59B6\" ) .InteractionStates(s => s .PointerOver(scale: 1.03f ) .Pressed(scale: 0.97f , opacity: 0.8f ), curve: Microsoft.UI.Reactor.Animation.Curve.Spring( 0.5f )) ) ).Padding( 24 ); } } The builder supports .PointerOver(...) , .Pressed(...) , and .Focused(...) . Each accepts optional opacity , scale , translation , rotation , background , foreground , and borderBrush parameters. Pass a curve: parameter for spring or ease transitions between states. Enter/Exit Transitions # .Transition() animates an element when it enters or leaves the tree. Combine transitions with + (parallel) and | (asymmetric enter/exit): class TransitionDemo : Component { public override Element Render () { var (visible, setVisible) = UseState( true ); return VStack( 12 , SubHeading( \"Enter/Exit Transition\" ), Button(visible ? \"Hide\" : \"Show\" , () => setVisible(!visible)), visible ? Border( TextBlock( \"Fade + Slide\" ).FontSize( 16 ).Bold() .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center) ).Padding( 12 ).CornerRadius( 8 ).Size( 200 , 60 ).Background( \"#E74C3C\" ) .Transition(Microsoft.UI.Reactor.Animation.Transition.Fade + Microsoft.UI.Reactor.Animation.Transition.Slide(Microsoft.UI.Reactor.Animation.Edge.Bottom)) : (Element)TextBlock( \"(removed from tree)\" ) ).Padding( 24 ); } } Built-in transitions: Transition Effect Transition.Fade Fade in/out Transition.Slide(edge) Slide from an edge (Left, Top, Right, Bottom) Transition.Scale(from) Scale from a starting factor a + b Run both in parallel enter | exit Different transitions for enter and exit The default curve is 300ms with Decelerate easing. Pass a custom Curve as the second parameter to override. Stagger # .Stagger(delay) on a container adds an incremental delay to each child's enter transition and layout animation, creating a cascade effect: class StaggerDemo : Component { public override Element Render () { var (items, setItems) = UseState( new [] { \"One\" , \"Two\" , \"Three\" , \"Four\" , \"Five\" }); return VStack( 12 , SubHeading( \"Staggered Animation\" ), Button( \"Shuffle\" , () => setItems(items.OrderBy(_ => Random.Shared.Next()).ToArray())), VStack( 4 , items.Select(item => TextBlock(item).Padding(horizontal: 8 , vertical: 12 ).Background( \"#f0f0f0\" ) .CornerRadius( 4 ).LayoutAnimation() .WithKey(item) ).ToArray()).Stagger(TimeSpan.FromMilliseconds( 40 )) ).Padding( 24 ); } } Each child's animation starts delay milliseconds after the previous one. Combine with .LayoutAnimation() on each child and .WithKey() for smooth reorder cascades. Keyframes # .Keyframes(name, trigger, configure) runs a multi-property keyframe animation whenever trigger changes. Define keyframes at progress points from 0.0 to 1.0: class KeyframeDemo : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return VStack( 12 , SubHeading( \"Keyframe Animation\" ), Button( \"Pulse!\" , () => setCount(count + 1 )), Border( TextBlock( \"Pulse target\" ).FontSize( 16 ).Bold() .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center) ).Padding( 12 ).CornerRadius( 8 ).Size( 200 , 60 ).Background( \"#9B59B6\" ) .Keyframes( \"pulse\" , count, kf => kf .Duration( 600 ) .At( 0.0f , scale: global ::System.Numerics.Vector3.One) .At( 0.4f , scale: new global ::System.Numerics.Vector3( 1.3f , 1.3f , 1f ), easing: Microsoft.UI.Reactor.Animation.Easing.Decelerate) .At( 0.7f , scale: new global ::System.Numerics.Vector3( 0.95f , 0.95f , 1f )) .At( 1.0f , scale: global ::System.Numerics.Vector3.One, easing: Microsoft.UI.Reactor.Animation.Easing.Accelerate)) ).Padding( 24 ); } } The builder supports .Duration(ms) , .Loop() for infinite repeat, and .At(progress, opacity?, scale?, translation?, rotation?, easing?) for each keyframe. Keyframes run on the compositor — no managed-code involvement during playback. Choreography # AnimationScope.WithAnimationAsync() returns a Task that completes when the compositor animation finishes. Chain multiple calls with await to build sequenced animations: class ChoreographyDemo : Component { public override Element Render () { var (phase, setPhase) = UseState( 0 ); return VStack( 12 , SubHeading( \"Choreography (WithAnimationAsync)\" ), Button( \"Run Sequence\" , async () => { await Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimationAsync( Microsoft.UI.Reactor.Animation.Curve.Ease( 200 ), () => setPhase( 1 )); await Microsoft.UI.Reactor.Animation.AnimationScope.WithAnimationAsync( Microsoft.UI.Reactor.Animation.Curve.Spring( 0.7f ), () => setPhase( 2 )); }), TextBlock( $\"Phase: {phase} \" ).FontSize( 18 ).Bold() .Opacity(phase == 0 ? 1.0 : phase == 1 ? 0.3 : 1.0 ) ).Padding( 24 ); } } Each await waits for the CompositionScopedBatch to complete before starting the next step. Use this for onboarding flows, multi-step reveals, or any animation that must happen in order. Patterns # Page enter/exit on route change # Wrap the route-mapped page in a .Transition(...) so each new page slides or fades into view. Pair Transition.Slide(Edge.Right) | Transition.Slide(Edge.Left) (asymmetric — enter from right, exit to left) with NavigationTransition.None on the host so the compositor animation runs without competing with the host's default Slide . See Navigation for the route plumbing. Skeleton-to-content fade # When data is loading, render a skeleton element; when it arrives, swap to the real content. Apply .Transition(Transition.Fade, Curve.Ease(150)) on the content element and the skeleton element separately so the crossfade looks intentional rather than a hard pop. Combine with UseResource for the pending-state plumbing. Reorder list with stable identity # Layout animations need UseState updates that preserve identity across reorders. Always set .WithKey($\"item-{model.Id}\") on each list child — the reconciler matches keys to track which element moved where, and .LayoutAnimation() reads the old → new position from the matched element. Without keys the reconciler treats the reorder as a destroy + create, and the animation falls back to a fade because there is no \"old position\" to animate from. Common Mistakes # Animating Width or Height with .Animate() # // Don't: Border(content).Width(expanded ? 400 : 200 ).Animate(Curve.Spring()) .Animate() only covers compositor properties: Opacity, Offset (Translation), Scale, Rotation, CenterPoint. Width and Height are layout properties; the compositor doesn't see them. The element will snap from 200 to 400 with no animation. Two correct shapes: scale instead ( Scale(expanded ? 2 : 1) plus .ScaleTransition() — the element renders at 200 and is visually 400), or animate via .LayoutAnimation() if a sibling-driven layout pass is what's changing the size. Awaiting inside WithAnimation # // Don't: AnimationScope.WithAnimation(Curve.Ease( 300 ), async () => { setStage( \"loading\" ); await api.SaveAsync(); setStage( \"done\" ); // No animation — scope is gone. }); AnimationScope is [ThreadStatic] ; the await continuation runs on a different stack frame with an empty scope. The second setStage runs unanimated. Use WithAnimationAsync (returns a Task tied to a composition batch) and one curve per phase, or sequence two separate WithAnimation calls between awaits. Re-running keyframes on every render # // Don't: .Keyframes( \"pulse\" , DateTime.Now, kf => ...) .Keyframes re-runs whenever its trigger value changes. Passing a value that changes every render ( DateTime.Now , a freshly-allocated list, an inline lambda) restarts the animation on every reconcile — the element flickers as the keyframes constantly reset. Use a state counter that increments only when you mean to retrigger ( setCount(c => c + 1) ). Tips # Keep durations short. 200--400ms feels responsive. Anything over 500ms feels sluggish. Use the default duration unless you have a specific reason. Use .InteractionStates() for hover/press effects. It runs entirely on the compositor with zero reconciles — far cheaper than tracking pointer state in UseState and re-rendering. Use .Transition() for conditional rendering. When an element enters or leaves the tree via When() or ternary, .Transition() adds polish without manual mount/unmount tracking. Combine transitions sparingly. One or two transitions per element is natural. Three or more competing animations can feel chaotic. Always set .WithKey() with layout animations. Without stable keys, the reconciler cannot track which element moved where, and the animation falls back to a simple fade. Use Curve.Spring() for interactive feedback. Spring curves feel natural for user-triggered animations. Use Curve.Ease() for timed, non-interactive transitions. Next Steps # Localization — previous topic: translate strings, format numbers/dates, and support RTL layouts Charting — next topic: data visualization with line, bar, area, and pie charts Navigation — pair connected animations with page transitions Collections — animate list items as they enter, reorder, and leave Styling and Theming — combine animations with theme-aware colors Async Resources — pair compositor-batched animation with data load lifecycles",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/animation.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/animation.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "opacity-transition",
          "text": "Opacity Transition"
        },
        {
          "depth": 2,
          "id": "scale-transition",
          "text": "Scale Transition"
        },
        {
          "depth": 2,
          "id": "translation-transition",
          "text": "Translation Transition"
        },
        {
          "depth": 2,
          "id": "background-transition",
          "text": "Background Transition"
        },
        {
          "depth": 2,
          "id": "combining-transitions",
          "text": "Combining Transitions"
        },
        {
          "depth": 2,
          "id": "layout-animation",
          "text": "Layout Animation"
        },
        {
          "depth": 2,
          "id": "connected-animation",
          "text": "Connected Animation"
        },
        {
          "depth": 2,
          "id": "transactional-animation--animationsanimate",
          "text": "Transactional animation — Animations.Animate(...)"
        },
        {
          "depth": 3,
          "id": "what-animate-does-not-do",
          "text": "What Animate does not do"
        },
        {
          "depth": 3,
          "id": "nesting-and-explicit-suppression",
          "text": "Nesting and explicit suppression"
        },
        {
          "depth": 3,
          "id": "reduced-motion",
          "text": "Reduced motion"
        },
        {
          "depth": 2,
          "id": "withanimation-scope",
          "text": "WithAnimation Scope"
        },
        {
          "depth": 2,
          "id": "animate-modifier",
          "text": ".Animate() Modifier"
        },
        {
          "depth": 2,
          "id": "interaction-states",
          "text": "Interaction States"
        },
        {
          "depth": 2,
          "id": "enterexit-transitions",
          "text": "Enter/Exit Transitions"
        },
        {
          "depth": 2,
          "id": "stagger",
          "text": "Stagger"
        },
        {
          "depth": 2,
          "id": "keyframes",
          "text": "Keyframes"
        },
        {
          "depth": 2,
          "id": "choreography",
          "text": "Choreography"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "page-enterexit-on-route-change",
          "text": "Page enter/exit on route change"
        },
        {
          "depth": 3,
          "id": "skeleton-to-content-fade",
          "text": "Skeleton-to-content fade"
        },
        {
          "depth": 3,
          "id": "reorder-list-with-stable-identity",
          "text": "Reorder list with stable identity"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "animating-width-or-height-with-animate",
          "text": "Animating Width or Height with .Animate()"
        },
        {
          "depth": 3,
          "id": "awaiting-inside-withanimation",
          "text": "Awaiting inside WithAnimation"
        },
        {
          "depth": 3,
          "id": "re-running-keyframes-on-every-render",
          "text": "Re-running keyframes on every render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Input and Gestures",
      "path": "input-and-gestures.md",
      "slug": "/input-and-gestures/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/input/\">Input</a>.</p>\n</blockquote>\n<p>Input flows from raw pointer and keyboard events through Microsoft.UI.Reactor (Reactor)'s\nmodifier tower into your component's render logic, with gesture\nrecognizers and focus state stacked on top. The bottom layer is the\nraw <code>.OnPointerPressed</code> / <code>.OnKeyDown</code> / <code>.OnGotFocus</code> etc. surface,\nwhich mirrors WinUI's routed events one-to-one and uses bubbling by\ndefault — events start at the deepest element and propagate up the\ntree. Above that sit the high-level recognizers: <code>.OnTapped</code>,\n<code>.OnDoubleTapped</code>, <code>.OnPan</code>, <code>.OnPinch</code>, <code>.OnRotate</code>, <code>.OnLongPress</code>\n— each auto-enables the underlying WinUI flag (e.g.\n<code>IsDoubleTapEnabled</code>) and installs a single stable trampoline so\nre-rendering with a new handler closure costs no re-subscription.\nFocus is the third tower: declarative <code>.TabIndex</code> / <code>.AccessKey</code> /\n<code>.IsTabStop</code> modifiers, the <a href=\"/hooks/\"><code>UseElementFocus</code></a> /\n<a href=\"/hooks/\"><code>UseElementRef</code></a> hooks for imperative focus, and\n<a href=\"/accessibility/\"><code>UseFocusTrap</code></a> for modal containment. Everything\nis re-render-safe by design; the <a href=\"/focus-and-input-internals/\">focus-and-input-internals</a>\npage covers the dispatcher and trampoline mechanics for readers who\nwant the implementation details.</p>\n<h1 id=\"input-and-gestures\">Input and gestures<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#input-and-gestures\">#</a></h1>\n<p>Reactor's input surface is declarative: attach handlers via <code>.On*</code> modifiers and\nthe reconciler wires WinUI events, auto-enables the matching flags (for example\n<code>IsDoubleTapEnabled</code>), and survives re-renders without re-subscribing the\nunderlying WinUI event.</p>\n<p>This page covers the modifiers you'll reach for on most screens, the higher-level\ngesture recognizers for continuous input, and the imperative escape hatches —\nref-based focus and access keys — that round out the commanding story.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Modifier / hook</th><th>Purpose</th></tr></thead><tbody><tr><td><code>.OnPointerPressed</code> / <code>.OnPointerReleased</code> / <code>.OnPointerEntered</code> / <code>.OnPointerExited</code> / <code>.OnPointerMoved</code></td><td>Raw pointer events. Bubble by default.</td></tr><tr><td><code>.OnTapped</code> / <code>.OnDoubleTapped</code> / <code>.OnRightTapped</code> / <code>.OnHolding</code></td><td>High-level tap recognizers. Auto-enable the matching <code>IsXEnabled</code> flag.</td></tr><tr><td><code>.OnPan</code> / <code>.OnPinch</code> / <code>.OnRotate</code> / <code>.OnLongPress</code></td><td>Continuous-gesture recognizers. Take <code>onBegan</code> / <code>onChanged</code> / <code>onEnded</code>.</td></tr><tr><td><code>.OnKeyDown</code> / <code>.OnKeyUp</code> / <code>.OnPreviewKeyDown</code> / <code>.OnPreviewKeyUp</code> / <code>.OnCharacterReceived</code></td><td>Keyboard events. Preview-pair tunnels; the unprefixed pair bubbles.</td></tr><tr><td><code>.OnGotFocus</code> / <code>.OnLostFocus</code></td><td>Focus events; bubble by default.</td></tr><tr><td><code>.OnDragStart&#x3C;TEl,T></code> / <code>.OnDrop&#x3C;TEl,T></code> / <code>.OnDragOver</code></td><td>Drag-and-drop sources and targets with typed in-process payloads.</td></tr><tr><td><code>.TabIndex(n)</code> / <code>.IsTabStop()</code> / <code>.AccessKey(\"S\")</code> / <code>.TabNavigation(...)</code></td><td>Declarative focus order and keyboard navigation.</td></tr><tr><td><a href=\"/hooks/\"><code>UseElementFocus</code></a></td><td>Untyped element ref + dispatcher-scheduled <code>RequestFocus()</code> action.</td></tr><tr><td><a href=\"/hooks/\"><code>UseElementRef&#x3C;T></code></a></td><td>Typed element ref for calling methods on the underlying control.</td></tr><tr><td><a href=\"/accessibility/\"><code>UseFocusTrap</code></a></td><td>Keyboard focus trapping for modals and flyouts.</td></tr></tbody></table>\n<h2 id=\"pointer-tap-and-keyboard-modifiers\">Pointer, tap, and keyboard modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pointer-tap-and-keyboard-modifiers\">#</a></h2>\n<p>Every <a href=\"/components/\">component</a> exposes the full WinUI input event surface as\n<code>.On*</code> modifiers. Attach a handler; Reactor auto-enables the matching flag and\ninstalls a trampoline so re-renders don't re-subscribe the underlying event.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PointerModifiersExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (hover, setHover) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> (tapCount, setTapCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Border(TextBlock(hover ? <span class=\"hljs-string\">\"hovered\"</span> : <span class=\"hljs-string\">\"hover me\"</span>)\n                .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center))\n                .Width(<span class=\"hljs-number\">240</span>).Height(<span class=\"hljs-number\">120</span>)\n                .Background(hover ? <span class=\"hljs-string\">\"#BFE3FF\"</span> : <span class=\"hljs-string\">\"#E5F1FB\"</span>)\n                .CornerRadius(<span class=\"hljs-number\">8</span>)\n                .OnPointerEntered((_, _) => setHover(<span class=\"hljs-literal\">true</span>))\n                .OnPointerExited((_, _) => setHover(<span class=\"hljs-literal\">false</span>))\n                .OnTapped((_, _) => setTapCount(tapCount + <span class=\"hljs-number\">1</span>))\n                .OnDoubleTap(() => setTapCount(<span class=\"hljs-number\">0</span>)),\n\n            TextBlock(<span class=\"hljs-string\">$\"Tapped <span class=\"hljs-subst\">{tapCount}</span> time(s) — double-tap to reset\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/input-and-gestures/pointer-modifiers.png\" alt=\"Pointer modifiers on a Border with hover, tap, and double-tap reset\"></p>\n<p>Auto-enablement keeps handlers honest: attaching <code>.OnDoubleTapped(...)</code> sets\n<code>IsDoubleTapEnabled = true</code> on the mounted control so the WinUI event actually\nfires. Remove the handler on a later render and the flag goes back to its\ndefault. Shape subclasses (for example <code>Rectangle</code> and <code>Ellipse</code>) also get a\ntransparent <code>Fill</code> auto-assigned when a pointer handler is attached and no fill\nwas set — otherwise hit-testing would miss the unfilled shape entirely.</p>\n<h3 id=\"keyboard-events\">Keyboard events<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyboard-events\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">TextBox(<span class=\"hljs-keyword\">value</span>, setValue)\n    .OnKeyDown((_, e) => { <span class=\"hljs-keyword\">if</span> (e.Key == VirtualKey.Enter) Submit(); })\n    .OnPreviewKeyDown((_, e) => Trace(<span class=\"hljs-string\">\"preview\"</span>))\n    .OnCharacterReceived((_, e) => Validate(e.Character));\n</code></pre>\n<p><code>OnPreviewKeyDown</code> / <code>OnPreviewKeyUp</code> map to the WinUI tunneling events and fire\nbefore the bubbling <code>OnKeyDown</code> / <code>OnKeyUp</code> pair, which is the right spot for\nshortcut interception that needs to suppress further routing.</p>\n<h3 id=\"focus-events\">Focus events<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-events\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">TextBox(<span class=\"hljs-keyword\">value</span>, setValue)\n    .OnGotFocus((_, _) => ShowValidationHint())\n    .OnLostFocus((_, _) => HideValidationHint());\n</code></pre>\n<p>Focus events flow through the same trampoline pattern as pointer events, so the\nhandler closure is updated in-place on each render and the WinUI subscription is\ninstalled exactly once per element.</p>\n<h2 id=\"continuous-gestures\">Continuous gestures<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#continuous-gestures\">#</a></h2>\n<p>Pan, pinch, and rotate are continuous gestures: a single user interaction\nproduces a stream of callbacks with deltas relative to the gesture start. Each\ngesture takes an <code>onChanged</code> action plus optional <code>onBegan</code> / <code>onEnded</code>\ncallbacks that fire exactly once per interaction.</p>\n<h3 id=\"pan\">Pan<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pan\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">Rectangle()\n    .OnPan(\n        onChanged: g => Translate(g.Translation),\n        onEnded: g => SnapToGrid(g.Translation),\n        minimumDistance: <span class=\"hljs-number\">8.0</span>,\n        axis: PanAxis.Both,\n        withInertia: <span class=\"hljs-literal\">true</span>);\n</code></pre>\n<p><code>minimumDistance</code> gates callbacks: until the cumulative translation exceeds the\nthreshold, nothing dispatches. On first crossing the reconciler emits <code>onBegan</code>\n(with <code>Phase = Began</code>) followed by the current delta as <code>Phase = Changed</code>. If\nthe manipulation completes before the threshold is crossed, neither <code>onBegan</code>\nnor <code>onEnded</code> fire.</p>\n<p><code>PanGesture.Translation</code> is cumulative since <code>Began</code>; <code>PanGesture.Delta</code> is\nper-callback.</p>\n<h3 id=\"keep-pans-at-60-hz-write-translation-directly\">Keep pans at 60 Hz: write Translation directly<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keep-pans-at-60-hz-write-translation-directly\">#</a></h3>\n<p>Reactor's render loop re-enqueues at <code>DispatcherQueuePriority.Low</code> on every\ntick so layout and paint aren't starved by back-to-back setState calls. That's\ncorrect for general UI, but it means a per-event <code>setState</code> during pan drops\nframes — by the time the Low-priority render drains, several manipulation\nevents have already piled up. <code>setOffset(offset + g.Delta)</code> would also read a\nstale <code>offset</code> closure, compounding the problem.</p>\n<p>For smooth 60 Hz panning, grab a ref to the mounted element and write\n<code>Translation</code> directly inside <code>onChanged</code>. No reconciler round-trip; the\ncompositor property update is cheap enough to run on every tick. Only call\nsetState at gesture end — once per drag, not 60× per second.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PanGestureExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// For 60 Hz smooth panning, write directly to the mounted element's</span>\n        <span class=\"hljs-comment\">// Translation inside onChanged. Going through setState would queue</span>\n        <span class=\"hljs-comment\">// Low-priority re-renders that get starved by the manipulation event</span>\n        <span class=\"hljs-comment\">// stream itself, producing a laggy drag. The committedRef holds the</span>\n        <span class=\"hljs-comment\">// position at the last gesture end so successive drags accumulate.</span>\n        <span class=\"hljs-comment\">// Reset lives on a sibling Button because WinUI suppresses the tap</span>\n        <span class=\"hljs-comment\">// recognizer when ManipulationMode ≠ System — .OnDoubleTap on the same</span>\n        <span class=\"hljs-comment\">// element as .OnPan wouldn't fire.</span>\n        <span class=\"hljs-keyword\">var</span> cardRef = UseRef&#x3C;FrameworkElement?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> committedRef = UseRef(Vector2.Zero);\n        <span class=\"hljs-keyword\">var</span> (offset, setOffset) = UseState(Vector2.Zero);\n\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Reset</span>()</span>\n        {\n            committedRef.Current = Vector2.Zero;\n            setOffset(Vector2.Zero);\n            <span class=\"hljs-keyword\">if</span> (cardRef.Current <span class=\"hljs-keyword\">is</span> { } fe)\n                fe.Translation = System.Numerics.Vector3.Zero;\n        }\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Border(\n                Border(TextBlock(<span class=\"hljs-string\">\"drag me\"</span>)\n                    .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center))\n                    .Width(<span class=\"hljs-number\">120</span>).Height(<span class=\"hljs-number\">120</span>)\n                    .Background(<span class=\"hljs-string\">\"#3A7BD5\"</span>)\n                    .Foreground(<span class=\"hljs-string\">\"#ffffff\"</span>)\n                    .CornerRadius(<span class=\"hljs-number\">8</span>)\n                    .Translation(offset.X, offset.Y, <span class=\"hljs-number\">0</span>)\n                    .OnMount(fe => cardRef.Current = fe)\n                    .OnPan(\n                        onChanged: g =>\n                        {\n                            <span class=\"hljs-keyword\">var</span> next = committedRef.Current +\n                                <span class=\"hljs-keyword\">new</span> Vector2((<span class=\"hljs-built_in\">float</span>)g.Translation.X, (<span class=\"hljs-built_in\">float</span>)g.Translation.Y);\n                            <span class=\"hljs-keyword\">if</span> (cardRef.Current <span class=\"hljs-keyword\">is</span> { } fe)\n                                fe.Translation = <span class=\"hljs-keyword\">new</span> System.Numerics.Vector3(next.X, next.Y, <span class=\"hljs-number\">0</span>);\n                        },\n                        onEnded: g =>\n                        {\n                            committedRef.Current += <span class=\"hljs-keyword\">new</span> Vector2((<span class=\"hljs-built_in\">float</span>)g.Translation.X, (<span class=\"hljs-built_in\">float</span>)g.Translation.Y);\n                            setOffset(committedRef.Current);\n                        },\n                        withInertia: <span class=\"hljs-literal\">true</span>)\n            ).Height(<span class=\"hljs-number\">260</span>).Background(<span class=\"hljs-string\">\"#f3f3f3\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Padding(<span class=\"hljs-number\">16</span>),\n\n            Button(<span class=\"hljs-string\">\"Reset position\"</span>, Reset)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/input-and-gestures/pan-gesture.png\" alt=\"Card with .OnPan that writes Translation directly for 60 Hz drag\"></p>\n<p>If the panned position doesn't drive anything else on the screen (no adjacent\ncounter, no snap-to-grid indicator), you can even skip the setState at\ngesture end and treat the ref as the single source of truth.</p>\n<h3 id=\"can-i-combine-onpan-with-ontapped--ondoubletapped-on-the-same-element\">Can I combine <code>.OnPan</code> with <code>.OnTapped</code> / <code>.OnDoubleTapped</code> on the same element?<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#can-i-combine-onpan-with-ontapped--ondoubletapped-on-the-same-element\">#</a></h3>\n<p>Not reliably. WinUI suppresses the tap, double-tap, right-tap, and hold\nrecognizers whenever <code>ManipulationMode</code> is set to anything other than <code>System</code>\n— adding <code>.OnPan</code> (or <code>.OnPinch</code> / <code>.OnRotate</code>) flips the mode to the\nspecific gesture bits, so taps stop firing on the same element. If you need\n\"drag, but also click to reset,\" put the click target on a separate sibling\n(a Reset button, an overlay, a parent Border) rather than the drag surface\nitself.</p>\n<p><code>axis: PanAxis.Horizontal</code> restricts to X-axis translation; <code>withInertia: true</code>\nadds the inertia flag so WinUI continues to emit callbacks after the pointer\nlifts.</p>\n<h3 id=\"pinch-and-rotate\">Pinch and rotate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pinch-and-rotate\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">Image(uri)\n    .OnPinch(\n        onChanged: g => Scale(g.Scale),\n        withInertia: <span class=\"hljs-literal\">true</span>)\n    .OnRotate(\n        onChanged: g => Rotate(g.Angle));\n</code></pre>\n<p>Both gestures share the same <code>Phase</code> lifecycle as pan. <code>ScaleDelta</code> and\n<code>AngleDelta</code> report the per-callback delta; <code>Scale</code> and <code>Angle</code> are cumulative\nsince <code>Began</code>. Combining gestures on one element is expected — the reconciler\nunions the required <code>ManipulationModes</code> flags.</p>\n<h3 id=\"long-press\">Long press<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#long-press\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LongPressExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (log, setLog) = UseState(<span class=\"hljs-string\">\"hold the card for 500 ms\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Border(TextBlock(<span class=\"hljs-string\">\"Hold me\"</span>)\n                .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center))\n                .Height(<span class=\"hljs-number\">80</span>).Background(<span class=\"hljs-string\">\"#FFF4CE\"</span>).CornerRadius(<span class=\"hljs-number\">6</span>).Padding(<span class=\"hljs-number\">12</span>)\n                .OnLongPress(\n                    g => setLog(<span class=\"hljs-string\">$\"long-press after <span class=\"hljs-subst\">{g.Duration.TotalMilliseconds:F0}</span>ms\"</span>),\n                    enableMouseEmulation: <span class=\"hljs-literal\">true</span>),\n\n            TextBlock(log)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/input-and-gestures/long-press.png\" alt=\"Long-press gesture showing duration of last trigger\"></p>\n<p>Long-press is touch-and-pen first: the reconciler routes <code>Holding</code> events into\nthe callback and sets <code>IsHoldingEnabled = true</code>. Mouse long-press is off by\ndefault because WinUI's <code>Holding</code> event doesn't fire for mouse pointers; opt in\nwith <code>enableMouseEmulation: true</code> and the reconciler arms a <code>DispatcherTimer</code>\nthat cancels on release, capture loss, or pointer motion past <code>cancelDistance</code>.</p>\n<pre><code class=\"hljs language-csharp\">listItem.OnLongPress(() => ShowContextMenu(), enableMouseEmulation: <span class=\"hljs-literal\">true</span>);\n</code></pre>\n<h2 id=\"focus-and-access-keys\">Focus and access keys<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-and-access-keys\">#</a></h2>\n<h3 id=\"declarative-focus-modifiers\">Declarative focus modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#declarative-focus-modifiers\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">Button(<span class=\"hljs-string\">\"Submit\"</span>, onClick)\n    .TabIndex(<span class=\"hljs-number\">3</span>)\n    .AccessKey(<span class=\"hljs-string\">\"S\"</span>)\n    .IsTabStop();   <span class=\"hljs-comment\">// default-true overload</span>\n</code></pre>\n<p><code>AccessKey</code> bound on a <code>.Command(...)</code> can be overridden per-site: a later\n<code>.AccessKey(...)</code> wins via the normal modifier-after-command ordering.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Save\"</span>, Execute = OnSave, AccessKey = <span class=\"hljs-string\">\"S\"</span> };\nButton(save).AccessKey(<span class=\"hljs-string\">\"F\"</span>);   <span class=\"hljs-comment\">// \"F\" wins on this site</span>\n</code></pre>\n<p>Advanced focus knobs are first-class too:</p>\n<pre><code class=\"hljs language-csharp\">container\n    .TabNavigation(KeyboardNavigationMode.Once)\n    .XYFocusKeyboardNavigation(XYFocusKeyboardNavigationMode.Enabled);\n</code></pre>\n<h3 id=\"imperative-focus-with-refs\">Imperative focus with refs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#imperative-focus-with-refs\">#</a></h3>\n<p>Sometimes you need to focus a control from an effect or event handler — for\nexample, auto-focusing the first input on mount. Use <code>UseElementFocus</code> to get a\nstable <code>ElementRef</code> plus a <code>RequestFocus</code> action that schedules the focus on the\nUI dispatcher so it runs after the current reconcile pass.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">UseElementFocusExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (inputRef, requestFocus) = <span class=\"hljs-keyword\">this</span>.UseElementFocus();\n        UseEffect(() => requestFocus(), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"The field below auto-focuses on mount via UseElementFocus().\"</span>),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"name\"</span>).Width(<span class=\"hljs-number\">280</span>).Ref(inputRef)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/input-and-gestures/use-element-focus.png\" alt=\"Form auto-focusing its first input on mount via UseElementFocus\"></p>\n<p><code>ElementRef.Current</code> is null until the referenced element mounts; the ref\nsurvives re-renders so the same instance reliably points at the currently\nmounted control. Call <code>Microsoft.UI.Reactor.Input.FocusManager.Focus(ref)</code> for\na synchronous focus attempt or <code>FocusManager.FocusAsync(ref)</code> for WinUI's\nasync API with a success result.</p>\n<h4 id=\"typed-refs\">Typed refs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#typed-refs\">#</a></h4>\n<p>When you actually need to call methods on the underlying control (e.g.\n<code>SelectAll()</code> on a <code>TextBox</code>, <code>Focus(FocusState.Programmatic)</code> on a <code>Button</code>),\nuse <code>UseElementRef&#x3C;T>()</code> instead. It gives you an <code>ElementRef&#x3C;T></code> whose\n<code>.Current</code> is already typed as <code>T</code> — no <code>as TextBox</code> cast at the call site:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SearchBox</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> inputRef = Context.UseElementRef&#x3C;TextBox>();\n\n        Context.UseEffect(() => inputRef.Current?.SelectAll(), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n        <span class=\"hljs-keyword\">return</span> TextBox(query, setQuery).Ref(inputRef);\n    }\n}\n</code></pre>\n<p>The constraint <code>T : FrameworkElement</code> keeps the ref type-checked. In <code>DEBUG</code>\nbuilds Reactor asserts the actual mounted element is a <code>T</code>; in release the\nmismatch is silent and <code>.Current</code> returns <code>null</code>.</p>\n<h2 id=\"behind-the-scenes-trampoline-dispatch\">Behind the scenes: trampoline dispatch<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#behind-the-scenes-trampoline-dispatch\">#</a></h2>\n<p>Re-rendering an element with a fresh handler closure is the common case in a\ndata-driven UI. Naively subscribing / unsubscribing on every render costs a COM\nround-trip per event per element — enough to dominate a 1,000-item list's frame\nbudget. Reactor installs one stable <em>trampoline</em> delegate per event per element\nand updates a mutable field that the trampoline reads. Re-renders swap the\nfield; the WinUI subscription is untouched.</p>\n<p>You don't opt in or configure this — every <code>.On*</code> modifier routes through the\ntrampoline path automatically. Inspect the\n<a href=\"/dev-tooling/\">devtools</a> ETW <code>EventDispatch</code> keyword (<code>0x40</code>) to see exactly\nwhen each trampoline attaches and dispatches.</p>\n<h2 id=\"migration-from-set-passthrough\">Migration from <code>.Set(...)</code> passthrough<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#migration-from-set-passthrough\">#</a></h2>\n<p>Pre-Tier-1 code often reaches through <code>.Set(...)</code> to subscribe to an event that\nthe element type didn't model declaratively:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Before — escapes the declarative surface and bypasses trampoline dispatch.</span>\nRectangle().Set(r =>\n{\n    r.PointerEntered += (_, _) => Hover();\n    r.PointerExited += (_, _) => Unhover();\n});\n</code></pre>\n<p>After Tier 1 lands, every pointer, tap, keyboard, and focus event has a\nfirst-class modifier. Replace the <code>.Set(...)</code> block with one modifier call per\nevent — shorter, re-render-safe, and covered by the reconciler's auto-enable\nlogic:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// After</span>\nRectangle()\n    .OnPointerEntered((_, _) => Hover())\n    .OnPointerExited((_, _) => Unhover());\n</code></pre>\n<h2 id=\"drag-and-drop\">Drag and drop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#drag-and-drop\">#</a></h2>\n<p>Reactor's DnD surface is a declarative wrapper over the full Windows drag-and-drop\nprotocol (<code>CanDrag</code> sources, <code>AllowDrop</code> targets, <code>DragStarting</code> / <code>Drop</code> /\n<code>DropCompleted</code>, <code>DataPackage</code> with text / URI / HTML / RTF / files / bitmap /\ncustom formats, <code>DragUIOverride</code> drop-indicator tweaks). Sources and targets\nauto-wire the underlying flags — you set a modifier, the reconciler flips the\nbit and subscribes once via the same trampoline path every other event uses.</p>\n<h3 id=\"typed-in-process-payloads\">Typed in-process payloads<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#typed-in-process-payloads\">#</a></h3>\n<p>For reorder-within-one-app scenarios (kanban columns, sortable lists), attach a\ntyped payload on the source and a matching typed drop handler on the target.\nThe payload is ferried through an in-memory transfer registry keyed by a GUID\nwritten into <code>DataPackage.Properties</code>, so arbitrary CLR objects can round-trip\nwithout a serializer.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">KanbanCard</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> Id, <span class=\"hljs-built_in\">string</span> Title</span>)</span>;\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">KanbanDndExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (todo, setTodo) = UseState&#x3C;IReadOnlyList&#x3C;KanbanCard>>(<span class=\"hljs-keyword\">new</span> KanbanCard[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"k1\"</span>, <span class=\"hljs-string\">\"Write docs\"</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"k2\"</span>, <span class=\"hljs-string\">\"Ship feature\"</span>),\n        });\n        <span class=\"hljs-keyword\">var</span> (done, setDone) = UseState&#x3C;IReadOnlyList&#x3C;KanbanCard>>(Array.Empty&#x3C;KanbanCard>());\n\n        <span class=\"hljs-function\">Element <span class=\"hljs-title\">Column</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> label,\n            IReadOnlyList&#x3C;KanbanCard> cards,\n            Action&#x3C;IReadOnlyList&#x3C;KanbanCard>> setThis</span>)</span>\n        {\n            <span class=\"hljs-keyword\">var</span> children = <span class=\"hljs-keyword\">new</span> List&#x3C;Element> { TextBlock(label).SemiBold() };\n            <span class=\"hljs-keyword\">foreach</span> (<span class=\"hljs-keyword\">var</span> card <span class=\"hljs-keyword\">in</span> cards)\n            {\n                <span class=\"hljs-keyword\">var</span> captured = card;\n                children.Add(\n                    Border(TextBlock(captured.Title).Foreground(<span class=\"hljs-string\">\"#ffffff\"</span>))\n                        .Background(<span class=\"hljs-string\">\"#4B7BEC\"</span>).CornerRadius(<span class=\"hljs-number\">6</span>).Padding(<span class=\"hljs-number\">10</span>)\n                        .OnDragStart&#x3C;BorderElement, KanbanCard>(\n                            getPayload: () => captured,\n                            allowedOperations: DragOperations.Move,\n                            onEnd: ctx =>\n                            {\n                                <span class=\"hljs-keyword\">if</span> (!ctx.WasCancelled &#x26;&#x26; ctx.CompletedOperation == DragOperations.Move)\n                                    setThis(cards.Where(c => c.Id != captured.Id).ToList());\n                            }));\n            }\n            <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">6</span>, children.ToArray())\n                .OnDrop&#x3C;StackElement, KanbanCard>(\n                    onDrop: c =>\n                    {\n                        <span class=\"hljs-keyword\">if</span> (!cards.Any(x => x.Id == c.Id))\n                            setThis(cards.Append(c).ToList());\n                    },\n                    acceptedOps: DragOperations.Move);\n        }\n\n        <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">12</span>,\n            Border(Column(<span class=\"hljs-string\">\"Todo\"</span>, todo, setTodo))\n                .Width(<span class=\"hljs-number\">240</span>).Background(<span class=\"hljs-string\">\"#F7F7F7\"</span>).CornerRadius(<span class=\"hljs-number\">6</span>).Padding(<span class=\"hljs-number\">10</span>),\n            Border(Column(<span class=\"hljs-string\">\"Done\"</span>, done, setDone))\n                .Width(<span class=\"hljs-number\">240</span>).Background(<span class=\"hljs-string\">\"#F1FFF4\"</span>).CornerRadius(<span class=\"hljs-number\">6</span>).Padding(<span class=\"hljs-number\">10</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/input-and-gestures/kanban-dnd.png\" alt=\"Typed kanban drag-and-drop columns with move-on-confirmation\"></p>\n<h3 id=\"standard-formats--cross-process-interop\">Standard formats + cross-process interop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#standard-formats--cross-process-interop\">#</a></h3>\n<p>Eager setters (<code>.WithText</code>, <code>.WithUri</code>, <code>.WithHtml</code>, <code>.WithRtf</code>, <code>.WithFiles</code>,\n<code>.WithBitmap</code>, <code>.WithCustomFormat</code>) write directly to the <code>DataPackage</code>, so\nNotepad / Word / File Explorer pick the drop up natively. On the target side,\n<code>TryGetText(out string)</code> and <code>GetTextAsync(CancellationToken)</code> work the same\nwhether the drag originated from the same process or a different one.</p>\n<pre><code class=\"hljs language-csharp\">Border(Text(<span class=\"hljs-string\">\"Drag me to Notepad\"</span>))\n    .OnDragStart&#x3C;BorderElement>(() => <span class=\"hljs-keyword\">new</span> DragData().WithText(<span class=\"hljs-string\">\"hello world\"</span>));\n\nRectangle()\n    .OnDrop&#x3C;RectangleElement>(<span class=\"hljs-keyword\">args</span> =>\n    {\n        <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">args</span>.Data.TryGetText(<span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> text))\n            Log(text);\n        <span class=\"hljs-keyword\">args</span>.AcceptedOperation = DragOperations.Copy;\n    });\n</code></pre>\n<h3 id=\"lazy-providers--pay-only-when-the-target-asks\">Lazy providers — pay only when the target asks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lazy-providers--pay-only-when-the-target-asks\">#</a></h3>\n<p>If producing the payload is expensive (rendering HTML from a view model,\nreading a large file, round-tripping through a server), register a provider\ninstead of an eager value. Reactor adapts your <code>Func&#x3C;T></code> or\n<code>Func&#x3C;CancellationToken, Task&#x3C;T>></code> onto WinUI's <code>DataProviderHandler</code>: the\ndeferral is acquired, your code runs on the thread pool, and the result is\npublished when the target requests that format. A target that only reads text\nnever pays the HTML cost.</p>\n<pre><code class=\"hljs language-csharp\">Border(Text(<span class=\"hljs-string\">\"Rich content\"</span>))\n    .OnDragStart&#x3C;BorderElement>(() => <span class=\"hljs-keyword\">new</span> DragData()\n        .WithText(<span class=\"hljs-string\">\"plain fallback\"</span>)\n        .WithHtml(ct => RenderExpensiveHtmlAsync(ct)));\n</code></pre>\n<h3 id=\"drop-indicator-overrides\">Drop indicator overrides<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#drop-indicator-overrides\">#</a></h3>\n<p>Drop-over callbacks can tweak the caption, glyph, and content-preview\nvisibility via <code>DragTargetArgs.UIOverride</code> — Reactor writes the changes back\nonto WinUI's <code>DragUIOverride</code> after your callback returns.</p>\n<pre><code class=\"hljs language-csharp\">VStack(children)\n    .OnDragOver(<span class=\"hljs-keyword\">args</span> =>\n    {\n        <span class=\"hljs-keyword\">args</span>.UIOverride.Caption = <span class=\"hljs-string\">\"Move to Inbox\"</span>;\n        <span class=\"hljs-keyword\">args</span>.UIOverride.IsGlyphVisible = <span class=\"hljs-literal\">false</span>;\n        <span class=\"hljs-keyword\">args</span>.AcceptedOperation = DragOperations.Move;\n    })\n    .OnDrop&#x3C;VStackElement, Card>(card => inbox.Add(card));\n</code></pre>\n<h3 id=\"the-move-on-confirmation-pattern\">The move-on-confirmation pattern<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-move-on-confirmation-pattern\">#</a></h3>\n<p>When a source declares <code>DragOperations.Move</code>, the source is responsible for\nremoving the moved item — but only after the drop is confirmed. Never remove\noptimistically in <code>getPayload</code>: the user might cancel (ESC), the drop might\nland outside any target, or a Ctrl-drag might downgrade Move to Copy. Wait for\n<code>onEnd</code> and branch on <code>CompletedOperation</code>:</p>\n<pre><code class=\"hljs language-csharp\">Border(Text(card.Title))\n    .OnDragStart&#x3C;BorderElement, Card>(\n        getPayload: () => card,\n        allowedOperations: DragOperations.Move | DragOperations.Copy,\n        onEnd: ctx =>\n        {\n            <span class=\"hljs-keyword\">if</span> (ctx.WasCancelled) <span class=\"hljs-keyword\">return</span>;\n            <span class=\"hljs-keyword\">if</span> (ctx.CompletedOperation == DragOperations.Move)\n                column.Remove(card);  <span class=\"hljs-comment\">// confirmed move — safe to remove</span>\n            <span class=\"hljs-comment\">// else: Copy succeeded, source keeps the item</span>\n        });\n</code></pre>\n<p><code>DragEndContext.WasCancelled</code> is true when the drop fell outside every valid\ntarget (ESC, dropped on empty space, system abort).\n<code>DragEndContext.CompletedOperation</code> carries the final negotiated operation\n— whatever the target set via <code>DragTargetArgs.AcceptedOperation</code> — or\n<code>DragOperations.None</code> when cancelled.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>.OnPointerPressed</code> on a parent fires <strong>after</strong> its children when using the\ndefault bubbling phase — Reactor mirrors WinUI's routed-event model, where\nevents start at the deepest hit-tested element and propagate up.\nIf a parent needs to capture the press <em>before</em> children see it, you have two\noptions: (1) set the matching <code>.On&#x3C;Event>Handled(true)</code> on the parent (the\nhandled-too overload) so the parent runs even if a child marked the event\nhandled, and bubble order still applies (parent runs last); (2) attach to\nthe preview/tunnel pair (<code>.OnPreviewKeyDown</code> for keys; for pointer events\nthere is no preview pair — you must drop down to the manual\n<code>AddHandler(PointerPressedEvent, handler, handledEventsToo: true)</code> via\n<code>.Set(c => ...)</code>). The trap: relying on tunneling for pointer capture\nsilently does the wrong thing in 80% of cases because the tunnel pair\ndoesn't exist for pointer events. The\n<a href=\"/focus-and-input-internals/\">focus-and-input-internals</a> page walks the\nrouted-event phases end-to-end.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"drag-reorder-a-list\">Drag-reorder a list<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#drag-reorder-a-list\">#</a></h3>\n<p>Combine <code>.OnDragStart&#x3C;TEl, T></code> on each item with <code>.OnDrop&#x3C;TEl, T></code> on the\nlist container. The kanban example above is the canonical shape — typed\npayload, GUID transfer registry, move-on-confirmation. For a\nrecipe-style walkthrough with full state management and animation,\nsee <a href=\"/recipes/drag-reorder/\">recipes/drag-reorder</a>.</p>\n<h3 id=\"pinch-to-zoom-on-an-image\">Pinch-to-zoom on an image<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pinch-to-zoom-on-an-image\">#</a></h3>\n<p>Combine <code>.OnPinch</code> with a <code>Translation</code> scale modifier driven from\nstate. Scale is cumulative since <code>Began</code> — apply it directly to the\nelement's <code>Scale</code> compositor property in <code>onChanged</code> for 60 Hz\nbehavior (same pattern as pan above), then commit to <code>UseState</code> in\n<code>onEnded</code>. The reconciler unions <code>ManipulationModes</code> so adding\n<code>.OnRotate</code> on the same element is free.</p>\n<h3 id=\"trap-focus-inside-a-modal\">Trap focus inside a modal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#trap-focus-inside-a-modal\">#</a></h3>\n<p>Wrap the modal's content tree in a container with\n<code>.FocusTrap(handle)</code> where the handle comes from\n<a href=\"/accessibility/\"><code>UseFocusTrap(isActive)</code></a>. Tab and Shift+Tab cycle\nwithin the trap; the modal's close handler flips <code>isActive</code> to false,\nwhich lifts the trap and returns focus to the previously-focused\nelement. The <a href=\"/accessibility/\">accessibility</a> page covers the\nfull ARIA dialog shape (announcement, restore-focus-on-close,\n<code>aria-labelledby</code> via <code>AutomationName</code>).</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"relying-on-tunneling-for-pointer-capture\">Relying on tunneling for pointer capture<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#relying-on-tunneling-for-pointer-capture\">#</a></h3>\n<p>Reactor exposes pointer events through the bubbling phase only —\nthere is no <code>.OnPreviewPointerPressed</code> / <code>.OnPreviewPointerReleased</code>\nmodifier because WinUI's routed-event surface for pointer events\ndoesn't expose a tunneling pair the same way it does for keyboard.\nIf a parent needs to see the press before children consume it, use\n<code>.OnPointerPressed</code> with the handled-too overload (or\n<code>.Set(c => c.AddHandler(...))</code> for the manual case). The\n<a href=\"#ai:caveat\">caveat above</a> goes into the routing detail.</p>\n<h3 id=\"using-onkeydown-for-accelerator-chords\">Using <code>.OnKeyDown</code> for accelerator chords<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-onkeydown-for-accelerator-chords\">#</a></h3>\n<p><code>.OnKeyDown</code> fires per element on focus — a <code>Ctrl+S</code> handler attached\nto a <code>TextBox</code> only fires while that field has focus. For app-wide\naccelerators (Save, Find, Run), use Reactor's\n<a href=\"/commanding/\">commanding</a> system: <code>new Command { ..., AccessKey = \"S\", AccessKeyModifiers = VirtualKeyModifiers.Control }</code> registers\nthe chord with the WinUI accelerator infrastructure, which routes\nthrough the window's <code>AccessKeyManager</code> regardless of focus. The\nanalyzer <code>REACTOR_INPUT_001</code> flags Ctrl/Alt key combinations attached\nvia <code>.OnKeyDown</code> and suggests the <code>Command</code> rewrite.</p>\n<h3 id=\"forgetting-focusabletrue-on-a-clickable-border\">Forgetting <code>Focusable(true)</code> on a clickable Border<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forgetting-focusabletrue-on-a-clickable-border\">#</a></h3>\n<p>A <code>Border</code> with <code>.OnTapped(...)</code> is hit-testable for pointer events\nbut is not in the keyboard tab order by default — pressing Tab skips\nright past it, which fails <a href=\"/accessibility/\">accessibility</a> (every\ninteractive control must be keyboard-reachable). Add <code>.IsTabStop()</code>\nor set <code>.TabIndex(n)</code> to put the Border in the tab order, and pair\nit with <code>.OnKeyDown</code> for Enter/Space activation. The\n<a href=\"/accessibility/\"><code>AccessibilityScanner</code></a> catches this as\n<code>A11Y_KEYBOARD_001</code>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use the high-level recognizer when one exists.</strong> <code>.OnTapped</code> is\nthe right primitive for click — it filters out drags, handles\ntouch and mouse uniformly, and respects the system's tap-distance\nthreshold. Reach for <code>.OnPointerPressed</code> only when you need the\nraw event (preview, capture, multi-touch coordination).</p>\n<p><strong>Write <code>Translation</code> directly during pan.</strong> A per-event <code>setState</code>\nduring pan drops frames — Reactor's render loop priorities lose to\nthe manipulation event stream. Grab a ref to the mounted element\nand write <code>Translation</code> inside <code>onChanged</code>; commit to state once on\n<code>onEnded</code>. The pan snippet above is the canonical shape.</p>\n<p><strong>Long-press needs <code>enableMouseEmulation: true</code> for mouse users.</strong>\nWinUI's <code>Holding</code> event doesn't fire for mouse pointers by design.\nIf your context-menu trigger needs mouse support (most do), opt in\nexplicitly. Touch and pen work without it.</p>\n<p><strong>Bridge to <code>Command</code> for anything keyboard-shortcut-shaped.</strong>\nPer-element <code>.OnKeyDown</code> handlers are right for \"Enter submits this\nform\"; app-wide chords belong on a <code>Command</code> with\n<code>AccessKey</code> + <code>AccessKeyModifiers</code>. See\n<a href=\"/commanding/\">commanding</a> for the full surface.</p>\n<p><strong>Run the a11y scanner.</strong> A keyboard-focusable interactive control\nwithout <code>AutomationName</code> or a focus-visible affordance is a bug.\n<a href=\"/accessibility/\"><code>AccessibilityScanner</code></a> flags both at runtime.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/focus-and-input-internals/\">Focus and Input Internals</a></strong> — under-the-hood: trampoline dispatch, routed-event phases, dispatcher coalescing for focus</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — <code>UseFocusTrap</code>, <code>UseAnnounce</code>, <code>AccessibilityScanner</code>, ARIA patterns</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — app-wide accelerators, command bars, the <code>Command</code> and <code>Command&#x3C;T></code> types</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — pairing gestures with <code>.InteractionStates()</code> for hover / press / focus visuals</li>\n<li><strong><a href=\"/recipes/drag-reorder/\">recipes/drag-reorder</a></strong> — full drag-to-reorder list recipe</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — previous topic: components, props, and composition</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Input . Input flows from raw pointer and keyboard events through Microsoft.UI.Reactor (Reactor)'s modifier tower into your component's render logic, with gesture recognizers and focus state stacked on top. The bottom layer is the raw .OnPointerPressed / .OnKeyDown / .OnGotFocus etc. surface, which mirrors WinUI's routed events one-to-one and uses bubbling by default — events start at the deepest element and propagate up the tree. Above that sit the high-level recognizers: .OnTapped , .OnDoubleTapped , .OnPan , .OnPinch , .OnRotate , .OnLongPress — each auto-enables the underlying WinUI flag (e.g. IsDoubleTapEnabled ) and installs a single stable trampoline so re-rendering with a new handler closure costs no re-subscription. Focus is the third tower: declarative .TabIndex / .AccessKey / .IsTabStop modifiers, the UseElementFocus / UseElementRef hooks for imperative focus, and UseFocusTrap for modal containment. Everything is re-render-safe by design; the focus-and-input-internals page covers the dispatcher and trampoline mechanics for readers who want the implementation details. Input and gestures # Reactor's input surface is declarative: attach handlers via .On* modifiers and the reconciler wires WinUI events, auto-enables the matching flags (for example IsDoubleTapEnabled ), and survives re-renders without re-subscribing the underlying WinUI event. This page covers the modifiers you'll reach for on most screens, the higher-level gesture recognizers for continuous input, and the imperative escape hatches — ref-based focus and access keys — that round out the commanding story. Reference # Modifier / hook Purpose .OnPointerPressed / .OnPointerReleased / .OnPointerEntered / .OnPointerExited / .OnPointerMoved Raw pointer events. Bubble by default. .OnTapped / .OnDoubleTapped / .OnRightTapped / .OnHolding High-level tap recognizers. Auto-enable the matching IsXEnabled flag. .OnPan / .OnPinch / .OnRotate / .OnLongPress Continuous-gesture recognizers. Take onBegan / onChanged / onEnded . .OnKeyDown / .OnKeyUp / .OnPreviewKeyDown / .OnPreviewKeyUp / .OnCharacterReceived Keyboard events. Preview-pair tunnels; the unprefixed pair bubbles. .OnGotFocus / .OnLostFocus Focus events; bubble by default. .OnDragStart&#x3C;TEl,T> / .OnDrop&#x3C;TEl,T> / .OnDragOver Drag-and-drop sources and targets with typed in-process payloads. .TabIndex(n) / .IsTabStop() / .AccessKey(\"S\") / .TabNavigation(...) Declarative focus order and keyboard navigation. UseElementFocus Untyped element ref + dispatcher-scheduled RequestFocus() action. UseElementRef&#x3C;T> Typed element ref for calling methods on the underlying control. UseFocusTrap Keyboard focus trapping for modals and flyouts. Pointer, tap, and keyboard modifiers # Every component exposes the full WinUI input event surface as .On* modifiers. Attach a handler; Reactor auto-enables the matching flag and installs a trampoline so re-renders don't re-subscribe the underlying event. class PointerModifiersExample : Component { public override Element Render () { var (hover, setHover) = UseState( false ); var (tapCount, setTapCount) = UseState( 0 ); return VStack( 12 , Border(TextBlock(hover ? \"hovered\" : \"hover me\" ) .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)) .Width( 240 ).Height( 120 ) .Background(hover ? \"#BFE3FF\" : \"#E5F1FB\" ) .CornerRadius( 8 ) .OnPointerEntered((_, _) => setHover( true )) .OnPointerExited((_, _) => setHover( false )) .OnTapped((_, _) => setTapCount(tapCount + 1 )) .OnDoubleTap(() => setTapCount( 0 )), TextBlock( $\"Tapped {tapCount} time(s) — double-tap to reset\" ) ).Padding( 24 ); } } Auto-enablement keeps handlers honest: attaching .OnDoubleTapped(...) sets IsDoubleTapEnabled = true on the mounted control so the WinUI event actually fires. Remove the handler on a later render and the flag goes back to its default. Shape subclasses (for example Rectangle and Ellipse ) also get a transparent Fill auto-assigned when a pointer handler is attached and no fill was set — otherwise hit-testing would miss the unfilled shape entirely. Keyboard events # TextBox( value , setValue) .OnKeyDown((_, e) => { if (e.Key == VirtualKey.Enter) Submit(); }) .OnPreviewKeyDown((_, e) => Trace( \"preview\" )) .OnCharacterReceived((_, e) => Validate(e.Character)); OnPreviewKeyDown / OnPreviewKeyUp map to the WinUI tunneling events and fire before the bubbling OnKeyDown / OnKeyUp pair, which is the right spot for shortcut interception that needs to suppress further routing. Focus events # TextBox( value , setValue) .OnGotFocus((_, _) => ShowValidationHint()) .OnLostFocus((_, _) => HideValidationHint()); Focus events flow through the same trampoline pattern as pointer events, so the handler closure is updated in-place on each render and the WinUI subscription is installed exactly once per element. Continuous gestures # Pan, pinch, and rotate are continuous gestures: a single user interaction produces a stream of callbacks with deltas relative to the gesture start. Each gesture takes an onChanged action plus optional onBegan / onEnded callbacks that fire exactly once per interaction. Pan # Rectangle() .OnPan( onChanged: g => Translate(g.Translation), onEnded: g => SnapToGrid(g.Translation), minimumDistance: 8.0 , axis: PanAxis.Both, withInertia: true ); minimumDistance gates callbacks: until the cumulative translation exceeds the threshold, nothing dispatches. On first crossing the reconciler emits onBegan (with Phase = Began ) followed by the current delta as Phase = Changed . If the manipulation completes before the threshold is crossed, neither onBegan nor onEnded fire. PanGesture.Translation is cumulative since Began ; PanGesture.Delta is per-callback. Keep pans at 60 Hz: write Translation directly # Reactor's render loop re-enqueues at DispatcherQueuePriority.Low on every tick so layout and paint aren't starved by back-to-back setState calls. That's correct for general UI, but it means a per-event setState during pan drops frames — by the time the Low-priority render drains, several manipulation events have already piled up. setOffset(offset + g.Delta) would also read a stale offset closure, compounding the problem. For smooth 60 Hz panning, grab a ref to the mounted element and write Translation directly inside onChanged . No reconciler round-trip; the compositor property update is cheap enough to run on every tick. Only call setState at gesture end — once per drag, not 60× per second. class PanGestureExample : Component { public override Element Render () { // For 60 Hz smooth panning, write directly to the mounted element's // Translation inside onChanged. Going through setState would queue // Low-priority re-renders that get starved by the manipulation event // stream itself, producing a laggy drag. The committedRef holds the // position at the last gesture end so successive drags accumulate. // Reset lives on a sibling Button because WinUI suppresses the tap // recognizer when ManipulationMode ≠ System — .OnDoubleTap on the same // element as .OnPan wouldn't fire. var cardRef = UseRef&#x3C;FrameworkElement?>( null ); var committedRef = UseRef(Vector2.Zero); var (offset, setOffset) = UseState(Vector2.Zero); void Reset () { committedRef.Current = Vector2.Zero; setOffset(Vector2.Zero); if (cardRef.Current is { } fe) fe.Translation = System.Numerics.Vector3.Zero; } return VStack( 8 , Border( Border(TextBlock( \"drag me\" ) .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)) .Width( 120 ).Height( 120 ) .Background( \"#3A7BD5\" ) .Foreground( \"#ffffff\" ) .CornerRadius( 8 ) .Translation(offset.X, offset.Y, 0 ) .OnMount(fe => cardRef.Current = fe) .OnPan( onChanged: g => { var next = committedRef.Current + new Vector2(( float )g.Translation.X, ( float )g.Translation.Y); if (cardRef.Current is { } fe) fe.Translation = new System.Numerics.Vector3(next.X, next.Y, 0 ); }, onEnded: g => { committedRef.Current += new Vector2(( float )g.Translation.X, ( float )g.Translation.Y); setOffset(committedRef.Current); }, withInertia: true ) ).Height( 260 ).Background( \"#f3f3f3\" ).CornerRadius( 8 ).Padding( 16 ), Button( \"Reset position\" , Reset) ); } } If the panned position doesn't drive anything else on the screen (no adjacent counter, no snap-to-grid indicator), you can even skip the setState at gesture end and treat the ref as the single source of truth. Can I combine .OnPan with .OnTapped / .OnDoubleTapped on the same element? # Not reliably. WinUI suppresses the tap, double-tap, right-tap, and hold recognizers whenever ManipulationMode is set to anything other than System — adding .OnPan (or .OnPinch / .OnRotate ) flips the mode to the specific gesture bits, so taps stop firing on the same element. If you need \"drag, but also click to reset,\" put the click target on a separate sibling (a Reset button, an overlay, a parent Border) rather than the drag surface itself. axis: PanAxis.Horizontal restricts to X-axis translation; withInertia: true adds the inertia flag so WinUI continues to emit callbacks after the pointer lifts. Pinch and rotate # Image(uri) .OnPinch( onChanged: g => Scale(g.Scale), withInertia: true ) .OnRotate( onChanged: g => Rotate(g.Angle)); Both gestures share the same Phase lifecycle as pan. ScaleDelta and AngleDelta report the per-callback delta; Scale and Angle are cumulative since Began . Combining gestures on one element is expected — the reconciler unions the required ManipulationModes flags. Long press # class LongPressExample : Component { public override Element Render () { var (log, setLog) = UseState( \"hold the card for 500 ms\" ); return VStack( 12 , Border(TextBlock( \"Hold me\" ) .HAlign(HorizontalAlignment.Center).VAlign(VerticalAlignment.Center)) .Height( 80 ).Background( \"#FFF4CE\" ).CornerRadius( 6 ).Padding( 12 ) .OnLongPress( g => setLog( $\"long-press after {g.Duration.TotalMilliseconds:F0} ms\" ), enableMouseEmulation: true ), TextBlock(log) ).Padding( 24 ); } } Long-press is touch-and-pen first: the reconciler routes Holding events into the callback and sets IsHoldingEnabled = true . Mouse long-press is off by default because WinUI's Holding event doesn't fire for mouse pointers; opt in with enableMouseEmulation: true and the reconciler arms a DispatcherTimer that cancels on release, capture loss, or pointer motion past cancelDistance . listItem.OnLongPress(() => ShowContextMenu(), enableMouseEmulation: true ); Focus and access keys # Declarative focus modifiers # Button( \"Submit\" , onClick) .TabIndex( 3 ) .AccessKey( \"S\" ) .IsTabStop(); // default-true overload AccessKey bound on a .Command(...) can be overridden per-site: a later .AccessKey(...) wins via the normal modifier-after-command ordering. var save = new Command { Label = \"Save\" , Execute = OnSave, AccessKey = \"S\" }; Button(save).AccessKey( \"F\" ); // \"F\" wins on this site Advanced focus knobs are first-class too: container .TabNavigation(KeyboardNavigationMode.Once) .XYFocusKeyboardNavigation(XYFocusKeyboardNavigationMode.Enabled); Imperative focus with refs # Sometimes you need to focus a control from an effect or event handler — for example, auto-focusing the first input on mount. Use UseElementFocus to get a stable ElementRef plus a RequestFocus action that schedules the focus on the UI dispatcher so it runs after the current reconcile pass. class UseElementFocusExample : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); var (inputRef, requestFocus) = this .UseElementFocus(); UseEffect(() => requestFocus(), Array.Empty&#x3C; object >()); return VStack( 12 , TextBlock( \"The field below auto-focuses on mount via UseElementFocus().\" ), TextBox(name, setName, placeholderText: \"name\" ).Width( 280 ).Ref(inputRef) ).Padding( 24 ); } } ElementRef.Current is null until the referenced element mounts; the ref survives re-renders so the same instance reliably points at the currently mounted control. Call Microsoft.UI.Reactor.Input.FocusManager.Focus(ref) for a synchronous focus attempt or FocusManager.FocusAsync(ref) for WinUI's async API with a success result. Typed refs # When you actually need to call methods on the underlying control (e.g. SelectAll() on a TextBox , Focus(FocusState.Programmatic) on a Button ), use UseElementRef&#x3C;T>() instead. It gives you an ElementRef&#x3C;T> whose .Current is already typed as T — no as TextBox cast at the call site: public class SearchBox : Component { public override Element Render () { var inputRef = Context.UseElementRef&#x3C;TextBox>(); Context.UseEffect(() => inputRef.Current?.SelectAll(), Array.Empty&#x3C; object >()); return TextBox(query, setQuery).Ref(inputRef); } } The constraint T : FrameworkElement keeps the ref type-checked. In DEBUG builds Reactor asserts the actual mounted element is a T ; in release the mismatch is silent and .Current returns null . Behind the scenes: trampoline dispatch # Re-rendering an element with a fresh handler closure is the common case in a data-driven UI. Naively subscribing / unsubscribing on every render costs a COM round-trip per event per element — enough to dominate a 1,000-item list's frame budget. Reactor installs one stable trampoline delegate per event per element and updates a mutable field that the trampoline reads. Re-renders swap the field; the WinUI subscription is untouched. You don't opt in or configure this — every .On* modifier routes through the trampoline path automatically. Inspect the devtools ETW EventDispatch keyword ( 0x40 ) to see exactly when each trampoline attaches and dispatches. Migration from .Set(...) passthrough # Pre-Tier-1 code often reaches through .Set(...) to subscribe to an event that the element type didn't model declaratively: // Before — escapes the declarative surface and bypasses trampoline dispatch. Rectangle().Set(r => { r.PointerEntered += (_, _) => Hover(); r.PointerExited += (_, _) => Unhover(); }); After Tier 1 lands, every pointer, tap, keyboard, and focus event has a first-class modifier. Replace the .Set(...) block with one modifier call per event — shorter, re-render-safe, and covered by the reconciler's auto-enable logic: // After Rectangle() .OnPointerEntered((_, _) => Hover()) .OnPointerExited((_, _) => Unhover()); Drag and drop # Reactor's DnD surface is a declarative wrapper over the full Windows drag-and-drop protocol ( CanDrag sources, AllowDrop targets, DragStarting / Drop / DropCompleted , DataPackage with text / URI / HTML / RTF / files / bitmap / custom formats, DragUIOverride drop-indicator tweaks). Sources and targets auto-wire the underlying flags — you set a modifier, the reconciler flips the bit and subscribes once via the same trampoline path every other event uses. Typed in-process payloads # For reorder-within-one-app scenarios (kanban columns, sortable lists), attach a typed payload on the source and a matching typed drop handler on the target. The payload is ferried through an in-memory transfer registry keyed by a GUID written into DataPackage.Properties , so arbitrary CLR objects can round-trip without a serializer. sealed record KanbanCard ( string Id, string Title ) ; class KanbanDndExample : Component { public override Element Render () { var (todo, setTodo) = UseState&#x3C;IReadOnlyList&#x3C;KanbanCard>>( new KanbanCard[] { new ( \"k1\" , \"Write docs\" ), new ( \"k2\" , \"Ship feature\" ), }); var (done, setDone) = UseState&#x3C;IReadOnlyList&#x3C;KanbanCard>>(Array.Empty&#x3C;KanbanCard>()); Element Column ( string label, IReadOnlyList&#x3C;KanbanCard> cards, Action&#x3C;IReadOnlyList&#x3C;KanbanCard>> setThis ) { var children = new List&#x3C;Element> { TextBlock(label).SemiBold() }; foreach ( var card in cards) { var captured = card; children.Add( Border(TextBlock(captured.Title).Foreground( \"#ffffff\" )) .Background( \"#4B7BEC\" ).CornerRadius( 6 ).Padding( 10 ) .OnDragStart&#x3C;BorderElement, KanbanCard>( getPayload: () => captured, allowedOperations: DragOperations.Move, onEnd: ctx => { if (!ctx.WasCancelled &#x26;&#x26; ctx.CompletedOperation == DragOperations.Move) setThis(cards.Where(c => c.Id != captured.Id).ToList()); })); } return VStack( 6 , children.ToArray()) .OnDrop&#x3C;StackElement, KanbanCard>( onDrop: c => { if (!cards.Any(x => x.Id == c.Id)) setThis(cards.Append(c).ToList()); }, acceptedOps: DragOperations.Move); } return HStack( 12 , Border(Column( \"Todo\" , todo, setTodo)) .Width( 240 ).Background( \"#F7F7F7\" ).CornerRadius( 6 ).Padding( 10 ), Border(Column( \"Done\" , done, setDone)) .Width( 240 ).Background( \"#F1FFF4\" ).CornerRadius( 6 ).Padding( 10 ) ).Padding( 24 ); } } Standard formats + cross-process interop # Eager setters ( .WithText , .WithUri , .WithHtml , .WithRtf , .WithFiles , .WithBitmap , .WithCustomFormat ) write directly to the DataPackage , so Notepad / Word / File Explorer pick the drop up natively. On the target side, TryGetText(out string) and GetTextAsync(CancellationToken) work the same whether the drag originated from the same process or a different one. Border(Text( \"Drag me to Notepad\" )) .OnDragStart&#x3C;BorderElement>(() => new DragData().WithText( \"hello world\" )); Rectangle() .OnDrop&#x3C;RectangleElement>( args => { if ( args .Data.TryGetText( out var text)) Log(text); args .AcceptedOperation = DragOperations.Copy; }); Lazy providers — pay only when the target asks # If producing the payload is expensive (rendering HTML from a view model, reading a large file, round-tripping through a server), register a provider instead of an eager value. Reactor adapts your Func&#x3C;T> or Func&#x3C;CancellationToken, Task&#x3C;T>> onto WinUI's DataProviderHandler : the deferral is acquired, your code runs on the thread pool, and the result is published when the target requests that format. A target that only reads text never pays the HTML cost. Border(Text( \"Rich content\" )) .OnDragStart&#x3C;BorderElement>(() => new DragData() .WithText( \"plain fallback\" ) .WithHtml(ct => RenderExpensiveHtmlAsync(ct))); Drop indicator overrides # Drop-over callbacks can tweak the caption, glyph, and content-preview visibility via DragTargetArgs.UIOverride — Reactor writes the changes back onto WinUI's DragUIOverride after your callback returns. VStack(children) .OnDragOver( args => { args .UIOverride.Caption = \"Move to Inbox\" ; args .UIOverride.IsGlyphVisible = false ; args .AcceptedOperation = DragOperations.Move; }) .OnDrop&#x3C;VStackElement, Card>(card => inbox.Add(card)); The move-on-confirmation pattern # When a source declares DragOperations.Move , the source is responsible for removing the moved item — but only after the drop is confirmed. Never remove optimistically in getPayload : the user might cancel (ESC), the drop might land outside any target, or a Ctrl-drag might downgrade Move to Copy. Wait for onEnd and branch on CompletedOperation : Border(Text(card.Title)) .OnDragStart&#x3C;BorderElement, Card>( getPayload: () => card, allowedOperations: DragOperations.Move | DragOperations.Copy, onEnd: ctx => { if (ctx.WasCancelled) return ; if (ctx.CompletedOperation == DragOperations.Move) column.Remove(card); // confirmed move — safe to remove // else: Copy succeeded, source keeps the item }); DragEndContext.WasCancelled is true when the drop fell outside every valid target (ESC, dropped on empty space, system abort). DragEndContext.CompletedOperation carries the final negotiated operation — whatever the target set via DragTargetArgs.AcceptedOperation — or DragOperations.None when cancelled. Caveat: .OnPointerPressed on a parent fires after its children when using the default bubbling phase — Reactor mirrors WinUI's routed-event model, where events start at the deepest hit-tested element and propagate up. If a parent needs to capture the press before children see it, you have two options: (1) set the matching .On&#x3C;Event>Handled(true) on the parent (the handled-too overload) so the parent runs even if a child marked the event handled, and bubble order still applies (parent runs last); (2) attach to the preview/tunnel pair ( .OnPreviewKeyDown for keys; for pointer events there is no preview pair — you must drop down to the manual AddHandler(PointerPressedEvent, handler, handledEventsToo: true) via .Set(c => ...) ). The trap: relying on tunneling for pointer capture silently does the wrong thing in 80% of cases because the tunnel pair doesn't exist for pointer events. The focus-and-input-internals page walks the routed-event phases end-to-end. Patterns # Drag-reorder a list # Combine .OnDragStart&#x3C;TEl, T> on each item with .OnDrop&#x3C;TEl, T> on the list container. The kanban example above is the canonical shape — typed payload, GUID transfer registry, move-on-confirmation. For a recipe-style walkthrough with full state management and animation, see recipes/drag-reorder . Pinch-to-zoom on an image # Combine .OnPinch with a Translation scale modifier driven from state. Scale is cumulative since Began — apply it directly to the element's Scale compositor property in onChanged for 60 Hz behavior (same pattern as pan above), then commit to UseState in onEnded . The reconciler unions ManipulationModes so adding .OnRotate on the same element is free. Trap focus inside a modal # Wrap the modal's content tree in a container with .FocusTrap(handle) where the handle comes from UseFocusTrap(isActive) . Tab and Shift+Tab cycle within the trap; the modal's close handler flips isActive to false, which lifts the trap and returns focus to the previously-focused element. The accessibility page covers the full ARIA dialog shape (announcement, restore-focus-on-close, aria-labelledby via AutomationName ). Common Mistakes # Relying on tunneling for pointer capture # Reactor exposes pointer events through the bubbling phase only — there is no .OnPreviewPointerPressed / .OnPreviewPointerReleased modifier because WinUI's routed-event surface for pointer events doesn't expose a tunneling pair the same way it does for keyboard. If a parent needs to see the press before children consume it, use .OnPointerPressed with the handled-too overload (or .Set(c => c.AddHandler(...)) for the manual case). The caveat above goes into the routing detail. Using .OnKeyDown for accelerator chords # .OnKeyDown fires per element on focus — a Ctrl+S handler attached to a TextBox only fires while that field has focus. For app-wide accelerators (Save, Find, Run), use Reactor's commanding system: new Command { ..., AccessKey = \"S\", AccessKeyModifiers = VirtualKeyModifiers.Control } registers the chord with the WinUI accelerator infrastructure, which routes through the window's AccessKeyManager regardless of focus. The analyzer REACTOR_INPUT_001 flags Ctrl/Alt key combinations attached via .OnKeyDown and suggests the Command rewrite. Forgetting Focusable(true) on a clickable Border # A Border with .OnTapped(...) is hit-testable for pointer events but is not in the keyboard tab order by default — pressing Tab skips right past it, which fails accessibility (every interactive control must be keyboard-reachable). Add .IsTabStop() or set .TabIndex(n) to put the Border in the tab order, and pair it with .OnKeyDown for Enter/Space activation. The AccessibilityScanner catches this as A11Y_KEYBOARD_001 . Tips # Use the high-level recognizer when one exists. .OnTapped is the right primitive for click — it filters out drags, handles touch and mouse uniformly, and respects the system's tap-distance threshold. Reach for .OnPointerPressed only when you need the raw event (preview, capture, multi-touch coordination). Write Translation directly during pan. A per-event setState during pan drops frames — Reactor's render loop priorities lose to the manipulation event stream. Grab a ref to the mounted element and write Translation inside onChanged ; commit to state once on onEnded . The pan snippet above is the canonical shape. Long-press needs enableMouseEmulation: true for mouse users. WinUI's Holding event doesn't fire for mouse pointers by design. If your context-menu trigger needs mouse support (most do), opt in explicitly. Touch and pen work without it. Bridge to Command for anything keyboard-shortcut-shaped. Per-element .OnKeyDown handlers are right for \"Enter submits this form\"; app-wide chords belong on a Command with AccessKey + AccessKeyModifiers . See commanding for the full surface. Run the a11y scanner. A keyboard-focusable interactive control without AutomationName or a focus-visible affordance is a bug. AccessibilityScanner flags both at runtime. Next Steps # Focus and Input Internals — under-the-hood: trampoline dispatch, routed-event phases, dispatcher coalescing for focus Accessibility — UseFocusTrap , UseAnnounce , AccessibilityScanner , ARIA patterns Commanding — app-wide accelerators, command bars, the Command and Command&#x3C;T> types Animation — pairing gestures with .InteractionStates() for hover / press / focus visuals recipes/drag-reorder — full drag-to-reorder list recipe Components — previous topic: components, props, and composition",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/input-and-gestures.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/input-and-gestures.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "pointer-tap-and-keyboard-modifiers",
          "text": "Pointer, tap, and keyboard modifiers"
        },
        {
          "depth": 3,
          "id": "keyboard-events",
          "text": "Keyboard events"
        },
        {
          "depth": 3,
          "id": "focus-events",
          "text": "Focus events"
        },
        {
          "depth": 2,
          "id": "continuous-gestures",
          "text": "Continuous gestures"
        },
        {
          "depth": 3,
          "id": "pan",
          "text": "Pan"
        },
        {
          "depth": 3,
          "id": "keep-pans-at-60-hz-write-translation-directly",
          "text": "Keep pans at 60 Hz: write Translation directly"
        },
        {
          "depth": 3,
          "id": "can-i-combine-onpan-with-ontapped--ondoubletapped-on-the-same-element",
          "text": "Can I combine .OnPan with .OnTapped / .OnDoubleTapped on the same element?"
        },
        {
          "depth": 3,
          "id": "pinch-and-rotate",
          "text": "Pinch and rotate"
        },
        {
          "depth": 3,
          "id": "long-press",
          "text": "Long press"
        },
        {
          "depth": 2,
          "id": "focus-and-access-keys",
          "text": "Focus and access keys"
        },
        {
          "depth": 3,
          "id": "declarative-focus-modifiers",
          "text": "Declarative focus modifiers"
        },
        {
          "depth": 3,
          "id": "imperative-focus-with-refs",
          "text": "Imperative focus with refs"
        },
        {
          "depth": 2,
          "id": "behind-the-scenes-trampoline-dispatch",
          "text": "Behind the scenes: trampoline dispatch"
        },
        {
          "depth": 2,
          "id": "migration-from-set-passthrough",
          "text": "Migration from .Set(...) passthrough"
        },
        {
          "depth": 2,
          "id": "drag-and-drop",
          "text": "Drag and drop"
        },
        {
          "depth": 3,
          "id": "typed-in-process-payloads",
          "text": "Typed in-process payloads"
        },
        {
          "depth": 3,
          "id": "standard-formats--cross-process-interop",
          "text": "Standard formats + cross-process interop"
        },
        {
          "depth": 3,
          "id": "lazy-providers--pay-only-when-the-target-asks",
          "text": "Lazy providers — pay only when the target asks"
        },
        {
          "depth": 3,
          "id": "drop-indicator-overrides",
          "text": "Drop indicator overrides"
        },
        {
          "depth": 3,
          "id": "the-move-on-confirmation-pattern",
          "text": "The move-on-confirmation pattern"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "drag-reorder-a-list",
          "text": "Drag-reorder a list"
        },
        {
          "depth": 3,
          "id": "pinch-to-zoom-on-an-image",
          "text": "Pinch-to-zoom on an image"
        },
        {
          "depth": 3,
          "id": "trap-focus-inside-a-modal",
          "text": "Trap focus inside a modal"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "relying-on-tunneling-for-pointer-capture",
          "text": "Relying on tunneling for pointer capture"
        },
        {
          "depth": 3,
          "id": "using-onkeydown-for-accelerator-chords",
          "text": "Using .OnKeyDown for accelerator chords"
        },
        {
          "depth": 3,
          "id": "forgetting-focusabletrue-on-a-clickable-border",
          "text": "Forgetting Focusable(true) on a clickable Border"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Controls Catalog",
      "path": "controls.md",
      "slug": "/controls/",
      "html": "<h1 id=\"controls\">Controls<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#controls\">#</a></h1>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ControlsCatalogApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">\"Controls catalog\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n        TextBlock(<span class=\"hljs-string\">\"Every Reactor control, grouped by category.\"</span>).Opacity(<span class=\"hljs-number\">0.7</span>),\n        Button(<span class=\"hljs-string\">\"Open Forms\"</span>, () => { })\n    ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p>Every visible thing on screen is a control. In Microsoft.UI.Reactor (Reactor) a control is the\nreturn value of a factory like <code>TextBlock(...)</code> or <code>Button(...)</code> — a\nplain element you compose, chain modifiers onto, and return from\n<code>Render()</code>. The seven categories below cover the full set Reactor\nexposes today. Pick a category, open its detail page, and you'll find\nthe factory signature, modifier matrix, and screenshots for every\ncontrol inside it.</p>\n<p>Two ground rules apply across the catalog:</p>\n<ol>\n<li><strong>Every control has a Reactor factory.</strong> No XAML in <code>Render()</code> — the\nfactories on <a href=\"/hooks/\"><code>Microsoft.UI.Reactor.Factories</code></a> are the\nonly thing you need to compose UI.</li>\n<li><strong>WinUI-wrapper controls link out, not duplicate.</strong> When a control\n(like <code>DatePicker</code> or <code>AutoSuggestBox</code>) is a transparent WinUI\nwrapper, the Reactor page covers the factory and modifier surface\nand links to the Microsoft Learn design page for theming, layout\nguidance, and accessibility behavior. Reactor-original controls\n(<code>DataGrid</code>, <code>MarkdownTextBlock</code>, <code>VirtualList</code>, <code>FlexPanel</code>) are\ndocumented exhaustively in their own pages.</li>\n</ol>\n<h2 id=\"categories\">Categories<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#categories\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Category</th><th>What's in it</th><th>Detail page</th></tr></thead><tbody><tr><td>Forms</td><td>Text input, picker, button, validation primitives</td><td><a href=\"/forms/\">Forms</a></td></tr><tr><td>Collections</td><td>List, grid, virtualized list, repeater</td><td><a href=\"/collections/\">Collections</a></td></tr><tr><td>Text &#x26; Media</td><td>Heading, rich text, image, media player</td><td><a href=\"/text-and-media/\">Text &#x26; Media</a></td></tr><tr><td>Status &#x26; Info</td><td>Progress, info bar, badge, teaching tip</td><td><a href=\"/status-and-info/\">Status &#x26; Info</a></td></tr><tr><td>Dialogs &#x26; Flyouts</td><td>Content dialog, menu flyout, command bar flyout</td><td><a href=\"/dialogs-and-flyouts/\">Dialogs &#x26; Flyouts</a></td></tr><tr><td>Data System</td><td>DataGrid, columns, data sources, paging</td><td><a href=\"/data-system/\">Data System</a></td></tr><tr><td>Charting</td><td>Line, bar, area, pie, force graph</td><td><a href=\"/charting/\">Charting</a></td></tr></tbody></table>\n<h2 id=\"forms\">Forms<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forms\">#</a></h2>\n<p>Text input, choice, sliders, buttons, and the validation pipeline.\nForms is the category most apps spend time in; the detail page\n(<a href=\"/forms/\">forms.md</a>) covers controlled-input patterns and the\n<code>FormField</code> validation surface.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FormsGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"Ada\"</span>);\n        <span class=\"hljs-keyword\">var</span> (agree, setAgree) = UseState(<span class=\"hljs-literal\">true</span>);\n        <span class=\"hljs-keyword\">var</span> (volume, setVolume) = UseState(<span class=\"hljs-number\">60.0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Name\"</span>).Width(<span class=\"hljs-number\">200</span>),\n            CheckBox(agree, setAgree, label: <span class=\"hljs-string\">\"I agree\"</span>),\n            Slider(volume, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">100</span>, setVolume).Width(<span class=\"hljs-number\">200</span>),\n            Button(<span class=\"hljs-string\">\"Submit\"</span>, () => { })\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/forms-thumb.png\" alt=\"Forms category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>TextBox</code></td><td>Single-line text input with placeholder + header.</td></tr><tr><td><code>PasswordBox</code></td><td>Obscured text input.</td></tr><tr><td><code>NumberBox</code></td><td>Numeric input with up/down spinner.</td></tr><tr><td><code>CheckBox</code></td><td>Two-state checkbox with optional label.</td></tr><tr><td><code>ToggleSwitch</code></td><td>Two-state switch with optional header.</td></tr><tr><td><code>Slider</code></td><td>Min/max-bounded numeric value.</td></tr><tr><td><code>ComboBox</code></td><td>Drop-down list of strings.</td></tr><tr><td><code>RadioButtons</code></td><td>Single-select radio group.</td></tr><tr><td><code>Button</code></td><td>Click handler with label.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/buttons\">Buttons</a>,\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/text-controls\">Text controls</a>.</p>\n<h2 id=\"collections\">Collections<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#collections\">#</a></h2>\n<p>Bound-list rendering and virtualization. Use <code>ListView</code> when the data\nfits in memory and the row template is uniform, <code>VirtualList</code> when the\nlist is large enough that mounting all rows would hurt frame time, and\n<code>LazyVStack</code> when the layout must be a stack but mounting cost is the\nbottleneck.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CollectionsGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> items = <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"Alpha\"</span>, <span class=\"hljs-string\">\"Bravo\"</span>, <span class=\"hljs-string\">\"Charlie\"</span>, <span class=\"hljs-string\">\"Delta\"</span> };\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n            TextBlock(<span class=\"hljs-string\">\"Items\"</span>).Bold(),\n            ForEach(items, item => TextBlock(<span class=\"hljs-string\">$\"  • <span class=\"hljs-subst\">{item}</span>\"</span>))\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/collections-thumb.png\" alt=\"Collections category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>ListView&#x3C;T></code></td><td>Bound, keyed list with per-item view builder.</td></tr><tr><td><code>GridView&#x3C;T></code></td><td>Tiled bound collection.</td></tr><tr><td><code>LazyVStack&#x3C;T></code></td><td>Defer-mounted vertical stack.</td></tr><tr><td><code>VirtualList</code></td><td>Index-driven virtual list with scroll API.</td></tr><tr><td><code>ForEach</code></td><td>Compose a sequence of elements without a list control.</td></tr></tbody></table>\n<p>Detail page: <a href=\"/collections/\">Collections</a>.</p>\n<h2 id=\"text--media\">Text &#x26; Media<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#text--media\">#</a></h2>\n<p>Read-only display surfaces: headings, body text, formatted text,\nimages, video. Most are transparent WinUI wrappers; the\nReactor-original here is <code>MarkdownTextBlock</code>, which renders Markdown\nwithout round-tripping through a WebView.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TextAndMediaGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">6</span>,\n        TextBlock(<span class=\"hljs-string\">\"Heading\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n        TextBlock(<span class=\"hljs-string\">\"Body text with a moderately long paragraph \"</span> +\n                  <span class=\"hljs-string\">\"for catalog-thumb composition.\"</span>).Opacity(<span class=\"hljs-number\">0.8</span>)\n    ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/text-and-media-thumb.png\" alt=\"Text &#x26; Media category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>TextBlock</code></td><td>Single-line or wrapping text.</td></tr><tr><td><code>Heading</code> / <code>SubHeading</code> / <code>Caption</code></td><td>Semantic text sizes.</td></tr><tr><td><code>RichTextBlock</code></td><td>Inline-formatted text.</td></tr><tr><td><code>RichEditBox</code></td><td>Editable rich text.</td></tr><tr><td><code>MarkdownTextBlock</code></td><td>Reactor-original Markdown renderer.</td></tr><tr><td><code>Image</code></td><td>Bitmap source.</td></tr><tr><td><code>MediaPlayerElement</code></td><td>Video / audio playback.</td></tr><tr><td><code>WebView2</code></td><td>Embedded Chromium surface.</td></tr><tr><td><code>InkCanvas</code></td><td>Pen input. <strong>Not yet wrapped — track in spec TBD.</strong></td></tr></tbody></table>\n<p>Detail page: <a href=\"/text-and-media/\">Text &#x26; Media</a>.</p>\n<h2 id=\"status--info\">Status &#x26; Info<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#status--info\">#</a></h2>\n<p>Non-interactive feedback: progress, badges, info bars, teaching tips.\nUse these to inform without stealing focus — for blocking confirmation\nyou want <a href=\"/dialogs-and-flyouts/\">Dialogs &#x26; Flyouts</a> instead.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StatusGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">\"Saving…\"</span>).Bold(),\n        TextBlock(<span class=\"hljs-string\">\"3 of 12 items\"</span>).Opacity(<span class=\"hljs-number\">0.7</span>)\n    ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/status-and-info-thumb.png\" alt=\"Status &#x26; Info category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>ProgressBar</code></td><td>Linear determinate / indeterminate progress.</td></tr><tr><td><code>ProgressRing</code></td><td>Spinner for indeterminate work.</td></tr><tr><td><code>InfoBar</code></td><td>Inline app-state message.</td></tr><tr><td><code>InfoBadge</code></td><td>Notification count badge.</td></tr><tr><td><code>TeachingTip</code></td><td>One-shot guided callout.</td></tr><tr><td><code>PipsPager</code></td><td>Compact paginator dots.</td></tr><tr><td><code>PersonPicture</code></td><td>Contact avatar.</td></tr><tr><td><code>RatingControl</code></td><td>1–5 star rating.</td></tr></tbody></table>\n<p>Detail page: <a href=\"/status-and-info/\">Status &#x26; Info</a>.</p>\n<h2 id=\"dialogs--flyouts\">Dialogs &#x26; Flyouts<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dialogs--flyouts\">#</a></h2>\n<p>Modal and ephemeral surfaces. Reactor wires these through the\n<a href=\"/commanding/\">commanding</a> system so the same <code>Command&#x3C;T></code> can light\nup a button, a menu item, and a keyboard shortcut without duplicating\nlogic.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DialogsGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">\"Confirm action\"</span>).Bold(),\n        TextBlock(<span class=\"hljs-string\">\"This cannot be undone.\"</span>).Opacity(<span class=\"hljs-number\">0.8</span>),\n        HStack(<span class=\"hljs-number\">8</span>,\n            Button(<span class=\"hljs-string\">\"Cancel\"</span>, () => { }),\n            Button(<span class=\"hljs-string\">\"Delete\"</span>, () => { })\n        )\n    ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/dialogs-and-flyouts-thumb.png\" alt=\"Dialogs &#x26; Flyouts category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>ContentDialog</code></td><td>Modal full-screen confirmation.</td></tr><tr><td><code>MenuFlyout</code></td><td>Context menu attached to a target.</td></tr><tr><td><code>CommandBarFlyout</code></td><td>Mini-toolbar with commands.</td></tr><tr><td><code>Popup</code></td><td>Free-form anchored surface.</td></tr></tbody></table>\n<p>Detail page: <a href=\"/dialogs-and-flyouts/\">Dialogs &#x26; Flyouts</a>.</p>\n<h2 id=\"data-system\">Data System<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data-system\">#</a></h2>\n<p><code>DataGrid</code> and the data-source / column / paging primitives. The\ndata-system surface is Reactor-original (no WinUI parallel) and is\ndocumented exhaustively in <a href=\"/data-system/\">data-system.md</a>.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DataSystemGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> rows = <span class=\"hljs-keyword\">new</span>[] { (<span class=\"hljs-string\">\"Ada\"</span>, <span class=\"hljs-number\">36</span>), (<span class=\"hljs-string\">\"Linus\"</span>, <span class=\"hljs-number\">55</span>), (<span class=\"hljs-string\">\"Grace\"</span>, <span class=\"hljs-number\">85</span>) };\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n            HStack(<span class=\"hljs-number\">16</span>,\n                TextBlock(<span class=\"hljs-string\">\"Name\"</span>).Bold(),\n                TextBlock(<span class=\"hljs-string\">\"Age\"</span>).Bold()\n            ),\n            ForEach(rows, r => HStack(<span class=\"hljs-number\">16</span>,\n                TextBlock(r.Item1),\n                TextBlock(r.Item2.ToString())\n            ))\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/data-system-thumb.png\" alt=\"Data System category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control / Type</th><th>Description</th></tr></thead><tbody><tr><td><code>DataGrid&#x3C;T></code></td><td>Virtualized grid with sort / filter / inline edit.</td></tr><tr><td><code>Column&#x3C;T></code></td><td>Column descriptor + builder.</td></tr><tr><td><code>IDataSource&#x3C;T></code></td><td>Pluggable data source abstraction.</td></tr><tr><td><code>ListDataSource&#x3C;T></code></td><td>In-memory source with client-side sort/filter.</td></tr><tr><td><code>DataPageCache&#x3C;T></code></td><td>Incremental paging cache.</td></tr></tbody></table>\n<p>Detail page: <a href=\"/data-system/\">Data System</a>.</p>\n<h2 id=\"charting\">Charting<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#charting\">#</a></h2>\n<p><code>ReactorCharting</code> package — chart primitives that compose like any\nother element. Bring the charts into scope with\n<code>using static Microsoft.UI.Reactor.Charting.Charts;</code>.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ChartingGroup</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">\"Revenue (Q1–Q4)\"</span>).Bold(),\n        <span class=\"hljs-comment\">// Placeholder visual — the real charting category uses ReactorCharting.</span>\n        TextBlock(<span class=\"hljs-string\">\"▁ ▃ ▅ ▇\"</span>).FontSize(<span class=\"hljs-number\">28</span>)\n    ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/controls/charting-thumb.png\" alt=\"Charting category thumbnail\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Description</th></tr></thead><tbody><tr><td><code>LineChart&#x3C;T></code></td><td>Line series with axes.</td></tr><tr><td><code>BarChart&#x3C;T></code></td><td>Bar / column series.</td></tr><tr><td><code>AreaChart&#x3C;T></code></td><td>Filled area under a line.</td></tr><tr><td><code>PieChart&#x3C;T></code></td><td>Categorical share.</td></tr><tr><td><code>TreeChart&#x3C;T></code></td><td>Hierarchical layout.</td></tr><tr><td><code>ForceGraph</code></td><td>Force-directed node graph.</td></tr></tbody></table>\n<p>Detail page: <a href=\"/charting/\">Charting</a>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Don't reach for a custom Component before checking the catalog.</strong>\nMost needs are met by composing existing factories — a \"card\" is a\n<code>VStack</code> inside a <code>Border</code>, a \"stat tile\" is two <code>TextBlock</code>s stacked.\nThe cost of a custom Component is the cost of maintaining its render\nfunction forever.</p>\n<p><strong>Reactor-original vs. WinUI wrapper matters for documentation.</strong>\nWrappers point to Microsoft Learn for design guidance; Reactor-originals\nown their full surface here. When the per-control page is missing a\nsection like \"accessibility behavior\", check whether the control is a\nwrapper — the answer is usually upstream.</p>\n<p><strong>Catalog thumbnails are not micro-tutorials.</strong> They show the control\nin a representative state, no more. The detail page covers usage,\nmodifiers, and the \"Don't\" cases.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/components/\">Components</a></strong> — Previous: how a <code>Component</code> is the\nthing that hosts a tree of controls.</li>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — Next: the biggest catalog category, with the\nfull input + validation surface.</li>\n<li><strong><a href=\"/layout/\">Layout</a></strong> — How <code>VStack</code> / <code>HStack</code> / <code>Grid</code> /\n<code>FlexPanel</code> compose any control into a real screen.</li>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — <code>ThemeRef</code> tokens, modifier chaining, and\nnamed styles applied across the catalog.</li>\n<li><strong><a href=\"/recipes/\">Recipes</a></strong> — Real-world compositions that pull\ncontrols together into common shapes.</li>\n</ul>",
      "text": "Controls # class ControlsCatalogApp : Component { public override Element Render () => VStack( 8 , TextBlock( \"Controls catalog\" ).FontSize( 20 ).Bold(), TextBlock( \"Every Reactor control, grouped by category.\" ).Opacity( 0.7 ), Button( \"Open Forms\" , () => { }) ).Padding( 16 ); } Every visible thing on screen is a control. In Microsoft.UI.Reactor (Reactor) a control is the return value of a factory like TextBlock(...) or Button(...) — a plain element you compose, chain modifiers onto, and return from Render() . The seven categories below cover the full set Reactor exposes today. Pick a category, open its detail page, and you'll find the factory signature, modifier matrix, and screenshots for every control inside it. Two ground rules apply across the catalog: Every control has a Reactor factory. No XAML in Render() — the factories on Microsoft.UI.Reactor.Factories are the only thing you need to compose UI. WinUI-wrapper controls link out, not duplicate. When a control (like DatePicker or AutoSuggestBox ) is a transparent WinUI wrapper, the Reactor page covers the factory and modifier surface and links to the Microsoft Learn design page for theming, layout guidance, and accessibility behavior. Reactor-original controls ( DataGrid , MarkdownTextBlock , VirtualList , FlexPanel ) are documented exhaustively in their own pages. Categories # Category What's in it Detail page Forms Text input, picker, button, validation primitives Forms Collections List, grid, virtualized list, repeater Collections Text &#x26; Media Heading, rich text, image, media player Text &#x26; Media Status &#x26; Info Progress, info bar, badge, teaching tip Status &#x26; Info Dialogs &#x26; Flyouts Content dialog, menu flyout, command bar flyout Dialogs &#x26; Flyouts Data System DataGrid, columns, data sources, paging Data System Charting Line, bar, area, pie, force graph Charting Forms # Text input, choice, sliders, buttons, and the validation pipeline. Forms is the category most apps spend time in; the detail page ( forms.md ) covers controlled-input patterns and the FormField validation surface. class FormsGroup : Component { public override Element Render () { var (name, setName) = UseState( \"Ada\" ); var (agree, setAgree) = UseState( true ); var (volume, setVolume) = UseState( 60.0 ); return VStack( 8 , TextBox(name, setName, placeholderText: \"Name\" ).Width( 200 ), CheckBox(agree, setAgree, label: \"I agree\" ), Slider(volume, 0 , 100 , setVolume).Width( 200 ), Button( \"Submit\" , () => { }) ).Padding( 16 ); } } Control Description TextBox Single-line text input with placeholder + header. PasswordBox Obscured text input. NumberBox Numeric input with up/down spinner. CheckBox Two-state checkbox with optional label. ToggleSwitch Two-state switch with optional header. Slider Min/max-bounded numeric value. ComboBox Drop-down list of strings. RadioButtons Single-select radio group. Button Click handler with label. WinUI design page: Buttons , Text controls . Collections # Bound-list rendering and virtualization. Use ListView when the data fits in memory and the row template is uniform, VirtualList when the list is large enough that mounting all rows would hurt frame time, and LazyVStack when the layout must be a stack but mounting cost is the bottleneck. class CollectionsGroup : Component { public override Element Render () { var items = new [] { \"Alpha\" , \"Bravo\" , \"Charlie\" , \"Delta\" }; return VStack( 4 , TextBlock( \"Items\" ).Bold(), ForEach(items, item => TextBlock( $\" • {item} \" )) ).Padding( 16 ); } } Control Description ListView&#x3C;T> Bound, keyed list with per-item view builder. GridView&#x3C;T> Tiled bound collection. LazyVStack&#x3C;T> Defer-mounted vertical stack. VirtualList Index-driven virtual list with scroll API. ForEach Compose a sequence of elements without a list control. Detail page: Collections . Text &#x26; Media # Read-only display surfaces: headings, body text, formatted text, images, video. Most are transparent WinUI wrappers; the Reactor-original here is MarkdownTextBlock , which renders Markdown without round-tripping through a WebView. class TextAndMediaGroup : Component { public override Element Render () => VStack( 6 , TextBlock( \"Heading\" ).FontSize( 20 ).Bold(), TextBlock( \"Body text with a moderately long paragraph \" + \"for catalog-thumb composition.\" ).Opacity( 0.8 ) ).Padding( 16 ); } Control Description TextBlock Single-line or wrapping text. Heading / SubHeading / Caption Semantic text sizes. RichTextBlock Inline-formatted text. RichEditBox Editable rich text. MarkdownTextBlock Reactor-original Markdown renderer. Image Bitmap source. MediaPlayerElement Video / audio playback. WebView2 Embedded Chromium surface. InkCanvas Pen input. Not yet wrapped — track in spec TBD. Detail page: Text &#x26; Media . Status &#x26; Info # Non-interactive feedback: progress, badges, info bars, teaching tips. Use these to inform without stealing focus — for blocking confirmation you want Dialogs &#x26; Flyouts instead. class StatusGroup : Component { public override Element Render () => VStack( 8 , TextBlock( \"Saving…\" ).Bold(), TextBlock( \"3 of 12 items\" ).Opacity( 0.7 ) ).Padding( 16 ); } Control Description ProgressBar Linear determinate / indeterminate progress. ProgressRing Spinner for indeterminate work. InfoBar Inline app-state message. InfoBadge Notification count badge. TeachingTip One-shot guided callout. PipsPager Compact paginator dots. PersonPicture Contact avatar. RatingControl 1–5 star rating. Detail page: Status &#x26; Info . Dialogs &#x26; Flyouts # Modal and ephemeral surfaces. Reactor wires these through the commanding system so the same Command&#x3C;T> can light up a button, a menu item, and a keyboard shortcut without duplicating logic. class DialogsGroup : Component { public override Element Render () => VStack( 8 , TextBlock( \"Confirm action\" ).Bold(), TextBlock( \"This cannot be undone.\" ).Opacity( 0.8 ), HStack( 8 , Button( \"Cancel\" , () => { }), Button( \"Delete\" , () => { }) ) ).Padding( 16 ); } Control Description ContentDialog Modal full-screen confirmation. MenuFlyout Context menu attached to a target. CommandBarFlyout Mini-toolbar with commands. Popup Free-form anchored surface. Detail page: Dialogs &#x26; Flyouts . Data System # DataGrid and the data-source / column / paging primitives. The data-system surface is Reactor-original (no WinUI parallel) and is documented exhaustively in data-system.md . class DataSystemGroup : Component { public override Element Render () { var rows = new [] { ( \"Ada\" , 36 ), ( \"Linus\" , 55 ), ( \"Grace\" , 85 ) }; return VStack( 4 , HStack( 16 , TextBlock( \"Name\" ).Bold(), TextBlock( \"Age\" ).Bold() ), ForEach(rows, r => HStack( 16 , TextBlock(r.Item1), TextBlock(r.Item2.ToString()) )) ).Padding( 16 ); } } Control / Type Description DataGrid&#x3C;T> Virtualized grid with sort / filter / inline edit. Column&#x3C;T> Column descriptor + builder. IDataSource&#x3C;T> Pluggable data source abstraction. ListDataSource&#x3C;T> In-memory source with client-side sort/filter. DataPageCache&#x3C;T> Incremental paging cache. Detail page: Data System . Charting # ReactorCharting package — chart primitives that compose like any other element. Bring the charts into scope with using static Microsoft.UI.Reactor.Charting.Charts; . class ChartingGroup : Component { public override Element Render () => VStack( 8 , TextBlock( \"Revenue (Q1–Q4)\" ).Bold(), // Placeholder visual — the real charting category uses ReactorCharting. TextBlock( \"▁ ▃ ▅ ▇\" ).FontSize( 28 ) ).Padding( 16 ); } Control Description LineChart&#x3C;T> Line series with axes. BarChart&#x3C;T> Bar / column series. AreaChart&#x3C;T> Filled area under a line. PieChart&#x3C;T> Categorical share. TreeChart&#x3C;T> Hierarchical layout. ForceGraph Force-directed node graph. Detail page: Charting . Tips # Don't reach for a custom Component before checking the catalog. Most needs are met by composing existing factories — a \"card\" is a VStack inside a Border , a \"stat tile\" is two TextBlock s stacked. The cost of a custom Component is the cost of maintaining its render function forever. Reactor-original vs. WinUI wrapper matters for documentation. Wrappers point to Microsoft Learn for design guidance; Reactor-originals own their full surface here. When the per-control page is missing a section like \"accessibility behavior\", check whether the control is a wrapper — the answer is usually upstream. Catalog thumbnails are not micro-tutorials. They show the control in a representative state, no more. The detail page covers usage, modifiers, and the \"Don't\" cases. Next Steps # Components — Previous: how a Component is the thing that hosts a tree of controls. Forms — Next: the biggest catalog category, with the full input + validation surface. Layout — How VStack / HStack / Grid / FlexPanel compose any control into a real screen. Styling — ThemeRef tokens, modifier chaining, and named styles applied across the catalog. Recipes — Real-world compositions that pull controls together into common shapes.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/controls.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/controls.md",
      "headings": [
        {
          "depth": 2,
          "id": "categories",
          "text": "Categories"
        },
        {
          "depth": 2,
          "id": "forms",
          "text": "Forms"
        },
        {
          "depth": 2,
          "id": "collections",
          "text": "Collections"
        },
        {
          "depth": 2,
          "id": "text--media",
          "text": "Text &#x26; Media"
        },
        {
          "depth": 2,
          "id": "status--info",
          "text": "Status &#x26; Info"
        },
        {
          "depth": 2,
          "id": "dialogs--flyouts",
          "text": "Dialogs &#x26; Flyouts"
        },
        {
          "depth": 2,
          "id": "data-system",
          "text": "Data System"
        },
        {
          "depth": 2,
          "id": "charting",
          "text": "Charting"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Forms and Input",
      "path": "forms.md",
      "slug": "/forms/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/forms\">Forms</a>.</p>\n</blockquote>\n<p>Forms are the Microsoft.UI.Reactor (Reactor) surface most apps spend most of their UI time inside.\nEvery input control on this page follows the same controlled-input\ncontract: you own the current value, you provide a change handler, the\ncontrol renders what you pass and calls your handler when the user edits.\nThere is no two-way <code>Binding</code>, no <code>INotifyPropertyChanged</code>, no\n<code>DependencyProperty.SetValue</code> — the value flows one direction\n(state → control) and edits flow one direction back\n(handler → state). That shape comes from\n<a href=\"/hooks/\">hooks</a> and <a href=\"/components/\">components</a>, and it makes every\nform testable: the form is whatever the\n<a href=\"/hooks/\"><code>UseState</code></a> snapshot says it is. Validation layers on top\nthrough <a href=\"/hooks/\"><code>UseValidationContext</code></a> and the <code>.Validate(...)</code>\nmodifier — the validators run on every render, the\n<a href=\"#formfield-helper\"><code>FormField</code></a> wrapper handles label / required marker /\nerror display, and the <code>ValidationContext</code> tracks touched/dirty per\nfield so errors only appear when the user has interacted. Read the\ncontrolled-input section first; everything else on this page is a\nspecialization.</p>\n<h1 id=\"forms-and-input\">Forms and Input<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forms-and-input\">#</a></h1>\n<p>Every form control in Reactor follows the controlled-input pattern: you own the\nvalue, you provide the change handler, and the control reflects your state.\nThere is no two-way binding. The data always flows one direction.</p>\n<h2 id=\"the-controlled-input-pattern\">The Controlled-Input Pattern<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-controlled-input-pattern\">#</a></h2>\n<p>Pass the current value and a setter. When the user types, <code>onChange</code> fires\nwith the new value. You call the setter, Reactor re-renders, and the control\nshows the updated text:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ControlledInputDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Controlled Input\"</span>),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Type your name\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"You typed: <span class=\"hljs-subst\">{name}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/controlled-input.png\" alt=\"Controlled input\"></p>\n<p>This is the same <a href=\"/hooks/\"><code>UseState</code></a> pattern from <a href=\"/getting-started/\">Getting Started</a>,\napplied to form inputs. The control never holds its own state — your component\nis the single source of truth.</p>\n<h2 id=\"input-control-types\">Input Control Types<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#input-control-types\">#</a></h2>\n<p>Reactor provides controls for every common input type. Each follows the same\npattern: current value in, change handler out.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InputTypesDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (password, setPassword) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (volume, setVolume) = UseState(<span class=\"hljs-number\">50.0</span>);\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">1.0</span>);\n        <span class=\"hljs-keyword\">var</span> (agree, setAgree) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> (notify, setNotify) = UseState(<span class=\"hljs-literal\">true</span>);\n        <span class=\"hljs-keyword\">var</span> (role, setRole) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (priority, setPriority) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(text, setText, placeholderText: <span class=\"hljs-string\">\"Email\"</span>,\n                header: <span class=\"hljs-string\">\"Email\"</span>),\n            PasswordBox(password, setPassword,\n                placeholderText: <span class=\"hljs-string\">\"Enter password\"</span>),\n            Slider(volume, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">100</span>, setVolume),\n            NumberBox(count, setCount, header: <span class=\"hljs-string\">\"Quantity\"</span>),\n            CheckBox(agree, setAgree, label: <span class=\"hljs-string\">\"I agree to the terms\"</span>),\n            ToggleSwitch(notify, setNotify,\n                header: <span class=\"hljs-string\">\"Notifications\"</span>),\n            ComboBox([<span class=\"hljs-string\">\"Admin\"</span>, <span class=\"hljs-string\">\"Editor\"</span>, <span class=\"hljs-string\">\"Viewer\"</span>],\n                role, setRole),\n            RadioButtons([<span class=\"hljs-string\">\"Low\"</span>, <span class=\"hljs-string\">\"Medium\"</span>, <span class=\"hljs-string\">\"High\"</span>],\n                priority, setPriority)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/input-types.png\" alt=\"All input types\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Value type</th><th>Change handler</th></tr></thead><tbody><tr><td><code>TextBox</code></td><td><code>string</code></td><td><code>Action&#x3C;string></code></td></tr><tr><td><code>PasswordBox</code></td><td><code>string</code></td><td><code>Action&#x3C;string></code></td></tr><tr><td><code>Slider</code></td><td><code>double</code></td><td><code>Action&#x3C;double></code></td></tr><tr><td><code>NumberBox</code></td><td><code>double</code></td><td><code>Action&#x3C;double></code></td></tr><tr><td><code>CheckBox</code></td><td><code>bool</code></td><td><code>Action&#x3C;bool></code></td></tr><tr><td><code>ToggleSwitch</code></td><td><code>bool</code></td><td><code>Action&#x3C;bool></code></td></tr><tr><td><code>ComboBox</code></td><td><code>int</code> (index)</td><td><code>Action&#x3C;int></code></td></tr><tr><td><code>RadioButtons</code></td><td><code>int</code> (index)</td><td><code>Action&#x3C;int></code></td></tr></tbody></table>\n<p>All controls accept optional parameters for labels, headers, and placeholder\ntext. Check the API reference for each control's full signature.</p>\n<h2 id=\"configuring-textbox\">Configuring TextBox<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#configuring-textbox\">#</a></h2>\n<p><code>TextBox</code> covers the common WinUI <code>TextBox</code> knobs through dedicated fluents\nso you rarely need <code>.Set(...)</code>. The named-input shapes set the appropriate\n<code>InputScope</code> for soft-keyboard and IME hinting:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TextBoxConfigDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (qty, setQty) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (url, setUrl) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (phone, setPhone) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (search, setSearch) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (note, setNote) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(qty, setQty, header: <span class=\"hljs-string\">\"Quantity\"</span>)\n                .NumericInput(),\n            TextBox(email, setEmail, header: <span class=\"hljs-string\">\"Email\"</span>)\n                .EmailInput(),\n            TextBox(url, setUrl, header: <span class=\"hljs-string\">\"URL\"</span>)\n                .UrlInput(),\n            TextBox(phone, setPhone, header: <span class=\"hljs-string\">\"Phone\"</span>)\n                .PhoneInput(),\n            TextBox(search, setSearch, placeholderText: <span class=\"hljs-string\">\"Search…\"</span>)\n                .SearchInput(),\n            TextBox(note, setNote, header: <span class=\"hljs-string\">\"Reference code\"</span>)\n                .MaxLength(<span class=\"hljs-number\">8</span>)\n                .CharacterCasing(CharacterCasing.Upper)\n                .TextAlignment(TextAlignment.Center)\n                .IsSpellCheckEnabled(<span class=\"hljs-literal\">false</span>)\n                .Description(<span class=\"hljs-string\">\"Eight characters, automatically uppercased.\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.NumericInput()</code></td><td>InputScope = Number — numeric soft keyboard</td></tr><tr><td><code>.EmailInput()</code></td><td>InputScope = EmailSmtpAddress</td></tr><tr><td><code>.UrlInput()</code></td><td>InputScope = Url</td></tr><tr><td><code>.PhoneInput()</code></td><td>InputScope = TelephoneNumber</td></tr><tr><td><code>.SearchInput()</code></td><td>InputScope = Search</td></tr><tr><td><code>.MaxLength(n)</code></td><td>Caps input length at <code>n</code> characters</td></tr><tr><td><code>.IsSpellCheckEnabled(bool)</code></td><td>Toggles the squiggle underline</td></tr><tr><td><code>.CharacterCasing(casing)</code></td><td><code>Normal</code> / <code>Upper</code> / <code>Lower</code></td></tr><tr><td><code>.TextAlignment(alignment)</code></td><td>Aligns text within the field</td></tr><tr><td><code>.Description(text)</code></td><td>Helper text rendered below the field</td></tr></tbody></table>\n<p>These fluents chain freely; the named-input shapes are the canonical way to\nhint mobile / touch keyboards instead of reaching for <code>.Set(c => c.InputScope = ...)</code>.\nSee <a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §2.3 and §4.7 for the\nrationale.</p>\n<h2 id=\"simple-validation\">Simple Validation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#simple-validation\">#</a></h2>\n<p>For quick forms, derive validation from state. Compute booleans on every\nrender, show inline error messages, and re-check the booleans inside the\nsubmit handler:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ValidationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (age, setAge) = UseState(<span class=\"hljs-number\">0.0</span>);\n        <span class=\"hljs-keyword\">var</span> (showErrors, setShowErrors) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">var</span> emailValid = email.Contains(<span class=\"hljs-string\">'@'</span>) &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'.'</span>);\n        <span class=\"hljs-keyword\">var</span> ageValid = age >= <span class=\"hljs-number\">18</span> &#x26;&#x26; age &#x3C;= <span class=\"hljs-number\">120</span>;\n        <span class=\"hljs-keyword\">var</span> formValid = emailValid &#x26;&#x26; ageValid\n            &#x26;&#x26; !<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(email);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Simple Validation\"</span>),\n            TextBox(email, setEmail, placeholderText: <span class=\"hljs-string\">\"user@example.com\"</span>,\n                header: <span class=\"hljs-string\">\"Email\"</span>),\n            When(!<span class=\"hljs-built_in\">string</span>.IsNullOrEmpty(email) &#x26;&#x26; !emailValid, () =>\n                TextBlock(<span class=\"hljs-string\">\"Enter a valid email address\"</span>)\n                    .Foreground(Theme.SystemCritical).FontSize(<span class=\"hljs-number\">12</span>)),\n            NumberBox(age, setAge, header: <span class=\"hljs-string\">\"Age\"</span>),\n            When((showErrors || age > <span class=\"hljs-number\">0</span>) &#x26;&#x26; !ageValid, () =>\n                TextBlock(<span class=\"hljs-string\">\"Age must be between 18 and 120\"</span>)\n                    .Foreground(Theme.SystemCritical).FontSize(<span class=\"hljs-number\">12</span>)),\n            Button(<span class=\"hljs-string\">\"Submit\"</span>, () =>\n            {\n                setShowErrors(<span class=\"hljs-literal\">true</span>);\n                <span class=\"hljs-keyword\">if</span> (!formValid) <span class=\"hljs-keyword\">return</span>;\n                <span class=\"hljs-comment\">// submit...</span>\n            }).Margin(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">0</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/validation.png\" alt=\"Validation demo\"></p>\n<p>This works well for small forms. For larger forms with cross-field rules,\ntouched/dirty tracking, and error display policies, use the validation\nframework described next.</p>\n<h2 id=\"keeping-submit-reachable\">Keeping Submit Reachable<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keeping-submit-reachable\">#</a></h2>\n<p>A common form pattern — <em>disable the Submit button until the form is valid</em> —\nis a keyboard accessibility trap when combined with controls like <code>NumberBox</code>,\n<code>DatePicker</code>, and <code>CalendarDatePicker</code>, which commit their value on blur\nrather than per keystroke. The user fixes the last invalid field, presses\nTab to reach Submit, and focus skips the still-disabled button. By the time\nthe field commits and Submit becomes enabled, focus has already moved on.\nThe user is stranded with no keyboard route to a button they can now press.</p>\n<p>Reactor offers two opt-in fixes for this. They compose:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">KeepSubmitReachableDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (age, setAge) = UseState(<span class=\"hljs-number\">0.0</span>);\n\n        <span class=\"hljs-keyword\">var</span> emailValid = email.Contains(<span class=\"hljs-string\">'@'</span>) &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'.'</span>);\n        <span class=\"hljs-keyword\">var</span> ageValid = age >= <span class=\"hljs-number\">18</span> &#x26;&#x26; age &#x3C;= <span class=\"hljs-number\">120</span>;\n        <span class=\"hljs-keyword\">var</span> formValid = emailValid &#x26;&#x26; ageValid;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Keeping Submit Reachable\"</span>),\n            TextBox(email, setEmail, header: <span class=\"hljs-string\">\"Email\"</span>,\n                placeholderText: <span class=\"hljs-string\">\"user@example.com\"</span>),\n\n            <span class=\"hljs-comment\">// .Immediate() switches NumberBox from commit-on-blur to</span>\n            <span class=\"hljs-comment\">// commit-on-keystroke, so validation reacts as the user types.</span>\n            NumberBox(age, setAge, header: <span class=\"hljs-string\">\"Age\"</span>).Immediate(),\n\n            <span class=\"hljs-comment\">// .IsDisabledFocusable() keeps the button tab-reachable and</span>\n            <span class=\"hljs-comment\">// visually dimmed while preventing invocation. Pattern mirrors</span>\n            <span class=\"hljs-comment\">// Fluent UI's `disabledFocusable` and ARIA `aria-disabled`.</span>\n            Button(<span class=\"hljs-string\">\"Submit\"</span>, () => { <span class=\"hljs-comment\">/* submit */</span> })\n                .IsDisabledFocusable(!formValid)\n                .Margin(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">0</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/keep-submit-reachable.png\" alt=\"Keep submit reachable\"></p>\n<p><strong><code>.IsDisabledFocusable(bool)</code> on Button</strong> keeps the button keyboard-focusable\nand tab-reachable while presenting it as disabled (dimmed; the click is\nsuppressed). Mirrors Fluent UI React's <code>disabledFocusable</code> and ARIA's\n<code>aria-disabled</code>. Use it for any Submit gated on validation, busy state, or\nother derived conditions.</p>\n<p><strong><code>.Immediate()</code> on NumberBox</strong> (in <code>Microsoft.UI.Reactor.Controls.Validation</code>)\nswitches the control from commit-on-blur to commit-on-keystroke, so\nvalidation reacts to every parseable digit instead of waiting for focus to\nleave the field. Opt-in by design — commit-on-blur is the WinUI default and\nthe right choice when an intermediate value would be expensive or surprising\n(snapping <code>2.50</code> to <code>2.5</code> mid-edit). Apply when validation gates UI state\nand you want it to feel live.</p>\n<p>Use <code>.IsDisabledFocusable()</code> whenever a button is conditionally disabled in a\nform — even if you've also applied <code>.Immediate()</code> to every commit-on-blur\ninput. The two cover different failure modes: <code>.Immediate()</code> keeps validity\nin sync with typing; <code>.IsDisabledFocusable()</code> keeps the button discoverable\nwhen validity is gated on async checks, required-but-untouched fields,\ncross-field rules, or any derived condition that can't be made instantaneous.</p>\n<blockquote>\n<p><strong>Where not to use <code>.IsDisabledFocusable()</code>:</strong> only buttons. For data-entry\ncontrols (<code>TextBox</code>, <code>NumberBox</code>, <code>CheckBox</code>, etc.), <code>IsEnabled=false</code>\nusually means \"this field isn't part of your current task\" (cascading\nfrom another input), and tab-skipping is the correct UX. Use\n<code>IsReadOnly</code> if you need a visible-but-non-editable text control.</p>\n</blockquote>\n<h2 id=\"validation-context\">Validation Context<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#validation-context\">#</a></h2>\n<p><code>UseValidationContext()</code> creates a <code>ValidationContext</code> that tracks messages,\ntouched/dirty state, and field registration. Attach validators to controls\nwith <code>.Validate()</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ValidationContextDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> ctx = <span class=\"hljs-keyword\">this</span>.UseValidationContext();\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (password, setPassword) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (submitted, setSubmitted) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Validation Context\"</span>),\n            TextBox(email, v => { setEmail(v); ctx.NotifyValueChanged(<span class=\"hljs-string\">\"email\"</span>, v); },\n                placeholderText: <span class=\"hljs-string\">\"user@example.com\"</span>, header: <span class=\"hljs-string\">\"Email\"</span>)\n                .Validate(<span class=\"hljs-string\">\"email\"</span>, email,\n                    Validate.Required(),\n                    Validate.Email()),\n            When(ctx.IsTouched(<span class=\"hljs-string\">\"email\"</span>) &#x26;&#x26; ctx.HasError(<span class=\"hljs-string\">\"email\"</span>), () =>\n                TextBlock(ctx.GetMessages(<span class=\"hljs-string\">\"email\"</span>).First().Text)\n                    .Foreground(Theme.SystemCritical).FontSize(<span class=\"hljs-number\">12</span>)),\n            PasswordBox(password, v => { setPassword(v); ctx.NotifyValueChanged(<span class=\"hljs-string\">\"password\"</span>, v); },\n                placeholderText: <span class=\"hljs-string\">\"Min 8 characters\"</span>)\n                .Validate(<span class=\"hljs-string\">\"password\"</span>, password,\n                    Validate.Required(),\n                    Validate.MinLength(<span class=\"hljs-number\">8</span>)),\n            When(ctx.IsTouched(<span class=\"hljs-string\">\"password\"</span>) &#x26;&#x26; ctx.HasError(<span class=\"hljs-string\">\"password\"</span>), () =>\n                TextBlock(ctx.GetMessages(<span class=\"hljs-string\">\"password\"</span>).First().Text)\n                    .Foreground(Theme.SystemCritical).FontSize(<span class=\"hljs-number\">12</span>)),\n            Button(<span class=\"hljs-string\">\"Register\"</span>, () =>\n            {\n                ctx.MarkAllTouched();\n                <span class=\"hljs-keyword\">if</span> (ctx.IsValid()) setSubmitted(<span class=\"hljs-literal\">true</span>);\n            }).IsEnabled(!submitted),\n            When(submitted, () =>\n                TextBlock(<span class=\"hljs-string\">\"Registration successful!\"</span>)\n                    .Foreground(Theme.SystemSuccess).SemiBold())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/validation-context.png\" alt=\"Validation context\"></p>\n<p>Key pieces:</p>\n<ul>\n<li><strong><code>UseValidationContext()</code></strong> creates or retrieves the nearest validation context.</li>\n<li><strong><code>.Validate(fieldName, value, validators...)</code></strong> attaches validators to a control.</li>\n<li><strong><code>Validate.Required()</code>, <code>Validate.Email()</code>, etc.</strong> are the 11 built-in validators.</li>\n<li><strong><code>ctx.IsValid()</code></strong> returns <code>true</code> when no error-severity messages exist.</li>\n<li><strong><code>ctx.MarkAllTouched()</code></strong> reveals all errors on submit attempt.</li>\n</ul>\n<h2 id=\"formfield-helper\">FormField Helper<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#formfield-helper\">#</a></h2>\n<p><code>FormField()</code> wraps a control with a label, required indicator, description\ntext, and inline error display:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FormFieldDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> ctx = <span class=\"hljs-keyword\">this</span>.UseValidationContext();\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"FormField Helper\"</span>),\n            FormField(\n                TextBox(name, v => { setName(v); ctx.NotifyValueChanged(<span class=\"hljs-string\">\"name\"</span>, v); })\n                    .Validate(<span class=\"hljs-string\">\"name\"</span>, name, Validate.Required()),\n                label: <span class=\"hljs-string\">\"Full Name\"</span>,\n                <span class=\"hljs-keyword\">required</span>: <span class=\"hljs-literal\">true</span>,\n                description: <span class=\"hljs-string\">\"As it appears on your ID\"</span>),\n            FormField(\n                TextBox(email, v => { setEmail(v); ctx.NotifyValueChanged(<span class=\"hljs-string\">\"email\"</span>, v); })\n                    .Validate(<span class=\"hljs-string\">\"email\"</span>, email,\n                        Validate.Required(), Validate.Email()),\n                label: <span class=\"hljs-string\">\"Email Address\"</span>,\n                <span class=\"hljs-keyword\">required</span>: <span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/form-field.png\" alt=\"FormField helper\"></p>\n<p><code>FormField</code> auto-detects the field name from the <code>.Validate()</code> attachment on\nits content. Errors appear below the field after the field is touched (focus\nthen blur). The <code>ShowWhen</code> parameter controls when errors become visible:\n<code>WhenTouched</code> (default), <code>WhenDirty</code>, <code>AfterFirstSubmit</code>, <code>Always</code>, or <code>Never</code>.</p>\n<h2 id=\"built-in-validators\">Built-in Validators<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#built-in-validators\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Validator</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Validate.Required()</code></td><td>Non-null, non-empty, non-default</td></tr><tr><td><code>Validate.MinLength(n)</code></td><td>String length >= n</td></tr><tr><td><code>Validate.MaxLength(n)</code></td><td>String length &#x3C;= n</td></tr><tr><td><code>Validate.Range(min, max)</code></td><td>Numeric value in range</td></tr><tr><td><code>Validate.Match(regex)</code></td><td>Regex pattern match</td></tr><tr><td><code>Validate.Email()</code></td><td>Valid email address</td></tr><tr><td><code>Validate.Url()</code></td><td>Valid URL (http/https)</td></tr><tr><td><code>Validate.Must&#x3C;T>(predicate, message)</code></td><td>Custom predicate</td></tr><tr><td><code>Validate.MustAsync&#x3C;T>(predicate, message)</code></td><td>Async predicate</td></tr><tr><td><code>Validate.MustBeTrue()</code></td><td>Boolean is true (checkboxes)</td></tr><tr><td><code>Validate.EqualTo&#x3C;T>(value)</code></td><td>Equality check (password confirm)</td></tr></tbody></table>\n<p>Every validator accepts an optional custom error message as its last parameter.</p>\n<h2 id=\"masked-input\">Masked Input<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#masked-input\">#</a></h2>\n<p><code>MaskEngine</code> applies input masks with auto-inserted literals. Use the built-in\npresets or define your own pattern:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MaskedInputDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> phoneMask = UseMemo(() => <span class=\"hljs-keyword\">new</span> MaskEngine(MaskPreset.PhoneUS));\n        <span class=\"hljs-keyword\">var</span> dateMask = UseMemo(() => <span class=\"hljs-keyword\">new</span> MaskEngine(MaskPreset.Date));\n        <span class=\"hljs-keyword\">var</span> (phone, setPhone) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (date, setDate) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Masked Input\"</span>),\n            TextBox(phoneMask.Apply(phone), v => setPhone(phoneMask.GetRawValue(v)),\n                placeholderText: <span class=\"hljs-string\">\"(___) ___-____\"</span>, header: <span class=\"hljs-string\">\"Phone\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Raw: <span class=\"hljs-subst\">{phone}</span>\"</span>).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            TextBox(dateMask.Apply(date), v => setDate(dateMask.GetRawValue(v)),\n                placeholderText: <span class=\"hljs-string\">\"__/__/____\"</span>, header: <span class=\"hljs-string\">\"Date\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Raw: <span class=\"hljs-subst\">{date}</span>\"</span>).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/masked-input.png\" alt=\"Masked input\"></p>\n<p>Mask tokens: <code>0</code> = required digit, <code>9</code> = optional digit, <code>A</code> = required\nletter, <code>a</code> = optional letter, <code>*</code> = required alphanumeric. All other\ncharacters are literals inserted automatically.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Preset</th><th>Pattern</th></tr></thead><tbody><tr><td><code>MaskPreset.PhoneUS</code></td><td><code>(000) 000-0000</code></td></tr><tr><td><code>MaskPreset.SSN</code></td><td><code>000-00-0000</code></td></tr><tr><td><code>MaskPreset.CreditCard</code></td><td><code>0000 0000 0000 0000</code></td></tr><tr><td><code>MaskPreset.Date</code></td><td><code>00/00/0000</code></td></tr><tr><td><code>MaskPreset.ZipCode</code></td><td><code>00000</code></td></tr><tr><td><code>MaskPreset.IPv4</code></td><td><code>099.099.099.099</code></td></tr></tbody></table>\n<h2 id=\"input-formatters\">Input Formatters<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#input-formatters\">#</a></h2>\n<p><code>InputFormatter</code> transforms text as the user types. Chain formatters into a\npipeline for complex formatting:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InputFormattersDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (currency, setCurrency) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (upper, setUpper) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> currencyFmt = UseMemo(() => InputFormatter.Currency());\n        <span class=\"hljs-keyword\">var</span> upperFmt = UseMemo(() => InputFormatter.UpperCase);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Input Formatters\"</span>),\n            TextBox(currencyFmt.Format(currency, <span class=\"hljs-number\">0</span>).Output,\n                v => setCurrency(currencyFmt.Parse(v)),\n                placeholderText: <span class=\"hljs-string\">\"$0.00\"</span>, header: <span class=\"hljs-string\">\"Amount\"</span>),\n            TextBox(upperFmt.Format(upper, <span class=\"hljs-number\">0</span>).Output,\n                v => setUpper(upperFmt.Parse(v)),\n                placeholderText: <span class=\"hljs-string\">\"UPPERCASE\"</span>, header: <span class=\"hljs-string\">\"Code\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/input-formatters.png\" alt=\"Input formatters\"></p>\n<p>Built-in formatters: <code>PhoneUS</code>, <code>Currency()</code>, <code>UpperCase</code>, <code>LowerCase</code>,\n<code>TitleCase</code>, <code>TrimWhitespace</code>, <code>MaxLength(n)</code>, <code>AllowOnly(regex)</code>,\n<code>DenyOnly(regex)</code>, and <code>Custom(format, parse)</code>.</p>\n<h2 id=\"autosuggestbox\">AutoSuggestBox<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#autosuggestbox\">#</a></h2>\n<p><code>AutoSuggestBox</code> is the search-with-suggestions input — type a prefix,\nsee a filtered list, pick one or submit free-form. The factory takes\nthe text value, an <code>onTextChanged</code> handler, and an optional\n<code>onQuerySubmitted</code> handler that fires on Enter or suggestion pick:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AutoSuggestDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span>[] Catalog =\n    [\n        <span class=\"hljs-string\">\"Aardvark\"</span>, <span class=\"hljs-string\">\"Albatross\"</span>, <span class=\"hljs-string\">\"Antelope\"</span>, <span class=\"hljs-string\">\"Badger\"</span>,\n        <span class=\"hljs-string\">\"Beaver\"</span>, <span class=\"hljs-string\">\"Buffalo\"</span>, <span class=\"hljs-string\">\"Camel\"</span>, <span class=\"hljs-string\">\"Capybara\"</span>\n    ];\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">var</span> matches = <span class=\"hljs-built_in\">string</span>.IsNullOrEmpty(text)\n            ? Array.Empty&#x3C;<span class=\"hljs-built_in\">string</span>>()\n            : Catalog.Where(c =>\n                c.StartsWith(text, StringComparison.OrdinalIgnoreCase))\n                .ToArray();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"AutoSuggestBox\"</span>),\n            AutoSuggestBox(text, setText,\n                onQuerySubmitted: q => setText(q))\n                .Header(<span class=\"hljs-string\">\"Animal\"</span>)\n                .QueryIcon(SymbolIcon(<span class=\"hljs-string\">\"Find\"</span>))\n                .Width(<span class=\"hljs-number\">280</span>),\n            <span class=\"hljs-comment\">// Suggestion list — bind to AutoSuggestBox.ItemsSource via .Set</span>\n            <span class=\"hljs-comment\">// when you need the in-control dropdown; the inline list below</span>\n            <span class=\"hljs-comment\">// is a custom presentation that gives full styling control.</span>\n            When(matches.Length > <span class=\"hljs-number\">0</span>, () =>\n                VStack(<span class=\"hljs-number\">2</span>,\n                    ForEach(matches, m =>\n                        TextBlock(m).Padding(<span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">4</span>))\n                ).Background(<span class=\"hljs-string\">\"#F5F5F5\"</span>).Width(<span class=\"hljs-number\">280</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/auto-suggest.png\" alt=\"AutoSuggestBox with filtered suggestions\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Header(string)</code></td><td>Label rendered above the box.</td></tr><tr><td><code>.QueryIcon(IconData)</code></td><td>Trailing icon (a search glyph is typical).</td></tr><tr><td><code>.IsSuggestionListOpen(bool)</code></td><td>Force the dropdown open/closed.</td></tr><tr><td><code>.SuggestionChosen(Action&#x3C;string>)</code></td><td>Fires when the user picks an item from the underlying dropdown.</td></tr><tr><td><code>.Set(b => b.ItemsSource = ...)</code></td><td>Bind to the WinUI suggestion list.</td></tr></tbody></table>\n<p>The snippet above renders an inline suggestion list as a custom\npresentation — more layout freedom than the built-in dropdown, at the\ncost of writing the visual surface yourself. For the canonical\nsearch-results recipe see <a href=\"/recipes/search-with-suggestions/\">recipes/search-with-suggestions</a>.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/auto-suggest-box\">Auto-suggest box</a>.</p>\n<h2 id=\"date-and-time-controls\">Date and time controls<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#date-and-time-controls\">#</a></h2>\n<p>Reactor exposes three date/time inputs and a calendar surface, each\ncovering a different shape of the same problem:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Value</th><th>Shape</th><th>Use when</th></tr></thead><tbody><tr><td><code>DatePicker</code></td><td><code>DateTimeOffset</code> (non-null)</td><td>Inline three-spinner picker</td><td>The date is always required and inline real estate is available.</td></tr><tr><td><code>CalendarDatePicker</code></td><td><code>DateTimeOffset?</code></td><td>Button that opens a popup calendar</td><td>Date is optional; you want a compact trigger.</td></tr><tr><td><code>CalendarView</code></td><td><code>IReadOnlyList&#x3C;DateTimeOffset></code></td><td>Full month grid</td><td>Single, multiple, or range selection from a grid.</td></tr><tr><td><code>TimePicker</code></td><td><code>TimeSpan</code></td><td>Hours / minutes / am-pm spinner</td><td>Time-of-day independent of a date.</td></tr></tbody></table>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DatePickerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (date, setDate) = UseState(DateTimeOffset.Now);\n        <span class=\"hljs-keyword\">var</span> (optionalDate, setOptionalDate) = UseState&#x3C;DateTimeOffset?>(<span class=\"hljs-literal\">null</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"DatePicker — always-set value\"</span>),\n            DatePicker(date, setDate)\n                .DayFormat(<span class=\"hljs-string\">\"{day.integer(2)}\"</span>)\n                .MonthFormat(<span class=\"hljs-string\">\"{month.abbreviated}\"</span>)\n                .YearFormat(<span class=\"hljs-string\">\"{year.full}\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Selected: <span class=\"hljs-subst\">{date:yyyy-MM-dd}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            SubHeading(<span class=\"hljs-string\">\"CalendarDatePicker — nullable, popup calendar\"</span>),\n            CalendarDatePicker(optionalDate, setOptionalDate)\n                .DateFormat(<span class=\"hljs-string\">\"{month.abbreviated} {day.integer(2)}, {year.full}\"</span>)\n                .IsTodayHighlighted(),\n            TextBlock(optionalDate <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n                ? <span class=\"hljs-string\">\"No date selected.\"</span>\n                : <span class=\"hljs-string\">$\"Selected: <span class=\"hljs-subst\">{optionalDate:yyyy-MM-dd}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/date-picker.png\" alt=\"DatePicker (always set) and CalendarDatePicker (nullable)\"></p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TimePickerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (time, setTime) = UseState(TimeSpan.FromHours(<span class=\"hljs-number\">9</span>));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"TimePicker\"</span>),\n            TimePicker(time, setTime),\n            TextBlock(<span class=\"hljs-string\">$\"Selected: <span class=\"hljs-subst\">{time:hh\\\\:mm}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/time-picker.png\" alt=\"TimePicker bound to a TimeSpan\"></p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CalendarViewDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (dates, setDates) = UseState&#x3C;IReadOnlyList&#x3C;DateTimeOffset>>(\n            Array.Empty&#x3C;DateTimeOffset>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"CalendarView — month grid\"</span>),\n            CalendarView()\n                .MinDate(DateTimeOffset.Now.AddYears(<span class=\"hljs-number\">-1</span>))\n                .MaxDate(DateTimeOffset.Now.AddYears(<span class=\"hljs-number\">1</span>))\n                .NumberOfWeeksInView(<span class=\"hljs-number\">6</span>)\n                .SelectedDatesChanged(setDates)\n                .SelectedDates(dates),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{dates.Count}</span> day(s) selected\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/calendar-view.png\" alt=\"CalendarView month grid with multi-select\"></p>\n<p>DatePicker / TimePicker / CalendarDatePicker share their format strings\nwith the underlying WinUI <code>DateTimeFormatter</code> — <code>\"{day.integer(2)}\"</code>,\n<code>\"{month.abbreviated}\"</code>, <code>\"{year.full}\"</code>, and friends. <code>CalendarView</code>\nexposes <code>.MinDate</code> / <code>.MaxDate</code> for range constraint and\n<code>.NumberOfWeeksInView</code> to compress the grid. For multi-day selection,\npair <code>.SelectedDates(...)</code> with <code>.SelectedDatesChanged(...)</code> — the\nevent hands you the full snapshot of the current selection, not\nadd/remove deltas (same shape as\n<a href=\"/collections/\"><code>ListView</code> multi-select</a>).</p>\n<p>WinUI design pages: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/date-picker\">Date picker</a>,\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/time-picker\">Time picker</a>,\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/calendar-view\">Calendar view</a>.</p>\n<h2 id=\"colorpicker\">ColorPicker<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#colorpicker\">#</a></h2>\n<p><code>ColorPicker</code> takes a <code>Windows.UI.Color</code> and a change handler — same\ncontrolled pattern as <code>Slider</code>. Its surface is the largest of any input\non this page; configure it for the shape of the chooser you need:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ColorPickerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (color, setColor) = UseState(\n            <span class=\"hljs-keyword\">global</span>::Windows.UI.Color.FromArgb(<span class=\"hljs-number\">255</span>, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">120</span>, <span class=\"hljs-number\">215</span>));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"ColorPicker\"</span>),\n            ColorPicker(color, setColor)\n                .AlphaEnabled()\n                .HexInputVisible(<span class=\"hljs-literal\">true</span>)\n                .ColorSpectrumShape(\n                    Microsoft.UI.Xaml.Controls.ColorSpectrumShape.Ring),\n            <span class=\"hljs-comment\">// Preview swatch driven by the picker.</span>\n            Border(Empty())\n                .Background(\n                    <span class=\"hljs-keyword\">new</span> Microsoft.UI.Xaml.Media.SolidColorBrush(color))\n                .Width(<span class=\"hljs-number\">80</span>).Height(<span class=\"hljs-number\">40</span>)\n                .WithBorder(<span class=\"hljs-string\">\"#888888\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/forms/color-picker.png\" alt=\"ColorPicker with alpha and hex input\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.AlphaEnabled(bool)</code></td><td>Show the alpha slider and pre-multiply by the alpha value.</td></tr><tr><td><code>.ColorSpectrumShape(shape)</code></td><td><code>Box</code> (default) or <code>Ring</code>.</td></tr><tr><td><code>.HexInputVisible(bool)</code></td><td>Toggle the hex-input field.</td></tr><tr><td><code>.ColorSpectrumVisible(bool)</code></td><td>Toggle the main 2D spectrum.</td></tr><tr><td><code>.ColorSliderVisible(bool)</code></td><td>Toggle the hue slider.</td></tr><tr><td><code>.ColorChannelTextInputVisible(bool)</code></td><td>Toggle RGB number inputs.</td></tr><tr><td><code>.HueRange(min, max)</code> / <code>.SaturationRange(min, max)</code> / <code>.ValueRange(min, max)</code></td><td>Constrain pickable hue / sat / value.</td></tr><tr><td><code>.MoreButtonVisible(bool)</code></td><td>\"More\" chevron that expands the picker.</td></tr></tbody></table>\n<p>For palette pickers (Material-style swatch grids), build the surface\nout of <code>Button</code>s and <code>Background(color)</code> rather than this control —\n<code>ColorPicker</code> is the right shape only when the user needs continuous\nselection from the full color space.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/color-picker\">Color picker</a>.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Commit-on-blur inputs (<code>NumberBox</code>, <code>DatePicker</code>, <code>CalendarDatePicker</code>,\n<code>TimePicker</code>) do <strong>not</strong> fire their change handlers per keystroke —\nthey fire when focus leaves the field. Validation derived from these\ncontrols' values is therefore stale until the user tabs out. The\nclassic failure mode: a Submit button disabled until the form is valid\nstays disabled while the user is still inside a <code>NumberBox</code> that\nalready contains a valid value. The user fixes the last field, presses\nTab to reach Submit, focus skips the still-disabled button, and the\nuser is stranded. Two fixes compose: <code>.Immediate()</code> on the input\nswitches it to per-keystroke commit (see\n<a href=\"#keeping-submit-reachable\">Keeping Submit Reachable</a>), and\n<code>.IsDisabledFocusable()</code> on the Submit button keeps it keyboard-focusable\nwhile it's gated.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"multi-step-form-with-shared-validationcontext\">Multi-step form with shared ValidationContext<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#multi-step-form-with-shared-validationcontext\">#</a></h3>\n<p>A wizard-style form spans multiple pages but the validation lives in\none place. Hoist <code>UseValidationContext()</code> to the wizard component and\npass it down via <a href=\"/context/\">context</a> — every step's\n<code>.Validate(...)</code> writes to the same store, and the final Submit checks\n<code>ctx.IsValid()</code> across all steps. The\n<a href=\"/recipes/multi-step-form/\"><code>recipes/multi-step-form</code></a> recipe walks\nthe full pattern; the key shape is one context per <strong>form</strong>, not one\nper page.</p>\n<h3 id=\"submit-on-enter\">Submit on Enter<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#submit-on-enter\">#</a></h3>\n<p>For a single-field form (search box, comment input), wire submission\nto Enter rather than a Submit button. <code>AutoSuggestBox</code> does this\nautomatically via <code>onQuerySubmitted</code>. For <code>TextBox</code>, route through\n<code>.KeyDown</code> or wrap the field in an <code>[ai:lock]</code> form panel; see\n<a href=\"/input-and-gestures/\">input-and-gestures</a> for the routed-events\nsurface.</p>\n<h3 id=\"validating-async-uniqueness-checks\">Validating async (uniqueness checks)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#validating-async-uniqueness-checks\">#</a></h3>\n<p><code>Validate.MustAsync&#x3C;T>(...)</code> runs a predicate that returns\n<code>Task&#x3C;bool></code>. The <code>ValidationContext</code> tracks the in-flight async work\nand reports <code>IsValidating</code> per field, so the Submit button can disable\nwhile async validation runs. Pair with <code>DisabledFocusable()</code> so the\nbutton stays in tab order while validating — same accessibility\nconcern as <a href=\"#keeping-submit-reachable\">Keeping Submit Reachable</a>.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"letting-the-control-hold-its-own-state\">Letting the control hold its own state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#letting-the-control-hold-its-own-state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nTextBox(initial: <span class=\"hljs-string\">\"default value\"</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ControlledInputDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Controlled Input\"</span>),\n            TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Type your name\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"You typed: <span class=\"hljs-subst\">{name}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Uncontrolled inputs work in trivial demos but break the moment the\nparent component needs to read, reset, or pre-fill the value. Reactor\ninputs all take <code>(value, onChanged)</code> — the control is always a passive\nview of state.</p>\n<h3 id=\"validating-in-the-click-handler\">Validating in the click handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#validating-in-the-click-handler\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Submit\"</span>, () =>\n{\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-built_in\">string</span>.IsNullOrEmpty(email)) { setError(<span class=\"hljs-string\">\"…\"</span>); <span class=\"hljs-keyword\">return</span>; }\n    <span class=\"hljs-keyword\">if</span> (!email.Contains(<span class=\"hljs-string\">'@'</span>))         { setError(<span class=\"hljs-string\">\"…\"</span>); <span class=\"hljs-keyword\">return</span>; }\n    <span class=\"hljs-keyword\">if</span> (age &#x3C; <span class=\"hljs-number\">18</span>)                      { setError(<span class=\"hljs-string\">\"…\"</span>); <span class=\"hljs-keyword\">return</span>; }\n    <span class=\"hljs-comment\">// submit…</span>\n})\n</code></pre>\n<p>Imperative checks duplicate every field's validation in the submit\nhandler, drift from the inline error display, and fall out of date\nwhen fields are added. Use <a href=\"/hooks/\"><code>UseValidationContext</code></a></p>\n<ul>\n<li><code>.Validate(...)</code> so each field carries its rules with it; the\nsubmit handler becomes one <code>ctx.IsValid()</code> call.</li>\n</ul>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Always use controlled inputs.</strong> Never let a control manage its own state.\nYour <code>Render()</code> method is the single source of truth — if you need to\npre-fill, validate, or reset a field, you just set the state value.</p>\n<p><strong>Use <code>FormField</code> for production forms.</strong> It handles labels, required\nindicators, and error display with consistent styling. Avoid building your\nown field wrapper.</p>\n<p><strong>Use <code>Validate.Must&#x3C;T>()</code> for custom rules.</strong> When built-in validators\ndon't cover your case, <code>Must</code> takes any <code>Func&#x3C;T, bool></code> predicate.</p>\n<p><strong>Call <code>ctx.MarkAllTouched()</code> on submit.</strong> This reveals all validation errors\nat once, so the user sees everything that needs fixing.</p>\n<p><strong>Reset forms with <code>ctx.ResetAll()</code>.</strong> It returns all fields to their initial\nvalues, clears touched/dirty state, and removes all messages.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/flex-layout/\">Flex Layout</a></strong> — Previous: flexible box layout for adaptive UIs</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Next: render lists, grids, and virtualized data sets</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — UseState, UseMemo, and other hooks that power form logic</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — wire submit buttons to async commands with busy/error handling</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — theme your forms with tokens and lightweight styling</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Forms . Forms are the Microsoft.UI.Reactor (Reactor) surface most apps spend most of their UI time inside. Every input control on this page follows the same controlled-input contract: you own the current value, you provide a change handler, the control renders what you pass and calls your handler when the user edits. There is no two-way Binding , no INotifyPropertyChanged , no DependencyProperty.SetValue — the value flows one direction (state → control) and edits flow one direction back (handler → state). That shape comes from hooks and components , and it makes every form testable: the form is whatever the UseState snapshot says it is. Validation layers on top through UseValidationContext and the .Validate(...) modifier — the validators run on every render, the FormField wrapper handles label / required marker / error display, and the ValidationContext tracks touched/dirty per field so errors only appear when the user has interacted. Read the controlled-input section first; everything else on this page is a specialization. Forms and Input # Every form control in Reactor follows the controlled-input pattern: you own the value, you provide the change handler, and the control reflects your state. There is no two-way binding. The data always flows one direction. The Controlled-Input Pattern # Pass the current value and a setter. When the user types, onChange fires with the new value. You call the setter, Reactor re-renders, and the control shows the updated text: class ControlledInputDemo : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); return VStack( 12 , SubHeading( \"Controlled Input\" ), TextBox(name, setName, placeholderText: \"Type your name\" ), TextBlock( $\"You typed: {name} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } This is the same UseState pattern from Getting Started , applied to form inputs. The control never holds its own state — your component is the single source of truth. Input Control Types # Reactor provides controls for every common input type. Each follows the same pattern: current value in, change handler out. class InputTypesDemo : Component { public override Element Render () { var (text, setText) = UseState( \"\" ); var (password, setPassword) = UseState( \"\" ); var (volume, setVolume) = UseState( 50.0 ); var (count, setCount) = UseState( 1.0 ); var (agree, setAgree) = UseState( false ); var (notify, setNotify) = UseState( true ); var (role, setRole) = UseState( 0 ); var (priority, setPriority) = UseState( 0 ); return VStack( 12 , TextBox(text, setText, placeholderText: \"Email\" , header: \"Email\" ), PasswordBox(password, setPassword, placeholderText: \"Enter password\" ), Slider(volume, 0 , 100 , setVolume), NumberBox(count, setCount, header: \"Quantity\" ), CheckBox(agree, setAgree, label: \"I agree to the terms\" ), ToggleSwitch(notify, setNotify, header: \"Notifications\" ), ComboBox([ \"Admin\" , \"Editor\" , \"Viewer\" ], role, setRole), RadioButtons([ \"Low\" , \"Medium\" , \"High\" ], priority, setPriority) ).Padding( 24 ); } } Control Value type Change handler TextBox string Action&#x3C;string> PasswordBox string Action&#x3C;string> Slider double Action&#x3C;double> NumberBox double Action&#x3C;double> CheckBox bool Action&#x3C;bool> ToggleSwitch bool Action&#x3C;bool> ComboBox int (index) Action&#x3C;int> RadioButtons int (index) Action&#x3C;int> All controls accept optional parameters for labels, headers, and placeholder text. Check the API reference for each control's full signature. Configuring TextBox # TextBox covers the common WinUI TextBox knobs through dedicated fluents so you rarely need .Set(...) . The named-input shapes set the appropriate InputScope for soft-keyboard and IME hinting: class TextBoxConfigDemo : Component { public override Element Render () { var (qty, setQty) = UseState( \"\" ); var (email, setEmail) = UseState( \"\" ); var (url, setUrl) = UseState( \"\" ); var (phone, setPhone) = UseState( \"\" ); var (search, setSearch) = UseState( \"\" ); var (note, setNote) = UseState( \"\" ); return VStack( 12 , TextBox(qty, setQty, header: \"Quantity\" ) .NumericInput(), TextBox(email, setEmail, header: \"Email\" ) .EmailInput(), TextBox(url, setUrl, header: \"URL\" ) .UrlInput(), TextBox(phone, setPhone, header: \"Phone\" ) .PhoneInput(), TextBox(search, setSearch, placeholderText: \"Search…\" ) .SearchInput(), TextBox(note, setNote, header: \"Reference code\" ) .MaxLength( 8 ) .CharacterCasing(CharacterCasing.Upper) .TextAlignment(TextAlignment.Center) .IsSpellCheckEnabled( false ) .Description( \"Eight characters, automatically uppercased.\" ) ).Padding( 24 ); } } Fluent Effect .NumericInput() InputScope = Number — numeric soft keyboard .EmailInput() InputScope = EmailSmtpAddress .UrlInput() InputScope = Url .PhoneInput() InputScope = TelephoneNumber .SearchInput() InputScope = Search .MaxLength(n) Caps input length at n characters .IsSpellCheckEnabled(bool) Toggles the squiggle underline .CharacterCasing(casing) Normal / Upper / Lower .TextAlignment(alignment) Aligns text within the field .Description(text) Helper text rendered below the field These fluents chain freely; the named-input shapes are the canonical way to hint mobile / touch keyboards instead of reaching for .Set(c => c.InputScope = ...) . See spec 039 §2.3 and §4.7 for the rationale. Simple Validation # For quick forms, derive validation from state. Compute booleans on every render, show inline error messages, and re-check the booleans inside the submit handler: class ValidationDemo : Component { public override Element Render () { var (email, setEmail) = UseState( \"\" ); var (age, setAge) = UseState( 0.0 ); var (showErrors, setShowErrors) = UseState( false ); var emailValid = email.Contains( '@' ) &#x26;&#x26; email.Contains( '.' ); var ageValid = age >= 18 &#x26;&#x26; age &#x3C;= 120 ; var formValid = emailValid &#x26;&#x26; ageValid &#x26;&#x26; ! string .IsNullOrWhiteSpace(email); return VStack( 12 , SubHeading( \"Simple Validation\" ), TextBox(email, setEmail, placeholderText: \"user@example.com\" , header: \"Email\" ), When(! string .IsNullOrEmpty(email) &#x26;&#x26; !emailValid, () => TextBlock( \"Enter a valid email address\" ) .Foreground(Theme.SystemCritical).FontSize( 12 )), NumberBox(age, setAge, header: \"Age\" ), When((showErrors || age > 0 ) &#x26;&#x26; !ageValid, () => TextBlock( \"Age must be between 18 and 120\" ) .Foreground(Theme.SystemCritical).FontSize( 12 )), Button( \"Submit\" , () => { setShowErrors( true ); if (!formValid) return ; // submit... }).Margin( 0 , 8 , 0 , 0 ) ).Padding( 24 ); } } This works well for small forms. For larger forms with cross-field rules, touched/dirty tracking, and error display policies, use the validation framework described next. Keeping Submit Reachable # A common form pattern — disable the Submit button until the form is valid — is a keyboard accessibility trap when combined with controls like NumberBox , DatePicker , and CalendarDatePicker , which commit their value on blur rather than per keystroke. The user fixes the last invalid field, presses Tab to reach Submit, and focus skips the still-disabled button. By the time the field commits and Submit becomes enabled, focus has already moved on. The user is stranded with no keyboard route to a button they can now press. Reactor offers two opt-in fixes for this. They compose: class KeepSubmitReachableDemo : Component { public override Element Render () { var (email, setEmail) = UseState( \"\" ); var (age, setAge) = UseState( 0.0 ); var emailValid = email.Contains( '@' ) &#x26;&#x26; email.Contains( '.' ); var ageValid = age >= 18 &#x26;&#x26; age &#x3C;= 120 ; var formValid = emailValid &#x26;&#x26; ageValid; return VStack( 12 , SubHeading( \"Keeping Submit Reachable\" ), TextBox(email, setEmail, header: \"Email\" , placeholderText: \"user@example.com\" ), // .Immediate() switches NumberBox from commit-on-blur to // commit-on-keystroke, so validation reacts as the user types. NumberBox(age, setAge, header: \"Age\" ).Immediate(), // .IsDisabledFocusable() keeps the button tab-reachable and // visually dimmed while preventing invocation. Pattern mirrors // Fluent UI's `disabledFocusable` and ARIA `aria-disabled`. Button( \"Submit\" , () => { /* submit */ }) .IsDisabledFocusable(!formValid) .Margin( 0 , 8 , 0 , 0 ) ).Padding( 24 ); } } .IsDisabledFocusable(bool) on Button keeps the button keyboard-focusable and tab-reachable while presenting it as disabled (dimmed; the click is suppressed). Mirrors Fluent UI React's disabledFocusable and ARIA's aria-disabled . Use it for any Submit gated on validation, busy state, or other derived conditions. .Immediate() on NumberBox (in Microsoft.UI.Reactor.Controls.Validation ) switches the control from commit-on-blur to commit-on-keystroke, so validation reacts to every parseable digit instead of waiting for focus to leave the field. Opt-in by design — commit-on-blur is the WinUI default and the right choice when an intermediate value would be expensive or surprising (snapping 2.50 to 2.5 mid-edit). Apply when validation gates UI state and you want it to feel live. Use .IsDisabledFocusable() whenever a button is conditionally disabled in a form — even if you've also applied .Immediate() to every commit-on-blur input. The two cover different failure modes: .Immediate() keeps validity in sync with typing; .IsDisabledFocusable() keeps the button discoverable when validity is gated on async checks, required-but-untouched fields, cross-field rules, or any derived condition that can't be made instantaneous. Where not to use .IsDisabledFocusable() : only buttons. For data-entry controls ( TextBox , NumberBox , CheckBox , etc.), IsEnabled=false usually means \"this field isn't part of your current task\" (cascading from another input), and tab-skipping is the correct UX. Use IsReadOnly if you need a visible-but-non-editable text control. Validation Context # UseValidationContext() creates a ValidationContext that tracks messages, touched/dirty state, and field registration. Attach validators to controls with .Validate() : class ValidationContextDemo : Component { public override Element Render () { var ctx = this .UseValidationContext(); var (email, setEmail) = UseState( \"\" ); var (password, setPassword) = UseState( \"\" ); var (submitted, setSubmitted) = UseState( false ); return VStack( 12 , SubHeading( \"Validation Context\" ), TextBox(email, v => { setEmail(v); ctx.NotifyValueChanged( \"email\" , v); }, placeholderText: \"user@example.com\" , header: \"Email\" ) .Validate( \"email\" , email, Validate.Required(), Validate.Email()), When(ctx.IsTouched( \"email\" ) &#x26;&#x26; ctx.HasError( \"email\" ), () => TextBlock(ctx.GetMessages( \"email\" ).First().Text) .Foreground(Theme.SystemCritical).FontSize( 12 )), PasswordBox(password, v => { setPassword(v); ctx.NotifyValueChanged( \"password\" , v); }, placeholderText: \"Min 8 characters\" ) .Validate( \"password\" , password, Validate.Required(), Validate.MinLength( 8 )), When(ctx.IsTouched( \"password\" ) &#x26;&#x26; ctx.HasError( \"password\" ), () => TextBlock(ctx.GetMessages( \"password\" ).First().Text) .Foreground(Theme.SystemCritical).FontSize( 12 )), Button( \"Register\" , () => { ctx.MarkAllTouched(); if (ctx.IsValid()) setSubmitted( true ); }).IsEnabled(!submitted), When(submitted, () => TextBlock( \"Registration successful!\" ) .Foreground(Theme.SystemSuccess).SemiBold()) ).Padding( 24 ); } } Key pieces: UseValidationContext() creates or retrieves the nearest validation context. .Validate(fieldName, value, validators...) attaches validators to a control. Validate.Required() , Validate.Email() , etc. are the 11 built-in validators. ctx.IsValid() returns true when no error-severity messages exist. ctx.MarkAllTouched() reveals all errors on submit attempt. FormField Helper # FormField() wraps a control with a label, required indicator, description text, and inline error display: class FormFieldDemo : Component { public override Element Render () { var ctx = this .UseValidationContext(); var (name, setName) = UseState( \"\" ); var (email, setEmail) = UseState( \"\" ); return VStack( 12 , SubHeading( \"FormField Helper\" ), FormField( TextBox(name, v => { setName(v); ctx.NotifyValueChanged( \"name\" , v); }) .Validate( \"name\" , name, Validate.Required()), label: \"Full Name\" , required : true , description: \"As it appears on your ID\" ), FormField( TextBox(email, v => { setEmail(v); ctx.NotifyValueChanged( \"email\" , v); }) .Validate( \"email\" , email, Validate.Required(), Validate.Email()), label: \"Email Address\" , required : true ) ).Padding( 24 ); } } FormField auto-detects the field name from the .Validate() attachment on its content. Errors appear below the field after the field is touched (focus then blur). The ShowWhen parameter controls when errors become visible: WhenTouched (default), WhenDirty , AfterFirstSubmit , Always , or Never . Built-in Validators # Validator Purpose Validate.Required() Non-null, non-empty, non-default Validate.MinLength(n) String length >= n Validate.MaxLength(n) String length &#x3C;= n Validate.Range(min, max) Numeric value in range Validate.Match(regex) Regex pattern match Validate.Email() Valid email address Validate.Url() Valid URL (http/https) Validate.Must&#x3C;T>(predicate, message) Custom predicate Validate.MustAsync&#x3C;T>(predicate, message) Async predicate Validate.MustBeTrue() Boolean is true (checkboxes) Validate.EqualTo&#x3C;T>(value) Equality check (password confirm) Every validator accepts an optional custom error message as its last parameter. Masked Input # MaskEngine applies input masks with auto-inserted literals. Use the built-in presets or define your own pattern: class MaskedInputDemo : Component { public override Element Render () { var phoneMask = UseMemo(() => new MaskEngine(MaskPreset.PhoneUS)); var dateMask = UseMemo(() => new MaskEngine(MaskPreset.Date)); var (phone, setPhone) = UseState( \"\" ); var (date, setDate) = UseState( \"\" ); return VStack( 12 , SubHeading( \"Masked Input\" ), TextBox(phoneMask.Apply(phone), v => setPhone(phoneMask.GetRawValue(v)), placeholderText: \"(___) ___-____\" , header: \"Phone\" ), TextBlock( $\"Raw: {phone} \" ).FontSize( 12 ).Opacity( 0.6 ), TextBox(dateMask.Apply(date), v => setDate(dateMask.GetRawValue(v)), placeholderText: \"__/__/____\" , header: \"Date\" ), TextBlock( $\"Raw: {date} \" ).FontSize( 12 ).Opacity( 0.6 ) ).Padding( 24 ); } } Mask tokens: 0 = required digit, 9 = optional digit, A = required letter, a = optional letter, * = required alphanumeric. All other characters are literals inserted automatically. Preset Pattern MaskPreset.PhoneUS (000) 000-0000 MaskPreset.SSN 000-00-0000 MaskPreset.CreditCard 0000 0000 0000 0000 MaskPreset.Date 00/00/0000 MaskPreset.ZipCode 00000 MaskPreset.IPv4 099.099.099.099 Input Formatters # InputFormatter transforms text as the user types. Chain formatters into a pipeline for complex formatting: class InputFormattersDemo : Component { public override Element Render () { var (currency, setCurrency) = UseState( \"\" ); var (upper, setUpper) = UseState( \"\" ); var currencyFmt = UseMemo(() => InputFormatter.Currency()); var upperFmt = UseMemo(() => InputFormatter.UpperCase); return VStack( 12 , SubHeading( \"Input Formatters\" ), TextBox(currencyFmt.Format(currency, 0 ).Output, v => setCurrency(currencyFmt.Parse(v)), placeholderText: \"$0.00\" , header: \"Amount\" ), TextBox(upperFmt.Format(upper, 0 ).Output, v => setUpper(upperFmt.Parse(v)), placeholderText: \"UPPERCASE\" , header: \"Code\" ) ).Padding( 24 ); } } Built-in formatters: PhoneUS , Currency() , UpperCase , LowerCase , TitleCase , TrimWhitespace , MaxLength(n) , AllowOnly(regex) , DenyOnly(regex) , and Custom(format, parse) . AutoSuggestBox # AutoSuggestBox is the search-with-suggestions input — type a prefix, see a filtered list, pick one or submit free-form. The factory takes the text value, an onTextChanged handler, and an optional onQuerySubmitted handler that fires on Enter or suggestion pick: class AutoSuggestDemo : Component { static readonly string [] Catalog = [ \"Aardvark\" , \"Albatross\" , \"Antelope\" , \"Badger\" , \"Beaver\" , \"Buffalo\" , \"Camel\" , \"Capybara\" ]; public override Element Render () { var (text, setText) = UseState( \"\" ); var matches = string .IsNullOrEmpty(text) ? Array.Empty&#x3C; string >() : Catalog.Where(c => c.StartsWith(text, StringComparison.OrdinalIgnoreCase)) .ToArray(); return VStack( 8 , SubHeading( \"AutoSuggestBox\" ), AutoSuggestBox(text, setText, onQuerySubmitted: q => setText(q)) .Header( \"Animal\" ) .QueryIcon(SymbolIcon( \"Find\" )) .Width( 280 ), // Suggestion list — bind to AutoSuggestBox.ItemsSource via .Set // when you need the in-control dropdown; the inline list below // is a custom presentation that gives full styling control. When(matches.Length > 0 , () => VStack( 2 , ForEach(matches, m => TextBlock(m).Padding( 8 , 4 )) ).Background( \"#F5F5F5\" ).Width( 280 )) ).Padding( 24 ); } } Fluent Effect .Header(string) Label rendered above the box. .QueryIcon(IconData) Trailing icon (a search glyph is typical). .IsSuggestionListOpen(bool) Force the dropdown open/closed. .SuggestionChosen(Action&#x3C;string>) Fires when the user picks an item from the underlying dropdown. .Set(b => b.ItemsSource = ...) Bind to the WinUI suggestion list. The snippet above renders an inline suggestion list as a custom presentation — more layout freedom than the built-in dropdown, at the cost of writing the visual surface yourself. For the canonical search-results recipe see recipes/search-with-suggestions . WinUI design page: Auto-suggest box . Date and time controls # Reactor exposes three date/time inputs and a calendar surface, each covering a different shape of the same problem: Control Value Shape Use when DatePicker DateTimeOffset (non-null) Inline three-spinner picker The date is always required and inline real estate is available. CalendarDatePicker DateTimeOffset? Button that opens a popup calendar Date is optional; you want a compact trigger. CalendarView IReadOnlyList&#x3C;DateTimeOffset> Full month grid Single, multiple, or range selection from a grid. TimePicker TimeSpan Hours / minutes / am-pm spinner Time-of-day independent of a date. class DatePickerDemo : Component { public override Element Render () { var (date, setDate) = UseState(DateTimeOffset.Now); var (optionalDate, setOptionalDate) = UseState&#x3C;DateTimeOffset?>( null ); return VStack( 8 , SubHeading( \"DatePicker — always-set value\" ), DatePicker(date, setDate) .DayFormat( \"{day.integer(2)}\" ) .MonthFormat( \"{month.abbreviated}\" ) .YearFormat( \"{year.full}\" ), TextBlock( $\"Selected: {date:yyyy-MM-dd} \" ).Opacity( 0.6 ), SubHeading( \"CalendarDatePicker — nullable, popup calendar\" ), CalendarDatePicker(optionalDate, setOptionalDate) .DateFormat( \"{month.abbreviated} {day.integer(2)}, {year.full}\" ) .IsTodayHighlighted(), TextBlock(optionalDate is null ? \"No date selected.\" : $\"Selected: {optionalDate:yyyy-MM-dd} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } class TimePickerDemo : Component { public override Element Render () { var (time, setTime) = UseState(TimeSpan.FromHours( 9 )); return VStack( 8 , SubHeading( \"TimePicker\" ), TimePicker(time, setTime), TextBlock( $\"Selected: {time:hh\\\\:mm} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } class CalendarViewDemo : Component { public override Element Render () { var (dates, setDates) = UseState&#x3C;IReadOnlyList&#x3C;DateTimeOffset>>( Array.Empty&#x3C;DateTimeOffset>()); return VStack( 8 , SubHeading( \"CalendarView — month grid\" ), CalendarView() .MinDate(DateTimeOffset.Now.AddYears( -1 )) .MaxDate(DateTimeOffset.Now.AddYears( 1 )) .NumberOfWeeksInView( 6 ) .SelectedDatesChanged(setDates) .SelectedDates(dates), TextBlock( $\" {dates.Count} day(s) selected\" ).Opacity( 0.6 ) ).Padding( 24 ); } } DatePicker / TimePicker / CalendarDatePicker share their format strings with the underlying WinUI DateTimeFormatter — \"{day.integer(2)}\" , \"{month.abbreviated}\" , \"{year.full}\" , and friends. CalendarView exposes .MinDate / .MaxDate for range constraint and .NumberOfWeeksInView to compress the grid. For multi-day selection, pair .SelectedDates(...) with .SelectedDatesChanged(...) — the event hands you the full snapshot of the current selection, not add/remove deltas (same shape as ListView multi-select ). WinUI design pages: Date picker , Time picker , Calendar view . ColorPicker # ColorPicker takes a Windows.UI.Color and a change handler — same controlled pattern as Slider . Its surface is the largest of any input on this page; configure it for the shape of the chooser you need: class ColorPickerDemo : Component { public override Element Render () { var (color, setColor) = UseState( global ::Windows.UI.Color.FromArgb( 255 , 0 , 120 , 215 )); return VStack( 8 , SubHeading( \"ColorPicker\" ), ColorPicker(color, setColor) .AlphaEnabled() .HexInputVisible( true ) .ColorSpectrumShape( Microsoft.UI.Xaml.Controls.ColorSpectrumShape.Ring), // Preview swatch driven by the picker. Border(Empty()) .Background( new Microsoft.UI.Xaml.Media.SolidColorBrush(color)) .Width( 80 ).Height( 40 ) .WithBorder( \"#888888\" ) ).Padding( 24 ); } } Fluent Effect .AlphaEnabled(bool) Show the alpha slider and pre-multiply by the alpha value. .ColorSpectrumShape(shape) Box (default) or Ring . .HexInputVisible(bool) Toggle the hex-input field. .ColorSpectrumVisible(bool) Toggle the main 2D spectrum. .ColorSliderVisible(bool) Toggle the hue slider. .ColorChannelTextInputVisible(bool) Toggle RGB number inputs. .HueRange(min, max) / .SaturationRange(min, max) / .ValueRange(min, max) Constrain pickable hue / sat / value. .MoreButtonVisible(bool) \"More\" chevron that expands the picker. For palette pickers (Material-style swatch grids), build the surface out of Button s and Background(color) rather than this control — ColorPicker is the right shape only when the user needs continuous selection from the full color space. WinUI design page: Color picker . Caveat: Commit-on-blur inputs ( NumberBox , DatePicker , CalendarDatePicker , TimePicker ) do not fire their change handlers per keystroke — they fire when focus leaves the field. Validation derived from these controls' values is therefore stale until the user tabs out. The classic failure mode: a Submit button disabled until the form is valid stays disabled while the user is still inside a NumberBox that already contains a valid value. The user fixes the last field, presses Tab to reach Submit, focus skips the still-disabled button, and the user is stranded. Two fixes compose: .Immediate() on the input switches it to per-keystroke commit (see Keeping Submit Reachable ), and .IsDisabledFocusable() on the Submit button keeps it keyboard-focusable while it's gated. Patterns # Multi-step form with shared ValidationContext # A wizard-style form spans multiple pages but the validation lives in one place. Hoist UseValidationContext() to the wizard component and pass it down via context — every step's .Validate(...) writes to the same store, and the final Submit checks ctx.IsValid() across all steps. The recipes/multi-step-form recipe walks the full pattern; the key shape is one context per form , not one per page. Submit on Enter # For a single-field form (search box, comment input), wire submission to Enter rather than a Submit button. AutoSuggestBox does this automatically via onQuerySubmitted . For TextBox , route through .KeyDown or wrap the field in an [ai:lock] form panel; see input-and-gestures for the routed-events surface. Validating async (uniqueness checks) # Validate.MustAsync&#x3C;T>(...) runs a predicate that returns Task&#x3C;bool> . The ValidationContext tracks the in-flight async work and reports IsValidating per field, so the Submit button can disable while async validation runs. Pair with DisabledFocusable() so the button stays in tab order while validating — same accessibility concern as Keeping Submit Reachable . Common Mistakes # Letting the control hold its own state # // Don't: TextBox(initial: \"default value\" ) class ControlledInputDemo : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); return VStack( 12 , SubHeading( \"Controlled Input\" ), TextBox(name, setName, placeholderText: \"Type your name\" ), TextBlock( $\"You typed: {name} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } Uncontrolled inputs work in trivial demos but break the moment the parent component needs to read, reset, or pre-fill the value. Reactor inputs all take (value, onChanged) — the control is always a passive view of state. Validating in the click handler # // Don't: Button( \"Submit\" , () => { if ( string .IsNullOrEmpty(email)) { setError( \"…\" ); return ; } if (!email.Contains( '@' )) { setError( \"…\" ); return ; } if (age &#x3C; 18 ) { setError( \"…\" ); return ; } // submit… }) Imperative checks duplicate every field's validation in the submit handler, drift from the inline error display, and fall out of date when fields are added. Use UseValidationContext .Validate(...) so each field carries its rules with it; the submit handler becomes one ctx.IsValid() call. Tips # Always use controlled inputs. Never let a control manage its own state. Your Render() method is the single source of truth — if you need to pre-fill, validate, or reset a field, you just set the state value. Use FormField for production forms. It handles labels, required indicators, and error display with consistent styling. Avoid building your own field wrapper. Use Validate.Must&#x3C;T>() for custom rules. When built-in validators don't cover your case, Must takes any Func&#x3C;T, bool> predicate. Call ctx.MarkAllTouched() on submit. This reveals all validation errors at once, so the user sees everything that needs fixing. Reset forms with ctx.ResetAll() . It returns all fields to their initial values, clears touched/dirty state, and removes all messages. Next Steps # Flex Layout — Previous: flexible box layout for adaptive UIs Collections — Next: render lists, grids, and virtualized data sets Hooks — UseState, UseMemo, and other hooks that power form logic Commanding — wire submit buttons to async commands with busy/error handling Styling and Theming — theme your forms with tokens and lightweight styling",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/forms.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/forms.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-controlled-input-pattern",
          "text": "The Controlled-Input Pattern"
        },
        {
          "depth": 2,
          "id": "input-control-types",
          "text": "Input Control Types"
        },
        {
          "depth": 2,
          "id": "configuring-textbox",
          "text": "Configuring TextBox"
        },
        {
          "depth": 2,
          "id": "simple-validation",
          "text": "Simple Validation"
        },
        {
          "depth": 2,
          "id": "keeping-submit-reachable",
          "text": "Keeping Submit Reachable"
        },
        {
          "depth": 2,
          "id": "validation-context",
          "text": "Validation Context"
        },
        {
          "depth": 2,
          "id": "formfield-helper",
          "text": "FormField Helper"
        },
        {
          "depth": 2,
          "id": "built-in-validators",
          "text": "Built-in Validators"
        },
        {
          "depth": 2,
          "id": "masked-input",
          "text": "Masked Input"
        },
        {
          "depth": 2,
          "id": "input-formatters",
          "text": "Input Formatters"
        },
        {
          "depth": 2,
          "id": "autosuggestbox",
          "text": "AutoSuggestBox"
        },
        {
          "depth": 2,
          "id": "date-and-time-controls",
          "text": "Date and time controls"
        },
        {
          "depth": 2,
          "id": "colorpicker",
          "text": "ColorPicker"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "multi-step-form-with-shared-validationcontext",
          "text": "Multi-step form with shared ValidationContext"
        },
        {
          "depth": 3,
          "id": "submit-on-enter",
          "text": "Submit on Enter"
        },
        {
          "depth": 3,
          "id": "validating-async-uniqueness-checks",
          "text": "Validating async (uniqueness checks)"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "letting-the-control-hold-its-own-state",
          "text": "Letting the control hold its own state"
        },
        {
          "depth": 3,
          "id": "validating-in-the-click-handler",
          "text": "Validating in the click handler"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Collections",
      "path": "collections.md",
      "slug": "/collections/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/items-collections\">Items Collections</a>.</p>\n</blockquote>\n<p>Collections are the highest-leverage primitive in any non-trivial app —\na contacts list, a feed, a settings tree, an editor's gutter. Microsoft.UI.Reactor (Reactor)\nships three typed bound collections (<code>ListView&#x3C;T></code>, <code>GridView&#x3C;T></code>,\n<code>LazyVStack&#x3C;T></code>) and one count-based virtualization primitive\n(<code>VirtualList</code>), plus the inline <code>ForEach</code> helper for non-scrolling\nmaps over data. The decision tree starts with two questions: how big is\nthe data, and how is it shaped. For a few dozen items in a list, reach\nfor <code>ListView&#x3C;T></code>. For thousands of items with the same row template,\nreach for <code>LazyVStack&#x3C;T></code> (virtualizes by default). For millions of\nitems or count-known-but-items-not-loaded scenarios, reach for\n<code>VirtualList</code>. For a tiled grid, <code>GridView&#x3C;T></code>. For inline maps inside\na <a href=\"/layout/\"><code>VStack</code></a>, <code>ForEach</code>. Every collection takes a\n<a href=\"#stable-identity-with-withkey\">key selector</a> so reconciliation can\nmatch items across renders; that is the single most important thing to\nget right. Skim the comparison table first, then jump to your control.</p>\n<h1 id=\"collections\">Collections<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#collections\">#</a></h1>\n<p>When you need to render a list of data, Reactor provides three typed collection\nelements and a simple <code>ForEach</code> helper. Each takes your data, a key selector,\nand a view builder function that returns an <a href=\"/components/\">element</a>.</p>\n<h2 id=\"sample-data\">Sample Data<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#sample-data\">#</a></h2>\n<p>The examples on this page use a shared <code>Contact</code> record and sample data\ngenerator:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Contact</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Name</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Email</span>);\n\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SampleData</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> List&#x3C;Contact> Contacts =\n        Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">50</span>).Select(i =>\n            <span class=\"hljs-keyword\">new</span> Contact(<span class=\"hljs-string\">$\"c<span class=\"hljs-subst\">{i}</span>\"</span>, <span class=\"hljs-string\">$\"Contact <span class=\"hljs-subst\">{i}</span>\"</span>,\n                <span class=\"hljs-string\">$\"user<span class=\"hljs-subst\">{i}</span>@example.com\"</span>)\n        ).ToList();\n}\n</code></pre>\n<h2 id=\"listview\">ListView<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#listview\">#</a></h2>\n<p><code>ListView&#x3C;T></code> renders a scrollable vertical list. Pass your data, a function\nthat returns a unique key for each item, and a builder that turns each item\ninto an element:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ListViewDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> contacts = SampleData.Contacts.Take(<span class=\"hljs-number\">10</span>).ToList();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"ListView\"</span>),\n            ListView&#x3C;Contact>(\n                contacts,\n                c => c.Id,\n                (contact, index) =>\n                    HStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(contact.Name).Bold(),\n                        TextBlock(contact.Email).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">8</span>)\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/listview.png\" alt=\"ListView with contacts\"></p>\n<p>The <code>keySelector</code> parameter (<code>c => c.Id</code>) tells Reactor how to identify each\nitem. When your data changes, Reactor uses keys to match old items to new ones\nand update only what changed — no full-list rebuild.</p>\n<h3 id=\"keyed-reconciliation-in-one-paragraph\">Keyed reconciliation, in one paragraph<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyed-reconciliation-in-one-paragraph\">#</a></h3>\n<p>When you replace the list (immutable state in, immutable state out), Reactor\nwalks the old and new key sequences and emits the minimum set of\n<code>Insert</code> / <code>Move</code> / <code>RemoveAt</code> operations to the underlying WinUI\n<code>ListView</code> / <code>GridView</code> / <code>ItemsRepeater</code>. A single insert at the front of\na 100-item list animates one row instead of re-realizing 100 containers.\nYou get this for free as long as <code>keySelector</code> returns a value that is:</p>\n<ul>\n<li><strong>Stable</strong> across re-renders for the lifetime of the item — using a\nrow index defeats reconciliation and produces the same churn as\nhaving no key.</li>\n<li><strong>Unique</strong> within the list — duplicate keys trigger a bulk-replace\nbailout and a one-shot diagnostic in the dev log.</li>\n<li><strong>Non-null</strong> — null keys bail out the diff for the affected list.</li>\n</ul>\n<h3 id=\"ireactorkeyed--identity-on-the-data\"><code>IReactorKeyed</code> — identity on the data<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#ireactorkeyed--identity-on-the-data\">#</a></h3>\n<p>When a model type owns its identity, implement <code>IReactorKeyed</code> to drop the\n<code>keySelector</code> boilerplate at every call site:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Contact</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Name</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Email</span>) : <span class=\"hljs-title\">IReactorKeyed</span>\n{\n    <span class=\"hljs-built_in\">string</span> IReactorKeyed.Key => Id;\n}\n\n<span class=\"hljs-comment\">// keySelector is inferred from IReactorKeyed.Key:</span>\nListView&#x3C;Contact>(contacts, (contact, index) => …);\nLazyVStack&#x3C;Contact>(contacts, (contact, index) => …);\nGridView&#x3C;Contact>(contacts, (contact, index) => …);\n</code></pre>\n<p>The explicit-<code>keySelector</code> overload remains the right choice for types you\ndo not own (interop / third-party POCOs without a natural identity\nproperty) — for those, keep <code>c => c.Id</code> at the call site.</p>\n<h3 id=\"withkeyitem-for-hand-built-children\"><code>.WithKey(item)</code> for hand-built children<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#withkeyitem-for-hand-built-children\">#</a></h3>\n<p>For hand-built keyed children — <code>FlexColumn(items.Select(…))</code> and\nsimilar — <code>.WithKey&#x3C;TKey>(TKey item) where TKey : IReactorKeyed</code> is the\nergonomic peer of <code>.WithKey(item.Key)</code>:</p>\n<pre><code class=\"hljs language-csharp\">FlexColumn(\n    contacts.Select(c =>\n        TextBlock(c.Name).WithKey(c)   <span class=\"hljs-comment\">// identity-on-data</span>\n    ).ToArray&#x3C;Element?>()\n)\n</code></pre>\n<p>Both shapes route through the same incremental diff, so a hand-built\n<code>FlexColumn</code> of contacts animates inserts and reorders just like the\ntemplated <code>ListView&#x3C;Contact></code>.</p>\n<h2 id=\"lazyvstack-virtualized\">LazyVStack (Virtualized)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lazyvstack-virtualized\">#</a></h2>\n<p><code>LazyVStack&#x3C;T></code> looks like <code>ListView&#x3C;T></code> but only creates elements for items\ncurrently visible on screen. Use it for large datasets:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LazyVStackDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> contacts = SampleData.Contacts;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">$\"LazyVStack (<span class=\"hljs-subst\">{contacts.Count}</span> items)\"</span>),\n            LazyVStack&#x3C;Contact>(\n                contacts,\n                c => c.Id,\n                (contact, index) =>\n                    HStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>.\"</span>).Width(<span class=\"hljs-number\">30</span>),\n                        TextBlock(contact.Name).Bold(),\n                        TextBlock(contact.Email).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">8</span>)\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/lazyvstack.png\" alt=\"LazyVStack with 50 items\"></p>\n<p>Even with 50 items in the list, <code>LazyVStack</code> only materializes the rows\nyou can see. As you scroll, it creates new rows and recycles old ones. This\nkeeps memory usage constant regardless of list size.</p>\n<p>When to use which:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Collection</th><th>Virtualized</th><th>Best for</th></tr></thead><tbody><tr><td><code>ListView&#x3C;T></code></td><td>No</td><td>Small lists (&#x3C; 50 items)</td></tr><tr><td><code>LazyVStack&#x3C;T></code></td><td>Yes</td><td>Large lists with known items</td></tr><tr><td><code>VirtualList</code></td><td>Yes</td><td>Count-based / async-loaded lists</td></tr></tbody></table>\n<h2 id=\"gridview\">GridView<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#gridview\">#</a></h2>\n<p><code>GridView&#x3C;T></code> lays items out in a wrapping grid. The framework determines\ncolumn count based on item width and available space:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GridViewDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> contacts = SampleData.Contacts.Take(<span class=\"hljs-number\">12</span>).ToList();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"GridView\"</span>),\n            GridView&#x3C;Contact>(\n                contacts,\n                c => c.Id,\n                (contact, index) =>\n                    VStack(<span class=\"hljs-number\">4</span>,\n                        TextBlock(contact.Name).Bold(),\n                        TextBlock(contact.Email).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">12</span>)\n                     .Background(<span class=\"hljs-string\">\"#f5f5f5\"</span>)\n                     .CornerRadius(<span class=\"hljs-number\">8</span>)\n                     .Width(<span class=\"hljs-number\">160</span>).Height(<span class=\"hljs-number\">80</span>)\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/gridview.png\" alt=\"GridView with contact cards\"></p>\n<p>Each item is sized by the element you return from the view builder. The\ngrid automatically wraps items into rows based on the container width.</p>\n<h2 id=\"virtuallist-count-based\">VirtualList (Count-Based)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#virtuallist-count-based\">#</a></h2>\n<p><code>VirtualList</code> provides count-based virtualization — you tell it how many\nitems exist and it calls your render function only for visible indices.\nUse it when items are loaded asynchronously or your data source provides\na count but not all items upfront:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">VirtualListDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"VirtualList (10,000 items)\"</span>),\n            VirtualList(\n                itemCount: <span class=\"hljs-number\">10</span>_000,\n                renderItem: index =>\n                    HStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>.\"</span>).Width(<span class=\"hljs-number\">50</span>),\n                        TextBlock(<span class=\"hljs-string\">$\"Item <span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>\"</span>).Bold(),\n                        TextBlock(<span class=\"hljs-string\">$\"data-<span class=\"hljs-subst\">{index}</span>@example.com\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">8</span>),\n                getItemKey: index => <span class=\"hljs-string\">$\"item-<span class=\"hljs-subst\">{index}</span>\"</span>,\n                itemHeight: <span class=\"hljs-number\">40</span>\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/virtuallist.png\" alt=\"VirtualList with 10,000 items\"></p>\n<p>Unlike <code>LazyVStack&#x3C;T></code> which takes a full list, <code>VirtualList</code> takes an\n<code>itemCount</code> and a <code>renderItem(index)</code> callback. This makes it ideal for\npaginated data sources where items load on demand.</p>\n<p><code>VirtualListRef</code> provides imperative control over the virtualized list:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">VirtualListRefDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> listRef = UseRef&#x3C;VirtualListRef?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> (targetIndex, setTargetIndex) = UseState(<span class=\"hljs-string\">\"5000\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"VirtualListRef — Imperative Scroll\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(targetIndex, setTargetIndex,\n                    placeholderText: <span class=\"hljs-string\">\"Index\"</span>),\n                Button(<span class=\"hljs-string\">\"Scroll To\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-built_in\">int</span>.TryParse(targetIndex, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> idx))\n                        listRef.Current?.ScrollToIndex(idx);\n                })\n            ),\n            VirtualList(\n                itemCount: <span class=\"hljs-number\">10</span>_000,\n                renderItem: index =>\n                    TextBlock(<span class=\"hljs-string\">$\"Row <span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>\"</span>).Padding(<span class=\"hljs-number\">8</span>),\n                getItemKey: index => <span class=\"hljs-string\">$\"row-<span class=\"hljs-subst\">{index}</span>\"</span>,\n                itemHeight: <span class=\"hljs-number\">36</span>,\n                @ref: r => listRef.Current = r\n            ).Height(<span class=\"hljs-number\">250</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ScrollToIndex(index)</code></td><td>Jump to a specific item</td></tr><tr><td><code>ScrollOffset</code></td><td>Current scroll position</td></tr><tr><td><code>RestoreScrollOffset(offset)</code></td><td>Restore a saved scroll position</td></tr><tr><td><code>Repeater</code></td><td>Access the underlying WinUI ItemsRepeater</td></tr></tbody></table>\n<p>Set <code>itemHeight</code> for a fixed-height fast path (O(1) offset calculation) or\n<code>estimatedItemHeight</code> for variable-height rows with automatic measurement.\nUse <code>onVisibleRangeChanged</code> to load data blocks as the user scrolls.</p>\n<h2 id=\"foreach\">ForEach<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#foreach\">#</a></h2>\n<p>For small, non-virtualized inline lists, use <code>ForEach</code>. It maps a collection\nto elements without creating a scrollable container:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ForEachDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> colors = <span class=\"hljs-keyword\">new</span>[]\n        {\n            (<span class=\"hljs-string\">\"Red\"</span>, <span class=\"hljs-string\">\"#ff4444\"</span>), (<span class=\"hljs-string\">\"Green\"</span>, <span class=\"hljs-string\">\"#44ff44\"</span>),\n            (<span class=\"hljs-string\">\"Blue\"</span>, <span class=\"hljs-string\">\"#4444ff\"</span>), (<span class=\"hljs-string\">\"Yellow\"</span>, <span class=\"hljs-string\">\"#ffff44\"</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"ForEach (non-virtualized)\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                ForEach(colors, ((<span class=\"hljs-built_in\">string</span> Name, <span class=\"hljs-built_in\">string</span> Hex) color) =>\n                    TextBlock(color.Name)\n                        .Padding(horizontal: <span class=\"hljs-number\">8</span>, vertical: <span class=\"hljs-number\">16</span>)\n                        .Background(color.Hex)\n                        .CornerRadius(<span class=\"hljs-number\">4</span>)\n                        .WithKey(color.Name)\n                )\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/foreach.png\" alt=\"ForEach colored tags\"></p>\n<p><code>ForEach</code> is a convenience for <code>items.Select(render).ToArray()</code> that works\ndirectly inside element trees. Use it when you want to inline a small list\nof items inside a larger <a href=\"/layout/\">layout</a>.</p>\n<h2 id=\"multi-select-with-selectionchanged\">Multi-Select with SelectionChanged<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#multi-select-with-selectionchanged\">#</a></h2>\n<p><code>ListView</code>, <code>GridView</code>, <code>ListBox</code>, and the typed peers (<code>ItemsView&#x3C;T></code>,\n<code>TemplatedListView&#x3C;T></code>, <code>TemplatedGridView&#x3C;T></code>) all expose a universal\n<code>SelectionChanged</code> fluent for multi-select scenarios. Set\n<code>SelectionMode = Multiple</code> (or <code>Extended</code>) and the handler fires with a\nsnapshot of the full selection on every change — not added/removed deltas:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MultiSelectDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> contacts = SampleData.Contacts.Take(<span class=\"hljs-number\">10</span>).ToList();\n        <span class=\"hljs-keyword\">var</span> (selectedIds, setSelectedIds) = UseState(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{selectedIds.Count}</span> selected\"</span>),\n            ListView&#x3C;Contact>(\n                contacts,\n                c => c.Id,\n                (contact, index) =>\n                    HStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(contact.Name).Bold(),\n                        TextBlock(contact.Email).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">8</span>)\n            )\n            .Set(lv => lv.SelectionMode =\n                Microsoft.UI.Xaml.Controls.ListViewSelectionMode.Multiple)\n            .SelectionChanged(selected =>\n                setSelectedIds(selected.Select(c => c.Id).ToList()))\n            .Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The handler signature varies by element type:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Handler</th></tr></thead><tbody><tr><td><code>ListView</code>, <code>GridView</code>, <code>ListBox</code></td><td><code>Action&#x3C;IReadOnlyList&#x3C;int>></code> (selected indices)</td></tr><tr><td><code>ItemsView&#x3C;T></code>, <code>TemplatedListView&#x3C;T></code>, <code>TemplatedGridView&#x3C;T></code></td><td><code>Action&#x3C;IReadOnlyList&#x3C;T>></code> (selected items)</td></tr></tbody></table>\n<p>Snapshot semantics match <code>CalendarView.SelectedDatesChanged</code> — the list you\nreceive is the full current selection, not the change since the last call.\nPassing <code>null</code> to the fluent clears any previously-set handler.</p>\n<blockquote>\n<p><code>TreeView</code> multi-select is intentionally deferred — see\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §5.8 for the\nrationale. Use single-select <code>OnItemInvoked</code> until then.</p>\n</blockquote>\n<h2 id=\"stable-identity-with-withkey\">Stable Identity with WithKey<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stable-identity-with-withkey\">#</a></h2>\n<p>When rendering dynamic lists, always give each item a stable key with\n<code>.WithKey()</code>. Without keys, Reactor matches items by position — adding or\nremoving an item causes every subsequent item to be rebuilt:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WithKeyDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(\n            <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>> { <span class=\"hljs-string\">\"Apple\"</span>, <span class=\"hljs-string\">\"Banana\"</span>, <span class=\"hljs-string\">\"Cherry\"</span> });\n        <span class=\"hljs-keyword\">var</span> (newItem, setNewItem) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Stable Identity with WithKey\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(newItem, setNewItem, placeholderText: <span class=\"hljs-string\">\"New item\"</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () => {\n                    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(newItem)) {\n                        updateItems(l => [.. l, newItem.Trim()]);\n                        setNewItem(<span class=\"hljs-string\">\"\"</span>);\n                    }\n                })\n            ),\n            VStack(<span class=\"hljs-number\">4</span>, items.Select((item, i) =>\n                HStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(item),\n                    Button(<span class=\"hljs-string\">\"Remove\"</span>, () => updateItems(\n                        l => l.Where((_, idx) => idx != i).ToList()))\n                ).WithKey(<span class=\"hljs-string\">$\"item-<span class=\"hljs-subst\">{item}</span>-<span class=\"hljs-subst\">{i}</span>\"</span>)\n            ).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/withkey.png\" alt=\"WithKey demo\"></p>\n<p>The typed collections (<code>ListView&#x3C;T></code>, <code>LazyVStack&#x3C;T></code>, <code>GridView&#x3C;T></code>) handle\nkeying automatically through their <code>keySelector</code> parameter. You only need\n<code>.WithKey()</code> manually when using <code>ForEach</code>, <code>Select().ToArray()</code>, or other\nmanual list rendering.</p>\n<p>Rules for good keys:</p>\n<ul>\n<li><strong>Use a stable identifier</strong> from your data (database ID, unique name).\nAvoid using the array index as a key — it defeats the purpose.</li>\n<li><strong>Keys must be unique</strong> within their sibling list. Duplicates cause\nundefined reconciliation behavior.</li>\n<li><strong>Keys should be strings.</strong> The <code>WithKey</code> modifier accepts a string.</li>\n</ul>\n<h2 id=\"grouping\">Grouping<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#grouping\">#</a></h2>\n<p>Reactor doesn't ship a built-in grouped-list control. The composition\nrecipe is straightforward: group the data with LINQ, then render a\n<code>VStack</code> of <code>header + items</code> per group. Each group's body is its own\ntyped collection, so virtualization still applies inside a section if\nyou swap <code>ForEach</code> for <code>LazyVStack&#x3C;T></code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GroupingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> grouped = SampleData.Contacts\n            .Take(<span class=\"hljs-number\">24</span>)\n            .GroupBy(c => c.Name[<span class=\"hljs-number\">0</span>])\n            .OrderBy(g => g.Key)\n            .ToList();\n\n        <span class=\"hljs-comment\">// Reactor doesn't ship a built-in grouped-list control; instead,</span>\n        <span class=\"hljs-comment\">// compose a VStack of header + items per group. The render</span>\n        <span class=\"hljs-comment\">// function for each group hands back its own typed collection,</span>\n        <span class=\"hljs-comment\">// so virtualization still applies inside each section if you</span>\n        <span class=\"hljs-comment\">// swap LazyVStack for ListView.</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">$\"Grouped: <span class=\"hljs-subst\">{grouped.Count}</span> sections\"</span>),\n            ScrollView(\n                VStack(<span class=\"hljs-number\">16</span>,\n                    ForEach(grouped, <span class=\"hljs-keyword\">group</span> =>\n                        VStack(<span class=\"hljs-number\">4</span>,\n                            TextBlock(<span class=\"hljs-string\">$\"— <span class=\"hljs-subst\">{<span class=\"hljs-keyword\">group</span>.Key}</span> —\"</span>).Bold()\n                                .Opacity(<span class=\"hljs-number\">0.7</span>),\n                            ForEach(<span class=\"hljs-keyword\">group</span>.ToArray(), c =>\n                                HStack(<span class=\"hljs-number\">8</span>,\n                                    TextBlock(c.Name).Bold(),\n                                    TextBlock(c.Email).Opacity(<span class=\"hljs-number\">0.6</span>))\n                                    .WithKey(c.Id))\n                        ).WithKey(<span class=\"hljs-string\">$\"group-<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">group</span>.Key}</span>\"</span>))\n                ).Padding(<span class=\"hljs-number\">8</span>)\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/grouping.png\" alt=\"Grouped contacts list with letter section headers\"></p>\n<p>The shape generalizes to two-level grouping (city → country), sticky\nheaders (set <code>Position</code> via a <code>Border</code> modifier), and collapsible\nsections (wrap each group's body in <code>When(expanded[key], ...)</code>).\nBecause every group's collection has its own keyed render, items can\nmove between groups across renders without remounting — the keys\ntravel with the items.</p>\n<h2 id=\"drag-to-reorder\">Drag-to-reorder<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#drag-to-reorder\">#</a></h2>\n<p>WinUI <code>ListView</code> and <code>GridView</code> ship drag-reorder, and Reactor exposes\nthe relevant properties through the <code>.Set</code> passthrough until a\nfirst-class fluent ships. Three properties switch the surface on —\n<code>CanReorderItems</code>, <code>AllowDrop</code>, and <code>CanDragItems</code> — and the list\nmutates its internal <code>ItemsSource</code> order on drop. Mirror the new order\nback into your state via the underlying <code>ItemsSource</code> collection or a\n<code>DragItemsCompleted</code> handler:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DragReorderDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, setItems) = UseState(\n            <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>> { <span class=\"hljs-string\">\"Alpha\"</span>, <span class=\"hljs-string\">\"Bravo\"</span>, <span class=\"hljs-string\">\"Charlie\"</span>,\n                <span class=\"hljs-string\">\"Delta\"</span>, <span class=\"hljs-string\">\"Echo\"</span>, <span class=\"hljs-string\">\"Foxtrot\"</span> });\n\n        <span class=\"hljs-comment\">// Reactor surfaces drag-reorder through the underlying WinUI</span>\n        <span class=\"hljs-comment\">// ListView's CanReorderItems / AllowDrop / CanDragItems. The</span>\n        <span class=\"hljs-comment\">// .Set passthrough is the supported escape hatch until a</span>\n        <span class=\"hljs-comment\">// first-class fluent ships. The user's reorder is mirrored</span>\n        <span class=\"hljs-comment\">// back into state via the ListView's reorder event.</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Drag to reorder\"</span>),\n            ListView&#x3C;<span class=\"hljs-built_in\">string</span>>(\n                items,\n                s => s,\n                (item, _) =>\n                    HStack(<span class=\"hljs-number\">8</span>,\n                        TextBlock(<span class=\"hljs-string\">\"☰\"</span>).Opacity(<span class=\"hljs-number\">0.4</span>),\n                        TextBlock(item).Bold()\n                    ).Padding(<span class=\"hljs-number\">8</span>))\n                .Set(lv =>\n                {\n                    lv.CanReorderItems = <span class=\"hljs-literal\">true</span>;\n                    lv.AllowDrop = <span class=\"hljs-literal\">true</span>;\n                    lv.CanDragItems = <span class=\"hljs-literal\">true</span>;\n                })\n                .Height(<span class=\"hljs-number\">260</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Property</th><th>Effect</th></tr></thead><tbody><tr><td><code>CanDragItems</code></td><td>The user can start a drag from a row.</td></tr><tr><td><code>AllowDrop</code></td><td>The list accepts drops.</td></tr><tr><td><code>CanReorderItems</code></td><td>Drops inside the list reorder; drops outside fire <code>DragItemsCompleted</code>.</td></tr></tbody></table>\n<p><code>GridView</code> and <code>ItemsView&#x3C;T></code> expose the same three properties. For\nfree-form drag-and-drop between two lists (move item from A to B),\nsubscribe to <code>DragItemsStarting</code> on the source and <code>Drop</code> on the\ndestination, then update both states. The\n<a href=\"/recipes/drag-reorder/\"><code>recipes/drag-reorder</code></a> recipe walks the\nsingle-list case end-to-end.</p>\n<h2 id=\"lazy-loading\">Lazy loading<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lazy-loading\">#</a></h2>\n<p>For data sources where the total count is known but the items are\nloaded incrementally (paged APIs, large local stores), the\n<code>onVisibleRangeChanged</code> callback on <code>VirtualList</code> is the load\ntrigger. The callback fires whenever the visible window changes;\ncompare the trailing edge to your high-water mark and request the\nnext page when the user scrolls past it:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LazyLoadingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Pretend \"loaded\" up to a high-water mark; new items fetch</span>\n        <span class=\"hljs-comment\">// when the visible range crosses into unloaded territory.</span>\n        <span class=\"hljs-keyword\">var</span> (loadedTo, setLoadedTo) = UseState(<span class=\"hljs-number\">50</span>);\n        <span class=\"hljs-keyword\">var</span> totalCount = <span class=\"hljs-number\">1</span>_000;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">$\"Lazy-load — fetched <span class=\"hljs-subst\">{loadedTo}</span> of <span class=\"hljs-subst\">{totalCount}</span>\"</span>),\n            VirtualList(\n                itemCount: totalCount,\n                renderItem: index =>\n                    index &#x3C; loadedTo\n                        ? HStack(<span class=\"hljs-number\">8</span>,\n                            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>.\"</span>).Width(<span class=\"hljs-number\">50</span>),\n                            TextBlock(<span class=\"hljs-string\">$\"Row <span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>\"</span>).Bold(),\n                            TextBlock(<span class=\"hljs-string\">$\"loaded\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>))\n                            .Padding(<span class=\"hljs-number\">8</span>)\n                        <span class=\"hljs-comment\">// Skeleton for not-yet-loaded indices.</span>\n                        : HStack(<span class=\"hljs-number\">8</span>,\n                            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>.\"</span>).Width(<span class=\"hljs-number\">50</span>),\n                            TextBlock(<span class=\"hljs-string\">\"loading…\"</span>).Opacity(<span class=\"hljs-number\">0.4</span>))\n                            .Padding(<span class=\"hljs-number\">8</span>),\n                getItemKey: index => <span class=\"hljs-string\">$\"lazy-<span class=\"hljs-subst\">{index}</span>\"</span>,\n                itemHeight: <span class=\"hljs-number\">40</span>,\n                <span class=\"hljs-comment\">// Watcher fires whenever the visible range changes —</span>\n                <span class=\"hljs-comment\">// bump the high-water mark when the bottom passes the</span>\n                <span class=\"hljs-comment\">// current limit.</span>\n                onVisibleRangeChanged: (first, last) =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (last >= loadedTo - <span class=\"hljs-number\">5</span> &#x26;&#x26; loadedTo &#x3C; totalCount)\n                        setLoadedTo(Math.Min(loadedTo + <span class=\"hljs-number\">50</span>, totalCount));\n                }\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/collections/lazy-loading.png\" alt=\"VirtualList with skeleton rows past the loaded high-water mark\"></p>\n<p>Pair this with <a href=\"/async-resources/\"><code>UseResource</code></a> to manage the\nasync fetch state — <code>Pending</code> becomes the skeleton row, <code>Loaded</code>\nbecomes the populated row, <code>Error</code> becomes a retry inline. The full\nshape lives in the <a href=\"/recipes/paginated-list/\"><code>recipes/paginated-list</code></a>\nrecipe.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>itemHeight</code> vs. <code>estimatedItemHeight</code> is the single most expensive\ndecision in <code>VirtualList</code>. With <code>itemHeight</code> set, scrollbar position\nis O(1) — multiply the index by the height. Without it, the list\nmeasures every row that has been seen and maintains a cumulative\noffset table; the scrollbar approximation drifts and large jumps can\ncause measure-storms. Set <code>itemHeight</code> whenever your rows are the\nsame fixed height — it is almost always the right choice for paginated\ndata, message lists, and table-shaped UIs. Fall back to\n<code>estimatedItemHeight</code> only when the row heights genuinely vary\n(masonry feeds, chat with rich attachments). The default\n<code>estimatedItemHeight: 40</code> is a guess; tune it to within ±25% of your\nreal row heights to keep scroll-bar drift under control.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"virtualized-contacts-with-letter-jump\">Virtualized contacts with letter-jump<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#virtualized-contacts-with-letter-jump\">#</a></h3>\n<p>Combine grouping (section per letter) with <code>VirtualListRef</code> imperative\nscroll: the user clicks a letter, the list calls <code>ScrollToIndex</code> for\nthe first row in that group. This is the canonical \"A-Z scrubber\"\npattern from contacts apps:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> listRef = UseRef&#x3C;VirtualListRef?>(<span class=\"hljs-literal\">null</span>);\n<span class=\"hljs-keyword\">var</span> groupStarts = UseMemo(() => ComputeStartIndices(contacts), contacts);\n\n<span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">0</span>,\n    VirtualList(contacts.Count, RenderRow,\n        getItemKey: i => contacts[i].Id,\n        itemHeight: <span class=\"hljs-number\">60</span>,\n        @ref: r => listRef.Current = r).Width(<span class=\"hljs-number\">360</span>),\n    VStack(<span class=\"hljs-number\">2</span>,\n        ForEach(<span class=\"hljs-string\">\"ABCDEFGHIJKLMNOPQRSTUVWXYZ\"</span>.ToCharArray(), letter =>\n            Button(letter.ToString(), () =>\n                listRef.Current?.ScrollToIndex(groupStarts[letter]))))\n);\n</code></pre>\n<h3 id=\"lift-state-for-selection-across-remounts\">Lift state for selection across remounts<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lift-state-for-selection-across-remounts\">#</a></h3>\n<p>Selection state belongs to the parent, never to the collection. The\nparent owns the <code>HashSet&#x3C;TKey></code> of selected IDs; the row template\nchecks membership on every render to set <code>IsSelected</code>. This pattern\nsurvives data refresh, sort changes, filter changes, and remounts —\nall of which would lose selection if it lived inside the list. Same\nshape as form state from <a href=\"/forms/\">forms.md</a>.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"using-array-index-as-key\">Using array index as key<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-array-index-as-key\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nForEach(items, (item, i) => Row(item).WithKey(i.ToString()))\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WithKeyDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(\n            <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>> { <span class=\"hljs-string\">\"Apple\"</span>, <span class=\"hljs-string\">\"Banana\"</span>, <span class=\"hljs-string\">\"Cherry\"</span> });\n        <span class=\"hljs-keyword\">var</span> (newItem, setNewItem) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Stable Identity with WithKey\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(newItem, setNewItem, placeholderText: <span class=\"hljs-string\">\"New item\"</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () => {\n                    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(newItem)) {\n                        updateItems(l => [.. l, newItem.Trim()]);\n                        setNewItem(<span class=\"hljs-string\">\"\"</span>);\n                    }\n                })\n            ),\n            VStack(<span class=\"hljs-number\">4</span>, items.Select((item, i) =>\n                HStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(item),\n                    Button(<span class=\"hljs-string\">\"Remove\"</span>, () => updateItems(\n                        l => l.Where((_, idx) => idx != i).ToList()))\n                ).WithKey(<span class=\"hljs-string\">$\"item-<span class=\"hljs-subst\">{item}</span>-<span class=\"hljs-subst\">{i}</span>\"</span>)\n            ).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Index keys defeat the purpose of keys. When the list reorders or an\nitem is removed, every subsequent item gets a new key, every row\nremounts, every text input inside a row loses focus, and animations\nrestart. Use a stable identifier from your data.</p>\n<h3 id=\"not-setting-itemheight-on-a-uniform-height-virtuallist\">Not setting <code>itemHeight</code> on a uniform-height VirtualList<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#not-setting-itemheight-on-a-uniform-height-virtuallist\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nVirtualList(itemCount, RenderItem, getItemKey: GetKey)\n<span class=\"hljs-comment\">// estimatedItemHeight defaults to 40 — drift accumulates for any</span>\n<span class=\"hljs-comment\">// row whose actual height differs.</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">VirtualListDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"VirtualList (10,000 items)\"</span>),\n            VirtualList(\n                itemCount: <span class=\"hljs-number\">10</span>_000,\n                renderItem: index =>\n                    HStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>.\"</span>).Width(<span class=\"hljs-number\">50</span>),\n                        TextBlock(<span class=\"hljs-string\">$\"Item <span class=\"hljs-subst\">{index + <span class=\"hljs-number\">1</span>}</span>\"</span>).Bold(),\n                        TextBlock(<span class=\"hljs-string\">$\"data-<span class=\"hljs-subst\">{index}</span>@example.com\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n                    ).Padding(<span class=\"hljs-number\">8</span>),\n                getItemKey: index => <span class=\"hljs-string\">$\"item-<span class=\"hljs-subst\">{index}</span>\"</span>,\n                itemHeight: <span class=\"hljs-number\">40</span>\n            ).Height(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>If your rows are all the same height (the common case), tell the list.\nThe O(1) offset math is dramatically faster than the cumulative\nmeasure table, and the scrollbar tracks the true position rather than\nthe estimate.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use <code>keySelector</code> wisely.</strong> The key must uniquely identify each item across\nre-renders. A database ID or GUID is ideal. Avoid index-based keys like\n<code>i.ToString()</code> — they break when items are reordered or removed.</p>\n<p><strong>Prefer <code>LazyVStack&#x3C;T></code> for anything beyond a handful of items.</strong> The\nvirtualization overhead is negligible, but the memory savings with large lists\nare significant.</p>\n<p><strong>Keep view builders simple.</strong> The function you pass to <code>ListView&#x3C;T></code> runs\nfor every visible item on every render. Extract complex item layouts into\ntheir own <a href=\"/components/\"><code>Component&#x3C;TProps></code></a> to get automatic memoization.</p>\n<p><strong>Use <code>ForEach</code> for inline lists, typed collections for scrollable lists.</strong>\n<code>ForEach</code> does not create a scroll container — it just maps data to elements.\nFor scrollable content, use <code>ListView&#x3C;T></code> or <code>LazyVStack&#x3C;T></code>.</p>\n<p><strong>Remember the <code>index</code> parameter.</strong> All view builders receive <code>(T item, int index)</code>.\nUse the index for display (row numbers) but not for keys.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — controlled input controls and validation patterns</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — stack-based routing, NavigationView, and tabs</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — DataGrid with sort, filter, search, and inline editing</li>\n<li><strong><a href=\"/flex-layout/\">Flex Layout</a></strong> — wrapping grids and proportional sizing for collection items</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — extract item templates into reusable memoized components</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Items Collections . Collections are the highest-leverage primitive in any non-trivial app — a contacts list, a feed, a settings tree, an editor's gutter. Microsoft.UI.Reactor (Reactor) ships three typed bound collections ( ListView&#x3C;T> , GridView&#x3C;T> , LazyVStack&#x3C;T> ) and one count-based virtualization primitive ( VirtualList ), plus the inline ForEach helper for non-scrolling maps over data. The decision tree starts with two questions: how big is the data, and how is it shaped. For a few dozen items in a list, reach for ListView&#x3C;T> . For thousands of items with the same row template, reach for LazyVStack&#x3C;T> (virtualizes by default). For millions of items or count-known-but-items-not-loaded scenarios, reach for VirtualList . For a tiled grid, GridView&#x3C;T> . For inline maps inside a VStack , ForEach . Every collection takes a key selector so reconciliation can match items across renders; that is the single most important thing to get right. Skim the comparison table first, then jump to your control. Collections # When you need to render a list of data, Reactor provides three typed collection elements and a simple ForEach helper. Each takes your data, a key selector, and a view builder function that returns an element . Sample Data # The examples on this page use a shared Contact record and sample data generator: record Contact ( string Id , string Name , string Email ); static class SampleData { public static readonly List&#x3C;Contact> Contacts = Enumerable.Range( 1 , 50 ).Select(i => new Contact( $\"c {i} \" , $\"Contact {i} \" , $\"user {i} @example.com\" ) ).ToList(); } ListView # ListView&#x3C;T> renders a scrollable vertical list. Pass your data, a function that returns a unique key for each item, and a builder that turns each item into an element: class ListViewDemo : Component { public override Element Render () { var contacts = SampleData.Contacts.Take( 10 ).ToList(); return VStack( 12 , SubHeading( \"ListView\" ), ListView&#x3C;Contact>( contacts, c => c.Id, (contact, index) => HStack( 12 , TextBlock(contact.Name).Bold(), TextBlock(contact.Email).Opacity( 0.6 ) ).Padding( 8 ) ).Height( 300 ) ).Padding( 24 ); } } The keySelector parameter ( c => c.Id ) tells Reactor how to identify each item. When your data changes, Reactor uses keys to match old items to new ones and update only what changed — no full-list rebuild. Keyed reconciliation, in one paragraph # When you replace the list (immutable state in, immutable state out), Reactor walks the old and new key sequences and emits the minimum set of Insert / Move / RemoveAt operations to the underlying WinUI ListView / GridView / ItemsRepeater . A single insert at the front of a 100-item list animates one row instead of re-realizing 100 containers. You get this for free as long as keySelector returns a value that is: Stable across re-renders for the lifetime of the item — using a row index defeats reconciliation and produces the same churn as having no key. Unique within the list — duplicate keys trigger a bulk-replace bailout and a one-shot diagnostic in the dev log. Non-null — null keys bail out the diff for the affected list. IReactorKeyed — identity on the data # When a model type owns its identity, implement IReactorKeyed to drop the keySelector boilerplate at every call site: record Contact ( string Id , string Name , string Email ) : IReactorKeyed { string IReactorKeyed.Key => Id; } // keySelector is inferred from IReactorKeyed.Key: ListView&#x3C;Contact>(contacts, (contact, index) => …); LazyVStack&#x3C;Contact>(contacts, (contact, index) => …); GridView&#x3C;Contact>(contacts, (contact, index) => …); The explicit- keySelector overload remains the right choice for types you do not own (interop / third-party POCOs without a natural identity property) — for those, keep c => c.Id at the call site. .WithKey(item) for hand-built children # For hand-built keyed children — FlexColumn(items.Select(…)) and similar — .WithKey&#x3C;TKey>(TKey item) where TKey : IReactorKeyed is the ergonomic peer of .WithKey(item.Key) : FlexColumn( contacts.Select(c => TextBlock(c.Name).WithKey(c) // identity-on-data ).ToArray&#x3C;Element?>() ) Both shapes route through the same incremental diff, so a hand-built FlexColumn of contacts animates inserts and reorders just like the templated ListView&#x3C;Contact> . LazyVStack (Virtualized) # LazyVStack&#x3C;T> looks like ListView&#x3C;T> but only creates elements for items currently visible on screen. Use it for large datasets: class LazyVStackDemo : Component { public override Element Render () { var contacts = SampleData.Contacts; return VStack( 12 , SubHeading( $\"LazyVStack ( {contacts.Count} items)\" ), LazyVStack&#x3C;Contact>( contacts, c => c.Id, (contact, index) => HStack( 12 , TextBlock( $\" {index + 1 } .\" ).Width( 30 ), TextBlock(contact.Name).Bold(), TextBlock(contact.Email).Opacity( 0.6 ) ).Padding( 8 ) ).Height( 300 ) ).Padding( 24 ); } } Even with 50 items in the list, LazyVStack only materializes the rows you can see. As you scroll, it creates new rows and recycles old ones. This keeps memory usage constant regardless of list size. When to use which: Collection Virtualized Best for ListView&#x3C;T> No Small lists (&#x3C; 50 items) LazyVStack&#x3C;T> Yes Large lists with known items VirtualList Yes Count-based / async-loaded lists GridView # GridView&#x3C;T> lays items out in a wrapping grid. The framework determines column count based on item width and available space: class GridViewDemo : Component { public override Element Render () { var contacts = SampleData.Contacts.Take( 12 ).ToList(); return VStack( 12 , SubHeading( \"GridView\" ), GridView&#x3C;Contact>( contacts, c => c.Id, (contact, index) => VStack( 4 , TextBlock(contact.Name).Bold(), TextBlock(contact.Email).FontSize( 12 ).Opacity( 0.6 ) ).Padding( 12 ) .Background( \"#f5f5f5\" ) .CornerRadius( 8 ) .Width( 160 ).Height( 80 ) ).Height( 300 ) ).Padding( 24 ); } } Each item is sized by the element you return from the view builder. The grid automatically wraps items into rows based on the container width. VirtualList (Count-Based) # VirtualList provides count-based virtualization — you tell it how many items exist and it calls your render function only for visible indices. Use it when items are loaded asynchronously or your data source provides a count but not all items upfront: class VirtualListDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"VirtualList (10,000 items)\" ), VirtualList( itemCount: 10 _000, renderItem: index => HStack( 12 , TextBlock( $\" {index + 1 } .\" ).Width( 50 ), TextBlock( $\"Item {index + 1 } \" ).Bold(), TextBlock( $\"data- {index} @example.com\" ).Opacity( 0.6 ) ).Padding( 8 ), getItemKey: index => $\"item- {index} \" , itemHeight: 40 ).Height( 300 ) ).Padding( 24 ); } } Unlike LazyVStack&#x3C;T> which takes a full list, VirtualList takes an itemCount and a renderItem(index) callback. This makes it ideal for paginated data sources where items load on demand. VirtualListRef provides imperative control over the virtualized list: class VirtualListRefDemo : Component { public override Element Render () { var listRef = UseRef&#x3C;VirtualListRef?>( null ); var (targetIndex, setTargetIndex) = UseState( \"5000\" ); return VStack( 12 , SubHeading( \"VirtualListRef — Imperative Scroll\" ), HStack( 8 , TextBox(targetIndex, setTargetIndex, placeholderText: \"Index\" ), Button( \"Scroll To\" , () => { if ( int .TryParse(targetIndex, out var idx)) listRef.Current?.ScrollToIndex(idx); }) ), VirtualList( itemCount: 10 _000, renderItem: index => TextBlock( $\"Row {index + 1 } \" ).Padding( 8 ), getItemKey: index => $\"row- {index} \" , itemHeight: 36 , @ref: r => listRef.Current = r ).Height( 250 ) ).Padding( 24 ); } } Member Purpose ScrollToIndex(index) Jump to a specific item ScrollOffset Current scroll position RestoreScrollOffset(offset) Restore a saved scroll position Repeater Access the underlying WinUI ItemsRepeater Set itemHeight for a fixed-height fast path (O(1) offset calculation) or estimatedItemHeight for variable-height rows with automatic measurement. Use onVisibleRangeChanged to load data blocks as the user scrolls. ForEach # For small, non-virtualized inline lists, use ForEach . It maps a collection to elements without creating a scrollable container: class ForEachDemo : Component { public override Element Render () { var colors = new [] { ( \"Red\" , \"#ff4444\" ), ( \"Green\" , \"#44ff44\" ), ( \"Blue\" , \"#4444ff\" ), ( \"Yellow\" , \"#ffff44\" ) }; return VStack( 12 , SubHeading( \"ForEach (non-virtualized)\" ), HStack( 8 , ForEach(colors, (( string Name, string Hex) color) => TextBlock(color.Name) .Padding(horizontal: 8 , vertical: 16 ) .Background(color.Hex) .CornerRadius( 4 ) .WithKey(color.Name) ) ) ).Padding( 24 ); } } ForEach is a convenience for items.Select(render).ToArray() that works directly inside element trees. Use it when you want to inline a small list of items inside a larger layout . Multi-Select with SelectionChanged # ListView , GridView , ListBox , and the typed peers ( ItemsView&#x3C;T> , TemplatedListView&#x3C;T> , TemplatedGridView&#x3C;T> ) all expose a universal SelectionChanged fluent for multi-select scenarios. Set SelectionMode = Multiple (or Extended ) and the handler fires with a snapshot of the full selection on every change — not added/removed deltas: class MultiSelectDemo : Component { public override Element Render () { var contacts = SampleData.Contacts.Take( 10 ).ToList(); var (selectedIds, setSelectedIds) = UseState( new List&#x3C; string >()); return VStack( 12 , SubHeading( $\" {selectedIds.Count} selected\" ), ListView&#x3C;Contact>( contacts, c => c.Id, (contact, index) => HStack( 12 , TextBlock(contact.Name).Bold(), TextBlock(contact.Email).Opacity( 0.6 ) ).Padding( 8 ) ) .Set(lv => lv.SelectionMode = Microsoft.UI.Xaml.Controls.ListViewSelectionMode.Multiple) .SelectionChanged(selected => setSelectedIds(selected.Select(c => c.Id).ToList())) .Height( 300 ) ).Padding( 24 ); } } The handler signature varies by element type: Element Handler ListView , GridView , ListBox Action&#x3C;IReadOnlyList&#x3C;int>> (selected indices) ItemsView&#x3C;T> , TemplatedListView&#x3C;T> , TemplatedGridView&#x3C;T> Action&#x3C;IReadOnlyList&#x3C;T>> (selected items) Snapshot semantics match CalendarView.SelectedDatesChanged — the list you receive is the full current selection, not the change since the last call. Passing null to the fluent clears any previously-set handler. TreeView multi-select is intentionally deferred — see spec 039 §5.8 for the rationale. Use single-select OnItemInvoked until then. Stable Identity with WithKey # When rendering dynamic lists, always give each item a stable key with .WithKey() . Without keys, Reactor matches items by position — adding or removing an item causes every subsequent item to be rebuilt: class WithKeyDemo : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C; string > { \"Apple\" , \"Banana\" , \"Cherry\" }); var (newItem, setNewItem) = UseState( \"\" ); return VStack( 12 , SubHeading( \"Stable Identity with WithKey\" ), HStack( 8 , TextBox(newItem, setNewItem, placeholderText: \"New item\" ), Button( \"Add\" , () => { if (! string .IsNullOrWhiteSpace(newItem)) { updateItems(l => [.. l, newItem.Trim()]); setNewItem( \"\" ); } }) ), VStack( 4 , items.Select((item, i) => HStack( 8 , TextBlock(item), Button( \"Remove\" , () => updateItems( l => l.Where((_, idx) => idx != i).ToList())) ).WithKey( $\"item- {item} - {i} \" ) ).ToArray()) ).Padding( 24 ); } } The typed collections ( ListView&#x3C;T> , LazyVStack&#x3C;T> , GridView&#x3C;T> ) handle keying automatically through their keySelector parameter. You only need .WithKey() manually when using ForEach , Select().ToArray() , or other manual list rendering. Rules for good keys: Use a stable identifier from your data (database ID, unique name). Avoid using the array index as a key — it defeats the purpose. Keys must be unique within their sibling list. Duplicates cause undefined reconciliation behavior. Keys should be strings. The WithKey modifier accepts a string. Grouping # Reactor doesn't ship a built-in grouped-list control. The composition recipe is straightforward: group the data with LINQ, then render a VStack of header + items per group. Each group's body is its own typed collection, so virtualization still applies inside a section if you swap ForEach for LazyVStack&#x3C;T> : class GroupingDemo : Component { public override Element Render () { var grouped = SampleData.Contacts .Take( 24 ) .GroupBy(c => c.Name[ 0 ]) .OrderBy(g => g.Key) .ToList(); // Reactor doesn't ship a built-in grouped-list control; instead, // compose a VStack of header + items per group. The render // function for each group hands back its own typed collection, // so virtualization still applies inside each section if you // swap LazyVStack for ListView. return VStack( 8 , SubHeading( $\"Grouped: {grouped.Count} sections\" ), ScrollView( VStack( 16 , ForEach(grouped, group => VStack( 4 , TextBlock( $\"— { group .Key} —\" ).Bold() .Opacity( 0.7 ), ForEach( group .ToArray(), c => HStack( 8 , TextBlock(c.Name).Bold(), TextBlock(c.Email).Opacity( 0.6 )) .WithKey(c.Id)) ).WithKey( $\"group- { group .Key} \" )) ).Padding( 8 ) ).Height( 300 ) ).Padding( 24 ); } } The shape generalizes to two-level grouping (city → country), sticky headers (set Position via a Border modifier), and collapsible sections (wrap each group's body in When(expanded[key], ...) ). Because every group's collection has its own keyed render, items can move between groups across renders without remounting — the keys travel with the items. Drag-to-reorder # WinUI ListView and GridView ship drag-reorder, and Reactor exposes the relevant properties through the .Set passthrough until a first-class fluent ships. Three properties switch the surface on — CanReorderItems , AllowDrop , and CanDragItems — and the list mutates its internal ItemsSource order on drop. Mirror the new order back into your state via the underlying ItemsSource collection or a DragItemsCompleted handler: class DragReorderDemo : Component { public override Element Render () { var (items, setItems) = UseState( new List&#x3C; string > { \"Alpha\" , \"Bravo\" , \"Charlie\" , \"Delta\" , \"Echo\" , \"Foxtrot\" }); // Reactor surfaces drag-reorder through the underlying WinUI // ListView's CanReorderItems / AllowDrop / CanDragItems. The // .Set passthrough is the supported escape hatch until a // first-class fluent ships. The user's reorder is mirrored // back into state via the ListView's reorder event. return VStack( 8 , SubHeading( \"Drag to reorder\" ), ListView&#x3C; string >( items, s => s, (item, _) => HStack( 8 , TextBlock( \"☰\" ).Opacity( 0.4 ), TextBlock(item).Bold() ).Padding( 8 )) .Set(lv => { lv.CanReorderItems = true ; lv.AllowDrop = true ; lv.CanDragItems = true ; }) .Height( 260 ) ).Padding( 24 ); } } Property Effect CanDragItems The user can start a drag from a row. AllowDrop The list accepts drops. CanReorderItems Drops inside the list reorder; drops outside fire DragItemsCompleted . GridView and ItemsView&#x3C;T> expose the same three properties. For free-form drag-and-drop between two lists (move item from A to B), subscribe to DragItemsStarting on the source and Drop on the destination, then update both states. The recipes/drag-reorder recipe walks the single-list case end-to-end. Lazy loading # For data sources where the total count is known but the items are loaded incrementally (paged APIs, large local stores), the onVisibleRangeChanged callback on VirtualList is the load trigger. The callback fires whenever the visible window changes; compare the trailing edge to your high-water mark and request the next page when the user scrolls past it: class LazyLoadingDemo : Component { public override Element Render () { // Pretend \"loaded\" up to a high-water mark; new items fetch // when the visible range crosses into unloaded territory. var (loadedTo, setLoadedTo) = UseState( 50 ); var totalCount = 1 _000; return VStack( 8 , SubHeading( $\"Lazy-load — fetched {loadedTo} of {totalCount} \" ), VirtualList( itemCount: totalCount, renderItem: index => index &#x3C; loadedTo ? HStack( 8 , TextBlock( $\" {index + 1 } .\" ).Width( 50 ), TextBlock( $\"Row {index + 1 } \" ).Bold(), TextBlock( $\"loaded\" ).Opacity( 0.6 )) .Padding( 8 ) // Skeleton for not-yet-loaded indices. : HStack( 8 , TextBlock( $\" {index + 1 } .\" ).Width( 50 ), TextBlock( \"loading…\" ).Opacity( 0.4 )) .Padding( 8 ), getItemKey: index => $\"lazy- {index} \" , itemHeight: 40 , // Watcher fires whenever the visible range changes — // bump the high-water mark when the bottom passes the // current limit. onVisibleRangeChanged: (first, last) => { if (last >= loadedTo - 5 &#x26;&#x26; loadedTo &#x3C; totalCount) setLoadedTo(Math.Min(loadedTo + 50 , totalCount)); } ).Height( 300 ) ).Padding( 24 ); } } Pair this with UseResource to manage the async fetch state — Pending becomes the skeleton row, Loaded becomes the populated row, Error becomes a retry inline. The full shape lives in the recipes/paginated-list recipe. Caveat: itemHeight vs. estimatedItemHeight is the single most expensive decision in VirtualList . With itemHeight set, scrollbar position is O(1) — multiply the index by the height. Without it, the list measures every row that has been seen and maintains a cumulative offset table; the scrollbar approximation drifts and large jumps can cause measure-storms. Set itemHeight whenever your rows are the same fixed height — it is almost always the right choice for paginated data, message lists, and table-shaped UIs. Fall back to estimatedItemHeight only when the row heights genuinely vary (masonry feeds, chat with rich attachments). The default estimatedItemHeight: 40 is a guess; tune it to within ±25% of your real row heights to keep scroll-bar drift under control. Patterns # Virtualized contacts with letter-jump # Combine grouping (section per letter) with VirtualListRef imperative scroll: the user clicks a letter, the list calls ScrollToIndex for the first row in that group. This is the canonical \"A-Z scrubber\" pattern from contacts apps: var listRef = UseRef&#x3C;VirtualListRef?>( null ); var groupStarts = UseMemo(() => ComputeStartIndices(contacts), contacts); return HStack( 0 , VirtualList(contacts.Count, RenderRow, getItemKey: i => contacts[i].Id, itemHeight: 60 , @ref: r => listRef.Current = r).Width( 360 ), VStack( 2 , ForEach( \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\" .ToCharArray(), letter => Button(letter.ToString(), () => listRef.Current?.ScrollToIndex(groupStarts[letter])))) ); Lift state for selection across remounts # Selection state belongs to the parent, never to the collection. The parent owns the HashSet&#x3C;TKey> of selected IDs; the row template checks membership on every render to set IsSelected . This pattern survives data refresh, sort changes, filter changes, and remounts — all of which would lose selection if it lived inside the list. Same shape as form state from forms.md . Common Mistakes # Using array index as key # // Don't: ForEach(items, (item, i) => Row(item).WithKey(i.ToString())) class WithKeyDemo : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C; string > { \"Apple\" , \"Banana\" , \"Cherry\" }); var (newItem, setNewItem) = UseState( \"\" ); return VStack( 12 , SubHeading( \"Stable Identity with WithKey\" ), HStack( 8 , TextBox(newItem, setNewItem, placeholderText: \"New item\" ), Button( \"Add\" , () => { if (! string .IsNullOrWhiteSpace(newItem)) { updateItems(l => [.. l, newItem.Trim()]); setNewItem( \"\" ); } }) ), VStack( 4 , items.Select((item, i) => HStack( 8 , TextBlock(item), Button( \"Remove\" , () => updateItems( l => l.Where((_, idx) => idx != i).ToList())) ).WithKey( $\"item- {item} - {i} \" ) ).ToArray()) ).Padding( 24 ); } } Index keys defeat the purpose of keys. When the list reorders or an item is removed, every subsequent item gets a new key, every row remounts, every text input inside a row loses focus, and animations restart. Use a stable identifier from your data. Not setting itemHeight on a uniform-height VirtualList # // Don't: VirtualList(itemCount, RenderItem, getItemKey: GetKey) // estimatedItemHeight defaults to 40 — drift accumulates for any // row whose actual height differs. class VirtualListDemo : Component { public override Element Render () { return VStack( 12 , SubHeading( \"VirtualList (10,000 items)\" ), VirtualList( itemCount: 10 _000, renderItem: index => HStack( 12 , TextBlock( $\" {index + 1 } .\" ).Width( 50 ), TextBlock( $\"Item {index + 1 } \" ).Bold(), TextBlock( $\"data- {index} @example.com\" ).Opacity( 0.6 ) ).Padding( 8 ), getItemKey: index => $\"item- {index} \" , itemHeight: 40 ).Height( 300 ) ).Padding( 24 ); } } If your rows are all the same height (the common case), tell the list. The O(1) offset math is dramatically faster than the cumulative measure table, and the scrollbar tracks the true position rather than the estimate. Tips # Use keySelector wisely. The key must uniquely identify each item across re-renders. A database ID or GUID is ideal. Avoid index-based keys like i.ToString() — they break when items are reordered or removed. Prefer LazyVStack&#x3C;T> for anything beyond a handful of items. The virtualization overhead is negligible, but the memory savings with large lists are significant. Keep view builders simple. The function you pass to ListView&#x3C;T> runs for every visible item on every render. Extract complex item layouts into their own Component&#x3C;TProps> to get automatic memoization. Use ForEach for inline lists, typed collections for scrollable lists. ForEach does not create a scroll container — it just maps data to elements. For scrollable content, use ListView&#x3C;T> or LazyVStack&#x3C;T> . Remember the index parameter. All view builders receive (T item, int index) . Use the index for display (row numbers) but not for keys. Next Steps # Forms and Input — controlled input controls and validation patterns Navigation — stack-based routing, NavigationView, and tabs Data System — DataGrid with sort, filter, search, and inline editing Flex Layout — wrapping grids and proportional sizing for collection items Components — extract item templates into reusable memoized components",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/collections.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/collections.md",
      "headings": [
        {
          "depth": 2,
          "id": "sample-data",
          "text": "Sample Data"
        },
        {
          "depth": 2,
          "id": "listview",
          "text": "ListView"
        },
        {
          "depth": 3,
          "id": "keyed-reconciliation-in-one-paragraph",
          "text": "Keyed reconciliation, in one paragraph"
        },
        {
          "depth": 3,
          "id": "ireactorkeyed--identity-on-the-data",
          "text": "IReactorKeyed — identity on the data"
        },
        {
          "depth": 3,
          "id": "withkeyitem-for-hand-built-children",
          "text": ".WithKey(item) for hand-built children"
        },
        {
          "depth": 2,
          "id": "lazyvstack-virtualized",
          "text": "LazyVStack (Virtualized)"
        },
        {
          "depth": 2,
          "id": "gridview",
          "text": "GridView"
        },
        {
          "depth": 2,
          "id": "virtuallist-count-based",
          "text": "VirtualList (Count-Based)"
        },
        {
          "depth": 2,
          "id": "foreach",
          "text": "ForEach"
        },
        {
          "depth": 2,
          "id": "multi-select-with-selectionchanged",
          "text": "Multi-Select with SelectionChanged"
        },
        {
          "depth": 2,
          "id": "stable-identity-with-withkey",
          "text": "Stable Identity with WithKey"
        },
        {
          "depth": 2,
          "id": "grouping",
          "text": "Grouping"
        },
        {
          "depth": 2,
          "id": "drag-to-reorder",
          "text": "Drag-to-reorder"
        },
        {
          "depth": 2,
          "id": "lazy-loading",
          "text": "Lazy loading"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "virtualized-contacts-with-letter-jump",
          "text": "Virtualized contacts with letter-jump"
        },
        {
          "depth": 3,
          "id": "lift-state-for-selection-across-remounts",
          "text": "Lift state for selection across remounts"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "using-array-index-as-key",
          "text": "Using array index as key"
        },
        {
          "depth": 3,
          "id": "not-setting-itemheight-on-a-uniform-height-virtuallist",
          "text": "Not setting itemHeight on a uniform-height VirtualList"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Text and Media",
      "path": "text-and-media.md",
      "slug": "/text-and-media/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/text-controls\">Text Controls</a>.</p>\n</blockquote>\n<p>Text and media controls are the read-only-display half of the Microsoft.UI.Reactor (Reactor)\ncatalog: surfaces that show content the user is reading, watching, or\ninspecting rather than editing. Most of them are thin wrappers over the\nWinUI text and media surface, so the modifier names and accessibility\nbehavior match what a WinUI developer already knows. The two outliers are\n<a href=\"#markdown-reactor-original\"><code>Markdown(string)</code></a> — a Reactor-original\nrenderer that parses GFM-style Markdown into the same element tree the\nrest of your UI uses, no WebView round trip — and the semantic text\nvariants <a href=\"#text-variants\"><code>Heading</code></a> / <a href=\"#text-variants\"><code>SubHeading</code></a> /\n<a href=\"#text-variants\"><code>Caption</code></a>, which preset typography so that accessibility\ntooling can infer document outline without manually setting\n<code>AutomationProperties</code>. The trade-off the catalog makes here is bias\ntoward composition: rich layouts come from many small text elements in a\n<a href=\"/layout/\"><code>VStack</code></a>, not from one giant <code>RichTextBlock</code> carrying\nmanually-tuned <code>Inline</code> records. Skim the modifier tables before the\nprose, then jump to the control you need.</p>\n<h1 id=\"text-and-media\">Text and Media<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#text-and-media\">#</a></h1>\n<p>This page covers every read-only-display and inline-rich-text control in\nReactor. For input controls (<code>TextBox</code>, <code>PasswordBox</code>, <code>RichEditBox</code>),\nsee <a href=\"/forms/\">Forms</a>. For data-bound collections, see\n<a href=\"/collections/\">Collections</a>.</p>\n<h2 id=\"text-variants\">Text variants<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#text-variants\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">Heading(<span class=\"hljs-built_in\">string</span>)         <span class=\"hljs-comment\">// section title, ~28pt</span>\nSubHeading(<span class=\"hljs-built_in\">string</span>)      <span class=\"hljs-comment\">// sub-section header, ~20pt</span>\nTextBlock(<span class=\"hljs-built_in\">string</span>)       <span class=\"hljs-comment\">// body prose</span>\nCaption(<span class=\"hljs-built_in\">string</span>)         <span class=\"hljs-comment\">// ~12pt, dimmed</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TextVariantsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        Heading(<span class=\"hljs-string\">\"Heading — page or section title\"</span>),\n        SubHeading(<span class=\"hljs-string\">\"SubHeading — region header\"</span>),\n        TextBlock(<span class=\"hljs-string\">\"Body text. The default size and weight for prose.\"</span>),\n        Caption(<span class=\"hljs-string\">\"Caption — secondary metadata, dates, labels.\"</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/text-variants.png\" alt=\"Heading / SubHeading / TextBlock / Caption stacked\"></p>\n<p><code>Heading</code>, <code>SubHeading</code>, and <code>Caption</code> return <a href=\"/components/\"><code>TextBlockElement</code></a>\nwith preset <code>FontSize</code>, weight, and <code>Foreground</code> from the active theme.\nThey are the right tool for document outline — screen readers and the\n<a href=\"/accessibility/\">accessibility scanner</a> treat them as hierarchical\nlandmarks, where a bare <code>TextBlock</code> styled with <code>.FontSize(24).Bold()</code> is\njust visually large. Reach for the variants first; reach for the\nmodifiers on <code>TextBlock</code> only when the position in the outline doesn't\nmatch the visual weight you want.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Factory</th><th>Default size</th><th>Use when</th></tr></thead><tbody><tr><td><code>Heading</code></td><td>~28pt, bold</td><td>One per page — the document title.</td></tr><tr><td><code>SubHeading</code></td><td>~20pt, semibold</td><td>Section header inside a long page.</td></tr><tr><td><code>TextBlock</code></td><td>Body</td><td>Paragraphs, labels, inline help.</td></tr><tr><td><code>Caption</code></td><td>~12pt, dimmed</td><td>Timestamps, metadata, helper text under a field.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/style/typography\">Typography in Windows 11</a>.</p>\n<h2 id=\"textblock-modifiers\">TextBlock modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#textblock-modifiers\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TextBlockModifiersDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">\"Bold + sized\"</span>).Bold().FontSize(<span class=\"hljs-number\">18</span>),\n        TextBlock(<span class=\"hljs-string\">\"Selectable so the user can copy.\"</span>).IsTextSelectionEnabled(),\n        TextBlock(\n            <span class=\"hljs-string\">\"A long paragraph that demonstrates wrapping behavior. \"</span> +\n            <span class=\"hljs-string\">\"Without TextWrapping, content stays on one line and is \"</span> +\n            <span class=\"hljs-string\">\"clipped or scrolls. With TextWrapping.Wrap, the block \"</span> +\n            <span class=\"hljs-string\">\"flows across multiple lines inside its width.\"</span>)\n            .TextWrapping()\n            .MaxLines(<span class=\"hljs-number\">2</span>)\n            .TextTrimming(Microsoft.UI.Xaml.TextTrimming.WordEllipsis)\n            .Width(<span class=\"hljs-number\">320</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/textblock-modifiers.png\" alt=\"TextBlock with bold/size, selectable, wrapped + trimmed variants\"></p>\n<p>The fluents you reach for most often:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Bold()</code> / <code>.SemiBold()</code></td><td>Sets <code>FontWeight</code>. Use <code>Heading</code> instead if it's a section title.</td></tr><tr><td><code>.FontSize(double)</code></td><td>Override the variant default.</td></tr><tr><td><code>.FontFamily(string)</code></td><td>Family name or <code>Microsoft.UI.Xaml.Media.FontFamily</code>.</td></tr><tr><td><code>.TextWrapping()</code></td><td>Default is no-wrap; call this to flow across lines.</td></tr><tr><td><code>.MaxLines(int)</code></td><td>Caps the visible line count — pairs with <code>TextTrimming</code>.</td></tr><tr><td><code>.TextTrimming(mode)</code></td><td><code>CharacterEllipsis</code> / <code>WordEllipsis</code> / <code>Clip</code> / <code>None</code>.</td></tr><tr><td><code>.TextAlignment(alignment)</code></td><td><code>Left</code> / <code>Right</code> / <code>Center</code> / <code>Justify</code>.</td></tr><tr><td><code>.LineHeight(double)</code></td><td>Override the line-box height (useful for dense lists).</td></tr><tr><td><code>.IsTextSelectionEnabled()</code></td><td>Lets the user select and copy the text.</td></tr><tr><td><code>.CharacterSpacing(int)</code></td><td>Hundredths of an em — <code>30</code> ≈ 0.3em tracking.</td></tr></tbody></table>\n<blockquote>\n<p><strong>Caveat:</strong> <code>TextBlock</code> has a fast-path renderer that activates only when you set the\n<code>Text</code> via the factory string overload (<code>TextBlock(\"…\")</code>). The moment you\nswitch to inlines via <code>RichTextBlock</code>, change <code>CharacterSpacing</code> to a\nnon-zero value, or set <code>TextTrimming</code> to <code>Clip</code>, layout falls back to the\nslow path and CPU cost per measure roughly doubles. For thousand-row\nlists this matters; for static prose it does not. The WinUI debug\nproperty <code>IsTextPerformanceVisualizationEnabled</code> highlights fast-path\ntext in green — turn it on when profiling a scroll-heavy screen and\ndemote any controls that aren't green to plain <code>TextBlock</code> with\nmodifiers.</p>\n</blockquote>\n<h2 id=\"richtextblock\">RichTextBlock<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#richtextblock\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">RichTextBlock(<span class=\"hljs-built_in\">string</span> text)\nRichTextBlock(RichTextParagraph[] paragraphs)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RichTextDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"Inline-formatted prose\"</span>),\n        RichTextBlock([\n            Paragraph(\n                Run(<span class=\"hljs-string\">\"Tap the \"</span>),\n                Hyperlink(<span class=\"hljs-string\">\"docs\"</span>,\n                    <span class=\"hljs-keyword\">new</span> Uri(<span class=\"hljs-string\">\"https://learn.microsoft.com/windows/apps/\"</span>)),\n                Run(<span class=\"hljs-string\">\" to keep reading.\"</span>)),\n            Paragraph(\n                Run(<span class=\"hljs-string\">\"Reactor builds the paragraph tree from value-typed \"</span> +\n                    <span class=\"hljs-string\">\"records. No XAML inlines, no DataTemplate.\"</span>))\n        ]).LineHeight(<span class=\"hljs-number\">22</span>).Width(<span class=\"hljs-number\">420</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/rich-text.png\" alt=\"RichTextBlock with an inline hyperlink across two paragraphs\"></p>\n<p><code>RichTextBlock</code> is for paragraphs that mix runs and inline elements —\nhyperlinks, mid-sentence bolds, color swaps. Use it sparingly: a list of\nfields lives better in <a href=\"/forms/\"><code>Forms</code></a>, and a multi-screen article\nlives better in <a href=\"#markdown-reactor-original\"><code>Markdown(string)</code></a>.\n<code>RichTextBlock</code> is the right tool when the structure is dynamic (a chat\nmessage with @-mentions, a search result with bolded query terms)\nbecause you build the <code>Paragraph[]</code> from data per render.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Helper</th><th>Use</th></tr></thead><tbody><tr><td><code>Paragraph(params RichTextInline[])</code></td><td>A paragraph; one per visual break.</td></tr><tr><td><code>Run(string text)</code></td><td>A plain text fragment.</td></tr><tr><td><code>Hyperlink(string text, Uri target)</code></td><td>Inline link with a navigate target.</td></tr></tbody></table>\n<p>Modifiers — <code>.MaxLines</code>, <code>.LineHeight</code>, <code>.TextAlignment</code>,\n<code>.TextTrimming</code>, <code>.CharacterSpacing</code> — match <code>TextBlock</code>. Inline runs do\nnot get the fast-path renderer, so prefer plain <code>TextBlock</code> for static\ntext.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/rich-text-block\">Rich text block</a>.</p>\n<h2 id=\"richeditbox\">RichEditBox<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#richeditbox\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">RichEditBox(<span class=\"hljs-built_in\">string</span> text = <span class=\"hljs-string\">\"\"</span>, Action&#x3C;<span class=\"hljs-built_in\">string</span>>? onTextChanged = <span class=\"hljs-literal\">null</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RichEditDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(\n            <span class=\"hljs-string\">\"Edit me. RichEditBox supports paste-with-formatting, \"</span> +\n            <span class=\"hljs-string\">\"spell-check, and Enter for new paragraphs.\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"RichEditBox\"</span>),\n            RichEditBox(text, setText)\n                .AcceptsReturn()\n                .IsSpellCheckEnabled()\n                .TextWrapping()\n                .Height(<span class=\"hljs-number\">160</span>).Width(<span class=\"hljs-number\">420</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/rich-edit.png\" alt=\"RichEditBox sized for paragraph editing\"></p>\n<p><code>RichEditBox</code> is the editable counterpart of <code>RichTextBlock</code> — multi-line\ntext with paste-from-formatted-source, spell-check, IME composition, and\nselection. The change handler fires with the plain text content; for\nformatted output you read <code>RichEditBox.Document</code> through <code>.Set(...)</code> and\nserialize the text range yourself. Single-line text input belongs in\n<a href=\"/forms/\"><code>TextBox</code></a>, not here.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.AcceptsReturn(bool)</code></td><td>Enter starts a new paragraph instead of submitting.</td></tr><tr><td><code>.TextWrapping()</code></td><td>Wrap (default) or NoWrap.</td></tr><tr><td><code>.MaxLength(int)</code></td><td>Cap input length.</td></tr><tr><td><code>.IsSpellCheckEnabled(bool)</code></td><td>Toggle squiggle underline.</td></tr><tr><td><code>.SelectionHighlightColor(brush)</code></td><td>Override selection background.</td></tr><tr><td><code>.TextChanged(Action&#x3C;string>)</code></td><td>Subscribe outside the constructor argument.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/rich-edit-box\">Rich edit box</a>.</p>\n<h2 id=\"markdown-reactor-original\">Markdown (Reactor-original)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#markdown-reactor-original\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">Markdown(<span class=\"hljs-built_in\">string</span> source)\nMarkdown(<span class=\"hljs-built_in\">string</span> source, MarkdownOptions options)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MarkdownDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">const</span> <span class=\"hljs-built_in\">string</span> source =\n            <span class=\"hljs-string\">\"# Release notes\\n\\n\"</span> +\n            <span class=\"hljs-string\">\"Reactor **0.42** ships:\\n\\n\"</span> +\n            <span class=\"hljs-string\">\"- Compositor animations via `UseAnimation`.\\n\"</span> +\n            <span class=\"hljs-string\">\"- A new [Markdown](https://example.com) renderer.\\n\"</span> +\n            <span class=\"hljs-string\">\"- Bug fixes for `LazyVStack` keyed reorder.\\n\\n\"</span> +\n            <span class=\"hljs-string\">\"> Migration guide lives in the spec.\\n\"</span>;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Markdown\"</span>),\n            Markdown(source)\n        ).Padding(<span class=\"hljs-number\">24</span>).Width(<span class=\"hljs-number\">440</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/markdown.png\" alt=\"Markdown rendering — heading, list, link, blockquote\"></p>\n<p><code>Markdown</code> is the largest Reactor-original control on this page. It\nparses GitHub-flavored Markdown with the embedded <code>md4c</code> parser and\nemits a Reactor element tree: headings become <code>TextBlock</code> with the\nheading variant, list items become <code>HStack</code>s, links become inline\nhyperlinks, code spans become monospace <code>TextBlock</code>. No WebView, no HTML\nround-trip — the output composes with every other modifier on this page\n(<code>.Padding</code>, <code>.Width</code>, <code>.TextWrapping</code>).</p>\n<p>The trade-off vs. <a href=\"#webview2\">WebView2</a> is fidelity: <code>Markdown</code> does not\nsupport arbitrary HTML, embedded <code>&#x3C;script></code>, or CSS. It supports paragraphs,\nheadings (h1–h6), lists (bullet, ordered, nested), inline code, fenced code\nblocks with optional language, links, images, bold, italic, strikethrough,\nblockquotes, hard line breaks, and tables. Anything beyond that — embedded\nvideos, custom layouts, syntax-highlighted code with theming — falls back to\nWebView2 with an HTML-rendered fallback or a separate viewer.</p>\n<p>Reach for it for release notes, in-app help, conversational AI output,\nuser-authored long-form prose (commit descriptions, knowledge-base\narticles), or any surface where the source is plain text and a thin\nformatting layer is what the reader needs.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Use Markdown when</th><th>Use RichTextBlock when</th></tr></thead><tbody><tr><td>Source is authored as a string (LLM output, README, user comment)</td><td>Inline structure is computed from typed data</td></tr><tr><td>You need lists, code blocks, blockquotes</td><td>Plain paragraphs with mid-line formatting suffice</td></tr><tr><td>The full GFM subset is acceptable</td><td>You need arbitrary <code>Inline</code> element types</td></tr><tr><td>Round-trip from <code>.md</code> files</td><td>Round-trip from a <code>RichTextParagraph[]</code> schema</td></tr></tbody></table>\n<p>No WinUI parallel: WinUI ships a <code>MarkdownTextBlock</code> in the Community\nToolkit, but the API surface and behavior differ. The Reactor factory is\nthe canonical reference for this control.</p>\n<h2 id=\"image\">Image<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#image\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">Image(<span class=\"hljs-built_in\">string</span> source)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ImageDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"Image\"</span>),\n        <span class=\"hljs-comment\">// Resource Uri — ms-appx:// for packaged assets, file:// for disk,</span>\n        <span class=\"hljs-comment\">// https:// for remote.</span>\n        Image(<span class=\"hljs-string\">\"ms-appx:///Assets/StoreLogo.png\"</span>)\n            .Width(<span class=\"hljs-number\">96</span>).Height(<span class=\"hljs-number\">96</span>),\n        TextBlock(<span class=\"hljs-string\">\"Stretch.UniformToFill for cover art; \"</span> +\n                  <span class=\"hljs-string\">\"ImageFailed to detect missing assets.\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/image.png\" alt=\"Image control rendering a packaged 96x96 asset\"></p>\n<p><code>Image</code> accepts the standard WinUI URI schemes — <code>ms-appx:///</code> for\nassets packaged with the app, <code>ms-appdata:///</code> for app-data files,\n<code>file:///</code> for arbitrary disk paths, and <code>http(s)://</code> for remote\nsources. The factory delegates source decoding to the WinUI\n<code>BitmapImage</code> — same caching, same DPI awareness.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Width(double)</code> / <code>.Height(double)</code></td><td>Layout size. Without one of these the image takes its natural pixel size.</td></tr><tr><td><code>.NineGrid(Thickness)</code></td><td>Stretch the middle, preserve the borders (chrome-style backgrounds).</td></tr><tr><td><code>.Set(img => img.Stretch = ...)</code></td><td>Stretch mode: <code>None</code> / <code>Uniform</code> (default) / <code>UniformToFill</code> / <code>Fill</code>.</td></tr><tr><td><code>.ImageOpened(Action)</code></td><td>Decode succeeded.</td></tr><tr><td><code>.ImageFailed(Action&#x3C;string>)</code></td><td>Decode failed — fire to swap in a fallback.</td></tr></tbody></table>\n<blockquote>\n<p><strong>Don't:</strong> decode large remote images on the UI thread by setting\n<code>Source</code> to a full-size <code>http://</code> URL inside a tight list-item\nrender. The WinUI decoder spawns work off the UI thread, but the\nnetwork fetch still blocks the visual tree from drawing the row\nuntil the bytes arrive. For lazy-loaded media inside a virtualized\nlist, route through a <a href=\"/async-resources/\"><code>UseResource</code></a> that\nfetches a downscaled blob and only render <code>Image</code> once <code>Pending</code> is\nresolved.</p>\n</blockquote>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/images-imagebrushes\">Images and image brushes</a>.</p>\n<h2 id=\"mediaplayerelement\">MediaPlayerElement<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mediaplayerelement\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">MediaPlayerElement(<span class=\"hljs-built_in\">string</span>? source = <span class=\"hljs-literal\">null</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MediaPlayerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"MediaPlayerElement\"</span>),\n        MediaPlayerElement(\n            <span class=\"hljs-string\">\"https://learn.microsoft.com/en-us/windows/apps/design/\"</span> +\n            <span class=\"hljs-string\">\"controls/images/ic_fluent_play_24_regular.svg\"</span>)\n            .Width(<span class=\"hljs-number\">420</span>).Height(<span class=\"hljs-number\">240</span>)\n            .Set(m =>\n            {\n                m.AreTransportControlsEnabled = <span class=\"hljs-literal\">true</span>;\n                m.AutoPlay = <span class=\"hljs-literal\">false</span>;\n            }),\n        TextBlock(<span class=\"hljs-string\">\"Use AreTransportControlsEnabled for play/pause UI.\"</span>)\n            .Opacity(<span class=\"hljs-number\">0.6</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/media-player.png\" alt=\"MediaPlayerElement with transport controls\"></p>\n<p><code>MediaPlayerElement</code> wraps the WinUI <code>MediaPlayerElement</code>, which itself\nhosts a <code>MediaPlayer</code> that drives audio and video playback. The factory\naccepts a string URL for the common case; for stream sources, set the\nplayer through <code>.Set(m => m.MediaPlayer.Source = ...)</code>.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Knob</th><th>Set via</th></tr></thead><tbody><tr><td><code>AreTransportControlsEnabled</code></td><td><code>.Set(m => m.AreTransportControlsEnabled = true)</code></td></tr><tr><td><code>AutoPlay</code></td><td><code>.Set(m => m.AutoPlay = false)</code></td></tr><tr><td><code>IsFullWindow</code></td><td><code>.Set(m => m.IsFullWindow = true)</code></td></tr><tr><td><code>MediaOpened</code> / <code>MediaEnded</code> / <code>MediaFailed</code></td><td>Dedicated fluent overloads</td></tr></tbody></table>\n<blockquote>\n<p><strong>Don't:</strong> mount and unmount <code>MediaPlayerElement</code> on every render to\ndrive playback. The WinUI control owns an underlying <code>MediaPlayer</code>\nthat re-initializes hardware decoders on each remount; thrash kills\nplayback smoothness. Render the element once with a stable position\nin the tree and drive <code>Source</code> / play state through a hook\n(<a href=\"/hooks/\"><code>UseRef</code></a> holding the player), not through unmounts.</p>\n</blockquote>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/media-playback\">Media player</a>.</p>\n<h2 id=\"webview2\">WebView2<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#webview2\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">WebView2(Uri? source = <span class=\"hljs-literal\">null</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WebViewDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (loaded, setLoaded) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"WebView2\"</span>),\n            WebView2(<span class=\"hljs-keyword\">new</span> Uri(<span class=\"hljs-string\">\"about:blank\"</span>))\n                .NavigationCompleted(_ => setLoaded(<span class=\"hljs-literal\">true</span>))\n                .Width(<span class=\"hljs-number\">420</span>).Height(<span class=\"hljs-number\">240</span>),\n            TextBlock(loaded ? <span class=\"hljs-string\">\"Loaded.\"</span> : <span class=\"hljs-string\">\"Loading…\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/webview.png\" alt=\"WebView2 hosting about:blank\"></p>\n<p><code>WebView2</code> embeds a Chromium-based browser. Use it for content that\ngenuinely needs HTML/CSS/JavaScript — a markdown page does not, an\nembedded Office viewer does. The control raises lifecycle events for\nnavigation and exposes a <code>WebMessageReceived</code> channel for postMessage\ninterop.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Event fluent</th><th>Fires when</th></tr></thead><tbody><tr><td><code>.NavigationStarting(Action&#x3C;Uri>)</code></td><td>A navigation begins — cancel via <code>.Set</code>.</td></tr><tr><td><code>.NavigationCompleted(Action&#x3C;Uri>)</code></td><td>Page finished loading.</td></tr><tr><td><code>.WebMessageReceived(Action&#x3C;string>)</code></td><td>Page called <code>window.chrome.webview.postMessage(...)</code>.</td></tr><tr><td><code>.CoreWebView2Initialized(Action)</code></td><td>The underlying CoreWebView2 is ready — configure it via <code>.Set(w => w.CoreWebView2....)</code>.</td></tr></tbody></table>\n<blockquote>\n<p><strong>Don't:</strong> lay out <code>WebView2</code> inside an indeterminate-sized parent\nlike an <code>HStack</code> without explicit dimensions. WebView2 measures to\nits content, which for a real web page is the viewport — without\nbounds it grows to fill the available size and triggers a layout\noscillation when the page reflows. Always pin <code>.Width</code> and <code>.Height</code>\n(or place the control in a fixed-size <code>Grid</code> cell).</p>\n</blockquote>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution\">WebView2</a>.</p>\n<h2 id=\"mapcontrol\">MapControl<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mapcontrol\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">MapControl(<span class=\"hljs-built_in\">string</span>? mapServiceToken = <span class=\"hljs-literal\">null</span>, <span class=\"hljs-built_in\">double</span> zoomLevel = <span class=\"hljs-number\">1</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MapControlDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"MapControl\"</span>),\n        <span class=\"hljs-comment\">// Token blank — replace with a real Bing Maps key for tile fetch.</span>\n        <span class=\"hljs-comment\">// Without a token the control renders the grid background only.</span>\n        MapControl(mapServiceToken: <span class=\"hljs-literal\">null</span>, zoomLevel: <span class=\"hljs-number\">4</span>)\n            .Width(<span class=\"hljs-number\">420</span>).Height(<span class=\"hljs-number\">240</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/text-and-media/map-control.png\" alt=\"MapControl at zoom level 4\"></p>\n<p><code>MapControl</code> wraps <code>Microsoft.UI.Xaml.Controls.Maps.MapControl</code>. The\nservice token comes from the Bing Maps developer portal — without it,\nthe grid background renders but no tiles fetch. For full customization\n(pushpins, overlays, scenes), reach through <code>.Set(m => ...)</code> to the\nunderlying control; the Reactor surface today exposes the factory\narguments and direct passthrough.</p>\n<blockquote>\n<p><strong>Don't:</strong> ship <code>MapControl</code> to an offline-tolerant app without a\nfallback. Tile fetch failures don't throw — the control just shows\nthe grid. Subscribe to the underlying <code>MapControl.MapServiceErrorOccurred</code>\nthrough <code>.Set</code> and swap in an <code>Image</code> of a static map (or a \"map\nunavailable\" panel) when offline.</p>\n</blockquote>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/develop/maps/\">Map control</a> (Windows App SDK).</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Factory</th><th>Reactor-original?</th><th>WinUI doc</th></tr></thead><tbody><tr><td><code>TextBlock</code></td><td><code>TextBlock(string)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/text-block\">Text block</a></td></tr><tr><td><code>Heading</code> / <code>SubHeading</code> / <code>Caption</code></td><td><code>Heading(string)</code> etc.</td><td>Variant presets</td><td>—</td></tr><tr><td><code>RichTextBlock</code></td><td><code>RichTextBlock(string)</code> or <code>RichTextBlock(RichTextParagraph[])</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/rich-text-block\">Rich text block</a></td></tr><tr><td><code>RichEditBox</code></td><td><code>RichEditBox(text, onChanged)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/rich-edit-box\">Rich edit box</a></td></tr><tr><td><code>Markdown</code></td><td><code>Markdown(string)</code></td><td><strong>Yes</strong></td><td>—</td></tr><tr><td><code>Image</code></td><td><code>Image(string)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/images-imagebrushes\">Images and image brushes</a></td></tr><tr><td><code>MediaPlayerElement</code></td><td><code>MediaPlayerElement(string?)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/media-playback\">Media player</a></td></tr><tr><td><code>WebView2</code></td><td><code>WebView2(Uri?)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/microsoft-edge/webview2/\">WebView2</a></td></tr><tr><td><code>MapControl</code></td><td><code>MapControl(token, zoom)</code></td><td>No</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/develop/maps/\">Map control</a></td></tr><tr><td><code>InkCanvas</code></td><td><em>Not yet wrapped</em></td><td>—</td><td><a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/inking-controls\">Ink controls</a></td></tr></tbody></table>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"long-form-prose-with-markdown\">Long-form prose with Markdown<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#long-form-prose-with-markdown\">#</a></h3>\n<p>The release-notes / changelog pattern: parse a string of GFM into the\nelement tree, then style the container — not the inner elements. Pad,\nconstrain width to ~640 logical pixels for readability, and let the\nMarkdown renderer hand back the hierarchy. Wrap the call in a\n<a href=\"/hooks/\"><code>UseMemo</code></a> keyed on the source string when the renderer is\ninside a frequently-rendered parent; the parser is cheap but allocates,\nand memoization keeps GC pressure flat:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> rendered = UseMemo(() => Markdown(source), source);\n<span class=\"hljs-keyword\">return</span> Border(rendered).Padding(<span class=\"hljs-number\">20</span>).Width(<span class=\"hljs-number\">640</span>);\n</code></pre>\n<p>This is the same shape as the <a href=\"/recipes/master-detail/\"><code>recipes/master-detail</code></a>\ncontent pane and the <a href=\"https://github.com/microsoft/reactor1/tree/main/samples/apps/chat\">chat sample</a>\nmessage bubble.</p>\n<h3 id=\"inline-data-display-with-richtextblock\">Inline data display with RichTextBlock<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#inline-data-display-with-richtextblock\">#</a></h3>\n<p>When a list-row needs <code>Name (status) — 3 hours ago</code> with the status\nbold and the timestamp dim, build a <code>RichTextBlock</code> per row out of the\ntyped data. Keep the paragraph construction inline in the row\n<a href=\"/components/\"><code>Component</code></a> so it re-runs only when the row's data\nchanges; <code>RichTextBlock</code> is not the bottleneck for moderate row counts\nbut the inline-element allocation can show up in profiles when a\n<a href=\"/collections/\"><code>LazyVStack</code></a> scrolls thousands of rows at 60fps.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"using-textblock-for-everything\">Using <code>TextBlock</code> for everything<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-textblock-for-everything\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nVStack(<span class=\"hljs-number\">8</span>,\n    TextBlock(<span class=\"hljs-string\">\"Settings\"</span>).FontSize(<span class=\"hljs-number\">28</span>).Bold(),\n    TextBlock(<span class=\"hljs-string\">\"Display\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n    TextBlock(<span class=\"hljs-string\">\"Adjust resolution and orientation.\"</span>)\n)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TextVariantsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        Heading(<span class=\"hljs-string\">\"Heading — page or section title\"</span>),\n        SubHeading(<span class=\"hljs-string\">\"SubHeading — region header\"</span>),\n        TextBlock(<span class=\"hljs-string\">\"Body text. The default size and weight for prose.\"</span>),\n        Caption(<span class=\"hljs-string\">\"Caption — secondary metadata, dates, labels.\"</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p>The \"don't\" form ships visual hierarchy but no semantic hierarchy —\nthe accessibility scanner reports no headings and screen-reader users\nget no document outline. The correct form uses the <code>Heading</code> /\n<code>SubHeading</code> / <code>TextBlock</code> factories so <a href=\"/accessibility/\"><code>accessibility.md</code></a>\nlandmark detection works.</p>\n<h3 id=\"rendering-markdown-inside-a-tight-scroll-loop-without-memoizing\">Rendering Markdown inside a tight scroll loop without memoizing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rendering-markdown-inside-a-tight-scroll-loop-without-memoizing\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nLazyVStack&#x3C;Message>(messages, m => m.Id, (m, i) =>\n    Markdown(m.Body))  <span class=\"hljs-comment\">// re-parses on every scroll-induced re-render</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Do:</span>\nLazyVStack&#x3C;Message>(messages, m => m.Id, (m, i) =>\n    Memo(_ => Markdown(m.Body), m.Body))\n</code></pre>\n<p>Markdown parsing is fast but not free — at 60fps × hundreds of visible\nrows × every state change, parser allocation shows up in GC. The\n<a href=\"/components/\"><code>Memo</code></a> wrapper keys on the source string so unchanged\nrows skip the parser entirely.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pick the smallest control that does the job.</strong> <code>TextBlock</code> >\n<code>RichTextBlock</code> > <code>Markdown</code> > <code>WebView2</code> in increasing layout cost.\nPromote only when the lower tier can't represent the structure.</p>\n<p><strong>Use <code>Selectable()</code> on any text the user might want to copy.</strong> Error\nmessages, IDs, paths, log lines, command output — all of them are more\nuseful when the user can drag-select and Ctrl+C. The cost is one\nmodifier per <code>TextBlock</code>.</p>\n<p><strong>Pin dimensions on media controls.</strong> <code>Image</code>, <code>MediaPlayerElement</code>,\n<code>WebView2</code>, and <code>MapControl</code> all default to \"fill available space\" or\n\"natural content size\", which interacts badly with <code>VStack</code>/<code>HStack</code>\nauto-layout. Set <code>.Width</code> and <code>.Height</code> (or place them in a fixed\n<a href=\"/layout/\"><code>Grid</code></a> cell).</p>\n<p><strong><code>Markdown</code> is the right choice for LLM output.</strong> Treat AI-generated\ntext as a markdown stream — parse it, render it, theme the container.\nThe renderer composes with the rest of the catalog and stays accessible.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — Previous: editable input controls and the validation system.</li>\n<li><strong><a href=\"/status-and-info/\">Status and Info</a></strong> — Next: ProgressBar, InfoBar, badges, and non-interactive feedback.</li>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — Theme tokens and font fluents applied across every text element.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — How <code>Heading</code> / <code>SubHeading</code> map to document landmarks.</li>\n<li><strong><a href=\"https://github.com/microsoft/reactor1/tree/main/samples/apps/chat\">Markdown samples</a></strong> — End-to-end LLM-output rendering in the chat sample.</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Text Controls . Text and media controls are the read-only-display half of the Microsoft.UI.Reactor (Reactor) catalog: surfaces that show content the user is reading, watching, or inspecting rather than editing. Most of them are thin wrappers over the WinUI text and media surface, so the modifier names and accessibility behavior match what a WinUI developer already knows. The two outliers are Markdown(string) — a Reactor-original renderer that parses GFM-style Markdown into the same element tree the rest of your UI uses, no WebView round trip — and the semantic text variants Heading / SubHeading / Caption , which preset typography so that accessibility tooling can infer document outline without manually setting AutomationProperties . The trade-off the catalog makes here is bias toward composition: rich layouts come from many small text elements in a VStack , not from one giant RichTextBlock carrying manually-tuned Inline records. Skim the modifier tables before the prose, then jump to the control you need. Text and Media # This page covers every read-only-display and inline-rich-text control in Reactor. For input controls ( TextBox , PasswordBox , RichEditBox ), see Forms . For data-bound collections, see Collections . Text variants # Heading( string ) // section title, ~28pt SubHeading( string ) // sub-section header, ~20pt TextBlock( string ) // body prose Caption( string ) // ~12pt, dimmed class TextVariantsDemo : Component { public override Element Render () => VStack( 8 , Heading( \"Heading — page or section title\" ), SubHeading( \"SubHeading — region header\" ), TextBlock( \"Body text. The default size and weight for prose.\" ), Caption( \"Caption — secondary metadata, dates, labels.\" ) ).Padding( 24 ); } Heading , SubHeading , and Caption return TextBlockElement with preset FontSize , weight, and Foreground from the active theme. They are the right tool for document outline — screen readers and the accessibility scanner treat them as hierarchical landmarks, where a bare TextBlock styled with .FontSize(24).Bold() is just visually large. Reach for the variants first; reach for the modifiers on TextBlock only when the position in the outline doesn't match the visual weight you want. Factory Default size Use when Heading ~28pt, bold One per page — the document title. SubHeading ~20pt, semibold Section header inside a long page. TextBlock Body Paragraphs, labels, inline help. Caption ~12pt, dimmed Timestamps, metadata, helper text under a field. WinUI design page: Typography in Windows 11 . TextBlock modifiers # class TextBlockModifiersDemo : Component { public override Element Render () => VStack( 8 , TextBlock( \"Bold + sized\" ).Bold().FontSize( 18 ), TextBlock( \"Selectable so the user can copy.\" ).IsTextSelectionEnabled(), TextBlock( \"A long paragraph that demonstrates wrapping behavior. \" + \"Without TextWrapping, content stays on one line and is \" + \"clipped or scrolls. With TextWrapping.Wrap, the block \" + \"flows across multiple lines inside its width.\" ) .TextWrapping() .MaxLines( 2 ) .TextTrimming(Microsoft.UI.Xaml.TextTrimming.WordEllipsis) .Width( 320 ) ).Padding( 24 ); } The fluents you reach for most often: Fluent Effect .Bold() / .SemiBold() Sets FontWeight . Use Heading instead if it's a section title. .FontSize(double) Override the variant default. .FontFamily(string) Family name or Microsoft.UI.Xaml.Media.FontFamily . .TextWrapping() Default is no-wrap; call this to flow across lines. .MaxLines(int) Caps the visible line count — pairs with TextTrimming . .TextTrimming(mode) CharacterEllipsis / WordEllipsis / Clip / None . .TextAlignment(alignment) Left / Right / Center / Justify . .LineHeight(double) Override the line-box height (useful for dense lists). .IsTextSelectionEnabled() Lets the user select and copy the text. .CharacterSpacing(int) Hundredths of an em — 30 ≈ 0.3em tracking. Caveat: TextBlock has a fast-path renderer that activates only when you set the Text via the factory string overload ( TextBlock(\"…\") ). The moment you switch to inlines via RichTextBlock , change CharacterSpacing to a non-zero value, or set TextTrimming to Clip , layout falls back to the slow path and CPU cost per measure roughly doubles. For thousand-row lists this matters; for static prose it does not. The WinUI debug property IsTextPerformanceVisualizationEnabled highlights fast-path text in green — turn it on when profiling a scroll-heavy screen and demote any controls that aren't green to plain TextBlock with modifiers. RichTextBlock # RichTextBlock( string text) RichTextBlock(RichTextParagraph[] paragraphs) class RichTextDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"Inline-formatted prose\" ), RichTextBlock([ Paragraph( Run( \"Tap the \" ), Hyperlink( \"docs\" , new Uri( \"https://learn.microsoft.com/windows/apps/\" )), Run( \" to keep reading.\" )), Paragraph( Run( \"Reactor builds the paragraph tree from value-typed \" + \"records. No XAML inlines, no DataTemplate.\" )) ]).LineHeight( 22 ).Width( 420 ) ).Padding( 24 ); } RichTextBlock is for paragraphs that mix runs and inline elements — hyperlinks, mid-sentence bolds, color swaps. Use it sparingly: a list of fields lives better in Forms , and a multi-screen article lives better in Markdown(string) . RichTextBlock is the right tool when the structure is dynamic (a chat message with @-mentions, a search result with bolded query terms) because you build the Paragraph[] from data per render. Helper Use Paragraph(params RichTextInline[]) A paragraph; one per visual break. Run(string text) A plain text fragment. Hyperlink(string text, Uri target) Inline link with a navigate target. Modifiers — .MaxLines , .LineHeight , .TextAlignment , .TextTrimming , .CharacterSpacing — match TextBlock . Inline runs do not get the fast-path renderer, so prefer plain TextBlock for static text. WinUI design page: Rich text block . RichEditBox # RichEditBox( string text = \"\" , Action&#x3C; string >? onTextChanged = null ) class RichEditDemo : Component { public override Element Render () { var (text, setText) = UseState( \"Edit me. RichEditBox supports paste-with-formatting, \" + \"spell-check, and Enter for new paragraphs.\" ); return VStack( 8 , SubHeading( \"RichEditBox\" ), RichEditBox(text, setText) .AcceptsReturn() .IsSpellCheckEnabled() .TextWrapping() .Height( 160 ).Width( 420 ) ).Padding( 24 ); } } RichEditBox is the editable counterpart of RichTextBlock — multi-line text with paste-from-formatted-source, spell-check, IME composition, and selection. The change handler fires with the plain text content; for formatted output you read RichEditBox.Document through .Set(...) and serialize the text range yourself. Single-line text input belongs in TextBox , not here. Fluent Effect .AcceptsReturn(bool) Enter starts a new paragraph instead of submitting. .TextWrapping() Wrap (default) or NoWrap. .MaxLength(int) Cap input length. .IsSpellCheckEnabled(bool) Toggle squiggle underline. .SelectionHighlightColor(brush) Override selection background. .TextChanged(Action&#x3C;string>) Subscribe outside the constructor argument. WinUI design page: Rich edit box . Markdown (Reactor-original) # Markdown( string source) Markdown( string source, MarkdownOptions options) class MarkdownDemo : Component { public override Element Render () { const string source = \"# Release notes\\n\\n\" + \"Reactor **0.42** ships:\\n\\n\" + \"- Compositor animations via `UseAnimation`.\\n\" + \"- A new [Markdown](https://example.com) renderer.\\n\" + \"- Bug fixes for `LazyVStack` keyed reorder.\\n\\n\" + \"> Migration guide lives in the spec.\\n\" ; return VStack( 8 , SubHeading( \"Markdown\" ), Markdown(source) ).Padding( 24 ).Width( 440 ); } } Markdown is the largest Reactor-original control on this page. It parses GitHub-flavored Markdown with the embedded md4c parser and emits a Reactor element tree: headings become TextBlock with the heading variant, list items become HStack s, links become inline hyperlinks, code spans become monospace TextBlock . No WebView, no HTML round-trip — the output composes with every other modifier on this page ( .Padding , .Width , .TextWrapping ). The trade-off vs. WebView2 is fidelity: Markdown does not support arbitrary HTML, embedded &#x3C;script> , or CSS. It supports paragraphs, headings (h1–h6), lists (bullet, ordered, nested), inline code, fenced code blocks with optional language, links, images, bold, italic, strikethrough, blockquotes, hard line breaks, and tables. Anything beyond that — embedded videos, custom layouts, syntax-highlighted code with theming — falls back to WebView2 with an HTML-rendered fallback or a separate viewer. Reach for it for release notes, in-app help, conversational AI output, user-authored long-form prose (commit descriptions, knowledge-base articles), or any surface where the source is plain text and a thin formatting layer is what the reader needs. Use Markdown when Use RichTextBlock when Source is authored as a string (LLM output, README, user comment) Inline structure is computed from typed data You need lists, code blocks, blockquotes Plain paragraphs with mid-line formatting suffice The full GFM subset is acceptable You need arbitrary Inline element types Round-trip from .md files Round-trip from a RichTextParagraph[] schema No WinUI parallel: WinUI ships a MarkdownTextBlock in the Community Toolkit, but the API surface and behavior differ. The Reactor factory is the canonical reference for this control. Image # Image( string source) class ImageDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"Image\" ), // Resource Uri — ms-appx:// for packaged assets, file:// for disk, // https:// for remote. Image( \"ms-appx:///Assets/StoreLogo.png\" ) .Width( 96 ).Height( 96 ), TextBlock( \"Stretch.UniformToFill for cover art; \" + \"ImageFailed to detect missing assets.\" ).Opacity( 0.6 ) ).Padding( 24 ); } Image accepts the standard WinUI URI schemes — ms-appx:/// for assets packaged with the app, ms-appdata:/// for app-data files, file:/// for arbitrary disk paths, and http(s):// for remote sources. The factory delegates source decoding to the WinUI BitmapImage — same caching, same DPI awareness. Fluent Effect .Width(double) / .Height(double) Layout size. Without one of these the image takes its natural pixel size. .NineGrid(Thickness) Stretch the middle, preserve the borders (chrome-style backgrounds). .Set(img => img.Stretch = ...) Stretch mode: None / Uniform (default) / UniformToFill / Fill . .ImageOpened(Action) Decode succeeded. .ImageFailed(Action&#x3C;string>) Decode failed — fire to swap in a fallback. Don't: decode large remote images on the UI thread by setting Source to a full-size http:// URL inside a tight list-item render. The WinUI decoder spawns work off the UI thread, but the network fetch still blocks the visual tree from drawing the row until the bytes arrive. For lazy-loaded media inside a virtualized list, route through a UseResource that fetches a downscaled blob and only render Image once Pending is resolved. WinUI design page: Images and image brushes . MediaPlayerElement # MediaPlayerElement( string ? source = null ) class MediaPlayerDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"MediaPlayerElement\" ), MediaPlayerElement( \"https://learn.microsoft.com/en-us/windows/apps/design/\" + \"controls/images/ic_fluent_play_24_regular.svg\" ) .Width( 420 ).Height( 240 ) .Set(m => { m.AreTransportControlsEnabled = true ; m.AutoPlay = false ; }), TextBlock( \"Use AreTransportControlsEnabled for play/pause UI.\" ) .Opacity( 0.6 ) ).Padding( 24 ); } MediaPlayerElement wraps the WinUI MediaPlayerElement , which itself hosts a MediaPlayer that drives audio and video playback. The factory accepts a string URL for the common case; for stream sources, set the player through .Set(m => m.MediaPlayer.Source = ...) . Knob Set via AreTransportControlsEnabled .Set(m => m.AreTransportControlsEnabled = true) AutoPlay .Set(m => m.AutoPlay = false) IsFullWindow .Set(m => m.IsFullWindow = true) MediaOpened / MediaEnded / MediaFailed Dedicated fluent overloads Don't: mount and unmount MediaPlayerElement on every render to drive playback. The WinUI control owns an underlying MediaPlayer that re-initializes hardware decoders on each remount; thrash kills playback smoothness. Render the element once with a stable position in the tree and drive Source / play state through a hook ( UseRef holding the player), not through unmounts. WinUI design page: Media player . WebView2 # WebView2(Uri? source = null ) class WebViewDemo : Component { public override Element Render () { var (loaded, setLoaded) = UseState( false ); return VStack( 8 , SubHeading( \"WebView2\" ), WebView2( new Uri( \"about:blank\" )) .NavigationCompleted(_ => setLoaded( true )) .Width( 420 ).Height( 240 ), TextBlock(loaded ? \"Loaded.\" : \"Loading…\" ).Opacity( 0.6 ) ).Padding( 24 ); } } WebView2 embeds a Chromium-based browser. Use it for content that genuinely needs HTML/CSS/JavaScript — a markdown page does not, an embedded Office viewer does. The control raises lifecycle events for navigation and exposes a WebMessageReceived channel for postMessage interop. Event fluent Fires when .NavigationStarting(Action&#x3C;Uri>) A navigation begins — cancel via .Set . .NavigationCompleted(Action&#x3C;Uri>) Page finished loading. .WebMessageReceived(Action&#x3C;string>) Page called window.chrome.webview.postMessage(...) . .CoreWebView2Initialized(Action) The underlying CoreWebView2 is ready — configure it via .Set(w => w.CoreWebView2....) . Don't: lay out WebView2 inside an indeterminate-sized parent like an HStack without explicit dimensions. WebView2 measures to its content, which for a real web page is the viewport — without bounds it grows to fill the available size and triggers a layout oscillation when the page reflows. Always pin .Width and .Height (or place the control in a fixed-size Grid cell). WinUI design page: WebView2 . MapControl # MapControl( string ? mapServiceToken = null , double zoomLevel = 1 ) class MapControlDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"MapControl\" ), // Token blank — replace with a real Bing Maps key for tile fetch. // Without a token the control renders the grid background only. MapControl(mapServiceToken: null , zoomLevel: 4 ) .Width( 420 ).Height( 240 ) ).Padding( 24 ); } MapControl wraps Microsoft.UI.Xaml.Controls.Maps.MapControl . The service token comes from the Bing Maps developer portal — without it, the grid background renders but no tiles fetch. For full customization (pushpins, overlays, scenes), reach through .Set(m => ...) to the underlying control; the Reactor surface today exposes the factory arguments and direct passthrough. Don't: ship MapControl to an offline-tolerant app without a fallback. Tile fetch failures don't throw — the control just shows the grid. Subscribe to the underlying MapControl.MapServiceErrorOccurred through .Set and swap in an Image of a static map (or a \"map unavailable\" panel) when offline. WinUI design page: Map control (Windows App SDK). Reference # Control Factory Reactor-original? WinUI doc TextBlock TextBlock(string) No Text block Heading / SubHeading / Caption Heading(string) etc. Variant presets — RichTextBlock RichTextBlock(string) or RichTextBlock(RichTextParagraph[]) No Rich text block RichEditBox RichEditBox(text, onChanged) No Rich edit box Markdown Markdown(string) Yes — Image Image(string) No Images and image brushes MediaPlayerElement MediaPlayerElement(string?) No Media player WebView2 WebView2(Uri?) No WebView2 MapControl MapControl(token, zoom) No Map control InkCanvas Not yet wrapped — Ink controls Patterns # Long-form prose with Markdown # The release-notes / changelog pattern: parse a string of GFM into the element tree, then style the container — not the inner elements. Pad, constrain width to ~640 logical pixels for readability, and let the Markdown renderer hand back the hierarchy. Wrap the call in a UseMemo keyed on the source string when the renderer is inside a frequently-rendered parent; the parser is cheap but allocates, and memoization keeps GC pressure flat: var rendered = UseMemo(() => Markdown(source), source); return Border(rendered).Padding( 20 ).Width( 640 ); This is the same shape as the recipes/master-detail content pane and the chat sample message bubble. Inline data display with RichTextBlock # When a list-row needs Name (status) — 3 hours ago with the status bold and the timestamp dim, build a RichTextBlock per row out of the typed data. Keep the paragraph construction inline in the row Component so it re-runs only when the row's data changes; RichTextBlock is not the bottleneck for moderate row counts but the inline-element allocation can show up in profiles when a LazyVStack scrolls thousands of rows at 60fps. Common Mistakes # Using TextBlock for everything # // Don't: VStack( 8 , TextBlock( \"Settings\" ).FontSize( 28 ).Bold(), TextBlock( \"Display\" ).FontSize( 20 ).Bold(), TextBlock( \"Adjust resolution and orientation.\" ) ) class TextVariantsDemo : Component { public override Element Render () => VStack( 8 , Heading( \"Heading — page or section title\" ), SubHeading( \"SubHeading — region header\" ), TextBlock( \"Body text. The default size and weight for prose.\" ), Caption( \"Caption — secondary metadata, dates, labels.\" ) ).Padding( 24 ); } The \"don't\" form ships visual hierarchy but no semantic hierarchy — the accessibility scanner reports no headings and screen-reader users get no document outline. The correct form uses the Heading / SubHeading / TextBlock factories so accessibility.md landmark detection works. Rendering Markdown inside a tight scroll loop without memoizing # // Don't: LazyVStack&#x3C;Message>(messages, m => m.Id, (m, i) => Markdown(m.Body)) // re-parses on every scroll-induced re-render // Do: LazyVStack&#x3C;Message>(messages, m => m.Id, (m, i) => Memo(_ => Markdown(m.Body), m.Body)) Markdown parsing is fast but not free — at 60fps × hundreds of visible rows × every state change, parser allocation shows up in GC. The Memo wrapper keys on the source string so unchanged rows skip the parser entirely. Tips # Pick the smallest control that does the job. TextBlock > RichTextBlock > Markdown > WebView2 in increasing layout cost. Promote only when the lower tier can't represent the structure. Use Selectable() on any text the user might want to copy. Error messages, IDs, paths, log lines, command output — all of them are more useful when the user can drag-select and Ctrl+C. The cost is one modifier per TextBlock . Pin dimensions on media controls. Image , MediaPlayerElement , WebView2 , and MapControl all default to \"fill available space\" or \"natural content size\", which interacts badly with VStack / HStack auto-layout. Set .Width and .Height (or place them in a fixed Grid cell). Markdown is the right choice for LLM output. Treat AI-generated text as a markdown stream — parse it, render it, theme the container. The renderer composes with the rest of the catalog and stays accessible. Next Steps # Forms and Input — Previous: editable input controls and the validation system. Status and Info — Next: ProgressBar, InfoBar, badges, and non-interactive feedback. Styling — Theme tokens and font fluents applied across every text element. Accessibility — How Heading / SubHeading map to document landmarks. Markdown samples — End-to-end LLM-output rendering in the chat sample.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/text-and-media.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/text-and-media.md",
      "headings": [
        {
          "depth": 2,
          "id": "text-variants",
          "text": "Text variants"
        },
        {
          "depth": 2,
          "id": "textblock-modifiers",
          "text": "TextBlock modifiers"
        },
        {
          "depth": 2,
          "id": "richtextblock",
          "text": "RichTextBlock"
        },
        {
          "depth": 2,
          "id": "richeditbox",
          "text": "RichEditBox"
        },
        {
          "depth": 2,
          "id": "markdown-reactor-original",
          "text": "Markdown (Reactor-original)"
        },
        {
          "depth": 2,
          "id": "image",
          "text": "Image"
        },
        {
          "depth": 2,
          "id": "mediaplayerelement",
          "text": "MediaPlayerElement"
        },
        {
          "depth": 2,
          "id": "webview2",
          "text": "WebView2"
        },
        {
          "depth": 2,
          "id": "mapcontrol",
          "text": "MapControl"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "long-form-prose-with-markdown",
          "text": "Long-form prose with Markdown"
        },
        {
          "depth": 3,
          "id": "inline-data-display-with-richtextblock",
          "text": "Inline data display with RichTextBlock"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "using-textblock-for-everything",
          "text": "Using TextBlock for everything"
        },
        {
          "depth": 3,
          "id": "rendering-markdown-inside-a-tight-scroll-loop-without-memoizing",
          "text": "Rendering Markdown inside a tight scroll loop without memoizing"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Status and Info",
      "path": "status-and-info.md",
      "slug": "/status-and-info/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/\">Controls</a>.</p>\n</blockquote>\n<h1 id=\"status-and-info\">Status and Info<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#status-and-info\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor)'s status and info controls report what the app is doing without stealing\nfocus. Reach for them when the user needs to know something — a save\nsucceeded, a long operation is running, a feature exists — and doesn't\nneed to decide anything. For controls that demand a choice\n(Save / Discard / Cancel), use <a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a>.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Factory</th><th>Purpose</th></tr></thead><tbody><tr><td><code>InfoBar</code></td><td><code>InfoBar(title?, message?)</code></td><td>App-level message banner with severity.</td></tr><tr><td><code>InfoBadge</code></td><td><code>InfoBadge()</code> / <code>InfoBadge(int)</code></td><td>Notification count or presence dot.</td></tr><tr><td><code>ProgressBar</code></td><td><code>Progress(double)</code> / <code>ProgressIndeterminate()</code></td><td>Linear progress, determinate or indeterminate.</td></tr><tr><td><code>ProgressRing</code></td><td><code>ProgressRing()</code> / <code>ProgressRing(double)</code></td><td>Spinner / determinate ring.</td></tr><tr><td><code>TeachingTip</code></td><td><code>TeachingTip(title, subtitle?)</code></td><td>One-shot guided callout anchored to a control.</td></tr><tr><td><code>PipsPager</code></td><td><code>PipsPager(count, index, onChanged?)</code></td><td>Compact paginator dots.</td></tr><tr><td><code>PersonPicture</code></td><td><code>PersonPicture()</code></td><td>Contact avatar — display name, initials, or image.</td></tr><tr><td><code>RatingControl</code></td><td><code>RatingControl(value, onChanged?)</code></td><td>0–5 star rating.</td></tr></tbody></table>\n<h2 id=\"infobar\">InfoBar<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#infobar\">#</a></h2>\n<p><code>InfoBar</code> is the right control for an app-level message that the user\nshould notice but doesn't have to act on. The severity fluents\n(<code>.Informational()</code>, <code>.Success()</code>, <code>.Warning()</code>, <code>.Error()</code>) set the\nicon, color, and accessibility role in one call:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InfoBarSeveritiesDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"InfoBar — severity fluents\"</span>),\n        InfoBar(<span class=\"hljs-string\">\"Saving…\"</span>, <span class=\"hljs-string\">\"Your changes are being written.\"</span>)\n            .Informational().IsClosable(<span class=\"hljs-literal\">false</span>),\n        InfoBar(<span class=\"hljs-string\">\"Saved\"</span>, <span class=\"hljs-string\">\"Your changes were saved.\"</span>)\n            .Success().IsClosable(<span class=\"hljs-literal\">false</span>),\n        InfoBar(<span class=\"hljs-string\">\"Slow connection\"</span>,\n            <span class=\"hljs-string\">\"Some assets may not load until the network recovers.\"</span>)\n            .Warning().IsClosable(<span class=\"hljs-literal\">false</span>),\n        InfoBar(<span class=\"hljs-string\">\"Save failed\"</span>,\n            <span class=\"hljs-string\">\"The destination drive is read-only.\"</span>)\n            .Error().IsClosable(<span class=\"hljs-literal\">false</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/status-and-info/infobar-severities.png\" alt=\"Four InfoBar severities stacked\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Informational()</code> / <code>.Success()</code> / <code>.Warning()</code> / <code>.Error()</code></td><td>Sets <code>Severity</code> + paired icon/color.</td></tr><tr><td><code>.IsClosable(bool)</code></td><td>Toggle the dismiss button.</td></tr><tr><td><code>.IconSource(IconData)</code></td><td>Override the severity icon.</td></tr><tr><td><code>.Content(Element)</code></td><td>Render rich content (links, embedded controls) below the message.</td></tr><tr><td><code>.ActionButtonClick(Action)</code></td><td>Subscribe to the trailing action button.</td></tr><tr><td><code>.Closed(Action)</code></td><td>Fires when the user dismisses.</td></tr></tbody></table>\n<p><code>IsOpen</code> defaults to <code>true</code>, so a bare <code>InfoBar(...)</code> is visible\nimmediately. For a dismissible bar that can re-open, treat <code>IsOpen</code> as\ncontrolled state — own the <code>bool</code>, set it to <code>false</code> on <code>OnClosed</code>, and\nflip it back to <code>true</code> when conditions change:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InfoBarDismissDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// InfoBar is a controlled component — you own the IsOpen flag,</span>\n        <span class=\"hljs-comment\">// and you reset it via the OnClosed callback the user dismiss</span>\n        <span class=\"hljs-comment\">// raises.</span>\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">true</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Dismiss and re-open\"</span>),\n            InfoBar(<span class=\"hljs-string\">\"Tip\"</span>, <span class=\"hljs-string\">\"InfoBar uses controlled visibility.\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = open,\n                IsClosable = <span class=\"hljs-literal\">true</span>,\n                OnClosed = () => setOpen(<span class=\"hljs-literal\">false</span>),\n                Severity = Microsoft.UI.Xaml.Controls.InfoBarSeverity.Informational,\n            },\n            Button(<span class=\"hljs-string\">\"Show again\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>)).IsEnabled(!open)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/infobar\">Info bar</a>.</p>\n<h2 id=\"infobadge\">InfoBadge<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#infobadge\">#</a></h2>\n<p><code>InfoBadge</code> is the dot or count that decorates a <code>NavigationViewItem</code>,\ntab, or any element with a notification. Pass an <code>int</code> for a count;\nomit the argument for a presence-only dot:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InfoBadgeDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"InfoBadge\"</span>),\n        HStack(<span class=\"hljs-number\">16</span>,\n            <span class=\"hljs-comment\">// Dot variant — no value, just a presence indicator.</span>\n            VStack(<span class=\"hljs-number\">4</span>,\n                InfoBadge(),\n                TextBlock(<span class=\"hljs-string\">\"dot\"</span>).FontSize(<span class=\"hljs-number\">11</span>).Opacity(<span class=\"hljs-number\">0.6</span>)),\n            <span class=\"hljs-comment\">// Numeric — common for unread counts.</span>\n            VStack(<span class=\"hljs-number\">4</span>,\n                InfoBadge(<span class=\"hljs-number\">3</span>),\n                TextBlock(<span class=\"hljs-string\">\"count\"</span>).FontSize(<span class=\"hljs-number\">11</span>).Opacity(<span class=\"hljs-number\">0.6</span>)),\n            VStack(<span class=\"hljs-number\">4</span>,\n                InfoBadge(<span class=\"hljs-number\">127</span>),\n                TextBlock(<span class=\"hljs-string\">\"large\"</span>).FontSize(<span class=\"hljs-number\">11</span>).Opacity(<span class=\"hljs-number\">0.6</span>))\n        )\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/status-and-info/infobadge.png\" alt=\"Dot, count, and large-count badges\"></p>\n<p><code>InfoBadgeElement.Value</code> is nullable — large counts (≥100) render as\n\"99+\" by the underlying WinUI control. Use <code>.Set(b => b.Icon = ...)</code>\nwhen you need a custom glyph instead of a numeric value.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/info-badge\">Info badge</a>.</p>\n<h2 id=\"progressbar-and-progressring\">ProgressBar and ProgressRing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#progressbar-and-progressring\">#</a></h2>\n<p><code>Progress(value)</code> returns a determinate bar; <code>ProgressIndeterminate()</code>\nreturns an indeterminate bar. <code>ProgressRing(value)</code> and <code>ProgressRing()</code>\nare the ring counterparts. Pick the bar for known-duration work in a\nstrip of horizontal real estate (uploads, downloads, batch progress);\npick the ring for unknown-duration spinners next to a label\n(\"Loading…\"):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ProgressBarDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">value</span>, setValue) = UseState(<span class=\"hljs-number\">35.0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"ProgressBar\"</span>),\n            Progress(<span class=\"hljs-keyword\">value</span>).Width(<span class=\"hljs-number\">320</span>),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">value</span>:<span class=\"hljs-number\">0</span>}</span>%\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"−10\"</span>, () => setValue(Math.Max(<span class=\"hljs-number\">0</span>, <span class=\"hljs-keyword\">value</span> - <span class=\"hljs-number\">10</span>))),\n                Button(<span class=\"hljs-string\">\"+10\"</span>, () => setValue(Math.Min(<span class=\"hljs-number\">100</span>, <span class=\"hljs-keyword\">value</span> + <span class=\"hljs-number\">10</span>)))\n            ),\n            <span class=\"hljs-comment\">// Indeterminate — no value argument.</span>\n            SubHeading(<span class=\"hljs-string\">\"Indeterminate\"</span>),\n            ProgressIndeterminate().Width(<span class=\"hljs-number\">320</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/status-and-info/progress-bar.png\" alt=\"Determinate ProgressBar with stepper buttons\"></p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ProgressRingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">12</span>,\n        SubHeading(<span class=\"hljs-string\">\"ProgressRing\"</span>),\n        <span class=\"hljs-comment\">// Determinate ring at 60%.</span>\n        ProgressRing(<span class=\"hljs-number\">0.6</span>).Width(<span class=\"hljs-number\">48</span>).Height(<span class=\"hljs-number\">48</span>),\n        <span class=\"hljs-comment\">// Indeterminate spinner.</span>\n        ProgressRing().IsActive().Width(<span class=\"hljs-number\">48</span>).Height(<span class=\"hljs-number\">48</span>)\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n<p>The <code>value</code> argument is 0–100 for <code>Progress(double)</code> and 0–1 for\n<code>ProgressRing(double)</code> — same convention as the underlying WinUI\ncontrols. The <code>ProgressBar</code> factory is the deprecated legacy name; use\n<code>Progress</code> and <code>ProgressIndeterminate</code> for new code (spec 039 §5).</p>\n<p>WinUI design pages: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/progress-controls\">Progress controls</a>.</p>\n<h2 id=\"teachingtip\">TeachingTip<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#teachingtip\">#</a></h2>\n<p><code>TeachingTip</code> is a one-shot guided callout — \"did you know this menu\nexists?\" — anchored next to a target control. Like <code>InfoBar</code>, it uses\ncontrolled <code>IsOpen</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TeachingTipDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (show, setShow) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"TeachingTip\"</span>),\n            Button(<span class=\"hljs-string\">\"Show tip\"</span>, () => setShow(<span class=\"hljs-literal\">true</span>)),\n            TeachingTip(<span class=\"hljs-string\">\"Try the new sort menu\"</span>,\n                <span class=\"hljs-string\">\"Sort across multiple columns by holding Shift.\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = show,\n                OnClosed = () => setShow(<span class=\"hljs-literal\">false</span>),\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.IconSource(IconData)</code></td><td>Leading icon next to the title.</td></tr><tr><td><code>.HeroContent(Element)</code></td><td>Image or rich content above the title.</td></tr><tr><td><code>.PreferredPlacement(mode)</code></td><td>Top / Bottom / Left / Right / Auto.</td></tr><tr><td><code>.PlacementMargin(Thickness)</code></td><td>Offset from the anchor.</td></tr><tr><td><code>.ActionButtonClick(Action)</code></td><td>Subscribe to the action button.</td></tr><tr><td><code>.Closed(Action)</code></td><td>Fires when the user dismisses.</td></tr></tbody></table>\n<p>Show a teaching tip once, not on every render. The\n<a href=\"/persistence/\"><code>UsePersisted</code></a> pattern keeps a <code>\"tour-seen\"</code> boolean\nthat gates <code>IsOpen</code> so the tip doesn't re-appear after the user\ndismisses it.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/teaching-tip\">Teaching tip</a>.</p>\n<h2 id=\"pipspager\">PipsPager<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pipspager\">#</a></h2>\n<p><code>PipsPager</code> is a compact paginator — a row of dots where the active\nindex is highlighted. Use it for short page sets (≤ 10 pages,\ntypically) where the standard <code>Pager</code> chrome would be heavy:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PipsPagerDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (page, setPage) = UseState(<span class=\"hljs-number\">2</span>);\n        <span class=\"hljs-keyword\">var</span> pageCount = <span class=\"hljs-number\">5</span>;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"PipsPager\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Page <span class=\"hljs-subst\">{page + <span class=\"hljs-number\">1</span>}</span> of <span class=\"hljs-subst\">{pageCount}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            PipsPager(pageCount, page, setPage)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/status-and-info/pips-pager.png\" alt=\"PipsPager at page 2 of 5\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.MaxVisiblePips(int)</code></td><td>Cap the visible dot count for long ranges.</td></tr><tr><td><code>.WrapMode(mode)</code></td><td><code>None</code> (default) / <code>Wrap</code> — Right of last → first.</td></tr><tr><td><code>.PreviousButtonVisibility(visibility)</code></td><td><code>Collapsed</code> / <code>Visible</code> / <code>VisibleOnPointerOver</code>.</td></tr><tr><td><code>.NextButtonVisibility(visibility)</code></td><td>Same options for the next button.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/pipspager\">Pips pager</a>.</p>\n<h2 id=\"personpicture\">PersonPicture<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#personpicture\">#</a></h2>\n<p><code>PersonPicture</code> is the contact avatar. It accepts a display name (uses\nthe first letter of each word as initials), explicit initials, or a\nprofile image through <code>.Set(p => p.ProfilePicture = ...)</code>. Without any\nof those, it falls back to a generic person glyph:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PersonPictureDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">8</span>,\n        SubHeading(<span class=\"hljs-string\">\"PersonPicture\"</span>),\n        HStack(<span class=\"hljs-number\">12</span>,\n            PersonPicture()\n                .DisplayName(<span class=\"hljs-string\">\"Ada Lovelace\"</span>)\n                .Width(<span class=\"hljs-number\">48</span>).Height(<span class=\"hljs-number\">48</span>),\n            PersonPicture()\n                .Initials(<span class=\"hljs-string\">\"CB\"</span>)\n                .Width(<span class=\"hljs-number\">48</span>).Height(<span class=\"hljs-number\">48</span>),\n            <span class=\"hljs-comment\">// No name or initials — falls back to the generic person glyph.</span>\n            PersonPicture().Width(<span class=\"hljs-number\">48</span>).Height(<span class=\"hljs-number\">48</span>)\n        )\n    ).Padding(<span class=\"hljs-number\">24</span>);\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.DisplayName(string)</code></td><td>Auto-derives initials.</td></tr><tr><td><code>.Initials(string)</code></td><td>Override the derived initials.</td></tr><tr><td><code>.Set(p => p.ProfilePicture = ...)</code></td><td>Provide a <code>BitmapImage</code>.</td></tr></tbody></table>\n<p>Pair <code>PersonPicture</code> with <code>InfoBadge</code> for a \"user with notification\"\nchrome — set <code>BadgeNumber</code> / <code>BadgeGlyph</code> on the underlying control\nthrough <code>.Set</code>.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/person-picture\">Person picture</a>.</p>\n<h2 id=\"ratingcontrol\">RatingControl<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#ratingcontrol\">#</a></h2>\n<p><code>RatingControl</code> is a 0–5 star rating (the maximum is configurable).\nThe factory takes the current value and an optional change handler —\nsame controlled-input pattern as <code>Slider</code> and <code>NumberBox</code> from\n<a href=\"/forms/\">Forms</a>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RatingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (rating, setRating) = UseState(<span class=\"hljs-number\">3.0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"RatingControl\"</span>),\n            RatingControl(rating, setRating)\n                .Caption(<span class=\"hljs-string\">\"Tap a star or use ←/→ to rate\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Selected: <span class=\"hljs-subst\">{rating}</span> stars\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.MaxRating(int)</code></td><td>Star count. Default 5.</td></tr><tr><td><code>.IsReadOnly(bool)</code></td><td>Display-only mode for showing an existing rating.</td></tr><tr><td><code>.Caption(string)</code></td><td>Caption shown below the stars.</td></tr><tr><td><code>.PlaceholderValue(double)</code></td><td>Greyed-out value before the user rates.</td></tr><tr><td><code>.InitialSetValue(int)</code></td><td>Value applied on first interaction.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/rating-control\">Rating control</a>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pick the smallest signal for the message.</strong> A 3-line <code>InfoBar</code> says\nthe same thing as a \"Saved!\" toast but takes ten times the screen\nheight. Reach for <code>InfoBadge</code> first, <code>InfoBar</code> next, dialogs last.</p>\n<p><strong>Determinate progress beats indeterminate every time you can compute\nit.</strong> The user can plan around 3 minutes remaining; they cannot plan\naround a spinner. Use the count of completed items / total items, even\nif the time-per-item is rough.</p>\n<p><strong>Don't combine multiple <code>TeachingTip</code>s on the same page.</strong> They steal\nattention from each other. Show one tip per session, dismiss it on\ninteraction with the target, and persist the dismissal with\n<a href=\"/persistence/\"><code>UsePersisted</code></a>.</p>\n<p><strong>Use <code>Severity.Error</code> sparingly.</strong> Reserve it for actual failure\nstates. If <code>Warning</code> works (\"operation took longer than expected\"), use\nit — <code>Error</code> borrows the alert role from the accessibility tree, which\nscreen readers announce immediately.</p>\n<p><strong>Bind <code>PersonPicture</code> to your user model, not to chrome.</strong> The\ncontrol is a passive view of the current user's identity — drive the\ndisplay name through your auth/profile state hook, never hardcode.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/text-and-media/\">Text and Media</a></strong> — Previous: read-only content surfaces.</li>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a></strong> — Next: modal and ephemeral interaction surfaces.</li>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — The interactive counterparts (RatingControl pattern matches Slider/NumberBox).</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — How InfoBar's severity maps to ARIA live regions.</li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — Persisting \"user has seen this teaching tip\" with <code>UsePersisted</code>.</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Controls . Status and Info # Microsoft.UI.Reactor (Reactor)'s status and info controls report what the app is doing without stealing focus. Reach for them when the user needs to know something — a save succeeded, a long operation is running, a feature exists — and doesn't need to decide anything. For controls that demand a choice (Save / Discard / Cancel), use Dialogs and Flyouts . Reference # Control Factory Purpose InfoBar InfoBar(title?, message?) App-level message banner with severity. InfoBadge InfoBadge() / InfoBadge(int) Notification count or presence dot. ProgressBar Progress(double) / ProgressIndeterminate() Linear progress, determinate or indeterminate. ProgressRing ProgressRing() / ProgressRing(double) Spinner / determinate ring. TeachingTip TeachingTip(title, subtitle?) One-shot guided callout anchored to a control. PipsPager PipsPager(count, index, onChanged?) Compact paginator dots. PersonPicture PersonPicture() Contact avatar — display name, initials, or image. RatingControl RatingControl(value, onChanged?) 0–5 star rating. InfoBar # InfoBar is the right control for an app-level message that the user should notice but doesn't have to act on. The severity fluents ( .Informational() , .Success() , .Warning() , .Error() ) set the icon, color, and accessibility role in one call: class InfoBarSeveritiesDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"InfoBar — severity fluents\" ), InfoBar( \"Saving…\" , \"Your changes are being written.\" ) .Informational().IsClosable( false ), InfoBar( \"Saved\" , \"Your changes were saved.\" ) .Success().IsClosable( false ), InfoBar( \"Slow connection\" , \"Some assets may not load until the network recovers.\" ) .Warning().IsClosable( false ), InfoBar( \"Save failed\" , \"The destination drive is read-only.\" ) .Error().IsClosable( false ) ).Padding( 24 ); } Fluent Effect .Informational() / .Success() / .Warning() / .Error() Sets Severity + paired icon/color. .IsClosable(bool) Toggle the dismiss button. .IconSource(IconData) Override the severity icon. .Content(Element) Render rich content (links, embedded controls) below the message. .ActionButtonClick(Action) Subscribe to the trailing action button. .Closed(Action) Fires when the user dismisses. IsOpen defaults to true , so a bare InfoBar(...) is visible immediately. For a dismissible bar that can re-open, treat IsOpen as controlled state — own the bool , set it to false on OnClosed , and flip it back to true when conditions change: class InfoBarDismissDemo : Component { public override Element Render () { // InfoBar is a controlled component — you own the IsOpen flag, // and you reset it via the OnClosed callback the user dismiss // raises. var (open, setOpen) = UseState( true ); return VStack( 8 , SubHeading( \"Dismiss and re-open\" ), InfoBar( \"Tip\" , \"InfoBar uses controlled visibility.\" ) with { IsOpen = open, IsClosable = true , OnClosed = () => setOpen( false ), Severity = Microsoft.UI.Xaml.Controls.InfoBarSeverity.Informational, }, Button( \"Show again\" , () => setOpen( true )).IsEnabled(!open) ).Padding( 24 ); } } WinUI design page: Info bar . InfoBadge # InfoBadge is the dot or count that decorates a NavigationViewItem , tab, or any element with a notification. Pass an int for a count; omit the argument for a presence-only dot: class InfoBadgeDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"InfoBadge\" ), HStack( 16 , // Dot variant — no value, just a presence indicator. VStack( 4 , InfoBadge(), TextBlock( \"dot\" ).FontSize( 11 ).Opacity( 0.6 )), // Numeric — common for unread counts. VStack( 4 , InfoBadge( 3 ), TextBlock( \"count\" ).FontSize( 11 ).Opacity( 0.6 )), VStack( 4 , InfoBadge( 127 ), TextBlock( \"large\" ).FontSize( 11 ).Opacity( 0.6 )) ) ).Padding( 24 ); } InfoBadgeElement.Value is nullable — large counts (≥100) render as \"99+\" by the underlying WinUI control. Use .Set(b => b.Icon = ...) when you need a custom glyph instead of a numeric value. WinUI design page: Info badge . ProgressBar and ProgressRing # Progress(value) returns a determinate bar; ProgressIndeterminate() returns an indeterminate bar. ProgressRing(value) and ProgressRing() are the ring counterparts. Pick the bar for known-duration work in a strip of horizontal real estate (uploads, downloads, batch progress); pick the ring for unknown-duration spinners next to a label (\"Loading…\"): class ProgressBarDemo : Component { public override Element Render () { var ( value , setValue) = UseState( 35.0 ); return VStack( 12 , SubHeading( \"ProgressBar\" ), Progress( value ).Width( 320 ), TextBlock( $\" { value : 0 } %\" ).Opacity( 0.6 ), HStack( 8 , Button( \"−10\" , () => setValue(Math.Max( 0 , value - 10 ))), Button( \"+10\" , () => setValue(Math.Min( 100 , value + 10 ))) ), // Indeterminate — no value argument. SubHeading( \"Indeterminate\" ), ProgressIndeterminate().Width( 320 ) ).Padding( 24 ); } } class ProgressRingDemo : Component { public override Element Render () => VStack( 12 , SubHeading( \"ProgressRing\" ), // Determinate ring at 60%. ProgressRing( 0.6 ).Width( 48 ).Height( 48 ), // Indeterminate spinner. ProgressRing().IsActive().Width( 48 ).Height( 48 ) ).Padding( 24 ); } The value argument is 0–100 for Progress(double) and 0–1 for ProgressRing(double) — same convention as the underlying WinUI controls. The ProgressBar factory is the deprecated legacy name; use Progress and ProgressIndeterminate for new code (spec 039 §5). WinUI design pages: Progress controls . TeachingTip # TeachingTip is a one-shot guided callout — \"did you know this menu exists?\" — anchored next to a target control. Like InfoBar , it uses controlled IsOpen : class TeachingTipDemo : Component { public override Element Render () { var (show, setShow) = UseState( false ); return VStack( 12 , SubHeading( \"TeachingTip\" ), Button( \"Show tip\" , () => setShow( true )), TeachingTip( \"Try the new sort menu\" , \"Sort across multiple columns by holding Shift.\" ) with { IsOpen = show, OnClosed = () => setShow( false ), } ).Padding( 24 ); } } Fluent Effect .IconSource(IconData) Leading icon next to the title. .HeroContent(Element) Image or rich content above the title. .PreferredPlacement(mode) Top / Bottom / Left / Right / Auto. .PlacementMargin(Thickness) Offset from the anchor. .ActionButtonClick(Action) Subscribe to the action button. .Closed(Action) Fires when the user dismisses. Show a teaching tip once, not on every render. The UsePersisted pattern keeps a \"tour-seen\" boolean that gates IsOpen so the tip doesn't re-appear after the user dismisses it. WinUI design page: Teaching tip . PipsPager # PipsPager is a compact paginator — a row of dots where the active index is highlighted. Use it for short page sets (≤ 10 pages, typically) where the standard Pager chrome would be heavy: class PipsPagerDemo : Component { public override Element Render () { var (page, setPage) = UseState( 2 ); var pageCount = 5 ; return VStack( 8 , SubHeading( \"PipsPager\" ), TextBlock( $\"Page {page + 1 } of {pageCount} \" ).Opacity( 0.6 ), PipsPager(pageCount, page, setPage) ).Padding( 24 ); } } Fluent Effect .MaxVisiblePips(int) Cap the visible dot count for long ranges. .WrapMode(mode) None (default) / Wrap — Right of last → first. .PreviousButtonVisibility(visibility) Collapsed / Visible / VisibleOnPointerOver . .NextButtonVisibility(visibility) Same options for the next button. WinUI design page: Pips pager . PersonPicture # PersonPicture is the contact avatar. It accepts a display name (uses the first letter of each word as initials), explicit initials, or a profile image through .Set(p => p.ProfilePicture = ...) . Without any of those, it falls back to a generic person glyph: class PersonPictureDemo : Component { public override Element Render () => VStack( 8 , SubHeading( \"PersonPicture\" ), HStack( 12 , PersonPicture() .DisplayName( \"Ada Lovelace\" ) .Width( 48 ).Height( 48 ), PersonPicture() .Initials( \"CB\" ) .Width( 48 ).Height( 48 ), // No name or initials — falls back to the generic person glyph. PersonPicture().Width( 48 ).Height( 48 ) ) ).Padding( 24 ); } Fluent Effect .DisplayName(string) Auto-derives initials. .Initials(string) Override the derived initials. .Set(p => p.ProfilePicture = ...) Provide a BitmapImage . Pair PersonPicture with InfoBadge for a \"user with notification\" chrome — set BadgeNumber / BadgeGlyph on the underlying control through .Set . WinUI design page: Person picture . RatingControl # RatingControl is a 0–5 star rating (the maximum is configurable). The factory takes the current value and an optional change handler — same controlled-input pattern as Slider and NumberBox from Forms : class RatingDemo : Component { public override Element Render () { var (rating, setRating) = UseState( 3.0 ); return VStack( 8 , SubHeading( \"RatingControl\" ), RatingControl(rating, setRating) .Caption( \"Tap a star or use ←/→ to rate\" ), TextBlock( $\"Selected: {rating} stars\" ).Opacity( 0.6 ) ).Padding( 24 ); } } Fluent Effect .MaxRating(int) Star count. Default 5. .IsReadOnly(bool) Display-only mode for showing an existing rating. .Caption(string) Caption shown below the stars. .PlaceholderValue(double) Greyed-out value before the user rates. .InitialSetValue(int) Value applied on first interaction. WinUI design page: Rating control . Tips # Pick the smallest signal for the message. A 3-line InfoBar says the same thing as a \"Saved!\" toast but takes ten times the screen height. Reach for InfoBadge first, InfoBar next, dialogs last. Determinate progress beats indeterminate every time you can compute it. The user can plan around 3 minutes remaining; they cannot plan around a spinner. Use the count of completed items / total items, even if the time-per-item is rough. Don't combine multiple TeachingTip s on the same page. They steal attention from each other. Show one tip per session, dismiss it on interaction with the target, and persist the dismissal with UsePersisted . Use Severity.Error sparingly. Reserve it for actual failure states. If Warning works (\"operation took longer than expected\"), use it — Error borrows the alert role from the accessibility tree, which screen readers announce immediately. Bind PersonPicture to your user model, not to chrome. The control is a passive view of the current user's identity — drive the display name through your auth/profile state hook, never hardcode. Next Steps # Text and Media — Previous: read-only content surfaces. Dialogs and Flyouts — Next: modal and ephemeral interaction surfaces. Forms — The interactive counterparts (RatingControl pattern matches Slider/NumberBox). Accessibility — How InfoBar's severity maps to ARIA live regions. Persistence — Persisting \"user has seen this teaching tip\" with UsePersisted .",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/status-and-info.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/status-and-info.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "infobar",
          "text": "InfoBar"
        },
        {
          "depth": 2,
          "id": "infobadge",
          "text": "InfoBadge"
        },
        {
          "depth": 2,
          "id": "progressbar-and-progressring",
          "text": "ProgressBar and ProgressRing"
        },
        {
          "depth": 2,
          "id": "teachingtip",
          "text": "TeachingTip"
        },
        {
          "depth": 2,
          "id": "pipspager",
          "text": "PipsPager"
        },
        {
          "depth": 2,
          "id": "personpicture",
          "text": "PersonPicture"
        },
        {
          "depth": 2,
          "id": "ratingcontrol",
          "text": "RatingControl"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Dialogs and Flyouts",
      "path": "dialogs-and-flyouts.md",
      "slug": "/dialogs-and-flyouts/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/\">Dialogs And Flyouts</a>.</p>\n</blockquote>\n<p>Dialog and flyout surfaces interrupt the user. That makes them the most\ncostly UI primitive in the app's attention budget — every modal dialog\nsteals focus, every flyout dismisses on outside click, every popup\narrives without a route back. Microsoft.UI.Reactor (Reactor) treats dialogs and flyouts as\ncontrolled components: you own the open/closed boolean, the component\ntree always contains the dialog element, and the visibility is a state\nflag the user-driven event handlers flip back. That is the same shape as\n<code>TextBox</code> from <a href=\"/forms/\">Forms</a>, and it makes the dialog testable —\nthe dialog is open if and only if its driving state says so. Read this\npage when you are reaching for <code>ContentDialog</code> for a Save/Discard choice,\n<code>MenuFlyout</code> for a right-click menu, <code>CommandBarFlyout</code> for a selection\ntoolbar, or <code>Popup</code> for the bespoke overlay that none of the above\ncovers. Pair every dialog with a corresponding entry in\n<a href=\"/commanding/\">commanding.md</a> — the same <code>Command</code> should usually back\nthe trigger (button or menu) and the dialog's primary action.</p>\n<h1 id=\"dialogs-and-flyouts\">Dialogs and Flyouts<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dialogs-and-flyouts\">#</a></h1>\n<p>Four primitives sit on this page. Pick by shape, not by aesthetic:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Control</th><th>Shape</th><th>Dismiss</th><th>Use when</th></tr></thead><tbody><tr><td><code>ContentDialog</code></td><td>Modal, screen-centered, three buttons</td><td>Button click</td><td>The user has to make a choice before continuing.</td></tr><tr><td><code>MenuFlyout</code></td><td>Pop-up menu next to a target</td><td>Outside click / Esc</td><td>Right-click or chevron menu of discrete actions.</td></tr><tr><td><code>CommandBarFlyout</code></td><td>Mini-toolbar next to a target</td><td>Outside click / Esc</td><td>Selection context — \"what to do with this thing\".</td></tr><tr><td><code>Popup</code></td><td>Free-form overlay</td><td>Light-dismiss / explicit</td><td>Anything the three above can't render — color picker, in-place editor, custom tooltip.</td></tr></tbody></table>\n<h2 id=\"contentdialog\">ContentDialog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#contentdialog\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">ContentDialog(<span class=\"hljs-built_in\">string</span> title, Element content, <span class=\"hljs-built_in\">string</span> primaryButtonText = <span class=\"hljs-string\">\"OK\"</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BasicDialogDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Basic ContentDialog\"</span>),\n            Button(<span class=\"hljs-string\">\"Show dialog\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>)),\n            <span class=\"hljs-comment\">// Dialog lives in the tree at all times. IsOpen controls</span>\n            <span class=\"hljs-comment\">// visibility; OnClosed flips it back when the user dismisses.</span>\n            ContentDialog(\n                <span class=\"hljs-string\">\"Welcome\"</span>,\n                TextBlock(<span class=\"hljs-string\">\"Thank you for trying Reactor.\"</span>),\n                primaryButtonText: <span class=\"hljs-string\">\"OK\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = open,\n                OnClosed = _ => setOpen(<span class=\"hljs-literal\">false</span>),\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/basic-dialog.png\" alt=\"Basic ContentDialog with one button\"></p>\n<p><code>ContentDialog</code> is the modal. Pass a title, a content element, and the\nprimary button label. The factory returns a <code>ContentDialogElement</code>\nrecord; you spread additional init-only properties (<code>IsOpen</code>,\n<code>OnClosed</code>, secondary/close button text) with the <code>with { ... }</code>\nexpression. The dialog element lives in the tree at all times — it's a\nrecord, not a method call. <code>IsOpen = false</code> keeps it hidden; flipping\n<code>IsOpen = true</code> from a button handler opens it; the <code>OnClosed</code> callback\nflips it back when the user dismisses.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Property (via <code>with { ... }</code>)</th><th>Effect</th></tr></thead><tbody><tr><td><code>IsOpen</code></td><td>The controlled visibility flag.</td></tr><tr><td><code>PrimaryButtonText</code></td><td>Default-action button label. Set via the factory argument.</td></tr><tr><td><code>SecondaryButtonText</code></td><td>Optional second button (left of primary).</td></tr><tr><td><code>CloseButtonText</code></td><td>Optional close button. The \"X\" / Esc dismiss.</td></tr><tr><td><code>DefaultButton</code></td><td>Which button gets the accent color and the Enter binding (<code>Primary</code> / <code>Secondary</code> / <code>Close</code>).</td></tr><tr><td><code>OnClosed</code></td><td><code>Action&#x3C;ContentDialogResult></code> — <code>Primary</code> / <code>Secondary</code> / <code>None</code>.</td></tr><tr><td><code>OnOpened</code></td><td>Fires after the dialog finishes opening (useful for autofocus).</td></tr><tr><td><code>IsPrimaryButtonEnabled</code></td><td>Disable the primary action without removing it.</td></tr><tr><td><code>IsSecondaryButtonEnabled</code></td><td>Same for the secondary.</td></tr></tbody></table>\n<h3 id=\"three-button-dialogs\">Three-button dialogs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#three-button-dialogs\">#</a></h3>\n<p>The three-button shape (<code>Primary</code> + <code>Secondary</code> + <code>Close</code>) is the\ndefault for destructive confirmations. The convention from\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/dialogs\">Fluent UI</a>\nand macOS HIG agrees: place the destructive action in <code>Primary</code>, the\nnon-destructive cancel in <code>Secondary</code>, and route Esc to whichever the\n<code>DefaultButton</code> is set to:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ConfirmDialogDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> (result, setResult) = UseState(<span class=\"hljs-string\">\"(none)\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Confirmation with three buttons\"</span>),\n            Button(<span class=\"hljs-string\">\"Delete item…\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>)),\n            TextBlock(<span class=\"hljs-string\">$\"Last result: <span class=\"hljs-subst\">{result}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            ContentDialog(\n                <span class=\"hljs-string\">\"Delete this item?\"</span>,\n                TextBlock(<span class=\"hljs-string\">\"This action cannot be undone.\"</span>),\n                primaryButtonText: <span class=\"hljs-string\">\"Delete\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = open,\n                SecondaryButtonText = <span class=\"hljs-string\">\"Cancel\"</span>,\n                CloseButtonText = <span class=\"hljs-string\">\"Close\"</span>,\n                DefaultButton = ContentDialogButton.Close,\n                OnClosed = r =>\n                {\n                    setResult(r.ToString());\n                    setOpen(<span class=\"hljs-literal\">false</span>);\n                },\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/confirm-dialog.png\" alt=\"Three-button confirm dialog\"></p>\n<p>The <code>ContentDialogResult</code> value in <code>OnClosed</code> tells you which path the\nuser took. <code>Primary</code> and <code>Secondary</code> correspond to the labeled buttons;\n<code>None</code> covers Esc, Close button, and click-on-scrim dismissal — treat\nall three as Cancel.</p>\n<h3 id=\"gated-primary\">Gated primary<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#gated-primary\">#</a></h3>\n<p>When the dialog content is itself a form (rename file, set password),\nthe primary button should be disabled until the form is valid. Use\n<code>IsPrimaryButtonEnabled</code> rather than <code>.IsEnabled(false)</code> on a custom button\ninside the content — the dialog primary keeps the accent color, the\nkeyboard binding to Enter, and the focus trap that the WinUI dialog\nprovides:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DialogGatedPrimaryDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Primary disabled until input is valid\"</span>),\n            Button(<span class=\"hljs-string\">\"Rename file…\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>)),\n            ContentDialog(\n                <span class=\"hljs-string\">\"Rename file\"</span>,\n                VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"New filename:\"</span>),\n                    TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"untitled.txt\"</span>)\n                        .Width(<span class=\"hljs-number\">280</span>)),\n                primaryButtonText: <span class=\"hljs-string\">\"Rename\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = open,\n                SecondaryButtonText = <span class=\"hljs-string\">\"Cancel\"</span>,\n                <span class=\"hljs-comment\">// .IsPrimaryButtonEnabled drives the inline primary</span>\n                <span class=\"hljs-comment\">// disabled state without taking it out of tab order.</span>\n                IsPrimaryButtonEnabled = !<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(name),\n                OnClosed = _ => setOpen(<span class=\"hljs-literal\">false</span>),\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/dialog-gated-primary.png\" alt=\"Dialog with TextBox; primary disabled until non-empty\"></p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>ContentDialog.ShowAsync</code> is single-instance per <code>XamlRoot</code> — the WinUI\ncontrol raises <code>Show another ContentDialog before closing the previous</code>\nif two dialogs try to open at once. Reactor's controlled-<code>IsOpen</code>\npattern routes through the same constraint: opening dialog B from\ndialog A's <code>Primary</code> handler runs B's <code>IsOpen = true</code> set before A's\nclose completes, and the second show throws. Always close dialog A\nfirst (via <code>setOpenA(false)</code> and the dispatcher's commit), then open B\nin the next render — for example, queue B's open via a\n<a href=\"/effects/\"><code>UseEffect</code></a> keyed on A's <code>IsOpen</code> going false.</p>\n</blockquote>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/dialogs-and-flyouts/dialogs\">Dialogs</a>.</p>\n<h2 id=\"menuflyout\">MenuFlyout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#menuflyout\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">MenuFlyout(Element target, <span class=\"hljs-keyword\">params</span> MenuFlyoutItemBase[] items)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">MenuFlyoutDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (action, setAction) = UseState(<span class=\"hljs-string\">\"(none)\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"MenuFlyout — right-click or button-click\"</span>),\n            MenuFlyout(\n                Button(<span class=\"hljs-string\">\"Edit ▾\"</span>),\n                MenuItem(<span class=\"hljs-string\">\"Cut\"</span>,   () => setAction(<span class=\"hljs-string\">\"Cut\"</span>),   icon: <span class=\"hljs-string\">\"Cut\"</span>),\n                MenuItem(<span class=\"hljs-string\">\"Copy\"</span>,  () => setAction(<span class=\"hljs-string\">\"Copy\"</span>),  icon: <span class=\"hljs-string\">\"Copy\"</span>),\n                MenuItem(<span class=\"hljs-string\">\"Paste\"</span>, () => setAction(<span class=\"hljs-string\">\"Paste\"</span>), icon: <span class=\"hljs-string\">\"Paste\"</span>),\n                MenuSeparator(),\n                MenuSubItem(<span class=\"hljs-string\">\"Format\"</span>,\n                    ToggleMenuItem(<span class=\"hljs-string\">\"Bold\"</span>, isChecked: <span class=\"hljs-literal\">false</span>),\n                    ToggleMenuItem(<span class=\"hljs-string\">\"Italic\"</span>, isChecked: <span class=\"hljs-literal\">true</span>)),\n                MenuSeparator(),\n                MenuItem(<span class=\"hljs-string\">\"Delete…\"</span>, () => setAction(<span class=\"hljs-string\">\"Delete\"</span>))\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"Last action: <span class=\"hljs-subst\">{action}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/menu-flyout.png\" alt=\"MenuFlyout with submenu and separators\"></p>\n<p><code>MenuFlyout</code> attaches a popup menu to a target element. Click the\ntarget — the menu opens at it. Click outside the menu — it dismisses.\nThe factory takes the target as the first argument and the items as a\nvariadic tail; the result wraps the target so it appears in the tree at\nthe target's position.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Item factory</th><th>Shape</th></tr></thead><tbody><tr><td><code>MenuItem(label, onClick, icon?)</code></td><td>Standard menu item with a click handler.</td></tr><tr><td><code>MenuItem(Command)</code></td><td>Bind to a command. Label, icon, enabled-state, accelerator come from the command.</td></tr><tr><td><code>ToggleMenuItem(label, isChecked, onChanged?)</code></td><td>Two-state check item.</td></tr><tr><td><code>RadioMenuItem(label, groupName, isChecked, onClick?)</code></td><td>One of a radio group.</td></tr><tr><td><code>MenuSeparator()</code></td><td>Horizontal divider.</td></tr><tr><td><code>MenuSubItem(label, items...)</code></td><td>Nested submenu.</td></tr></tbody></table>\n<p>The same <code>Command</code> record that drives a <code>Button</code> plugs into\n<a href=\"/commanding/\"><code>MenuItem(Command)</code></a> — one declaration, three surfaces\n(button, menu, keyboard shortcut). See the\n<a href=\"#commanding-integration\">commanding integration pattern</a> below.</p>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/menus\">Menus and context menus</a>.</p>\n<h2 id=\"commandbarflyout\">CommandBarFlyout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#commandbarflyout\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">CommandBarFlyout(\n    Element target,\n    AppBarItemBase[]? primaryCommands = <span class=\"hljs-literal\">null</span>,\n    AppBarItemBase[]? secondaryCommands = <span class=\"hljs-literal\">null</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CommandBarFlyoutDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (action, setAction) = UseState(<span class=\"hljs-string\">\"(none)\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"CommandBarFlyout\"</span>),\n            CommandBarFlyout(\n                Button(<span class=\"hljs-string\">\"Selection ▾\"</span>),\n                primaryCommands: <span class=\"hljs-keyword\">new</span> AppBarItemBase[]\n                {\n                    AppBarButton(<span class=\"hljs-string\">\"Cut\"</span>,   () => setAction(<span class=\"hljs-string\">\"Cut\"</span>),   icon: <span class=\"hljs-string\">\"Cut\"</span>),\n                    AppBarButton(<span class=\"hljs-string\">\"Copy\"</span>,  () => setAction(<span class=\"hljs-string\">\"Copy\"</span>),  icon: <span class=\"hljs-string\">\"Copy\"</span>),\n                    AppBarButton(<span class=\"hljs-string\">\"Paste\"</span>, () => setAction(<span class=\"hljs-string\">\"Paste\"</span>), icon: <span class=\"hljs-string\">\"Paste\"</span>),\n                },\n                secondaryCommands: <span class=\"hljs-keyword\">new</span> AppBarItemBase[]\n                {\n                    AppBarButton(<span class=\"hljs-string\">\"Select All\"</span>, () => setAction(<span class=\"hljs-string\">\"Select All\"</span>)),\n                    AppBarButton(<span class=\"hljs-string\">\"Find\"</span>,       () => setAction(<span class=\"hljs-string\">\"Find\"</span>)),\n                }),\n            TextBlock(<span class=\"hljs-string\">$\"Last action: <span class=\"hljs-subst\">{action}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/command-bar-flyout.png\" alt=\"CommandBarFlyout with primary and secondary commands\"></p>\n<p><code>CommandBarFlyout</code> is the selection-context toolbar — the Cut / Copy /\nPaste row that floats over a text selection, the inline \"actions on\nthis row\" surface that some lists show. Primary commands render\ninline as icon buttons; secondary commands collapse into the overflow\nmenu. The same <code>AppBarItemBase</code> records that fill a <code>CommandBar</code> fill\nthis surface — the entire item array is interchangeable.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Item factory</th><th>Shape</th></tr></thead><tbody><tr><td><code>AppBarButton(label, onClick, icon?)</code></td><td>Standard tool-strip button.</td></tr><tr><td><code>AppBarButton(Command)</code></td><td>Command-driven variant.</td></tr><tr><td><code>AppBarToggleButton(...)</code></td><td>Two-state.</td></tr><tr><td><code>AppBarSeparator()</code></td><td>Vertical divider.</td></tr></tbody></table>\n<p>WinUI design page: <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/controls/command-bar-flyout\">Command bar flyout</a>.</p>\n<h2 id=\"popup\">Popup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#popup\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">Popup(Element child, <span class=\"hljs-built_in\">bool</span> isOpen = <span class=\"hljs-literal\">false</span>, Action? onClosed = <span class=\"hljs-literal\">null</span>)\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PopupDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-comment\">// Popup is a free-form positioned surface. Use it for overlays</span>\n        <span class=\"hljs-comment\">// that aren't dialogs or flyouts — color pickers, in-place</span>\n        <span class=\"hljs-comment\">// editors, custom tooltips.</span>\n        <span class=\"hljs-keyword\">var</span> popupContent = Border(\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"This is a Popup.\"</span>).Bold(),\n                TextBlock(<span class=\"hljs-string\">\"Click outside to dismiss.\"</span>)\n            ).Padding(<span class=\"hljs-number\">12</span>)\n        ).Background(<span class=\"hljs-string\">\"#FFFFFF\"</span>).WithBorder(<span class=\"hljs-string\">\"#888888\"</span>).CornerRadius(<span class=\"hljs-number\">6</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Popup\"</span>),\n            Button(open ? <span class=\"hljs-string\">\"Hide popup\"</span> : <span class=\"hljs-string\">\"Show popup\"</span>,\n                () => setOpen(!open)),\n            Popup(popupContent, isOpen: open,\n                onClosed: () => setOpen(<span class=\"hljs-literal\">false</span>))\n                .IsLightDismissEnabled()\n                .Offset(<span class=\"hljs-number\">120</span>, <span class=\"hljs-number\">0</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dialogs-and-flyouts/popup.png\" alt=\"Popup anchored to a button with light-dismiss enabled\"></p>\n<p><code>Popup</code> is the unstructured overlay — what you reach for when\n<code>ContentDialog</code>, <code>MenuFlyout</code>, and <code>CommandBarFlyout</code> are all the wrong\nshape. Color pickers, in-place rename editors, custom tooltips, dropdown\ncontent that isn't a menu — all of them are popups.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Effect</th></tr></thead><tbody><tr><td><code>.IsLightDismissEnabled(bool)</code></td><td>Close on outside click. Default <code>true</code>.</td></tr><tr><td><code>.Offset(horizontal, vertical)</code></td><td>Position relative to the popup's anchor point.</td></tr><tr><td><code>.Opened(Action)</code></td><td>Fires after the popup is on screen.</td></tr><tr><td><code>.Closed(Action)</code></td><td>Fires after the popup leaves the screen.</td></tr><tr><td><code>.Set(p => p.PlacementTarget = ...)</code></td><td>Anchor to a specific element.</td></tr></tbody></table>\n<p>The popup itself does not provide a focus trap or an ARIA dialog role.\nFor modal-feeling popups, wrap the content in your own focus management\n(<code>UseFocusTrap</code> from <a href=\"/accessibility/\">accessibility.md</a>) and set\n<code>AutomationProperties.Role = Dialog</code> on the root through <code>.Set(...)</code>.\nFor non-modal popups (tooltips, hover cards), neither is needed.</p>\n<p>WinUI primitives reference: <a href=\"https://learn.microsoft.com/en-us/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.controls.primitives.popup\">Popup class</a>.</p>\n<h2 id=\"commanding-integration\">Commanding integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#commanding-integration\">#</a></h2>\n<p>The cleanest way to keep dialog logic out of every surface that opens\nit is to put the action behind a <code>Command</code>. The same record lights up\na button, a menu item, and (with <code>.Accelerator</code>) a keyboard shortcut —\nauthoring the dialog primary as <code>Command.Execute</code> is the natural\nextension:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CommandingIntegrationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// One Command drives the button, the menu item, and (via</span>\n        <span class=\"hljs-comment\">// .Accelerator) Ctrl+S. The same Command can light up an</span>\n        <span class=\"hljs-comment\">// AppBarButton in a CommandBarFlyout too — same declaration,</span>\n        <span class=\"hljs-comment\">// three surfaces.</span>\n        <span class=\"hljs-keyword\">var</span> (saved, setSaved) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">var</span> save = <span class=\"hljs-keyword\">new</span> Command\n        {\n            Label = <span class=\"hljs-string\">\"Save\"</span>,\n            Execute = () => setSaved(<span class=\"hljs-literal\">true</span>),\n            CanExecute = !saved,\n            Icon = SymbolIcon(<span class=\"hljs-string\">\"Save\"</span>),\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"One Command, two surfaces\"</span>),\n            Button(save),                          <span class=\"hljs-comment\">// primary CTA</span>\n            MenuFlyout(\n                Button(<span class=\"hljs-string\">\"File ▾\"</span>),\n                MenuItem(save),                    <span class=\"hljs-comment\">// menu duplicate</span>\n                MenuSeparator(),\n                MenuItem(<span class=\"hljs-string\">\"Reset\"</span>, () => setSaved(<span class=\"hljs-literal\">false</span>))),\n            TextBlock(saved ? <span class=\"hljs-string\">\"Saved.\"</span> : <span class=\"hljs-string\">\"Unsaved changes.\"</span>)\n                .Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><code>Command.CanExecute = false</code> greys out every surface bound to that\ncommand — the menu item, the button, the keyboard binding all disable\nin one place. Without commanding, you would duplicate the\n<code>isEnabled</code> derivation per surface. See <a href=\"/commanding/\">commanding.md</a>\nfor the full pattern (async commands, parameterized <code>Command&#x3C;T></code>,\nre-entrance guards).</p>\n<h2 id=\"focus-and-aria\">Focus and ARIA<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-and-aria\">#</a></h2>\n<p>WinUI's <code>ContentDialog</code> ships a focus trap and the <code>Dialog</code> ARIA role\nout of the box. When the dialog opens, focus moves to the default\nbutton; tabbing wraps within the dialog; Esc closes via the close\nbutton. Screen readers announce the dialog title as the\n<code>AutomationProperties.Name</code>. None of this needs configuration — pass\nthe title and content, and the accessibility surface is correct.</p>\n<p><code>MenuFlyout</code> and <code>CommandBarFlyout</code> apply the same focus rules at the\nWinUI level: opening moves focus to the first enabled item, arrow keys\nnavigate, Esc dismisses, focus returns to the originating element on\nclose.</p>\n<p>The exception is <code>Popup</code>. The popup has none of the above; it is a\npositioning primitive. For a popup that should behave like a modal,\nwrap its content with <code>UseFocusTrap</code> and apply the dialog role\nexplicitly:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Inside a popup that should behave modally:</span>\n<span class=\"hljs-keyword\">var</span> trap = UseFocusTrap();\n<span class=\"hljs-keyword\">return</span> Popup(\n    Border(content).WithFocusTrap(trap),\n    isOpen: open);\n</code></pre>\n<p>The trap shape and the ARIA mapping are documented in\n<a href=\"/accessibility/\">accessibility.md</a>.</p>\n<h2 id=\"dismiss-reasons\">Dismiss reasons<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dismiss-reasons\">#</a></h2>\n<p>Every dialog and flyout can close five different ways. Treat them as\nequivalent in your handlers — there is no \"real cancel\" vs. \"implicit\ncancel\":</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Surface</th><th>Primary close paths</th></tr></thead><tbody><tr><td><code>ContentDialog</code></td><td>Primary button, Secondary button, Close button, Esc, click-on-scrim (returns <code>None</code>).</td></tr><tr><td><code>MenuFlyout</code></td><td>Item click (the action), outside click, Esc.</td></tr><tr><td><code>CommandBarFlyout</code></td><td>Command click, outside click, Esc.</td></tr><tr><td><code>Popup</code></td><td>Outside click (when <code>LightDismiss = true</code>), explicit <code>IsOpen = false</code>.</td></tr></tbody></table>\n<p>For <code>ContentDialog</code>, the <code>OnClosed</code> callback is the single dismiss\nnotification — read <code>ContentDialogResult</code> to discover which path the\nuser took, but accept that <code>None</code> is the user's right.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Factory</th><th>Open/close</th><th>Lifecycle</th></tr></thead><tbody><tr><td><code>ContentDialogElement</code></td><td><code>ContentDialog(title, content, primaryButtonText)</code></td><td><code>IsOpen</code> (init), <code>OnClosed(result)</code></td><td><code>OnOpened</code>, <code>OnClosed</code></td></tr><tr><td><code>MenuFlyoutElement</code></td><td><code>MenuFlyout(target, items...)</code></td><td>Auto on target click</td><td>n/a</td></tr><tr><td><code>CommandBarFlyoutElement</code></td><td><code>CommandBarFlyout(target, primary?, secondary?)</code></td><td>Auto on target click</td><td>n/a</td></tr><tr><td><code>PopupElement</code></td><td><code>Popup(child, isOpen?, onClosed?)</code></td><td><code>isOpen</code> arg or <code>.IsOpen</code> init</td><td><code>.Opened</code>, <code>.Closed</code></td></tr></tbody></table>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"dialog-driven-async-command\">Dialog-driven async command<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dialog-driven-async-command\">#</a></h3>\n<p>Async confirmations — \"Are you sure?\" → \"Doing it…\" → \"Done.\" — fit a\nsingle <code>Command</code> with <code>ExecuteAsync</code>. The dialog primary triggers the\ncommand; the command tracks <code>IsExecuting</code> via <a href=\"/commanding/\"><code>UseCommand</code></a>;\nthe dialog's primary disables itself while the action runs. The user\ncan't double-tap the destructive button:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> delete = ctx.UseCommand(<span class=\"hljs-keyword\">new</span> Command\n{\n    Label = <span class=\"hljs-string\">\"Delete\"</span>,\n    ExecuteAsync = <span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">await</span> api.DeleteAsync(item.Id);\n        setOpen(<span class=\"hljs-literal\">false</span>);\n    },\n    CanExecute = !item.IsLocked,\n});\n\nContentDialog(<span class=\"hljs-string\">\"Delete?\"</span>, body, primaryButtonText: <span class=\"hljs-string\">\"Delete\"</span>) <span class=\"hljs-keyword\">with</span>\n{\n    IsOpen = open,\n    SecondaryButtonText = <span class=\"hljs-string\">\"Cancel\"</span>,\n    IsPrimaryButtonEnabled = delete.IsEnabled,\n    OnClosed = r =>\n    {\n        <span class=\"hljs-keyword\">if</span> (r == ContentDialogResult.Primary &#x26;&#x26; delete.IsEnabled)\n            delete.Execute?.Invoke();\n        <span class=\"hljs-keyword\">else</span>\n            setOpen(<span class=\"hljs-literal\">false</span>);\n    },\n}\n</code></pre>\n<p>This mirrors the <a href=\"/recipes/modal-dialog/\"><code>recipes/modal-dialog</code></a>\nrecipe, which builds the same shape out of an inline modal instead of\nthe WinUI control.</p>\n<h3 id=\"right-click-on-a-list-row\">Right-click on a list row<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#right-click-on-a-list-row\">#</a></h3>\n<p><code>MenuFlyout</code> attaches to its target. Inside a <a href=\"/collections/\"><code>ListView</code></a>\nrow template, wrap the row in <code>MenuFlyout(rowContent, items...)</code> and\nthe menu binds to that row instance. Tie the menu items to the row's\ndata — <code>Command&#x3C;T></code> lets a single command apply to every row's\ncontext:</p>\n<pre><code class=\"hljs language-csharp\">ListView(items, x => x.Id, (item, _) =>\n    MenuFlyout(\n        RowContent(item),\n        MenuItem(deleteCommand, item),\n        MenuItem(renameCommand, item),\n        MenuSeparator(),\n        MenuItem(propertiesCommand, item)))\n</code></pre>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"imperatively-opening-a-dialog-from-an-event-handler\">Imperatively opening a dialog from an event handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#imperatively-opening-a-dialog-from-an-event-handler\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, <span class=\"hljs-keyword\">async</span> () =>\n{\n    <span class=\"hljs-keyword\">var</span> dialog = <span class=\"hljs-keyword\">new</span> ContentDialog { Title = <span class=\"hljs-string\">\"Confirm\"</span> };\n    <span class=\"hljs-keyword\">await</span> dialog.ShowAsync();\n})\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BasicDialogDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Basic ContentDialog\"</span>),\n            Button(<span class=\"hljs-string\">\"Show dialog\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>)),\n            <span class=\"hljs-comment\">// Dialog lives in the tree at all times. IsOpen controls</span>\n            <span class=\"hljs-comment\">// visibility; OnClosed flips it back when the user dismisses.</span>\n            ContentDialog(\n                <span class=\"hljs-string\">\"Welcome\"</span>,\n                TextBlock(<span class=\"hljs-string\">\"Thank you for trying Reactor.\"</span>),\n                primaryButtonText: <span class=\"hljs-string\">\"OK\"</span>) <span class=\"hljs-keyword\">with</span>\n            {\n                IsOpen = open,\n                OnClosed = _ => setOpen(<span class=\"hljs-literal\">false</span>),\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>The imperative path drops out of the Reactor render model — the dialog\nisn't in the tree, so it can't be styled by the parent's theme, can't\nbe tested by the renderer fixture from <a href=\"/testing/\">testing.md</a>, and\ncan't share state with the rest of the component. The controlled\n<code>IsOpen</code> flag is the canonical pattern.</p>\n<h3 id=\"reusing-a-single-dialog-for-unrelated-decisions\">Reusing a single dialog for unrelated decisions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reusing-a-single-dialog-for-unrelated-decisions\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n<span class=\"hljs-keyword\">var</span> (dialogType, setDialogType) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n<span class=\"hljs-comment\">// One dialog, branching on dialogType for title/body/buttons.</span>\n</code></pre>\n<p>Two decisions, two dialogs. The branching version makes every dialog\nstate read the dialog-type string, which leaks dialog concerns into\nyour component's data model and makes the testing fixture fragile.\nEach modal action gets its own dialog element with its own state pair\n— the cost is two <code>useState</code> calls and an extra <code>ContentDialog</code>\ndeclaration, both nearly free.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pair every dialog trigger with a <code>Command</code>.</strong> The trigger lives in\none or more surfaces (toolbar, menu, keyboard shortcut); the command\ngives you one place to wire enabled-state, telemetry, and undo. The\ndialog primary then routes through <code>command.Execute</code>.</p>\n<p><strong>Treat <code>ContentDialogResult.None</code> as Cancel.</strong> Esc, click-on-scrim,\nand the close-X all return <code>None</code>. Don't try to distinguish them — if\nthe user didn't pick <code>Primary</code> or <code>Secondary</code>, they didn't pick.</p>\n<p><strong>Don't reach for <code>Popup</code> until the three structured surfaces have\nfailed.</strong> <code>ContentDialog</code>, <code>MenuFlyout</code>, and <code>CommandBarFlyout</code> carry\ntheir own focus and ARIA semantics. <code>Popup</code> doesn't, and you have to\nre-implement focus trapping yourself.</p>\n<p><strong>Open the dialog once per decision, not once per render.</strong> If\n<code>open</code> is in your state, only the button handler should flip it true.\nA <code>UseEffect</code> that does <code>setOpen(true)</code> on a condition will reopen the\ndialog after every dismiss until the condition changes — gate the\neffect with a \"user-acknowledged\" flag.</p>\n<p><strong>Show a long-running action's progress <em>inside</em> the dialog, not\nafter.</strong> Closing a dialog mid-action is jarring. Wire the dialog\nprimary to an async <a href=\"/commanding/\"><code>Command</code></a>, let <code>IsExecuting</code> swap\nthe primary label for a spinner, then close on completion.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/status-and-info/\">Status and Info</a></strong> — Previous: non-interactive feedback (InfoBar, ProgressRing, TeachingTip).</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — Next: DataGrid and the data-source pipeline.</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — The Command record that drives buttons, menu items, dialogs, and shortcuts.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Focus traps, ARIA roles, and dialog landmark behavior.</li>\n<li><strong><a href=\"/recipes/modal-dialog/\">Recipes: Modal dialog</a></strong> — End-to-end recipe building a confirmation modal.</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Dialogs And Flyouts . Dialog and flyout surfaces interrupt the user. That makes them the most costly UI primitive in the app's attention budget — every modal dialog steals focus, every flyout dismisses on outside click, every popup arrives without a route back. Microsoft.UI.Reactor (Reactor) treats dialogs and flyouts as controlled components: you own the open/closed boolean, the component tree always contains the dialog element, and the visibility is a state flag the user-driven event handlers flip back. That is the same shape as TextBox from Forms , and it makes the dialog testable — the dialog is open if and only if its driving state says so. Read this page when you are reaching for ContentDialog for a Save/Discard choice, MenuFlyout for a right-click menu, CommandBarFlyout for a selection toolbar, or Popup for the bespoke overlay that none of the above covers. Pair every dialog with a corresponding entry in commanding.md — the same Command should usually back the trigger (button or menu) and the dialog's primary action. Dialogs and Flyouts # Four primitives sit on this page. Pick by shape, not by aesthetic: Control Shape Dismiss Use when ContentDialog Modal, screen-centered, three buttons Button click The user has to make a choice before continuing. MenuFlyout Pop-up menu next to a target Outside click / Esc Right-click or chevron menu of discrete actions. CommandBarFlyout Mini-toolbar next to a target Outside click / Esc Selection context — \"what to do with this thing\". Popup Free-form overlay Light-dismiss / explicit Anything the three above can't render — color picker, in-place editor, custom tooltip. ContentDialog # ContentDialog( string title, Element content, string primaryButtonText = \"OK\" ) class BasicDialogDemo : Component { public override Element Render () { var (open, setOpen) = UseState( false ); return VStack( 8 , SubHeading( \"Basic ContentDialog\" ), Button( \"Show dialog\" , () => setOpen( true )), // Dialog lives in the tree at all times. IsOpen controls // visibility; OnClosed flips it back when the user dismisses. ContentDialog( \"Welcome\" , TextBlock( \"Thank you for trying Reactor.\" ), primaryButtonText: \"OK\" ) with { IsOpen = open, OnClosed = _ => setOpen( false ), } ).Padding( 24 ); } } ContentDialog is the modal. Pass a title, a content element, and the primary button label. The factory returns a ContentDialogElement record; you spread additional init-only properties ( IsOpen , OnClosed , secondary/close button text) with the with { ... } expression. The dialog element lives in the tree at all times — it's a record, not a method call. IsOpen = false keeps it hidden; flipping IsOpen = true from a button handler opens it; the OnClosed callback flips it back when the user dismisses. Property (via with { ... } ) Effect IsOpen The controlled visibility flag. PrimaryButtonText Default-action button label. Set via the factory argument. SecondaryButtonText Optional second button (left of primary). CloseButtonText Optional close button. The \"X\" / Esc dismiss. DefaultButton Which button gets the accent color and the Enter binding ( Primary / Secondary / Close ). OnClosed Action&#x3C;ContentDialogResult> — Primary / Secondary / None . OnOpened Fires after the dialog finishes opening (useful for autofocus). IsPrimaryButtonEnabled Disable the primary action without removing it. IsSecondaryButtonEnabled Same for the secondary. Three-button dialogs # The three-button shape ( Primary + Secondary + Close ) is the default for destructive confirmations. The convention from Fluent UI and macOS HIG agrees: place the destructive action in Primary , the non-destructive cancel in Secondary , and route Esc to whichever the DefaultButton is set to: class ConfirmDialogDemo : Component { public override Element Render () { var (open, setOpen) = UseState( false ); var (result, setResult) = UseState( \"(none)\" ); return VStack( 8 , SubHeading( \"Confirmation with three buttons\" ), Button( \"Delete item…\" , () => setOpen( true )), TextBlock( $\"Last result: {result} \" ).Opacity( 0.6 ), ContentDialog( \"Delete this item?\" , TextBlock( \"This action cannot be undone.\" ), primaryButtonText: \"Delete\" ) with { IsOpen = open, SecondaryButtonText = \"Cancel\" , CloseButtonText = \"Close\" , DefaultButton = ContentDialogButton.Close, OnClosed = r => { setResult(r.ToString()); setOpen( false ); }, } ).Padding( 24 ); } } The ContentDialogResult value in OnClosed tells you which path the user took. Primary and Secondary correspond to the labeled buttons; None covers Esc, Close button, and click-on-scrim dismissal — treat all three as Cancel. Gated primary # When the dialog content is itself a form (rename file, set password), the primary button should be disabled until the form is valid. Use IsPrimaryButtonEnabled rather than .IsEnabled(false) on a custom button inside the content — the dialog primary keeps the accent color, the keyboard binding to Enter, and the focus trap that the WinUI dialog provides: class DialogGatedPrimaryDemo : Component { public override Element Render () { var (open, setOpen) = UseState( false ); var (name, setName) = UseState( \"\" ); return VStack( 8 , SubHeading( \"Primary disabled until input is valid\" ), Button( \"Rename file…\" , () => setOpen( true )), ContentDialog( \"Rename file\" , VStack( 8 , TextBlock( \"New filename:\" ), TextBox(name, setName, placeholderText: \"untitled.txt\" ) .Width( 280 )), primaryButtonText: \"Rename\" ) with { IsOpen = open, SecondaryButtonText = \"Cancel\" , // .IsPrimaryButtonEnabled drives the inline primary // disabled state without taking it out of tab order. IsPrimaryButtonEnabled = ! string .IsNullOrWhiteSpace(name), OnClosed = _ => setOpen( false ), } ).Padding( 24 ); } } Caveat: ContentDialog.ShowAsync is single-instance per XamlRoot — the WinUI control raises Show another ContentDialog before closing the previous if two dialogs try to open at once. Reactor's controlled- IsOpen pattern routes through the same constraint: opening dialog B from dialog A's Primary handler runs B's IsOpen = true set before A's close completes, and the second show throws. Always close dialog A first (via setOpenA(false) and the dispatcher's commit), then open B in the next render — for example, queue B's open via a UseEffect keyed on A's IsOpen going false. WinUI design page: Dialogs . MenuFlyout # MenuFlyout(Element target, params MenuFlyoutItemBase[] items) class MenuFlyoutDemo : Component { public override Element Render () { var (action, setAction) = UseState( \"(none)\" ); return VStack( 8 , SubHeading( \"MenuFlyout — right-click or button-click\" ), MenuFlyout( Button( \"Edit ▾\" ), MenuItem( \"Cut\" , () => setAction( \"Cut\" ), icon: \"Cut\" ), MenuItem( \"Copy\" , () => setAction( \"Copy\" ), icon: \"Copy\" ), MenuItem( \"Paste\" , () => setAction( \"Paste\" ), icon: \"Paste\" ), MenuSeparator(), MenuSubItem( \"Format\" , ToggleMenuItem( \"Bold\" , isChecked: false ), ToggleMenuItem( \"Italic\" , isChecked: true )), MenuSeparator(), MenuItem( \"Delete…\" , () => setAction( \"Delete\" )) ), TextBlock( $\"Last action: {action} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } MenuFlyout attaches a popup menu to a target element. Click the target — the menu opens at it. Click outside the menu — it dismisses. The factory takes the target as the first argument and the items as a variadic tail; the result wraps the target so it appears in the tree at the target's position. Item factory Shape MenuItem(label, onClick, icon?) Standard menu item with a click handler. MenuItem(Command) Bind to a command. Label, icon, enabled-state, accelerator come from the command. ToggleMenuItem(label, isChecked, onChanged?) Two-state check item. RadioMenuItem(label, groupName, isChecked, onClick?) One of a radio group. MenuSeparator() Horizontal divider. MenuSubItem(label, items...) Nested submenu. The same Command record that drives a Button plugs into MenuItem(Command) — one declaration, three surfaces (button, menu, keyboard shortcut). See the commanding integration pattern below. WinUI design page: Menus and context menus . CommandBarFlyout # CommandBarFlyout( Element target, AppBarItemBase[]? primaryCommands = null , AppBarItemBase[]? secondaryCommands = null ) class CommandBarFlyoutDemo : Component { public override Element Render () { var (action, setAction) = UseState( \"(none)\" ); return VStack( 8 , SubHeading( \"CommandBarFlyout\" ), CommandBarFlyout( Button( \"Selection ▾\" ), primaryCommands: new AppBarItemBase[] { AppBarButton( \"Cut\" , () => setAction( \"Cut\" ), icon: \"Cut\" ), AppBarButton( \"Copy\" , () => setAction( \"Copy\" ), icon: \"Copy\" ), AppBarButton( \"Paste\" , () => setAction( \"Paste\" ), icon: \"Paste\" ), }, secondaryCommands: new AppBarItemBase[] { AppBarButton( \"Select All\" , () => setAction( \"Select All\" )), AppBarButton( \"Find\" , () => setAction( \"Find\" )), }), TextBlock( $\"Last action: {action} \" ).Opacity( 0.6 ) ).Padding( 24 ); } } CommandBarFlyout is the selection-context toolbar — the Cut / Copy / Paste row that floats over a text selection, the inline \"actions on this row\" surface that some lists show. Primary commands render inline as icon buttons; secondary commands collapse into the overflow menu. The same AppBarItemBase records that fill a CommandBar fill this surface — the entire item array is interchangeable. Item factory Shape AppBarButton(label, onClick, icon?) Standard tool-strip button. AppBarButton(Command) Command-driven variant. AppBarToggleButton(...) Two-state. AppBarSeparator() Vertical divider. WinUI design page: Command bar flyout . Popup # Popup(Element child, bool isOpen = false , Action? onClosed = null ) class PopupDemo : Component { public override Element Render () { var (open, setOpen) = UseState( false ); // Popup is a free-form positioned surface. Use it for overlays // that aren't dialogs or flyouts — color pickers, in-place // editors, custom tooltips. var popupContent = Border( VStack( 8 , TextBlock( \"This is a Popup.\" ).Bold(), TextBlock( \"Click outside to dismiss.\" ) ).Padding( 12 ) ).Background( \"#FFFFFF\" ).WithBorder( \"#888888\" ).CornerRadius( 6 ); return VStack( 8 , SubHeading( \"Popup\" ), Button(open ? \"Hide popup\" : \"Show popup\" , () => setOpen(!open)), Popup(popupContent, isOpen: open, onClosed: () => setOpen( false )) .IsLightDismissEnabled() .Offset( 120 , 0 ) ).Padding( 24 ); } } Popup is the unstructured overlay — what you reach for when ContentDialog , MenuFlyout , and CommandBarFlyout are all the wrong shape. Color pickers, in-place rename editors, custom tooltips, dropdown content that isn't a menu — all of them are popups. Fluent Effect .IsLightDismissEnabled(bool) Close on outside click. Default true . .Offset(horizontal, vertical) Position relative to the popup's anchor point. .Opened(Action) Fires after the popup is on screen. .Closed(Action) Fires after the popup leaves the screen. .Set(p => p.PlacementTarget = ...) Anchor to a specific element. The popup itself does not provide a focus trap or an ARIA dialog role. For modal-feeling popups, wrap the content in your own focus management ( UseFocusTrap from accessibility.md ) and set AutomationProperties.Role = Dialog on the root through .Set(...) . For non-modal popups (tooltips, hover cards), neither is needed. WinUI primitives reference: Popup class . Commanding integration # The cleanest way to keep dialog logic out of every surface that opens it is to put the action behind a Command . The same record lights up a button, a menu item, and (with .Accelerator ) a keyboard shortcut — authoring the dialog primary as Command.Execute is the natural extension: class CommandingIntegrationDemo : Component { public override Element Render () { // One Command drives the button, the menu item, and (via // .Accelerator) Ctrl+S. The same Command can light up an // AppBarButton in a CommandBarFlyout too — same declaration, // three surfaces. var (saved, setSaved) = UseState( false ); var save = new Command { Label = \"Save\" , Execute = () => setSaved( true ), CanExecute = !saved, Icon = SymbolIcon( \"Save\" ), }; return VStack( 8 , SubHeading( \"One Command, two surfaces\" ), Button(save), // primary CTA MenuFlyout( Button( \"File ▾\" ), MenuItem(save), // menu duplicate MenuSeparator(), MenuItem( \"Reset\" , () => setSaved( false ))), TextBlock(saved ? \"Saved.\" : \"Unsaved changes.\" ) .Opacity( 0.6 ) ).Padding( 24 ); } } Command.CanExecute = false greys out every surface bound to that command — the menu item, the button, the keyboard binding all disable in one place. Without commanding, you would duplicate the isEnabled derivation per surface. See commanding.md for the full pattern (async commands, parameterized Command&#x3C;T> , re-entrance guards). Focus and ARIA # WinUI's ContentDialog ships a focus trap and the Dialog ARIA role out of the box. When the dialog opens, focus moves to the default button; tabbing wraps within the dialog; Esc closes via the close button. Screen readers announce the dialog title as the AutomationProperties.Name . None of this needs configuration — pass the title and content, and the accessibility surface is correct. MenuFlyout and CommandBarFlyout apply the same focus rules at the WinUI level: opening moves focus to the first enabled item, arrow keys navigate, Esc dismisses, focus returns to the originating element on close. The exception is Popup . The popup has none of the above; it is a positioning primitive. For a popup that should behave like a modal, wrap its content with UseFocusTrap and apply the dialog role explicitly: // Inside a popup that should behave modally: var trap = UseFocusTrap(); return Popup( Border(content).WithFocusTrap(trap), isOpen: open); The trap shape and the ARIA mapping are documented in accessibility.md . Dismiss reasons # Every dialog and flyout can close five different ways. Treat them as equivalent in your handlers — there is no \"real cancel\" vs. \"implicit cancel\": Surface Primary close paths ContentDialog Primary button, Secondary button, Close button, Esc, click-on-scrim (returns None ). MenuFlyout Item click (the action), outside click, Esc. CommandBarFlyout Command click, outside click, Esc. Popup Outside click (when LightDismiss = true ), explicit IsOpen = false . For ContentDialog , the OnClosed callback is the single dismiss notification — read ContentDialogResult to discover which path the user took, but accept that None is the user's right. Reference # Element Factory Open/close Lifecycle ContentDialogElement ContentDialog(title, content, primaryButtonText) IsOpen (init), OnClosed(result) OnOpened , OnClosed MenuFlyoutElement MenuFlyout(target, items...) Auto on target click n/a CommandBarFlyoutElement CommandBarFlyout(target, primary?, secondary?) Auto on target click n/a PopupElement Popup(child, isOpen?, onClosed?) isOpen arg or .IsOpen init .Opened , .Closed Patterns # Dialog-driven async command # Async confirmations — \"Are you sure?\" → \"Doing it…\" → \"Done.\" — fit a single Command with ExecuteAsync . The dialog primary triggers the command; the command tracks IsExecuting via UseCommand ; the dialog's primary disables itself while the action runs. The user can't double-tap the destructive button: var delete = ctx.UseCommand( new Command { Label = \"Delete\" , ExecuteAsync = async () => { await api.DeleteAsync(item.Id); setOpen( false ); }, CanExecute = !item.IsLocked, }); ContentDialog( \"Delete?\" , body, primaryButtonText: \"Delete\" ) with { IsOpen = open, SecondaryButtonText = \"Cancel\" , IsPrimaryButtonEnabled = delete.IsEnabled, OnClosed = r => { if (r == ContentDialogResult.Primary &#x26;&#x26; delete.IsEnabled) delete.Execute?.Invoke(); else setOpen( false ); }, } This mirrors the recipes/modal-dialog recipe, which builds the same shape out of an inline modal instead of the WinUI control. Right-click on a list row # MenuFlyout attaches to its target. Inside a ListView row template, wrap the row in MenuFlyout(rowContent, items...) and the menu binds to that row instance. Tie the menu items to the row's data — Command&#x3C;T> lets a single command apply to every row's context: ListView(items, x => x.Id, (item, _) => MenuFlyout( RowContent(item), MenuItem(deleteCommand, item), MenuItem(renameCommand, item), MenuSeparator(), MenuItem(propertiesCommand, item))) Common Mistakes # Imperatively opening a dialog from an event handler # // Don't: Button( \"Save\" , async () => { var dialog = new ContentDialog { Title = \"Confirm\" }; await dialog.ShowAsync(); }) class BasicDialogDemo : Component { public override Element Render () { var (open, setOpen) = UseState( false ); return VStack( 8 , SubHeading( \"Basic ContentDialog\" ), Button( \"Show dialog\" , () => setOpen( true )), // Dialog lives in the tree at all times. IsOpen controls // visibility; OnClosed flips it back when the user dismisses. ContentDialog( \"Welcome\" , TextBlock( \"Thank you for trying Reactor.\" ), primaryButtonText: \"OK\" ) with { IsOpen = open, OnClosed = _ => setOpen( false ), } ).Padding( 24 ); } } The imperative path drops out of the Reactor render model — the dialog isn't in the tree, so it can't be styled by the parent's theme, can't be tested by the renderer fixture from testing.md , and can't share state with the rest of the component. The controlled IsOpen flag is the canonical pattern. Reusing a single dialog for unrelated decisions # // Don't: var (open, setOpen) = UseState( false ); var (dialogType, setDialogType) = UseState( \"\" ); // One dialog, branching on dialogType for title/body/buttons. Two decisions, two dialogs. The branching version makes every dialog state read the dialog-type string, which leaks dialog concerns into your component's data model and makes the testing fixture fragile. Each modal action gets its own dialog element with its own state pair — the cost is two useState calls and an extra ContentDialog declaration, both nearly free. Tips # Pair every dialog trigger with a Command . The trigger lives in one or more surfaces (toolbar, menu, keyboard shortcut); the command gives you one place to wire enabled-state, telemetry, and undo. The dialog primary then routes through command.Execute . Treat ContentDialogResult.None as Cancel. Esc, click-on-scrim, and the close-X all return None . Don't try to distinguish them — if the user didn't pick Primary or Secondary , they didn't pick. Don't reach for Popup until the three structured surfaces have failed. ContentDialog , MenuFlyout , and CommandBarFlyout carry their own focus and ARIA semantics. Popup doesn't, and you have to re-implement focus trapping yourself. Open the dialog once per decision, not once per render. If open is in your state, only the button handler should flip it true. A UseEffect that does setOpen(true) on a condition will reopen the dialog after every dismiss until the condition changes — gate the effect with a \"user-acknowledged\" flag. Show a long-running action's progress inside the dialog, not after. Closing a dialog mid-action is jarring. Wire the dialog primary to an async Command , let IsExecuting swap the primary label for a spinner, then close on completion. Next Steps # Status and Info — Previous: non-interactive feedback (InfoBar, ProgressRing, TeachingTip). Data System — Next: DataGrid and the data-source pipeline. Commanding — The Command record that drives buttons, menu items, dialogs, and shortcuts. Accessibility — Focus traps, ARIA roles, and dialog landmark behavior. Recipes: Modal dialog — End-to-end recipe building a confirmation modal.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/dialogs-and-flyouts.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/dialogs-and-flyouts.md",
      "headings": [
        {
          "depth": 2,
          "id": "contentdialog",
          "text": "ContentDialog"
        },
        {
          "depth": 3,
          "id": "three-button-dialogs",
          "text": "Three-button dialogs"
        },
        {
          "depth": 3,
          "id": "gated-primary",
          "text": "Gated primary"
        },
        {
          "depth": 2,
          "id": "menuflyout",
          "text": "MenuFlyout"
        },
        {
          "depth": 2,
          "id": "commandbarflyout",
          "text": "CommandBarFlyout"
        },
        {
          "depth": 2,
          "id": "popup",
          "text": "Popup"
        },
        {
          "depth": 2,
          "id": "commanding-integration",
          "text": "Commanding integration"
        },
        {
          "depth": 2,
          "id": "focus-and-aria",
          "text": "Focus and ARIA"
        },
        {
          "depth": 2,
          "id": "dismiss-reasons",
          "text": "Dismiss reasons"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "dialog-driven-async-command",
          "text": "Dialog-driven async command"
        },
        {
          "depth": 3,
          "id": "right-click-on-a-list-row",
          "text": "Right-click on a list row"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "imperatively-opening-a-dialog-from-an-event-handler",
          "text": "Imperatively opening a dialog from an event handler"
        },
        {
          "depth": 3,
          "id": "reusing-a-single-dialog-for-unrelated-decisions",
          "text": "Reusing a single dialog for unrelated decisions"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Data System",
      "path": "data-system.md",
      "slug": "/data-system/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s <code>DataGrid&#x3C;T></code> is a virtualized table that renders rows lazily from an\n<code>IDataSource&#x3C;T></code>. The source is the contract, not the data: it returns\npages keyed by sort, filter, and search state, declares its\n<code>Capabilities</code> (server-side sort? mutate?), and yields a stable\n<a href=\"#\"><code>RowKey</code></a> per item. The grid is a thin view over that contract — it\nasks the source for the visible window on every render, diffs the\nreturned rows by key, and renders only what changed. This is the\nopposite of AG Grid's \"row data + column defs\" array-shaped input and\ncloser in spirit to TanStack Table's headless split: source owns data\naccess, <a href=\"#headless-testing-with-datagridstate\"><code>DataGridState&#x3C;T></code></a> owns\nsort/selection/edit state, and the grid is the presentation. Two\ncolumns of <code>Column&#x3C;T>(...)</code> definitions plus a <code>ListDataSource&#x3C;T></code>\nwrapper is the smallest working grid; an <code>ObservableListDataSource&#x3C;T></code>\nswap turns it live; a custom <code>IDataSource&#x3C;T></code> against your REST or\nGraphQL endpoint turns it into a server-driven grid without changing\nthe column code. Read the source section first — every other section\non this page is about how the grid asks more of it.</p>\n<h1 id=\"data-system\">Data System<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data-system\">#</a></h1>\n<p>Reactor's data system provides a virtualized <code>DataGrid&#x3C;T></code> backed by a\npluggable data source abstraction. You define columns (or auto-generate\nthem), connect a data source, and the grid handles sorting, filtering,\nsearching, selection, and inline editing.</p>\n<h2 id=\"data-sources\">Data Sources<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data-sources\">#</a></h2>\n<p>All data flows through <code>IDataSource&#x3C;T></code> — an async, page-based abstraction.\nYou never pass raw lists to the grid; instead you wrap your data in a\nsource that declares its capabilities:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DataSourceExample</span>\n{\n    <span class=\"hljs-comment\">// Wrap an in-memory list — supports client-side sort, filter, search</span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> ListDataSource&#x3C;Product> <span class=\"hljs-title\">CreateSource</span>()</span> =>\n        <span class=\"hljs-keyword\">new</span>(SampleProducts.Items, p => (RowKey)p.Id);\n\n    <span class=\"hljs-comment\">// source.Capabilities → Sort | Filter | Search | Count | Mutate</span>\n}\n</code></pre>\n<p><code>ListDataSource&#x3C;T></code> wraps an in-memory list and provides client-side sort,\nfilter, and search. For data-bound collections, use\n<code>ObservableListDataSource&#x3C;T></code> which tracks <code>ObservableCollection&#x3C;T></code>\nmutations and fires <code>DataChanged</code>.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Source</th><th>Best for</th></tr></thead><tbody><tr><td><code>ListDataSource&#x3C;T></code></td><td>In-memory lists, local data</td></tr><tr><td><code>ObservableListDataSource&#x3C;T></code></td><td>Observable collections, live-updating data</td></tr><tr><td>Custom <code>IDataSource&#x3C;T></code></td><td>REST APIs, databases, GraphQL endpoints</td></tr></tbody></table>\n<p>The <code>Capabilities</code> flag is the negotiation point. A source that returns\n<code>ServerSort | ServerFilter</code> tells the grid to <em>send</em> sort/filter through\n<code>DataRequest</code> and trust the page response; a source that returns <code>None</code>\nopts into the grid's client-side fallback path. Custom sources usually\nsit between the two — server sort, client search — and the grid honors\neach flag independently. See <a href=\"/async-resources/\">async-resources</a> for\nthe pattern that wraps a REST endpoint into an <code>IDataSource&#x3C;T></code>\nwithout leaking <code>HttpClient</code> into your components.</p>\n<h2 id=\"defining-columns\">Defining Columns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#defining-columns\">#</a></h2>\n<p>Use <code>Column&#x3C;T>()</code> to define columns with a fluent builder. Each column has\na name, an accessor function, and optional configuration:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ExplicitColumnsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => <span class=\"hljs-keyword\">new</span> FieldDescriptor[]\n        {\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Id\"</span>, p => p.Id, width: <span class=\"hljs-number\">60</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Name\"</span>, p => p.Name, width: <span class=\"hljs-number\">180</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Category\"</span>, p => p.Category, width: <span class=\"hljs-number\">120</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Price\"</span>, p => p.Price, format: <span class=\"hljs-string\">\"C2\"</span>, width: <span class=\"hljs-number\">100</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Stock\"</span>, p => p.Stock, width: <span class=\"hljs-number\">80</span>),\n        });\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/explicit-columns.png\" alt=\"DataGrid with explicit columns\"></p>\n<p>The <code>ColumnBuilder&#x3C;T></code> supports chaining:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Effect</th></tr></thead><tbody><tr><td><code>.Validate(validators...)</code></td><td>Attach validators for inline editing</td></tr><tr><td><code>.CellRenderer(fn)</code></td><td>Custom cell rendering function</td></tr><tr><td><code>.NotSortable()</code></td><td>Disable sort for this column</td></tr><tr><td><code>.Build()</code></td><td>Finalize the <code>FieldDescriptor</code></td></tr></tbody></table>\n<p><code>Column&#x3C;T>(...)</code> and <code>AutoColumns&#x3C;T>(...)</code> are static methods on\n<code>Microsoft.UI.Reactor.Factories</code> — already imported by the standard\n<code>using static Microsoft.UI.Reactor.Factories;</code> that the rest of Reactor\nrelies on. No second using statement.</p>\n<h2 id=\"auto-generated-columns\">Auto-Generated Columns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#auto-generated-columns\">#</a></h2>\n<p>For quick prototyping, <code>AutoColumns&#x3C;T>()</code> generates columns from public\nproperties using reflection:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AutoColumnsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> registry = UseMemo(() => <span class=\"hljs-keyword\">new</span> TypeRegistry());\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, registry).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/auto-columns.png\" alt=\"DataGrid with auto-generated columns\"></p>\n<p>Auto-generation uses <code>TypeRegistry</code> for custom type metadata when available.\nPass a <code>columnOverrides</code> function to tweak individual columns without\ndefining them all manually.</p>\n<p><code>AutoColumns&#x3C;T>()</code> is a fast-path for demos and admin panels. For\nuser-facing grids, define columns explicitly — auto-generated columns\nfollow property order (often arbitrary), use the property name as the\nheader (often wrong for end users), and expose every public getter\n(including ones you didn't mean to surface). Switch to explicit columns\nthe moment a designer touches the grid.</p>\n<h2 id=\"sorting-and-filtering\">Sorting and Filtering<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#sorting-and-filtering\">#</a></h2>\n<p>Click column headers to sort. The grid delegates sorting to the data\nsource — <code>ListDataSource</code> handles it client-side, while custom sources can\nimplement server-side sorting:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SortFilterDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => <span class=\"hljs-keyword\">new</span> FieldDescriptor[]\n        {\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Name\"</span>, p => p.Name, width: <span class=\"hljs-number\">180</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Category\"</span>, p => p.Category, width: <span class=\"hljs-number\">120</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Price\"</span>, p => p.Price, format: <span class=\"hljs-string\">\"C2\"</span>, width: <span class=\"hljs-number\">100</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Stock\"</span>, p => p.Stock, width: <span class=\"hljs-number\">80</span>).NotSortable(),\n        });\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns, showSearch: <span class=\"hljs-literal\">true</span>).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/sort-filter.png\" alt=\"Sorted and filtered grid\"></p>\n<p>Filtering uses <code>FilterDescriptor</code> with 10 operators: <code>Equals</code>, <code>NotEquals</code>,\n<code>Contains</code>, <code>StartsWith</code>, <code>EndsWith</code>, <code>GreaterThan</code>, <code>LessThan</code>,\n<code>GreaterThanOrEqual</code>, <code>LessThanOrEqual</code>, and <code>Between</code>.</p>\n<p>Enable <code>showSearch: true</code> to add a built-in search bar that highlights\nmatching cells.</p>\n<h2 id=\"selection\">Selection<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#selection\">#</a></h2>\n<p><code>DataGrid</code> supports single and multiple selection modes. Selection state is\nreported via the <code>onSelectionChanged</code> callback:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SelectionDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (selected, setSelected) = UseState&#x3C;IReadOnlySet&#x3C;RowKey>>(\n            <span class=\"hljs-keyword\">new</span> HashSet&#x3C;RowKey>());\n\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => AutoColumns&#x3C;Product>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Selected: <span class=\"hljs-subst\">{selected.Count}</span> items\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            DataGrid&#x3C;Product>(source, columns,\n                selectionMode: SelectionMode.Multiple,\n                onSelectionChanged: setSelected).Height(<span class=\"hljs-number\">350</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/selection.png\" alt=\"Grid with multi-select\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Mode</th><th>Behavior</th></tr></thead><tbody><tr><td><code>SelectionMode.None</code></td><td>No selection (default)</td></tr><tr><td><code>SelectionMode.Single</code></td><td>One row at a time</td></tr><tr><td><code>SelectionMode.Multiple</code></td><td>Ctrl+Click, Shift+Click, anchor-based</td></tr></tbody></table>\n<p>Selected rows are identified by <code>RowKey</code> — a stable identity derived from\nyour data source's <code>GetRowKey</code> implementation. The callback hands you the\nfull snapshot (<code>IReadOnlySet&#x3C;RowKey></code>), not added/removed deltas — same\nshape as <a href=\"/collections/#multi-select-with-selectionchanged\">multi-select on <code>ListView</code></a>.\nLift the selection state into the parent component so it survives sort,\nfilter, and refresh; see the <a href=\"#master-detail-with-lifted-selection\">master-detail pattern</a>\nbelow.</p>\n<h2 id=\"inline-editing\">Inline Editing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#inline-editing\">#</a></h2>\n<p>Set <code>editable: true</code> to enable inline editing. Two edit modes are available:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InlineEditingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => <span class=\"hljs-keyword\">new</span> FieldDescriptor[]\n        {\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Id\"</span>, p => p.Id, width: <span class=\"hljs-number\">60</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Name\"</span>, p => p.Name, editable: <span class=\"hljs-literal\">true</span>, width: <span class=\"hljs-number\">180</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Price\"</span>, p => p.Price, editable: <span class=\"hljs-literal\">true</span>,\n                format: <span class=\"hljs-string\">\"C2\"</span>, width: <span class=\"hljs-number\">100</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Stock\"</span>, p => p.Stock, editable: <span class=\"hljs-literal\">true</span>, width: <span class=\"hljs-number\">80</span>),\n        });\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns,\n            editable: <span class=\"hljs-literal\">true</span>,\n            editMode: EditMode.Cell,\n            onRowChanged: <span class=\"hljs-keyword\">async</span> (key, product) =>\n            {\n                <span class=\"hljs-comment\">// Persist the change — e.g., call an API</span>\n            }).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/inline-editing.png\" alt=\"Inline cell editing\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Mode</th><th>Behavior</th></tr></thead><tbody><tr><td><code>EditMode.Cell</code></td><td>Edit one cell at a time; commits on blur/Enter</td></tr><tr><td><code>EditMode.Row</code></td><td>Edit an entire row; explicit Save/Cancel buttons</td></tr></tbody></table>\n<p>Editing supports validation — attach validators via <code>Column&#x3C;T>().Validate()</code>.\nThe <code>onRowChanged</code> callback fires after a successful commit, receiving the\n<code>RowKey</code> and updated item. For mutable classes, the grid updates in place;\nfor records, it creates a new instance with the changed values. The\nvalidator catalogue is the same one <a href=\"/forms/\">forms</a> uses — <code>Validate.Required()</code>,\n<code>Validate.Range(min, max)</code>, <code>Validate.Must&#x3C;T>(predicate)</code>, etc.</p>\n<h2 id=\"column-resize-and-reorder\">Column Resize and Reorder<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#column-resize-and-reorder\">#</a></h2>\n<p>Users can drag column borders to resize and drag headers to reorder.\nColumn state (widths, order, visibility, pinning) is managed by\n<code>DataGridState</code> and can be persisted:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ColumnFeaturesDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => <span class=\"hljs-keyword\">new</span> FieldDescriptor[]\n        {\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Id\"</span>, p => p.Id, width: <span class=\"hljs-number\">60</span>,\n                pin: PinPosition.Left),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Name\"</span>, p => p.Name, width: <span class=\"hljs-number\">200</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Category\"</span>, p => p.Category, width: <span class=\"hljs-number\">140</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Price\"</span>, p => p.Price, format: <span class=\"hljs-string\">\"C2\"</span>, width: <span class=\"hljs-number\">120</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Stock\"</span>, p => p.Stock, width: <span class=\"hljs-number\">100</span>),\n        });\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/column-features.png\" alt=\"Column resize and pin\"></p>\n<p>Pin columns to <code>PinPosition.Left</code> or <code>PinPosition.Right</code> to keep them\nvisible during horizontal scrolling. Set <code>width</code> in the column definition\nfor an initial width, or let the grid auto-size.</p>\n<h2 id=\"incremental-paging\">Incremental Paging<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#incremental-paging\">#</a></h2>\n<p>For large datasets, <code>DataPageCache&#x3C;T></code> loads data in blocks as the user\nscrolls. The grid shows placeholder rows for unloaded blocks:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PagingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() =>\n        {\n            <span class=\"hljs-keyword\">var</span> products = Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">10</span>_000)\n                .Select(i => <span class=\"hljs-keyword\">new</span> Product(i, <span class=\"hljs-string\">$\"Product <span class=\"hljs-subst\">{i}</span>\"</span>,\n                    i % <span class=\"hljs-number\">3</span> == <span class=\"hljs-number\">0</span> ? <span class=\"hljs-string\">\"Electronics\"</span> : i % <span class=\"hljs-number\">3</span> == <span class=\"hljs-number\">1</span> ? <span class=\"hljs-string\">\"Furniture\"</span> : <span class=\"hljs-string\">\"Accessories\"</span>,\n                    Math.Round(<span class=\"hljs-number\">10</span> + i * <span class=\"hljs-number\">0.99</span>, <span class=\"hljs-number\">2</span>), i % <span class=\"hljs-number\">200</span>))\n                .ToList();\n            <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(products, p => (RowKey)p.Id);\n        });\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => AutoColumns&#x3C;Product>());\n\n        <span class=\"hljs-comment\">// DataPageCache loads 50-row blocks on demand, keeps 20 in LRU cache</span>\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/paging.png\" alt=\"Incremental paging with block loading\"></p>\n<p>The cache uses an LRU eviction policy — when <code>maxBlocks</code> is reached, the\nleast-recently-accessed block is evicted. The <code>BlockLoaded</code> event fires\nwhen a block finishes loading, triggering a re-render for the affected rows.</p>\n<p><code>DataPageCache&#x3C;T></code> follows a pull model: the grid asks for a row index,\nthe cache returns the loaded block or initiates the fetch and returns a\n<code>Loading</code> placeholder. This is the same paging shape Compose Paging 3\nuses, and it differs from the \"fetch on scroll\" pattern in\n<a href=\"/collections/#lazy-loading\"><code>VirtualList.onVisibleRangeChanged</code></a> by\nkeying off row index rather than scroll position. Use <code>DataPageCache&#x3C;T></code>\nwhen you want a count-known surface; use the visible-range callback\nwhen you want a count-unknown infinite feed.</p>\n<h2 id=\"row-details\">Row Details<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#row-details\">#</a></h2>\n<p>Expand individual rows to show additional detail content. Pass a\n<code>rowDetailTemplate</code> to render expandable content below each row:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RowDetailsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => AutoColumns&#x3C;Product>());\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns,\n            rowDetailTemplate: (product, key) =>\n                VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">$\"Product ID: <span class=\"hljs-subst\">{product.Id}</span>\"</span>).Bold(),\n                    TextBlock(<span class=\"hljs-string\">$\"Full details for <span class=\"hljs-subst\">{product.Name}</span>\"</span>),\n                    TextBlock(<span class=\"hljs-string\">$\"Category: <span class=\"hljs-subst\">{product.Category}</span>\"</span>),\n                    TextBlock(<span class=\"hljs-string\">$\"Unit price: <span class=\"hljs-subst\">{product.Price:C2}</span>, Stock: <span class=\"hljs-subst\">{product.Stock}</span>\"</span>)\n                ).Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#f5f5f5\"</span>)\n        ).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/data-system/row-details.png\" alt=\"Expanded row detail\"></p>\n<p>Row details are lazily rendered — the template function only runs when a\nrow is expanded. Use this for showing related data, inline forms, or\nnested grids.</p>\n<h2 id=\"headless-testing-with-datagridstate\">Headless testing with DataGridState<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#headless-testing-with-datagridstate\">#</a></h2>\n<p><code>DataGridState&#x3C;T></code> is the headless state machine the grid uses\ninternally — sort descriptors, filter descriptors, selection, focused\ncell, edit buffer. It has no UI dependencies; you can construct one\nagainst a <code>ListDataSource&#x3C;T></code> in a unit test, dispatch sort/select\ncalls, and assert on the resulting state without ever mounting the\ngrid. This is the same separation TanStack Table draws between core\nlogic and presentation. Most apps never touch <code>DataGridState&#x3C;T></code>\ndirectly — the grid mounts and owns one — but if you ship a custom\ndata layer, the headless state is what your tests should drive.\nSee <a href=\"/testing/\">testing</a> for the renderer-fixture pattern that pairs\nwith this.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Don't construct <code>ListDataSource&#x3C;T></code> inline in <code>Render()</code>. Every render\ncreates a new instance, the grid's <code>useMemo</code> keyed on the source\nidentity invalidates, the page cache empties, scroll position resets,\nand selection clears (selection keys are interpreted against the\n<em>source</em>'s <code>GetRowKey</code>, not the items). The right shape is\n<code>UseMemo(() => new ListDataSource&#x3C;T>(items, x => (RowKey)x.Id), items)</code>\n— the source is recreated only when the underlying list reference\nchanges. The same rule applies to <code>AutoColumns&#x3C;T>()</code> and explicit\n<code>Column&#x3C;T>()</code> arrays: stable identity matters. The first failure mode\nis usually \"selection disappears whenever I change anything\" — that's\nthe source identity churning.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"master-detail-with-lifted-selection\">Master-detail with lifted selection<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#master-detail-with-lifted-selection\">#</a></h3>\n<p>The grid lives on the left, the detail panel renders on the right, and\nthe parent component owns the selected key. The grid's\n<code>onSelectionChanged</code> writes through to parent state; the detail panel\nreads from the same state. Selection survives sort changes, filter\nchanges, and refresh because the state lives outside the grid:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (selected, setSelected) = UseState&#x3C;RowKey?>(<span class=\"hljs-literal\">null</span>);\n<span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Order>(orders, o => (RowKey)o.Id), orders);\n<span class=\"hljs-keyword\">var</span> detail = orders.FirstOrDefault(o => selected <span class=\"hljs-keyword\">is</span> { } k &#x26;&#x26; (RowKey)o.Id == k);\n\n<span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">0</span>,\n    DataGrid&#x3C;Order>(source, columns,\n        selectionMode: SelectionMode.Single,\n        onSelectionChanged: keys => setSelected(keys.FirstOrDefault())\n    ).Width(<span class=\"hljs-number\">480</span>),\n    detail <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n        ? Border(Caption(<span class=\"hljs-string\">\"Select an order\"</span>)).Padding(<span class=\"hljs-number\">24</span>)\n        : OrderDetail(detail).Padding(<span class=\"hljs-number\">24</span>).Width(<span class=\"hljs-number\">360</span>));\n</code></pre>\n<p>The full pattern with sub-grids, optimistic updates, and async detail\nload lives in the <a href=\"/recipes/master-detail/\"><code>recipes/master-detail</code></a>\nrecipe. The key structural point: selection state outlives the grid.</p>\n<h3 id=\"live-data-via-observablelistdatasource\">Live data via ObservableListDataSource<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#live-data-via-observablelistdatasource\">#</a></h3>\n<p>When your data is an <code>ObservableCollection&#x3C;T></code> driven by a\n<a href=\"/hooks/\"><code>UseObservableTree</code></a> source or a background worker,\n<code>ObservableListDataSource&#x3C;T></code> is the bridge. It listens to\n<code>CollectionChanged</code>, fires <code>IObservableDataSource&#x3C;T>.DataChanged</code>, and\nthe grid responds by re-fetching the visible page. No render-loop\nhooks; the grid subscribes when it mounts and unsubscribes when it\nunmounts:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> collection = UseRef(<span class=\"hljs-keyword\">new</span> ObservableCollection&#x3C;Order>(orders));\n<span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ObservableListDataSource&#x3C;Order>(\n    collection.Current, o => (RowKey)o.Id), collection.Current);\n\n<span class=\"hljs-comment\">// Mutations to collection.Current flow through to the grid:</span>\n<span class=\"hljs-comment\">//   collection.Current.Add(new Order(...));</span>\n<span class=\"hljs-comment\">//   collection.Current.RemoveAt(i);</span>\n</code></pre>\n<p>The mutation pattern works for incoming server pushes (SignalR feed,\nWebSocket stream) — push into the observable collection on the UI\nthread (see <a href=\"/threading-and-dispatch/\">threading-and-dispatch</a>) and\nthe grid catches up.</p>\n<h3 id=\"server-driven-paging-with-a-custom-idatasource\">Server-driven paging with a custom IDataSource<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#server-driven-paging-with-a-custom-idatasource\">#</a></h3>\n<p>For data behind a paged REST or GraphQL endpoint, implement\n<code>IDataSource&#x3C;T></code> directly. <code>GetPageAsync(DataRequest)</code> receives the\nsort/filter/search state, the requested page offset, and a\n<code>CancellationToken</code>; return a <code>DataPage&#x3C;T></code> with the items and\n<code>TotalCount</code> (if known). Mount the grid against your source — same\ncolumn code, same selection callback. Set\n<code>Capabilities = ServerSort | ServerFilter | ServerCount</code> to opt out of\nthe client-side fallback path. The\n<a href=\"/recipes/paginated-list/\"><code>recipes/paginated-list</code></a> recipe walks the\nshape end-to-end for a list; the grid wires up identically.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"recreating-the-data-source-on-every-render\">Recreating the data source on every render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recreating-the-data-source-on-every-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> source = <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(SampleProducts.Items, p => (RowKey)p.Id);\n    <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ExplicitColumnsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> source = UseMemo(() => <span class=\"hljs-keyword\">new</span> ListDataSource&#x3C;Product>(\n            SampleProducts.Items, p => (RowKey)p.Id));\n\n        <span class=\"hljs-keyword\">var</span> columns = UseMemo(() => <span class=\"hljs-keyword\">new</span> FieldDescriptor[]\n        {\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Id\"</span>, p => p.Id, width: <span class=\"hljs-number\">60</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Name\"</span>, p => p.Name, width: <span class=\"hljs-number\">180</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Category\"</span>, p => p.Category, width: <span class=\"hljs-number\">120</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Price\"</span>, p => p.Price, format: <span class=\"hljs-string\">\"C2\"</span>, width: <span class=\"hljs-number\">100</span>),\n            Column&#x3C;Product>(<span class=\"hljs-string\">\"Stock\"</span>, p => p.Stock, width: <span class=\"hljs-number\">80</span>),\n        });\n\n        <span class=\"hljs-keyword\">return</span> DataGrid&#x3C;Product>(source, columns).Height(<span class=\"hljs-number\">400</span>);\n    }\n}\n</code></pre>\n<p>A fresh <code>ListDataSource&#x3C;T></code> on every render churns the grid's internal\nidentity, dumps the page cache, and clears selection. Wrap source\nconstruction in <code>UseMemo</code> keyed on the underlying data — the source\nlives across renders, the grid keeps its scroll position, and\nselection survives the next state update.</p>\n<h3 id=\"storing-selection-inside-the-grid\">Storing selection inside the grid<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#storing-selection-inside-the-grid\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nDataGrid&#x3C;Order>(source, columns,\n    selectionMode: SelectionMode.Multiple)\n<span class=\"hljs-comment\">// (no onSelectionChanged — selection state lives only inside the grid)</span>\n</code></pre>\n<p>The grid does maintain selection internally, but reading it requires a\nref and the state is invisible to the rest of your component. Lift the\nselection out: <code>var (selected, setSelected) = UseState(new HashSet&#x3C;RowKey>())</code>\nplus <code>onSelectionChanged: setSelected</code>. Toolbars, badges, and detail\npanels that need \"what's selected\" can now read it.</p>\n<h3 id=\"using-autocolumns-in-production\">Using AutoColumns in production<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-autocolumns-in-production\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nDataGrid&#x3C;Order>(source, registry)\n<span class=\"hljs-comment\">// — relies on property names as headers, property order as column order,</span>\n<span class=\"hljs-comment\">//   and every public getter being a sensible column</span>\n</code></pre>\n<p><code>AutoColumns&#x3C;T>()</code> is for demos and admin tools. Production grids define\ncolumns explicitly: <code>Column&#x3C;Order>(\"Order #\", o => o.Id, width: 80)</code> —\nthe header is reviewed, the order is intentional, and you don't\naccidentally surface <code>o.InternalAuditFlag</code>. The five extra lines pay\nfor themselves the first time a designer asks for \"Order #\" instead of\n\"Id\".</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start with <code>ListDataSource</code> and explicit columns.</strong> Auto-columns and\ncustom data sources add complexity. Get the grid working with a simple\nin-memory list first, then evolve.</p>\n<p><strong>Use <code>EditMode.Cell</code> for spreadsheet-style editing.</strong> Cell mode is faster\nfor quick edits. Use <code>EditMode.Row</code> when edits need validation across\nmultiple fields before committing.</p>\n<p><strong>Pin ID or key columns.</strong> When horizontal scrolling is likely, pin the\nidentifying column so users always know which row they are looking at.</p>\n<p><strong>Prefer records for immutable data.</strong> The grid handles both mutable classes\nand immutable records. Records are simpler and safer — the grid creates\n<code>with</code> copies automatically.</p>\n<p><strong>Set <code>rowHeight</code> for uniform rows.</strong> A fixed height enables O(1) scroll\noffset calculation. Omit it only when rows genuinely vary in height.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — next topic: host Reactor components inside WinForms apps</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — simpler list and grid elements for non-tabular data</li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — controlled inputs and validation patterns used in grid editing</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — performance tuning, error boundaries, and observable data binding</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — the hook system powering DataGrid's internal state</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s DataGrid&#x3C;T> is a virtualized table that renders rows lazily from an IDataSource&#x3C;T> . The source is the contract, not the data: it returns pages keyed by sort, filter, and search state, declares its Capabilities (server-side sort? mutate?), and yields a stable RowKey per item. The grid is a thin view over that contract — it asks the source for the visible window on every render, diffs the returned rows by key, and renders only what changed. This is the opposite of AG Grid's \"row data + column defs\" array-shaped input and closer in spirit to TanStack Table's headless split: source owns data access, DataGridState&#x3C;T> owns sort/selection/edit state, and the grid is the presentation. Two columns of Column&#x3C;T>(...) definitions plus a ListDataSource&#x3C;T> wrapper is the smallest working grid; an ObservableListDataSource&#x3C;T> swap turns it live; a custom IDataSource&#x3C;T> against your REST or GraphQL endpoint turns it into a server-driven grid without changing the column code. Read the source section first — every other section on this page is about how the grid asks more of it. Data System # Reactor's data system provides a virtualized DataGrid&#x3C;T> backed by a pluggable data source abstraction. You define columns (or auto-generate them), connect a data source, and the grid handles sorting, filtering, searching, selection, and inline editing. Data Sources # All data flows through IDataSource&#x3C;T> — an async, page-based abstraction. You never pass raw lists to the grid; instead you wrap your data in a source that declares its capabilities: class DataSourceExample { // Wrap an in-memory list — supports client-side sort, filter, search static ListDataSource&#x3C;Product> CreateSource () => new (SampleProducts.Items, p => (RowKey)p.Id); // source.Capabilities → Sort | Filter | Search | Count | Mutate } ListDataSource&#x3C;T> wraps an in-memory list and provides client-side sort, filter, and search. For data-bound collections, use ObservableListDataSource&#x3C;T> which tracks ObservableCollection&#x3C;T> mutations and fires DataChanged . Source Best for ListDataSource&#x3C;T> In-memory lists, local data ObservableListDataSource&#x3C;T> Observable collections, live-updating data Custom IDataSource&#x3C;T> REST APIs, databases, GraphQL endpoints The Capabilities flag is the negotiation point. A source that returns ServerSort | ServerFilter tells the grid to send sort/filter through DataRequest and trust the page response; a source that returns None opts into the grid's client-side fallback path. Custom sources usually sit between the two — server sort, client search — and the grid honors each flag independently. See async-resources for the pattern that wraps a REST endpoint into an IDataSource&#x3C;T> without leaking HttpClient into your components. Defining Columns # Use Column&#x3C;T>() to define columns with a fluent builder. Each column has a name, an accessor function, and optional configuration: class ExplicitColumnsDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => new FieldDescriptor[] { Column&#x3C;Product>( \"Id\" , p => p.Id, width: 60 ), Column&#x3C;Product>( \"Name\" , p => p.Name, width: 180 ), Column&#x3C;Product>( \"Category\" , p => p.Category, width: 120 ), Column&#x3C;Product>( \"Price\" , p => p.Price, format: \"C2\" , width: 100 ), Column&#x3C;Product>( \"Stock\" , p => p.Stock, width: 80 ), }); return DataGrid&#x3C;Product>(source, columns).Height( 400 ); } } The ColumnBuilder&#x3C;T> supports chaining: Method Effect .Validate(validators...) Attach validators for inline editing .CellRenderer(fn) Custom cell rendering function .NotSortable() Disable sort for this column .Build() Finalize the FieldDescriptor Column&#x3C;T>(...) and AutoColumns&#x3C;T>(...) are static methods on Microsoft.UI.Reactor.Factories — already imported by the standard using static Microsoft.UI.Reactor.Factories; that the rest of Reactor relies on. No second using statement. Auto-Generated Columns # For quick prototyping, AutoColumns&#x3C;T>() generates columns from public properties using reflection: class AutoColumnsDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var registry = UseMemo(() => new TypeRegistry()); return DataGrid&#x3C;Product>(source, registry).Height( 400 ); } } Auto-generation uses TypeRegistry for custom type metadata when available. Pass a columnOverrides function to tweak individual columns without defining them all manually. AutoColumns&#x3C;T>() is a fast-path for demos and admin panels. For user-facing grids, define columns explicitly — auto-generated columns follow property order (often arbitrary), use the property name as the header (often wrong for end users), and expose every public getter (including ones you didn't mean to surface). Switch to explicit columns the moment a designer touches the grid. Sorting and Filtering # Click column headers to sort. The grid delegates sorting to the data source — ListDataSource handles it client-side, while custom sources can implement server-side sorting: class SortFilterDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => new FieldDescriptor[] { Column&#x3C;Product>( \"Name\" , p => p.Name, width: 180 ), Column&#x3C;Product>( \"Category\" , p => p.Category, width: 120 ), Column&#x3C;Product>( \"Price\" , p => p.Price, format: \"C2\" , width: 100 ), Column&#x3C;Product>( \"Stock\" , p => p.Stock, width: 80 ).NotSortable(), }); return DataGrid&#x3C;Product>(source, columns, showSearch: true ).Height( 400 ); } } Filtering uses FilterDescriptor with 10 operators: Equals , NotEquals , Contains , StartsWith , EndsWith , GreaterThan , LessThan , GreaterThanOrEqual , LessThanOrEqual , and Between . Enable showSearch: true to add a built-in search bar that highlights matching cells. Selection # DataGrid supports single and multiple selection modes. Selection state is reported via the onSelectionChanged callback: class SelectionDemo : Component { public override Element Render () { var (selected, setSelected) = UseState&#x3C;IReadOnlySet&#x3C;RowKey>>( new HashSet&#x3C;RowKey>()); var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => AutoColumns&#x3C;Product>()); return VStack( 12 , TextBlock( $\"Selected: {selected.Count} items\" ).Opacity( 0.6 ), DataGrid&#x3C;Product>(source, columns, selectionMode: SelectionMode.Multiple, onSelectionChanged: setSelected).Height( 350 ) ); } } Mode Behavior SelectionMode.None No selection (default) SelectionMode.Single One row at a time SelectionMode.Multiple Ctrl+Click, Shift+Click, anchor-based Selected rows are identified by RowKey — a stable identity derived from your data source's GetRowKey implementation. The callback hands you the full snapshot ( IReadOnlySet&#x3C;RowKey> ), not added/removed deltas — same shape as multi-select on ListView . Lift the selection state into the parent component so it survives sort, filter, and refresh; see the master-detail pattern below. Inline Editing # Set editable: true to enable inline editing. Two edit modes are available: class InlineEditingDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => new FieldDescriptor[] { Column&#x3C;Product>( \"Id\" , p => p.Id, width: 60 ), Column&#x3C;Product>( \"Name\" , p => p.Name, editable: true , width: 180 ), Column&#x3C;Product>( \"Price\" , p => p.Price, editable: true , format: \"C2\" , width: 100 ), Column&#x3C;Product>( \"Stock\" , p => p.Stock, editable: true , width: 80 ), }); return DataGrid&#x3C;Product>(source, columns, editable: true , editMode: EditMode.Cell, onRowChanged: async (key, product) => { // Persist the change — e.g., call an API }).Height( 400 ); } } Mode Behavior EditMode.Cell Edit one cell at a time; commits on blur/Enter EditMode.Row Edit an entire row; explicit Save/Cancel buttons Editing supports validation — attach validators via Column&#x3C;T>().Validate() . The onRowChanged callback fires after a successful commit, receiving the RowKey and updated item. For mutable classes, the grid updates in place; for records, it creates a new instance with the changed values. The validator catalogue is the same one forms uses — Validate.Required() , Validate.Range(min, max) , Validate.Must&#x3C;T>(predicate) , etc. Column Resize and Reorder # Users can drag column borders to resize and drag headers to reorder. Column state (widths, order, visibility, pinning) is managed by DataGridState and can be persisted: class ColumnFeaturesDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => new FieldDescriptor[] { Column&#x3C;Product>( \"Id\" , p => p.Id, width: 60 , pin: PinPosition.Left), Column&#x3C;Product>( \"Name\" , p => p.Name, width: 200 ), Column&#x3C;Product>( \"Category\" , p => p.Category, width: 140 ), Column&#x3C;Product>( \"Price\" , p => p.Price, format: \"C2\" , width: 120 ), Column&#x3C;Product>( \"Stock\" , p => p.Stock, width: 100 ), }); return DataGrid&#x3C;Product>(source, columns).Height( 400 ); } } Pin columns to PinPosition.Left or PinPosition.Right to keep them visible during horizontal scrolling. Set width in the column definition for an initial width, or let the grid auto-size. Incremental Paging # For large datasets, DataPageCache&#x3C;T> loads data in blocks as the user scrolls. The grid shows placeholder rows for unloaded blocks: class PagingDemo : Component { public override Element Render () { var source = UseMemo(() => { var products = Enumerable.Range( 1 , 10 _000) .Select(i => new Product(i, $\"Product {i} \" , i % 3 == 0 ? \"Electronics\" : i % 3 == 1 ? \"Furniture\" : \"Accessories\" , Math.Round( 10 + i * 0.99 , 2 ), i % 200 )) .ToList(); return new ListDataSource&#x3C;Product>(products, p => (RowKey)p.Id); }); var columns = UseMemo(() => AutoColumns&#x3C;Product>()); // DataPageCache loads 50-row blocks on demand, keeps 20 in LRU cache return DataGrid&#x3C;Product>(source, columns).Height( 400 ); } } The cache uses an LRU eviction policy — when maxBlocks is reached, the least-recently-accessed block is evicted. The BlockLoaded event fires when a block finishes loading, triggering a re-render for the affected rows. DataPageCache&#x3C;T> follows a pull model: the grid asks for a row index, the cache returns the loaded block or initiates the fetch and returns a Loading placeholder. This is the same paging shape Compose Paging 3 uses, and it differs from the \"fetch on scroll\" pattern in VirtualList.onVisibleRangeChanged by keying off row index rather than scroll position. Use DataPageCache&#x3C;T> when you want a count-known surface; use the visible-range callback when you want a count-unknown infinite feed. Row Details # Expand individual rows to show additional detail content. Pass a rowDetailTemplate to render expandable content below each row: class RowDetailsDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => AutoColumns&#x3C;Product>()); return DataGrid&#x3C;Product>(source, columns, rowDetailTemplate: (product, key) => VStack( 8 , TextBlock( $\"Product ID: {product.Id} \" ).Bold(), TextBlock( $\"Full details for {product.Name} \" ), TextBlock( $\"Category: {product.Category} \" ), TextBlock( $\"Unit price: {product.Price:C2} , Stock: {product.Stock} \" ) ).Padding( 16 ).Background( \"#f5f5f5\" ) ).Height( 400 ); } } Row details are lazily rendered — the template function only runs when a row is expanded. Use this for showing related data, inline forms, or nested grids. Headless testing with DataGridState # DataGridState&#x3C;T> is the headless state machine the grid uses internally — sort descriptors, filter descriptors, selection, focused cell, edit buffer. It has no UI dependencies; you can construct one against a ListDataSource&#x3C;T> in a unit test, dispatch sort/select calls, and assert on the resulting state without ever mounting the grid. This is the same separation TanStack Table draws between core logic and presentation. Most apps never touch DataGridState&#x3C;T> directly — the grid mounts and owns one — but if you ship a custom data layer, the headless state is what your tests should drive. See testing for the renderer-fixture pattern that pairs with this. Caveat: Don't construct ListDataSource&#x3C;T> inline in Render() . Every render creates a new instance, the grid's useMemo keyed on the source identity invalidates, the page cache empties, scroll position resets, and selection clears (selection keys are interpreted against the source 's GetRowKey , not the items). The right shape is UseMemo(() => new ListDataSource&#x3C;T>(items, x => (RowKey)x.Id), items) — the source is recreated only when the underlying list reference changes. The same rule applies to AutoColumns&#x3C;T>() and explicit Column&#x3C;T>() arrays: stable identity matters. The first failure mode is usually \"selection disappears whenever I change anything\" — that's the source identity churning. Patterns # Master-detail with lifted selection # The grid lives on the left, the detail panel renders on the right, and the parent component owns the selected key. The grid's onSelectionChanged writes through to parent state; the detail panel reads from the same state. Selection survives sort changes, filter changes, and refresh because the state lives outside the grid: var (selected, setSelected) = UseState&#x3C;RowKey?>( null ); var source = UseMemo(() => new ListDataSource&#x3C;Order>(orders, o => (RowKey)o.Id), orders); var detail = orders.FirstOrDefault(o => selected is { } k &#x26;&#x26; (RowKey)o.Id == k); return HStack( 0 , DataGrid&#x3C;Order>(source, columns, selectionMode: SelectionMode.Single, onSelectionChanged: keys => setSelected(keys.FirstOrDefault()) ).Width( 480 ), detail is null ? Border(Caption( \"Select an order\" )).Padding( 24 ) : OrderDetail(detail).Padding( 24 ).Width( 360 )); The full pattern with sub-grids, optimistic updates, and async detail load lives in the recipes/master-detail recipe. The key structural point: selection state outlives the grid. Live data via ObservableListDataSource # When your data is an ObservableCollection&#x3C;T> driven by a UseObservableTree source or a background worker, ObservableListDataSource&#x3C;T> is the bridge. It listens to CollectionChanged , fires IObservableDataSource&#x3C;T>.DataChanged , and the grid responds by re-fetching the visible page. No render-loop hooks; the grid subscribes when it mounts and unsubscribes when it unmounts: var collection = UseRef( new ObservableCollection&#x3C;Order>(orders)); var source = UseMemo(() => new ObservableListDataSource&#x3C;Order>( collection.Current, o => (RowKey)o.Id), collection.Current); // Mutations to collection.Current flow through to the grid: // collection.Current.Add(new Order(...)); // collection.Current.RemoveAt(i); The mutation pattern works for incoming server pushes (SignalR feed, WebSocket stream) — push into the observable collection on the UI thread (see threading-and-dispatch ) and the grid catches up. Server-driven paging with a custom IDataSource # For data behind a paged REST or GraphQL endpoint, implement IDataSource&#x3C;T> directly. GetPageAsync(DataRequest) receives the sort/filter/search state, the requested page offset, and a CancellationToken ; return a DataPage&#x3C;T> with the items and TotalCount (if known). Mount the grid against your source — same column code, same selection callback. Set Capabilities = ServerSort | ServerFilter | ServerCount to opt out of the client-side fallback path. The recipes/paginated-list recipe walks the shape end-to-end for a list; the grid wires up identically. Common Mistakes # Recreating the data source on every render # // Don't: public override Element Render () { var source = new ListDataSource&#x3C;Product>(SampleProducts.Items, p => (RowKey)p.Id); return DataGrid&#x3C;Product>(source, columns); } class ExplicitColumnsDemo : Component { public override Element Render () { var source = UseMemo(() => new ListDataSource&#x3C;Product>( SampleProducts.Items, p => (RowKey)p.Id)); var columns = UseMemo(() => new FieldDescriptor[] { Column&#x3C;Product>( \"Id\" , p => p.Id, width: 60 ), Column&#x3C;Product>( \"Name\" , p => p.Name, width: 180 ), Column&#x3C;Product>( \"Category\" , p => p.Category, width: 120 ), Column&#x3C;Product>( \"Price\" , p => p.Price, format: \"C2\" , width: 100 ), Column&#x3C;Product>( \"Stock\" , p => p.Stock, width: 80 ), }); return DataGrid&#x3C;Product>(source, columns).Height( 400 ); } } A fresh ListDataSource&#x3C;T> on every render churns the grid's internal identity, dumps the page cache, and clears selection. Wrap source construction in UseMemo keyed on the underlying data — the source lives across renders, the grid keeps its scroll position, and selection survives the next state update. Storing selection inside the grid # // Don't: DataGrid&#x3C;Order>(source, columns, selectionMode: SelectionMode.Multiple) // (no onSelectionChanged — selection state lives only inside the grid) The grid does maintain selection internally, but reading it requires a ref and the state is invisible to the rest of your component. Lift the selection out: var (selected, setSelected) = UseState(new HashSet&#x3C;RowKey>()) plus onSelectionChanged: setSelected . Toolbars, badges, and detail panels that need \"what's selected\" can now read it. Using AutoColumns in production # // Don't: DataGrid&#x3C;Order>(source, registry) // — relies on property names as headers, property order as column order, // and every public getter being a sensible column AutoColumns&#x3C;T>() is for demos and admin tools. Production grids define columns explicitly: Column&#x3C;Order>(\"Order #\", o => o.Id, width: 80) — the header is reviewed, the order is intentional, and you don't accidentally surface o.InternalAuditFlag . The five extra lines pay for themselves the first time a designer asks for \"Order #\" instead of \"Id\". Tips # Start with ListDataSource and explicit columns. Auto-columns and custom data sources add complexity. Get the grid working with a simple in-memory list first, then evolve. Use EditMode.Cell for spreadsheet-style editing. Cell mode is faster for quick edits. Use EditMode.Row when edits need validation across multiple fields before committing. Pin ID or key columns. When horizontal scrolling is likely, pin the identifying column so users always know which row they are looking at. Prefer records for immutable data. The grid handles both mutable classes and immutable records. Records are simpler and safer — the grid creates with copies automatically. Set rowHeight for uniform rows. A fixed height enables O(1) scroll offset calculation. Omit it only when rows genuinely vary in height. Next Steps # WinForms Interop — next topic: host Reactor components inside WinForms apps Collections — simpler list and grid elements for non-tabular data Forms and Input — controlled inputs and validation patterns used in grid editing Advanced Patterns — performance tuning, error boundaries, and observable data binding Hooks — the hook system powering DataGrid's internal state",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/data-system.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/data-system.md",
      "headings": [
        {
          "depth": 2,
          "id": "data-sources",
          "text": "Data Sources"
        },
        {
          "depth": 2,
          "id": "defining-columns",
          "text": "Defining Columns"
        },
        {
          "depth": 2,
          "id": "auto-generated-columns",
          "text": "Auto-Generated Columns"
        },
        {
          "depth": 2,
          "id": "sorting-and-filtering",
          "text": "Sorting and Filtering"
        },
        {
          "depth": 2,
          "id": "selection",
          "text": "Selection"
        },
        {
          "depth": 2,
          "id": "inline-editing",
          "text": "Inline Editing"
        },
        {
          "depth": 2,
          "id": "column-resize-and-reorder",
          "text": "Column Resize and Reorder"
        },
        {
          "depth": 2,
          "id": "incremental-paging",
          "text": "Incremental Paging"
        },
        {
          "depth": 2,
          "id": "row-details",
          "text": "Row Details"
        },
        {
          "depth": 2,
          "id": "headless-testing-with-datagridstate",
          "text": "Headless testing with DataGridState"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "master-detail-with-lifted-selection",
          "text": "Master-detail with lifted selection"
        },
        {
          "depth": 3,
          "id": "live-data-via-observablelistdatasource",
          "text": "Live data via ObservableListDataSource"
        },
        {
          "depth": 3,
          "id": "server-driven-paging-with-a-custom-idatasource",
          "text": "Server-driven paging with a custom IDataSource"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "recreating-the-data-source-on-every-render",
          "text": "Recreating the data source on every render"
        },
        {
          "depth": 3,
          "id": "storing-selection-inside-the-grid",
          "text": "Storing selection inside the grid"
        },
        {
          "depth": 3,
          "id": "using-autocolumns-in-production",
          "text": "Using AutoColumns in production"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Charting",
      "path": "charting.md",
      "slug": "/charting/",
      "html": "<p>ReactorD3 sits on two layers and you choose the level you author at.\nThe high-level DSL — <code>LineChart</code>, <code>BarChart</code>, <code>AreaChart</code>, <code>PieChart</code>,\nplus <code>TreeChart</code> and <code>ForceGraph</code> — covers the cases dashboards keep\nasking for: a series, an x accessor, a y accessor, and an opinionated\nvisual surface that the library wires up for you (axes, grids,\npalettes, keyboard navigation, screen-reader summary). When the\nopinionated surface doesn't fit — when you want a slice's percent\nrendered inside the slice itself, when you want a heatmap, when you\nwant a bespoke parallel-coordinates view — the same library exposes\nthe D3 primitives that built the high-level shapes: <code>LinearScale</code>,\n<code>BandScale</code>, <code>D3Canvas</code>, <code>D3LinePath&#x3C;T></code>, <code>D3ArcPath</code>, and the\n<code>*LabelView</code> escape hatches on the high-level charts. This is the\nsame trade-off Recharts and Observable Plot draw — convenience first,\nescape hatch beneath — except every D3 primitive renders as a WinUI\nshape on the same <code>Canvas</code> the high-level charts use, so dropping a\nlevel never means switching renderers. Pick the high level by default;\ndrop into D3 only when the higher-level surface refuses what you need.</p>\n<h1 id=\"charting\">Charting<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#charting\">#</a></h1>\n<p>ReactorD3 brings data visualization to Microsoft.UI.Reactor (Reactor). The chart DSL provides high-level\n<code>LineChart</code>, <code>BarChart</code>, <code>AreaChart</code>, and <code>PieChart</code> factories that produce\nstandard Reactor elements. You bind data, configure appearance with a fluent\nAPI, and the chart renders as native WinUI shapes on a Canvas.</p>\n<p>Import the DSL alongside the standard Reactor factories:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">static</span> Microsoft.UI.Reactor.Factories;\n<span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">static</span> Microsoft.UI.Reactor.Charting.Charts;\n</code></pre>\n<h2 id=\"line-chart\">Line Chart<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#line-chart\">#</a></h2>\n<p><code>LineChart</code> draws a continuous line through your data points. Pass a\ncollection and accessor functions for X and Y:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LineChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">120</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">180</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">150</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">220</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">310</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">280</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">7</span>, <span class=\"hljs-number\">350</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">400</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">9</span>, <span class=\"hljs-number\">380</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">420</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">11</span>, <span class=\"hljs-number\">460</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">12</span>, <span class=\"hljs-number\">510</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Line Chart\"</span>),\n            LineChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Monthly Revenue — Line\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .AxisLabel(ChartAxisType.X, <span class=\"hljs-string\">\"Month\"</span>)\n                .AxisLabel(ChartAxisType.Y, <span class=\"hljs-string\">\"Revenue (USD)\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>).StrokeWidth(<span class=\"hljs-number\">2.5</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/line-chart.png\" alt=\"Line chart\"></p>\n<p>The chart auto-scales axes to fit your data with <code>.Nice()</code> rounding. Toggle\ngrid lines with <code>.ShowGrid()</code> and axis labels with <code>.ShowAxes()</code>.</p>\n<h2 id=\"bar-chart\">Bar Chart<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#bar-chart\">#</a></h2>\n<p><code>BarChart</code> renders vertical bars. Each data point becomes a bar whose height\nmaps to the Y value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BarChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">340</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">420</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">510</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">380</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Bar Chart\"</span>),\n            BarChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Quarterly Revenue — Bar\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"quarters\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .AxisLabel(ChartAxisType.X, <span class=\"hljs-string\">\"Quarter\"</span>)\n                .AxisLabel(ChartAxisType.Y, <span class=\"hljs-string\">\"Revenue (USD)\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Fill(<span class=\"hljs-string\">\"#50C878\"</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/bar-chart.png\" alt=\"Bar chart\"></p>\n<p>Bar width is calculated automatically based on the number of data points.\nUse <code>.Fill()</code> to set the bar color and <code>.FillOpacity()</code> to control\ntransparency.</p>\n<h2 id=\"area-chart\">Area Chart<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#area-chart\">#</a></h2>\n<p><code>AreaChart</code> fills the region between the data line and the baseline. It\ncombines a filled area with a line overlay:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AreaChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">50</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">120</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">200</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">350</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">480</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">600</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">7</span>, <span class=\"hljs-number\">720</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">8</span>, <span class=\"hljs-number\">850</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">9</span>, <span class=\"hljs-number\">1020</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">10</span>, <span class=\"hljs-number\">1150</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">11</span>, <span class=\"hljs-number\">1300</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">12</span>, <span class=\"hljs-number\">1500</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Area Chart\"</span>),\n            AreaChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Monthly Revenue — Area\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .AxisLabel(ChartAxisType.X, <span class=\"hljs-string\">\"Month\"</span>)\n                .AxisLabel(ChartAxisType.Y, <span class=\"hljs-string\">\"Revenue (USD)\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Stroke(<span class=\"hljs-string\">\"#9B59B6\"</span>).Fill(<span class=\"hljs-string\">\"#9B59B6\"</span>)\n                .FillOpacity(<span class=\"hljs-number\">0.2</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/area-chart.png\" alt=\"Area chart\"></p>\n<p>Use <code>.FillOpacity()</code> to control area transparency. A low value (0.15--0.3)\nlets grid lines show through while still filling the shape.</p>\n<h2 id=\"pie-chart\">Pie Chart<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pie-chart\">#</a></h2>\n<p><code>PieChart</code> divides data into proportional arcs. Set <code>.InnerRadius()</code> > 0\nfor a donut chart:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PieChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> CategoryData[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Engineering\"</span>, <span class=\"hljs-number\">42</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Marketing\"</span>, <span class=\"hljs-number\">18</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Sales\"</span>, <span class=\"hljs-number\">25</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-string\">\"Support\"</span>, <span class=\"hljs-number\">15</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Pie Chart\"</span>),\n            PieChart(data, d => d.Value, d => d.Name)\n                .Title(<span class=\"hljs-string\">\"Team Distribution\"</span>)\n                .Description(<span class=\"hljs-string\">\"Pie chart showing team size across Engineering, Marketing, Sales, and Support.\"</span>)\n                .Width(<span class=\"hljs-number\">300</span>).Height(<span class=\"hljs-number\">300</span>)\n                .InnerRadius(<span class=\"hljs-number\">60</span>)\n                .PadAngle(<span class=\"hljs-number\">0.03</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/pie-chart.png\" alt=\"Pie chart\"></p>\n<p>Pass a label accessor to display text at each arc's centroid. Colors cycle\nthrough the Category10 palette by default — override with <code>.SetColors()</code>.</p>\n<h2 id=\"chart-configuration\">Chart Configuration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#chart-configuration\">#</a></h2>\n<p>All chart types share a common set of builder methods:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>.Width(n)</code></td><td>400</td><td>Canvas width in pixels</td></tr><tr><td><code>.Height(n)</code></td><td>300</td><td>Canvas height in pixels</td></tr><tr><td><code>.Margin(t, r, b, l)</code></td><td>20, 20, 30, 40</td><td>Axis/label margins</td></tr><tr><td><code>.Stroke(color)</code></td><td><code>#4285f4</code></td><td>Line/border color</td></tr><tr><td><code>.Fill(color)</code></td><td><code>#4285f4</code></td><td>Fill color</td></tr><tr><td><code>.StrokeWidth(n)</code></td><td>2</td><td>Line thickness</td></tr><tr><td><code>.FillOpacity(n)</code></td><td>0.3</td><td>Fill transparency (0--1)</td></tr><tr><td><code>.ShowAxes(bool)</code></td><td>true</td><td>Show axis lines and labels</td></tr><tr><td><code>.ShowGrid(bool)</code></td><td>true</td><td>Show horizontal grid lines</td></tr></tbody></table>\n<p>Colors accept any CSS-style string: <code>#RGB</code>, <code>#RRGGBB</code>, <code>rgb(r,g,b)</code>, or\nnamed colors like <code>steelblue</code>. For palette-aware colors that adapt to\ndark mode, see <a href=\"/styling/\">styling</a> and the\n<a href=\"#accessibility-modifiers\"><code>.Palette(...)</code> accessibility modifier</a> below.</p>\n<h2 id=\"binding-data-to-state\">Binding Data to State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#binding-data-to-state\">#</a></h2>\n<p>Charts are standard Reactor elements. When state changes, the chart re-renders\nwith the new data:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CombinedChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (year, setYear) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> years = <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"2024\"</span>, <span class=\"hljs-string\">\"2025\"</span> };\n\n        <span class=\"hljs-keyword\">var</span> data2024 = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">100</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">140</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">180</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">200</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">260</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">300</span>)\n        };\n        <span class=\"hljs-keyword\">var</span> data2025 = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">160</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">220</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">280</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">320</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">390</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">450</span>)\n        };\n\n        <span class=\"hljs-keyword\">var</span> data = year == <span class=\"hljs-number\">0</span> ? data2024 : data2025;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Interactive Chart\"</span>),\n            ComboBox(years, year, setYear),\n            AreaChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Revenue by Year\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .Interactive()\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>).Fill(<span class=\"hljs-string\">\"#0078D4\"</span>)\n                .FillOpacity(<span class=\"hljs-number\">0.15</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/combined-chart.png\" alt=\"Interactive chart\"></p>\n<p>Use <a href=\"/hooks/\">hooks</a> like <code>UseState</code> to drive data selection. The chart\nrebuilds on every render — keep datasets small (under ~1000 points) for\nsmooth interaction.</p>\n<h2 id=\"dynamic-data-updates\">Dynamic Data Updates<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dynamic-data-updates\">#</a></h2>\n<p>Charts are native Reactor elements, so changing state is all you need. Use\n<code>UseState</code>, <code>UseReducer</code>, or any hook to update the data — the reconciler\ndiffs the old and new element trees and patches only what changed:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DynamicDataDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (points, updatePoints) = UseReducer(\n            Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">8</span>)\n                .Select(i => <span class=\"hljs-keyword\">new</span> SalesPoint(i, Random.Shared.Next(<span class=\"hljs-number\">50</span>, <span class=\"hljs-number\">500</span>)))\n                .ToList());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Dynamic Data\"</span>),\n            Button(<span class=\"hljs-string\">\"Randomize\"</span>, () => updatePoints(_ =>\n                Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">8</span>)\n                    .Select(i => <span class=\"hljs-keyword\">new</span> SalesPoint(i, Random.Shared.Next(<span class=\"hljs-number\">50</span>, <span class=\"hljs-number\">500</span>)))\n                    .ToList())),\n            BarChart&#x3C;SalesPoint>(points, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Dynamic Revenue Data\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Fill(<span class=\"hljs-string\">\"#E74C3C\"</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/dynamic-data.png\" alt=\"Dynamic data\"></p>\n<p>For high-frequency updates (60fps streaming), use <code>OnReady</code> to get a\nhandle that exposes the underlying <code>Canvas</code> for direct manipulation.\nThis is the same compositor-property escape hatch the\n<a href=\"/animation-pipeline/\">animation pipeline</a> uses — render the static\ngeometry through Reactor, then mutate the live values directly.</p>\n<h2 id=\"custom-label-elements\">Custom Label Elements<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-label-elements\">#</a></h2>\n<p>Most charts only need the string-based label APIs (<code>AxisLabel</code>,\n<code>LabelAccessor</code>, <code>DataLabel</code>). Reach for the <code>*View</code> extensions only when\nplain text isn't enough — for icon-plus-text ticks, multi-line labels with\nmixed typography, or rendering a slice's percent inside the slice itself.</p>\n<h3 id=\"pie-slice-labels\">Pie slice labels<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pie-slice-labels\">#</a></h3>\n<p><code>LabelView</code> replaces the built-in text label on each pie slice. The delegate\nreceives the slice's data item plus a <code>PieSliceLayout</code> describing its\ngeometry, and returns any Element:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Percent rendered inside the slice. The string label accessor</span>\n<span class=\"hljs-comment\">// is still passed so screen readers describe the slice.</span>\nPieChart(data, d => d.Value, d => d.Name)\n    .Title(<span class=\"hljs-string\">\"Team Distribution\"</span>)\n    .Width(<span class=\"hljs-number\">300</span>).Height(<span class=\"hljs-number\">300</span>)\n    .InnerRadius(<span class=\"hljs-number\">50</span>).PadAngle(<span class=\"hljs-number\">0.02</span>)\n    .LabelView((d, layout) =>\n        TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{layout.Fraction:P0}</span>\"</span>)\n            .FontSize(<span class=\"hljs-number\">12</span>).Bold().Foreground(<span class=\"hljs-string\">\"White\"</span>))\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/pie-label-view.png\" alt=\"Pie chart with the percent rendered inside each slice\"></p>\n<p><code>PieSliceLayout</code> exposes the slice's <code>Index</code>, <code>Value</code>, <code>Fraction</code>,\n<code>CentroidX</code>/<code>CentroidY</code>, <code>StartAngle</code>/<code>EndAngle</code>, <code>InnerRadius</code>/<code>OuterRadius</code>,\nand the resolved palette <code>Color</code> so a label can echo slice geometry without\nrecomputing it.</p>\n<h3 id=\"axis-tick-labels\">Axis tick labels<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#axis-tick-labels\">#</a></h3>\n<p><code>XTickLabelView</code> and <code>YTickLabelView</code> replace the numeric tick labels with\nany Element. Each delegate receives the tick's domain value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// X axis ticks: render month name plus a caption per tick.</span>\nLineChart(data, d => d.Month, d => d.Revenue)\n    .Title(<span class=\"hljs-string\">\"Revenue by Month\"</span>)\n    .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n    .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">220</span>)\n    .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>).StrokeWidth(<span class=\"hljs-number\">2.5</span>)\n    .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n    .XTickLabelView(t => VStack(<span class=\"hljs-number\">2</span>,\n        TextBlock(months[Math.Clamp((<span class=\"hljs-built_in\">int</span>)t - <span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">0</span>, months.Length - <span class=\"hljs-number\">1</span>)])\n            .FontSize(<span class=\"hljs-number\">11</span>).SemiBold(),\n        TextBlock(<span class=\"hljs-string\">\"month\"</span>).FontSize(<span class=\"hljs-number\">8</span>).Opacity(<span class=\"hljs-number\">0.6</span>)))\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/axis-tick-view.png\" alt=\"Line chart with month-name axis ticks\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>PieChartElement&#x3C;T>.LabelView(Func&#x3C;T, PieSliceLayout, Element>)</code></td><td>Replace the built-in slice text with any Element, anchored on the slice centroid</td></tr><tr><td><code>ChartElement&#x3C;T>.XTickLabelView(Func&#x3C;double, Element>)</code></td><td>Replace the X-axis tick label, horizontally centered on the tick</td></tr><tr><td><code>ChartElement&#x3C;T>.YTickLabelView(Func&#x3C;double, Element>)</code></td><td>Replace the Y-axis tick label, right-anchored to the axis edge</td></tr></tbody></table>\n<p>The element you return is auto-anchored — you don't need a known size at\nconstruction time, and the chart re-positions on layout. It is also rendered\nnon-interactive and hidden from the UIA tree, so the chart's structured\naccessibility description (see below) stays canonical. Always keep the\nstring <code>LabelAccessor</code> (pie) or <code>DataLabel</code> (line/bar/area) set when you use\na <code>*View</code> override; that's what screen readers read.</p>\n<h2 id=\"chart-accessibility\">Chart Accessibility<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#chart-accessibility\">#</a></h2>\n<p>Charts are fully accessible out of the box. Add <code>.Title()</code> and\n<code>.SeriesName()</code> for screen-reader identification, <code>.Units()</code> for axis\nannotations, and <code>.Interactive()</code> for keyboard navigation:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Canonical accessible chart pattern — demonstrates all recommended accessibility</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> modifiers for both static and interactive charts. Follow this pattern to ensure</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> charts are fully accessible to screen readers, keyboard users, and users who</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> need forced-colors or reduced-motion.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AccessibleChartDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> SalesPoint[]\n        {\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">120</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">180</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">3</span>, <span class=\"hljs-number\">150</span>),\n            <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">4</span>, <span class=\"hljs-number\">220</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">310</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">280</span>)\n        };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Accessible Chart\"</span>),\n\n            <span class=\"hljs-comment\">// Static accessible chart: Title + SeriesName + Units</span>\n            LineChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Monthly Revenue 2024\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .AxisLabel(ChartAxisType.X, <span class=\"hljs-string\">\"Month\"</span>)\n                .AxisLabel(ChartAxisType.Y, <span class=\"hljs-string\">\"Revenue (USD)\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>).StrokeWidth(<span class=\"hljs-number\">2.5</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>),\n\n            <span class=\"hljs-comment\">// Interactive accessible chart: adds keyboard nav and point invocation</span>\n            BarChart(data, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Monthly Revenue — Interactive\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .Interactive()\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Fill(<span class=\"hljs-string\">\"#50C878\"</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/charting/accessible-chart.png\" alt=\"Accessible chart\"></p>\n<h3 id=\"accessibility-modifiers\">Accessibility Modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibility-modifiers\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>.Title(str)</code></td><td>Visible title and accessible name</td></tr><tr><td><code>.Description(str)</code></td><td>Override auto-generated summary</td></tr><tr><td><code>.SeriesName(str)</code></td><td>Name the data series</td></tr><tr><td><code>.Units(x, y)</code></td><td>Axis unit annotations (e.g., \"months\", \"USD\")</td></tr><tr><td><code>.AxisLabel(axis, str)</code></td><td>Explicit axis label</td></tr><tr><td><code>.Interactive()</code></td><td>Enable keyboard navigation and virtual focus</td></tr><tr><td><code>.OnPointInvoke(handler)</code></td><td>Callback when Enter/Space pressed on a point</td></tr><tr><td><code>.AlternateView(element)</code></td><td>Toggle between chart and data table (T key)</td></tr><tr><td><code>.Palette(palette)</code></td><td>Curated colorblind-safe palette</td></tr><tr><td><code>.SeriesShapes(shapes)</code></td><td>Marker shapes for double-encoding</td></tr><tr><td><code>.SeriesDashes(dashes)</code></td><td>Dash patterns for double-encoding</td></tr></tbody></table>\n<p>Screen readers announce the chart type, series count, data range, and\nindividual point values. Keyboard users navigate with arrow keys, invoke\npoints with Enter, and toggle an alternate data-table view with T. The\nfull a11y story for the framework is in <a href=\"/accessibility/\">accessibility</a>;\nthe chart-specific contract is that color is never the only encoding —\ncombine <code>.Palette(...)</code> with <code>.SeriesShapes(...)</code> or <code>.SeriesDashes(...)</code>\nfor any multi-series chart that ships to forced-colors users.</p>\n<h2 id=\"low-level-drawing\">Low-Level Drawing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#low-level-drawing\">#</a></h2>\n<p>For custom visualizations, ReactorD3 provides shape generators and a Canvas\nDSL. Import <code>using static Microsoft.UI.Reactor.Charting.D3.D3Charts</code> for:</p>\n<ul>\n<li><strong><code>D3Canvas(w, h, children)</code></strong> — create a drawing surface</li>\n<li><strong><code>D3Rect</code>, <code>D3Circle</code>, <code>D3Line</code>, <code>D3Path</code></strong> — primitive shapes</li>\n<li><strong><code>D3Text</code>, <code>D3TextRight</code>, <code>D3TextCenter</code></strong> — positioned text</li>\n<li><strong><code>D3LinePath&#x3C;T></code>, <code>D3AreaPath&#x3C;T></code>, <code>D3ArcPath</code></strong> — generator helpers</li>\n<li><strong><code>D3Axes</code>, <code>D3Grid</code>, <code>D3Legend</code></strong> — axis/legend composites</li>\n<li><strong><code>LinearScale</code>, <code>BandScale</code>, <code>LogScale</code></strong> — map data to pixels</li>\n</ul>\n<h2 id=\"scale-types\">Scale Types<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#scale-types\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Scale</th><th>Purpose</th></tr></thead><tbody><tr><td><code>LinearScale</code></td><td>Continuous numeric mapping with <code>.Nice()</code> and <code>.Ticks()</code></td></tr><tr><td><code>BandScale&#x3C;T></code></td><td>Categorical mapping with bandwidth (bar charts)</td></tr><tr><td><code>LogScale</code></td><td>Logarithmic mapping for exponential data</td></tr><tr><td><code>PowScale</code></td><td>Power/sqrt mapping</td></tr><tr><td><code>OrdinalScale&#x3C;T></code></td><td>Discrete-to-discrete mapping</td></tr></tbody></table>\n<blockquote>\n<p><strong>Caveat:</strong> Recreate the data array inside <code>Render()</code> only at your own peril.\nCharts compare incoming data by reference on the fast path — a new\narray on every render means the chart re-derives scales, re-runs the\npie layout, and re-allocates the WinUI shape pool every frame, even\nwhen nothing changed. The symptom is a chart that flickers when an\nunrelated piece of state updates. The fix is the same shape Reactor\nuses everywhere: wrap the data in <code>UseMemo(() => …, deps)</code> keyed on\nwhatever actually changes. The pattern is most obvious with\n<code>PieChart</code> — a fresh <code>new[] { … }</code> on every render makes each slice\nremount, the <code>.Transition()</code> enter/exit fires for slices that haven't\nmoved, and the chart \"redraws itself\" on every keystroke into an\nunrelated <code>TextBox</code>.</p>\n</blockquote>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"live-updating-chart-from-a-ticking-source\">Live-updating chart from a ticking source<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#live-updating-chart-from-a-ticking-source\">#</a></h3>\n<p>A streaming chart — a price feed, a sensor reading — drives a sliding\nwindow of points. Hold the window in <code>UseState</code>, append on tick, drop\nthe head once <code>Count</code> exceeds the window size:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (samples, setSamples) = UseState&#x3C;IReadOnlyList&#x3C;Sample>>(Array.Empty&#x3C;Sample>());\n\nUseEffect(() =>\n{\n    <span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromMilliseconds(<span class=\"hljs-number\">200</span>));\n    <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(token))\n    {\n        setSamples(prev =>\n        {\n            <span class=\"hljs-keyword\">var</span> next = prev.Append(Sample.Now()).ToList();\n            <span class=\"hljs-keyword\">return</span> next.Count > <span class=\"hljs-number\">60</span> ? next.Skip(next.Count - <span class=\"hljs-number\">60</span>).ToList() : next;\n        });\n    }\n}, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n\n<span class=\"hljs-keyword\">return</span> LineChart(samples, s => s.Time.Ticks, s => s.Value)\n    .Title(<span class=\"hljs-string\">\"Live feed\"</span>)\n    .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">220</span>)\n    .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>);\n</code></pre>\n<p>The chart redraws on every tick. For sub-100ms cadence, drop into\n<code>OnReady</code> and mutate the <code>Canvas</code> directly — the reconciler is fast\nbut it isn't free. See <a href=\"/effects-scheduling/\">effects-scheduling</a> for\nthe timer-cleanup pattern.</p>\n<h3 id=\"switching-chart-type-without-losing-the-data-binding\">Switching chart type without losing the data binding<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#switching-chart-type-without-losing-the-data-binding\">#</a></h3>\n<p>The same data feeds a line, a bar, or an area chart — let users pick\nthe shape. Hold the chart kind in state, switch the factory call:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (kind, setKind) = UseState(<span class=\"hljs-string\">\"line\"</span>);\n\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n    ComboBox(<span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"line\"</span>, <span class=\"hljs-string\">\"bar\"</span>, <span class=\"hljs-string\">\"area\"</span> }, kind, setKind),\n    (kind <span class=\"hljs-keyword\">switch</span>\n    {\n        <span class=\"hljs-string\">\"line\"</span>  => LineChart(data, d => d.X, d => d.Y),\n        <span class=\"hljs-string\">\"bar\"</span>   => BarChart(data, d => d.X, d => d.Y),\n        _       => AreaChart(data, d => d.X, d => d.Y),\n    })\n    .Title(<span class=\"hljs-string\">\"Revenue\"</span>)\n    .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n    .Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>));\n</code></pre>\n<p>Because each factory returns a <code>ChartElement&#x3C;T></code>, the modifier chain is\nidentical — the only branching is the constructor.</p>\n<h3 id=\"dropping-into-d3-for-a-custom-shape\">Dropping into D3 for a custom shape<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dropping-into-d3-for-a-custom-shape\">#</a></h3>\n<p>When the high-level surface doesn't fit (heatmap, parallel coordinates,\nswarm plot), render <code>D3Canvas</code> directly and compose D3 shape generators.\nThe <code>LinearScale</code> / <code>BandScale</code> factories are the same ones the high-level\ncharts use internally, so behavior matches — <code>.Nice()</code>, <code>.Ticks()</code>,\n<code>.Domain()</code> and friends work identically:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> x = <span class=\"hljs-keyword\">new</span> LinearScale().Domain(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">100</span>).Range(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">600</span>);\n<span class=\"hljs-keyword\">var</span> y = <span class=\"hljs-keyword\">new</span> LinearScale().Domain(min, max).Range(<span class=\"hljs-number\">220</span>, <span class=\"hljs-number\">20</span>).Nice();\n\n<span class=\"hljs-keyword\">return</span> D3Canvas(<span class=\"hljs-number\">600</span>, <span class=\"hljs-number\">240</span>,\n    D3Axes(x, y),\n    D3LinePath(data, d => x(d.X), d => y(d.Y)).Stroke(<span class=\"hljs-string\">\"#0078D4\"</span>),\n    ForEach(data, d => D3Circle(x(d.X), y(d.Y), <span class=\"hljs-number\">3</span>).Fill(<span class=\"hljs-string\">\"#0078D4\"</span>)));\n</code></pre>\n<p>This is the same composition strategy Observable Plot uses — geometric\nprimitives over scales — except the primitives render as WinUI shapes,\nnot SVG. For a heatmap, swap <code>D3LinePath</code> for nested\n<code>D3Rect</code>s; for parallel coordinates, repeat the line-path call per axis.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"constructing-the-data-array-in-render\">Constructing the data array in Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#constructing-the-data-array-in-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">new</span> SalesPoint[] { <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">120</span>), <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">180</span>), <span class=\"hljs-comment\">/* … */</span> };\n    <span class=\"hljs-keyword\">return</span> LineChart(data, d => d.Month, d => d.Revenue);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DynamicDataDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (points, updatePoints) = UseReducer(\n            Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">8</span>)\n                .Select(i => <span class=\"hljs-keyword\">new</span> SalesPoint(i, Random.Shared.Next(<span class=\"hljs-number\">50</span>, <span class=\"hljs-number\">500</span>)))\n                .ToList());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Dynamic Data\"</span>),\n            Button(<span class=\"hljs-string\">\"Randomize\"</span>, () => updatePoints(_ =>\n                Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">8</span>)\n                    .Select(i => <span class=\"hljs-keyword\">new</span> SalesPoint(i, Random.Shared.Next(<span class=\"hljs-number\">50</span>, <span class=\"hljs-number\">500</span>)))\n                    .ToList())),\n            BarChart&#x3C;SalesPoint>(points, d => d.Month, d => d.Revenue)\n                .Title(<span class=\"hljs-string\">\"Dynamic Revenue Data\"</span>)\n                .SeriesName(<span class=\"hljs-string\">\"Revenue\"</span>)\n                .Units(<span class=\"hljs-string\">\"months\"</span>, <span class=\"hljs-string\">\"USD\"</span>)\n                .Width(<span class=\"hljs-number\">600</span>).Height(<span class=\"hljs-number\">250</span>)\n                .Fill(<span class=\"hljs-string\">\"#E74C3C\"</span>)\n                .ShowGrid(<span class=\"hljs-literal\">true</span>).ShowAxes(<span class=\"hljs-literal\">true</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>Every render allocates a new array, so the chart's reference-equality\ncheck fails, scales re-derive, and shape pools reset — even though\nnothing visible changed. Hoist the static data into a <code>static readonly</code>\nfield or wrap dynamic data in <code>UseMemo(() => BuildData(...), deps)</code>.</p>\n<h3 id=\"using-color-as-the-only-series-encoding\">Using color as the only series encoding<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-color-as-the-only-series-encoding\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nLineChart(serverA, ...).Stroke(<span class=\"hljs-string\">\"red\"</span>),\nLineChart(serverB, ...).Stroke(<span class=\"hljs-string\">\"green\"</span>),\nLineChart(serverC, ...).Stroke(<span class=\"hljs-string\">\"blue\"</span>)\n</code></pre>\n<p>In forced-colors mode (Windows High Contrast), every stroke collapses\nto the active accent color and the user can't tell the series apart.\nPair color with <code>.SeriesShapes(MarkerShape.Circle, Square, Triangle)</code>\nor <code>.SeriesDashes(DashStyle.Solid, Dot, Dash)</code> so the series is\nidentifiable without color. Same principle as charts in WCAG 2.2 SC\n1.4.1.</p>\n<h3 id=\"animating-individual-d3-shapes-by-hand\">Animating individual D3 shapes by hand<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animating-individual-d3-shapes-by-hand\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nD3Circle(x(d.X), y(d.Y), <span class=\"hljs-number\">3</span>)\n    .Set(c =>\n    {\n        <span class=\"hljs-keyword\">var</span> anim = c.Compositor.CreateScalarKeyFrameAnimation();\n        anim.InsertKeyFrame(<span class=\"hljs-number\">0f</span>, <span class=\"hljs-number\">0.0f</span>);\n        anim.InsertKeyFrame(<span class=\"hljs-number\">1f</span>, <span class=\"hljs-number\">1.0f</span>);\n        c.StartAnimation(<span class=\"hljs-string\">\"Opacity\"</span>, anim);\n    })\n</code></pre>\n<p>Inline composition animation duplicates what <code>.OpacityTransition()</code>\nand <code>.Transition(Transition.Fade)</code> already do, and it bypasses the\nreduced-motion preference Reactor honors at the\n<a href=\"/animation-pipeline/\">animation pipeline</a> level. Use the Reactor\nanimation modifiers on the D3 shape — they work the same way they do\non a <code>Border</code> or <code>TextBlock</code>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start with the high-level DSL.</strong> <code>LineChart</code>, <code>BarChart</code>, <code>AreaChart</code>, and\n<code>PieChart</code> cover most dashboard needs without touching scales or generators.</p>\n<p><strong>Use <code>.ShowGrid(true)</code> for readability.</strong> Grid lines make it much easier\nto read values from a chart, especially line and area charts.</p>\n<p><strong>Keep datasets under 1000 points.</strong> Each data point creates WinUI shapes on\na Canvas. For larger datasets, aggregate or sample before charting.</p>\n<p><strong>Just change state for live data.</strong> Charts diff efficiently — updating\nstate triggers the reconciler to patch only what changed. For 60fps\nescape hatches, use <code>OnReady</code> to access the underlying Canvas directly.</p>\n<p><strong>Use donut charts (<code>InnerRadius > 0</code>) for proportional data.</strong> The center\nspace works well for displaying a total or label.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — Previous: transitions, keyframes, interaction states</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — Next: error boundaries, memoization, and performance tuning</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Bind chart data from virtualized lists and observable collections</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — Load chart data asynchronously with UseEffect</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — Use theme tokens for chart colors that adapt to dark mode</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Screen reader, keyboard navigation, and forced-colors guidance</li>\n</ul>",
      "text": "ReactorD3 sits on two layers and you choose the level you author at. The high-level DSL — LineChart , BarChart , AreaChart , PieChart , plus TreeChart and ForceGraph — covers the cases dashboards keep asking for: a series, an x accessor, a y accessor, and an opinionated visual surface that the library wires up for you (axes, grids, palettes, keyboard navigation, screen-reader summary). When the opinionated surface doesn't fit — when you want a slice's percent rendered inside the slice itself, when you want a heatmap, when you want a bespoke parallel-coordinates view — the same library exposes the D3 primitives that built the high-level shapes: LinearScale , BandScale , D3Canvas , D3LinePath&#x3C;T> , D3ArcPath , and the *LabelView escape hatches on the high-level charts. This is the same trade-off Recharts and Observable Plot draw — convenience first, escape hatch beneath — except every D3 primitive renders as a WinUI shape on the same Canvas the high-level charts use, so dropping a level never means switching renderers. Pick the high level by default; drop into D3 only when the higher-level surface refuses what you need. Charting # ReactorD3 brings data visualization to Microsoft.UI.Reactor (Reactor). The chart DSL provides high-level LineChart , BarChart , AreaChart , and PieChart factories that produce standard Reactor elements. You bind data, configure appearance with a fluent API, and the chart renders as native WinUI shapes on a Canvas. Import the DSL alongside the standard Reactor factories: using static Microsoft.UI.Reactor.Factories; using static Microsoft.UI.Reactor.Charting.Charts; Line Chart # LineChart draws a continuous line through your data points. Pass a collection and accessor functions for X and Y: class LineChartDemo : Component { public override Element Render () { var data = new SalesPoint[] { new ( 1 , 120 ), new ( 2 , 180 ), new ( 3 , 150 ), new ( 4 , 220 ), new ( 5 , 310 ), new ( 6 , 280 ), new ( 7 , 350 ), new ( 8 , 400 ), new ( 9 , 380 ), new ( 10 , 420 ), new ( 11 , 460 ), new ( 12 , 510 ) }; return VStack( 12 , SubHeading( \"Line Chart\" ), LineChart(data, d => d.Month, d => d.Revenue) .Title( \"Monthly Revenue — Line\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .AxisLabel(ChartAxisType.X, \"Month\" ) .AxisLabel(ChartAxisType.Y, \"Revenue (USD)\" ) .Width( 600 ).Height( 250 ) .Stroke( \"#0078D4\" ).StrokeWidth( 2.5 ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } The chart auto-scales axes to fit your data with .Nice() rounding. Toggle grid lines with .ShowGrid() and axis labels with .ShowAxes() . Bar Chart # BarChart renders vertical bars. Each data point becomes a bar whose height maps to the Y value: class BarChartDemo : Component { public override Element Render () { var data = new SalesPoint[] { new ( 1 , 340 ), new ( 2 , 420 ), new ( 3 , 510 ), new ( 4 , 380 ) }; return VStack( 12 , SubHeading( \"Bar Chart\" ), BarChart(data, d => d.Month, d => d.Revenue) .Title( \"Quarterly Revenue — Bar\" ) .SeriesName( \"Revenue\" ) .Units( \"quarters\" , \"USD\" ) .AxisLabel(ChartAxisType.X, \"Quarter\" ) .AxisLabel(ChartAxisType.Y, \"Revenue (USD)\" ) .Width( 600 ).Height( 250 ) .Fill( \"#50C878\" ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } Bar width is calculated automatically based on the number of data points. Use .Fill() to set the bar color and .FillOpacity() to control transparency. Area Chart # AreaChart fills the region between the data line and the baseline. It combines a filled area with a line overlay: class AreaChartDemo : Component { public override Element Render () { var data = new SalesPoint[] { new ( 1 , 50 ), new ( 2 , 120 ), new ( 3 , 200 ), new ( 4 , 350 ), new ( 5 , 480 ), new ( 6 , 600 ), new ( 7 , 720 ), new ( 8 , 850 ), new ( 9 , 1020 ), new ( 10 , 1150 ), new ( 11 , 1300 ), new ( 12 , 1500 ) }; return VStack( 12 , SubHeading( \"Area Chart\" ), AreaChart(data, d => d.Month, d => d.Revenue) .Title( \"Monthly Revenue — Area\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .AxisLabel(ChartAxisType.X, \"Month\" ) .AxisLabel(ChartAxisType.Y, \"Revenue (USD)\" ) .Width( 600 ).Height( 250 ) .Stroke( \"#9B59B6\" ).Fill( \"#9B59B6\" ) .FillOpacity( 0.2 ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } Use .FillOpacity() to control area transparency. A low value (0.15--0.3) lets grid lines show through while still filling the shape. Pie Chart # PieChart divides data into proportional arcs. Set .InnerRadius() > 0 for a donut chart: class PieChartDemo : Component { public override Element Render () { var data = new CategoryData[] { new ( \"Engineering\" , 42 ), new ( \"Marketing\" , 18 ), new ( \"Sales\" , 25 ), new ( \"Support\" , 15 ) }; return VStack( 12 , SubHeading( \"Pie Chart\" ), PieChart(data, d => d.Value, d => d.Name) .Title( \"Team Distribution\" ) .Description( \"Pie chart showing team size across Engineering, Marketing, Sales, and Support.\" ) .Width( 300 ).Height( 300 ) .InnerRadius( 60 ) .PadAngle( 0.03 ) ).Padding( 24 ); } } Pass a label accessor to display text at each arc's centroid. Colors cycle through the Category10 palette by default — override with .SetColors() . Chart Configuration # All chart types share a common set of builder methods: Method Default Purpose .Width(n) 400 Canvas width in pixels .Height(n) 300 Canvas height in pixels .Margin(t, r, b, l) 20, 20, 30, 40 Axis/label margins .Stroke(color) #4285f4 Line/border color .Fill(color) #4285f4 Fill color .StrokeWidth(n) 2 Line thickness .FillOpacity(n) 0.3 Fill transparency (0--1) .ShowAxes(bool) true Show axis lines and labels .ShowGrid(bool) true Show horizontal grid lines Colors accept any CSS-style string: #RGB , #RRGGBB , rgb(r,g,b) , or named colors like steelblue . For palette-aware colors that adapt to dark mode, see styling and the .Palette(...) accessibility modifier below. Binding Data to State # Charts are standard Reactor elements. When state changes, the chart re-renders with the new data: class CombinedChartDemo : Component { public override Element Render () { var (year, setYear) = UseState( 0 ); var years = new [] { \"2024\" , \"2025\" }; var data2024 = new SalesPoint[] { new ( 1 , 100 ), new ( 2 , 140 ), new ( 3 , 180 ), new ( 4 , 200 ), new ( 5 , 260 ), new ( 6 , 300 ) }; var data2025 = new SalesPoint[] { new ( 1 , 160 ), new ( 2 , 220 ), new ( 3 , 280 ), new ( 4 , 320 ), new ( 5 , 390 ), new ( 6 , 450 ) }; var data = year == 0 ? data2024 : data2025; return VStack( 12 , SubHeading( \"Interactive Chart\" ), ComboBox(years, year, setYear), AreaChart(data, d => d.Month, d => d.Revenue) .Title( \"Revenue by Year\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .Interactive() .Width( 600 ).Height( 250 ) .Stroke( \"#0078D4\" ).Fill( \"#0078D4\" ) .FillOpacity( 0.15 ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } Use hooks like UseState to drive data selection. The chart rebuilds on every render — keep datasets small (under ~1000 points) for smooth interaction. Dynamic Data Updates # Charts are native Reactor elements, so changing state is all you need. Use UseState , UseReducer , or any hook to update the data — the reconciler diffs the old and new element trees and patches only what changed: class DynamicDataDemo : Component { public override Element Render () { var (points, updatePoints) = UseReducer( Enumerable.Range( 1 , 8 ) .Select(i => new SalesPoint(i, Random.Shared.Next( 50 , 500 ))) .ToList()); return VStack( 12 , SubHeading( \"Dynamic Data\" ), Button( \"Randomize\" , () => updatePoints(_ => Enumerable.Range( 1 , 8 ) .Select(i => new SalesPoint(i, Random.Shared.Next( 50 , 500 ))) .ToList())), BarChart&#x3C;SalesPoint>(points, d => d.Month, d => d.Revenue) .Title( \"Dynamic Revenue Data\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .Width( 600 ).Height( 250 ) .Fill( \"#E74C3C\" ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } For high-frequency updates (60fps streaming), use OnReady to get a handle that exposes the underlying Canvas for direct manipulation. This is the same compositor-property escape hatch the animation pipeline uses — render the static geometry through Reactor, then mutate the live values directly. Custom Label Elements # Most charts only need the string-based label APIs ( AxisLabel , LabelAccessor , DataLabel ). Reach for the *View extensions only when plain text isn't enough — for icon-plus-text ticks, multi-line labels with mixed typography, or rendering a slice's percent inside the slice itself. Pie slice labels # LabelView replaces the built-in text label on each pie slice. The delegate receives the slice's data item plus a PieSliceLayout describing its geometry, and returns any Element: // Percent rendered inside the slice. The string label accessor // is still passed so screen readers describe the slice. PieChart(data, d => d.Value, d => d.Name) .Title( \"Team Distribution\" ) .Width( 300 ).Height( 300 ) .InnerRadius( 50 ).PadAngle( 0.02 ) .LabelView((d, layout) => TextBlock( $\" {layout.Fraction:P0} \" ) .FontSize( 12 ).Bold().Foreground( \"White\" )) PieSliceLayout exposes the slice's Index , Value , Fraction , CentroidX / CentroidY , StartAngle / EndAngle , InnerRadius / OuterRadius , and the resolved palette Color so a label can echo slice geometry without recomputing it. Axis tick labels # XTickLabelView and YTickLabelView replace the numeric tick labels with any Element. Each delegate receives the tick's domain value: // X axis ticks: render month name plus a caption per tick. LineChart(data, d => d.Month, d => d.Revenue) .Title( \"Revenue by Month\" ) .SeriesName( \"Revenue\" ) .Width( 600 ).Height( 220 ) .Stroke( \"#0078D4\" ).StrokeWidth( 2.5 ) .ShowGrid( true ).ShowAxes( true ) .XTickLabelView(t => VStack( 2 , TextBlock(months[Math.Clamp(( int )t - 1 , 0 , months.Length - 1 )]) .FontSize( 11 ).SemiBold(), TextBlock( \"month\" ).FontSize( 8 ).Opacity( 0.6 ))) Method Purpose PieChartElement&#x3C;T>.LabelView(Func&#x3C;T, PieSliceLayout, Element>) Replace the built-in slice text with any Element, anchored on the slice centroid ChartElement&#x3C;T>.XTickLabelView(Func&#x3C;double, Element>) Replace the X-axis tick label, horizontally centered on the tick ChartElement&#x3C;T>.YTickLabelView(Func&#x3C;double, Element>) Replace the Y-axis tick label, right-anchored to the axis edge The element you return is auto-anchored — you don't need a known size at construction time, and the chart re-positions on layout. It is also rendered non-interactive and hidden from the UIA tree, so the chart's structured accessibility description (see below) stays canonical. Always keep the string LabelAccessor (pie) or DataLabel (line/bar/area) set when you use a *View override; that's what screen readers read. Chart Accessibility # Charts are fully accessible out of the box. Add .Title() and .SeriesName() for screen-reader identification, .Units() for axis annotations, and .Interactive() for keyboard navigation: /// &#x3C;summary> /// Canonical accessible chart pattern — demonstrates all recommended accessibility /// modifiers for both static and interactive charts. Follow this pattern to ensure /// charts are fully accessible to screen readers, keyboard users, and users who /// need forced-colors or reduced-motion. /// &#x3C;/summary> class AccessibleChartDemo : Component { public override Element Render () { var data = new SalesPoint[] { new ( 1 , 120 ), new ( 2 , 180 ), new ( 3 , 150 ), new ( 4 , 220 ), new ( 5 , 310 ), new ( 6 , 280 ) }; return VStack( 12 , SubHeading( \"Accessible Chart\" ), // Static accessible chart: Title + SeriesName + Units LineChart(data, d => d.Month, d => d.Revenue) .Title( \"Monthly Revenue 2024\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .AxisLabel(ChartAxisType.X, \"Month\" ) .AxisLabel(ChartAxisType.Y, \"Revenue (USD)\" ) .Width( 600 ).Height( 250 ) .Stroke( \"#0078D4\" ).StrokeWidth( 2.5 ) .ShowGrid( true ).ShowAxes( true ), // Interactive accessible chart: adds keyboard nav and point invocation BarChart(data, d => d.Month, d => d.Revenue) .Title( \"Monthly Revenue — Interactive\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .Interactive() .Width( 600 ).Height( 250 ) .Fill( \"#50C878\" ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } Accessibility Modifiers # Method Purpose .Title(str) Visible title and accessible name .Description(str) Override auto-generated summary .SeriesName(str) Name the data series .Units(x, y) Axis unit annotations (e.g., \"months\", \"USD\") .AxisLabel(axis, str) Explicit axis label .Interactive() Enable keyboard navigation and virtual focus .OnPointInvoke(handler) Callback when Enter/Space pressed on a point .AlternateView(element) Toggle between chart and data table (T key) .Palette(palette) Curated colorblind-safe palette .SeriesShapes(shapes) Marker shapes for double-encoding .SeriesDashes(dashes) Dash patterns for double-encoding Screen readers announce the chart type, series count, data range, and individual point values. Keyboard users navigate with arrow keys, invoke points with Enter, and toggle an alternate data-table view with T. The full a11y story for the framework is in accessibility ; the chart-specific contract is that color is never the only encoding — combine .Palette(...) with .SeriesShapes(...) or .SeriesDashes(...) for any multi-series chart that ships to forced-colors users. Low-Level Drawing # For custom visualizations, ReactorD3 provides shape generators and a Canvas DSL. Import using static Microsoft.UI.Reactor.Charting.D3.D3Charts for: D3Canvas(w, h, children) — create a drawing surface D3Rect , D3Circle , D3Line , D3Path — primitive shapes D3Text , D3TextRight , D3TextCenter — positioned text D3LinePath&#x3C;T> , D3AreaPath&#x3C;T> , D3ArcPath — generator helpers D3Axes , D3Grid , D3Legend — axis/legend composites LinearScale , BandScale , LogScale — map data to pixels Scale Types # Scale Purpose LinearScale Continuous numeric mapping with .Nice() and .Ticks() BandScale&#x3C;T> Categorical mapping with bandwidth (bar charts) LogScale Logarithmic mapping for exponential data PowScale Power/sqrt mapping OrdinalScale&#x3C;T> Discrete-to-discrete mapping Caveat: Recreate the data array inside Render() only at your own peril. Charts compare incoming data by reference on the fast path — a new array on every render means the chart re-derives scales, re-runs the pie layout, and re-allocates the WinUI shape pool every frame, even when nothing changed. The symptom is a chart that flickers when an unrelated piece of state updates. The fix is the same shape Reactor uses everywhere: wrap the data in UseMemo(() => …, deps) keyed on whatever actually changes. The pattern is most obvious with PieChart — a fresh new[] { … } on every render makes each slice remount, the .Transition() enter/exit fires for slices that haven't moved, and the chart \"redraws itself\" on every keystroke into an unrelated TextBox . Patterns # Live-updating chart from a ticking source # A streaming chart — a price feed, a sensor reading — drives a sliding window of points. Hold the window in UseState , append on tick, drop the head once Count exceeds the window size: var (samples, setSamples) = UseState&#x3C;IReadOnlyList&#x3C;Sample>>(Array.Empty&#x3C;Sample>()); UseEffect(() => { using var timer = new PeriodicTimer(TimeSpan.FromMilliseconds( 200 )); while ( await timer.WaitForNextTickAsync(token)) { setSamples(prev => { var next = prev.Append(Sample.Now()).ToList(); return next.Count > 60 ? next.Skip(next.Count - 60 ).ToList() : next; }); } }, Array.Empty&#x3C; object >()); return LineChart(samples, s => s.Time.Ticks, s => s.Value) .Title( \"Live feed\" ) .Width( 600 ).Height( 220 ) .Stroke( \"#0078D4\" ); The chart redraws on every tick. For sub-100ms cadence, drop into OnReady and mutate the Canvas directly — the reconciler is fast but it isn't free. See effects-scheduling for the timer-cleanup pattern. Switching chart type without losing the data binding # The same data feeds a line, a bar, or an area chart — let users pick the shape. Hold the chart kind in state, switch the factory call: var (kind, setKind) = UseState( \"line\" ); return VStack( 8 , ComboBox( new [] { \"line\" , \"bar\" , \"area\" }, kind, setKind), (kind switch { \"line\" => LineChart(data, d => d.X, d => d.Y), \"bar\" => BarChart(data, d => d.X, d => d.Y), _ => AreaChart(data, d => d.X, d => d.Y), }) .Title( \"Revenue\" ) .Width( 600 ).Height( 250 ) .Stroke( \"#0078D4\" )); Because each factory returns a ChartElement&#x3C;T> , the modifier chain is identical — the only branching is the constructor. Dropping into D3 for a custom shape # When the high-level surface doesn't fit (heatmap, parallel coordinates, swarm plot), render D3Canvas directly and compose D3 shape generators. The LinearScale / BandScale factories are the same ones the high-level charts use internally, so behavior matches — .Nice() , .Ticks() , .Domain() and friends work identically: var x = new LinearScale().Domain( 0 , 100 ).Range( 0 , 600 ); var y = new LinearScale().Domain(min, max).Range( 220 , 20 ).Nice(); return D3Canvas( 600 , 240 , D3Axes(x, y), D3LinePath(data, d => x(d.X), d => y(d.Y)).Stroke( \"#0078D4\" ), ForEach(data, d => D3Circle(x(d.X), y(d.Y), 3 ).Fill( \"#0078D4\" ))); This is the same composition strategy Observable Plot uses — geometric primitives over scales — except the primitives render as WinUI shapes, not SVG. For a heatmap, swap D3LinePath for nested D3Rect s; for parallel coordinates, repeat the line-path call per axis. Common Mistakes # Constructing the data array in Render # // Don't: public override Element Render () { var data = new SalesPoint[] { new ( 1 , 120 ), new ( 2 , 180 ), /* … */ }; return LineChart(data, d => d.Month, d => d.Revenue); } class DynamicDataDemo : Component { public override Element Render () { var (points, updatePoints) = UseReducer( Enumerable.Range( 1 , 8 ) .Select(i => new SalesPoint(i, Random.Shared.Next( 50 , 500 ))) .ToList()); return VStack( 12 , SubHeading( \"Dynamic Data\" ), Button( \"Randomize\" , () => updatePoints(_ => Enumerable.Range( 1 , 8 ) .Select(i => new SalesPoint(i, Random.Shared.Next( 50 , 500 ))) .ToList())), BarChart&#x3C;SalesPoint>(points, d => d.Month, d => d.Revenue) .Title( \"Dynamic Revenue Data\" ) .SeriesName( \"Revenue\" ) .Units( \"months\" , \"USD\" ) .Width( 600 ).Height( 250 ) .Fill( \"#E74C3C\" ) .ShowGrid( true ).ShowAxes( true ) ).Padding( 24 ); } } Every render allocates a new array, so the chart's reference-equality check fails, scales re-derive, and shape pools reset — even though nothing visible changed. Hoist the static data into a static readonly field or wrap dynamic data in UseMemo(() => BuildData(...), deps) . Using color as the only series encoding # // Don't: LineChart(serverA, ...).Stroke( \"red\" ), LineChart(serverB, ...).Stroke( \"green\" ), LineChart(serverC, ...).Stroke( \"blue\" ) In forced-colors mode (Windows High Contrast), every stroke collapses to the active accent color and the user can't tell the series apart. Pair color with .SeriesShapes(MarkerShape.Circle, Square, Triangle) or .SeriesDashes(DashStyle.Solid, Dot, Dash) so the series is identifiable without color. Same principle as charts in WCAG 2.2 SC 1.4.1. Animating individual D3 shapes by hand # // Don't: D3Circle(x(d.X), y(d.Y), 3 ) .Set(c => { var anim = c.Compositor.CreateScalarKeyFrameAnimation(); anim.InsertKeyFrame( 0f , 0.0f ); anim.InsertKeyFrame( 1f , 1.0f ); c.StartAnimation( \"Opacity\" , anim); }) Inline composition animation duplicates what .OpacityTransition() and .Transition(Transition.Fade) already do, and it bypasses the reduced-motion preference Reactor honors at the animation pipeline level. Use the Reactor animation modifiers on the D3 shape — they work the same way they do on a Border or TextBlock . Tips # Start with the high-level DSL. LineChart , BarChart , AreaChart , and PieChart cover most dashboard needs without touching scales or generators. Use .ShowGrid(true) for readability. Grid lines make it much easier to read values from a chart, especially line and area charts. Keep datasets under 1000 points. Each data point creates WinUI shapes on a Canvas. For larger datasets, aggregate or sample before charting. Just change state for live data. Charts diff efficiently — updating state triggers the reconciler to patch only what changed. For 60fps escape hatches, use OnReady to access the underlying Canvas directly. Use donut charts ( InnerRadius > 0 ) for proportional data. The center space works well for displaying a total or label. Next Steps # Animation — Previous: transitions, keyframes, interaction states Advanced Patterns — Next: error boundaries, memoization, and performance tuning Collections — Bind chart data from virtualized lists and observable collections Effects and Lifecycle — Load chart data asynchronously with UseEffect Styling and Theming — Use theme tokens for chart colors that adapt to dark mode Accessibility — Screen reader, keyboard navigation, and forced-colors guidance",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/charting.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/charting.md",
      "headings": [
        {
          "depth": 2,
          "id": "line-chart",
          "text": "Line Chart"
        },
        {
          "depth": 2,
          "id": "bar-chart",
          "text": "Bar Chart"
        },
        {
          "depth": 2,
          "id": "area-chart",
          "text": "Area Chart"
        },
        {
          "depth": 2,
          "id": "pie-chart",
          "text": "Pie Chart"
        },
        {
          "depth": 2,
          "id": "chart-configuration",
          "text": "Chart Configuration"
        },
        {
          "depth": 2,
          "id": "binding-data-to-state",
          "text": "Binding Data to State"
        },
        {
          "depth": 2,
          "id": "dynamic-data-updates",
          "text": "Dynamic Data Updates"
        },
        {
          "depth": 2,
          "id": "custom-label-elements",
          "text": "Custom Label Elements"
        },
        {
          "depth": 3,
          "id": "pie-slice-labels",
          "text": "Pie slice labels"
        },
        {
          "depth": 3,
          "id": "axis-tick-labels",
          "text": "Axis tick labels"
        },
        {
          "depth": 2,
          "id": "chart-accessibility",
          "text": "Chart Accessibility"
        },
        {
          "depth": 3,
          "id": "accessibility-modifiers",
          "text": "Accessibility Modifiers"
        },
        {
          "depth": 2,
          "id": "low-level-drawing",
          "text": "Low-Level Drawing"
        },
        {
          "depth": 2,
          "id": "scale-types",
          "text": "Scale Types"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "live-updating-chart-from-a-ticking-source",
          "text": "Live-updating chart from a ticking source"
        },
        {
          "depth": 3,
          "id": "switching-chart-type-without-losing-the-data-binding",
          "text": "Switching chart type without losing the data binding"
        },
        {
          "depth": 3,
          "id": "dropping-into-d3-for-a-custom-shape",
          "text": "Dropping into D3 for a custom shape"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "constructing-the-data-array-in-render",
          "text": "Constructing the data array in Render"
        },
        {
          "depth": 3,
          "id": "using-color-as-the-only-series-encoding",
          "text": "Using color as the only series encoding"
        },
        {
          "depth": 3,
          "id": "animating-individual-d3-shapes-by-hand",
          "text": "Animating individual D3 shapes by hand"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Navigation",
      "path": "navigation.md",
      "slug": "/navigation/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/basics/navigation-basics\">Navigation Basics</a>.</p>\n</blockquote>\n<p>Microsoft.UI.Reactor (Reactor)'s navigation is a stack of typed routes that lives in a hook. You\ncall <code>UseNavigation(initialRoute)</code> once, in a root component, and you get\nback a <code>NavigationHandle&#x3C;TRoute></code> whose <code>Navigate</code>, <code>GoBack</code>, <code>Replace</code>,\nand <code>Reset</code> methods mutate the stack. A <code>NavigationHost</code> reads the\ncurrent route on each render and projects it through a <code>route => Element</code> function, so the page tree is a pure function of the stack —\nexactly the same shape as the rest of the framework. There is no\n<code>Frame.Navigate(typeof(Page))</code>, no <code>INotifyPropertyChanged</code> on a\n<code>CurrentPage</code> property, no <code>NavigationService</code> registered in DI. The\nstack is state; the page is render output. That makes lifecycle\nhooks (<a href=\"/hooks/\"><code>UseNavigationLifecycle</code></a>) testable, deep links\n(<code>DeepLinkMap&#x3C;TRoute></code>) trivially round-trippable through <code>GetState</code> /\n<code>SetState</code>, and the back stack visible to your component code as a\nplain <code>IReadOnlyList&#x3C;TRoute></code>. Read <a href=\"#defining-routes\">Defining Routes</a>\nfirst if you're new; the rest of the page composes against that one\nshape.</p>\n<h1 id=\"navigation\">Navigation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigation\">#</a></h1>\n<p>Reactor uses a stack-based navigation model with type-safe routes. You define\nyour routes as an enum, create a navigation handle with\n<a href=\"/hooks/\"><code>UseNavigation</code></a>, and render the current page with <code>NavigationHost</code>.</p>\n<h2 id=\"defining-routes\">Defining Routes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#defining-routes\">#</a></h2>\n<p>Start by defining an enum for your pages:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-built_in\">enum</span> Route { Home, Settings, Profile, Details }\n</code></pre>\n<p>Each enum value represents a distinct page in your app. The navigation system\nuses this type to ensure you can only navigate to valid routes. For pages\nthat carry data — a detail page bound to a row ID, a wizard step with\ncaptured form state — use a discriminated-union pattern with C# records\nimplementing a sealed interface instead of a flat enum.</p>\n<h2 id=\"basic-navigation\">Basic Navigation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#basic-navigation\">#</a></h2>\n<p>Call <code>UseNavigation(Route.Home)</code> to create a navigation handle rooted at\nyour initial route. Use <code>NavigationHost</code> to render the current page:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BasicNavDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">$\"Current: <span class=\"hljs-subst\">{nav.CurrentRoute}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Stack depth: <span class=\"hljs-subst\">{nav.Depth}</span>\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => nav.Navigate(Route.Home)),\n                Button(<span class=\"hljs-string\">\"Settings\"</span>, () => nav.Navigate(Route.Settings)),\n                Button(<span class=\"hljs-string\">\"Profile\"</span>, () => nav.Navigate(Route.Profile)),\n                Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())\n                    .IsEnabled(nav.CanGoBack)\n            ),\n            NavigationHost(nav, route => route <span class=\"hljs-keyword\">switch</span>\n            {\n                Route.Home => TextBlock(<span class=\"hljs-string\">\"Welcome home!\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Padding(<span class=\"hljs-number\">16</span>),\n                Route.Settings => TextBlock(<span class=\"hljs-string\">\"Settings page\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Padding(<span class=\"hljs-number\">16</span>),\n                Route.Profile => TextBlock(<span class=\"hljs-string\">\"Your profile\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Padding(<span class=\"hljs-number\">16</span>),\n                _ => TextBlock(<span class=\"hljs-string\">\"Not found\"</span>).Padding(<span class=\"hljs-number\">16</span>)\n            })\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/basic-navigation.png\" alt=\"Basic navigation\"></p>\n<p>Here is what each piece does:</p>\n<ul>\n<li><strong><code>UseNavigation(Route.Home)</code></strong> creates a <code>NavigationHandle&#x3C;Route></code> with\n<code>Home</code> as the initial route. Call this once in your root component.</li>\n<li><strong><code>nav.Navigate(route)</code></strong> pushes a new route onto the stack.</li>\n<li><strong><code>nav.GoBack()</code></strong> pops the current route and returns to the previous one.</li>\n<li><strong><code>NavigationHost(nav, route => ...)</code></strong> renders whichever element the\nroute map returns for the current route.</li>\n</ul>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Shape</th><th>Purpose</th></tr></thead><tbody><tr><td><code>UseNavigation&#x3C;TRoute>(initial)</code></td><td>hook (root)</td><td>Create the navigation handle for this subtree.</td></tr><tr><td><code>UseNavigation&#x3C;TRoute>()</code></td><td>hook (descendant)</td><td>Read the ancestor handle via context.</td></tr><tr><td><code>UseNavigationLifecycle(...)</code></td><td>hook</td><td>Page-side <code>onNavigatedTo</code> / <code>onNavigatingFrom</code> / <code>onNavigatedFrom</code>. The <code>from</code> callback receives a context with <code>.Cancel()</code>.</td></tr><tr><td><code>UseSystemBackButton(nav, window)</code></td><td>hook</td><td>Wire the title-bar / hardware Back button to <code>nav.GoBack</code>.</td></tr><tr><td><code>NavigationHost(nav, routeMap)</code></td><td>element</td><td>Renders the current route. Set <code>Transition</code>, <code>CacheMode</code>, <code>CacheSize</code>.</td></tr><tr><td><code>NavigationView(items, content)</code></td><td>element</td><td>Sidebar shell. Use <code>.SelectedTagChanged(handler)</code> for selection events.</td></tr><tr><td><code>TabView(tabs)</code></td><td>element</td><td>Parallel workspaces; tabs keep their own state.</td></tr><tr><td><code>BreadcrumbBar(items)</code></td><td>element</td><td>Trail of ancestor routes for drill-down nav.</td></tr><tr><td><code>Frame(...)</code></td><td>element</td><td>Raw WinUI Frame for XAML-page interop; <code>.Navigating</code>, <code>.Navigated</code>, <code>.NavigationFailed</code>.</td></tr><tr><td><code>DeepLinkMap&#x3C;TRoute></code></td><td>type</td><td>URI pattern → route factory. <code>.Resolve(uri)</code> returns the matched route and back-stack.</td></tr><tr><td><code>NavigationDiagnostics</code></td><td>static</td><td>Static events for tracing: <code>NavigationRequested</code>, <code>NavigationCompleted</code>, <code>NavigationCancelled</code>, cache and transition events.</td></tr></tbody></table>\n<h2 id=\"navigationview\">NavigationView<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigationview\">#</a></h2>\n<p><code>NavigationView</code> creates a sidebar menu with icons. Pair it with\n<code>NavigationHost</code> for a standard app layout:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NavViewDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n        <span class=\"hljs-keyword\">return</span> NavigationView(\n            [\n                NavItem(<span class=\"hljs-string\">\"Home\"</span>, icon: <span class=\"hljs-string\">\"Home\"</span>, tag: <span class=\"hljs-string\">\"Home\"</span>),\n                NavItem(<span class=\"hljs-string\">\"Settings\"</span>, icon: <span class=\"hljs-string\">\"Setting\"</span>, tag: <span class=\"hljs-string\">\"Settings\"</span>),\n                NavItem(<span class=\"hljs-string\">\"Profile\"</span>, icon: <span class=\"hljs-string\">\"Contact\"</span>, tag: <span class=\"hljs-string\">\"Profile\"</span>)\n            ],\n            content: NavigationHost(nav, route => route <span class=\"hljs-keyword\">switch</span>\n            {\n                Route.Home => VStack(<span class=\"hljs-number\">12</span>, Heading(<span class=\"hljs-string\">\"Home\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Welcome to the app.\"</span>),\n                    Button(<span class=\"hljs-string\">\"Go to Settings\"</span>,\n                        () => nav.Navigate(Route.Settings))).Padding(<span class=\"hljs-number\">24</span>),\n                Route.Settings => VStack(<span class=\"hljs-number\">12</span>, Heading(<span class=\"hljs-string\">\"Settings\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Configure your preferences.\"</span>),\n                    Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())).Padding(<span class=\"hljs-number\">24</span>),\n                Route.Profile => VStack(<span class=\"hljs-number\">12</span>, Heading(<span class=\"hljs-string\">\"Profile\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"View your profile info.\"</span>)).Padding(<span class=\"hljs-number\">24</span>),\n                _ => TextBlock(<span class=\"hljs-string\">\"Not found\"</span>).Padding(<span class=\"hljs-number\">24</span>)\n            })\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/navigation-view.png\" alt=\"NavigationView with sidebar\"></p>\n<p><code>NavItem</code> takes a label, an optional icon name (from the Segoe Fluent Icons\nfont), and an optional tag string. The <code>NavigationView</code> handles selection\nstate and displays the content you pass.</p>\n<h2 id=\"stack-operations\">Stack Operations<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stack-operations\">#</a></h2>\n<p>The <code>NavigationHandle</code> supports several stack manipulation methods beyond\nsimple push and pop:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StackOperationsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">$\"Current: <span class=\"hljs-subst\">{nav.CurrentRoute}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Back stack: <span class=\"hljs-subst\">{nav.BackStack.Count}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Forward stack: <span class=\"hljs-subst\">{nav.ForwardStack.Count}</span>\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Navigate\"</span>, () =>\n                    nav.Navigate(Route.Settings)),\n                Button(<span class=\"hljs-string\">\"Replace\"</span>, () =>\n                    nav.Replace(Route.Profile)),\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () =>\n                    nav.Reset(Route.Home)),\n                Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())\n                    .IsEnabled(nav.CanGoBack),\n                Button(<span class=\"hljs-string\">\"Forward\"</span>, () => nav.GoForward())\n                    .IsEnabled(nav.CanGoForward)\n            ),\n            NavigationHost(nav, route =>\n                TextBlock(<span class=\"hljs-string\">$\"Page: <span class=\"hljs-subst\">{route}</span>\"</span>)\n                    .FontSize(<span class=\"hljs-number\">18</span>).Padding(<span class=\"hljs-number\">16</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/stack-operations.png\" alt=\"Stack operations\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Effect</th></tr></thead><tbody><tr><td><code>Navigate(route)</code></td><td>Push route onto back stack, navigate to it</td></tr><tr><td><code>GoBack()</code></td><td>Pop current, return to previous</td></tr><tr><td><code>GoForward()</code></td><td>Move forward (after going back)</td></tr><tr><td><code>Replace(route)</code></td><td>Swap current route without touching the stack</td></tr><tr><td><code>Reset(route)</code></td><td>Clear all stacks, start fresh at route</td></tr><tr><td><code>PopTo(predicate)</code></td><td>Pop until a matching route is found</td></tr></tbody></table>\n<p>Use <code>CanGoBack</code> and <code>CanGoForward</code> to enable/disable navigation buttons.</p>\n<h2 id=\"page-lifecycle\">Page Lifecycle<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#page-lifecycle\">#</a></h2>\n<p><code>UseNavigationLifecycle</code> lets a component react to navigation events. Use it\nto load data when a page appears or save state when it disappears:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LifecyclePage</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (log, updateLog) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n\n        UseNavigationLifecycle(\n            onNavigatedTo: ctx =>\n                updateLog(l => [.. l,\n                    <span class=\"hljs-string\">$\"Arrived from <span class=\"hljs-subst\">{ctx.PreviousRoute}</span>\"</span>]),\n            onNavigatingFrom: ctx =>\n                updateLog(l => [.. l,\n                    <span class=\"hljs-string\">$\"Leaving to <span class=\"hljs-subst\">{ctx.TargetRoute}</span>\"</span>]),\n            onNavigatedFrom: ctx =>\n                updateLog(l => [.. l,\n                    <span class=\"hljs-string\">$\"Left for <span class=\"hljs-subst\">{ctx.TargetRoute}</span>\"</span>])\n        );\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Lifecycle Events\"</span>),\n            VStack(<span class=\"hljs-number\">4</span>,\n                log.TakeLast(<span class=\"hljs-number\">5</span>).Select(entry =>\n                    TextBlock(entry).FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.7</span>)\n                ).ToArray()\n            )\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/lifecycle.png\" alt=\"Lifecycle events\"></p>\n<p>The four callbacks fire at different points:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Callback</th><th>When it fires</th></tr></thead><tbody><tr><td><code>onNavigatingTo</code></td><td>Before this page becomes active. Call <code>ctx.Cancel()</code> to reject from the destination side.</td></tr><tr><td><code>onNavigatedTo</code></td><td>After this page becomes active</td></tr><tr><td><code>onNavigatingFrom</code></td><td>Before leaving this page. Call <code>ctx.Cancel()</code> to block — the classic \"unsaved changes\" guard.</td></tr><tr><td><code>onNavigatedFrom</code></td><td>After this page is no longer active</td></tr></tbody></table>\n<p>Use <code>onNavigatedTo</code> to fetch data or start timers. Use <code>onNavigatingFrom</code> to\nsave drafts or confirm unsaved changes — call <code>ctx.Cancel()</code> on the\n<code>NavigatingFromContext</code> to prevent navigation entirely. See\n<a href=\"/effects/\">Effects and Lifecycle</a> for more on lifecycle patterns and\nasync cancellation.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Reset(route)</code>, <code>Replace(route)</code>, and <code>PopTo(predicate)</code> all run the\n<code>onNavigatingFrom</code> guard — <code>ctx.Cancel()</code> will block them. But\n<code>SetState(state)</code> does <strong>not</strong>: it routes through <code>NavigationStack.RestoreState</code>\nwhich bypasses <code>InvokeGuard</code> entirely and fires <code>Navigated</code> with\n<code>NavigationMode.Reset</code> after the stacks are already rewritten. If your\nunsaved-changes guard lives only in <code>onNavigatingFrom</code>, an app that\nrestores a saved navigation state from disk on launch will silently\noverwrite the active page without ever asking the user. Either gate\nstate restoration explicitly (check the in-memory dirty flag before\ncalling <code>SetState</code>), or surface the same dirty check from a higher\nlevel — e.g. an app-shutdown handler in <code>Window.Closed</code>.</p>\n</blockquote>\n<h2 id=\"page-transitions\">Page Transitions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#page-transitions\">#</a></h2>\n<p><code>NavigationHost</code> supports animated page transitions. Set the <code>Transition</code>\nproperty to control how pages animate in and out:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PageTransitionsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Page Transitions\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => nav.Navigate(Route.Home)),\n                Button(<span class=\"hljs-string\">\"Settings\"</span>, () => nav.Navigate(Route.Settings)),\n                Button(<span class=\"hljs-string\">\"Profile\"</span>, () => nav.Navigate(Route.Profile)),\n                Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())\n                    .IsEnabled(nav.CanGoBack)\n            ),\n            NavigationHost(nav, route => route <span class=\"hljs-keyword\">switch</span>\n            {\n                Route.Home => VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Home\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n                    TextBlock(<span class=\"hljs-string\">\"Slide transition on navigate\"</span>)).Padding(<span class=\"hljs-number\">16</span>),\n                Route.Settings => VStack(<span class=\"hljs-number\">8</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Settings\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n                    TextBlock(<span class=\"hljs-string\">\"DrillIn transition to detail\"</span>)).Padding(<span class=\"hljs-number\">16</span>),\n                _ => TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{route}</span>\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Padding(<span class=\"hljs-number\">16</span>)\n            }) <span class=\"hljs-keyword\">with</span> { Transition = NavigationTransition.DrillIn() }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/page-transitions.png\" alt=\"Page transitions\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Transition</th><th>Effect</th></tr></thead><tbody><tr><td><code>NavigationTransition.Slide()</code></td><td>Slide + fade (default)</td></tr><tr><td><code>NavigationTransition.Fade()</code></td><td>Crossfade</td></tr><tr><td><code>NavigationTransition.DrillIn()</code></td><td>Scale + fade from center</td></tr><tr><td><code>NavigationTransition.Spring()</code></td><td>Spring-physics slide</td></tr><tr><td><code>NavigationTransition.None</code></td><td>Instant swap</td></tr></tbody></table>\n<p>Transitions run on the compositor thread — no managed-code involvement during\nplayback. GoBack automatically reverses the direction. See\n<a href=\"/animation/\">Animation</a> for more on compositor transitions.</p>\n<h2 id=\"deep-linking\">Deep Linking<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#deep-linking\">#</a></h2>\n<p><code>DeepLinkMap</code> maps URI patterns to route constructors. Use it to restore\nnavigation state from activation URIs or protocol handlers:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DeepLinkingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> map = UseMemo(() => <span class=\"hljs-keyword\">new</span> DeepLinkMap&#x3C;Route>()\n            .Map(<span class=\"hljs-string\">\"/\"</span>, _ => Route.Home)\n            .Map(<span class=\"hljs-string\">\"/settings\"</span>, _ => Route.Settings)\n            .Map(<span class=\"hljs-string\">\"/profile\"</span>, _ => Route.Profile));\n\n        <span class=\"hljs-keyword\">var</span> (result, setResult) = UseState(<span class=\"hljs-string\">\"(none)\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Deep Linking\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Resolve /\"</span>, () =>\n                    setResult(<span class=\"hljs-string\">$\"/ -> <span class=\"hljs-subst\">{map.Resolve(<span class=\"hljs-string\">\"/\"</span>).Matched}</span>\"</span>)),\n                Button(<span class=\"hljs-string\">\"Resolve /settings\"</span>, () =>\n                    setResult(<span class=\"hljs-string\">$\"/settings -> <span class=\"hljs-subst\">{map.Resolve(<span class=\"hljs-string\">\"/settings\"</span>).Matched}</span>\"</span>)),\n                Button(<span class=\"hljs-string\">\"Resolve /unknown\"</span>, () =>\n                    setResult(<span class=\"hljs-string\">$\"/unknown -> <span class=\"hljs-subst\">{map.Resolve(<span class=\"hljs-string\">\"/unknown\"</span>).Matched}</span>\"</span>))\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"Result: <span class=\"hljs-subst\">{result}</span>\"</span>).FontSize(<span class=\"hljs-number\">14</span>).Opacity(<span class=\"hljs-number\">0.7</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/deep-linking.png\" alt=\"Deep linking\"></p>\n<p>Pattern segments support rich matching:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Segment</th><th>Matches</th></tr></thead><tbody><tr><td><code>/literal</code></td><td>Exact match</td></tr><tr><td><code>/{param}</code></td><td>Captures a string</td></tr><tr><td><code>/{param:int}</code></td><td>Typed capture (int, long, bool, Guid)</td></tr><tr><td><code>/{param?}</code></td><td>Optional parameter</td></tr><tr><td><code>/{**}</code></td><td>Wildcard — matches remaining path</td></tr><tr><td><code>?key=value</code></td><td>Query string parameters</td></tr></tbody></table>\n<p>Use <code>RouteArgs.Get&#x3C;T>(name)</code> for required params, <code>RouteArgs.GetOrDefault&#x3C;T>(name)</code>\nfor optional ones, and <code>RouteArgs.Query&#x3C;T>(name)</code> for query string values.\nThe <code>backStackFactory</code> parameter builds a synthetic back stack so GoBack works\neven after deep-linked entry.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DeepLinkQueryDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (info, setInfo) = UseState(<span class=\"hljs-string\">\"(none)\"</span>);\n\n        <span class=\"hljs-comment\">// RouteArgs are available inside the factory lambda —</span>\n        <span class=\"hljs-comment\">// capture typed params and query values there</span>\n        <span class=\"hljs-keyword\">var</span> map = UseMemo(() => <span class=\"hljs-keyword\">new</span> DeepLinkMap&#x3C;Route>()\n            .Map(<span class=\"hljs-string\">\"/\"</span>, _ => Route.Home)\n            .Map(<span class=\"hljs-string\">\"/settings\"</span>, _ => Route.Settings)\n            .Map(<span class=\"hljs-string\">\"/users/{id:int}/posts/{postId:int}\"</span>,\n                <span class=\"hljs-keyword\">args</span> =>\n                {\n                    <span class=\"hljs-keyword\">var</span> userId = <span class=\"hljs-keyword\">args</span>.Get&#x3C;<span class=\"hljs-built_in\">int</span>>(<span class=\"hljs-string\">\"id\"</span>);\n                    <span class=\"hljs-keyword\">var</span> postId = <span class=\"hljs-keyword\">args</span>.Get&#x3C;<span class=\"hljs-built_in\">int</span>>(<span class=\"hljs-string\">\"postId\"</span>);\n                    <span class=\"hljs-keyword\">var</span> sort = <span class=\"hljs-keyword\">args</span>.Query&#x3C;<span class=\"hljs-built_in\">string</span>>(<span class=\"hljs-string\">\"sort\"</span>);\n                    setInfo(<span class=\"hljs-string\">$\"userId=<span class=\"hljs-subst\">{userId}</span>, postId=<span class=\"hljs-subst\">{postId}</span>, sort=<span class=\"hljs-subst\">{sort}</span>\"</span>);\n                    <span class=\"hljs-keyword\">return</span> Route.Details;\n                },\n                () => <span class=\"hljs-keyword\">new</span>[] { Route.Home })\n        );\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Deep Link Query Params\"</span>),\n            Button(<span class=\"hljs-string\">\"Resolve /users/42/posts/7?sort=date\"</span>, () =>\n                map.Resolve(<span class=\"hljs-string\">\"/users/42/posts/7?sort=date\"</span>)),\n            TextBlock(<span class=\"hljs-string\">$\"Captured: <span class=\"hljs-subst\">{info}</span>\"</span>).FontSize(<span class=\"hljs-number\">14</span>).Opacity(<span class=\"hljs-number\">0.7</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p>This example shows query parameters and optional path segments. The resolver\nparses <code>/users/42/posts?sort=date&#x26;page=2</code> into typed values you can use\ndirectly in route constructors.</p>\n<h2 id=\"page-caching\">Page Caching<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#page-caching\">#</a></h2>\n<p>Set <code>CacheMode</code> on <code>NavigationHost</code> to preserve page state across\nnavigations. Cached pages keep their visual tree alive instead of remounting:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PageCachingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Page Caching\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Text input is preserved across navigations.\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => nav.Navigate(Route.Home)),\n                Button(<span class=\"hljs-string\">\"Settings\"</span>, () => nav.Navigate(Route.Settings)),\n                Button(<span class=\"hljs-string\">\"Back\"</span>, () => nav.GoBack())\n                    .IsEnabled(nav.CanGoBack)\n            ),\n            NavigationHost(nav, route => route <span class=\"hljs-keyword\">switch</span>\n            {\n                Route.Home => CachedPage(<span class=\"hljs-string\">\"Home\"</span>),\n                Route.Settings => CachedPage(<span class=\"hljs-string\">\"Settings\"</span>),\n                _ => TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{route}</span>\"</span>).Padding(<span class=\"hljs-number\">16</span>)\n            }) <span class=\"hljs-keyword\">with</span>\n            {\n                CacheMode = NavigationCacheMode.Enabled,\n                CacheSize = <span class=\"hljs-number\">5</span>\n            }\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">CachedPage</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> name</span>)</span> =>\n        VStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(name).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Type here — state persists\"</span>)\n        ).Padding(<span class=\"hljs-number\">16</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/page-caching.png\" alt=\"Page caching\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Cache mode</th><th>Behavior</th></tr></thead><tbody><tr><td><code>Disabled</code></td><td>Always unmount/remount (default)</td></tr><tr><td><code>Enabled</code></td><td>LRU cache up to <code>CacheSize</code> entries</td></tr><tr><td><code>Required</code></td><td>Always cached, never evicted</td></tr></tbody></table>\n<p>Caching preserves scroll position, text input, and component state. Use it\nfor pages that are expensive to render or where losing state would frustrate\nthe user. <code>Required</code> is the right choice for a small fixed set of always-hot\npages (an app's three tab panes); for an unbounded route space (a detail\npage parameterized by row ID), stick with <code>Enabled</code> and tune <code>CacheSize</code> —\n<code>Required</code> never evicts and will retain every visited route's element tree\nfor the process lifetime.</p>\n<h2 id=\"tabview\">TabView<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tabview\">#</a></h2>\n<p>For tab-based navigation, use <code>TabView</code> with <code>Tab</code> items. Each tab holds its\nown content independently:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TabNavDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> TabView(\n            Tab(<span class=\"hljs-string\">\"Documents\"</span>,\n                VStack(<span class=\"hljs-number\">12</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Your documents appear here.\"</span>),\n                    Button(<span class=\"hljs-string\">\"New Document\"</span>, () => { })\n                ).Padding(<span class=\"hljs-number\">24</span>)\n            ),\n            Tab(<span class=\"hljs-string\">\"Recent\"</span>,\n                VStack(<span class=\"hljs-number\">12</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Recently opened files.\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"No recent files.\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>)\n                ).Padding(<span class=\"hljs-number\">24</span>)\n            ),\n            Tab(<span class=\"hljs-string\">\"Shared\"</span>,\n                VStack(<span class=\"hljs-number\">12</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Files shared with you.\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Nothing shared yet.\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>)\n                ).Padding(<span class=\"hljs-number\">24</span>)\n            )\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/tab-navigation.png\" alt=\"Tab navigation\"></p>\n<p>Unlike stack-based navigation, tabs keep all their content alive simultaneously.\nUse tabs when users need to switch freely between parallel workspaces.</p>\n<h2 id=\"state-serialization\">State Serialization<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state-serialization\">#</a></h2>\n<p><code>NavigationHandle</code> can capture and restore the full navigation state —\nback stack, current route, and forward stack — as a plain\n<code>NavigationState&#x3C;TRoute></code> snapshot. Reactor intentionally does <strong>not</strong>\npick a serialization format: persist the snapshot however you like\n(JSON via your own source-gen context, MessagePack, hand-rolled binary).</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StateSerializationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n        <span class=\"hljs-keyword\">var</span> (savedJson, setSavedJson) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"State Serialization\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Navigate\"</span>, () =>\n                    nav.Navigate(Route.Settings)),\n                Button(<span class=\"hljs-string\">\"Save State\"</span>, () =>\n                    setSavedJson(System.Text.Json.JsonSerializer.Serialize(\n                        nav.GetState(), DocsNavJsonContext.Default.NavigationStateRoute))),\n                Button(<span class=\"hljs-string\">\"Restore State\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (savedJson <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                    {\n                        <span class=\"hljs-keyword\">var</span> state = System.Text.Json.JsonSerializer.Deserialize(\n                            savedJson, DocsNavJsonContext.Default.NavigationStateRoute);\n                        <span class=\"hljs-keyword\">if</span> (state <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>) nav.SetState(state);\n                    }\n                }).IsEnabled(savedJson <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"Current: <span class=\"hljs-subst\">{nav.CurrentRoute}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Saved: <span class=\"hljs-subst\">{savedJson?[..Math.Min(<span class=\"hljs-number\">50</span>, savedJson?.Length ?? <span class=\"hljs-number\">0</span>)] ?? <span class=\"hljs-string\">\"(none)\"</span>}</span>\"</span>)\n                .FontSize(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            NavigationHost(nav, route =>\n                TextBlock(<span class=\"hljs-string\">$\"Page: <span class=\"hljs-subst\">{route}</span>\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n\n[<span class=\"hljs-meta\">System.Text.Json.Serialization.JsonSerializable(typeof(NavigationState&#x3C;Route>))</span>]\n<span class=\"hljs-keyword\">partial</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DocsNavJsonContext</span> : <span class=\"hljs-title\">System.Text.Json.Serialization.JsonSerializerContext</span> { }\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/navigation/state-serialization.png\" alt=\"State serialization\"></p>\n<p><code>GetState()</code> returns a <code>NavigationState&#x3C;TRoute></code> record. <code>SetState(state)</code>\nrestores the stacks and fires <code>Navigated</code> with <code>Reset</code> mode. The record\ncarries <code>[JsonPropertyName]</code> metadata so a one-line\n<code>JsonSerializer.Serialize(snapshot, MyJsonContext.Default.NavigationStateRoute)</code>\ngives you camelCase JSON for free — fully AOT-safe when paired with a\n<code>JsonSerializerContext</code>. For polymorphic route hierarchies, mark the base\ntype with <code>[JsonPolymorphic]</code> and <code>[JsonDerivedType]</code> so the round trip\npreserves the discriminator.</p>\n<h2 id=\"frame-events\">Frame Events<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#frame-events\">#</a></h2>\n<p>When you embed a raw WinUI <code>Frame</code> for interop with XAML pages (instead of\nusing <code>NavigationHost</code>), the navigation lifecycle is exposed through fluent\nevent extensions:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FrameEventsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (log, updateLog) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Frame events\"</span>),\n            Frame(sourcePageType: <span class=\"hljs-keyword\">typeof</span>(DocsFrameDemoPage))\n                .Navigating(target =>\n                    updateLog(l => [.. l, <span class=\"hljs-string\">$\"Navigating to <span class=\"hljs-subst\">{target.Name}</span>\"</span>]))\n                .Navigated(target =>\n                    updateLog(l => [.. l, <span class=\"hljs-string\">$\"Arrived at <span class=\"hljs-subst\">{target.Name}</span>\"</span>]))\n                .NavigationFailed((target, ex) =>\n                    updateLog(l => [.. l, <span class=\"hljs-string\">$\"Failed <span class=\"hljs-subst\">{target.Name}</span>: <span class=\"hljs-subst\">{ex.Message}</span>\"</span>]))\n                .Height(<span class=\"hljs-number\">300</span>),\n            VStack(<span class=\"hljs-number\">4</span>, log.TakeLast(<span class=\"hljs-number\">6</span>).Select(\n                entry => TextBlock(entry).FontSize(<span class=\"hljs-number\">11</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n            ).ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fluent</th><th>Fires</th></tr></thead><tbody><tr><td><code>.Navigating(handler)</code></td><td>Before navigating away from the current page</td></tr><tr><td><code>.Navigated(handler)</code></td><td>After the new page has been shown</td></tr><tr><td><code>.NavigationFailed(handler)</code></td><td>If page construction throws — receives the target type and the exception</td></tr></tbody></table>\n<p>The underlying init properties (<code>OnNavigating</code>, <code>OnNavigated</code>,\n<code>OnNavigationFailed</code>) are still available for record-construction syntax.\nThe fluent extensions drop the leading <code>On</code> — see\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-property-and-event-scrub.md\">spec 039</a> §0.1 for the C# naming\nconstraint that drove that convention.</p>\n<h2 id=\"navigationviewselectedtagchanged\">NavigationView.SelectedTagChanged<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigationviewselectedtagchanged\">#</a></h2>\n<p><code>NavigationView</code> has a matching fluent for the tag-changed callback, which\nfires whenever the user picks a different <code>NavItem</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SelectedTagChangedDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n        <span class=\"hljs-keyword\">var</span> (lastTag, setLastTag) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n\n        <span class=\"hljs-keyword\">return</span> NavigationView(\n            [\n                NavItem(<span class=\"hljs-string\">\"Home\"</span>, icon: <span class=\"hljs-string\">\"Home\"</span>, tag: <span class=\"hljs-string\">\"Home\"</span>),\n                NavItem(<span class=\"hljs-string\">\"Settings\"</span>, icon: <span class=\"hljs-string\">\"Setting\"</span>, tag: <span class=\"hljs-string\">\"Settings\"</span>)\n            ],\n            content: VStack(<span class=\"hljs-number\">12</span>,\n                Heading(lastTag ?? <span class=\"hljs-string\">\"Home\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Last selected tag: \"</span> + (lastTag ?? <span class=\"hljs-string\">\"(none)\"</span>))\n                    .Opacity(<span class=\"hljs-number\">0.6</span>)\n            ).Padding(<span class=\"hljs-number\">24</span>)\n        ).SelectedTagChanged(tag =>\n        {\n            setLastTag(tag);\n            <span class=\"hljs-keyword\">if</span> (tag == <span class=\"hljs-string\">\"Settings\"</span>) nav.Navigate(Route.Settings);\n        });\n    }\n}\n</code></pre>\n<p><code>SelectedTagChanged</code> receives the tag string (or <code>null</code> if no item is\nselected). Passing <code>null</code> to the fluent clears any previously-set handler.</p>\n<h2 id=\"navigation-diagnostics\">Navigation Diagnostics<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigation-diagnostics\">#</a></h2>\n<p><code>NavigationDiagnostics</code> exposes static events for debugging and telemetry.\nSubscribe to trace navigation activity without modifying page code:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DiagnosticsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n        <span class=\"hljs-keyword\">var</span> (log, updateLog) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n\n        UseEffect(() =>\n        {\n            EventHandler&#x3C;NavigationDiagnosticEvent> onRequested =\n                (_, e) => updateLog(l => [.. l, <span class=\"hljs-string\">$\"Requested: <span class=\"hljs-subst\">{e.From}</span> → <span class=\"hljs-subst\">{e.To}</span>\"</span>]);\n            EventHandler&#x3C;NavigationDiagnosticEvent> onCompleted =\n                (_, e) => updateLog(l => [.. l, <span class=\"hljs-string\">$\"Completed: <span class=\"hljs-subst\">{e.To}</span>\"</span>]);\n\n            NavigationDiagnostics.NavigationRequested += onRequested;\n            NavigationDiagnostics.NavigationCompleted += onCompleted;\n            <span class=\"hljs-keyword\">return</span> () =>\n            {\n                NavigationDiagnostics.NavigationRequested -= onRequested;\n                NavigationDiagnostics.NavigationCompleted -= onCompleted;\n            };\n        });\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Navigation Diagnostics\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => nav.Navigate(Route.Home)),\n                Button(<span class=\"hljs-string\">\"Settings\"</span>, () => nav.Navigate(Route.Settings))\n            ),\n            VStack(<span class=\"hljs-number\">4</span>, log.TakeLast(<span class=\"hljs-number\">6</span>).Select(\n                entry => TextBlock(entry).FontSize(<span class=\"hljs-number\">11</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n            ).ToArray()),\n            NavigationHost(nav, route =>\n                TextBlock(<span class=\"hljs-string\">$\"Page: <span class=\"hljs-subst\">{route}</span>\"</span>).Padding(<span class=\"hljs-number\">16</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Event</th><th>Fires when</th></tr></thead><tbody><tr><td><code>NavigationRequested</code></td><td>A navigation is about to start</td></tr><tr><td><code>NavigationCompleted</code></td><td>A page has finished loading</td></tr><tr><td><code>NavigationCancelled</code></td><td>A lifecycle guard cancelled the navigation</td></tr><tr><td>Cache events</td><td>Page cached, evicted, or hit</td></tr><tr><td>Transition events</td><td>Transition started or completed</td></tr></tbody></table>\n<p>Events fire synchronously on the UI thread. Use them for development\nlogging, analytics, or custom progress indicators.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"guarded-leave-on-a-dirty-form\">Guarded leave on a dirty form<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#guarded-leave-on-a-dirty-form\">#</a></h3>\n<p>The canonical use of <code>onNavigatingFrom</code> is the \"unsaved changes\" guard.\nTrack a <code>dirty</code> boolean alongside form state with <a href=\"/hooks/\"><code>UseState</code></a>,\nand reject the navigation when the user tries to leave with pending\nedits. Prompt the user with a <a href=\"/dialogs-and-flyouts/\"><code>ContentDialog</code></a>;\nif they choose Discard, set <code>dirty = false</code> and call <code>nav.GoBack()</code>\nagain — the second call sees a clean form and succeeds. The recipe at\n<a href=\"/recipes/multi-step-form/\"><code>recipes/multi-step-form</code></a> wires the same\nshape across a wizard.</p>\n<h3 id=\"restoring-scroll-position-per-route\">Restoring scroll position per route<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#restoring-scroll-position-per-route\">#</a></h3>\n<p>Page caching preserves the element tree, but <code>Disabled</code> mode does not —\nremount means a fresh <code>ScrollView</code>. When <code>CacheMode = Disabled</code> is the\nright shape (memory-bounded apps, dynamic routes), keep a\n<code>Dictionary&#x3C;TRoute, double></code> of scroll offsets in a\n<a href=\"/hooks/\"><code>UseRef</code></a> at the navigation host's parent, save the\noffset in <code>onNavigatingFrom</code>, and restore it in a\n<a href=\"/effects/\"><code>UseEffect</code></a> on the page that runs after the\n<code>ScrollView</code> mounts. Pair with <code>VirtualListRef.RestoreScrollOffset</code> for\nvirtualized lists.</p>\n<h3 id=\"deep-link--typed-route-with-back-stack\">Deep link → typed route, with back stack<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#deep-link--typed-route-with-back-stack\">#</a></h3>\n<p>Deep linking by default lands the user on the matched route with an\nempty back stack — Back goes nowhere. Pass <code>backStackFactory</code> to\n<code>DeepLinkMap.Map(...)</code> to synthesize the stack the user would have\nbuilt up if they'd navigated naturally. For <code>/users/42/posts/7</code>, the\nfactory should return <code>[Route.Home, Route.UserList, Route.UserDetail(42)]</code> so the user can drill back up the hierarchy.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"reading-routes-from-a-string-typed-prop\">Reading routes from a string-typed prop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-routes-from-a-string-typed-prop\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Shell</span> : <span class=\"hljs-title\">Component</span>&#x3C;<span class=\"hljs-title\">ShellProps</span>>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> Props.CurrentRoute <span class=\"hljs-keyword\">switch</span>\n        {\n            <span class=\"hljs-string\">\"home\"</span> => Home(),\n            <span class=\"hljs-string\">\"settings\"</span> => Settings(),\n            ...\n        };\n    }\n}\n</code></pre>\n<p>A string-routed shell loses every type-safety win the navigation system\nprovides. The compiler can't catch a typo; the analyzer can't warn\nabout an unreachable route; refactoring rename is a grep. Use the\nenum (or record-union) form the <a href=\"#defining-routes\">Defining Routes</a>\nsection shows — the route map's exhaustiveness check is in the C#\nswitch expression.</p>\n<h3 id=\"treating-usenavigation-like-a-singleton\">Treating <code>UseNavigation</code> like a singleton<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#treating-usenavigation-like-a-singleton\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> NavigationHandle&#x3C;Route>? Nav;\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Shell</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> nav = UseNavigation(Route.Home);\n        Nav = nav;  <span class=\"hljs-comment\">// capture for later use from anywhere</span>\n        <span class=\"hljs-keyword\">return</span> ...;\n    }\n}\n</code></pre>\n<p>The handle is bound to the dispatcher of the component that created\nit. Calling <code>Nav.Navigate(...)</code> from a background timer or an event\nhandler captured in a closure that outlives the page is a silent\nno-op when the bound dispatcher has shut down. Use\n<code>UseNavigation&#x3C;TRoute>()</code> (no initial value) in a descendant component\nto access the same handle via context, or pass the handle through\n<a href=\"/context/\">Context</a> explicitly.</p>\n<h3 id=\"forgetting-usesystembackbutton\">Forgetting <code>UseSystemBackButton</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forgetting-usesystembackbutton\">#</a></h3>\n<p>Without <code>UseSystemBackButton(nav, window)</code>, the title-bar Back arrow\nand hardware Back key on touch devices do not trigger <code>nav.GoBack</code> —\nthey fall through to the OS default, which closes the window on the\nroot page. Wire <code>UseSystemBackButton</code> once at the same scope where\nyou call <code>UseNavigation(initial)</code>; the back arrow becomes\n<code>nav.GoBack</code> and the visibility tracks <code>nav.CanGoBack</code>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use enums for routes.</strong> Enums give you compile-time safety — you cannot\nnavigate to a route that does not exist. For routes that carry data (like a\ndetail page ID), use a discriminated union pattern with records.</p>\n<p><strong>Call <code>UseNavigation(initial)</code> once at the root.</strong> Child components access\nthe same handle with <code>UseNavigation&#x3C;Route>()</code> (no initial value). This\nretrieves the nearest ancestor's handle via <a href=\"/context/\">context</a>.</p>\n<p><strong>Use <code>NavigationTransition.DrillIn()</code> for list-to-detail flows.</strong> It\nsignals hierarchy and pairs naturally with Connected Animation keys.</p>\n<p><strong>Use <code>Reset</code> for sign-out flows.</strong> It clears the entire stack and starts\nfresh, preventing the user from navigating back to authenticated pages.</p>\n<p><strong>Pair <code>UseSystemBackButton</code> with your nav handle.</strong> Call\n<code>UseSystemBackButton(nav, window)</code> to wire the system back button (title bar\nor hardware) to your navigation stack automatically.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Next: render data-driven lists and grids within pages</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — apply visual styles and themes across pages</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — share navigation handles and other state across the component tree</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — run side effects when pages appear or disappear</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — combine page transitions with enter/exit animations</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — data grids with sort, filter, and inline editing</li>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a></strong> — prompt the user from inside a navigation guard</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Navigation Basics . Microsoft.UI.Reactor (Reactor)'s navigation is a stack of typed routes that lives in a hook. You call UseNavigation(initialRoute) once, in a root component, and you get back a NavigationHandle&#x3C;TRoute> whose Navigate , GoBack , Replace , and Reset methods mutate the stack. A NavigationHost reads the current route on each render and projects it through a route => Element function, so the page tree is a pure function of the stack — exactly the same shape as the rest of the framework. There is no Frame.Navigate(typeof(Page)) , no INotifyPropertyChanged on a CurrentPage property, no NavigationService registered in DI. The stack is state; the page is render output. That makes lifecycle hooks ( UseNavigationLifecycle ) testable, deep links ( DeepLinkMap&#x3C;TRoute> ) trivially round-trippable through GetState / SetState , and the back stack visible to your component code as a plain IReadOnlyList&#x3C;TRoute> . Read Defining Routes first if you're new; the rest of the page composes against that one shape. Navigation # Reactor uses a stack-based navigation model with type-safe routes. You define your routes as an enum, create a navigation handle with UseNavigation , and render the current page with NavigationHost . Defining Routes # Start by defining an enum for your pages: enum Route { Home, Settings, Profile, Details } Each enum value represents a distinct page in your app. The navigation system uses this type to ensure you can only navigate to valid routes. For pages that carry data — a detail page bound to a row ID, a wizard step with captured form state — use a discriminated-union pattern with C# records implementing a sealed interface instead of a flat enum. Basic Navigation # Call UseNavigation(Route.Home) to create a navigation handle rooted at your initial route. Use NavigationHost to render the current page: class BasicNavDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); return VStack( 12 , SubHeading( $\"Current: {nav.CurrentRoute} \" ), TextBlock( $\"Stack depth: {nav.Depth} \" ), HStack( 8 , Button( \"Home\" , () => nav.Navigate(Route.Home)), Button( \"Settings\" , () => nav.Navigate(Route.Settings)), Button( \"Profile\" , () => nav.Navigate(Route.Profile)), Button( \"Back\" , () => nav.GoBack()) .IsEnabled(nav.CanGoBack) ), NavigationHost(nav, route => route switch { Route.Home => TextBlock( \"Welcome home!\" ).FontSize( 18 ).Padding( 16 ), Route.Settings => TextBlock( \"Settings page\" ).FontSize( 18 ).Padding( 16 ), Route.Profile => TextBlock( \"Your profile\" ).FontSize( 18 ).Padding( 16 ), _ => TextBlock( \"Not found\" ).Padding( 16 ) }) ).Padding( 24 ); } } Here is what each piece does: UseNavigation(Route.Home) creates a NavigationHandle&#x3C;Route> with Home as the initial route. Call this once in your root component. nav.Navigate(route) pushes a new route onto the stack. nav.GoBack() pops the current route and returns to the previous one. NavigationHost(nav, route => ...) renders whichever element the route map returns for the current route. Reference # API Shape Purpose UseNavigation&#x3C;TRoute>(initial) hook (root) Create the navigation handle for this subtree. UseNavigation&#x3C;TRoute>() hook (descendant) Read the ancestor handle via context. UseNavigationLifecycle(...) hook Page-side onNavigatedTo / onNavigatingFrom / onNavigatedFrom . The from callback receives a context with .Cancel() . UseSystemBackButton(nav, window) hook Wire the title-bar / hardware Back button to nav.GoBack . NavigationHost(nav, routeMap) element Renders the current route. Set Transition , CacheMode , CacheSize . NavigationView(items, content) element Sidebar shell. Use .SelectedTagChanged(handler) for selection events. TabView(tabs) element Parallel workspaces; tabs keep their own state. BreadcrumbBar(items) element Trail of ancestor routes for drill-down nav. Frame(...) element Raw WinUI Frame for XAML-page interop; .Navigating , .Navigated , .NavigationFailed . DeepLinkMap&#x3C;TRoute> type URI pattern → route factory. .Resolve(uri) returns the matched route and back-stack. NavigationDiagnostics static Static events for tracing: NavigationRequested , NavigationCompleted , NavigationCancelled , cache and transition events. NavigationView # NavigationView creates a sidebar menu with icons. Pair it with NavigationHost for a standard app layout: class NavViewDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); return NavigationView( [ NavItem( \"Home\" , icon: \"Home\" , tag: \"Home\" ), NavItem( \"Settings\" , icon: \"Setting\" , tag: \"Settings\" ), NavItem( \"Profile\" , icon: \"Contact\" , tag: \"Profile\" ) ], content: NavigationHost(nav, route => route switch { Route.Home => VStack( 12 , Heading( \"Home\" ), TextBlock( \"Welcome to the app.\" ), Button( \"Go to Settings\" , () => nav.Navigate(Route.Settings))).Padding( 24 ), Route.Settings => VStack( 12 , Heading( \"Settings\" ), TextBlock( \"Configure your preferences.\" ), Button( \"Back\" , () => nav.GoBack())).Padding( 24 ), Route.Profile => VStack( 12 , Heading( \"Profile\" ), TextBlock( \"View your profile info.\" )).Padding( 24 ), _ => TextBlock( \"Not found\" ).Padding( 24 ) }) ); } } NavItem takes a label, an optional icon name (from the Segoe Fluent Icons font), and an optional tag string. The NavigationView handles selection state and displays the content you pass. Stack Operations # The NavigationHandle supports several stack manipulation methods beyond simple push and pop: class StackOperationsDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); return VStack( 12 , SubHeading( $\"Current: {nav.CurrentRoute} \" ), TextBlock( $\"Back stack: {nav.BackStack.Count} \" ), TextBlock( $\"Forward stack: {nav.ForwardStack.Count} \" ), HStack( 8 , Button( \"Navigate\" , () => nav.Navigate(Route.Settings)), Button( \"Replace\" , () => nav.Replace(Route.Profile)), Button( \"Reset\" , () => nav.Reset(Route.Home)), Button( \"Back\" , () => nav.GoBack()) .IsEnabled(nav.CanGoBack), Button( \"Forward\" , () => nav.GoForward()) .IsEnabled(nav.CanGoForward) ), NavigationHost(nav, route => TextBlock( $\"Page: {route} \" ) .FontSize( 18 ).Padding( 16 )) ).Padding( 24 ); } } Method Effect Navigate(route) Push route onto back stack, navigate to it GoBack() Pop current, return to previous GoForward() Move forward (after going back) Replace(route) Swap current route without touching the stack Reset(route) Clear all stacks, start fresh at route PopTo(predicate) Pop until a matching route is found Use CanGoBack and CanGoForward to enable/disable navigation buttons. Page Lifecycle # UseNavigationLifecycle lets a component react to navigation events. Use it to load data when a page appears or save state when it disappears: class LifecyclePage : Component { public override Element Render () { var (log, updateLog) = UseReducer( new List&#x3C; string >()); UseNavigationLifecycle( onNavigatedTo: ctx => updateLog(l => [.. l, $\"Arrived from {ctx.PreviousRoute} \" ]), onNavigatingFrom: ctx => updateLog(l => [.. l, $\"Leaving to {ctx.TargetRoute} \" ]), onNavigatedFrom: ctx => updateLog(l => [.. l, $\"Left for {ctx.TargetRoute} \" ]) ); return VStack( 8 , SubHeading( \"Lifecycle Events\" ), VStack( 4 , log.TakeLast( 5 ).Select(entry => TextBlock(entry).FontSize( 12 ).Opacity( 0.7 ) ).ToArray() ) ).Padding( 16 ); } } The four callbacks fire at different points: Callback When it fires onNavigatingTo Before this page becomes active. Call ctx.Cancel() to reject from the destination side. onNavigatedTo After this page becomes active onNavigatingFrom Before leaving this page. Call ctx.Cancel() to block — the classic \"unsaved changes\" guard. onNavigatedFrom After this page is no longer active Use onNavigatedTo to fetch data or start timers. Use onNavigatingFrom to save drafts or confirm unsaved changes — call ctx.Cancel() on the NavigatingFromContext to prevent navigation entirely. See Effects and Lifecycle for more on lifecycle patterns and async cancellation. Caveat: Reset(route) , Replace(route) , and PopTo(predicate) all run the onNavigatingFrom guard — ctx.Cancel() will block them. But SetState(state) does not : it routes through NavigationStack.RestoreState which bypasses InvokeGuard entirely and fires Navigated with NavigationMode.Reset after the stacks are already rewritten. If your unsaved-changes guard lives only in onNavigatingFrom , an app that restores a saved navigation state from disk on launch will silently overwrite the active page without ever asking the user. Either gate state restoration explicitly (check the in-memory dirty flag before calling SetState ), or surface the same dirty check from a higher level — e.g. an app-shutdown handler in Window.Closed . Page Transitions # NavigationHost supports animated page transitions. Set the Transition property to control how pages animate in and out: class PageTransitionsDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); return VStack( 12 , SubHeading( \"Page Transitions\" ), HStack( 8 , Button( \"Home\" , () => nav.Navigate(Route.Home)), Button( \"Settings\" , () => nav.Navigate(Route.Settings)), Button( \"Profile\" , () => nav.Navigate(Route.Profile)), Button( \"Back\" , () => nav.GoBack()) .IsEnabled(nav.CanGoBack) ), NavigationHost(nav, route => route switch { Route.Home => VStack( 8 , TextBlock( \"Home\" ).FontSize( 24 ).Bold(), TextBlock( \"Slide transition on navigate\" )).Padding( 16 ), Route.Settings => VStack( 8 , TextBlock( \"Settings\" ).FontSize( 24 ).Bold(), TextBlock( \"DrillIn transition to detail\" )).Padding( 16 ), _ => TextBlock( $\" {route} \" ).FontSize( 18 ).Padding( 16 ) }) with { Transition = NavigationTransition.DrillIn() } ).Padding( 24 ); } } Transition Effect NavigationTransition.Slide() Slide + fade (default) NavigationTransition.Fade() Crossfade NavigationTransition.DrillIn() Scale + fade from center NavigationTransition.Spring() Spring-physics slide NavigationTransition.None Instant swap Transitions run on the compositor thread — no managed-code involvement during playback. GoBack automatically reverses the direction. See Animation for more on compositor transitions. Deep Linking # DeepLinkMap maps URI patterns to route constructors. Use it to restore navigation state from activation URIs or protocol handlers: class DeepLinkingDemo : Component { public override Element Render () { var map = UseMemo(() => new DeepLinkMap&#x3C;Route>() .Map( \"/\" , _ => Route.Home) .Map( \"/settings\" , _ => Route.Settings) .Map( \"/profile\" , _ => Route.Profile)); var (result, setResult) = UseState( \"(none)\" ); return VStack( 12 , SubHeading( \"Deep Linking\" ), HStack( 8 , Button( \"Resolve /\" , () => setResult( $\"/ -> {map.Resolve( \"/\" ).Matched} \" )), Button( \"Resolve /settings\" , () => setResult( $\"/settings -> {map.Resolve( \"/settings\" ).Matched} \" )), Button( \"Resolve /unknown\" , () => setResult( $\"/unknown -> {map.Resolve( \"/unknown\" ).Matched} \" )) ), TextBlock( $\"Result: {result} \" ).FontSize( 14 ).Opacity( 0.7 ) ).Padding( 24 ); } } Pattern segments support rich matching: Segment Matches /literal Exact match /{param} Captures a string /{param:int} Typed capture (int, long, bool, Guid) /{param?} Optional parameter /{**} Wildcard — matches remaining path ?key=value Query string parameters Use RouteArgs.Get&#x3C;T>(name) for required params, RouteArgs.GetOrDefault&#x3C;T>(name) for optional ones, and RouteArgs.Query&#x3C;T>(name) for query string values. The backStackFactory parameter builds a synthetic back stack so GoBack works even after deep-linked entry. class DeepLinkQueryDemo : Component { public override Element Render () { var (info, setInfo) = UseState( \"(none)\" ); // RouteArgs are available inside the factory lambda — // capture typed params and query values there var map = UseMemo(() => new DeepLinkMap&#x3C;Route>() .Map( \"/\" , _ => Route.Home) .Map( \"/settings\" , _ => Route.Settings) .Map( \"/users/{id:int}/posts/{postId:int}\" , args => { var userId = args .Get&#x3C; int >( \"id\" ); var postId = args .Get&#x3C; int >( \"postId\" ); var sort = args .Query&#x3C; string >( \"sort\" ); setInfo( $\"userId= {userId} , postId= {postId} , sort= {sort} \" ); return Route.Details; }, () => new [] { Route.Home }) ); return VStack( 12 , SubHeading( \"Deep Link Query Params\" ), Button( \"Resolve /users/42/posts/7?sort=date\" , () => map.Resolve( \"/users/42/posts/7?sort=date\" )), TextBlock( $\"Captured: {info} \" ).FontSize( 14 ).Opacity( 0.7 ) ).Padding( 24 ); } } This example shows query parameters and optional path segments. The resolver parses /users/42/posts?sort=date&#x26;page=2 into typed values you can use directly in route constructors. Page Caching # Set CacheMode on NavigationHost to preserve page state across navigations. Cached pages keep their visual tree alive instead of remounting: class PageCachingDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); return VStack( 12 , SubHeading( \"Page Caching\" ), TextBlock( \"Text input is preserved across navigations.\" ), HStack( 8 , Button( \"Home\" , () => nav.Navigate(Route.Home)), Button( \"Settings\" , () => nav.Navigate(Route.Settings)), Button( \"Back\" , () => nav.GoBack()) .IsEnabled(nav.CanGoBack) ), NavigationHost(nav, route => route switch { Route.Home => CachedPage( \"Home\" ), Route.Settings => CachedPage( \"Settings\" ), _ => TextBlock( $\" {route} \" ).Padding( 16 ) }) with { CacheMode = NavigationCacheMode.Enabled, CacheSize = 5 } ).Padding( 24 ); } static Element CachedPage ( string name ) => VStack( 8 , TextBlock(name).FontSize( 20 ).Bold(), TextBox( \"\" , _ => { }, placeholderText: \"Type here — state persists\" ) ).Padding( 16 ); } Cache mode Behavior Disabled Always unmount/remount (default) Enabled LRU cache up to CacheSize entries Required Always cached, never evicted Caching preserves scroll position, text input, and component state. Use it for pages that are expensive to render or where losing state would frustrate the user. Required is the right choice for a small fixed set of always-hot pages (an app's three tab panes); for an unbounded route space (a detail page parameterized by row ID), stick with Enabled and tune CacheSize — Required never evicts and will retain every visited route's element tree for the process lifetime. TabView # For tab-based navigation, use TabView with Tab items. Each tab holds its own content independently: class TabNavDemo : Component { public override Element Render () { return TabView( Tab( \"Documents\" , VStack( 12 , TextBlock( \"Your documents appear here.\" ), Button( \"New Document\" , () => { }) ).Padding( 24 ) ), Tab( \"Recent\" , VStack( 12 , TextBlock( \"Recently opened files.\" ), TextBlock( \"No recent files.\" ).Opacity( 0.5 ) ).Padding( 24 ) ), Tab( \"Shared\" , VStack( 12 , TextBlock( \"Files shared with you.\" ), TextBlock( \"Nothing shared yet.\" ).Opacity( 0.5 ) ).Padding( 24 ) ) ); } } Unlike stack-based navigation, tabs keep all their content alive simultaneously. Use tabs when users need to switch freely between parallel workspaces. State Serialization # NavigationHandle can capture and restore the full navigation state — back stack, current route, and forward stack — as a plain NavigationState&#x3C;TRoute> snapshot. Reactor intentionally does not pick a serialization format: persist the snapshot however you like (JSON via your own source-gen context, MessagePack, hand-rolled binary). class StateSerializationDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); var (savedJson, setSavedJson) = UseState&#x3C; string ?>( null ); return VStack( 12 , SubHeading( \"State Serialization\" ), HStack( 8 , Button( \"Navigate\" , () => nav.Navigate(Route.Settings)), Button( \"Save State\" , () => setSavedJson(System.Text.Json.JsonSerializer.Serialize( nav.GetState(), DocsNavJsonContext.Default.NavigationStateRoute))), Button( \"Restore State\" , () => { if (savedJson is not null ) { var state = System.Text.Json.JsonSerializer.Deserialize( savedJson, DocsNavJsonContext.Default.NavigationStateRoute); if (state is not null ) nav.SetState(state); } }).IsEnabled(savedJson is not null ) ), TextBlock( $\"Current: {nav.CurrentRoute} \" ), TextBlock( $\"Saved: {savedJson?[..Math.Min( 50 , savedJson?.Length ?? 0 )] ?? \"(none)\" } \" ) .FontSize( 12 ).Opacity( 0.6 ), NavigationHost(nav, route => TextBlock( $\"Page: {route} \" ).Padding( 16 )) ).Padding( 24 ); } } [ System.Text.Json.Serialization.JsonSerializable(typeof(NavigationState&#x3C;Route>)) ] partial class DocsNavJsonContext : System.Text.Json.Serialization.JsonSerializerContext { } GetState() returns a NavigationState&#x3C;TRoute> record. SetState(state) restores the stacks and fires Navigated with Reset mode. The record carries [JsonPropertyName] metadata so a one-line JsonSerializer.Serialize(snapshot, MyJsonContext.Default.NavigationStateRoute) gives you camelCase JSON for free — fully AOT-safe when paired with a JsonSerializerContext . For polymorphic route hierarchies, mark the base type with [JsonPolymorphic] and [JsonDerivedType] so the round trip preserves the discriminator. Frame Events # When you embed a raw WinUI Frame for interop with XAML pages (instead of using NavigationHost ), the navigation lifecycle is exposed through fluent event extensions: class FrameEventsDemo : Component { public override Element Render () { var (log, updateLog) = UseReducer( new List&#x3C; string >()); return VStack( 8 , SubHeading( \"Frame events\" ), Frame(sourcePageType: typeof (DocsFrameDemoPage)) .Navigating(target => updateLog(l => [.. l, $\"Navigating to {target.Name} \" ])) .Navigated(target => updateLog(l => [.. l, $\"Arrived at {target.Name} \" ])) .NavigationFailed((target, ex) => updateLog(l => [.. l, $\"Failed {target.Name} : {ex.Message} \" ])) .Height( 300 ), VStack( 4 , log.TakeLast( 6 ).Select( entry => TextBlock(entry).FontSize( 11 ).Opacity( 0.6 ) ).ToArray()) ).Padding( 24 ); } } Fluent Fires .Navigating(handler) Before navigating away from the current page .Navigated(handler) After the new page has been shown .NavigationFailed(handler) If page construction throws — receives the target type and the exception The underlying init properties ( OnNavigating , OnNavigated , OnNavigationFailed ) are still available for record-construction syntax. The fluent extensions drop the leading On — see spec 039 §0.1 for the C# naming constraint that drove that convention. NavigationView.SelectedTagChanged # NavigationView has a matching fluent for the tag-changed callback, which fires whenever the user picks a different NavItem : class SelectedTagChangedDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); var (lastTag, setLastTag) = UseState&#x3C; string ?>( null ); return NavigationView( [ NavItem( \"Home\" , icon: \"Home\" , tag: \"Home\" ), NavItem( \"Settings\" , icon: \"Setting\" , tag: \"Settings\" ) ], content: VStack( 12 , Heading(lastTag ?? \"Home\" ), TextBlock( \"Last selected tag: \" + (lastTag ?? \"(none)\" )) .Opacity( 0.6 ) ).Padding( 24 ) ).SelectedTagChanged(tag => { setLastTag(tag); if (tag == \"Settings\" ) nav.Navigate(Route.Settings); }); } } SelectedTagChanged receives the tag string (or null if no item is selected). Passing null to the fluent clears any previously-set handler. Navigation Diagnostics # NavigationDiagnostics exposes static events for debugging and telemetry. Subscribe to trace navigation activity without modifying page code: class DiagnosticsDemo : Component { public override Element Render () { var nav = UseNavigation(Route.Home); var (log, updateLog) = UseReducer( new List&#x3C; string >()); UseEffect(() => { EventHandler&#x3C;NavigationDiagnosticEvent> onRequested = (_, e) => updateLog(l => [.. l, $\"Requested: {e.From} → {e.To} \" ]); EventHandler&#x3C;NavigationDiagnosticEvent> onCompleted = (_, e) => updateLog(l => [.. l, $\"Completed: {e.To} \" ]); NavigationDiagnostics.NavigationRequested += onRequested; NavigationDiagnostics.NavigationCompleted += onCompleted; return () => { NavigationDiagnostics.NavigationRequested -= onRequested; NavigationDiagnostics.NavigationCompleted -= onCompleted; }; }); return VStack( 12 , SubHeading( \"Navigation Diagnostics\" ), HStack( 8 , Button( \"Home\" , () => nav.Navigate(Route.Home)), Button( \"Settings\" , () => nav.Navigate(Route.Settings)) ), VStack( 4 , log.TakeLast( 6 ).Select( entry => TextBlock(entry).FontSize( 11 ).Opacity( 0.6 ) ).ToArray()), NavigationHost(nav, route => TextBlock( $\"Page: {route} \" ).Padding( 16 )) ).Padding( 24 ); } } Event Fires when NavigationRequested A navigation is about to start NavigationCompleted A page has finished loading NavigationCancelled A lifecycle guard cancelled the navigation Cache events Page cached, evicted, or hit Transition events Transition started or completed Events fire synchronously on the UI thread. Use them for development logging, analytics, or custom progress indicators. Patterns # Guarded leave on a dirty form # The canonical use of onNavigatingFrom is the \"unsaved changes\" guard. Track a dirty boolean alongside form state with UseState , and reject the navigation when the user tries to leave with pending edits. Prompt the user with a ContentDialog ; if they choose Discard, set dirty = false and call nav.GoBack() again — the second call sees a clean form and succeeds. The recipe at recipes/multi-step-form wires the same shape across a wizard. Restoring scroll position per route # Page caching preserves the element tree, but Disabled mode does not — remount means a fresh ScrollView . When CacheMode = Disabled is the right shape (memory-bounded apps, dynamic routes), keep a Dictionary&#x3C;TRoute, double> of scroll offsets in a UseRef at the navigation host's parent, save the offset in onNavigatingFrom , and restore it in a UseEffect on the page that runs after the ScrollView mounts. Pair with VirtualListRef.RestoreScrollOffset for virtualized lists. Deep link → typed route, with back stack # Deep linking by default lands the user on the matched route with an empty back stack — Back goes nowhere. Pass backStackFactory to DeepLinkMap.Map(...) to synthesize the stack the user would have built up if they'd navigated naturally. For /users/42/posts/7 , the factory should return [Route.Home, Route.UserList, Route.UserDetail(42)] so the user can drill back up the hierarchy. Common Mistakes # Reading routes from a string-typed prop # // Don't: class Shell : Component &#x3C; ShellProps > { public override Element Render () { return Props.CurrentRoute switch { \"home\" => Home(), \"settings\" => Settings(), ... }; } } A string-routed shell loses every type-safety win the navigation system provides. The compiler can't catch a typo; the analyzer can't warn about an unreachable route; refactoring rename is a grep. Use the enum (or record-union) form the Defining Routes section shows — the route map's exhaustiveness check is in the C# switch expression. Treating UseNavigation like a singleton # // Don't: public static NavigationHandle&#x3C;Route>? Nav; class Shell : Component { public override Element Render () { var nav = UseNavigation(Route.Home); Nav = nav; // capture for later use from anywhere return ...; } } The handle is bound to the dispatcher of the component that created it. Calling Nav.Navigate(...) from a background timer or an event handler captured in a closure that outlives the page is a silent no-op when the bound dispatcher has shut down. Use UseNavigation&#x3C;TRoute>() (no initial value) in a descendant component to access the same handle via context, or pass the handle through Context explicitly. Forgetting UseSystemBackButton # Without UseSystemBackButton(nav, window) , the title-bar Back arrow and hardware Back key on touch devices do not trigger nav.GoBack — they fall through to the OS default, which closes the window on the root page. Wire UseSystemBackButton once at the same scope where you call UseNavigation(initial) ; the back arrow becomes nav.GoBack and the visibility tracks nav.CanGoBack . Tips # Use enums for routes. Enums give you compile-time safety — you cannot navigate to a route that does not exist. For routes that carry data (like a detail page ID), use a discriminated union pattern with records. Call UseNavigation(initial) once at the root. Child components access the same handle with UseNavigation&#x3C;Route>() (no initial value). This retrieves the nearest ancestor's handle via context . Use NavigationTransition.DrillIn() for list-to-detail flows. It signals hierarchy and pairs naturally with Connected Animation keys. Use Reset for sign-out flows. It clears the entire stack and starts fresh, preventing the user from navigating back to authenticated pages. Pair UseSystemBackButton with your nav handle. Call UseSystemBackButton(nav, window) to wire the system back button (title bar or hardware) to your navigation stack automatically. Next Steps # Collections — Next: render data-driven lists and grids within pages Styling and Theming — apply visual styles and themes across pages Context — share navigation handles and other state across the component tree Effects and Lifecycle — run side effects when pages appear or disappear Animation — combine page transitions with enter/exit animations Data System — data grids with sort, filter, and inline editing Dialogs and Flyouts — prompt the user from inside a navigation guard",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/navigation.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/navigation.md",
      "headings": [
        {
          "depth": 2,
          "id": "defining-routes",
          "text": "Defining Routes"
        },
        {
          "depth": 2,
          "id": "basic-navigation",
          "text": "Basic Navigation"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "navigationview",
          "text": "NavigationView"
        },
        {
          "depth": 2,
          "id": "stack-operations",
          "text": "Stack Operations"
        },
        {
          "depth": 2,
          "id": "page-lifecycle",
          "text": "Page Lifecycle"
        },
        {
          "depth": 2,
          "id": "page-transitions",
          "text": "Page Transitions"
        },
        {
          "depth": 2,
          "id": "deep-linking",
          "text": "Deep Linking"
        },
        {
          "depth": 2,
          "id": "page-caching",
          "text": "Page Caching"
        },
        {
          "depth": 2,
          "id": "tabview",
          "text": "TabView"
        },
        {
          "depth": 2,
          "id": "state-serialization",
          "text": "State Serialization"
        },
        {
          "depth": 2,
          "id": "frame-events",
          "text": "Frame Events"
        },
        {
          "depth": 2,
          "id": "navigationviewselectedtagchanged",
          "text": "NavigationView.SelectedTagChanged"
        },
        {
          "depth": 2,
          "id": "navigation-diagnostics",
          "text": "Navigation Diagnostics"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "guarded-leave-on-a-dirty-form",
          "text": "Guarded leave on a dirty form"
        },
        {
          "depth": 3,
          "id": "restoring-scroll-position-per-route",
          "text": "Restoring scroll position per route"
        },
        {
          "depth": 3,
          "id": "deep-link--typed-route-with-back-stack",
          "text": "Deep link → typed route, with back stack"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "reading-routes-from-a-string-typed-prop",
          "text": "Reading routes from a string-typed prop"
        },
        {
          "depth": 3,
          "id": "treating-usenavigation-like-a-singleton",
          "text": "Treating UseNavigation like a singleton"
        },
        {
          "depth": 3,
          "id": "forgetting-usesystembackbutton",
          "text": "Forgetting UseSystemBackButton"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Windows",
      "path": "windows.md",
      "slug": "/windows/",
      "html": "<h1 id=\"windows\">Windows<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#windows\">#</a></h1>\n<p>Most Microsoft.UI.Reactor (Reactor) apps start with a single window — the one <code>ReactorApp.Run</code> opens\nfor you. When you need more (a settings dialog, a multi-document app, a tray\nflyout), the <code>ReactorApp</code> static surface and a handful of hooks give you a\ndeclarative way to manage every top-level window in the process.</p>\n<h2 id=\"the-primary-window\">The Primary Window<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-primary-window\">#</a></h2>\n<p><code>ReactorApp.Run&#x3C;TRoot>(...)</code> opens one window with <code>TRoot</code> mounted as its\ncontent:</p>\n<pre><code class=\"hljs language-csharp\">ReactorApp.Run&#x3C;WindowsApp>(<span class=\"hljs-string\">\"Windows Demo\"</span>, width: <span class=\"hljs-number\">640</span>, height: <span class=\"hljs-number\">520</span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">if</span> DEBUG</span>\n    , preview: <span class=\"hljs-literal\">true</span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">endif</span></span>\n);\n</code></pre>\n<p>That window becomes <code>ReactorApp.PrimaryWindow</code>. By default, when the primary\nwindow closes the process exits — see <a href=\"#shutdown-policy\">Shutdown Policy</a>\nbelow if you need different behavior.</p>\n<h2 id=\"opening-a-secondary-window\">Opening a Secondary Window<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#opening-a-secondary-window\">#</a></h2>\n<p>Call <code>ReactorApp.OpenWindow(spec, factory)</code> from anywhere on the UI thread.\nThe <code>WindowSpec</code> describes the window's chrome (title, size, icon,\npresenter); the factory builds the root component:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WindowsApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Heading(<span class=\"hljs-string\">\"Top-level Windows\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"New Notepad window\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">var</span> n = count + <span class=\"hljs-number\">1</span>;\n                    ReactorApp.OpenWindow(\n                        <span class=\"hljs-keyword\">new</span> WindowSpec\n                        {\n                            Title = <span class=\"hljs-string\">$\"Notepad #<span class=\"hljs-subst\">{n}</span>\"</span>,\n                            Width = <span class=\"hljs-number\">420</span>,\n                            Height = <span class=\"hljs-number\">300</span>,\n                        },\n                        () => <span class=\"hljs-keyword\">new</span> NotePadWindow(<span class=\"hljs-string\">$\"Document #<span class=\"hljs-subst\">{n}</span>\"</span>));\n                    setCount(n);\n                }),\n                Button(<span class=\"hljs-string\">\"Open settings\"</span>, () =>\n                {\n                    <span class=\"hljs-comment\">// Reuse the same window if it's already open: FindWindow</span>\n                    <span class=\"hljs-comment\">// looks the surface up by its WindowKey, so a second</span>\n                    <span class=\"hljs-comment\">// click brings the existing window forward instead of</span>\n                    <span class=\"hljs-comment\">// opening a duplicate.</span>\n                    <span class=\"hljs-keyword\">var</span> key = WindowKey.Of(<span class=\"hljs-string\">\"settings\"</span>);\n                    <span class=\"hljs-keyword\">var</span> existing = ReactorApp.FindWindow(key);\n                    <span class=\"hljs-keyword\">if</span> (existing <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                    {\n                        existing.Activate();\n                        <span class=\"hljs-keyword\">return</span>;\n                    }\n\n                    ReactorApp.OpenWindow(\n                        <span class=\"hljs-keyword\">new</span> WindowSpec\n                        {\n                            Title = <span class=\"hljs-string\">\"Settings\"</span>,\n                            Width = <span class=\"hljs-number\">480</span>,\n                            Height = <span class=\"hljs-number\">360</span>,\n                            Key = key,\n                        },\n                        () => <span class=\"hljs-keyword\">new</span> SettingsWindow());\n                })\n            ),\n            TextBlock(<span class=\"hljs-string\">$\"Open windows: <span class=\"hljs-subst\">{ReactorApp.Windows.Count}</span>\"</span>)\n        ).Padding(<span class=\"hljs-number\">20</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/windows/shell.png\" alt=\"Top-level windows demo\"></p>\n<p>Each click of \"New Notepad window\" spawns an independent window. They have\ntheir own state, their own DPI, their own lifecycle — closing one doesn't\naffect the others.</p>\n<p><code>WindowSpec</code> is an immutable record. The fields you'll reach for most often:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Field</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Title</code></td><td><code>\"Reactor App\"</code></td><td>Caption text</td></tr><tr><td><code>Width</code> / <code>Height</code></td><td><code>1024</code> / <code>768</code></td><td>Initial size in DIPs</td></tr><tr><td><code>MinWidth</code> / <code>MinHeight</code></td><td><code>null</code></td><td>Optional resize floor</td></tr><tr><td><code>Icon</code></td><td><code>null</code></td><td><code>WindowIcon.FromPath(...)</code> or <code>WindowIcon.FromResource(...)</code></td></tr><tr><td><code>StartPosition</code></td><td><code>Default</code></td><td><code>CenterOnPrimary</code>, <code>CenterOnOwner</code>, <code>Manual</code>, <code>RestoreFromPersistence</code></td></tr><tr><td><code>PersistenceId</code></td><td><code>null</code></td><td>Save and restore placement across sessions</td></tr><tr><td><code>Key</code></td><td><code>null</code></td><td>Stable identity for <code>FindWindow</code> and <code>UseOpenWindow</code></td></tr><tr><td><code>Owner</code></td><td><code>null</code></td><td>Owner window for owned-window semantics</td></tr></tbody></table>\n<p>All sizes are DIPs (device-independent pixels). Reactor handles the\nDIP→physical conversion at the destination monitor's DPI.</p>\n<h2 id=\"reading-the-current-window\">Reading the Current Window<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-the-current-window\">#</a></h2>\n<p>Inside any component rendered as a window's content, hooks reach the owning\n<code>ReactorWindow</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NotePadWindow</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span> _label;\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">NotePadWindow</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> label</span>)</span> { _label = label; }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> window = UseWindow();\n        <span class=\"hljs-keyword\">var</span> state = UseWindowState();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(_label),\n            TextBlock(window <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n                ? <span class=\"hljs-string\">\"(no owning window)\"</span>\n                : <span class=\"hljs-string\">$\"id=<span class=\"hljs-subst\">{window.Id}</span>  state=<span class=\"hljs-subst\">{state}</span>  dpi=<span class=\"hljs-subst\">{window.Dpi}</span>\"</span>),\n            TextBox(text, setText, placeholderText: <span class=\"hljs-string\">\"Type something...\"</span>)\n                .Width(<span class=\"hljs-number\">360</span>),\n            Button(<span class=\"hljs-string\">\"Close\"</span>, () => window?.Close())\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Purpose</th></tr></thead><tbody><tr><td><code>UseWindow()</code></td><td>The owning <code>ReactorWindow</code> — <code>null</code> outside a window (e.g. tray flyouts)</td></tr><tr><td><code>UseWindowState()</code></td><td><code>Normal</code> / <code>Minimized</code> / <code>Maximized</code> / <code>FullScreen</code> / <code>CompactOverlay</code>; re-renders on change</td></tr><tr><td><code>UseIsActive()</code></td><td><code>true</code> while the window has foreground; re-renders on activation change</td></tr><tr><td><code>UseDpi()</code></td><td>Current DPI; re-renders on monitor change</td></tr><tr><td><code>UseClosingGuard(canClose)</code></td><td>Register a synchronous predicate that can veto a close</td></tr></tbody></table>\n<p><code>ReactorWindow</code> itself exposes <code>Close()</code>, <code>Activate()</code>, <code>SetSize(...)</code>,\n<code>SetPosition(...)</code>, and lifecycle events (<code>Closing</code>, <code>Closed</code>,\n<code>SizeChanged</code>, <code>StateChanged</code>).</p>\n<h2 id=\"finding-and-enumerating-windows\">Finding and Enumerating Windows<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#finding-and-enumerating-windows\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">ReactorApp.Windows          <span class=\"hljs-comment\">// IReadOnlyList&#x3C;ReactorWindow> snapshot</span>\nReactorApp.PrimaryWindow    <span class=\"hljs-comment\">// First window opened, or null after it closes</span>\nReactorApp.FindWindow(key)  <span class=\"hljs-comment\">// Look up by WindowKey</span>\n</code></pre>\n<p><code>WindowKey</code> is a string-typed identity stamped onto a <code>WindowSpec.Key</code>. Use\nit to dedupe \"open the settings window\" against an existing one, as the\nbutton handler in the shell snippet above does.</p>\n<h2 id=\"reusing-a-window-with-useopenwindow\">Reusing a Window with <code>UseOpenWindow</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reusing-a-window-with-useopenwindow\">#</a></h2>\n<p><code>UseOpenWindow</code> lets a component declaratively own a window's lifetime:\nwhile the component is mounted, the window stays open; renders that pass\nthe same <code>WindowKey</code> reuse the same handle.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SettingsHost</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// While this component is mounted, ensure a settings window keyed</span>\n        <span class=\"hljs-comment\">// to \"settings\" is open. Re-renders that pass the same WindowKey</span>\n        <span class=\"hljs-comment\">// reuse the same handle; the hook dedupes against the live window</span>\n        <span class=\"hljs-comment\">// registry via FindWindow.</span>\n        <span class=\"hljs-keyword\">var</span> settings = UseOpenWindow(\n            key: <span class=\"hljs-string\">\"settings\"</span>,\n            spec: <span class=\"hljs-keyword\">new</span> WindowSpec { Title = <span class=\"hljs-string\">\"Settings\"</span>, Width = <span class=\"hljs-number\">480</span>, Height = <span class=\"hljs-number\">360</span> },\n            factory: () => <span class=\"hljs-keyword\">new</span> SettingsWindow());\n\n        <span class=\"hljs-keyword\">return</span> TextBlock(settings <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n            ? <span class=\"hljs-string\">\"(no UI dispatcher)\"</span>\n            : <span class=\"hljs-string\">$\"Settings open — id=<span class=\"hljs-subst\">{settings.Id}</span>\"</span>);\n    }\n}\n</code></pre>\n<p><code>UseOpenWindow</code> doesn't unmount-close the window — that's deliberate, since\nwindows usually outlive the menu item that opened them. To close on\nunmount, return a <code>UseEffect</code> cleanup that calls <code>Close()</code> on the handle.</p>\n<p>(Tray icons go the other way — <code>UseTrayIcon</code> <em>does</em> close on unmount, since\na tray icon belongs to the component that declared it.)</p>\n<h2 id=\"shutdown-policy\">Shutdown Policy<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#shutdown-policy\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Call once at startup, before ReactorApp.Run. With OnLastSurfaceClosed the</span>\n<span class=\"hljs-comment\">// process keeps running while a tray icon or any window is alive; with</span>\n<span class=\"hljs-comment\">// Explicit you must call ReactorApp.Exit() yourself.</span>\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Startup</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ConfigureShutdown</span>()</span>\n    {\n        ReactorApp.ShutdownPolicy = ShutdownPolicy.OnLastSurfaceClosed;\n    }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Policy</th><th>Process exits when...</th></tr></thead><tbody><tr><td><code>OnPrimaryWindowClosed</code> <em>(default)</em></td><td>The primary window closes</td></tr><tr><td><code>OnLastSurfaceClosed</code></td><td>The last window AND the last tray icon both close</td></tr><tr><td><code>Explicit</code></td><td>Never automatically — you must call <code>ReactorApp.Exit()</code></td></tr></tbody></table>\n<p>Pick <code>OnLastSurfaceClosed</code> for tray-resident apps that should keep running\nwhile only an icon is visible. Pick <code>Explicit</code> when you need a custom\n\"quit\" gesture (a tray menu item, a <code>Cmd-Q</code> accelerator, etc.) and call\n<code>ReactorApp.Exit(exitCode)</code> from the handler.</p>\n<h2 id=\"tray-icons-in-brief\">Tray Icons (in Brief)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tray-icons-in-brief\">#</a></h2>\n<p>A tray icon is a non-window surface registered with the shell's notification\narea. Use <code>UseTrayIcon</code> from a component to register one whose lifetime is\ntied to that component:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TrayHost</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> icon = UseMemo(() => WindowIcon.FromPath(<span class=\"hljs-string\">\"Assets/TrayIcon.ico\"</span>));\n        <span class=\"hljs-keyword\">var</span> tray = UseTrayIcon(<span class=\"hljs-keyword\">new</span> TrayIconSpec(\n            Icon: icon,\n            Tooltip: <span class=\"hljs-string\">\"My App\"</span>,\n            Key: WindowKey.Of(<span class=\"hljs-string\">\"main-tray\"</span>)));\n\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">if</span> (tray <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span> () => { };\n            <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">onClick</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span>? s, EventArgs e</span>)</span>\n                => ReactorApp.PrimaryWindow?.Activate();\n            tray.Click += onClick;\n            <span class=\"hljs-keyword\">return</span> () => tray.Click -= onClick;\n        }, tray ?? (<span class=\"hljs-built_in\">object</span>)<span class=\"hljs-string\">\"no-tray\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"Tray icon registered while this component is mounted.\"</span>);\n    }\n}\n</code></pre>\n<p>The hook returns a <code>ReactorTrayIcon?</code> exposing <code>Click</code>, <code>DoubleClick</code>, and\n<code>RightClick</code> events plus <code>ShowFlyout(content)</code> for in-place flyout UI.\nFor app-scoped tray icons that survive component unmount, call\n<code>ReactorApp.OpenTrayIcon(spec)</code> directly.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>One spec record per window — don't rebuild it in <code>Render()</code> if you can\navoid it.</strong> Wrap the <code>WindowSpec</code> (and especially the <code>WindowIcon</code>) in a\n<code>UseMemo</code> so re-renders don't reallocate them. Identity-stable specs let\n<code>UseOpenWindow</code>'s value-equality compare see no change and skip the\nchrome-update pass.</p>\n<p><strong><code>Width</code>/<code>Height</code> are doubles, not ints.</strong> All sizes and positions are\nDIPs. Literal call sites bind cleanly (<code>Width = 480</code>), but a variable\ntyped <code>int</code> needs a cast.</p>\n<p><strong>Use <code>WindowKey</code> for any window you might want to find again.</strong> The cost\nis one string allocation; the payoff is <code>FindWindow</code> lookups,\n<code>UseOpenWindow</code> reuse, and shell tools that can address the surface by\nname.</p>\n<p><strong>Closing guards are synchronous.</strong> <code>UseClosingGuard(() => …)</code> runs on the\nUI thread and must return immediately. For an async confirmation dialog,\nreturn <code>false</code> from the guard and re-trigger <code>Close()</code> from the dialog\ncallback.</p>\n<p><strong>The primary window is special only by default.</strong> Once you change\n<code>ShutdownPolicy</code>, \"primary\" is just a name for the first window you opened\n— there's nothing the framework treats differently about it.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — previous topic: hosting Reactor inside WinForms via XAML Islands</li>\n<li><strong><a href=\"/\">Reactor</a></strong> — back to the index: overview of the framework and full topic list</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — in-window routing with <code>UseNavigation</code>, <code>NavigationView</code>, deep links</li>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — <code>UseEffect</code> patterns for the imperative work that pairs with windows</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — wire keyboard accelerators and tray-menu items to commands</li>\n</ul>",
      "text": "Windows # Most Microsoft.UI.Reactor (Reactor) apps start with a single window — the one ReactorApp.Run opens for you. When you need more (a settings dialog, a multi-document app, a tray flyout), the ReactorApp static surface and a handful of hooks give you a declarative way to manage every top-level window in the process. The Primary Window # ReactorApp.Run&#x3C;TRoot>(...) opens one window with TRoot mounted as its content: ReactorApp.Run&#x3C;WindowsApp>( \"Windows Demo\" , width: 640 , height: 520 # if DEBUG , preview: true # endif ); That window becomes ReactorApp.PrimaryWindow . By default, when the primary window closes the process exits — see Shutdown Policy below if you need different behavior. Opening a Secondary Window # Call ReactorApp.OpenWindow(spec, factory) from anywhere on the UI thread. The WindowSpec describes the window's chrome (title, size, icon, presenter); the factory builds the root component: class WindowsApp : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return VStack( 12 , Heading( \"Top-level Windows\" ), HStack( 8 , Button( \"New Notepad window\" , () => { var n = count + 1 ; ReactorApp.OpenWindow( new WindowSpec { Title = $\"Notepad # {n} \" , Width = 420 , Height = 300 , }, () => new NotePadWindow( $\"Document # {n} \" )); setCount(n); }), Button( \"Open settings\" , () => { // Reuse the same window if it's already open: FindWindow // looks the surface up by its WindowKey, so a second // click brings the existing window forward instead of // opening a duplicate. var key = WindowKey.Of( \"settings\" ); var existing = ReactorApp.FindWindow(key); if (existing is not null ) { existing.Activate(); return ; } ReactorApp.OpenWindow( new WindowSpec { Title = \"Settings\" , Width = 480 , Height = 360 , Key = key, }, () => new SettingsWindow()); }) ), TextBlock( $\"Open windows: {ReactorApp.Windows.Count} \" ) ).Padding( 20 ); } } Each click of \"New Notepad window\" spawns an independent window. They have their own state, their own DPI, their own lifecycle — closing one doesn't affect the others. WindowSpec is an immutable record. The fields you'll reach for most often: Field Default Purpose Title \"Reactor App\" Caption text Width / Height 1024 / 768 Initial size in DIPs MinWidth / MinHeight null Optional resize floor Icon null WindowIcon.FromPath(...) or WindowIcon.FromResource(...) StartPosition Default CenterOnPrimary , CenterOnOwner , Manual , RestoreFromPersistence PersistenceId null Save and restore placement across sessions Key null Stable identity for FindWindow and UseOpenWindow Owner null Owner window for owned-window semantics All sizes are DIPs (device-independent pixels). Reactor handles the DIP→physical conversion at the destination monitor's DPI. Reading the Current Window # Inside any component rendered as a window's content, hooks reach the owning ReactorWindow : class NotePadWindow : Component { private readonly string _label; public NotePadWindow ( string label ) { _label = label; } public override Element Render () { var (text, setText) = UseState( \"\" ); var window = UseWindow(); var state = UseWindowState(); return VStack( 12 , SubHeading(_label), TextBlock(window is null ? \"(no owning window)\" : $\"id= {window.Id} state= {state} dpi= {window.Dpi} \" ), TextBox(text, setText, placeholderText: \"Type something...\" ) .Width( 360 ), Button( \"Close\" , () => window?.Close()) ).Padding( 16 ); } } Hook Purpose UseWindow() The owning ReactorWindow — null outside a window (e.g. tray flyouts) UseWindowState() Normal / Minimized / Maximized / FullScreen / CompactOverlay ; re-renders on change UseIsActive() true while the window has foreground; re-renders on activation change UseDpi() Current DPI; re-renders on monitor change UseClosingGuard(canClose) Register a synchronous predicate that can veto a close ReactorWindow itself exposes Close() , Activate() , SetSize(...) , SetPosition(...) , and lifecycle events ( Closing , Closed , SizeChanged , StateChanged ). Finding and Enumerating Windows # ReactorApp.Windows // IReadOnlyList&#x3C;ReactorWindow> snapshot ReactorApp.PrimaryWindow // First window opened, or null after it closes ReactorApp.FindWindow(key) // Look up by WindowKey WindowKey is a string-typed identity stamped onto a WindowSpec.Key . Use it to dedupe \"open the settings window\" against an existing one, as the button handler in the shell snippet above does. Reusing a Window with UseOpenWindow # UseOpenWindow lets a component declaratively own a window's lifetime: while the component is mounted, the window stays open; renders that pass the same WindowKey reuse the same handle. class SettingsHost : Component { public override Element Render () { // While this component is mounted, ensure a settings window keyed // to \"settings\" is open. Re-renders that pass the same WindowKey // reuse the same handle; the hook dedupes against the live window // registry via FindWindow. var settings = UseOpenWindow( key: \"settings\" , spec: new WindowSpec { Title = \"Settings\" , Width = 480 , Height = 360 }, factory: () => new SettingsWindow()); return TextBlock(settings is null ? \"(no UI dispatcher)\" : $\"Settings open — id= {settings.Id} \" ); } } UseOpenWindow doesn't unmount-close the window — that's deliberate, since windows usually outlive the menu item that opened them. To close on unmount, return a UseEffect cleanup that calls Close() on the handle. (Tray icons go the other way — UseTrayIcon does close on unmount, since a tray icon belongs to the component that declared it.) Shutdown Policy # // Call once at startup, before ReactorApp.Run. With OnLastSurfaceClosed the // process keeps running while a tray icon or any window is alive; with // Explicit you must call ReactorApp.Exit() yourself. static class Startup { public static void ConfigureShutdown () { ReactorApp.ShutdownPolicy = ShutdownPolicy.OnLastSurfaceClosed; } } Policy Process exits when... OnPrimaryWindowClosed (default) The primary window closes OnLastSurfaceClosed The last window AND the last tray icon both close Explicit Never automatically — you must call ReactorApp.Exit() Pick OnLastSurfaceClosed for tray-resident apps that should keep running while only an icon is visible. Pick Explicit when you need a custom \"quit\" gesture (a tray menu item, a Cmd-Q accelerator, etc.) and call ReactorApp.Exit(exitCode) from the handler. Tray Icons (in Brief) # A tray icon is a non-window surface registered with the shell's notification area. Use UseTrayIcon from a component to register one whose lifetime is tied to that component: class TrayHost : Component { public override Element Render () { var icon = UseMemo(() => WindowIcon.FromPath( \"Assets/TrayIcon.ico\" )); var tray = UseTrayIcon( new TrayIconSpec( Icon: icon, Tooltip: \"My App\" , Key: WindowKey.Of( \"main-tray\" ))); UseEffect(() => { if (tray is null ) return () => { }; void onClick ( object ? s, EventArgs e ) => ReactorApp.PrimaryWindow?.Activate(); tray.Click += onClick; return () => tray.Click -= onClick; }, tray ?? ( object ) \"no-tray\" ); return TextBlock( \"Tray icon registered while this component is mounted.\" ); } } The hook returns a ReactorTrayIcon? exposing Click , DoubleClick , and RightClick events plus ShowFlyout(content) for in-place flyout UI. For app-scoped tray icons that survive component unmount, call ReactorApp.OpenTrayIcon(spec) directly. Tips # One spec record per window — don't rebuild it in Render() if you can avoid it. Wrap the WindowSpec (and especially the WindowIcon ) in a UseMemo so re-renders don't reallocate them. Identity-stable specs let UseOpenWindow 's value-equality compare see no change and skip the chrome-update pass. Width / Height are doubles, not ints. All sizes and positions are DIPs. Literal call sites bind cleanly ( Width = 480 ), but a variable typed int needs a cast. Use WindowKey for any window you might want to find again. The cost is one string allocation; the payoff is FindWindow lookups, UseOpenWindow reuse, and shell tools that can address the surface by name. Closing guards are synchronous. UseClosingGuard(() => …) runs on the UI thread and must return immediately. For an async confirmation dialog, return false from the guard and re-trigger Close() from the dialog callback. The primary window is special only by default. Once you change ShutdownPolicy , \"primary\" is just a name for the first window you opened — there's nothing the framework treats differently about it. Next Steps # WinForms Interop — previous topic: hosting Reactor inside WinForms via XAML Islands Reactor — back to the index: overview of the framework and full topic list Navigation — in-window routing with UseNavigation , NavigationView , deep links Effects and Lifecycle — UseEffect patterns for the imperative work that pairs with windows Commanding — wire keyboard accelerators and tray-menu items to commands",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/windows.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/windows.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-primary-window",
          "text": "The Primary Window"
        },
        {
          "depth": 2,
          "id": "opening-a-secondary-window",
          "text": "Opening a Secondary Window"
        },
        {
          "depth": 2,
          "id": "reading-the-current-window",
          "text": "Reading the Current Window"
        },
        {
          "depth": 2,
          "id": "finding-and-enumerating-windows",
          "text": "Finding and Enumerating Windows"
        },
        {
          "depth": 2,
          "id": "reusing-a-window-with-useopenwindow",
          "text": "Reusing a Window with UseOpenWindow"
        },
        {
          "depth": 2,
          "id": "shutdown-policy",
          "text": "Shutdown Policy"
        },
        {
          "depth": 2,
          "id": "tray-icons-in-brief",
          "text": "Tray Icons (in Brief)"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Docking Windows",
      "path": "docking.md",
      "slug": "/docking/",
      "html": "<h1 id=\"docking-windows\">Docking Windows<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#docking-windows\">#</a></h1>\n<p>Microsoft.UI.Reactor's docking system lets a single shell host multiple\nuser-rearrangeable surfaces — the Visual Studio / VS Code / Photoshop /\nFigma layout idiom. Users drag tabs between groups, split panes, pin\ntool windows to a side, and tear panes out into floating sub-windows;\nlayouts persist across sessions.</p>\n<p>The element is <code>DockManager</code>. Its <code>Layout</code> is an immutable <code>DockNode</code>\ntree describing the desired arrangement; the reconciler turns that\ntree into native WinUI controls and applies the minimum mutations on\nevery re-render.</p>\n<h2 id=\"minimal-setup\">Minimal Setup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#minimal-setup\">#</a></h2>\n<p>Docking is an opt-in element type — register it at host construction\ntime, then use <code>DockManager</code> like any other Reactor element:</p>\n<pre><code class=\"hljs language-csharp\">ReactorApp.Run&#x3C;DockingApp>(\n    title: <span class=\"hljs-string\">\"Docking\"</span>,\n    width: <span class=\"hljs-number\">900</span>,\n    height: <span class=\"hljs-number\">600</span>,\n    devtools: <span class=\"hljs-literal\">true</span>,\n    configure: host => DockingNativeInterop.Register(host.Reconciler));\n</code></pre>\n<p><code>DockingNativeInterop.Register</code> wires the <code>DockManager</code>, splitter, and\ndrop-target elements into the reconciler. Without it, a <code>DockManager</code>\nin your tree will not be recognized.</p>\n<p>A two-pane horizontal split:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TwoPaneDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => <span class=\"hljs-keyword\">new</span> DockManager\n    {\n        Layout = <span class=\"hljs-keyword\">new</span> DockSplit(\n            Orientation.Horizontal,\n            <span class=\"hljs-keyword\">new</span> DockNode[]\n            {\n                <span class=\"hljs-keyword\">new</span> ToolWindow\n                {\n                    Title = <span class=\"hljs-string\">\"Solution Explorer\"</span>,\n                    Key = <span class=\"hljs-string\">\"tool:solution\"</span>,\n                    Width = <span class=\"hljs-number\">260</span>,\n                    Content = VStack(<span class=\"hljs-number\">6</span>,\n                        TextBlock(<span class=\"hljs-string\">\"MyApp.sln\"</span>).SemiBold(),\n                        TextBlock(<span class=\"hljs-string\">\"  src\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"    App.cs\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"    MainView.cs\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"  tests\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"    MainViewTests.cs\"</span>)\n                    ).Padding(<span class=\"hljs-number\">12</span>)\n                },\n\n                <span class=\"hljs-keyword\">new</span> DockTabGroup(\n                    Documents: <span class=\"hljs-keyword\">new</span> DockableContent[]\n                    {\n                        <span class=\"hljs-keyword\">new</span> Document\n                        {\n                            Title = <span class=\"hljs-string\">\"App.cs\"</span>,\n                            Key = <span class=\"hljs-string\">\"doc:app-cs\"</span>,\n                            Content = VStack(<span class=\"hljs-number\">4</span>,\n                                TextBlock(<span class=\"hljs-string\">\"// App.cs\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"ReactorApp.Run&#x3C;MainView>(title: \\\"MyApp\\\");\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"class MainView : Component\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"{\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"    public override Element Render() => Text(\\\"Hello\\\");\"</span>),\n                                TextBlock(<span class=\"hljs-string\">\"}\"</span>)\n                            ).Padding(<span class=\"hljs-number\">16</span>)\n                        }\n                    },\n                    SelectedIndex: <span class=\"hljs-number\">0</span>),\n            }),\n    };\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/docking/two-pane.png\" alt=\"Two-pane docking layout with a Solution tool on the left and an App.cs editor on the right\"></p>\n<p>The leaves of the tree are pane records. Prefer <code>Document</code> for closable\neditor-like panes and <code>ToolWindow</code> for hideable, side-pinnable tools; both\nderive from the source-compatible <code>DockableContent</code> base. Each pane carries\na <code>Title</code> (shown on the tab / floating window), an optional <code>Content</code>\nelement subtree, and — importantly — a stable <code>Key</code>.</p>\n<p><strong><code>Key</code> is required for any pane whose state should survive\nreorderings, tab moves, and tear-outs.</strong> Reactor's keyed reconciler\nmatches panes by <code>Key</code> and preserves the element subtree (and its\n<code>UseState</code> slots) across tree rebuilds. There is no implicit\n<code>Title</code>-as-key fallback; always supply one.</p>\n<p>The <code>DockNode</code> algebra has three node kinds (all immutable records):</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Type</th><th>Purpose</th></tr></thead><tbody><tr><td><code>DockSplit(Orientation, Children, …)</code></td><td>Splits children along one axis, with drag-resize splitters between them</td></tr><tr><td><code>DockTabGroup(Documents, TabPosition, CompactTabs, …)</code></td><td>Presents children as tabs</td></tr><tr><td><code>Document</code></td><td>Closable leaf pane for editor/document surfaces; cannot pin by default</td></tr><tr><td><code>ToolWindow</code></td><td>Hideable leaf pane for tool surfaces; can auto-hide/pin by default</td></tr><tr><td><code>DockableContent</code></td><td>Base leaf pane kept for older positional-constructor code</td></tr></tbody></table>\n<p><code>DockManager</code> itself accepts these props:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Prop</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Layout</code></td><td>Root of the <code>DockNode</code> tree</td></tr><tr><td><code>LeftSide</code> / <code>TopSide</code> / <code>RightSide</code> / <code>BottomSide</code></td><td>Pinned tool windows along an edge</td></tr><tr><td><code>ActiveDocument</code></td><td>Resolves by <code>Key</code> against <code>Layout</code>; mismatched keys leave activation alone</td></tr><tr><td><code>Adapter</code></td><td><code>IDockAdapter</code> for rehydration and floating chrome</td></tr><tr><td><code>PersistenceId</code></td><td>Routes layout JSON through <code>WindowPersistedScope</code></td></tr><tr><td><code>LayoutStrategy</code></td><td>Routes programmatic document/tool inserts before default placement</td></tr><tr><td><code>OnLayoutChanging</code> / <code>OnLayoutChanged</code> and pane lifecycle events</td><td>Observe or cancel layout, close, hide, float, and dock transitions</td></tr><tr><td><code>ShowDropTargets</code></td><td>Shows the drop-target overlay for drag, keyboard, or test-driven moves</td></tr><tr><td><code>SplitRatios</code> / <code>OperationLog</code></td><td>Optional diagnostics and externally-owned split sizing</td></tr></tbody></table>\n<h2 id=\"tab-groups\">Tab Groups<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tab-groups\">#</a></h2>\n<p><code>DockTabGroup</code> holds N <code>DockableContent</code> leaves and presents them as\ntabs. Users reorder by drag; <code>SelectedIndex</code> reports the active tab:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TabGroupDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => <span class=\"hljs-keyword\">new</span> DockManager\n    {\n        Layout = <span class=\"hljs-keyword\">new</span> DockTabGroup(\n            Documents: <span class=\"hljs-keyword\">new</span>[]\n            {\n                <span class=\"hljs-keyword\">new</span> Document\n                {\n                    Title = <span class=\"hljs-string\">\"App.cs\"</span>,\n                    Key = <span class=\"hljs-string\">\"doc:app\"</span>,\n                    Content = VStack(<span class=\"hljs-number\">4</span>,\n                        TextBlock(<span class=\"hljs-string\">\"// App.cs\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"public sealed class App : Component\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"{\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"    public override Element Render() =>\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"        Text(\\\"hello, world\\\");\"</span>),\n                        TextBlock(<span class=\"hljs-string\">\"}\"</span>)\n                    ).Padding(<span class=\"hljs-number\">16</span>)\n                },\n                <span class=\"hljs-keyword\">new</span> Document\n                {\n                    Title = <span class=\"hljs-string\">\"MainView.cs\"</span>,\n                    Key = <span class=\"hljs-string\">\"doc:main\"</span>,\n                    Content = TextBlock(<span class=\"hljs-string\">\"// MainView.cs body\"</span>).Padding(<span class=\"hljs-number\">16</span>)\n                },\n                <span class=\"hljs-keyword\">new</span> Document\n                {\n                    Title = <span class=\"hljs-string\">\"Readme.md\"</span>,\n                    Key = <span class=\"hljs-string\">\"doc:readme\"</span>,\n                    Content = TextBlock(<span class=\"hljs-string\">\"# Readme\"</span>).Padding(<span class=\"hljs-number\">16</span>)\n                },\n            },\n            SelectedIndex: <span class=\"hljs-number\">0</span>),\n    };\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/docking/tab-group.png\" alt=\"Three editor tabs in a single dock tab group\"></p>\n<p><code>TabPosition.Bottom</code> combined with <code>CompactTabs: true</code> produces\nOffice's tool-pane shape. <code>Document</code> panes are closeable by default; a\nclosed document raises the document lifecycle callbacks and is removed\nfrom the native layout.</p>\n<h2 id=\"shaping-a-document-well\">Shaping a Document Well<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#shaping-a-document-well\">#</a></h2>\n<p>Set <code>Role</code> on a <code>DockTabGroup</code> to declare the IDE-class document-vs-tool\ndistinction. Three values:</p>\n<ul>\n<li><code>DockGroupRole.General</code> — the default; accepts every category and\nculls when emptied. Pre-spec-046 behavior.</li>\n<li><code>DockGroupRole.DocumentArea</code> — the document well. Preferred target\nfor <code>Dock(Center)</code> on <code>Document</code> panes; rejects <code>ToolWindow</code> drops by\ndefault. Survives empty (no per-group <code>ShowWhenEmpty</code> needed) so the\nwell stays a visible drop target after the last document closes.</li>\n<li><code>DockGroupRole.ToolWindowStrip</code> — an edge strip of tool windows.\nPreferred target for <code>ToolWindow</code> panes; rejects <code>Document</code> drops.</li>\n</ul>\n<p>A Visual-Studio-shaped layout: tool strip on the left, document well in\nthe middle, tool strip on the right. <code>model.Dock(doc, DockTarget.Center)</code>\nlands in the middle group regardless of where it sits in tree order:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">new</span> DockSplit(Orientation.Horizontal, <span class=\"hljs-keyword\">new</span> DockNode[]\n{\n    <span class=\"hljs-keyword\">new</span> DockTabGroup(\n        <span class=\"hljs-keyword\">new</span>[] { galleryItemsToolWindow },\n        Width: <span class=\"hljs-number\">260</span>,\n        Role: DockGroupRole.ToolWindowStrip),\n    <span class=\"hljs-keyword\">new</span> DockTabGroup(\n        Array.Empty&#x3C;DockableContent>(),\n        Role: DockGroupRole.DocumentArea), <span class=\"hljs-comment\">// implies ShowWhenEmpty</span>\n    <span class=\"hljs-keyword\">new</span> DockTabGroup(\n        <span class=\"hljs-keyword\">new</span>[] { configurationToolWindow },\n        Width: <span class=\"hljs-number\">320</span>,\n        Role: DockGroupRole.ToolWindowStrip),\n})\n</code></pre>\n<blockquote>\n<p>Programmatic <code>Dock(content, DockTarget.Center)</code> routes to the first\n<code>DockGroupRole.DocumentArea</code> group, falling back to the first compatible\ngroup (and logging a diagnostic if none accepts the payload). To target\na specific group regardless of role, use the\n<code>Dock(content, DockTabGroup, DockTarget)</code> overload — the explicit\nplacement is trusted and skips the role compatibility check.</p>\n</blockquote>\n<p>When the user splits a <code>Document</code> inside a <code>DocumentArea</code> via drag, the\nnew sibling group inherits the role — splitting a doc well produces two\ndoc wells, not one well plus a <code>General</code> group. The same propagation\nfires for <code>ToolWindowStrip</code> when the user drops a tool on the layout's\nedge: <code>DockBottom</code> with a <code>ToolWindow</code> payload creates a new\n<code>ToolWindowStrip</code>-roled group at the bottom, even when no strip existed\nbefore.</p>\n<h2 id=\"constraining-tool-window-placement\">Constraining Tool Window Placement<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#constraining-tool-window-placement\">#</a></h2>\n<p><code>ToolWindow.AllowedSides</code> is a <code>[Flags]</code> mask that limits which edges a\ntool window may dock to (Qt's <code>setAllowedAreas</code> shape). Default\n<code>DockSides.All</code> preserves unconstrained placement. Set it to\n<code>DockSides.Bottom</code> to lock an Errors pane to the bottom strip:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> errors = <span class=\"hljs-keyword\">new</span> ToolWindow {\n    Title = <span class=\"hljs-string\">\"Errors\"</span>,\n    Key = <span class=\"hljs-string\">\"tool:errors\"</span>,\n    AllowedSides = DockSides.Bottom,\n};\n</code></pre>\n<p>Effect:</p>\n<ul>\n<li>The drop-target overlay dims edges the mask excludes during drag, and\nthe hit-test ignores those targets — releasing over a forbidden edge\nsnaps the pane back.</li>\n<li><code>model.PinToSide(tw, DockSide.Left)</code> throws\n<code>InvalidOperationException</code> when <code>Left</code> isn't in the mask. Strategies\nthat need to bypass should clone via <code>tw with { AllowedSides = DockSides.All }</code> before calling.</li>\n<li><code>DockSides.None</code> is allowed and means the tool window is float-only —\nevery <code>PinToSide</code> throws and every dock-edge target dims during drag.</li>\n</ul>\n<h2 id=\"side-pins-auto-hide\">Side Pins (Auto-Hide)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#side-pins-auto-hide\">#</a></h2>\n<p><code>LeftSide</code>, <code>TopSide</code>, <code>RightSide</code>, and <code>BottomSide</code> on <code>DockManager</code>\ncarry pinned tool windows. Each collapses to an edge icon; clicking\nthe icon expands a popup, clicking out collapses it back:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SidePinDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => <span class=\"hljs-keyword\">new</span> DockManager\n    {\n        Layout = <span class=\"hljs-keyword\">new</span> Document\n        {\n            Title = <span class=\"hljs-string\">\"Document\"</span>,\n            Key = <span class=\"hljs-string\">\"doc:main\"</span>,\n            Content = VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Document area\"</span>).SemiBold(),\n                TextBlock(<span class=\"hljs-string\">\"Click the pinned tab on the right to expand it.\"</span>),\n                TextBlock(<span class=\"hljs-string\">\"Pin / unpin from inside the popup to toggle.\"</span>)\n            ).Padding(<span class=\"hljs-number\">16</span>)\n        },\n\n        RightSide = <span class=\"hljs-keyword\">new</span>[]\n        {\n            <span class=\"hljs-keyword\">new</span> ToolWindow\n            {\n                Title = <span class=\"hljs-string\">\"Properties\"</span>,\n                Key = <span class=\"hljs-string\">\"tool:properties\"</span>,\n                Content = VStack(<span class=\"hljs-number\">4</span>,\n                    TextBlock(<span class=\"hljs-string\">\"Name\"</span>).SemiBold(),\n                    TextBlock(<span class=\"hljs-string\">\"Width: 240\"</span>),\n                    TextBlock(<span class=\"hljs-string\">\"Height: 120\"</span>)\n                ).Padding(<span class=\"hljs-number\">12</span>)\n            },\n        },\n    };\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/docking/side-pin.png\" alt=\"Editor on the left with a Properties tool pinned to the right edge\"></p>\n<p>Use <code>ToolWindow</code> for side-pin content. It enables pin and auto-hide\naffordances by default, so users can pin and unpin at runtime, and the\nmoved-to-side state round-trips through persistence.</p>\n<h2 id=\"persistence\">Persistence<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#persistence\">#</a></h2>\n<p>Set <code>PersistenceId</code> to enable automatic save/restore. Reactor routes\nthe layout JSON through <code>WindowPersistedScope[\"docking:&#x3C;id>\"]</code> so the\narrangement survives app restarts:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PersistenceDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => <span class=\"hljs-keyword\">new</span> DockManager\n    {\n        <span class=\"hljs-comment\">// Layout JSON is auto-saved to WindowPersistedScope[\"docking:my-shell\"].</span>\n        <span class=\"hljs-comment\">// It is the restore fallback when a later mount leaves Layout null.</span>\n        PersistenceId = <span class=\"hljs-string\">\"my-shell\"</span>,\n        Layout = <span class=\"hljs-keyword\">new</span> DockSplit(\n            Orientation.Horizontal,\n            <span class=\"hljs-keyword\">new</span> DockNode[]\n            {\n                <span class=\"hljs-keyword\">new</span> ToolWindow\n                {\n                    Title = <span class=\"hljs-string\">\"Outline\"</span>,\n                    Key = <span class=\"hljs-string\">\"tool:outline\"</span>,\n                    Width = <span class=\"hljs-number\">240</span>,\n                    Content = TextBlock(<span class=\"hljs-string\">\"Rearrange me, then relaunch.\"</span>).Padding(<span class=\"hljs-number\">12</span>)\n                },\n                <span class=\"hljs-keyword\">new</span> Document\n                {\n                    Title = <span class=\"hljs-string\">\"Editor\"</span>,\n                    Key = <span class=\"hljs-string\">\"doc:editor\"</span>,\n                    Content = TextBlock(<span class=\"hljs-string\">\"Layout restores from PersistenceId when no declarative Layout is supplied.\"</span>).Padding(<span class=\"hljs-number\">12</span>)\n                },\n            }),\n    };\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/docking/persistence.png\" alt=\"Persisted two-pane layout that restores across launches\"></p>\n<p>The declarative <code>Layout</code> remains the source of truth while you provide\none. Persisted JSON is used as the restore fallback when a later mount\nsets the same <code>PersistenceId</code> and leaves <code>Layout</code> null. Re-render with a\ndifferent <code>PersistenceId</code> to start fresh.</p>\n<h2 id=\"floating-tear-outs\">Floating Tear-Outs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#floating-tear-outs\">#</a></h2>\n<p>When a user drags a tab title into open space, a floating window\nappears at the pointer with a custom title bar supplied by an\n<code>IDockAdapter</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FloatingChromeAdapter</span> : <span class=\"hljs-title\">IDockAdapter</span>\n{\n    <span class=\"hljs-keyword\">public</span> Element? OnContentCreated(DockableContent content) => <span class=\"hljs-literal\">null</span>;\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">OnGroupCreated</span>(<span class=\"hljs-params\">DockTabGroupContext <span class=\"hljs-keyword\">group</span></span>)</span> { }\n\n    <span class=\"hljs-comment\">// Custom title bar painted on torn-out floating windows.</span>\n    <span class=\"hljs-keyword\">public</span> Element? GetFloatingWindowTitleBar(DockableContent? source) =>\n        HStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(<span class=\"hljs-string\">\"📌\"</span>).Opacity(<span class=\"hljs-number\">0.7</span>),\n            TextBlock(source?.Title ?? <span class=\"hljs-string\">\"Floating\"</span>).SemiBold(),\n            TextBlock(<span class=\"hljs-string\">\" — My App\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>)\n        ).Padding(<span class=\"hljs-number\">12</span>, <span class=\"hljs-number\">6</span>, <span class=\"hljs-number\">12</span>, <span class=\"hljs-number\">6</span>);\n}\n</code></pre>\n<p>Pass the adapter on <code>DockManager.Adapter</code>. <code>OnContentCreated</code> is called\nwhen a pane is rehydrated from persisted JSON — return the Reactor\nsubtree to mount inside it, keyed off <code>content.Key</code>. For observation and\ncancellation, prefer the per-event <code>DockManager</code> callbacks such as\n<code>OnContentFloating</code>, <code>OnContentDocking</code>, <code>OnDocumentClosing</code>, and\n<code>OnLayoutChanged</code>; the older behavior interface is obsolete.</p>\n<h2 id=\"pane-hooks\">Pane Hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pane-hooks\">#</a></h2>\n<p>Pane content can read docking context through hooks:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Use</th></tr></thead><tbody><tr><td><code>ctx.UsePane()</code></td><td>Stable pane identity (<code>Key</code>, title, role) for the current subtree</td></tr><tr><td><code>ctx.UseDockState()</code></td><td>Current pane state: docked, floating, auto-hidden, expanded, or hidden</td></tr><tr><td><code>ctx.UseIsActivePane()</code></td><td>True only for the active pane</td></tr><tr><td><code>ctx.UseDockPanePersisted(key, initial)</code></td><td>Window-scoped persisted state automatically prefixed by pane key</td></tr></tbody></table>\n<p>Use <code>ctx.UseDockLayout()</code> sparingly — it re-renders on any structural\nlayout change and is intended for devtools and diagnostics, not ordinary\npane bodies.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Always set <code>Key</code> on panes with stateful content.</strong> A controlled\n<code>TextBox</code> inside a pane without a <code>Key</code> will lose its draft text the\nmoment a user drags the tab — the reconciler can't tell it's the\n\"same\" pane, so it remounts the subtree. Keys can be strings, GUIDs,\nenums, or any equatable domain identifier.</p>\n<p><strong>Build the tree from data, not branches.</strong> Mapping a\n<code>List&#x3C;DocumentVm></code> through <code>.Select(d => new DockableContent(…))</code> is\nthe idiomatic way to drive open documents. There is no\n<code>DocumentsSource</code> binding API; the closure does the work.</p>\n<p><strong>Register once per host.</strong> <code>DockingNativeInterop.Register</code> is\nidempotent, but the natural place to call it is the <code>configure:</code>\ncallback on <code>ReactorApp.Run</code>. Apps that open secondary windows via\n<code>ReactorApp.OpenWindow</code> should register on each new <code>ReactorHost</code>.</p>\n<p><strong>Layouts are immutable records — produce a new tree.</strong> Like any\nReactor element, mutate by re-rendering with a new <code>DockManager</code>.\nKeyed reconciliation handles the diff; you don't manage the underlying\ncontrol yourself.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/windows/\">Windows</a></strong> — top-level window lifecycle, the host\nsurface that docking lives inside.</li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — <code>UsePersisted</code>, scopes, and the\n<code>WindowPersistedScope</code> that docking layouts route through.</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — <code>Key</code> rules and the reconciler\nidentity model that <code>DockableContent.Key</code> plugs into.</li>\n<li><strong><a href=\"/\">Reactor</a></strong> — back to the docset index.</li>\n</ul>",
      "text": "Docking Windows # Microsoft.UI.Reactor's docking system lets a single shell host multiple user-rearrangeable surfaces — the Visual Studio / VS Code / Photoshop / Figma layout idiom. Users drag tabs between groups, split panes, pin tool windows to a side, and tear panes out into floating sub-windows; layouts persist across sessions. The element is DockManager . Its Layout is an immutable DockNode tree describing the desired arrangement; the reconciler turns that tree into native WinUI controls and applies the minimum mutations on every re-render. Minimal Setup # Docking is an opt-in element type — register it at host construction time, then use DockManager like any other Reactor element: ReactorApp.Run&#x3C;DockingApp>( title: \"Docking\" , width: 900 , height: 600 , devtools: true , configure: host => DockingNativeInterop.Register(host.Reconciler)); DockingNativeInterop.Register wires the DockManager , splitter, and drop-target elements into the reconciler. Without it, a DockManager in your tree will not be recognized. A two-pane horizontal split: class TwoPaneDemo : Component { public override Element Render () => new DockManager { Layout = new DockSplit( Orientation.Horizontal, new DockNode[] { new ToolWindow { Title = \"Solution Explorer\" , Key = \"tool:solution\" , Width = 260 , Content = VStack( 6 , TextBlock( \"MyApp.sln\" ).SemiBold(), TextBlock( \" src\" ), TextBlock( \" App.cs\" ), TextBlock( \" MainView.cs\" ), TextBlock( \" tests\" ), TextBlock( \" MainViewTests.cs\" ) ).Padding( 12 ) }, new DockTabGroup( Documents: new DockableContent[] { new Document { Title = \"App.cs\" , Key = \"doc:app-cs\" , Content = VStack( 4 , TextBlock( \"// App.cs\" ), TextBlock( \"ReactorApp.Run&#x3C;MainView>(title: \\\"MyApp\\\");\" ), TextBlock( \"\" ), TextBlock( \"class MainView : Component\" ), TextBlock( \"{\" ), TextBlock( \" public override Element Render() => Text(\\\"Hello\\\");\" ), TextBlock( \"}\" ) ).Padding( 16 ) } }, SelectedIndex: 0 ), }), }; } The leaves of the tree are pane records. Prefer Document for closable editor-like panes and ToolWindow for hideable, side-pinnable tools; both derive from the source-compatible DockableContent base. Each pane carries a Title (shown on the tab / floating window), an optional Content element subtree, and — importantly — a stable Key . Key is required for any pane whose state should survive reorderings, tab moves, and tear-outs. Reactor's keyed reconciler matches panes by Key and preserves the element subtree (and its UseState slots) across tree rebuilds. There is no implicit Title -as-key fallback; always supply one. The DockNode algebra has three node kinds (all immutable records): Type Purpose DockSplit(Orientation, Children, …) Splits children along one axis, with drag-resize splitters between them DockTabGroup(Documents, TabPosition, CompactTabs, …) Presents children as tabs Document Closable leaf pane for editor/document surfaces; cannot pin by default ToolWindow Hideable leaf pane for tool surfaces; can auto-hide/pin by default DockableContent Base leaf pane kept for older positional-constructor code DockManager itself accepts these props: Prop Purpose Layout Root of the DockNode tree LeftSide / TopSide / RightSide / BottomSide Pinned tool windows along an edge ActiveDocument Resolves by Key against Layout ; mismatched keys leave activation alone Adapter IDockAdapter for rehydration and floating chrome PersistenceId Routes layout JSON through WindowPersistedScope LayoutStrategy Routes programmatic document/tool inserts before default placement OnLayoutChanging / OnLayoutChanged and pane lifecycle events Observe or cancel layout, close, hide, float, and dock transitions ShowDropTargets Shows the drop-target overlay for drag, keyboard, or test-driven moves SplitRatios / OperationLog Optional diagnostics and externally-owned split sizing Tab Groups # DockTabGroup holds N DockableContent leaves and presents them as tabs. Users reorder by drag; SelectedIndex reports the active tab: class TabGroupDemo : Component { public override Element Render () => new DockManager { Layout = new DockTabGroup( Documents: new [] { new Document { Title = \"App.cs\" , Key = \"doc:app\" , Content = VStack( 4 , TextBlock( \"// App.cs\" ), TextBlock( \"public sealed class App : Component\" ), TextBlock( \"{\" ), TextBlock( \" public override Element Render() =>\" ), TextBlock( \" Text(\\\"hello, world\\\");\" ), TextBlock( \"}\" ) ).Padding( 16 ) }, new Document { Title = \"MainView.cs\" , Key = \"doc:main\" , Content = TextBlock( \"// MainView.cs body\" ).Padding( 16 ) }, new Document { Title = \"Readme.md\" , Key = \"doc:readme\" , Content = TextBlock( \"# Readme\" ).Padding( 16 ) }, }, SelectedIndex: 0 ), }; } TabPosition.Bottom combined with CompactTabs: true produces Office's tool-pane shape. Document panes are closeable by default; a closed document raises the document lifecycle callbacks and is removed from the native layout. Shaping a Document Well # Set Role on a DockTabGroup to declare the IDE-class document-vs-tool distinction. Three values: DockGroupRole.General — the default; accepts every category and culls when emptied. Pre-spec-046 behavior. DockGroupRole.DocumentArea — the document well. Preferred target for Dock(Center) on Document panes; rejects ToolWindow drops by default. Survives empty (no per-group ShowWhenEmpty needed) so the well stays a visible drop target after the last document closes. DockGroupRole.ToolWindowStrip — an edge strip of tool windows. Preferred target for ToolWindow panes; rejects Document drops. A Visual-Studio-shaped layout: tool strip on the left, document well in the middle, tool strip on the right. model.Dock(doc, DockTarget.Center) lands in the middle group regardless of where it sits in tree order: new DockSplit(Orientation.Horizontal, new DockNode[] { new DockTabGroup( new [] { galleryItemsToolWindow }, Width: 260 , Role: DockGroupRole.ToolWindowStrip), new DockTabGroup( Array.Empty&#x3C;DockableContent>(), Role: DockGroupRole.DocumentArea), // implies ShowWhenEmpty new DockTabGroup( new [] { configurationToolWindow }, Width: 320 , Role: DockGroupRole.ToolWindowStrip), }) Programmatic Dock(content, DockTarget.Center) routes to the first DockGroupRole.DocumentArea group, falling back to the first compatible group (and logging a diagnostic if none accepts the payload). To target a specific group regardless of role, use the Dock(content, DockTabGroup, DockTarget) overload — the explicit placement is trusted and skips the role compatibility check. When the user splits a Document inside a DocumentArea via drag, the new sibling group inherits the role — splitting a doc well produces two doc wells, not one well plus a General group. The same propagation fires for ToolWindowStrip when the user drops a tool on the layout's edge: DockBottom with a ToolWindow payload creates a new ToolWindowStrip -roled group at the bottom, even when no strip existed before. Constraining Tool Window Placement # ToolWindow.AllowedSides is a [Flags] mask that limits which edges a tool window may dock to (Qt's setAllowedAreas shape). Default DockSides.All preserves unconstrained placement. Set it to DockSides.Bottom to lock an Errors pane to the bottom strip: var errors = new ToolWindow { Title = \"Errors\" , Key = \"tool:errors\" , AllowedSides = DockSides.Bottom, }; Effect: The drop-target overlay dims edges the mask excludes during drag, and the hit-test ignores those targets — releasing over a forbidden edge snaps the pane back. model.PinToSide(tw, DockSide.Left) throws InvalidOperationException when Left isn't in the mask. Strategies that need to bypass should clone via tw with { AllowedSides = DockSides.All } before calling. DockSides.None is allowed and means the tool window is float-only — every PinToSide throws and every dock-edge target dims during drag. Side Pins (Auto-Hide) # LeftSide , TopSide , RightSide , and BottomSide on DockManager carry pinned tool windows. Each collapses to an edge icon; clicking the icon expands a popup, clicking out collapses it back: class SidePinDemo : Component { public override Element Render () => new DockManager { Layout = new Document { Title = \"Document\" , Key = \"doc:main\" , Content = VStack( 8 , TextBlock( \"Document area\" ).SemiBold(), TextBlock( \"Click the pinned tab on the right to expand it.\" ), TextBlock( \"Pin / unpin from inside the popup to toggle.\" ) ).Padding( 16 ) }, RightSide = new [] { new ToolWindow { Title = \"Properties\" , Key = \"tool:properties\" , Content = VStack( 4 , TextBlock( \"Name\" ).SemiBold(), TextBlock( \"Width: 240\" ), TextBlock( \"Height: 120\" ) ).Padding( 12 ) }, }, }; } Use ToolWindow for side-pin content. It enables pin and auto-hide affordances by default, so users can pin and unpin at runtime, and the moved-to-side state round-trips through persistence. Persistence # Set PersistenceId to enable automatic save/restore. Reactor routes the layout JSON through WindowPersistedScope[\"docking:&#x3C;id>\"] so the arrangement survives app restarts: class PersistenceDemo : Component { public override Element Render () => new DockManager { // Layout JSON is auto-saved to WindowPersistedScope[\"docking:my-shell\"]. // It is the restore fallback when a later mount leaves Layout null. PersistenceId = \"my-shell\" , Layout = new DockSplit( Orientation.Horizontal, new DockNode[] { new ToolWindow { Title = \"Outline\" , Key = \"tool:outline\" , Width = 240 , Content = TextBlock( \"Rearrange me, then relaunch.\" ).Padding( 12 ) }, new Document { Title = \"Editor\" , Key = \"doc:editor\" , Content = TextBlock( \"Layout restores from PersistenceId when no declarative Layout is supplied.\" ).Padding( 12 ) }, }), }; } The declarative Layout remains the source of truth while you provide one. Persisted JSON is used as the restore fallback when a later mount sets the same PersistenceId and leaves Layout null. Re-render with a different PersistenceId to start fresh. Floating Tear-Outs # When a user drags a tab title into open space, a floating window appears at the pointer with a custom title bar supplied by an IDockAdapter : class FloatingChromeAdapter : IDockAdapter { public Element? OnContentCreated(DockableContent content) => null ; public void OnGroupCreated ( DockTabGroupContext group ) { } // Custom title bar painted on torn-out floating windows. public Element? GetFloatingWindowTitleBar(DockableContent? source) => HStack( 8 , TextBlock( \"📌\" ).Opacity( 0.7 ), TextBlock(source?.Title ?? \"Floating\" ).SemiBold(), TextBlock( \" — My App\" ).Opacity( 0.5 ) ).Padding( 12 , 6 , 12 , 6 ); } Pass the adapter on DockManager.Adapter . OnContentCreated is called when a pane is rehydrated from persisted JSON — return the Reactor subtree to mount inside it, keyed off content.Key . For observation and cancellation, prefer the per-event DockManager callbacks such as OnContentFloating , OnContentDocking , OnDocumentClosing , and OnLayoutChanged ; the older behavior interface is obsolete. Pane Hooks # Pane content can read docking context through hooks: Hook Use ctx.UsePane() Stable pane identity ( Key , title, role) for the current subtree ctx.UseDockState() Current pane state: docked, floating, auto-hidden, expanded, or hidden ctx.UseIsActivePane() True only for the active pane ctx.UseDockPanePersisted(key, initial) Window-scoped persisted state automatically prefixed by pane key Use ctx.UseDockLayout() sparingly — it re-renders on any structural layout change and is intended for devtools and diagnostics, not ordinary pane bodies. Tips # Always set Key on panes with stateful content. A controlled TextBox inside a pane without a Key will lose its draft text the moment a user drags the tab — the reconciler can't tell it's the \"same\" pane, so it remounts the subtree. Keys can be strings, GUIDs, enums, or any equatable domain identifier. Build the tree from data, not branches. Mapping a List&#x3C;DocumentVm> through .Select(d => new DockableContent(…)) is the idiomatic way to drive open documents. There is no DocumentsSource binding API; the closure does the work. Register once per host. DockingNativeInterop.Register is idempotent, but the natural place to call it is the configure: callback on ReactorApp.Run . Apps that open secondary windows via ReactorApp.OpenWindow should register on each new ReactorHost . Layouts are immutable records — produce a new tree. Like any Reactor element, mutate by re-rendering with a new DockManager . Keyed reconciliation handles the diff; you don't manage the underlying control yourself. Next Steps # Windows — top-level window lifecycle, the host surface that docking lives inside. Persistence — UsePersisted , scopes, and the WindowPersistedScope that docking layouts route through. Components — Key rules and the reconciler identity model that DockableContent.Key plugs into. Reactor — back to the docset index.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/docking.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/docking.md",
      "headings": [
        {
          "depth": 2,
          "id": "minimal-setup",
          "text": "Minimal Setup"
        },
        {
          "depth": 2,
          "id": "tab-groups",
          "text": "Tab Groups"
        },
        {
          "depth": 2,
          "id": "shaping-a-document-well",
          "text": "Shaping a Document Well"
        },
        {
          "depth": 2,
          "id": "constraining-tool-window-placement",
          "text": "Constraining Tool Window Placement"
        },
        {
          "depth": 2,
          "id": "side-pins-auto-hide",
          "text": "Side Pins (Auto-Hide)"
        },
        {
          "depth": 2,
          "id": "persistence",
          "text": "Persistence"
        },
        {
          "depth": 2,
          "id": "floating-tear-outs",
          "text": "Floating Tear-Outs"
        },
        {
          "depth": 2,
          "id": "pane-hooks",
          "text": "Pane Hooks"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Async Resources",
      "path": "async-resources.md",
      "slug": "/async-resources/",
      "html": "<h1 id=\"async-resources\">Async Resources<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-resources\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor)'s async hooks — <code>UseResource</code>, <code>UseInfiniteResource</code>, and\n<code>UseMutation</code> — replace the \"one <code>UseState</code> for data, one for loading, one\nfor error, one <code>UseEffect</code> to tie them together\" pattern. They own the\nfetch lifecycle: cancellation on deps-change, drop late results after\nunmount, shared caching across siblings, and stale-while-revalidate by default.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Use it for</th></tr></thead><tbody><tr><td><code>UseResource</code></td><td>A single async read (fetch one record, one page, one computed value)</td></tr><tr><td><code>UseInfiniteResource</code></td><td>Cursor-paginated reads paired with <code>VirtualList</code> or scroll-driven loaders</td></tr><tr><td><code>UseMutation</code></td><td>Optimistic writes with <code>InvalidateKeys</code> to refresh sibling resources</td></tr><tr><td><code>PendingFactory.Pending</code></td><td>Hold a fallback in place until every nested resource leaves <code>Loading</code></td></tr></tbody></table>\n<p>For the full state-machine reference, see\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/reference/async-system.md\">async-system.md</a>. This page is task-oriented:\neach section is a recipe you can copy.</p>\n<h2 id=\"porting-useeffect--usestate--useresource\">Porting UseEffect + UseState → UseResource<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#porting-useeffect--usestate--useresource\">#</a></h2>\n<p>The old pattern: three <code>UseState</code> hooks (data, error, loading) plus a\n<code>UseEffect</code> that orchestrates the fetch, cancellation, and state updates:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BeforeUseResourceExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (data, setData) = UseState&#x3C;DemoApi.User?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> (error, setError) = UseState&#x3C;Exception?>(<span class=\"hljs-literal\">null</span>);\n        <span class=\"hljs-keyword\">var</span> (loading, setLoading) = UseState(<span class=\"hljs-literal\">true</span>);\n\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n            setLoading(<span class=\"hljs-literal\">true</span>);\n            _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n            {\n                <span class=\"hljs-keyword\">try</span>\n                {\n                    <span class=\"hljs-keyword\">var</span> u = <span class=\"hljs-keyword\">await</span> DemoApi.GetUserAsync(<span class=\"hljs-number\">42</span>, cts.Token);\n                    <span class=\"hljs-keyword\">if</span> (!cts.IsCancellationRequested)\n                    {\n                        setData(u);\n                        setError(<span class=\"hljs-literal\">null</span>);\n                        setLoading(<span class=\"hljs-literal\">false</span>);\n                    }\n                }\n                <span class=\"hljs-keyword\">catch</span> (OperationCanceledException) { <span class=\"hljs-comment\">/* swallow */</span> }\n                <span class=\"hljs-keyword\">catch</span> (Exception ex)\n                {\n                    <span class=\"hljs-keyword\">if</span> (!cts.IsCancellationRequested)\n                    {\n                        setError(ex);\n                        setLoading(<span class=\"hljs-literal\">false</span>);\n                    }\n                }\n            });\n            <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n        }, <span class=\"hljs-number\">42</span>);\n\n        <span class=\"hljs-keyword\">if</span> (loading) <span class=\"hljs-keyword\">return</span> (Element)TextBlock(<span class=\"hljs-string\">\"Loading…\"</span>).Padding(<span class=\"hljs-number\">24</span>);\n        <span class=\"hljs-keyword\">if</span> (error <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span> (Element)TextBlock(<span class=\"hljs-string\">$\"Error: <span class=\"hljs-subst\">{error.Message}</span>\"</span>).Padding(<span class=\"hljs-number\">24</span>);\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n            Heading(<span class=\"hljs-string\">\"Before: manual plumbing\"</span>).FontSize(<span class=\"hljs-number\">14</span>),\n            TextBlock(data?.Name ?? <span class=\"hljs-string\">\"(none)\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n            TextBlock(data?.Role ?? <span class=\"hljs-string\">\"\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/async-resources-cookbook/before-useresource.png\" alt=\"Before: four hooks and manual cancellation plumbing\"></p>\n<p><code>UseResource</code> collapses all four into one call. The fetcher receives a\ncancellation token that fires on deps change or unmount; the hook returns\nan <code>AsyncValue&#x3C;T></code> you pattern-match against:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AfterUseResourceExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> user = UseResource(\n            ct => DemoApi.GetUserAsync(<span class=\"hljs-number\">42</span>, ct),\n            deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-number\">42</span> });\n\n        <span class=\"hljs-keyword\">return</span> user.Match&#x3C;Element>(\n            loading: () => TextBlock(<span class=\"hljs-string\">\"Loading…\"</span>).Padding(<span class=\"hljs-number\">24</span>),\n            data: u => VStack(<span class=\"hljs-number\">4</span>,\n                Heading(<span class=\"hljs-string\">\"After: one hook\"</span>).FontSize(<span class=\"hljs-number\">14</span>),\n                TextBlock(u.Name).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n                TextBlock(u.Role).Opacity(<span class=\"hljs-number\">0.6</span>)\n            ).Padding(<span class=\"hljs-number\">24</span>),\n            error: ex => TextBlock(<span class=\"hljs-string\">$\"Error: <span class=\"hljs-subst\">{ex.Message}</span>\"</span>).Padding(<span class=\"hljs-number\">24</span>));\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/async-resources-cookbook/after-useresource.png\" alt=\"After: one hook returning an AsyncValue\"></p>\n<p><code>AsyncValue&#x3C;T>.Match</code> dispatches on the four states:\n<code>Loading</code> (first fetch), <code>Data(value)</code> (success), <code>Error(exception)</code> (failure),\nand <code>Reloading(previous)</code> (stale-while-revalidate — a refetch with the old value\nstill available). Omitting the <code>reloading</code> arm makes it fall back to <code>data</code>, so\nthe UI keeps the last-known value visible while revalidating.</p>\n<h2 id=\"infinite-scroll-with-useinfiniteresource\">Infinite Scroll with UseInfiniteResource<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#infinite-scroll-with-useinfiniteresource\">#</a></h2>\n<p><code>UseInfiniteResource</code> models a cursor-paginated read. Drive fetches from\n<code>ItemAt(i)</code> inside a <code>VirtualList</code> <code>renderItem</code> — that's the pull model:\nthe virtualizer asks for item <em>i</em>, the hook schedules whichever page covers it,\nand the row renders a shimmer until the page lands:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">InfiniteScrollExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> commits = UseInfiniteResource&#x3C;DemoApi.Commit, <span class=\"hljs-built_in\">string</span>>(\n            fetchPage: <span class=\"hljs-keyword\">async</span> (cursor, ct) =>\n            {\n                <span class=\"hljs-keyword\">var</span> (items, next, total) = <span class=\"hljs-keyword\">await</span> DemoApi.GetCommitsPageAsync(cursor, ct);\n                <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> Page&#x3C;DemoApi.Commit, <span class=\"hljs-built_in\">string</span>>(items, next, total);\n            },\n            deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-string\">\"repo-main\"</span> });\n\n        <span class=\"hljs-comment\">// With a real virtualizer, drive fetches from ItemAt inside VirtualList's</span>\n        <span class=\"hljs-comment\">// renderItem. The important bit: null return = placeholder row.</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n            Heading(<span class=\"hljs-string\">$\"Commits (<span class=\"hljs-subst\">{commits.TotalCount ?? <span class=\"hljs-number\">0</span>}</span>)\"</span>).FontSize(<span class=\"hljs-number\">14</span>),\n            VStack(<span class=\"hljs-number\">2</span>,\n                Enumerable.Range(<span class=\"hljs-number\">0</span>, Math.Min(commits.Items.Count, <span class=\"hljs-number\">6</span>))\n                    .Select(i =>\n                    {\n                        <span class=\"hljs-keyword\">var</span> commit = commits.ItemAt(i);\n                        <span class=\"hljs-keyword\">return</span> commit <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n                            ? TextBlock(<span class=\"hljs-string\">\"…\"</span>).Opacity(<span class=\"hljs-number\">0.4</span>).Padding(<span class=\"hljs-number\">4</span>)\n                            : TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{commit.Sha}</span> — <span class=\"hljs-subst\">{commit.Message}</span>\"</span>).Padding(<span class=\"hljs-number\">4</span>);\n                    })\n                    .ToArray())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/async-resources-cookbook/infinite-scroll.png\" alt=\"Infinite scroll with on-demand page loading\"></p>\n<p>Scrolling ahead faults in new pages automatically. <code>LoadState</code> drives\nfooter UI (spinner, end-of-list marker, retry button). For explicit\nprefetching, call <code>commits.EnsureRange(first, last)</code> from the\n<code>onVisibleRangeChanged</code> callback — faster than row-by-row <code>ItemAt</code>.</p>\n<p>Pull-to-refresh is <code>commits.Refresh()</code>: it cancels in-flight fetches,\nclears the page table, and refetches page 0. Retry after error is\n<code>commits.Retry()</code> — only the failed page is re-requested.</p>\n<h2 id=\"migrating-from-a-datapagecache-style-cache\">Migrating from a DataPageCache-Style Cache<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#migrating-from-a-datapagecache-style-cache\">#</a></h2>\n<p>If you've rolled a block-cache over <code>IDataSource&#x3C;T></code> — the pre-Phase-3\n<code>DataPageCache&#x3C;T></code> is the canonical example — the replacement path is five\nmechanical steps:</p>\n<ol>\n<li><strong>Swap the cache field for a hook call.</strong> Replace the long-lived\n<code>DataPageCache&#x3C;T></code> instance with <code>var resource = UseDataSource(source, request, options);</code>.\n<code>UseDataSource</code> bridges any <code>IDataSource&#x3C;T></code> onto <code>UseInfiniteResource</code>\nusing <code>request.ContinuationToken</code> as the cursor.</li>\n<li><strong>Read from <code>resource.Items[i]</code>.</strong> The sparse <code>IReadOnlyList&#x3C;T?></code> has\nnull placeholders for in-flight slots. This replaces <code>cache.PeekItem(i)</code>\nplus the <code>LoadingBlock</code> sentinel.</li>\n<li><strong>Drop <code>BlockLoaded</code> event wiring.</strong> The hook re-renders the component\non every state transition — no manual subscription needed.</li>\n<li><strong>Route prefetch through <code>EnsureRange</code>.</strong> Replace <code>cache.RequestBlock(i)</code>\ncalls inside <code>onVisibleRangeChanged</code> with <code>resource.EnsureRange(first, last)</code>.\nThe hook dedups in-flight fetches.</li>\n<li><strong>Request changes = deps change.</strong> Sort/filter/search updates flow\nthrough <code>request</code> into the hook's deps. The hook cancels in-flight\nfetches, unsubscribes the old cache keys, and restarts on page 0.</li>\n</ol>\n<p>The <code>DataGrid&#x3C;T></code> built-in runs both code paths today, gated by\n<code>ReactorFeatureFlags.UseHookBasedPaging</code>. See\n<a href=\"/data-system/\">data-system.md</a> for the full DataGrid story.</p>\n<blockquote>\n<p><strong>Mutation overlay still lives on the caller.</strong> The hook's page table is\nserver-sourced and immutable. Optimistic edits should be kept in your\nown overlay (<code>Dictionary&#x3C;int, T></code>) that takes precedence over <code>resource.Items[i]</code>\nat read time. <code>DataGridState&#x3C;T></code> implements this pattern — see its\n<code>_mutations</code> field for the reference shape.</p>\n</blockquote>\n<h2 id=\"optimistic-writes-with-usemutation\">Optimistic Writes with UseMutation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#optimistic-writes-with-usemutation\">#</a></h2>\n<p><code>UseMutation</code> separates reads from writes. Register it once per component;\ncall <code>mutation.RunAsync(input)</code> from click handlers. The optimistic\ncallback fires synchronously so the UI never flashes stale data waiting\nfor the server:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">UseMutationExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (todos, setTodos) = UseState&#x3C;IReadOnlyList&#x3C;DemoApi.Todo>>(Array.Empty&#x3C;DemoApi.Todo>());\n\n        <span class=\"hljs-keyword\">var</span> mutation = UseMutation&#x3C;DemoApi.TodoInput, DemoApi.Todo>(\n            mutator: (input, ct) => DemoApi.AddTodoAsync(input, ct),\n            options: <span class=\"hljs-keyword\">new</span> MutationOptions&#x3C;DemoApi.TodoInput, DemoApi.Todo>(\n                OnOptimistic: input =>\n                    setTodos([.. todos, <span class=\"hljs-keyword\">new</span> DemoApi.Todo(input.Title, IsTemporary: <span class=\"hljs-literal\">true</span>)]),\n                OnSuccess: (todo, _) =>\n                    setTodos([.. todos.Select(t => t.IsTemporary ? todo : t)]),\n                OnError: (_, input) =>\n                    setTodos([.. todos.Where(t => t.Title != input.Title)]),\n                InvalidateKeys: [<span class=\"hljs-string\">\"todos/list\"</span>]));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            Heading(<span class=\"hljs-string\">$\"Todos (<span class=\"hljs-subst\">{todos.Count}</span>)\"</span>).FontSize(<span class=\"hljs-number\">14</span>),\n            VStack(<span class=\"hljs-number\">2</span>, todos.Select(t =>\n                TextBlock(t.Title + (t.IsTemporary ? <span class=\"hljs-string\">\" (saving…)\"</span> : <span class=\"hljs-string\">\"\"</span>))\n                    .Opacity(t.IsTemporary ? <span class=\"hljs-number\">0.5</span> : <span class=\"hljs-number\">1.0</span>)).ToArray()),\n            Button(<span class=\"hljs-string\">\"Add Todo\"</span>,\n                () => _ = mutation.RunAsync(<span class=\"hljs-keyword\">new</span> DemoApi.TodoInput(<span class=\"hljs-string\">$\"Item <span class=\"hljs-subst\">{todos.Count + <span class=\"hljs-number\">1</span>}</span>\"</span>)))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/async-resources-cookbook/use-mutation.png\" alt=\"Optimistic add with server confirmation\"></p>\n<p><code>InvalidateKeys</code> invalidates cache entries on success. Any sibling\n<code>UseResource</code> subscribed to those keys observes the invalidation and\nrefetches. On error, <code>InvalidateKeys</code> does <strong>not</strong> fire — the server\nstate didn't change, so the cache is still valid.</p>\n<p>Unmount during pending cancels the mutator token. <code>OnError</code> does <strong>not</strong>\nfire for cancellation — it's silent, matching <code>UseResource</code>'s\ncancellation semantics.</p>\n<h2 id=\"pending-fallbacks\">Pending Fallbacks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pending-fallbacks\">#</a></h2>\n<p>Wrap a subtree that depends on multiple resources in\n<code>PendingFactory.Pending(fallback, child)</code>. The fallback stays visible\nuntil every <code>UseResource</code> / <code>UseInfiniteResource</code> inside the subtree has\nleft the initial <code>Loading</code> state:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PendingFallbackExample</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> PendingFactory.Pending(\n            fallback: TextBlock(<span class=\"hljs-string\">\"Loading dashboard…\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>).Padding(<span class=\"hljs-number\">24</span>),\n            child: VStack(<span class=\"hljs-number\">8</span>,\n                Heading(<span class=\"hljs-string\">\"Dashboard\"</span>).FontSize(<span class=\"hljs-number\">14</span>),\n                Component&#x3C;UserHeader>(),\n                Component&#x3C;RecentActivity>(),\n                Component&#x3C;Stats>()\n            ).Padding(<span class=\"hljs-number\">24</span>));\n    }\n\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">UserHeader</span> : <span class=\"hljs-title\">Component</span>\n    {\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n        {\n            <span class=\"hljs-keyword\">var</span> user = UseResource(\n                ct => DemoApi.GetUserAsync(<span class=\"hljs-number\">1</span>, ct),\n                deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-string\">\"user-1\"</span> });\n            <span class=\"hljs-keyword\">return</span> user.Match&#x3C;Element>(\n                loading: () => TextBlock(<span class=\"hljs-string\">\"• user loading…\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>),\n                data: u => TextBlock(<span class=\"hljs-string\">$\"• <span class=\"hljs-subst\">{u.Name}</span> (<span class=\"hljs-subst\">{u.Role}</span>)\"</span>),\n                error: ex => TextBlock(<span class=\"hljs-string\">$\"• error: <span class=\"hljs-subst\">{ex.Message}</span>\"</span>));\n        }\n    }\n\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RecentActivity</span> : <span class=\"hljs-title\">Component</span>\n    {\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n        {\n            <span class=\"hljs-keyword\">var</span> feed = UseInfiniteResource&#x3C;DemoApi.Commit, <span class=\"hljs-built_in\">string</span>>(\n                fetchPage: <span class=\"hljs-keyword\">async</span> (cursor, ct) =>\n                {\n                    <span class=\"hljs-keyword\">var</span> (items, next, total) = <span class=\"hljs-keyword\">await</span> DemoApi.GetCommitsPageAsync(cursor, ct);\n                    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> Page&#x3C;DemoApi.Commit, <span class=\"hljs-built_in\">string</span>>(items, next, total);\n                },\n                deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-string\">\"feed\"</span> });\n            <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"• <span class=\"hljs-subst\">{feed.Items.Count}</span> recent items\"</span>);\n        }\n    }\n\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Stats</span> : <span class=\"hljs-title\">Component</span>\n    {\n        <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n        {\n            <span class=\"hljs-keyword\">var</span> user = UseResource(\n                ct => DemoApi.GetUserAsync(<span class=\"hljs-number\">99</span>, ct),\n                deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-string\">\"stats\"</span> });\n            <span class=\"hljs-keyword\">return</span> user.Match(\n                loading: () => TextBlock(<span class=\"hljs-string\">\"• stats loading…\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>),\n                data: _ => TextBlock(<span class=\"hljs-string\">\"• stats ready\"</span>),\n                error: ex => TextBlock(<span class=\"hljs-string\">$\"• error: <span class=\"hljs-subst\">{ex.Message}</span>\"</span>));\n        }\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/async-resources-cookbook/pending-fallback.png\" alt=\"Pending fallback while nested resources load\"></p>\n<p>Both trees are mounted — the child renders in the background so it's\nready when all three resources resolve. <code>Reloading</code> (stale-while-revalidate\nrefetches) does <strong>not</strong> re-trigger the fallback; only the initial <code>Loading</code>\ndoes. This matches TanStack's <code>Suspense</code> semantics and avoids the \"blink\nevery revalidation\" anti-pattern.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Keep deps value-comparable.</strong> A new <code>List&#x3C;T></code> or lambda each render\nwill thrash the hook's deps-change detection. Memoize with <code>UseMemo</code>, or\nproject to a scalar cache key. Wait for <code>REACTOR_HOOKS_004</code> (WIP) to\nflag the pattern at compile time.</p>\n<p><strong>Only use <code>UseResource</code> for reads.</strong> The hook may retry and refetch —\nnon-idempotent fetchers will double-create on retry. Use\n<a href=\"#optimistic-writes-with-usemutation\"><code>UseMutation</code></a> for writes.</p>\n<p><strong>Cursor paging is inherently serial.</strong> <code>UseInfiniteResource</code> must load\npage <em>N-1</em> before requesting page <em>N</em> because the cursor lives in the\nprevious page's payload. For parallel offset-based paging, pass an offset\nas the cursor and <code>InfiniteResourceOptions.PageSize</code> to size each batch —\nthe hook won't parallelize for you.</p>\n<p><strong>Don't share a <code>CacheKey</code> across unrelated hooks.</strong> The default per-hook\nauto-key keeps siblings independent. Opt into sharing explicitly via\n<code>ResourceOptions.CacheKey</code> — typically when you want two distant subtrees\nto observe the same entry.</p>\n<p><strong>Pending applies only to Loading, not Reloading.</strong> If the fallback\nflashes on every refetch, you've probably wrapped the resource in a\n<code>Pending</code> that's checking <code>Reloading</code> too. Read the spec's §10.1 for the\nexact predicate.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/effects/\">Effects and Lifecycle</a></strong> — Previous: the <code>UseEffect</code> primitive underneath the old pattern</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Next: command tracking with async execution</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — Full DataGrid story including hook-based paging</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Core hook primitives (<code>UseState</code>, <code>UseReducer</code>, <code>UseRef</code>)</li>\n</ul>",
      "text": "Async Resources # Microsoft.UI.Reactor (Reactor)'s async hooks — UseResource , UseInfiniteResource , and UseMutation — replace the \"one UseState for data, one for loading, one for error, one UseEffect to tie them together\" pattern. They own the fetch lifecycle: cancellation on deps-change, drop late results after unmount, shared caching across siblings, and stale-while-revalidate by default. Hook Use it for UseResource A single async read (fetch one record, one page, one computed value) UseInfiniteResource Cursor-paginated reads paired with VirtualList or scroll-driven loaders UseMutation Optimistic writes with InvalidateKeys to refresh sibling resources PendingFactory.Pending Hold a fallback in place until every nested resource leaves Loading For the full state-machine reference, see async-system.md . This page is task-oriented: each section is a recipe you can copy. Porting UseEffect + UseState → UseResource # The old pattern: three UseState hooks (data, error, loading) plus a UseEffect that orchestrates the fetch, cancellation, and state updates: class BeforeUseResourceExample : Component { public override Element Render () { var (data, setData) = UseState&#x3C;DemoApi.User?>( null ); var (error, setError) = UseState&#x3C;Exception?>( null ); var (loading, setLoading) = UseState( true ); UseEffect(() => { var cts = new CancellationTokenSource(); setLoading( true ); _ = Task.Run( async () => { try { var u = await DemoApi.GetUserAsync( 42 , cts.Token); if (!cts.IsCancellationRequested) { setData(u); setError( null ); setLoading( false ); } } catch (OperationCanceledException) { /* swallow */ } catch (Exception ex) { if (!cts.IsCancellationRequested) { setError(ex); setLoading( false ); } } }); return () => cts.Cancel(); }, 42 ); if (loading) return (Element)TextBlock( \"Loading…\" ).Padding( 24 ); if (error is not null ) return (Element)TextBlock( $\"Error: {error.Message} \" ).Padding( 24 ); return VStack( 4 , Heading( \"Before: manual plumbing\" ).FontSize( 14 ), TextBlock(data?.Name ?? \"(none)\" ).FontSize( 20 ).Bold(), TextBlock(data?.Role ?? \"\" ).Opacity( 0.6 ) ).Padding( 24 ); } } UseResource collapses all four into one call. The fetcher receives a cancellation token that fires on deps change or unmount; the hook returns an AsyncValue&#x3C;T> you pattern-match against: class AfterUseResourceExample : Component { public override Element Render () { var user = UseResource( ct => DemoApi.GetUserAsync( 42 , ct), deps: new object [] { 42 }); return user.Match&#x3C;Element>( loading: () => TextBlock( \"Loading…\" ).Padding( 24 ), data: u => VStack( 4 , Heading( \"After: one hook\" ).FontSize( 14 ), TextBlock(u.Name).FontSize( 20 ).Bold(), TextBlock(u.Role).Opacity( 0.6 ) ).Padding( 24 ), error: ex => TextBlock( $\"Error: {ex.Message} \" ).Padding( 24 )); } } AsyncValue&#x3C;T>.Match dispatches on the four states: Loading (first fetch), Data(value) (success), Error(exception) (failure), and Reloading(previous) (stale-while-revalidate — a refetch with the old value still available). Omitting the reloading arm makes it fall back to data , so the UI keeps the last-known value visible while revalidating. Infinite Scroll with UseInfiniteResource # UseInfiniteResource models a cursor-paginated read. Drive fetches from ItemAt(i) inside a VirtualList renderItem — that's the pull model: the virtualizer asks for item i , the hook schedules whichever page covers it, and the row renders a shimmer until the page lands: class InfiniteScrollExample : Component { public override Element Render () { var commits = UseInfiniteResource&#x3C;DemoApi.Commit, string >( fetchPage: async (cursor, ct) => { var (items, next, total) = await DemoApi.GetCommitsPageAsync(cursor, ct); return new Page&#x3C;DemoApi.Commit, string >(items, next, total); }, deps: new object [] { \"repo-main\" }); // With a real virtualizer, drive fetches from ItemAt inside VirtualList's // renderItem. The important bit: null return = placeholder row. return VStack( 4 , Heading( $\"Commits ( {commits.TotalCount ?? 0 } )\" ).FontSize( 14 ), VStack( 2 , Enumerable.Range( 0 , Math.Min(commits.Items.Count, 6 )) .Select(i => { var commit = commits.ItemAt(i); return commit is null ? TextBlock( \"…\" ).Opacity( 0.4 ).Padding( 4 ) : TextBlock( $\" {commit.Sha} — {commit.Message} \" ).Padding( 4 ); }) .ToArray()) ).Padding( 24 ); } } Scrolling ahead faults in new pages automatically. LoadState drives footer UI (spinner, end-of-list marker, retry button). For explicit prefetching, call commits.EnsureRange(first, last) from the onVisibleRangeChanged callback — faster than row-by-row ItemAt . Pull-to-refresh is commits.Refresh() : it cancels in-flight fetches, clears the page table, and refetches page 0. Retry after error is commits.Retry() — only the failed page is re-requested. Migrating from a DataPageCache-Style Cache # If you've rolled a block-cache over IDataSource&#x3C;T> — the pre-Phase-3 DataPageCache&#x3C;T> is the canonical example — the replacement path is five mechanical steps: Swap the cache field for a hook call. Replace the long-lived DataPageCache&#x3C;T> instance with var resource = UseDataSource(source, request, options); . UseDataSource bridges any IDataSource&#x3C;T> onto UseInfiniteResource using request.ContinuationToken as the cursor. Read from resource.Items[i] . The sparse IReadOnlyList&#x3C;T?> has null placeholders for in-flight slots. This replaces cache.PeekItem(i) plus the LoadingBlock sentinel. Drop BlockLoaded event wiring. The hook re-renders the component on every state transition — no manual subscription needed. Route prefetch through EnsureRange . Replace cache.RequestBlock(i) calls inside onVisibleRangeChanged with resource.EnsureRange(first, last) . The hook dedups in-flight fetches. Request changes = deps change. Sort/filter/search updates flow through request into the hook's deps. The hook cancels in-flight fetches, unsubscribes the old cache keys, and restarts on page 0. The DataGrid&#x3C;T> built-in runs both code paths today, gated by ReactorFeatureFlags.UseHookBasedPaging . See data-system.md for the full DataGrid story. Mutation overlay still lives on the caller. The hook's page table is server-sourced and immutable. Optimistic edits should be kept in your own overlay ( Dictionary&#x3C;int, T> ) that takes precedence over resource.Items[i] at read time. DataGridState&#x3C;T> implements this pattern — see its _mutations field for the reference shape. Optimistic Writes with UseMutation # UseMutation separates reads from writes. Register it once per component; call mutation.RunAsync(input) from click handlers. The optimistic callback fires synchronously so the UI never flashes stale data waiting for the server: class UseMutationExample : Component { public override Element Render () { var (todos, setTodos) = UseState&#x3C;IReadOnlyList&#x3C;DemoApi.Todo>>(Array.Empty&#x3C;DemoApi.Todo>()); var mutation = UseMutation&#x3C;DemoApi.TodoInput, DemoApi.Todo>( mutator: (input, ct) => DemoApi.AddTodoAsync(input, ct), options: new MutationOptions&#x3C;DemoApi.TodoInput, DemoApi.Todo>( OnOptimistic: input => setTodos([.. todos, new DemoApi.Todo(input.Title, IsTemporary: true )]), OnSuccess: (todo, _) => setTodos([.. todos.Select(t => t.IsTemporary ? todo : t)]), OnError: (_, input) => setTodos([.. todos.Where(t => t.Title != input.Title)]), InvalidateKeys: [ \"todos/list\" ])); return VStack( 8 , Heading( $\"Todos ( {todos.Count} )\" ).FontSize( 14 ), VStack( 2 , todos.Select(t => TextBlock(t.Title + (t.IsTemporary ? \" (saving…)\" : \"\" )) .Opacity(t.IsTemporary ? 0.5 : 1.0 )).ToArray()), Button( \"Add Todo\" , () => _ = mutation.RunAsync( new DemoApi.TodoInput( $\"Item {todos.Count + 1 } \" ))) ).Padding( 24 ); } } InvalidateKeys invalidates cache entries on success. Any sibling UseResource subscribed to those keys observes the invalidation and refetches. On error, InvalidateKeys does not fire — the server state didn't change, so the cache is still valid. Unmount during pending cancels the mutator token. OnError does not fire for cancellation — it's silent, matching UseResource 's cancellation semantics. Pending Fallbacks # Wrap a subtree that depends on multiple resources in PendingFactory.Pending(fallback, child) . The fallback stays visible until every UseResource / UseInfiniteResource inside the subtree has left the initial Loading state: class PendingFallbackExample : Component { public override Element Render () { return PendingFactory.Pending( fallback: TextBlock( \"Loading dashboard…\" ).Opacity( 0.5 ).Padding( 24 ), child: VStack( 8 , Heading( \"Dashboard\" ).FontSize( 14 ), Component&#x3C;UserHeader>(), Component&#x3C;RecentActivity>(), Component&#x3C;Stats>() ).Padding( 24 )); } private class UserHeader : Component { public override Element Render () { var user = UseResource( ct => DemoApi.GetUserAsync( 1 , ct), deps: new object [] { \"user-1\" }); return user.Match&#x3C;Element>( loading: () => TextBlock( \"• user loading…\" ).Opacity( 0.5 ), data: u => TextBlock( $\"• {u.Name} ( {u.Role} )\" ), error: ex => TextBlock( $\"• error: {ex.Message} \" )); } } private class RecentActivity : Component { public override Element Render () { var feed = UseInfiniteResource&#x3C;DemoApi.Commit, string >( fetchPage: async (cursor, ct) => { var (items, next, total) = await DemoApi.GetCommitsPageAsync(cursor, ct); return new Page&#x3C;DemoApi.Commit, string >(items, next, total); }, deps: new object [] { \"feed\" }); return TextBlock( $\"• {feed.Items.Count} recent items\" ); } } private class Stats : Component { public override Element Render () { var user = UseResource( ct => DemoApi.GetUserAsync( 99 , ct), deps: new object [] { \"stats\" }); return user.Match( loading: () => TextBlock( \"• stats loading…\" ).Opacity( 0.5 ), data: _ => TextBlock( \"• stats ready\" ), error: ex => TextBlock( $\"• error: {ex.Message} \" )); } } } Both trees are mounted — the child renders in the background so it's ready when all three resources resolve. Reloading (stale-while-revalidate refetches) does not re-trigger the fallback; only the initial Loading does. This matches TanStack's Suspense semantics and avoids the \"blink every revalidation\" anti-pattern. Tips # Keep deps value-comparable. A new List&#x3C;T> or lambda each render will thrash the hook's deps-change detection. Memoize with UseMemo , or project to a scalar cache key. Wait for REACTOR_HOOKS_004 (WIP) to flag the pattern at compile time. Only use UseResource for reads. The hook may retry and refetch — non-idempotent fetchers will double-create on retry. Use UseMutation for writes. Cursor paging is inherently serial. UseInfiniteResource must load page N-1 before requesting page N because the cursor lives in the previous page's payload. For parallel offset-based paging, pass an offset as the cursor and InfiniteResourceOptions.PageSize to size each batch — the hook won't parallelize for you. Don't share a CacheKey across unrelated hooks. The default per-hook auto-key keeps siblings independent. Opt into sharing explicitly via ResourceOptions.CacheKey — typically when you want two distant subtrees to observe the same entry. Pending applies only to Loading, not Reloading. If the fallback flashes on every refetch, you've probably wrapped the resource in a Pending that's checking Reloading too. Read the spec's §10.1 for the exact predicate. Next Steps # Effects and Lifecycle — Previous: the UseEffect primitive underneath the old pattern Commanding — Next: command tracking with async execution Data System — Full DataGrid story including hook-based paging Hooks — Core hook primitives ( UseState , UseReducer , UseRef )",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/async-resources.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/async-resources.md",
      "headings": [
        {
          "depth": 2,
          "id": "porting-useeffect--usestate--useresource",
          "text": "Porting UseEffect + UseState → UseResource"
        },
        {
          "depth": 2,
          "id": "infinite-scroll-with-useinfiniteresource",
          "text": "Infinite Scroll with UseInfiniteResource"
        },
        {
          "depth": 2,
          "id": "migrating-from-a-datapagecache-style-cache",
          "text": "Migrating from a DataPageCache-Style Cache"
        },
        {
          "depth": 2,
          "id": "optimistic-writes-with-usemutation",
          "text": "Optimistic Writes with UseMutation"
        },
        {
          "depth": 2,
          "id": "pending-fallbacks",
          "text": "Pending Fallbacks"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Persistence",
      "path": "persistence.md",
      "slug": "/persistence/",
      "html": "<h1 id=\"persistence\">Persistence<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#persistence\">#</a></h1>\n<p><a href=\"/hooks/\"><code>UsePersisted</code></a> holds a value past a component re-mount —\nthe user navigates away, the component unmounts, the user navigates\nback, and the captured state is still there. It's the right hook for\n\"keep what the user typed across a tab switch\" or \"remember the\nselected row in a list while the user opens a side pane\". It is <em>not</em>\nthe right hook for \"save to disk so the value survives an app\nrestart\"; that one needs a disk bridge in <code>UseEffect</code>, covered below.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// UsePersisted with explicit Window scope. The text outlives a re-mount</span>\n<span class=\"hljs-comment\">// of the component (e.g. navigation away and back) but is dropped when</span>\n<span class=\"hljs-comment\">// the host window closes. Replace PersistedScope.Window with</span>\n<span class=\"hljs-comment\">// PersistedScope.Application for process-lifetime persistence.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NotesEditor</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UsePersisted(\n            <span class=\"hljs-string\">\"notes/body\"</span>,\n            initialValue: <span class=\"hljs-string\">\"\"</span>,\n            scope: PersistedScope.Window);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Notes\"</span>),\n            TextBox(text, setText, placeholderText: <span class=\"hljs-string\">\"Start typing…\"</span>).Width(<span class=\"hljs-number\">380</span>),\n            TextBlock(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{text.Length}</span> characters\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/persistence/notes-editor.png\" alt=\"Notes editor — UsePersisted demo\"></p>\n<p>The lookup happens once per mount: the hook checks the named scope\nfor <code>\"notes/body\"</code>, returns the cached value if present (otherwise\nthe supplied <code>initialValue</code>), and writes the latest value back into\nthe scope on every state update. When the component unmounts and\nlater remounts, the second mount finds the stored value and skips the\ndefault.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Returns</th><th>Notes</th></tr></thead><tbody><tr><td><code>UsePersisted&#x3C;T>(key, initialValue)</code></td><td><code>(T, Action&#x3C;T>)</code></td><td>Defaults to <code>PersistedScope.Application</code>.</td></tr><tr><td><code>UsePersisted&#x3C;T>(key, initialValue, scope)</code></td><td><code>(T, Action&#x3C;T>)</code></td><td>Explicit scope — recommended in new code.</td></tr><tr><td><code>PersistedScope.Window</code></td><td>enum value</td><td>Bound to the host window; dropped on unload.</td></tr><tr><td><code>PersistedScope.Application</code></td><td>enum value</td><td>Process lifetime; survives across windows.</td></tr><tr><td><code>IPersistedStateScope</code></td><td>interface</td><td>LRU-bounded cache; <code>TryGet</code>/<code>Set</code>/<code>Remove</code>.</td></tr><tr><td><code>ApplicationPersistedScope.Default</code></td><td>static</td><td>Process-wide singleton, 4096-entry LRU.</td></tr><tr><td><code>ReactorWindow.PersistedScope</code></td><td>property</td><td>Per-window scope <code>PersistedScope.Window</code> resolves to.</td></tr></tbody></table>\n<p>The cache is <strong>in-memory only</strong>. It is bounded by LRU (4096 entries\nby default for application scope) and registers for OS\nmemory-pressure notifications — when the host signals pressure, the\nscope shrinks to 25% of capacity. No serialization to disk happens\ninside the framework.</p>\n<h2 id=\"scopes\">Scopes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#scopes\">#</a></h2>\n<p><code>PersistedScope.Window</code> ties the value to the <a href=\"/windows/\"><code>ReactorWindow</code></a>\nthe component lives in. Two open windows of the same app component\nclass hold independent state under the same key. When the window\ncloses, the entire <code>WindowPersistedScope</code> is disposed and its\nentries drop.</p>\n<p><code>PersistedScope.Application</code> survives across windows in the same\nprocess. Two windows opened sequentially against the same key see\nthe previous one's value. The process exiting always clears the\nstate.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Survives a tab swap; dropped on window close.</span>\n<span class=\"hljs-keyword\">var</span> (filter, setFilter) = UsePersisted(\n    <span class=\"hljs-string\">\"list/filter\"</span>, <span class=\"hljs-string\">\"\"</span>, PersistedScope.Window);\n\n<span class=\"hljs-comment\">// Survives navigation across windows in this process.</span>\n<span class=\"hljs-keyword\">var</span> (token, setToken) = UsePersisted(\n    <span class=\"hljs-string\">\"auth/token\"</span>, <span class=\"hljs-string\">\"\"</span>, PersistedScope.Application);\n</code></pre>\n<p>The two-arg overload <code>UsePersisted(key, initial)</code> defaults to\n<code>PersistedScope.Application</code> for back-compat and will eventually\ntrigger an analyzer warning — new code should pass the scope\nexplicitly.</p>\n<h2 id=\"versioned-shape-migration\">Versioned shape migration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#versioned-shape-migration\">#</a></h2>\n<p>When the shape stored under a key changes, never reuse the same key.\nBump the version in the key suffix and write a one-shot migrator that\nmoves the v1 payload into v2 at app startup:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Versioned persisted shape. When the field set changes, bump the</span>\n<span class=\"hljs-comment\">// version and migrate forward. The reader matches on the stored shape</span>\n<span class=\"hljs-comment\">// and never trusts the cache to hold a current schema.</span>\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">NotesStateV1</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Body</span>, <span class=\"hljs-title\">DateTimeOffset</span> <span class=\"hljs-title\">LastEdit</span>);\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">NotesStateV2</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Body</span>, <span class=\"hljs-title\">DateTimeOffset</span> <span class=\"hljs-title\">LastEdit</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Title</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">VersionedNotesEditor</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (state, setState) = UsePersisted(\n            <span class=\"hljs-string\">\"notes/state-v2\"</span>,\n            initialValue: <span class=\"hljs-keyword\">new</span> NotesStateV2(<span class=\"hljs-string\">\"\"</span>, DateTimeOffset.Now, <span class=\"hljs-string\">\"\"</span>),\n            scope: PersistedScope.Application);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            TextBox(state.Title, t => setState(state <span class=\"hljs-keyword\">with</span> { Title = t }),\n                placeholderText: <span class=\"hljs-string\">\"Title\"</span>),\n            TextBox(state.Body, b => setState(state <span class=\"hljs-keyword\">with</span> { Body = b, LastEdit = DateTimeOffset.Now }),\n                placeholderText: <span class=\"hljs-string\">\"Body\"</span>)\n        );\n    }\n\n    <span class=\"hljs-comment\">// One-shot migration from the v1 key to the v2 key. Run once at app</span>\n    <span class=\"hljs-comment\">// startup; thereafter the v1 key is empty and never consulted again.</span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">MigrateOnce</span>(<span class=\"hljs-params\">IPersistedStateScope scope</span>)</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (scope.TryGet&#x3C;NotesStateV1>(<span class=\"hljs-string\">\"notes/state-v1\"</span>, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> v1))\n        {\n            scope.Set(<span class=\"hljs-string\">\"notes/state-v2\"</span>,\n                <span class=\"hljs-keyword\">new</span> NotesStateV2(v1.Body, v1.LastEdit, Title: <span class=\"hljs-string\">\"\"</span>));\n            scope.Remove(<span class=\"hljs-string\">\"notes/state-v1\"</span>);\n        }\n    }\n}\n</code></pre>\n<p>Two design choices to call out:</p>\n<ul>\n<li><strong>The key carries the version, not the payload.</strong> A reader that\ntrusts the cache to hold a current schema is one shape change away\nfrom a runtime cast exception. Reading by versioned key lets\nunknown-version lookups miss cleanly and fall back to the default.</li>\n<li><strong>Migrate once, then drop the old key.</strong> The migrator's <code>Remove</code>\ncall frees the old slot so the LRU doesn't carry two copies\nforever. Run the migrator at app startup — not in the component —\nso it executes before any consumer sees a half-populated state.</li>\n</ul>\n<h2 id=\"disk-bridge-cross-process-persistence\">Disk bridge (cross-process persistence)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#disk-bridge-cross-process-persistence\">#</a></h2>\n<p><code>UsePersisted</code> does not write to disk. For state that needs to\nsurvive a process restart — app settings, the last-opened document,\nwindow placement — combine <code>UsePersisted</code> (or <code>UseState</code>) with a\n<code>UseEffect</code> that mirrors writes to disk:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Disk-backed bridge. UsePersisted alone is in-memory only — to outlive</span>\n<span class=\"hljs-comment\">// a process restart, mirror to disk in UseEffect. The state hook holds</span>\n<span class=\"hljs-comment\">// the live value; the effect writes it whenever it changes.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PersistentSettings</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span> SettingsPath = System.IO.Path.Combine(\n        Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),\n        <span class=\"hljs-string\">\"MyApp\"</span>, <span class=\"hljs-string\">\"settings.json\"</span>);\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Seed from disk once via UseMemo; UsePersisted holds the live value</span>\n        <span class=\"hljs-comment\">// across re-mounts; the effect mirrors writes to disk.</span>\n        <span class=\"hljs-keyword\">var</span> initial = UseMemo(LoadFromDisk, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n        <span class=\"hljs-keyword\">var</span> (settings, setSettings) = UsePersisted(\n            <span class=\"hljs-string\">\"settings\"</span>, initial, PersistedScope.Application);\n\n        UseEffect(() =>\n        {\n            Directory.CreateDirectory(System.IO.Path.GetDirectoryName(SettingsPath)!);\n            File.WriteAllText(SettingsPath, JsonSerializer.Serialize(settings));\n            <span class=\"hljs-keyword\">return</span> () => { };\n        }, settings);\n\n        <span class=\"hljs-keyword\">return</span> ToggleSwitch(settings.NotificationsOn,\n            <span class=\"hljs-keyword\">on</span> => setSettings(settings <span class=\"hljs-keyword\">with</span> { NotificationsOn = <span class=\"hljs-keyword\">on</span> }),\n            header: <span class=\"hljs-string\">\"Notifications\"</span>);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> AppSettings <span class=\"hljs-title\">LoadFromDisk</span>()</span> =>\n        File.Exists(SettingsPath)\n            ? JsonSerializer.Deserialize&#x3C;AppSettings>(File.ReadAllText(SettingsPath))\n                ?? <span class=\"hljs-keyword\">new</span> AppSettings(NotificationsOn: <span class=\"hljs-literal\">true</span>)\n            : <span class=\"hljs-keyword\">new</span> AppSettings(NotificationsOn: <span class=\"hljs-literal\">true</span>);\n}\n\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">AppSettings</span>(<span class=\"hljs-title\">bool</span> <span class=\"hljs-title\">NotificationsOn</span>);\n</code></pre>\n<p>The pattern is small but the constraints are real:</p>\n<ol>\n<li><strong>Seed once.</strong> <code>UseMemo</code> with an empty dependency array reads from\ndisk on first render only. Re-running the read on every render\nwould race the writer.</li>\n<li><strong>Throttle writes.</strong> Every keystroke updates <code>settings</code>, which\nfires the effect, which writes a file. For a high-frequency value\nlike a text-box body, debounce inside the effect (e.g.\n<code>await Task.Delay(500, ct)</code>, write on cancellation-free exit).</li>\n<li><strong>Handle malformed input.</strong> <code>JsonSerializer.Deserialize</code> returns\n<code>null</code> for empty files; the example falls back to a default.\nDon't catch and swallow <code>JsonException</code> — log it; a corrupt\nsettings file is a real failure mode worth seeing.</li>\n<li><strong>Cancel on unmount.</strong> The <code>UseEffect</code> cleanup function is the\nplace to cancel any in-flight write so a closing window doesn't\nleave a half-written file on disk.</li>\n</ol>\n<p>The <a href=\"/async-resources/\">async-resources</a> page covers the cancellation\npattern in depth; reach for it once the bridge gets non-trivial.</p>\n<h2 id=\"conflict-resolution\">Conflict resolution<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#conflict-resolution\">#</a></h2>\n<p>Two components writing to the same key is a programming bug, not a\nframework feature — Microsoft.UI.Reactor (Reactor) doesn't merge concurrent updates. The\nlast writer wins. If you find two components writing to the same key,\nthe right move is to lift state up to a <a href=\"/context/\">context</a> value\nshared by both rather than coordinating through the cache. The cache\nis a <em>survive-unmount</em> facility, not a <em>coordinate-between-components</em>\none.</p>\n<p>For the cross-process disk-bridge case, \"conflict\" means a second\nprocess touched the same JSON file. Reactor doesn't lock or arbitrate\nthis; if two processes can run simultaneously against the same store,\nbuild a single-writer model (e.g. only the most-recently-focused\nwindow writes) or move to a real database (<code>Microsoft.Data.Sqlite</code>,\n<code>LiteDB</code>).</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use <code>PersistedScope.Window</code> until you have a reason to widen it.</strong>\nWindow-scoped state has clear lifecycle bounds and won't leak into a\nsecond window opened by mistake. Application scope is the right\ndefault only for genuinely process-global facts (auth tokens, feature\nflags, recently-opened-files lists).</p>\n<p><strong>Prefix keys by topic.</strong> <code>\"notes/body\"</code> over <code>\"body\"</code>; <code>\"prefs/theme\"</code>\nover <code>\"theme\"</code>. The cache is process-wide for application scope, so\ntwo unrelated topics colliding on <code>\"id\"</code> is a bug waiting to happen.\nKeys are limited to 256 characters; the validator rejects null,\nempty, or whitespace.</p>\n<p><strong>Don't cache large objects.</strong> The LRU policy means a single 1MB\npayload can evict thousands of small values. Hold the live model in\nordinary state and use <code>UsePersisted</code> to remember the <em>position</em>\ninside it (selected row id, scroll offset) — the position survives,\nthe model rehydrates from the source of truth.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Previous: the <code>UseState</code> / <code>UseMemo</code> /\n<code>UseEffect</code> shapes this hook composes with.</li>\n<li><strong><a href=\"/effects/\">Effects</a></strong> — Next: the lifecycle that drives the\ndisk-bridge pattern's <code>UseEffect</code> block.</li>\n<li><strong><a href=\"/windows/\">Windows</a></strong> — Where <code>PersistedScope.Window</code> actually\nresolves; how <code>ReactorWindow.PersistedScope</code> is wired.</li>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — Cancellation and\ndebouncing for the disk-bridge writes.</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — When persistence is the wrong tool and\nyou should be lifting state up instead.</li>\n</ul>",
      "text": "Persistence # UsePersisted holds a value past a component re-mount — the user navigates away, the component unmounts, the user navigates back, and the captured state is still there. It's the right hook for \"keep what the user typed across a tab switch\" or \"remember the selected row in a list while the user opens a side pane\". It is not the right hook for \"save to disk so the value survives an app restart\"; that one needs a disk bridge in UseEffect , covered below. // UsePersisted with explicit Window scope. The text outlives a re-mount // of the component (e.g. navigation away and back) but is dropped when // the host window closes. Replace PersistedScope.Window with // PersistedScope.Application for process-lifetime persistence. class NotesEditor : Component { public override Element Render () { var (text, setText) = UsePersisted( \"notes/body\" , initialValue: \"\" , scope: PersistedScope.Window); return VStack( 8 , SubHeading( \"Notes\" ), TextBox(text, setText, placeholderText: \"Start typing…\" ).Width( 380 ), TextBlock( $\" {text.Length} characters\" ).Opacity( 0.6 ) ).Padding( 16 ); } } The lookup happens once per mount: the hook checks the named scope for \"notes/body\" , returns the cached value if present (otherwise the supplied initialValue ), and writes the latest value back into the scope on every state update. When the component unmounts and later remounts, the second mount finds the stored value and skips the default. Reference # API Returns Notes UsePersisted&#x3C;T>(key, initialValue) (T, Action&#x3C;T>) Defaults to PersistedScope.Application . UsePersisted&#x3C;T>(key, initialValue, scope) (T, Action&#x3C;T>) Explicit scope — recommended in new code. PersistedScope.Window enum value Bound to the host window; dropped on unload. PersistedScope.Application enum value Process lifetime; survives across windows. IPersistedStateScope interface LRU-bounded cache; TryGet / Set / Remove . ApplicationPersistedScope.Default static Process-wide singleton, 4096-entry LRU. ReactorWindow.PersistedScope property Per-window scope PersistedScope.Window resolves to. The cache is in-memory only . It is bounded by LRU (4096 entries by default for application scope) and registers for OS memory-pressure notifications — when the host signals pressure, the scope shrinks to 25% of capacity. No serialization to disk happens inside the framework. Scopes # PersistedScope.Window ties the value to the ReactorWindow the component lives in. Two open windows of the same app component class hold independent state under the same key. When the window closes, the entire WindowPersistedScope is disposed and its entries drop. PersistedScope.Application survives across windows in the same process. Two windows opened sequentially against the same key see the previous one's value. The process exiting always clears the state. // Survives a tab swap; dropped on window close. var (filter, setFilter) = UsePersisted( \"list/filter\" , \"\" , PersistedScope.Window); // Survives navigation across windows in this process. var (token, setToken) = UsePersisted( \"auth/token\" , \"\" , PersistedScope.Application); The two-arg overload UsePersisted(key, initial) defaults to PersistedScope.Application for back-compat and will eventually trigger an analyzer warning — new code should pass the scope explicitly. Versioned shape migration # When the shape stored under a key changes, never reuse the same key. Bump the version in the key suffix and write a one-shot migrator that moves the v1 payload into v2 at app startup: // Versioned persisted shape. When the field set changes, bump the // version and migrate forward. The reader matches on the stored shape // and never trusts the cache to hold a current schema. record NotesStateV1 ( string Body , DateTimeOffset LastEdit ); record NotesStateV2 ( string Body , DateTimeOffset LastEdit , string Title ); class VersionedNotesEditor : Component { public override Element Render () { var (state, setState) = UsePersisted( \"notes/state-v2\" , initialValue: new NotesStateV2( \"\" , DateTimeOffset.Now, \"\" ), scope: PersistedScope.Application); return VStack( 8 , TextBox(state.Title, t => setState(state with { Title = t }), placeholderText: \"Title\" ), TextBox(state.Body, b => setState(state with { Body = b, LastEdit = DateTimeOffset.Now }), placeholderText: \"Body\" ) ); } // One-shot migration from the v1 key to the v2 key. Run once at app // startup; thereafter the v1 key is empty and never consulted again. public static void MigrateOnce ( IPersistedStateScope scope ) { if (scope.TryGet&#x3C;NotesStateV1>( \"notes/state-v1\" , out var v1)) { scope.Set( \"notes/state-v2\" , new NotesStateV2(v1.Body, v1.LastEdit, Title: \"\" )); scope.Remove( \"notes/state-v1\" ); } } } Two design choices to call out: The key carries the version, not the payload. A reader that trusts the cache to hold a current schema is one shape change away from a runtime cast exception. Reading by versioned key lets unknown-version lookups miss cleanly and fall back to the default. Migrate once, then drop the old key. The migrator's Remove call frees the old slot so the LRU doesn't carry two copies forever. Run the migrator at app startup — not in the component — so it executes before any consumer sees a half-populated state. Disk bridge (cross-process persistence) # UsePersisted does not write to disk. For state that needs to survive a process restart — app settings, the last-opened document, window placement — combine UsePersisted (or UseState ) with a UseEffect that mirrors writes to disk: // Disk-backed bridge. UsePersisted alone is in-memory only — to outlive // a process restart, mirror to disk in UseEffect. The state hook holds // the live value; the effect writes it whenever it changes. class PersistentSettings : Component { private static readonly string SettingsPath = System.IO.Path.Combine( Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), \"MyApp\" , \"settings.json\" ); public override Element Render () { // Seed from disk once via UseMemo; UsePersisted holds the live value // across re-mounts; the effect mirrors writes to disk. var initial = UseMemo(LoadFromDisk, Array.Empty&#x3C; object >()); var (settings, setSettings) = UsePersisted( \"settings\" , initial, PersistedScope.Application); UseEffect(() => { Directory.CreateDirectory(System.IO.Path.GetDirectoryName(SettingsPath)!); File.WriteAllText(SettingsPath, JsonSerializer.Serialize(settings)); return () => { }; }, settings); return ToggleSwitch(settings.NotificationsOn, on => setSettings(settings with { NotificationsOn = on }), header: \"Notifications\" ); } private static AppSettings LoadFromDisk () => File.Exists(SettingsPath) ? JsonSerializer.Deserialize&#x3C;AppSettings>(File.ReadAllText(SettingsPath)) ?? new AppSettings(NotificationsOn: true ) : new AppSettings(NotificationsOn: true ); } record AppSettings ( bool NotificationsOn ); The pattern is small but the constraints are real: Seed once. UseMemo with an empty dependency array reads from disk on first render only. Re-running the read on every render would race the writer. Throttle writes. Every keystroke updates settings , which fires the effect, which writes a file. For a high-frequency value like a text-box body, debounce inside the effect (e.g. await Task.Delay(500, ct) , write on cancellation-free exit). Handle malformed input. JsonSerializer.Deserialize returns null for empty files; the example falls back to a default. Don't catch and swallow JsonException — log it; a corrupt settings file is a real failure mode worth seeing. Cancel on unmount. The UseEffect cleanup function is the place to cancel any in-flight write so a closing window doesn't leave a half-written file on disk. The async-resources page covers the cancellation pattern in depth; reach for it once the bridge gets non-trivial. Conflict resolution # Two components writing to the same key is a programming bug, not a framework feature — Microsoft.UI.Reactor (Reactor) doesn't merge concurrent updates. The last writer wins. If you find two components writing to the same key, the right move is to lift state up to a context value shared by both rather than coordinating through the cache. The cache is a survive-unmount facility, not a coordinate-between-components one. For the cross-process disk-bridge case, \"conflict\" means a second process touched the same JSON file. Reactor doesn't lock or arbitrate this; if two processes can run simultaneously against the same store, build a single-writer model (e.g. only the most-recently-focused window writes) or move to a real database ( Microsoft.Data.Sqlite , LiteDB ). Tips # Use PersistedScope.Window until you have a reason to widen it. Window-scoped state has clear lifecycle bounds and won't leak into a second window opened by mistake. Application scope is the right default only for genuinely process-global facts (auth tokens, feature flags, recently-opened-files lists). Prefix keys by topic. \"notes/body\" over \"body\" ; \"prefs/theme\" over \"theme\" . The cache is process-wide for application scope, so two unrelated topics colliding on \"id\" is a bug waiting to happen. Keys are limited to 256 characters; the validator rejects null, empty, or whitespace. Don't cache large objects. The LRU policy means a single 1MB payload can evict thousands of small values. Hold the live model in ordinary state and use UsePersisted to remember the position inside it (selected row id, scroll offset) — the position survives, the model rehydrates from the source of truth. Next Steps # Hooks — Previous: the UseState / UseMemo / UseEffect shapes this hook composes with. Effects — Next: the lifecycle that drives the disk-bridge pattern's UseEffect block. Windows — Where PersistedScope.Window actually resolves; how ReactorWindow.PersistedScope is wired. Async Resources — Cancellation and debouncing for the disk-bridge writes. Context — When persistence is the wrong tool and you should be lifting state up instead.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/persistence.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/persistence.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "scopes",
          "text": "Scopes"
        },
        {
          "depth": 2,
          "id": "versioned-shape-migration",
          "text": "Versioned shape migration"
        },
        {
          "depth": 2,
          "id": "disk-bridge-cross-process-persistence",
          "text": "Disk bridge (cross-process persistence)"
        },
        {
          "depth": 2,
          "id": "conflict-resolution",
          "text": "Conflict resolution"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Localization",
      "path": "localization.md",
      "slug": "/localization/",
      "html": "<h1 id=\"localization\">Localization<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#localization\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor)'s localization system wraps your component tree in a <code>LocaleProvider</code>\nthat supplies an <code>IntlAccessor</code> to every descendant via <a href=\"/context/\">context</a>.\nYou look up messages, format numbers and dates, and detect RTL layouts — all\nlocale-aware, all reactive.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Purpose</th></tr></thead><tbody><tr><td><code>LocaleProvider</code></td><td>Wraps a subtree with the active locale and resource provider</td></tr><tr><td><code>UseIntl()</code></td><td>Hook returning the <code>IntlAccessor</code> for messages, formatting, and direction</td></tr><tr><td><code>IStringResourceProvider</code></td><td>Pluggable string source (<code>.resw</code>, in-memory, custom)</td></tr><tr><td><code>RtlHelper.IsRtlLocale(tag)</code></td><td>Static check for right-to-left locales</td></tr><tr><td><code>pseudoLocalize: true</code></td><td><code>LocaleProvider</code> flag that accents and pads strings for testing</td></tr></tbody></table>\n<h2 id=\"string-resource-provider\">String Resource Provider<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#string-resource-provider\">#</a></h2>\n<p>You start by implementing <code>IStringResourceProvider</code>. It maps a locale,\nnamespace, and key to a translated string. For production apps, use\n<code>ReswResourceProvider</code> to load <code>.resw</code> files. For demos and tests, an\nin-memory dictionary works:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DemoResourceProvider</span> : <span class=\"hljs-title\">IStringResourceProvider</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, <span class=\"hljs-built_in\">string</span>>> _strings = <span class=\"hljs-keyword\">new</span>()\n    {\n        [<span class=\"hljs-string\">\"en-US\"</span>] = <span class=\"hljs-keyword\">new</span>() {\n            [<span class=\"hljs-string\">\"App.title\"</span>] = <span class=\"hljs-string\">\"My Application\"</span>,\n            [<span class=\"hljs-string\">\"App.greeting\"</span>] = <span class=\"hljs-string\">\"Hello, {name}!\"</span>,\n        },\n        [<span class=\"hljs-string\">\"fr-FR\"</span>] = <span class=\"hljs-keyword\">new</span>() {\n            [<span class=\"hljs-string\">\"App.title\"</span>] = <span class=\"hljs-string\">\"Mon Application\"</span>,\n            [<span class=\"hljs-string\">\"App.greeting\"</span>] = <span class=\"hljs-string\">\"Bonjour, {name} !\"</span>,\n        },\n        [<span class=\"hljs-string\">\"ar-SA\"</span>] = <span class=\"hljs-keyword\">new</span>() {\n            [<span class=\"hljs-string\">\"App.title\"</span>] = <span class=\"hljs-string\">\"\\u062a\\u0637\\u0628\\u064a\\u0642\\u064a\"</span>,\n            [<span class=\"hljs-string\">\"App.greeting\"</span>] = <span class=\"hljs-string\">\"\\u0645\\u0631\\u062d\\u0628\\u0627\\u060c {name}!\"</span>,\n        }\n    };\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? GetString(<span class=\"hljs-built_in\">string</span> locale, <span class=\"hljs-built_in\">string</span> ns, <span class=\"hljs-built_in\">string</span> key)\n    {\n        <span class=\"hljs-keyword\">var</span> fullKey = <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{ns}</span>.<span class=\"hljs-subst\">{key}</span>\"</span>;\n        <span class=\"hljs-keyword\">return</span> _strings.TryGetValue(locale, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> s)\n            &#x26;&#x26; s.TryGetValue(fullKey, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> v) ? v : <span class=\"hljs-literal\">null</span>;\n    }\n}\n</code></pre>\n<p>The <code>GetString</code> method receives the full locale tag (e.g., <code>\"fr-FR\"</code>), the\nnamespace (matching the <code>.resw</code> file name), and the key. Return <code>null</code> for\nmissing keys — the system falls back to the default locale automatically.</p>\n<h2 id=\"localeprovider\">LocaleProvider<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#localeprovider\">#</a></h2>\n<p>Wrap your app tree with <code>LocaleProvider</code>. It takes a locale string, a child\nelement, and an optional resource provider:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LocaleSwitcher</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (localeIndex, setLocaleIndex) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> locales = <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"en-US\"</span>, <span class=\"hljs-string\">\"fr-FR\"</span>, <span class=\"hljs-string\">\"ar-SA\"</span> };\n        <span class=\"hljs-keyword\">var</span> locale = locales[localeIndex];\n        <span class=\"hljs-keyword\">var</span> provider = <span class=\"hljs-keyword\">new</span> DemoResourceProvider();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            ComboBox([<span class=\"hljs-string\">\"English (US)\"</span>, <span class=\"hljs-string\">\"Fran\\u00e7ais\"</span>, <span class=\"hljs-string\">\"\\u0627\\u0644\\u0639\\u0631\\u0628\\u064a\\u0629\"</span>],\n                localeIndex, setLocaleIndex),\n            LocaleProvider(locale,\n                Component&#x3C;LocalizedContent>(),\n                resourceProvider: provider,\n                defaultLocale: <span class=\"hljs-string\">\"en-US\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/localization/locale-provider.png\" alt=\"Locale switcher with LocaleProvider\"></p>\n<p>When the locale changes (here via a <code>ComboBox</code>), <code>LocaleProvider</code> re-renders\nits subtree with a new <code>IntlAccessor</code>. Every component that calls <code>UseIntl()</code>\npicks up the new locale automatically.</p>\n<h2 id=\"message-lookup\">Message Lookup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#message-lookup\">#</a></h2>\n<p>Call <code>UseIntl()</code> in any descendant — it is a <a href=\"/hooks/\">hook</a> — to get the\n<code>IntlAccessor</code>. Use <code>.Message()</code> to look up translated strings by key. Pass\narguments as <code>(\"name\", value)</code> tuples for interpolation — the first item is\nthe placeholder name as a string literal (matching the <code>{name}</code> in the\n<code>.resw</code> pattern), the second is the value. This is the compact, AOT-safe\nshape supported by the tuple-params overload:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LocalizedContent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> intl = UseIntl();\n        <span class=\"hljs-keyword\">var</span> title = intl.Message(<span class=\"hljs-keyword\">new</span> MessageKey(<span class=\"hljs-string\">\"App\"</span>, <span class=\"hljs-string\">\"title\"</span>));\n        <span class=\"hljs-keyword\">var</span> greeting = intl.Message(\n            <span class=\"hljs-keyword\">new</span> MessageKey(<span class=\"hljs-string\">\"App\"</span>, <span class=\"hljs-string\">\"greeting\"</span>),\n            (<span class=\"hljs-string\">\"name\"</span>, <span class=\"hljs-string\">\"Alice\"</span>));\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(title).FontSize(<span class=\"hljs-number\">24</span>).Bold(),\n            TextBlock(greeting).FontSize(<span class=\"hljs-number\">16</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Locale: <span class=\"hljs-subst\">{intl.Locale}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Direction: <span class=\"hljs-subst\">{intl.Direction}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        );\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/localization/useintl-messages.png\" alt=\"Localized messages\"></p>\n<p><code>MessageKey</code> takes a namespace and key. The namespace maps to your <code>.resw</code>\nfile name (e.g., <code>\"App\"</code> for <code>App.resw</code>). The accessor also exposes the\ncurrent <code>Locale</code>, <code>Direction</code>, and <code>IsRtl</code> flag.</p>\n<h2 id=\"formatting-numbers-and-dates\">Formatting Numbers and Dates<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#formatting-numbers-and-dates\">#</a></h2>\n<p><code>IntlAccessor</code> provides locale-aware formatting for numbers, dates, and\nlists. Each method returns a string formatted according to the current\nlocale's rules:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FormattingDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> intl = UseIntl();\n        <span class=\"hljs-keyword\">var</span> price = intl.FormatNumber(<span class=\"hljs-number\">1234.56</span>,\n            <span class=\"hljs-keyword\">new</span> NumberFormatOptions { Style = NumberStyle.Currency });\n        <span class=\"hljs-keyword\">var</span> percent = intl.FormatNumber(<span class=\"hljs-number\">0.875</span>,\n            <span class=\"hljs-keyword\">new</span> NumberFormatOptions { Style = NumberStyle.Percent });\n        <span class=\"hljs-keyword\">var</span> date = intl.FormatDate(DateTimeOffset.Now,\n            <span class=\"hljs-keyword\">new</span> DateFormatOptions { Style = DateStyle.Long });\n        <span class=\"hljs-keyword\">var</span> items = intl.FormatList(\n            <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"Apples\"</span>, <span class=\"hljs-string\">\"Bananas\"</span>, <span class=\"hljs-string\">\"Cherries\"</span> },\n            ListFormatType.Conjunction);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"Formatting\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Price: <span class=\"hljs-subst\">{price}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Rate: <span class=\"hljs-subst\">{percent}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Date: <span class=\"hljs-subst\">{date}</span>\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"List: <span class=\"hljs-subst\">{items}</span>\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/localization/format-numbers-dates.png\" alt=\"Formatted numbers and dates\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Options</th></tr></thead><tbody><tr><td><code>FormatNumber(value, options?)</code></td><td><code>NumberStyle.Default</code>, <code>.Currency</code>, <code>.Percent</code>; fraction digit control</td></tr><tr><td><code>FormatDate(value, options?)</code></td><td><code>DateStyle.Short</code>, <code>.Long</code>, <code>.Full</code>, <code>.Default</code></td></tr><tr><td><code>FormatList(values, type)</code></td><td><code>ListFormatType.Conjunction</code> (\"and\") or <code>.Disjunction</code> (\"or\")</td></tr></tbody></table>\n<p>Formatting follows the locale's conventions — decimal separators, date order,\ncurrency symbols, and list conjunctions all adapt automatically.</p>\n<h2 id=\"rtl-detection\">RTL Detection<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rtl-detection\">#</a></h2>\n<p>Use <code>RtlHelper.IsRtlLocale()</code> to check whether a locale is right-to-left.\nThe <code>IntlAccessor</code> exposes <code>IsRtl</code> and <code>Direction</code> for the active locale:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RtlDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> intl = UseIntl();\n        <span class=\"hljs-keyword\">var</span> locales = <span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"en-US\"</span>, <span class=\"hljs-string\">\"fr-FR\"</span>, <span class=\"hljs-string\">\"ar-SA\"</span>, <span class=\"hljs-string\">\"he-IL\"</span>, <span class=\"hljs-string\">\"ja-JP\"</span> };\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(<span class=\"hljs-string\">\"RTL Detection\"</span>),\n            VStack(<span class=\"hljs-number\">4</span>,\n                locales.Select(loc =>\n                    HStack(<span class=\"hljs-number\">8</span>,\n                        TextBlock(loc).Width(<span class=\"hljs-number\">60</span>),\n                        TextBlock(RtlHelper.IsRtlLocale(loc) ? <span class=\"hljs-string\">\"RTL\"</span> : <span class=\"hljs-string\">\"LTR\"</span>)\n                            .Bold()\n                            .Foreground(RtlHelper.IsRtlLocale(loc)\n                                ? <span class=\"hljs-string\">\"#d13438\"</span> : <span class=\"hljs-string\">\"#107c10\"</span>)\n                    )\n                ).ToArray()\n            ),\n            When(intl.IsRtl, () =>\n                TextBlock(<span class=\"hljs-string\">\"Current layout is right-to-left\"</span>)\n                    .Foreground(<span class=\"hljs-string\">\"#d13438\"</span>).SemiBold())\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/localization/rtl-detection.png\" alt=\"RTL detection\"></p>\n<p>Arabic, Hebrew, Farsi, Urdu, and several other languages are detected as RTL.\nUse <code>intl.Direction</code> to set <code>FlowDirection</code> on your <a href=\"/layout/\">layout</a>\ncontainers so text and UI elements flow correctly.</p>\n<h2 id=\"pseudo-localization\">Pseudo-Localization<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pseudo-localization\">#</a></h2>\n<p>Pseudo-localization replaces characters with accented equivalents and adds\npadding to expose hardcoded strings and truncation issues. Enable it by\nsetting <code>pseudoLocalize: true</code> on <code>LocaleProvider</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PseudoLocDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (pseudo, setPseudo) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> provider = <span class=\"hljs-keyword\">new</span> DemoResourceProvider();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Pseudo-Localization\"</span>),\n            ToggleSwitch(pseudo, setPseudo,\n                header: <span class=\"hljs-string\">\"Enable pseudo-localization\"</span>),\n            LocaleProvider(<span class=\"hljs-string\">\"en-US\"</span>,\n                Func(ctx =>\n                {\n                    <span class=\"hljs-keyword\">var</span> intl = ctx.UseIntl();\n                    <span class=\"hljs-keyword\">var</span> title = intl.Message(<span class=\"hljs-keyword\">new</span> MessageKey(<span class=\"hljs-string\">\"App\"</span>, <span class=\"hljs-string\">\"title\"</span>));\n                    <span class=\"hljs-keyword\">var</span> greeting = intl.Message(\n                        <span class=\"hljs-keyword\">new</span> MessageKey(<span class=\"hljs-string\">\"App\"</span>, <span class=\"hljs-string\">\"greeting\"</span>),\n                        (<span class=\"hljs-string\">\"name\"</span>, <span class=\"hljs-string\">\"World\"</span>));\n                    <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">4</span>,\n                        TextBlock(title).FontSize(<span class=\"hljs-number\">18</span>).Bold(),\n                        TextBlock(greeting));\n                }),\n                resourceProvider: provider,\n                pseudoLocalize: pseudo)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/localization/pseudo-localization.png\" alt=\"Pseudo-localization\"></p>\n<p>Run pseudo-localization during development to catch problems early: strings\nthat were not routed through <code>intl.Message()</code> appear unchanged, making them\neasy to spot. The padded text reveals truncation in fixed-width layouts.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Always wrap with <code>LocaleProvider</code> at the root.</strong> Components that call\n<code>UseIntl()</code> without a provider fall back to the OS locale, but you lose\ncontrol over locale switching and resource loading.</p>\n<p><strong>Use namespaced keys.</strong> Organize your <code>.resw</code> files by feature area\n(<code>\"Settings\"</code>, <code>\"Checkout\"</code>, <code>\"Common\"</code>) so translations stay manageable as\nthe app grows.</p>\n<p><strong>Test with pseudo-localization early.</strong> Turn it on in debug builds. It costs\nnothing at runtime and catches layout issues that only surface in German or\nArabic.</p>\n<p><strong>Format all user-visible numbers and dates.</strong> Never call <code>.ToString()</code>\ndirectly. <code>FormatNumber</code> and <code>FormatDate</code> handle thousands separators, decimal\nsymbols, and date ordering for every locale.</p>\n<p><strong>Check <code>intl.IsRtl</code> for layout-sensitive logic.</strong> If you have directional\nicons (arrows, chevrons) or absolute positioning, flip them when the locale\nis RTL.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — previous topic: label controls, set landmarks, and support screen readers</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — next topic: add transitions and layout animations to your UI</li>\n<li><strong><a href=\"/context/\">Context</a></strong> — understand the provider pattern that <code>LocaleProvider</code> uses under the hood</li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — localize form labels, placeholders, and validation messages</li>\n</ul>",
      "text": "Localization # Microsoft.UI.Reactor (Reactor)'s localization system wraps your component tree in a LocaleProvider that supplies an IntlAccessor to every descendant via context . You look up messages, format numbers and dates, and detect RTL layouts — all locale-aware, all reactive. API Purpose LocaleProvider Wraps a subtree with the active locale and resource provider UseIntl() Hook returning the IntlAccessor for messages, formatting, and direction IStringResourceProvider Pluggable string source ( .resw , in-memory, custom) RtlHelper.IsRtlLocale(tag) Static check for right-to-left locales pseudoLocalize: true LocaleProvider flag that accents and pads strings for testing String Resource Provider # You start by implementing IStringResourceProvider . It maps a locale, namespace, and key to a translated string. For production apps, use ReswResourceProvider to load .resw files. For demos and tests, an in-memory dictionary works: class DemoResourceProvider : IStringResourceProvider { private readonly Dictionary&#x3C; string , Dictionary&#x3C; string , string >> _strings = new () { [ \"en-US\" ] = new () { [ \"App.title\" ] = \"My Application\" , [ \"App.greeting\" ] = \"Hello, {name}!\" , }, [ \"fr-FR\" ] = new () { [ \"App.title\" ] = \"Mon Application\" , [ \"App.greeting\" ] = \"Bonjour, {name} !\" , }, [ \"ar-SA\" ] = new () { [ \"App.title\" ] = \"\\u062a\\u0637\\u0628\\u064a\\u0642\\u064a\" , [ \"App.greeting\" ] = \"\\u0645\\u0631\\u062d\\u0628\\u0627\\u060c {name}!\" , } }; public string ? GetString( string locale, string ns, string key) { var fullKey = $\" {ns} . {key} \" ; return _strings.TryGetValue(locale, out var s) &#x26;&#x26; s.TryGetValue(fullKey, out var v) ? v : null ; } } The GetString method receives the full locale tag (e.g., \"fr-FR\" ), the namespace (matching the .resw file name), and the key. Return null for missing keys — the system falls back to the default locale automatically. LocaleProvider # Wrap your app tree with LocaleProvider . It takes a locale string, a child element, and an optional resource provider: class LocaleSwitcher : Component { public override Element Render () { var (localeIndex, setLocaleIndex) = UseState( 0 ); var locales = new [] { \"en-US\" , \"fr-FR\" , \"ar-SA\" }; var locale = locales[localeIndex]; var provider = new DemoResourceProvider(); return VStack( 16 , ComboBox([ \"English (US)\" , \"Fran\\u00e7ais\" , \"\\u0627\\u0644\\u0639\\u0631\\u0628\\u064a\\u0629\" ], localeIndex, setLocaleIndex), LocaleProvider(locale, Component&#x3C;LocalizedContent>(), resourceProvider: provider, defaultLocale: \"en-US\" ) ).Padding( 24 ); } } When the locale changes (here via a ComboBox ), LocaleProvider re-renders its subtree with a new IntlAccessor . Every component that calls UseIntl() picks up the new locale automatically. Message Lookup # Call UseIntl() in any descendant — it is a hook — to get the IntlAccessor . Use .Message() to look up translated strings by key. Pass arguments as (\"name\", value) tuples for interpolation — the first item is the placeholder name as a string literal (matching the {name} in the .resw pattern), the second is the value. This is the compact, AOT-safe shape supported by the tuple-params overload: class LocalizedContent : Component { public override Element Render () { var intl = UseIntl(); var title = intl.Message( new MessageKey( \"App\" , \"title\" )); var greeting = intl.Message( new MessageKey( \"App\" , \"greeting\" ), ( \"name\" , \"Alice\" )); return VStack( 12 , TextBlock(title).FontSize( 24 ).Bold(), TextBlock(greeting).FontSize( 16 ), TextBlock( $\"Locale: {intl.Locale} \" ).Opacity( 0.6 ), TextBlock( $\"Direction: {intl.Direction} \" ).Opacity( 0.6 ) ); } } MessageKey takes a namespace and key. The namespace maps to your .resw file name (e.g., \"App\" for App.resw ). The accessor also exposes the current Locale , Direction , and IsRtl flag. Formatting Numbers and Dates # IntlAccessor provides locale-aware formatting for numbers, dates, and lists. Each method returns a string formatted according to the current locale's rules: class FormattingDemo : Component { public override Element Render () { var intl = UseIntl(); var price = intl.FormatNumber( 1234.56 , new NumberFormatOptions { Style = NumberStyle.Currency }); var percent = intl.FormatNumber( 0.875 , new NumberFormatOptions { Style = NumberStyle.Percent }); var date = intl.FormatDate(DateTimeOffset.Now, new DateFormatOptions { Style = DateStyle.Long }); var items = intl.FormatList( new [] { \"Apples\" , \"Bananas\" , \"Cherries\" }, ListFormatType.Conjunction); return VStack( 8 , SubHeading( \"Formatting\" ), TextBlock( $\"Price: {price} \" ), TextBlock( $\"Rate: {percent} \" ), TextBlock( $\"Date: {date} \" ), TextBlock( $\"List: {items} \" ) ).Padding( 24 ); } } Method Options FormatNumber(value, options?) NumberStyle.Default , .Currency , .Percent ; fraction digit control FormatDate(value, options?) DateStyle.Short , .Long , .Full , .Default FormatList(values, type) ListFormatType.Conjunction (\"and\") or .Disjunction (\"or\") Formatting follows the locale's conventions — decimal separators, date order, currency symbols, and list conjunctions all adapt automatically. RTL Detection # Use RtlHelper.IsRtlLocale() to check whether a locale is right-to-left. The IntlAccessor exposes IsRtl and Direction for the active locale: class RtlDemo : Component { public override Element Render () { var intl = UseIntl(); var locales = new [] { \"en-US\" , \"fr-FR\" , \"ar-SA\" , \"he-IL\" , \"ja-JP\" }; return VStack( 8 , SubHeading( \"RTL Detection\" ), VStack( 4 , locales.Select(loc => HStack( 8 , TextBlock(loc).Width( 60 ), TextBlock(RtlHelper.IsRtlLocale(loc) ? \"RTL\" : \"LTR\" ) .Bold() .Foreground(RtlHelper.IsRtlLocale(loc) ? \"#d13438\" : \"#107c10\" ) ) ).ToArray() ), When(intl.IsRtl, () => TextBlock( \"Current layout is right-to-left\" ) .Foreground( \"#d13438\" ).SemiBold()) ).Padding( 24 ); } } Arabic, Hebrew, Farsi, Urdu, and several other languages are detected as RTL. Use intl.Direction to set FlowDirection on your layout containers so text and UI elements flow correctly. Pseudo-Localization # Pseudo-localization replaces characters with accented equivalents and adds padding to expose hardcoded strings and truncation issues. Enable it by setting pseudoLocalize: true on LocaleProvider : class PseudoLocDemo : Component { public override Element Render () { var (pseudo, setPseudo) = UseState( false ); var provider = new DemoResourceProvider(); return VStack( 12 , SubHeading( \"Pseudo-Localization\" ), ToggleSwitch(pseudo, setPseudo, header: \"Enable pseudo-localization\" ), LocaleProvider( \"en-US\" , Func(ctx => { var intl = ctx.UseIntl(); var title = intl.Message( new MessageKey( \"App\" , \"title\" )); var greeting = intl.Message( new MessageKey( \"App\" , \"greeting\" ), ( \"name\" , \"World\" )); return VStack( 4 , TextBlock(title).FontSize( 18 ).Bold(), TextBlock(greeting)); }), resourceProvider: provider, pseudoLocalize: pseudo) ).Padding( 24 ); } } Run pseudo-localization during development to catch problems early: strings that were not routed through intl.Message() appear unchanged, making them easy to spot. The padded text reveals truncation in fixed-width layouts. Tips # Always wrap with LocaleProvider at the root. Components that call UseIntl() without a provider fall back to the OS locale, but you lose control over locale switching and resource loading. Use namespaced keys. Organize your .resw files by feature area ( \"Settings\" , \"Checkout\" , \"Common\" ) so translations stay manageable as the app grows. Test with pseudo-localization early. Turn it on in debug builds. It costs nothing at runtime and catches layout issues that only surface in German or Arabic. Format all user-visible numbers and dates. Never call .ToString() directly. FormatNumber and FormatDate handle thousands separators, decimal symbols, and date ordering for every locale. Check intl.IsRtl for layout-sensitive logic. If you have directional icons (arrows, chevrons) or absolute positioning, flip them when the locale is RTL. Next Steps # Accessibility — previous topic: label controls, set landmarks, and support screen readers Animation — next topic: add transitions and layout animations to your UI Context — understand the provider pattern that LocaleProvider uses under the hood Forms and Input — localize form labels, placeholders, and validation messages",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/localization.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/localization.md",
      "headings": [
        {
          "depth": 2,
          "id": "string-resource-provider",
          "text": "String Resource Provider"
        },
        {
          "depth": 2,
          "id": "localeprovider",
          "text": "LocaleProvider"
        },
        {
          "depth": 2,
          "id": "message-lookup",
          "text": "Message Lookup"
        },
        {
          "depth": 2,
          "id": "formatting-numbers-and-dates",
          "text": "Formatting Numbers and Dates"
        },
        {
          "depth": 2,
          "id": "rtl-detection",
          "text": "RTL Detection"
        },
        {
          "depth": 2,
          "id": "pseudo-localization",
          "text": "Pseudo-Localization"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Accessibility",
      "path": "accessibility.md",
      "slug": "/accessibility/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s accessibility surface is two layers: modifiers that map to\nWinUI automation properties (<code>.AutomationName</code>, <code>.HeadingLevel</code>,\n<code>.Landmark</code>, <code>.LiveRegion</code>, <code>.TabIndex</code>, <code>.AccessKey</code>) and hooks that\nadd runtime behavior (<code>UseFocusTrap</code> for modal focus trapping,\n<code>UseAnnounce</code> for live-region announcements, <code>SemanticPanel</code> for\ncustom automation peers that the modifier set can't express). The\nanalyzer set is the third layer — <code>REACTOR_A11Y_001..003</code> catch the\nthree most common omissions (icon-only buttons missing an automation\nname, images missing alt text, form fields missing a label) at\ncompile time. The <code>AccessibilityScanner</code> is the runtime cousin: it\nwalks the post-reconciliation element tree and emits 8 categories of\nWCAG-mapped diagnostics with concrete fix suggestions. Aim for clean\nanalyzer output as you type, clean scanner output before merge, and\nNarrator tab-through as the final manual check. Read <a href=\"#tier-1-modifiers\">Tier 1\nModifiers</a> first — those five modifiers cover most\nproduction cases; everything else on this page is for the\nnon-trivial 20%.</p>\n<h1 id=\"accessibility\">Accessibility<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibility\">#</a></h1>\n<p>Reactor provides accessibility modifiers on every <a href=\"/components/\">component</a>. They map directly to\nWinUI's automation properties, so screen readers, keyboard navigation, and\ntest tools work out of the box. Modifiers are split into two tiers based on\nhow often you need them.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Surface</th><th>Where it lives</th><th>What it does</th></tr></thead><tbody><tr><td>Tier 1 modifiers</td><td><code>.AutomationName</code>, <code>.HeadingLevel</code>, <code>.TabIndex</code>, <code>.AccessKey</code>, <code>.IsTabStop</code></td><td>Inline on every render — labels, headings, keyboard order.</td></tr><tr><td>Tier 2 modifiers</td><td><code>.HelpText</code>, <code>.FullDescription</code>, <code>.AccessibilityHidden</code>, <code>.AccessibilityView</code>, <code>.Landmark</code>, <code>.Required</code>, <code>.LiveRegion</code></td><td>Lazy-allocated; descriptions, landmarks, hidden subtrees, regions.</td></tr><tr><td><code>UseFocusTrap(isActive)</code></td><td>hook</td><td>Returns a <code>FocusTrapHandle</code>; apply with <code>.FocusTrap(handle)</code> on a container.</td></tr><tr><td><code>UseAnnounce()</code></td><td>hook</td><td>Returns an <code>AnnounceHandle</code> with <code>.Region</code> (zero-size element) and <code>.Announce(message, assertive?)</code>.</td></tr><tr><td><code>SemanticPanel</code></td><td>wrapper</td><td>Custom automation peer (role / value / range) for composites.</td></tr><tr><td><code>AccessibilityScanner.Scan(root)</code></td><td>static</td><td>Post-render runtime diagnostic — 8 WCAG checks with fix suggestions.</td></tr><tr><td><code>REACTOR_A11Y_001</code></td><td>analyzer</td><td>Icon-only <code>Button(icon, action)</code> without <code>.AutomationName()</code>.</td></tr><tr><td><code>REACTOR_A11Y_002</code></td><td>analyzer</td><td><code>Image()</code> without <code>.AutomationName()</code> or <code>.AccessibilityHidden()</code>.</td></tr><tr><td><code>REACTOR_A11Y_003</code></td><td>analyzer</td><td>Form field without <code>header:</code> or label modifier.</td></tr></tbody></table>\n<h2 id=\"tier-1-modifiers\">Tier 1 Modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tier-1-modifiers\">#</a></h2>\n<p>Tier 1 modifiers are the ones you use constantly: labels, headings, tab order,\nand keyboard shortcuts. They are applied inline on every render — no lazy\nallocation.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Tier1Demo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Account Settings\"</span>)\n                .FontSize(<span class=\"hljs-number\">24</span>).Bold()\n                .HeadingLevel(AutomationHeadingLevel.Level1),\n            TextBlock(<span class=\"hljs-string\">\"Profile\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).SemiBold()\n                .HeadingLevel(AutomationHeadingLevel.Level2),\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Display name\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Display name\"</span>)\n                .TabIndex(<span class=\"hljs-number\">1</span>)\n                .AccessKey(<span class=\"hljs-string\">\"N\"</span>),\n            Button(<span class=\"hljs-string\">\"Save\"</span>, () => { })\n                .AutomationName(<span class=\"hljs-string\">\"Save profile changes\"</span>)\n                .TabIndex(<span class=\"hljs-number\">2</span>)\n                .AccessKey(<span class=\"hljs-string\">\"S\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/tier1-modifiers.png\" alt=\"Tier 1 accessibility modifiers\"></p>\n<p>Here is what each modifier does:</p>\n<ul>\n<li><strong><code>.HeadingLevel()</code></strong> marks an element as a heading (Level1 through Level9).\nScreen reader users navigate by heading, just like <code>h1</code>--<code>h6</code> in HTML.</li>\n<li><strong><code>.AutomationName()</code></strong> sets the accessible label. Use it when the visible\ntext does not fully describe the control's purpose.</li>\n<li><strong><code>.TabIndex()</code></strong> sets the tab order. Lower values receive focus first.</li>\n<li><strong><code>.AccessKey()</code></strong> assigns an Alt+key shortcut. WinUI shows the key hint\nwhen the user presses Alt.</li>\n<li><strong><code>.IsTabStop()</code></strong> controls whether the element participates in Tab\nnavigation at all.</li>\n</ul>\n<h2 id=\"tier-2-modifiers\">Tier 2 Modifiers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tier-2-modifiers\">#</a></h2>\n<p>Tier 2 modifiers cover supplemental information, landmarks, and visibility\ncontrol. They are lazy-allocated — Reactor only creates the backing storage when\nyou use them, keeping the common case lightweight.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Tier2Demo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Search...\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Search products\"</span>)\n                .HelpText(<span class=\"hljs-string\">\"Type a product name or SKU to filter results\"</span>)\n                .Width(<span class=\"hljs-number\">300</span>),\n            VStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(<span class=\"hljs-string\">\"Revenue by Region\"</span>).Bold(),\n                TextBlock(<span class=\"hljs-string\">\"Bar chart placeholder\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>)\n            ).FullDescription(\n                <span class=\"hljs-string\">\"Bar chart showing Q1 revenue: East $4.2M, \"</span> +\n                <span class=\"hljs-string\">\"West $3.8M, Central $2.1M\"</span>)\n             .Padding(<span class=\"hljs-number\">16</span>).Background(<span class=\"hljs-string\">\"#f5f5f5\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>),\n            TextBlock(<span class=\"hljs-string\">\"Decorative divider\"</span>)\n                .Opacity(<span class=\"hljs-number\">0.2</span>)\n                .AccessibilityHidden()\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/tier2-modifiers.png\" alt=\"Tier 2 accessibility modifiers\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Modifier</th><th>Purpose</th></tr></thead><tbody><tr><td><code>.HelpText()</code></td><td>Extra hint read after the name</td></tr><tr><td><code>.FullDescription()</code></td><td>Extended description for complex visuals</td></tr><tr><td><code>.AccessibilityHidden()</code></td><td>Hide decorative elements from the tree</td></tr><tr><td><code>.AccessibilityView()</code></td><td>Content, Control, or Raw visibility</td></tr><tr><td><code>.Landmark()</code></td><td>Main, Navigation, Search, Form, Custom</td></tr><tr><td><code>.Required()</code></td><td>Announces \"required\" for form fields</td></tr><tr><td><code>.LiveRegion()</code></td><td>Announces dynamic content changes</td></tr></tbody></table>\n<p>The tiered design means you pay zero cost for Tier 2 on elements that only\nneed a label and heading level.</p>\n<h2 id=\"accessible-form\">Accessible Form<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessible-form\">#</a></h2>\n<p>A real <a href=\"/forms/\">form</a> combines Tier 1 and Tier 2 modifiers. Labels, required markers,\nhelp text, landmarks, and tab order work together:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AccessibleFormDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n        <span class=\"hljs-keyword\">var</span> (agree, setAgree) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> valid = !<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(name)\n            &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'@'</span>) &#x26;&#x26; agree;\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Create Account\"</span>).FontSize(<span class=\"hljs-number\">24</span>).Bold()\n                .HeadingLevel(AutomationHeadingLevel.Level1),\n            TextBox(name, setName, header: <span class=\"hljs-string\">\"Full Name\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Full name\"</span>).Required().TabIndex(<span class=\"hljs-number\">1</span>),\n            TextBox(email, setEmail, header: <span class=\"hljs-string\">\"Email\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Email address\"</span>).Required().TabIndex(<span class=\"hljs-number\">2</span>)\n                .HelpText(<span class=\"hljs-string\">\"We'll send a verification link\"</span>),\n            CheckBox(agree, setAgree, label: <span class=\"hljs-string\">\"I accept the terms\"</span>)\n                .TabIndex(<span class=\"hljs-number\">3</span>),\n            Button(<span class=\"hljs-string\">\"Register\"</span>, () => { })\n                .IsEnabled(valid).TabIndex(<span class=\"hljs-number\">4</span>).AccessKey(<span class=\"hljs-string\">\"R\"</span>)\n        ).Landmark(AutomationLandmarkType.Form).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/accessible-form.png\" alt=\"Accessible registration form\"></p>\n<p>The form container uses <code>.Landmark(AutomationLandmarkType.Form)</code> so screen\nreader users can jump directly to it. Each field has <code>.AutomationName()</code> for\nits label, <code>.Required()</code> for required fields, and <code>.TabIndex()</code> for\npredictable keyboard order. The email field adds <code>.HelpText()</code> to explain\nwhat happens after submission.</p>\n<h2 id=\"navigation-landmarks\">Navigation Landmarks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#navigation-landmarks\">#</a></h2>\n<p>Landmarks let screen reader users jump between major page regions. Use them\non your navigation bar, main content area, and search box:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LandmarksDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Home\"</span>, () => { }),\n                Button(<span class=\"hljs-string\">\"Products\"</span>, () => { }),\n                Button(<span class=\"hljs-string\">\"About\"</span>, () => { })\n            ).Landmark(AutomationLandmarkType.Navigation)\n             .AutomationName(<span class=\"hljs-string\">\"Main navigation\"</span>),\n\n            VStack(<span class=\"hljs-number\">12</span>,\n                TextBlock(<span class=\"hljs-string\">\"Dashboard\"</span>)\n                    .FontSize(<span class=\"hljs-number\">20</span>).Bold()\n                    .HeadingLevel(AutomationHeadingLevel.Level1),\n                TextBlock(<span class=\"hljs-string\">\"Welcome back. Here is your overview.\"</span>)\n            ).Landmark(AutomationLandmarkType.Main)\n             .AutomationName(<span class=\"hljs-string\">\"Main content\"</span>),\n\n            TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Search...\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Site search\"</span>)\n                .Landmark(AutomationLandmarkType.Search)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/landmarks.png\" alt=\"Navigation landmarks\"></p>\n<p>WinUI supports five landmark types: <code>Navigation</code>, <code>Main</code>, <code>Search</code>, <code>Form</code>,\nand <code>Custom</code>. Pair each landmark with <code>.AutomationName()</code> so screen readers\nannounce \"Main navigation\" rather than just \"navigation.\"</p>\n<h2 id=\"heading-hierarchy\">Heading Hierarchy<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#heading-hierarchy\">#</a></h2>\n<p>A clear heading structure lets screen reader users skim your page. This pairs\nwell with your <a href=\"/layout/\">layout</a> hierarchy. Use <code>Level1</code> for the page\ntitle, <code>Level2</code> for sections, and <code>Level3</code> for subsections:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HeadingHierarchyDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Application Settings\"</span>)\n                .FontSize(<span class=\"hljs-number\">24</span>).Bold()\n                .HeadingLevel(AutomationHeadingLevel.Level1),\n            TextBlock(<span class=\"hljs-string\">\"Appearance\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).SemiBold()\n                .HeadingLevel(AutomationHeadingLevel.Level2),\n            TextBlock(<span class=\"hljs-string\">\"Choose your preferred theme and font size.\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Notifications\"</span>)\n                .FontSize(<span class=\"hljs-number\">18</span>).SemiBold()\n                .HeadingLevel(AutomationHeadingLevel.Level2),\n            TextBlock(<span class=\"hljs-string\">\"Email Alerts\"</span>)\n                .FontSize(<span class=\"hljs-number\">15</span>).SemiBold()\n                .HeadingLevel(AutomationHeadingLevel.Level3),\n            TextBlock(<span class=\"hljs-string\">\"Configure which emails you receive.\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/heading-hierarchy.png\" alt=\"Heading hierarchy\"></p>\n<p>Keep your heading levels sequential — do not skip from Level1 to Level3.\nScreen readers use the hierarchy to build a page outline, and gaps confuse\nusers.</p>\n<h2 id=\"focus-trapping\">Focus Trapping<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-trapping\">#</a></h2>\n<p><code>UseFocusTrap</code> locks keyboard focus inside a container — essential for modal\ndialogs and flyouts. When active, Tab and Shift+Tab cycle within the\ntrapped subtree and cannot escape:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FocusTrapDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (showModal, setShowModal) = UseState(<span class=\"hljs-literal\">false</span>);\n        <span class=\"hljs-keyword\">var</span> trap = <span class=\"hljs-keyword\">this</span>.UseFocusTrap(showModal);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Focus Trapping\"</span>),\n            Button(<span class=\"hljs-string\">\"Open Modal\"</span>, () => setShowModal(<span class=\"hljs-literal\">true</span>)),\n            When(showModal, () =>\n                Border(\n                    VStack(<span class=\"hljs-number\">12</span>,\n                        TextBlock(<span class=\"hljs-string\">\"Modal Dialog\"</span>).FontSize(<span class=\"hljs-number\">18</span>).Bold(),\n                        TextBlock(<span class=\"hljs-string\">\"Tab/Shift+Tab stays inside this panel.\"</span>),\n                        TextBox(<span class=\"hljs-string\">\"\"</span>, _ => { }, placeholderText: <span class=\"hljs-string\">\"Name\"</span>)\n                            .TabIndex(<span class=\"hljs-number\">0</span>),\n                        Button(<span class=\"hljs-string\">\"Close\"</span>, () => setShowModal(<span class=\"hljs-literal\">false</span>))\n                            .TabIndex(<span class=\"hljs-number\">1</span>)\n                    ).Padding(<span class=\"hljs-number\">24</span>)\n                ).WithBorder(<span class=\"hljs-string\">\"#888\"</span>, <span class=\"hljs-number\">1</span>)\n                 .CornerRadius(<span class=\"hljs-number\">8</span>)\n                 .Background(<span class=\"hljs-string\">\"#ffffff\"</span>)\n                 .FocusTrap(trap)\n            )\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/focus-trap.png\" alt=\"Focus trap in a modal dialog\"></p>\n<p>Create a <code>FocusTrapHandle</code> with <code>UseFocusTrap(isActive)</code>. Apply it to a\ncontainer with <code>.FocusTrap(handle)</code>. When <code>isActive</code> is true, focus wraps\nwithin the subtree; when false, normal tab behavior resumes.</p>\n<p>Use focus trapping for any overlay that should prevent interaction with\nbackground content: modal dialogs, confirmation sheets, and dropdown menus.\nFor a complete dialog pattern that combines <code>UseFocusTrap</code> with\n<a href=\"/dialogs-and-flyouts/\"><code>ContentDialog</code></a>, see the\n<a href=\"/recipes/modal-dialog/\">modal-dialog recipe</a>.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>UseFocusTrap</code> guards its <code>LosingFocus</code> handler against three \"no\nsensible container\" states — <code>IsLoaded == false</code>, <code>Visibility != Visible</code>, and <code>!IsHitTestVisible</code>. Outside those guards, the trap\nkeeps eating focus changes for as long as the handle says\n<code>IsActive</code>. The classic failure: an <code>When(isOpen, () => ...)</code>\nmodal whose <code>setOpen(false)</code> runs first (unmounting the trapped\ncontainer) and <em>then</em> flips <code>isActive</code> to false on the next render —\nbetween the two, <code>Tab</code> from outside the now-removed container still\nmatches the stale handle and <code>args.Cancel = true</code> wedges focus on a\nneighboring page element. Always flip <code>isActive</code> to false in the same\nstate batch that unmounts the container, or apply the trap to an\nelement that stays in the tree across the open/closed transition\n(an always-mounted overlay with <code>.IsVisible(open)</code> rather than\n<code>When(open, ...)</code>).</p>\n</blockquote>\n<h2 id=\"screen-reader-announcements\">Screen Reader Announcements<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#screen-reader-announcements\">#</a></h2>\n<p><code>UseAnnounce</code> sends programmatic announcements to screen readers via live\nregions (WCAG 4.1.3). Use it to notify users of dynamic state changes that\nare not visible in the focus path:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AnnouncementsDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> announce = <span class=\"hljs-keyword\">this</span>.UseAnnounce();\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Screen Reader Announcements\"</span>),\n            Button(<span class=\"hljs-string\">\"Save\"</span>, () =>\n            {\n                setCount(count + <span class=\"hljs-number\">1</span>);\n                announce.Announce(<span class=\"hljs-string\">$\"Document saved (<span class=\"hljs-subst\">{count + <span class=\"hljs-number\">1</span>}</span> times)\"</span>);\n            }),\n            Button(<span class=\"hljs-string\">\"Error (Assertive)\"</span>, () =>\n                announce.Announce(<span class=\"hljs-string\">\"Connection lost!\"</span>, assertive: <span class=\"hljs-literal\">true</span>)),\n            TextBlock(<span class=\"hljs-string\">$\"Saves: <span class=\"hljs-subst\">{count}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>),\n            announce.Region  <span class=\"hljs-comment\">// invisible live region — must be in tree</span>\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/announcements.png\" alt=\"Screen reader announcement\"></p>\n<p>Create an <code>AnnounceHandle</code> with <code>UseAnnounce()</code>. Include <code>announce.Region</code>\nsomewhere in your element tree — it renders an invisible live region. Then\ncall <code>announce.Announce(message)</code> for polite announcements (queued after\ncurrent speech) or <code>announce.Announce(message, assertive: true)</code> to\ninterrupt.</p>\n<p>Calling <code>Announce</code> before <code>Region</code> has been mounted is a silent no-op —\nthe handle holds an internal <code>TextBlock?</code> that the modifier pipeline\nfills in <code>OnMount</code>. If a <a href=\"/effects/\"><code>UseEffect</code></a> with <code>[]</code> deps fires\nan <code>Announce</code> immediately on mount, the live region may not have wired\nup yet; defer the first announcement by a dispatcher tick or trigger\nit from <code>onNavigatedTo</code>.</p>\n<p>Common use cases: form submission confirmations, async operation completions,\nerror messages, and timer-based status updates.</p>\n<h2 id=\"semantic-panel\">Semantic Panel<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#semantic-panel\">#</a></h2>\n<p><code>SemanticPanel</code> wraps a child element to provide custom automation metadata\nthat Reactor components cannot expose directly (since they are C# records, not\nWinUI controls with overridable automation peers):</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SemanticPanelDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (rating, setRating) = UseState(<span class=\"hljs-number\">3</span>);\n\n        <span class=\"hljs-comment\">// .Semantics() wraps the element in a SemanticPanel so</span>\n        <span class=\"hljs-comment\">// screen readers announce it as a slider, not raw buttons</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            SubHeading(<span class=\"hljs-string\">\"Star Rating (Semantic Panel)\"</span>),\n            HStack(<span class=\"hljs-number\">4</span>, Enumerable.Range(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">5</span>).Select(i =>\n                Button(i &#x3C;= rating ? <span class=\"hljs-string\">\"\\u2605\"</span> : <span class=\"hljs-string\">\"\\u2606\"</span>,\n                    () => setRating(i))\n                    .AutomationName(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{i}</span> star<span class=\"hljs-subst\">{(i == <span class=\"hljs-number\">1</span> ? <span class=\"hljs-string\">\"\"</span> : <span class=\"hljs-string\">\"s\"</span>)}</span>\"</span>)\n            ).ToArray())\n            .Semantics(\n                role: <span class=\"hljs-string\">\"slider\"</span>,\n                <span class=\"hljs-keyword\">value</span>: <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{rating}</span> of 5 stars\"</span>,\n                rangeMin: <span class=\"hljs-number\">1</span>, rangeMax: <span class=\"hljs-number\">5</span>, rangeValue: rating),\n            TextBlock(<span class=\"hljs-string\">$\"Current: <span class=\"hljs-subst\">{rating}</span>/5\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/accessibility/semantic-panel.png\" alt=\"Semantic panel for star rating\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Property</th><th>Purpose</th></tr></thead><tbody><tr><td><code>SemanticRole</code></td><td>Automation role (e.g., \"slider\")</td></tr><tr><td><code>SemanticValue</code></td><td>Current value reported to assistive tech</td></tr><tr><td><code>RangeMinimum</code> / <code>RangeMaximum</code></td><td>Numeric range for range-value patterns</td></tr><tr><td><code>RangeValue</code></td><td>Current numeric position in the range</td></tr><tr><td><code>IsReadOnly</code></td><td>Whether the value can be changed</td></tr></tbody></table>\n<p>Use <code>SemanticPanel</code> for custom controls like star ratings, progress\nindicators, or any composite widget that needs a specific automation role\nbeyond what WinUI infers from its visual tree.</p>\n<h2 id=\"accessibility-scanner\">Accessibility Scanner<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibility-scanner\">#</a></h2>\n<p><code>AccessibilityScanner</code> is a post-reconciliation diagnostic tool that walks\nthe element tree and flags common accessibility issues. Run it during\ndevelopment or in CI to catch violations early:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Run AccessibilityScanner during development or CI:</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// var diagnostics = AccessibilityScanner.Scan(rootElement);</span>\n<span class=\"hljs-comment\">// foreach (var d in diagnostics)</span>\n<span class=\"hljs-comment\">// {</span>\n<span class=\"hljs-comment\">//     Console.WriteLine($\"[{d.Severity}] {d.Message}\");</span>\n<span class=\"hljs-comment\">//     Console.WriteLine($\"  WCAG: {d.WcagCriterion}\");</span>\n<span class=\"hljs-comment\">//     Console.WriteLine($\"  Fix: {d.Fix?.Modifier}({d.Fix?.SuggestedValue})\");</span>\n<span class=\"hljs-comment\">// }</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// Export structured JSON for CI integration:</span>\n<span class=\"hljs-comment\">// AccessibilityScanner.ExportJson(diagnostics, \"a11y-report.json\");</span>\n</code></pre>\n<p>The scanner checks for 8 common issues:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Check</th><th>WCAG Criterion</th></tr></thead><tbody><tr><td>Icon buttons without AutomationName</td><td>4.1.2 Name, Role, Value</td></tr><tr><td>Images without alt text</td><td>1.1.1 Non-text Content</td></tr><tr><td>Form fields without labels</td><td>1.3.1 Info and Relationships</td></tr><tr><td>Heading-styled text missing HeadingLevel</td><td>1.3.1 Info and Relationships</td></tr><tr><td>Concrete brushes on interactive controls</td><td>1.4.11 Non-text Contrast</td></tr><tr><td>Missing Main landmark</td><td>1.3.1 Info and Relationships</td></tr><tr><td>TabIndex gaps</td><td>2.4.3 Focus Order</td></tr><tr><td>Unresolved LabeledBy references</td><td>1.3.1 Info and Relationships</td></tr></tbody></table>\n<p>Each <code>A11yDiagnostic</code> includes a <code>Fix</code> suggestion with the exact modifier\nand value to add. Export to JSON with <code>AccessibilityScanner.ExportJson()</code>.</p>\n<h2 id=\"roslyn-analyzers\">Roslyn Analyzers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#roslyn-analyzers\">#</a></h2>\n<p>Reactor ships three compile-time accessibility analyzers that flag violations\nas you type in your IDE:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Diagnostic ID</th><th>Rule</th></tr></thead><tbody><tr><td><code>REACTOR_A11Y_001</code></td><td>Icon-only <code>Button(icon, action)</code> calls without <code>.AutomationName()</code></td></tr><tr><td><code>REACTOR_A11Y_002</code></td><td><code>Image()</code> without <code>.AutomationName()</code> or <code>.AccessibilityHidden()</code></td></tr><tr><td><code>REACTOR_A11Y_003</code></td><td><code>TextBox</code>/<code>NumberBox</code>/<code>PasswordBox</code> without <code>header:</code> arg or label modifier</td></tr></tbody></table>\n<p>These analyzers run automatically when you reference the <code>Reactor.Analyzers</code>\npackage. They complement the runtime <code>AccessibilityScanner</code> by catching the\nmost common violations at compile time. The analyzer architecture itself is\ndocumented in <a href=\"/analyzer-architecture/\">Analyzer Architecture</a> under the\nUnder-the-hood track.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"modal-dialog-with-focus-trap-and-announcement\">Modal dialog with focus trap and announcement<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#modal-dialog-with-focus-trap-and-announcement\">#</a></h3>\n<p>The standard accessible modal combines three primitives: a controlled\n<code>isOpen</code> boolean, a <code>UseFocusTrap</code> keyed on it, and a\n<code>UseAnnounce</code> that announces \"Dialog opened\" on first render and\n\"Dialog closed\" on dismiss. The trap container should stay mounted\nacross the open/closed transition (use <code>.IsVisible(open)</code> not\n<code>When(open, ...)</code>) so focus is released cleanly when <code>isActive</code>\nflips false — see the caveat above. The\n<a href=\"/recipes/modal-dialog/\">modal-dialog recipe</a> wires all three end\nto end against <a href=\"/dialogs-and-flyouts/\"><code>ContentDialog</code></a>.</p>\n<h3 id=\"toast--status-updates\">Toast / status updates<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#toast--status-updates\">#</a></h3>\n<p>Application-wide live announcements (save succeeded, connection\nrestored, error toast) belong on a single <code>AnnounceHandle</code> hoisted to\nthe app root and shared through <a href=\"/context/\">context</a>. Each surface\nthat wants to announce reads it with <code>UseContext</code> and calls\n<code>announce.Announce(message)</code>. The single live region keeps WCAG 4.1.3\nsatisfied without sprinkling regions through the tree, and message\nordering is sequential because the underlying\n<code>RaiseNotificationEvent</code> queues at the automation peer.</p>\n<h3 id=\"custom-control-semantics\">Custom-control semantics<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-control-semantics\">#</a></h3>\n<p>Composite controls (a star rating built from <code>Button</code> children, a\ncustom toggle group, a draggable slider) need a single automation node\nthat says \"this is a slider, the value is 3 of 5\". Wrap the composite\nin <code>SemanticPanel { SemanticRole = \"slider\", RangeMinimum = 1, RangeMaximum = 5, RangeValue = rating }</code>. Mark the child buttons\n<code>.AccessibilityHidden()</code> so screen readers see one node, not five — the\nhidden modifier removes the descendants from the automation tree but\nkeeps them keyboard- and pointer-interactive.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"disabled-submit-with-no-isdisabledfocusable\">Disabled Submit with no <code>.IsDisabledFocusable()</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#disabled-submit-with-no-isdisabledfocusable\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Submit\"</span>, onSubmit).IsEnabled(form.IsValid)\n</code></pre>\n<p>A disabled button is removed from the Tab order. The user fixes the\nlast field, presses Tab to reach Submit, focus skips past the still-\ndisabled button, and they're stranded with no keyboard route. Use\n<code>.IsDisabledFocusable(!form.IsValid)</code> on the button — it keeps the\nbutton keyboard-focusable while presenting as disabled. The\n<a href=\"/forms/#keeping-submit-reachable\">Forms page</a> covers the full\nshape (it composes with <code>.Immediate()</code> on commit-on-blur inputs).</p>\n<h3 id=\"accessibilityhidden-on-a-focusable-element\"><code>AccessibilityHidden</code> on a focusable element<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibilityhidden-on-a-focusable-element\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, onSave)\n    .AccessibilityHidden()  <span class=\"hljs-comment\">// hides the button from screen readers</span>\n</code></pre>\n<p><code>.AccessibilityHidden()</code> only affects the automation tree — keyboard\nfocus and pointer hit-testing still work. The result is a button the\nuser can Tab to and click but a screen reader user cannot discover.\nIf the control should not be exposed to assistive tech, it should\nnot exist for keyboard users either; use <code>.IsEnabled(false)</code> or pull it\nfrom the tree with <code>When(...)</code>. Use <code>.AccessibilityHidden()</code> only on\ndecorative elements that don't accept focus (icons, ornamental\nborders, redundant labels).</p>\n<h3 id=\"heading-levels-skipped-or-out-of-order\">Heading levels skipped or out of order<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#heading-levels-skipped-or-out-of-order\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nTextBlock(<span class=\"hljs-string\">\"Page Title\"</span>).HeadingLevel(Level1),\nTextBlock(<span class=\"hljs-string\">\"Subtle Detail\"</span>).HeadingLevel(Level4)\n</code></pre>\n<p>Screen readers build a page outline by walking heading levels in\norder; gaps confuse navigation (NVDA's \"next heading at level 2\"\nsilently fails). Promote the second heading to <code>Level2</code> or demote\nthe first to match the structure. Treat heading levels like HTML —\n<code>h1</code> once per page, <code>h2</code> for sections, <code>h3</code> for subsections.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Label every interactive control.</strong> If the visible text is sufficient, WinUI\ninfers the name automatically. Use <code>.AutomationName()</code> when it is not — icon\nbuttons, image buttons, and controls with placeholder-only text all need it.</p>\n<p><strong>Use <code>.Required()</code> instead of asterisks.</strong> Screen readers announce \"required\"\nautomatically. Visual asterisks are invisible to assistive technology unless\nyou also set the automation property.</p>\n<p><strong>Test with Narrator.</strong> Press Win+Ctrl+Enter to launch Narrator and tab\nthrough your app. Every control should announce its name, role, and state.</p>\n<p><strong>Keep landmarks minimal.</strong> One <code>Main</code>, one <code>Navigation</code>, and optionally one\n<code>Search</code> per page. Too many landmarks defeat the purpose — users cannot\nquickly jump when every section is a landmark.</p>\n<p><strong>Set <code>.AccessKey()</code> on frequent actions.</strong> Alt+S for Save, Alt+N for New.\nWinUI renders the key tips automatically when the user presses Alt.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/context/\">Context</a></strong> — previous topic: share state across the tree without prop drilling</li>\n<li><strong><a href=\"/localization/\">Localization</a></strong> — next topic: translate strings, format numbers/dates, and support RTL layouts</li>\n<li><strong><a href=\"/forms/\">Forms and Input</a></strong> — build accessible forms with labels, validation, and tab order</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — add landmarks and keyboard-navigable page structure</li>\n<li><strong><a href=\"/styling/\">Styling and Theming</a></strong> — ensure high-contrast themes work with your accessible controls</li>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs and Flyouts</a></strong> — focus traps and ARIA semantics for modal surfaces</li>\n<li><strong><a href=\"/analyzer-architecture/\">Analyzer Architecture</a></strong> — how <code>REACTOR_A11Y_001..003</code> are authored</li>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — accessibility bridging between WinForms and Reactor</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s accessibility surface is two layers: modifiers that map to WinUI automation properties ( .AutomationName , .HeadingLevel , .Landmark , .LiveRegion , .TabIndex , .AccessKey ) and hooks that add runtime behavior ( UseFocusTrap for modal focus trapping, UseAnnounce for live-region announcements, SemanticPanel for custom automation peers that the modifier set can't express). The analyzer set is the third layer — REACTOR_A11Y_001..003 catch the three most common omissions (icon-only buttons missing an automation name, images missing alt text, form fields missing a label) at compile time. The AccessibilityScanner is the runtime cousin: it walks the post-reconciliation element tree and emits 8 categories of WCAG-mapped diagnostics with concrete fix suggestions. Aim for clean analyzer output as you type, clean scanner output before merge, and Narrator tab-through as the final manual check. Read Tier 1 Modifiers first — those five modifiers cover most production cases; everything else on this page is for the non-trivial 20%. Accessibility # Reactor provides accessibility modifiers on every component . They map directly to WinUI's automation properties, so screen readers, keyboard navigation, and test tools work out of the box. Modifiers are split into two tiers based on how often you need them. Reference # Surface Where it lives What it does Tier 1 modifiers .AutomationName , .HeadingLevel , .TabIndex , .AccessKey , .IsTabStop Inline on every render — labels, headings, keyboard order. Tier 2 modifiers .HelpText , .FullDescription , .AccessibilityHidden , .AccessibilityView , .Landmark , .Required , .LiveRegion Lazy-allocated; descriptions, landmarks, hidden subtrees, regions. UseFocusTrap(isActive) hook Returns a FocusTrapHandle ; apply with .FocusTrap(handle) on a container. UseAnnounce() hook Returns an AnnounceHandle with .Region (zero-size element) and .Announce(message, assertive?) . SemanticPanel wrapper Custom automation peer (role / value / range) for composites. AccessibilityScanner.Scan(root) static Post-render runtime diagnostic — 8 WCAG checks with fix suggestions. REACTOR_A11Y_001 analyzer Icon-only Button(icon, action) without .AutomationName() . REACTOR_A11Y_002 analyzer Image() without .AutomationName() or .AccessibilityHidden() . REACTOR_A11Y_003 analyzer Form field without header: or label modifier. Tier 1 Modifiers # Tier 1 modifiers are the ones you use constantly: labels, headings, tab order, and keyboard shortcuts. They are applied inline on every render — no lazy allocation. class Tier1Demo : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Account Settings\" ) .FontSize( 24 ).Bold() .HeadingLevel(AutomationHeadingLevel.Level1), TextBlock( \"Profile\" ) .FontSize( 18 ).SemiBold() .HeadingLevel(AutomationHeadingLevel.Level2), TextBox( \"\" , _ => { }, placeholderText: \"Display name\" ) .AutomationName( \"Display name\" ) .TabIndex( 1 ) .AccessKey( \"N\" ), Button( \"Save\" , () => { }) .AutomationName( \"Save profile changes\" ) .TabIndex( 2 ) .AccessKey( \"S\" ) ).Padding( 24 ); } } Here is what each modifier does: .HeadingLevel() marks an element as a heading (Level1 through Level9). Screen reader users navigate by heading, just like h1 -- h6 in HTML. .AutomationName() sets the accessible label. Use it when the visible text does not fully describe the control's purpose. .TabIndex() sets the tab order. Lower values receive focus first. .AccessKey() assigns an Alt+key shortcut. WinUI shows the key hint when the user presses Alt. .IsTabStop() controls whether the element participates in Tab navigation at all. Tier 2 Modifiers # Tier 2 modifiers cover supplemental information, landmarks, and visibility control. They are lazy-allocated — Reactor only creates the backing storage when you use them, keeping the common case lightweight. class Tier2Demo : Component { public override Element Render () { return VStack( 12 , TextBox( \"\" , _ => { }, placeholderText: \"Search...\" ) .AutomationName( \"Search products\" ) .HelpText( \"Type a product name or SKU to filter results\" ) .Width( 300 ), VStack( 8 , TextBlock( \"Revenue by Region\" ).Bold(), TextBlock( \"Bar chart placeholder\" ).Opacity( 0.5 ) ).FullDescription( \"Bar chart showing Q1 revenue: East $4.2M, \" + \"West $3.8M, Central $2.1M\" ) .Padding( 16 ).Background( \"#f5f5f5\" ).CornerRadius( 8 ), TextBlock( \"Decorative divider\" ) .Opacity( 0.2 ) .AccessibilityHidden() ).Padding( 24 ); } } Modifier Purpose .HelpText() Extra hint read after the name .FullDescription() Extended description for complex visuals .AccessibilityHidden() Hide decorative elements from the tree .AccessibilityView() Content, Control, or Raw visibility .Landmark() Main, Navigation, Search, Form, Custom .Required() Announces \"required\" for form fields .LiveRegion() Announces dynamic content changes The tiered design means you pay zero cost for Tier 2 on elements that only need a label and heading level. Accessible Form # A real form combines Tier 1 and Tier 2 modifiers. Labels, required markers, help text, landmarks, and tab order work together: class AccessibleFormDemo : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); var (email, setEmail) = UseState( \"\" ); var (agree, setAgree) = UseState( false ); var valid = ! string .IsNullOrWhiteSpace(name) &#x26;&#x26; email.Contains( '@' ) &#x26;&#x26; agree; return VStack( 12 , TextBlock( \"Create Account\" ).FontSize( 24 ).Bold() .HeadingLevel(AutomationHeadingLevel.Level1), TextBox(name, setName, header: \"Full Name\" ) .AutomationName( \"Full name\" ).Required().TabIndex( 1 ), TextBox(email, setEmail, header: \"Email\" ) .AutomationName( \"Email address\" ).Required().TabIndex( 2 ) .HelpText( \"We'll send a verification link\" ), CheckBox(agree, setAgree, label: \"I accept the terms\" ) .TabIndex( 3 ), Button( \"Register\" , () => { }) .IsEnabled(valid).TabIndex( 4 ).AccessKey( \"R\" ) ).Landmark(AutomationLandmarkType.Form).Padding( 24 ); } } The form container uses .Landmark(AutomationLandmarkType.Form) so screen reader users can jump directly to it. Each field has .AutomationName() for its label, .Required() for required fields, and .TabIndex() for predictable keyboard order. The email field adds .HelpText() to explain what happens after submission. Navigation Landmarks # Landmarks let screen reader users jump between major page regions. Use them on your navigation bar, main content area, and search box: class LandmarksDemo : Component { public override Element Render () { return VStack( 16 , HStack( 8 , Button( \"Home\" , () => { }), Button( \"Products\" , () => { }), Button( \"About\" , () => { }) ).Landmark(AutomationLandmarkType.Navigation) .AutomationName( \"Main navigation\" ), VStack( 12 , TextBlock( \"Dashboard\" ) .FontSize( 20 ).Bold() .HeadingLevel(AutomationHeadingLevel.Level1), TextBlock( \"Welcome back. Here is your overview.\" ) ).Landmark(AutomationLandmarkType.Main) .AutomationName( \"Main content\" ), TextBox( \"\" , _ => { }, placeholderText: \"Search...\" ) .AutomationName( \"Site search\" ) .Landmark(AutomationLandmarkType.Search) ).Padding( 24 ); } } WinUI supports five landmark types: Navigation , Main , Search , Form , and Custom . Pair each landmark with .AutomationName() so screen readers announce \"Main navigation\" rather than just \"navigation.\" Heading Hierarchy # A clear heading structure lets screen reader users skim your page. This pairs well with your layout hierarchy. Use Level1 for the page title, Level2 for sections, and Level3 for subsections: class HeadingHierarchyDemo : Component { public override Element Render () { return VStack( 12 , TextBlock( \"Application Settings\" ) .FontSize( 24 ).Bold() .HeadingLevel(AutomationHeadingLevel.Level1), TextBlock( \"Appearance\" ) .FontSize( 18 ).SemiBold() .HeadingLevel(AutomationHeadingLevel.Level2), TextBlock( \"Choose your preferred theme and font size.\" ), TextBlock( \"Notifications\" ) .FontSize( 18 ).SemiBold() .HeadingLevel(AutomationHeadingLevel.Level2), TextBlock( \"Email Alerts\" ) .FontSize( 15 ).SemiBold() .HeadingLevel(AutomationHeadingLevel.Level3), TextBlock( \"Configure which emails you receive.\" ) ).Padding( 24 ); } } Keep your heading levels sequential — do not skip from Level1 to Level3. Screen readers use the hierarchy to build a page outline, and gaps confuse users. Focus Trapping # UseFocusTrap locks keyboard focus inside a container — essential for modal dialogs and flyouts. When active, Tab and Shift+Tab cycle within the trapped subtree and cannot escape: class FocusTrapDemo : Component { public override Element Render () { var (showModal, setShowModal) = UseState( false ); var trap = this .UseFocusTrap(showModal); return VStack( 12 , SubHeading( \"Focus Trapping\" ), Button( \"Open Modal\" , () => setShowModal( true )), When(showModal, () => Border( VStack( 12 , TextBlock( \"Modal Dialog\" ).FontSize( 18 ).Bold(), TextBlock( \"Tab/Shift+Tab stays inside this panel.\" ), TextBox( \"\" , _ => { }, placeholderText: \"Name\" ) .TabIndex( 0 ), Button( \"Close\" , () => setShowModal( false )) .TabIndex( 1 ) ).Padding( 24 ) ).WithBorder( \"#888\" , 1 ) .CornerRadius( 8 ) .Background( \"#ffffff\" ) .FocusTrap(trap) ) ).Padding( 24 ); } } Create a FocusTrapHandle with UseFocusTrap(isActive) . Apply it to a container with .FocusTrap(handle) . When isActive is true, focus wraps within the subtree; when false, normal tab behavior resumes. Use focus trapping for any overlay that should prevent interaction with background content: modal dialogs, confirmation sheets, and dropdown menus. For a complete dialog pattern that combines UseFocusTrap with ContentDialog , see the modal-dialog recipe . Caveat: UseFocusTrap guards its LosingFocus handler against three \"no sensible container\" states — IsLoaded == false , Visibility != Visible , and !IsHitTestVisible . Outside those guards, the trap keeps eating focus changes for as long as the handle says IsActive . The classic failure: an When(isOpen, () => ...) modal whose setOpen(false) runs first (unmounting the trapped container) and then flips isActive to false on the next render — between the two, Tab from outside the now-removed container still matches the stale handle and args.Cancel = true wedges focus on a neighboring page element. Always flip isActive to false in the same state batch that unmounts the container, or apply the trap to an element that stays in the tree across the open/closed transition (an always-mounted overlay with .IsVisible(open) rather than When(open, ...) ). Screen Reader Announcements # UseAnnounce sends programmatic announcements to screen readers via live regions (WCAG 4.1.3). Use it to notify users of dynamic state changes that are not visible in the focus path: class AnnouncementsDemo : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); var announce = this .UseAnnounce(); return VStack( 12 , SubHeading( \"Screen Reader Announcements\" ), Button( \"Save\" , () => { setCount(count + 1 ); announce.Announce( $\"Document saved ( {count + 1 } times)\" ); }), Button( \"Error (Assertive)\" , () => announce.Announce( \"Connection lost!\" , assertive: true )), TextBlock( $\"Saves: {count} \" ).Opacity( 0.6 ), announce.Region // invisible live region — must be in tree ).Padding( 24 ); } } Create an AnnounceHandle with UseAnnounce() . Include announce.Region somewhere in your element tree — it renders an invisible live region. Then call announce.Announce(message) for polite announcements (queued after current speech) or announce.Announce(message, assertive: true) to interrupt. Calling Announce before Region has been mounted is a silent no-op — the handle holds an internal TextBlock? that the modifier pipeline fills in OnMount . If a UseEffect with [] deps fires an Announce immediately on mount, the live region may not have wired up yet; defer the first announcement by a dispatcher tick or trigger it from onNavigatedTo . Common use cases: form submission confirmations, async operation completions, error messages, and timer-based status updates. Semantic Panel # SemanticPanel wraps a child element to provide custom automation metadata that Reactor components cannot expose directly (since they are C# records, not WinUI controls with overridable automation peers): class SemanticPanelDemo : Component { public override Element Render () { var (rating, setRating) = UseState( 3 ); // .Semantics() wraps the element in a SemanticPanel so // screen readers announce it as a slider, not raw buttons return VStack( 12 , SubHeading( \"Star Rating (Semantic Panel)\" ), HStack( 4 , Enumerable.Range( 1 , 5 ).Select(i => Button(i &#x3C;= rating ? \"\\u2605\" : \"\\u2606\" , () => setRating(i)) .AutomationName( $\" {i} star {(i == 1 ? \"\" : \"s\" )} \" ) ).ToArray()) .Semantics( role: \"slider\" , value : $\" {rating} of 5 stars\" , rangeMin: 1 , rangeMax: 5 , rangeValue: rating), TextBlock( $\"Current: {rating} /5\" ).Opacity( 0.6 ) ).Padding( 24 ); } } Property Purpose SemanticRole Automation role (e.g., \"slider\") SemanticValue Current value reported to assistive tech RangeMinimum / RangeMaximum Numeric range for range-value patterns RangeValue Current numeric position in the range IsReadOnly Whether the value can be changed Use SemanticPanel for custom controls like star ratings, progress indicators, or any composite widget that needs a specific automation role beyond what WinUI infers from its visual tree. Accessibility Scanner # AccessibilityScanner is a post-reconciliation diagnostic tool that walks the element tree and flags common accessibility issues. Run it during development or in CI to catch violations early: // Run AccessibilityScanner during development or CI: // // var diagnostics = AccessibilityScanner.Scan(rootElement); // foreach (var d in diagnostics) // { // Console.WriteLine($\"[{d.Severity}] {d.Message}\"); // Console.WriteLine($\" WCAG: {d.WcagCriterion}\"); // Console.WriteLine($\" Fix: {d.Fix?.Modifier}({d.Fix?.SuggestedValue})\"); // } // // Export structured JSON for CI integration: // AccessibilityScanner.ExportJson(diagnostics, \"a11y-report.json\"); The scanner checks for 8 common issues: Check WCAG Criterion Icon buttons without AutomationName 4.1.2 Name, Role, Value Images without alt text 1.1.1 Non-text Content Form fields without labels 1.3.1 Info and Relationships Heading-styled text missing HeadingLevel 1.3.1 Info and Relationships Concrete brushes on interactive controls 1.4.11 Non-text Contrast Missing Main landmark 1.3.1 Info and Relationships TabIndex gaps 2.4.3 Focus Order Unresolved LabeledBy references 1.3.1 Info and Relationships Each A11yDiagnostic includes a Fix suggestion with the exact modifier and value to add. Export to JSON with AccessibilityScanner.ExportJson() . Roslyn Analyzers # Reactor ships three compile-time accessibility analyzers that flag violations as you type in your IDE: Diagnostic ID Rule REACTOR_A11Y_001 Icon-only Button(icon, action) calls without .AutomationName() REACTOR_A11Y_002 Image() without .AutomationName() or .AccessibilityHidden() REACTOR_A11Y_003 TextBox / NumberBox / PasswordBox without header: arg or label modifier These analyzers run automatically when you reference the Reactor.Analyzers package. They complement the runtime AccessibilityScanner by catching the most common violations at compile time. The analyzer architecture itself is documented in Analyzer Architecture under the Under-the-hood track. Patterns # Modal dialog with focus trap and announcement # The standard accessible modal combines three primitives: a controlled isOpen boolean, a UseFocusTrap keyed on it, and a UseAnnounce that announces \"Dialog opened\" on first render and \"Dialog closed\" on dismiss. The trap container should stay mounted across the open/closed transition (use .IsVisible(open) not When(open, ...) ) so focus is released cleanly when isActive flips false — see the caveat above. The modal-dialog recipe wires all three end to end against ContentDialog . Toast / status updates # Application-wide live announcements (save succeeded, connection restored, error toast) belong on a single AnnounceHandle hoisted to the app root and shared through context . Each surface that wants to announce reads it with UseContext and calls announce.Announce(message) . The single live region keeps WCAG 4.1.3 satisfied without sprinkling regions through the tree, and message ordering is sequential because the underlying RaiseNotificationEvent queues at the automation peer. Custom-control semantics # Composite controls (a star rating built from Button children, a custom toggle group, a draggable slider) need a single automation node that says \"this is a slider, the value is 3 of 5\". Wrap the composite in SemanticPanel { SemanticRole = \"slider\", RangeMinimum = 1, RangeMaximum = 5, RangeValue = rating } . Mark the child buttons .AccessibilityHidden() so screen readers see one node, not five — the hidden modifier removes the descendants from the automation tree but keeps them keyboard- and pointer-interactive. Common Mistakes # Disabled Submit with no .IsDisabledFocusable() # // Don't: Button( \"Submit\" , onSubmit).IsEnabled(form.IsValid) A disabled button is removed from the Tab order. The user fixes the last field, presses Tab to reach Submit, focus skips past the still- disabled button, and they're stranded with no keyboard route. Use .IsDisabledFocusable(!form.IsValid) on the button — it keeps the button keyboard-focusable while presenting as disabled. The Forms page covers the full shape (it composes with .Immediate() on commit-on-blur inputs). AccessibilityHidden on a focusable element # // Don't: Button( \"Save\" , onSave) .AccessibilityHidden() // hides the button from screen readers .AccessibilityHidden() only affects the automation tree — keyboard focus and pointer hit-testing still work. The result is a button the user can Tab to and click but a screen reader user cannot discover. If the control should not be exposed to assistive tech, it should not exist for keyboard users either; use .IsEnabled(false) or pull it from the tree with When(...) . Use .AccessibilityHidden() only on decorative elements that don't accept focus (icons, ornamental borders, redundant labels). Heading levels skipped or out of order # // Don't: TextBlock( \"Page Title\" ).HeadingLevel(Level1), TextBlock( \"Subtle Detail\" ).HeadingLevel(Level4) Screen readers build a page outline by walking heading levels in order; gaps confuse navigation (NVDA's \"next heading at level 2\" silently fails). Promote the second heading to Level2 or demote the first to match the structure. Treat heading levels like HTML — h1 once per page, h2 for sections, h3 for subsections. Tips # Label every interactive control. If the visible text is sufficient, WinUI infers the name automatically. Use .AutomationName() when it is not — icon buttons, image buttons, and controls with placeholder-only text all need it. Use .Required() instead of asterisks. Screen readers announce \"required\" automatically. Visual asterisks are invisible to assistive technology unless you also set the automation property. Test with Narrator. Press Win+Ctrl+Enter to launch Narrator and tab through your app. Every control should announce its name, role, and state. Keep landmarks minimal. One Main , one Navigation , and optionally one Search per page. Too many landmarks defeat the purpose — users cannot quickly jump when every section is a landmark. Set .AccessKey() on frequent actions. Alt+S for Save, Alt+N for New. WinUI renders the key tips automatically when the user presses Alt. Next Steps # Context — previous topic: share state across the tree without prop drilling Localization — next topic: translate strings, format numbers/dates, and support RTL layouts Forms and Input — build accessible forms with labels, validation, and tab order Navigation — add landmarks and keyboard-navigable page structure Styling and Theming — ensure high-contrast themes work with your accessible controls Dialogs and Flyouts — focus traps and ARIA semantics for modal surfaces Analyzer Architecture — how REACTOR_A11Y_001..003 are authored WinForms Interop — accessibility bridging between WinForms and Reactor",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/accessibility.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/accessibility.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "tier-1-modifiers",
          "text": "Tier 1 Modifiers"
        },
        {
          "depth": 2,
          "id": "tier-2-modifiers",
          "text": "Tier 2 Modifiers"
        },
        {
          "depth": 2,
          "id": "accessible-form",
          "text": "Accessible Form"
        },
        {
          "depth": 2,
          "id": "navigation-landmarks",
          "text": "Navigation Landmarks"
        },
        {
          "depth": 2,
          "id": "heading-hierarchy",
          "text": "Heading Hierarchy"
        },
        {
          "depth": 2,
          "id": "focus-trapping",
          "text": "Focus Trapping"
        },
        {
          "depth": 2,
          "id": "screen-reader-announcements",
          "text": "Screen Reader Announcements"
        },
        {
          "depth": 2,
          "id": "semantic-panel",
          "text": "Semantic Panel"
        },
        {
          "depth": 2,
          "id": "accessibility-scanner",
          "text": "Accessibility Scanner"
        },
        {
          "depth": 2,
          "id": "roslyn-analyzers",
          "text": "Roslyn Analyzers"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "modal-dialog-with-focus-trap-and-announcement",
          "text": "Modal dialog with focus trap and announcement"
        },
        {
          "depth": 3,
          "id": "toast--status-updates",
          "text": "Toast / status updates"
        },
        {
          "depth": 3,
          "id": "custom-control-semantics",
          "text": "Custom-control semantics"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "disabled-submit-with-no-isdisabledfocusable",
          "text": "Disabled Submit with no .IsDisabledFocusable()"
        },
        {
          "depth": 3,
          "id": "accessibilityhidden-on-a-focusable-element",
          "text": "AccessibilityHidden on a focusable element"
        },
        {
          "depth": 3,
          "id": "heading-levels-skipped-or-out-of-order",
          "text": "Heading levels skipped or out of order"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Recipes",
      "path": "recipes/index.md",
      "slug": "/recipes/",
      "html": "<h1 id=\"recipes\">Recipes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipes\">#</a></h1>\n<p>A recipe is a small composition of Microsoft.UI.Reactor (Reactor) primitives that solves a\nreal UI problem — login, master-detail, settings, modal confirmation,\nsearch-with-suggestions. The recipes here are not exhaustive apps;\neach is a single screen showing the pattern, and each ships a tiny\ndoc app you can clone and adapt.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">RecipesIndexApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">12</span>,\n        Heading(<span class=\"hljs-string\">\"Recipes\"</span>),\n        TextBlock(<span class=\"hljs-string\">\"Real-world compositions made of Reactor primitives.\"</span>)\n            .Opacity(<span class=\"hljs-number\">0.7</span>),\n        HStack(<span class=\"hljs-number\">8</span>,\n            Tile(<span class=\"hljs-string\">\"Login\"</span>, <span class=\"hljs-string\">\"Validation + async submit\"</span>),\n            Tile(<span class=\"hljs-string\">\"Master-detail\"</span>, <span class=\"hljs-string\">\"Selection-driven layout\"</span>),\n            Tile(<span class=\"hljs-string\">\"Settings\"</span>, <span class=\"hljs-string\">\"Persisted preferences\"</span>)\n        )\n    ).Padding(<span class=\"hljs-number\">20</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipes-index/gallery.png\" alt=\"Recipes gallery preview\"></p>\n<p>The gallery uses the same primitives every recipe page does — a\n<code>VStack</code> for the column layout, <code>TextBlock</code> for descriptions, <code>HStack</code>\nfor the tile row. The tile helper is a private static method, not a\ncomponent, so it has no hook scope:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">Tile</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> title, <span class=\"hljs-built_in\">string</span> sub</span>)</span> => VStack(<span class=\"hljs-number\">4</span>,\n    TextBlock(title).Bold(),\n    TextBlock(sub).Opacity(<span class=\"hljs-number\">0.6</span>)\n).Padding(<span class=\"hljs-number\">12</span>);\n</code></pre>\n<p>Every page in this folder follows the same shape:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Every recipe page in this folder pulls a tiny dedicated doc app under</span>\n<span class=\"hljs-comment\">// docs/_pipeline/apps/recipe-&#x3C;name>/. The recipe template renders three</span>\n<span class=\"hljs-comment\">// snippet markers (state / shape / render) plus one screenshot for the</span>\n<span class=\"hljs-comment\">// gallery thumbnail above.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">GalleryShape</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => TextBlock(<span class=\"hljs-string\">\"see docs/_pipeline/apps/recipe-*\"</span>);\n}\n</code></pre>\n<h2 id=\"gallery\">Gallery<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#gallery\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Recipe</th><th>What it shows</th></tr></thead><tbody><tr><td><a href=\"/recipes/login/\">Login</a></td><td>Validation, async submit with optimistic UI, error display.</td></tr><tr><td><a href=\"/recipes/master-detail/\">Master-detail</a></td><td>Two-pane selection-driven layout from a list and a record.</td></tr><tr><td><a href=\"/recipes/settings-page/\">Settings page</a></td><td>Per-key <code>UsePersisted</code> for <code>Toggle</code> / <code>ComboBox</code> / <code>Slider</code>.</td></tr><tr><td><a href=\"/recipes/modal-dialog/\">Modal dialog</a></td><td>Confirmation pattern with scrim and conditional render.</td></tr><tr><td><a href=\"/recipes/search-with-suggestions/\">Search with suggestions</a></td><td><code>UseMemo</code>-debounced suggestion list against a static catalog.</td></tr><tr><td>Paginated list <em>(Phase 2.5)</em></td><td>Coming soon — page cursor + lazy-load.</td></tr><tr><td>Multi-step form <em>(Phase 2.5)</em></td><td>Coming soon — wizard with per-step validation.</td></tr><tr><td>Command palette <em>(Phase 2.5)</em></td><td>Coming soon — Ctrl+K palette wired through <code>commanding</code>.</td></tr><tr><td>Drag-reorder <em>(Phase 2.5)</em></td><td>Coming soon — drag-handle list with optimistic reorder.</td></tr></tbody></table>\n<p>The \"Phase 2.5\" rows are stubs that ship in this docset so the gallery\nshape is visible and discoverable; the full recipes land in a follow-up.</p>\n<h2 id=\"how-to-read-a-recipe\">How to read a recipe<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#how-to-read-a-recipe\">#</a></h2>\n<p>Every recipe has the same shape:</p>\n<ol>\n<li><strong>A snippet of the recipe's working code</strong>, pulled from a real\ndoc app under <code>docs/_pipeline/apps/recipe-&#x3C;name>/</code>.</li>\n<li><strong>A screenshot of the recipe running</strong>, captured by the\ndoc-pipeline harness.</li>\n<li><strong>A walkthrough paragraph or two</strong> naming the primitives the\nrecipe combines and the design decisions that hold the pattern up.</li>\n</ol>\n<p>The recipes prefer composition of existing factories — no custom\n<code>Component</code> per recipe. If you want the recipe in your app, copy the\nsnippet and replace the catalog data with yours.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Primitive</th><th>Used in</th></tr></thead><tbody><tr><td><code>UseState</code></td><td>Every recipe.</td></tr><tr><td><code>UsePersisted</code></td><td><a href=\"/recipes/settings-page/\">Settings</a>.</td></tr><tr><td><code>UseMemo</code></td><td><a href=\"/recipes/search-with-suggestions/\">Search</a>.</td></tr><tr><td><code>UseEffect</code> + <code>Task</code></td><td><a href=\"/recipes/login/\">Login</a> (async submit).</td></tr><tr><td>Conditional render</td><td><a href=\"/recipes/modal-dialog/\">Modal dialog</a>.</td></tr><tr><td>Two-pane HStack</td><td><a href=\"/recipes/master-detail/\">Master-detail</a>.</td></tr></tbody></table>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Reach for a recipe before custom code.</strong> Most \"I need a settings\npage\" or \"we need a login form\" needs are met by one of these\npatterns. The recipe is the composition; the cost of inventing your\nown is the cost of debugging it.</p>\n<p><strong>Recipes are starting points, not products.</strong> Drop the snippet into\nyour app and adapt it — the data, the styling tokens, the validation\nrules. The shape of the composition is the value here.</p>\n<p><strong>Search the controls catalog before reaching for a recipe.</strong> A\nproblem solved by a single control (<a href=\"/forms/\">forms</a>,\n<a href=\"/data-system/\">data-system</a>) doesn't need a recipe; recipes\nexist for shapes that span multiple controls and hooks.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/controls/\">Controls</a></strong> — Previous: the catalog of factories\nthe recipes compose.</li>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — Forms-heavy recipes start here.</li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — Behind the Settings recipe.</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Backs the (Phase 2.5) Command-palette recipe.</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — Recipes that span multiple\nscreens lean on this.</li>\n</ul>",
      "text": "Recipes # A recipe is a small composition of Microsoft.UI.Reactor (Reactor) primitives that solves a real UI problem — login, master-detail, settings, modal confirmation, search-with-suggestions. The recipes here are not exhaustive apps; each is a single screen showing the pattern, and each ships a tiny doc app you can clone and adapt. class RecipesIndexApp : Component { public override Element Render () => VStack( 12 , Heading( \"Recipes\" ), TextBlock( \"Real-world compositions made of Reactor primitives.\" ) .Opacity( 0.7 ), HStack( 8 , Tile( \"Login\" , \"Validation + async submit\" ), Tile( \"Master-detail\" , \"Selection-driven layout\" ), Tile( \"Settings\" , \"Persisted preferences\" ) ) ).Padding( 20 ); The gallery uses the same primitives every recipe page does — a VStack for the column layout, TextBlock for descriptions, HStack for the tile row. The tile helper is a private static method, not a component, so it has no hook scope: private static Element Tile ( string title, string sub ) => VStack( 4 , TextBlock(title).Bold(), TextBlock(sub).Opacity( 0.6 ) ).Padding( 12 ); Every page in this folder follows the same shape: // Every recipe page in this folder pulls a tiny dedicated doc app under // docs/_pipeline/apps/recipe-&#x3C;name>/. The recipe template renders three // snippet markers (state / shape / render) plus one screenshot for the // gallery thumbnail above. class GalleryShape : Component { public override Element Render () => TextBlock( \"see docs/_pipeline/apps/recipe-*\" ); } Gallery # Recipe What it shows Login Validation, async submit with optimistic UI, error display. Master-detail Two-pane selection-driven layout from a list and a record. Settings page Per-key UsePersisted for Toggle / ComboBox / Slider . Modal dialog Confirmation pattern with scrim and conditional render. Search with suggestions UseMemo -debounced suggestion list against a static catalog. Paginated list (Phase 2.5) Coming soon — page cursor + lazy-load. Multi-step form (Phase 2.5) Coming soon — wizard with per-step validation. Command palette (Phase 2.5) Coming soon — Ctrl+K palette wired through commanding . Drag-reorder (Phase 2.5) Coming soon — drag-handle list with optimistic reorder. The \"Phase 2.5\" rows are stubs that ship in this docset so the gallery shape is visible and discoverable; the full recipes land in a follow-up. How to read a recipe # Every recipe has the same shape: A snippet of the recipe's working code , pulled from a real doc app under docs/_pipeline/apps/recipe-&#x3C;name>/ . A screenshot of the recipe running , captured by the doc-pipeline harness. A walkthrough paragraph or two naming the primitives the recipe combines and the design decisions that hold the pattern up. The recipes prefer composition of existing factories — no custom Component per recipe. If you want the recipe in your app, copy the snippet and replace the catalog data with yours. Reference # Primitive Used in UseState Every recipe. UsePersisted Settings . UseMemo Search . UseEffect + Task Login (async submit). Conditional render Modal dialog . Two-pane HStack Master-detail . Tips # Reach for a recipe before custom code. Most \"I need a settings page\" or \"we need a login form\" needs are met by one of these patterns. The recipe is the composition; the cost of inventing your own is the cost of debugging it. Recipes are starting points, not products. Drop the snippet into your app and adapt it — the data, the styling tokens, the validation rules. The shape of the composition is the value here. Search the controls catalog before reaching for a recipe. A problem solved by a single control ( forms , data-system ) doesn't need a recipe; recipes exist for shapes that span multiple controls and hooks. Next Steps # Controls — Previous: the catalog of factories the recipes compose. Forms — Forms-heavy recipes start here. Persistence — Behind the Settings recipe. Commanding — Backs the (Phase 2.5) Command-palette recipe. Navigation — Recipes that span multiple screens lean on this.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/index.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/index.md",
      "headings": [
        {
          "depth": 2,
          "id": "gallery",
          "text": "Gallery"
        },
        {
          "depth": 2,
          "id": "how-to-read-a-recipe",
          "text": "How to read a recipe"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Command Palette",
      "path": "recipes/command-palette.md",
      "slug": "/recipes/command-palette/",
      "html": "<h1 id=\"recipe-command-palette\">Recipe: Command Palette<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-command-palette\">#</a></h1>\n<p>A command palette is a filter-as-you-type list with a keyboard\naccelerator to open and Enter to execute. In Microsoft.UI.Reactor (Reactor) it's three\n<code>UseState</code> hooks, a static catalog of <a href=\"/commanding/\"><code>Command</code></a>\nrecords, and an <code>OnPreviewKeyDown</code> handler — no menubar, no modal\nmanager, no view model. The palette is just another conditional\noverlay, layered over the page the same way the\n<a href=\"/recipes/modal-dialog/\">modal-dialog recipe</a> layers its confirmation sheet.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Open / query / selection state</td><td><code>UseState&#x3C;bool></code> / <code>UseState&#x3C;string></code> / <code>UseState&#x3C;int></code></td></tr><tr><td>Command catalog</td><td><a href=\"/commanding/\"><code>Command</code></a> records — same shape used by <code>Button</code>, <code>MenuItem</code>, <code>AppBarButton</code></td></tr><tr><td>Filter</td><td>LINQ <code>Where(...Contains(query, OrdinalIgnoreCase))</code></td></tr><tr><td>Open accelerator</td><td><a href=\"/input-and-gestures/\"><code>.OnKeyDown</code></a> on the root surface</td></tr><tr><td>List navigation</td><td><a href=\"/input-and-gestures/\"><code>.OnPreviewKeyDown</code></a> on the palette container</td></tr><tr><td>Overlay composition</td><td><code>Group(page, palette)</code> — same shape as <a href=\"/recipes/modal-dialog/\">Recipe: Modal dialog</a></td></tr></tbody></table>\n<h3 id=\"command-catalog\">Command catalog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#command-catalog\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The catalog is a static array of Reactor Command records. The same</span>\n<span class=\"hljs-comment\">// record could just as well be bound to a Button or a MenuItem — the</span>\n<span class=\"hljs-comment\">// palette is one more surface that consumes it.</span>\n<span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Command[] Catalog = <span class=\"hljs-keyword\">new</span>[]\n{\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"File: New\"</span>,        Execute = () => Log(<span class=\"hljs-string\">\"new\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"File: Open…\"</span>,      Execute = () => Log(<span class=\"hljs-string\">\"open\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"File: Save\"</span>,       Execute = () => Log(<span class=\"hljs-string\">\"save\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Edit: Find\"</span>,       Execute = () => Log(<span class=\"hljs-string\">\"find\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Edit: Replace\"</span>,    Execute = () => Log(<span class=\"hljs-string\">\"replace\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"View: Toggle Theme\"</span>, Execute = () => Log(<span class=\"hljs-string\">\"theme\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"View: Zen Mode\"</span>,   Execute = () => Log(<span class=\"hljs-string\">\"zen\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Go: Go to Line…\"</span>,  Execute = () => Log(<span class=\"hljs-string\">\"goto-line\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Go: Go to Symbol…\"</span>,Execute = () => Log(<span class=\"hljs-string\">\"goto-symbol\"</span>) },\n    <span class=\"hljs-keyword\">new</span> Command { Label = <span class=\"hljs-string\">\"Help: About\"</span>,      Execute = () => Log(<span class=\"hljs-string\">\"about\"</span>) },\n};\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Log</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> id</span>)</span> { <span class=\"hljs-comment\">/* hook to telemetry in a real app */</span> }\n</code></pre>\n<p>A <code>Command</code> bundles a label with an action and metadata\n(<a href=\"/commanding/\">commanding</a> covers the full surface — icon,\naccelerator, <code>CanExecute</code>, async tracking). The palette only needs\n<code>Label</code> and <code>Execute</code>; the rest is there when you want to share the\nsame record with a toolbar button or a context menu entry.</p>\n<h3 id=\"state\">State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Three pieces of state run the palette: whether it's open, the</span>\n<span class=\"hljs-comment\">// typed query, and the highlighted row in the filtered list.</span>\n<span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n<span class=\"hljs-keyword\">var</span> (query, setQuery) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> (index, setIndex) = UseState(<span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">var</span> (last, setLast) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n</code></pre>\n<p>Three hooks own the palette: <code>open</code> toggles the overlay, <code>query</code>\ndrives the filter, and <code>index</code> is the highlighted row. A fourth\n<code>last</code> hook just echoes the most recently executed command for the\ndemo — it's not part of the palette pattern.</p>\n<h3 id=\"filter\">Filter<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#filter\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Re-derive the filtered list on every render. The catalog is</span>\n<span class=\"hljs-comment\">// small; a real palette with hundreds of commands would key this</span>\n<span class=\"hljs-comment\">// through UseMemo on `query`.</span>\n<span class=\"hljs-keyword\">var</span> matches = <span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(query)\n    ? Catalog\n    : Catalog.Where(c => c.Label.Contains(query,\n        StringComparison.OrdinalIgnoreCase)).ToArray();\n<span class=\"hljs-comment\">// Clamp the selection so it never points off the end of the list.</span>\n<span class=\"hljs-keyword\">var</span> safeIndex = matches.Length == <span class=\"hljs-number\">0</span>\n    ? <span class=\"hljs-number\">0</span>\n    : Math.Clamp(index, <span class=\"hljs-number\">0</span>, matches.Length - <span class=\"hljs-number\">1</span>);\n</code></pre>\n<p>The filter runs on every render. For a small catalog that's the\nright trade — no <code>UseMemo</code> ceremony, and the filter sees the latest\n<code>query</code> without an extra dependency array. Promote to\n<a href=\"/hooks/\"><code>UseMemo</code></a> once the catalog crosses a few hundred\nentries or the predicate grows past <code>Contains</code>. The <code>safeIndex</code>\nclamp keeps the highlight valid when typing shortens the match list\nout from under the old selection.</p>\n<h3 id=\"keyboard-handler\">Keyboard handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyboard-handler\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Esc closes; Up / Down move the selection; Enter invokes the</span>\n<span class=\"hljs-comment\">// highlighted command. The OnPreviewKeyDown handler intercepts</span>\n<span class=\"hljs-comment\">// before the TextBox gets the keystroke, so arrow keys move</span>\n<span class=\"hljs-comment\">// the list instead of the caret.</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">OnPaletteKey</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span> _, Microsoft.UI.Xaml.Input.KeyRoutedEventArgs e</span>)</span>\n{\n    <span class=\"hljs-keyword\">switch</span> (e.Key)\n    {\n        <span class=\"hljs-keyword\">case</span> VirtualKey.Escape:\n            setOpen(<span class=\"hljs-literal\">false</span>);\n            e.Handled = <span class=\"hljs-literal\">true</span>;\n            <span class=\"hljs-keyword\">break</span>;\n        <span class=\"hljs-keyword\">case</span> VirtualKey.Down:\n            <span class=\"hljs-keyword\">if</span> (matches.Length > <span class=\"hljs-number\">0</span>)\n                setIndex((safeIndex + <span class=\"hljs-number\">1</span>) % matches.Length);\n            e.Handled = <span class=\"hljs-literal\">true</span>;\n            <span class=\"hljs-keyword\">break</span>;\n        <span class=\"hljs-keyword\">case</span> VirtualKey.Up:\n            <span class=\"hljs-keyword\">if</span> (matches.Length > <span class=\"hljs-number\">0</span>)\n                setIndex((safeIndex - <span class=\"hljs-number\">1</span> + matches.Length) % matches.Length);\n            e.Handled = <span class=\"hljs-literal\">true</span>;\n            <span class=\"hljs-keyword\">break</span>;\n        <span class=\"hljs-keyword\">case</span> VirtualKey.Enter:\n            <span class=\"hljs-keyword\">if</span> (matches.Length > <span class=\"hljs-number\">0</span>)\n            {\n                <span class=\"hljs-keyword\">var</span> cmd = matches[safeIndex];\n                cmd.Execute?.Invoke();\n                setLast(cmd.Label);\n                setOpen(<span class=\"hljs-literal\">false</span>);\n                setQuery(<span class=\"hljs-string\">\"\"</span>);\n                setIndex(<span class=\"hljs-number\">0</span>);\n            }\n            e.Handled = <span class=\"hljs-literal\">true</span>;\n            <span class=\"hljs-keyword\">break</span>;\n    }\n}\n</code></pre>\n<p><code>OnPreviewKeyDown</code> tunnels — it fires before the bubbling pair, so\nUp / Down move the selection instead of the <code>TextBox</code> caret, and\nEnter fires the highlighted command rather than inserting a newline.\nSetting <code>e.Handled = true</code> stops further routing so the underlying\ncontrol stays out of the way. The same handler closes the palette on\nEscape; the <a href=\"/input-and-gestures/\">input-and-gestures</a> page\ncovers the preview-vs-bubble pairing in detail.</p>\n<h3 id=\"render\">Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The page is rendered normally; the palette is a conditional</span>\n<span class=\"hljs-comment\">// overlay on top, just like the modal-dialog recipe. The root</span>\n<span class=\"hljs-comment\">// surface owns the Ctrl+K accelerator so the palette can open</span>\n<span class=\"hljs-comment\">// from anywhere on the page.</span>\n<span class=\"hljs-keyword\">var</span> page = VStack(<span class=\"hljs-number\">12</span>,\n    Heading(<span class=\"hljs-string\">\"Command Palette Demo\"</span>),\n    TextBlock(<span class=\"hljs-string\">\"Press Ctrl+K to open the palette.\"</span>).Opacity(<span class=\"hljs-number\">0.7</span>),\n    last <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n        ? Empty()\n        : TextBlock(<span class=\"hljs-string\">$\"Last command: <span class=\"hljs-subst\">{last}</span>\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n).Padding(<span class=\"hljs-number\">24</span>);\n\nElement palette = Border(\n    VStack(<span class=\"hljs-number\">0</span>,\n        TextBox(query, v => { setQuery(v); setIndex(<span class=\"hljs-number\">0</span>); },\n            placeholderText: <span class=\"hljs-string\">\"Type a command…\"</span>).Width(<span class=\"hljs-number\">420</span>),\n        matches.Length == <span class=\"hljs-number\">0</span>\n            ? TextBlock(<span class=\"hljs-string\">\"No commands match.\"</span>).Padding(<span class=\"hljs-number\">12</span>).Opacity(<span class=\"hljs-number\">0.6</span>)\n            : VStack(<span class=\"hljs-number\">0</span>,\n                matches.Select((c, i) =>\n                    TextBlock(c.Label)\n                        .Padding(<span class=\"hljs-number\">10</span>)\n                        .Background(i == safeIndex ? <span class=\"hljs-string\">\"#E5F1FB\"</span> : <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n                ).ToArray&#x3C;Element>()\n            )\n    ).Background(<span class=\"hljs-string\">\"#FFFFFF\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>).Width(<span class=\"hljs-number\">440</span>)\n).Background(<span class=\"hljs-string\">\"#80000000\"</span>).Padding(<span class=\"hljs-number\">60</span>)\n .OnPreviewKeyDown(OnPaletteKey);\n\n<span class=\"hljs-keyword\">var</span> root = (open ? Group(page, palette) : page)\n    .OnKeyDown((_, e) =>\n    {\n        <span class=\"hljs-comment\">// Ctrl+K toggles the palette. A real app would prefer a</span>\n        <span class=\"hljs-comment\">// KeyboardAccelerator on the window root; this keeps the</span>\n        <span class=\"hljs-comment\">// recipe self-contained.</span>\n        <span class=\"hljs-keyword\">var</span> ctrl = (Microsoft.UI.Xaml.Window.Current?.CoreWindow\n            .GetKeyState(VirtualKey.Control)\n            &#x26; Windows.UI.Core.CoreVirtualKeyStates.Down)\n            == Windows.UI.Core.CoreVirtualKeyStates.Down;\n        <span class=\"hljs-keyword\">if</span> (ctrl &#x26;&#x26; e.Key == VirtualKey.K)\n        {\n            setOpen(!open);\n            e.Handled = <span class=\"hljs-literal\">true</span>;\n        }\n    });\n<span class=\"hljs-keyword\">return</span> root;\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-command-palette/palette.png\" alt=\"Command palette open with a filtered list\"></p>\n<p>The page renders normally; the palette is a <code>Border</code> wrapping a\n<code>VStack</code> returned alongside it in a <code>Group(page, palette)</code>. The\nscrim background <code>#80000000</code> dims the page underneath. The root\nsurface's <code>.OnKeyDown</code> watches for Ctrl+K and toggles <code>open</code>, which\nis the cheapest \"global accelerator\" that works from any focus state\non the page.</p>\n<p>For a real app, hoist the open accelerator to a window-level\n<a href=\"/commanding/\"><code>KeyboardAccelerator</code></a> so the palette opens even\nwhen focus is inside a chart or a third-party control that swallows\nrouted key events. The recipe keeps the handler inline so the\ncomposition is visible end-to-end.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The palette is not a modal.</strong> It's an overlay with an input that\nauto-focuses on open. Don't reach for <a href=\"/accessibility/\"><code>UseFocusTrap</code></a>\nunless your palette grows a help row, a settings flyout, or anything\nelse that takes focus — for a single text field plus a result list,\nEsc-to-close is enough.</p>\n<p><strong>Bind to <code>Command</code>, not raw <code>Action</code>.</strong> Even though the palette only\nneeds <code>Label</code> and <code>Execute</code> today, declaring the catalog as\n<a href=\"../commanding.md\"><code>Command[]</code></a> means the same record can drive a\ntoolbar button or a menu item tomorrow without rewriting the catalog.\nThe metadata travels with the action — that's the whole point of the\ncommanding surface.</p>\n<p><strong>Refilter on every keystroke; memoize on every catalog.</strong> The\nfilter is <code>O(n)</code> per keystroke; for the catalog sizes a command\npalette actually has (tens to low hundreds), a recompute on each\nrender is well under a frame. Reach for <a href=\"/hooks/\"><code>UseMemo</code></a> when\nthe catalog grows or when the matcher does — a real fuzzy ranker\ndeserves its own cache and is worth a separate recipe.</p>\n<p><strong>Reset on close.</strong> When the user dismisses, clear <code>query</code> and\nreset <code>index</code> to <code>0</code> so the next open starts fresh. The Enter\nhandler in the recipe does both; the Esc branch can be tightened the\nsame way if a stale query would surprise the user on the next open.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — The full <code>Command</code> surface,\nincluding async tracking and shared accelerators.</li>\n<li><strong><a href=\"/input-and-gestures/\">Input and Gestures</a></strong> — Preview-vs-\nbubble keyboard routing and the modifier tower the palette layers on.</li>\n<li><strong><a href=\"/recipes/modal-dialog/\">Recipe: Modal dialog</a></strong> — Same overlay shape,\ndifferent ergonomics — a confirmation pane instead of a filter.</li>\n<li><strong><a href=\"/recipes/search-with-suggestions/\">Recipe: Search with suggestions</a></strong> —\nThe filter-as-you-type pattern in isolation, without the overlay.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Focus management when the\npalette grows secondary surfaces.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Command Palette # A command palette is a filter-as-you-type list with a keyboard accelerator to open and Enter to execute. In Microsoft.UI.Reactor (Reactor) it's three UseState hooks, a static catalog of Command records, and an OnPreviewKeyDown handler — no menubar, no modal manager, no view model. The palette is just another conditional overlay, layered over the page the same way the modal-dialog recipe layers its confirmation sheet. Primitives # Concern API Open / query / selection state UseState&#x3C;bool> / UseState&#x3C;string> / UseState&#x3C;int> Command catalog Command records — same shape used by Button , MenuItem , AppBarButton Filter LINQ Where(...Contains(query, OrdinalIgnoreCase)) Open accelerator .OnKeyDown on the root surface List navigation .OnPreviewKeyDown on the palette container Overlay composition Group(page, palette) — same shape as Recipe: Modal dialog Command catalog # // The catalog is a static array of Reactor Command records. The same // record could just as well be bound to a Button or a MenuItem — the // palette is one more surface that consumes it. private static readonly Command[] Catalog = new [] { new Command { Label = \"File: New\" , Execute = () => Log( \"new\" ) }, new Command { Label = \"File: Open…\" , Execute = () => Log( \"open\" ) }, new Command { Label = \"File: Save\" , Execute = () => Log( \"save\" ) }, new Command { Label = \"Edit: Find\" , Execute = () => Log( \"find\" ) }, new Command { Label = \"Edit: Replace\" , Execute = () => Log( \"replace\" ) }, new Command { Label = \"View: Toggle Theme\" , Execute = () => Log( \"theme\" ) }, new Command { Label = \"View: Zen Mode\" , Execute = () => Log( \"zen\" ) }, new Command { Label = \"Go: Go to Line…\" , Execute = () => Log( \"goto-line\" ) }, new Command { Label = \"Go: Go to Symbol…\" ,Execute = () => Log( \"goto-symbol\" ) }, new Command { Label = \"Help: About\" , Execute = () => Log( \"about\" ) }, }; private static void Log ( string id ) { /* hook to telemetry in a real app */ } A Command bundles a label with an action and metadata ( commanding covers the full surface — icon, accelerator, CanExecute , async tracking). The palette only needs Label and Execute ; the rest is there when you want to share the same record with a toolbar button or a context menu entry. State # // Three pieces of state run the palette: whether it's open, the // typed query, and the highlighted row in the filtered list. var (open, setOpen) = UseState( false ); var (query, setQuery) = UseState( \"\" ); var (index, setIndex) = UseState( 0 ); var (last, setLast) = UseState&#x3C; string ?>( null ); Three hooks own the palette: open toggles the overlay, query drives the filter, and index is the highlighted row. A fourth last hook just echoes the most recently executed command for the demo — it's not part of the palette pattern. Filter # // Re-derive the filtered list on every render. The catalog is // small; a real palette with hundreds of commands would key this // through UseMemo on `query`. var matches = string .IsNullOrWhiteSpace(query) ? Catalog : Catalog.Where(c => c.Label.Contains(query, StringComparison.OrdinalIgnoreCase)).ToArray(); // Clamp the selection so it never points off the end of the list. var safeIndex = matches.Length == 0 ? 0 : Math.Clamp(index, 0 , matches.Length - 1 ); The filter runs on every render. For a small catalog that's the right trade — no UseMemo ceremony, and the filter sees the latest query without an extra dependency array. Promote to UseMemo once the catalog crosses a few hundred entries or the predicate grows past Contains . The safeIndex clamp keeps the highlight valid when typing shortens the match list out from under the old selection. Keyboard handler # // Esc closes; Up / Down move the selection; Enter invokes the // highlighted command. The OnPreviewKeyDown handler intercepts // before the TextBox gets the keystroke, so arrow keys move // the list instead of the caret. void OnPaletteKey ( object _, Microsoft.UI.Xaml.Input.KeyRoutedEventArgs e ) { switch (e.Key) { case VirtualKey.Escape: setOpen( false ); e.Handled = true ; break ; case VirtualKey.Down: if (matches.Length > 0 ) setIndex((safeIndex + 1 ) % matches.Length); e.Handled = true ; break ; case VirtualKey.Up: if (matches.Length > 0 ) setIndex((safeIndex - 1 + matches.Length) % matches.Length); e.Handled = true ; break ; case VirtualKey.Enter: if (matches.Length > 0 ) { var cmd = matches[safeIndex]; cmd.Execute?.Invoke(); setLast(cmd.Label); setOpen( false ); setQuery( \"\" ); setIndex( 0 ); } e.Handled = true ; break ; } } OnPreviewKeyDown tunnels — it fires before the bubbling pair, so Up / Down move the selection instead of the TextBox caret, and Enter fires the highlighted command rather than inserting a newline. Setting e.Handled = true stops further routing so the underlying control stays out of the way. The same handler closes the palette on Escape; the input-and-gestures page covers the preview-vs-bubble pairing in detail. Render # // The page is rendered normally; the palette is a conditional // overlay on top, just like the modal-dialog recipe. The root // surface owns the Ctrl+K accelerator so the palette can open // from anywhere on the page. var page = VStack( 12 , Heading( \"Command Palette Demo\" ), TextBlock( \"Press Ctrl+K to open the palette.\" ).Opacity( 0.7 ), last is null ? Empty() : TextBlock( $\"Last command: {last} \" ).Opacity( 0.6 ) ).Padding( 24 ); Element palette = Border( VStack( 0 , TextBox(query, v => { setQuery(v); setIndex( 0 ); }, placeholderText: \"Type a command…\" ).Width( 420 ), matches.Length == 0 ? TextBlock( \"No commands match.\" ).Padding( 12 ).Opacity( 0.6 ) : VStack( 0 , matches.Select((c, i) => TextBlock(c.Label) .Padding( 10 ) .Background(i == safeIndex ? \"#E5F1FB\" : \"#FFFFFF\" ) ).ToArray&#x3C;Element>() ) ).Background( \"#FFFFFF\" ).CornerRadius( 8 ).Width( 440 ) ).Background( \"#80000000\" ).Padding( 60 ) .OnPreviewKeyDown(OnPaletteKey); var root = (open ? Group(page, palette) : page) .OnKeyDown((_, e) => { // Ctrl+K toggles the palette. A real app would prefer a // KeyboardAccelerator on the window root; this keeps the // recipe self-contained. var ctrl = (Microsoft.UI.Xaml.Window.Current?.CoreWindow .GetKeyState(VirtualKey.Control) &#x26; Windows.UI.Core.CoreVirtualKeyStates.Down) == Windows.UI.Core.CoreVirtualKeyStates.Down; if (ctrl &#x26;&#x26; e.Key == VirtualKey.K) { setOpen(!open); e.Handled = true ; } }); return root; The page renders normally; the palette is a Border wrapping a VStack returned alongside it in a Group(page, palette) . The scrim background #80000000 dims the page underneath. The root surface's .OnKeyDown watches for Ctrl+K and toggles open , which is the cheapest \"global accelerator\" that works from any focus state on the page. For a real app, hoist the open accelerator to a window-level KeyboardAccelerator so the palette opens even when focus is inside a chart or a third-party control that swallows routed key events. The recipe keeps the handler inline so the composition is visible end-to-end. Tips # The palette is not a modal. It's an overlay with an input that auto-focuses on open. Don't reach for UseFocusTrap unless your palette grows a help row, a settings flyout, or anything else that takes focus — for a single text field plus a result list, Esc-to-close is enough. Bind to Command , not raw Action . Even though the palette only needs Label and Execute today, declaring the catalog as Command[] means the same record can drive a toolbar button or a menu item tomorrow without rewriting the catalog. The metadata travels with the action — that's the whole point of the commanding surface. Refilter on every keystroke; memoize on every catalog. The filter is O(n) per keystroke; for the catalog sizes a command palette actually has (tens to low hundreds), a recompute on each render is well under a frame. Reach for UseMemo when the catalog grows or when the matcher does — a real fuzzy ranker deserves its own cache and is worth a separate recipe. Reset on close. When the user dismisses, clear query and reset index to 0 so the next open starts fresh. The Enter handler in the recipe does both; the Esc branch can be tightened the same way if a stale query would surprise the user on the next open. Next Steps # Commanding — The full Command surface, including async tracking and shared accelerators. Input and Gestures — Preview-vs- bubble keyboard routing and the modifier tower the palette layers on. Recipe: Modal dialog — Same overlay shape, different ergonomics — a confirmation pane instead of a filter. Recipe: Search with suggestions — The filter-as-you-type pattern in isolation, without the overlay. Accessibility — Focus management when the palette grows secondary surfaces. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/command-palette.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/command-palette.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "command-catalog",
          "text": "Command catalog"
        },
        {
          "depth": 3,
          "id": "state",
          "text": "State"
        },
        {
          "depth": 3,
          "id": "filter",
          "text": "Filter"
        },
        {
          "depth": 3,
          "id": "keyboard-handler",
          "text": "Keyboard handler"
        },
        {
          "depth": 3,
          "id": "render",
          "text": "Render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Drag Reorder",
      "path": "recipes/drag-reorder.md",
      "slug": "/recipes/drag-reorder/",
      "html": "<h1 id=\"recipe-drag-to-reorder\">Recipe: Drag to Reorder<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-drag-to-reorder\">#</a></h1>\n<p>Drag-reorder is a <code>UseState&#x3C;List&#x3C;T>></code> plus a function that splices\none item from one index to another. Microsoft.UI.Reactor (Reactor)'s keyed reconciler does\nthe rest — moves preserve identity, so the same row element keeps\nits focus, its hover state, and its place in the visual tree.\nPointer drag is one entry point; an Alt+Up / Alt+Down keyboard\nshortcut is the second, and it carries the accessibility story.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Ordered state</td><td><code>UseState&#x3C;List&#x3C;T>></code></td></tr><tr><td>Stable identity across moves</td><td>Record <code>Id</code> field; row keyed in <code>Select</code></td></tr><tr><td>Source-side drag</td><td><a href=\"/input-and-gestures/\"><code>.OnDragStart&#x3C;T, TPayload></code></a></td></tr><tr><td>Target-side drop</td><td><a href=\"/input-and-gestures/\"><code>.OnDrop&#x3C;T, TPayload></code></a></td></tr><tr><td>Hover indicator</td><td><code>.OnDragEnter</code> writing to local state</td></tr><tr><td>Keyboard alternative</td><td><code>.OnKeyDown</code> + <code>InputKeyboardSource.GetKeyStateForCurrentThread</code></td></tr><tr><td>Focusable rows</td><td><code>.IsTabStop(true)</code> + <code>.OnGotFocus</code></td></tr></tbody></table>\n<h3 id=\"data\">Data<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Identity lives on the record. Moves preserve `Id`, so the reconciler keeps</span>\n<span class=\"hljs-comment\">// the same row element and its focus state across a reorder.</span>\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">TaskItem</span>(<span class=\"hljs-title\">int</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Title</span>);\n\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Seed</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> TaskItem[] Initial = <span class=\"hljs-keyword\">new</span>[]\n    {\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">1</span>, <span class=\"hljs-string\">\"Write the recipe lead\"</span>),\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">2</span>, <span class=\"hljs-string\">\"Wire the drag source\"</span>),\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">3</span>, <span class=\"hljs-string\">\"Wire the drop target\"</span>),\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">4</span>, <span class=\"hljs-string\">\"Add the keyboard alternative\"</span>),\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">5</span>, <span class=\"hljs-string\">\"Land the snippets\"</span>),\n        <span class=\"hljs-keyword\">new</span> TaskItem(<span class=\"hljs-number\">6</span>, <span class=\"hljs-string\">\"Run tier-lint\"</span>),\n    };\n}\n</code></pre>\n<p>The record carries an <code>Id</code>. Moves never mint new ids — that's what\nlets the reconciler keep the same row element across a reorder\ninstead of unmounting one and mounting another at the new slot.</p>\n<h3 id=\"state\">State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The list itself is a UseState&#x3C;List&#x3C;TaskItem>>. `draggingId` tracks</span>\n<span class=\"hljs-comment\">// the row currently being dragged so we can dim it; `hoverId` tracks</span>\n<span class=\"hljs-comment\">// the drop target so we can draw the insertion hint. Both reset on</span>\n<span class=\"hljs-comment\">// drop-completed.</span>\n<span class=\"hljs-keyword\">var</span> (items, setItems) = UseState&#x3C;List&#x3C;TaskItem>>(Seed.Initial.ToList());\n<span class=\"hljs-keyword\">var</span> (draggingId, setDraggingId) = UseState&#x3C;<span class=\"hljs-built_in\">int</span>?>(<span class=\"hljs-literal\">null</span>);\n<span class=\"hljs-keyword\">var</span> (hoverId, setHoverId) = UseState&#x3C;<span class=\"hljs-built_in\">int</span>?>(<span class=\"hljs-literal\">null</span>);\n<span class=\"hljs-keyword\">var</span> (focusedId, setFocusedId) = UseState(Seed.Initial[<span class=\"hljs-number\">0</span>].Id);\n</code></pre>\n<p>Four <code>UseState</code> hooks: the list itself, the id of the row being\ndragged (so we can dim it), the id of the hovered drop target (so\nwe can outline it), and the focused row id (so the keyboard\nshortcut knows which row to move). Two of the four exist only\nduring an active drag and reset on completion.</p>\n<h3 id=\"the-move\">The move<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-move\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Splice a single item from `fromIndex` to `toIndex`. The reconciler</span>\n<span class=\"hljs-comment\">// keys rows by `Id`, so this is a pure data move — no row remounts,</span>\n<span class=\"hljs-comment\">// no lost focus, no animation seam.</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Move</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> fromIndex, <span class=\"hljs-built_in\">int</span> toIndex</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (fromIndex == toIndex) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">var</span> copy = <span class=\"hljs-keyword\">new</span> List&#x3C;TaskItem>(items);\n    <span class=\"hljs-keyword\">if</span> (fromIndex &#x3C; <span class=\"hljs-number\">0</span> || fromIndex >= copy.Count) <span class=\"hljs-keyword\">return</span>;\n    toIndex = System.Math.Clamp(toIndex, <span class=\"hljs-number\">0</span>, copy.Count - <span class=\"hljs-number\">1</span>);\n    <span class=\"hljs-keyword\">var</span> item = copy[fromIndex];\n    copy.RemoveAt(fromIndex);\n    copy.Insert(toIndex, item);\n    setItems(copy);\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">MoveById</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> sourceId, <span class=\"hljs-built_in\">int</span> targetId</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> <span class=\"hljs-keyword\">from</span> = items.FindIndex(i => i.Id == sourceId);\n    <span class=\"hljs-keyword\">var</span> to = items.FindIndex(i => i.Id == targetId);\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">from</span> >= <span class=\"hljs-number\">0</span> &#x26;&#x26; to >= <span class=\"hljs-number\">0</span>) Move(<span class=\"hljs-keyword\">from</span>, to);\n}\n</code></pre>\n<p>The whole reorder is a list splice. <code>RemoveAt</code> then <code>Insert</code> on a\nfresh copy, then <code>setItems</code> — the reconciler diffs old vs. new by\n<code>Id</code>, sees the same set of records in a different order, and reuses\nevery row. <code>MoveById</code> is a small ergonomic wrapper so the pointer\nhandlers can pass payload ids without computing indices.</p>\n<h3 id=\"keyboard-alternative\">Keyboard alternative<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyboard-alternative\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Alt+Up / Alt+Down moves the focused row. This is the load-bearing</span>\n<span class=\"hljs-comment\">// accessibility story — drag-and-drop alone fails screen-reader and</span>\n<span class=\"hljs-comment\">// motor-impaired users; a keyboard alternative makes the recipe</span>\n<span class=\"hljs-comment\">// WCAG-conformant.</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">HandleKey</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> rowId, Microsoft.UI.Xaml.Input.KeyRoutedEventArgs e</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> alt = (Microsoft.UI.Input.InputKeyboardSource\n        .GetKeyStateForCurrentThread(VirtualKey.Menu)\n        &#x26; Windows.UI.Core.CoreVirtualKeyStates.Down) != <span class=\"hljs-number\">0</span>;\n    <span class=\"hljs-keyword\">if</span> (!alt) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> idx = items.FindIndex(i => i.Id == rowId);\n    <span class=\"hljs-keyword\">if</span> (idx &#x3C; <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (e.Key == VirtualKey.Up &#x26;&#x26; idx > <span class=\"hljs-number\">0</span>)\n    {\n        Move(idx, idx - <span class=\"hljs-number\">1</span>);\n        setFocusedId(rowId);\n        e.Handled = <span class=\"hljs-literal\">true</span>;\n    }\n    <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (e.Key == VirtualKey.Down &#x26;&#x26; idx &#x3C; items.Count - <span class=\"hljs-number\">1</span>)\n    {\n        Move(idx, idx + <span class=\"hljs-number\">1</span>);\n        setFocusedId(rowId);\n        e.Handled = <span class=\"hljs-literal\">true</span>;\n    }\n}\n</code></pre>\n<p>Pointer drag is half the recipe — the keyboard path is the other\nhalf. <code>Alt+Up</code> and <code>Alt+Down</code> move the focused row by one slot;\n<code>e.Handled = true</code> keeps the event from bubbling to a parent\n<code>ScrollViewer</code>. Modifier detection uses\n<a href=\"https://learn.microsoft.com/uwp/api/windows.ui.core.corewindow.getkeystate\"><code>InputKeyboardSource.GetKeyStateForCurrentThread</code></a>\nbecause <code>KeyRoutedEventArgs.KeyStatus</code> doesn't carry Alt cleanly.</p>\n<h3 id=\"render\">Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\">Element <span class=\"hljs-title\">Row</span>(<span class=\"hljs-params\">TaskItem item</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> isDragging = draggingId == item.Id;\n    <span class=\"hljs-keyword\">var</span> isHover = hoverId == item.Id &#x26;&#x26; draggingId <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> &#x26;&#x26; draggingId != item.Id;\n    <span class=\"hljs-keyword\">var</span> isFocused = focusedId == item.Id;\n\n    <span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(<span class=\"hljs-string\">\"☰\"</span>).Opacity(<span class=\"hljs-number\">0.4</span>).Width(<span class=\"hljs-number\">20</span>),     <span class=\"hljs-comment\">// grab handle glyph</span>\n            TextBlock(item.Title)\n        )\n        .Padding(<span class=\"hljs-number\">10</span>)\n        .Background(isFocused ? <span class=\"hljs-string\">\"#EEF4FB\"</span> : <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n        .WithBorder(isHover ? <span class=\"hljs-string\">\"#0078D4\"</span> : <span class=\"hljs-string\">\"#E1E1E1\"</span>, isHover ? <span class=\"hljs-number\">2</span> : <span class=\"hljs-number\">1</span>)\n        .Opacity(isDragging ? <span class=\"hljs-number\">0.4</span> : <span class=\"hljs-number\">1.0</span>)\n        .IsTabStop(<span class=\"hljs-literal\">true</span>)\n        .OnGotFocus((_, _) => setFocusedId(item.Id))\n        .OnKeyDown((_, e) => HandleKey(item.Id, e))\n        .OnDragStart&#x3C;StackElement, <span class=\"hljs-built_in\">int</span>>(\n            getPayload: () => { setDraggingId(item.Id); <span class=\"hljs-keyword\">return</span> item.Id; },\n            allowedOperations: DragOperations.Move,\n            onEnd: _ => { setDraggingId(<span class=\"hljs-literal\">null</span>); setHoverId(<span class=\"hljs-literal\">null</span>); })\n        .OnDragEnter(<span class=\"hljs-keyword\">args</span> =>\n        {\n            <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">args</span>.Data.TryGetTypedPayload&#x3C;<span class=\"hljs-built_in\">int</span>>(<span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> srcId) &#x26;&#x26; srcId != item.Id)\n                setHoverId(item.Id);\n        })\n        .OnDrop&#x3C;StackElement, <span class=\"hljs-built_in\">int</span>>(srcId =>\n        {\n            MoveById(srcId, item.Id);\n            setDraggingId(<span class=\"hljs-literal\">null</span>);\n            setHoverId(<span class=\"hljs-literal\">null</span>);\n        }, acceptedOps: DragOperations.Move);\n}\n\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n    Heading(<span class=\"hljs-string\">\"Reorder tasks\"</span>),\n    TextBlock(<span class=\"hljs-string\">\"Drag a row, or focus one and press Alt+Up / Alt+Down.\"</span>)\n        .Opacity(<span class=\"hljs-number\">0.7</span>),\n    VStack(<span class=\"hljs-number\">4</span>,\n        items.Select(Row).ToArray()\n    )\n).Padding(<span class=\"hljs-number\">16</span>).Width(<span class=\"hljs-number\">320</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-drag-reorder/list.png\" alt=\"Reorderable list with a dragged row in flight\"></p>\n<p>Each row is a <code>HStack</code> with a grab-handle glyph and the title.\n<code>.OnDragStart&#x3C;StackElement, int></code> advertises the row's <code>Id</code> as a\ntyped <code>int</code> payload; <code>.OnDrop&#x3C;StackElement, int></code> accepts an <code>int</code>\npayload on any sibling row and calls <code>MoveById</code>. The dragged row\nfades to 0.4 opacity; the hover target gets a 2-px accent border.\n<code>IsTabStop(true)</code> plus <code>.OnGotFocus</code> makes every row a keyboard\nlanding pad so the Alt+Arrow shortcut has somewhere to anchor.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Key your rows by the record's <code>Id</code>, not by index.</strong> Index keying\ndefeats the whole point — every move would invalidate every row\nafter the drop position and the reconciler would remount them.\n<a href=\"/collections/\"><code>Collections</code></a> covers the key-selector pattern\nin depth.</p>\n<p><strong>Treat the keyboard path as load-bearing, not as a fallback.</strong>\nA drag-only reorder is unusable with a screen reader, with a\nkeyboard-only workflow, or with motor impairments that preclude\nfine pointer control. The Alt+Arrow shortcut is the same <code>Move</code>\nfunction as the pointer path — there's no duplicated logic and the\n<a href=\"/accessibility/\"><code>Accessibility</code></a> story flows from one code\npath.</p>\n<p><strong>Don't reach for a <code>ListView&#x3C;T></code> for ten rows.</strong> A virtualized\ncollection's drag surface is genuinely harder (the row element\nunder the pointer may unmount mid-drag as the user scrolls), and a\nplain <code>VStack</code> of rows is enough for recipe-sized lists. Promote\nto <code>ListView&#x3C;T></code> only when row counts cross the dozens.</p>\n<p><strong>Animation is optional polish.</strong> Reactor doesn't ship a turnkey\nlist-reorder animation primitive at the moment; for the doc-app\nabove, the position change is instant. <a href=\"/animation/\"><code>Animation</code></a>\ncovers the building blocks (<code>UseAnimation</code>, easing curves) if you\nwant to layer a 150ms slide on top.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Key selectors, the\nunderlying list primitives, and when to promote to <code>ListView&#x3C;T></code>.</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — Optional motion polish for the\nreorder transition.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Focus, tab order, and\nthe full keyboard-alternative story that the Alt+Arrow handler\nparticipates in.</li>\n<li><strong><a href=\"/input-and-gestures/\">Input and Gestures</a></strong> — The full\ndrag/drop surface: typed payloads, allowed operations, UI\noverride hooks, drag-end callbacks.</li>\n<li><strong><a href=\"/recipes/master-detail/\">Recipe: Master-detail</a></strong> — Adjacent recipe\nfor the selection-driven shape.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Drag to Reorder # Drag-reorder is a UseState&#x3C;List&#x3C;T>> plus a function that splices one item from one index to another. Microsoft.UI.Reactor (Reactor)'s keyed reconciler does the rest — moves preserve identity, so the same row element keeps its focus, its hover state, and its place in the visual tree. Pointer drag is one entry point; an Alt+Up / Alt+Down keyboard shortcut is the second, and it carries the accessibility story. Primitives # Concern API Ordered state UseState&#x3C;List&#x3C;T>> Stable identity across moves Record Id field; row keyed in Select Source-side drag .OnDragStart&#x3C;T, TPayload> Target-side drop .OnDrop&#x3C;T, TPayload> Hover indicator .OnDragEnter writing to local state Keyboard alternative .OnKeyDown + InputKeyboardSource.GetKeyStateForCurrentThread Focusable rows .IsTabStop(true) + .OnGotFocus Data # // Identity lives on the record. Moves preserve `Id`, so the reconciler keeps // the same row element and its focus state across a reorder. record TaskItem ( int Id , string Title ); static class Seed { public static readonly TaskItem[] Initial = new [] { new TaskItem( 1 , \"Write the recipe lead\" ), new TaskItem( 2 , \"Wire the drag source\" ), new TaskItem( 3 , \"Wire the drop target\" ), new TaskItem( 4 , \"Add the keyboard alternative\" ), new TaskItem( 5 , \"Land the snippets\" ), new TaskItem( 6 , \"Run tier-lint\" ), }; } The record carries an Id . Moves never mint new ids — that's what lets the reconciler keep the same row element across a reorder instead of unmounting one and mounting another at the new slot. State # // The list itself is a UseState&#x3C;List&#x3C;TaskItem>>. `draggingId` tracks // the row currently being dragged so we can dim it; `hoverId` tracks // the drop target so we can draw the insertion hint. Both reset on // drop-completed. var (items, setItems) = UseState&#x3C;List&#x3C;TaskItem>>(Seed.Initial.ToList()); var (draggingId, setDraggingId) = UseState&#x3C; int ?>( null ); var (hoverId, setHoverId) = UseState&#x3C; int ?>( null ); var (focusedId, setFocusedId) = UseState(Seed.Initial[ 0 ].Id); Four UseState hooks: the list itself, the id of the row being dragged (so we can dim it), the id of the hovered drop target (so we can outline it), and the focused row id (so the keyboard shortcut knows which row to move). Two of the four exist only during an active drag and reset on completion. The move # // Splice a single item from `fromIndex` to `toIndex`. The reconciler // keys rows by `Id`, so this is a pure data move — no row remounts, // no lost focus, no animation seam. void Move ( int fromIndex, int toIndex ) { if (fromIndex == toIndex) return ; var copy = new List&#x3C;TaskItem>(items); if (fromIndex &#x3C; 0 || fromIndex >= copy.Count) return ; toIndex = System.Math.Clamp(toIndex, 0 , copy.Count - 1 ); var item = copy[fromIndex]; copy.RemoveAt(fromIndex); copy.Insert(toIndex, item); setItems(copy); } void MoveById ( int sourceId, int targetId ) { var from = items.FindIndex(i => i.Id == sourceId); var to = items.FindIndex(i => i.Id == targetId); if ( from >= 0 &#x26;&#x26; to >= 0 ) Move( from , to); } The whole reorder is a list splice. RemoveAt then Insert on a fresh copy, then setItems — the reconciler diffs old vs. new by Id , sees the same set of records in a different order, and reuses every row. MoveById is a small ergonomic wrapper so the pointer handlers can pass payload ids without computing indices. Keyboard alternative # // Alt+Up / Alt+Down moves the focused row. This is the load-bearing // accessibility story — drag-and-drop alone fails screen-reader and // motor-impaired users; a keyboard alternative makes the recipe // WCAG-conformant. void HandleKey ( int rowId, Microsoft.UI.Xaml.Input.KeyRoutedEventArgs e ) { var alt = (Microsoft.UI.Input.InputKeyboardSource .GetKeyStateForCurrentThread(VirtualKey.Menu) &#x26; Windows.UI.Core.CoreVirtualKeyStates.Down) != 0 ; if (!alt) return ; var idx = items.FindIndex(i => i.Id == rowId); if (idx &#x3C; 0 ) return ; if (e.Key == VirtualKey.Up &#x26;&#x26; idx > 0 ) { Move(idx, idx - 1 ); setFocusedId(rowId); e.Handled = true ; } else if (e.Key == VirtualKey.Down &#x26;&#x26; idx &#x3C; items.Count - 1 ) { Move(idx, idx + 1 ); setFocusedId(rowId); e.Handled = true ; } } Pointer drag is half the recipe — the keyboard path is the other half. Alt+Up and Alt+Down move the focused row by one slot; e.Handled = true keeps the event from bubbling to a parent ScrollViewer . Modifier detection uses InputKeyboardSource.GetKeyStateForCurrentThread because KeyRoutedEventArgs.KeyStatus doesn't carry Alt cleanly. Render # Element Row ( TaskItem item ) { var isDragging = draggingId == item.Id; var isHover = hoverId == item.Id &#x26;&#x26; draggingId is not null &#x26;&#x26; draggingId != item.Id; var isFocused = focusedId == item.Id; return HStack( 8 , TextBlock( \"☰\" ).Opacity( 0.4 ).Width( 20 ), // grab handle glyph TextBlock(item.Title) ) .Padding( 10 ) .Background(isFocused ? \"#EEF4FB\" : \"#FFFFFF\" ) .WithBorder(isHover ? \"#0078D4\" : \"#E1E1E1\" , isHover ? 2 : 1 ) .Opacity(isDragging ? 0.4 : 1.0 ) .IsTabStop( true ) .OnGotFocus((_, _) => setFocusedId(item.Id)) .OnKeyDown((_, e) => HandleKey(item.Id, e)) .OnDragStart&#x3C;StackElement, int >( getPayload: () => { setDraggingId(item.Id); return item.Id; }, allowedOperations: DragOperations.Move, onEnd: _ => { setDraggingId( null ); setHoverId( null ); }) .OnDragEnter( args => { if ( args .Data.TryGetTypedPayload&#x3C; int >( out var srcId) &#x26;&#x26; srcId != item.Id) setHoverId(item.Id); }) .OnDrop&#x3C;StackElement, int >(srcId => { MoveById(srcId, item.Id); setDraggingId( null ); setHoverId( null ); }, acceptedOps: DragOperations.Move); } return VStack( 8 , Heading( \"Reorder tasks\" ), TextBlock( \"Drag a row, or focus one and press Alt+Up / Alt+Down.\" ) .Opacity( 0.7 ), VStack( 4 , items.Select(Row).ToArray() ) ).Padding( 16 ).Width( 320 ); Each row is a HStack with a grab-handle glyph and the title. .OnDragStart&#x3C;StackElement, int> advertises the row's Id as a typed int payload; .OnDrop&#x3C;StackElement, int> accepts an int payload on any sibling row and calls MoveById . The dragged row fades to 0.4 opacity; the hover target gets a 2-px accent border. IsTabStop(true) plus .OnGotFocus makes every row a keyboard landing pad so the Alt+Arrow shortcut has somewhere to anchor. Tips # Key your rows by the record's Id , not by index. Index keying defeats the whole point — every move would invalidate every row after the drop position and the reconciler would remount them. Collections covers the key-selector pattern in depth. Treat the keyboard path as load-bearing, not as a fallback. A drag-only reorder is unusable with a screen reader, with a keyboard-only workflow, or with motor impairments that preclude fine pointer control. The Alt+Arrow shortcut is the same Move function as the pointer path — there's no duplicated logic and the Accessibility story flows from one code path. Don't reach for a ListView&#x3C;T> for ten rows. A virtualized collection's drag surface is genuinely harder (the row element under the pointer may unmount mid-drag as the user scrolls), and a plain VStack of rows is enough for recipe-sized lists. Promote to ListView&#x3C;T> only when row counts cross the dozens. Animation is optional polish. Reactor doesn't ship a turnkey list-reorder animation primitive at the moment; for the doc-app above, the position change is instant. Animation covers the building blocks ( UseAnimation , easing curves) if you want to layer a 150ms slide on top. Next Steps # Collections — Key selectors, the underlying list primitives, and when to promote to ListView&#x3C;T> . Animation — Optional motion polish for the reorder transition. Accessibility — Focus, tab order, and the full keyboard-alternative story that the Alt+Arrow handler participates in. Input and Gestures — The full drag/drop surface: typed payloads, allowed operations, UI override hooks, drag-end callbacks. Recipe: Master-detail — Adjacent recipe for the selection-driven shape. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/drag-reorder.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/drag-reorder.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "data",
          "text": "Data"
        },
        {
          "depth": 3,
          "id": "state",
          "text": "State"
        },
        {
          "depth": 3,
          "id": "the-move",
          "text": "The move"
        },
        {
          "depth": 3,
          "id": "keyboard-alternative",
          "text": "Keyboard alternative"
        },
        {
          "depth": 3,
          "id": "render",
          "text": "Render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Login",
      "path": "recipes/login.md",
      "slug": "/recipes/login/",
      "html": "<h1 id=\"recipe-login\">Recipe: Login<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-login\">#</a></h1>\n<p>A Microsoft.UI.Reactor (Reactor) login form is four pieces working together: validated input,\nsubmit-gated state, an async call, and an error-display surface. The\nrecipe below wires them with three <code>UseState</code> hooks and a <code>Task</code> —\nno view model, no event handler classes.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Per-field state</td><td><code>UseState&#x3C;string></code> / <code>UseState&#x3C;bool></code></td></tr><tr><td>Submit-disabled gating</td><td><code>.IsEnabled(canSubmit)</code></td></tr><tr><td>Async submit</td><td><code>async Task</code> + <code>Task.Delay</code></td></tr><tr><td>Error display</td><td>Conditional <code>Empty()</code> vs <code>TextBlock</code></td></tr><tr><td>Password input</td><td><a href=\"/forms/\"><code>PasswordBox</code></a></td></tr></tbody></table>\n<h3 id=\"state\">State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> (pwd, setPwd) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> (submitting, setSubmitting) = UseState(<span class=\"hljs-literal\">false</span>);\n<span class=\"hljs-keyword\">var</span> (error, setError) = UseState&#x3C;<span class=\"hljs-built_in\">string</span>?>(<span class=\"hljs-literal\">null</span>);\n</code></pre>\n<p>Four <code>UseState</code> calls — input, password, in-flight flag, and the\nlast error. Nothing else needs to survive a re-render.</p>\n<h3 id=\"per-keystroke-validation\">Per-keystroke validation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#per-keystroke-validation\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Local validation runs on every keystroke. The submit button is</span>\n<span class=\"hljs-comment\">// disabled until the form is valid; in-flight submits are gated</span>\n<span class=\"hljs-comment\">// by the same predicate.</span>\n<span class=\"hljs-keyword\">var</span> emailValid = email.Contains(<span class=\"hljs-string\">'@'</span>) &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'.'</span>);\n<span class=\"hljs-keyword\">var</span> pwdValid = pwd.Length >= <span class=\"hljs-number\">8</span>;\n<span class=\"hljs-keyword\">var</span> canSubmit = emailValid &#x26;&#x26; pwdValid &#x26;&#x26; !submitting;\n</code></pre>\n<p>The form derives <code>emailValid</code> and <code>pwdValid</code> from raw state on every\nrender — no debounce, no separate validation pass. Re-rendering on\nevery keystroke is fine in Reactor; the work happens in pure C# and\nthe reconciler skips slots that didn't change.</p>\n<h3 id=\"async-submit--disabled-button\">Async submit + disabled button<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-submit--disabled-button\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">async</span> Task <span class=\"hljs-title\">Submit</span>()</span>\n{\n    setSubmitting(<span class=\"hljs-literal\">true</span>);\n    setError(<span class=\"hljs-literal\">null</span>);\n    <span class=\"hljs-keyword\">try</span>\n    {\n        <span class=\"hljs-keyword\">await</span> Task.Delay(<span class=\"hljs-number\">800</span>);                       <span class=\"hljs-comment\">// pretend API call</span>\n        <span class=\"hljs-keyword\">if</span> (pwd == <span class=\"hljs-string\">\"wrong\"</span>) setError(<span class=\"hljs-string\">\"Invalid credentials.\"</span>);\n    }\n    <span class=\"hljs-keyword\">finally</span> { setSubmitting(<span class=\"hljs-literal\">false</span>); }\n}\n</code></pre>\n<p>The submit handler is a local <code>async Task</code> declared inside <code>Render()</code>.\nThat works because <code>Render()</code> runs on the UI thread and Reactor's\nreconciler handles re-rendering when the captured setters fire.\n<a href=\"/hooks/\"><code>UseState</code></a> setters are stable across renders, so the\ncaptured closure keeps working.</p>\n<h3 id=\"render\">Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n    Heading(<span class=\"hljs-string\">\"Sign in\"</span>),\n    TextBox(email, setEmail, placeholderText: <span class=\"hljs-string\">\"you@example.com\"</span>,\n        header: <span class=\"hljs-string\">\"Email\"</span>).Width(<span class=\"hljs-number\">280</span>),\n    PasswordBox(pwd, setPwd, placeholderText: <span class=\"hljs-string\">\"8+ characters\"</span>),\n    error <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n        ? Empty()\n        : TextBlock(error).Foreground(<span class=\"hljs-string\">\"#C42B1C\"</span>),\n    Button(submitting ? <span class=\"hljs-string\">\"Signing in…\"</span> : <span class=\"hljs-string\">\"Sign in\"</span>, () => _ = Submit())\n        .IsEnabled(canSubmit)\n).Padding(<span class=\"hljs-number\">20</span>).Width(<span class=\"hljs-number\">320</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-login/form.png\" alt=\"Login form with inline validation\"></p>\n<p>The <code>canSubmit</code> predicate gates the button on a single render —\ndisabling the button is enough; an analyzer-flagged guard inside\n<code>Submit()</code> would double-fire on a re-render race. The <code>submitting</code>\nstate owns both the spinner label (\"Signing in…\") and the disabled\nstate, so an in-flight submit can't be re-triggered by an Enter press.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Don't reach for a view model.</strong> A 30-line login form doesn't need\none; the three <code>UseState</code> hooks and the local <code>async Task</code> are the\nview model. The cost of a class hierarchy is the cost of maintaining\nit.</p>\n<p><strong>Gate at the button, not inside the handler.</strong> A disabled button is\na single render check; a guard inside <code>Submit()</code> runs after the user\nalready pressed it and the UI looked ready. Both layers are good\nhygiene but the button gate is the load-bearing one.</p>\n<p><strong>Use <a href=\"/forms/\"><code>PasswordBox</code></a>, not <code>TextBox</code> with a hex style.</strong>\nThe control implements paste-without-reveal, autofill, and the\naccessibility peer; reinventing it in user code drops those.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — The full input + validation surface this\nrecipe pulls from.</li>\n<li><strong><a href=\"/effects/\">Effects</a></strong> — Cancellation pattern when the user\nnavigates away mid-submit.</li>\n<li><strong><a href=\"/recipes/modal-dialog/\">Recipe: Modal dialog</a></strong> — Pair this with a\n\"Forgot password?\" modal.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Focus order rules that\napply once you add more fields.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Login # A Microsoft.UI.Reactor (Reactor) login form is four pieces working together: validated input, submit-gated state, an async call, and an error-display surface. The recipe below wires them with three UseState hooks and a Task — no view model, no event handler classes. Primitives # Concern API Per-field state UseState&#x3C;string> / UseState&#x3C;bool> Submit-disabled gating .IsEnabled(canSubmit) Async submit async Task + Task.Delay Error display Conditional Empty() vs TextBlock Password input PasswordBox State # var (email, setEmail) = UseState( \"\" ); var (pwd, setPwd) = UseState( \"\" ); var (submitting, setSubmitting) = UseState( false ); var (error, setError) = UseState&#x3C; string ?>( null ); Four UseState calls — input, password, in-flight flag, and the last error. Nothing else needs to survive a re-render. Per-keystroke validation # // Local validation runs on every keystroke. The submit button is // disabled until the form is valid; in-flight submits are gated // by the same predicate. var emailValid = email.Contains( '@' ) &#x26;&#x26; email.Contains( '.' ); var pwdValid = pwd.Length >= 8 ; var canSubmit = emailValid &#x26;&#x26; pwdValid &#x26;&#x26; !submitting; The form derives emailValid and pwdValid from raw state on every render — no debounce, no separate validation pass. Re-rendering on every keystroke is fine in Reactor; the work happens in pure C# and the reconciler skips slots that didn't change. Async submit + disabled button # async Task Submit () { setSubmitting( true ); setError( null ); try { await Task.Delay( 800 ); // pretend API call if (pwd == \"wrong\" ) setError( \"Invalid credentials.\" ); } finally { setSubmitting( false ); } } The submit handler is a local async Task declared inside Render() . That works because Render() runs on the UI thread and Reactor's reconciler handles re-rendering when the captured setters fire. UseState setters are stable across renders, so the captured closure keeps working. Render # return VStack( 12 , Heading( \"Sign in\" ), TextBox(email, setEmail, placeholderText: \"you@example.com\" , header: \"Email\" ).Width( 280 ), PasswordBox(pwd, setPwd, placeholderText: \"8+ characters\" ), error is null ? Empty() : TextBlock(error).Foreground( \"#C42B1C\" ), Button(submitting ? \"Signing in…\" : \"Sign in\" , () => _ = Submit()) .IsEnabled(canSubmit) ).Padding( 20 ).Width( 320 ); The canSubmit predicate gates the button on a single render — disabling the button is enough; an analyzer-flagged guard inside Submit() would double-fire on a re-render race. The submitting state owns both the spinner label (\"Signing in…\") and the disabled state, so an in-flight submit can't be re-triggered by an Enter press. Tips # Don't reach for a view model. A 30-line login form doesn't need one; the three UseState hooks and the local async Task are the view model. The cost of a class hierarchy is the cost of maintaining it. Gate at the button, not inside the handler. A disabled button is a single render check; a guard inside Submit() runs after the user already pressed it and the UI looked ready. Both layers are good hygiene but the button gate is the load-bearing one. Use PasswordBox , not TextBox with a hex style. The control implements paste-without-reveal, autofill, and the accessibility peer; reinventing it in user code drops those. Next Steps # Forms — The full input + validation surface this recipe pulls from. Effects — Cancellation pattern when the user navigates away mid-submit. Recipe: Modal dialog — Pair this with a \"Forgot password?\" modal. Accessibility — Focus order rules that apply once you add more fields. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/login.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/login.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "state",
          "text": "State"
        },
        {
          "depth": 3,
          "id": "per-keystroke-validation",
          "text": "Per-keystroke validation"
        },
        {
          "depth": 3,
          "id": "async-submit--disabled-button",
          "text": "Async submit + disabled button"
        },
        {
          "depth": 3,
          "id": "render",
          "text": "Render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Master Detail",
      "path": "recipes/master-detail.md",
      "slug": "/recipes/master-detail/",
      "html": "<h1 id=\"recipe-master-detail\">Recipe: Master-detail<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-master-detail\">#</a></h1>\n<p>Master-detail in Microsoft.UI.Reactor (Reactor) is the canonical multi-pane shape: a list of records on\none side, the selected record's details on the other. The whole thing\nis one <code>UseState</code> for the selected id and two slots in an <code>HStack</code>.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Slot</th><th>API</th></tr></thead><tbody><tr><td>Selection state</td><td><code>UseState&#x3C;int?></code></td></tr><tr><td>List render</td><td><code>VStack</code> + <code>Select(...).ToArray()</code></td></tr><tr><td>Selected highlight</td><td>Per-row <code>.Background(...)</code></td></tr><tr><td>Detail branch</td><td><code>Element</code> typed local for null case</td></tr><tr><td>Layout split</td><td><code>HStack(0, list, detail)</code></td></tr></tbody></table>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Note</span>(<span class=\"hljs-title\">int</span> <span class=\"hljs-title\">Id</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Title</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Body</span>);\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NoteBrowser</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Note[] Notes = <span class=\"hljs-keyword\">new</span>[] {\n        <span class=\"hljs-keyword\">new</span> Note(<span class=\"hljs-number\">1</span>, <span class=\"hljs-string\">\"Project plan\"</span>, <span class=\"hljs-string\">\"Draft the milestone sequence; ship before Friday.\"</span>),\n        <span class=\"hljs-keyword\">new</span> Note(<span class=\"hljs-number\">2</span>, <span class=\"hljs-string\">\"Grocery list\"</span>, <span class=\"hljs-string\">\"Bread, olive oil, lemons, parsley, two limes.\"</span>),\n        <span class=\"hljs-keyword\">new</span> Note(<span class=\"hljs-number\">3</span>, <span class=\"hljs-string\">\"Bug triage\"</span>,   <span class=\"hljs-string\">\"Refocus on the persistence regression; defer the WinForms host.\"</span>),\n    };\n</code></pre>\n<p>The data layer is plain C#. A real app pulls notes from\n<a href=\"/data-system/\"><code>IDataSource&#x3C;T></code></a> or an\n<a href=\"/async-resources/\"><code>async-resources</code></a> source; the shape stays\nthe same.</p>\n<h3 id=\"selection-state\">Selection state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#selection-state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Single source of truth for \"which note is selected\" — the list</span>\n<span class=\"hljs-comment\">// writes to it via the button click; the detail pane reads from it.</span>\n<span class=\"hljs-comment\">// Re-renders are scoped to slots that actually changed.</span>\n<span class=\"hljs-keyword\">var</span> (selectedId, setSelectedId) = UseState&#x3C;<span class=\"hljs-built_in\">int</span>?>(<span class=\"hljs-number\">1</span>);\n<span class=\"hljs-keyword\">var</span> selected = Notes.FirstOrDefault(n => n.Id == selectedId);\n</code></pre>\n<p>One <code>UseState&#x3C;int?></code> holds the id; the list writes via a button click,\nthe detail reads via <code>FirstOrDefault</code>. Both slots re-render only when\nthe selection actually changes.</p>\n<h3 id=\"layout\">Layout<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#layout\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> list = VStack(<span class=\"hljs-number\">2</span>,\n    Notes.Select(n =>\n        Button(n.Title, () => setSelectedId(n.Id))\n            .HAlign(Microsoft.UI.Xaml.HorizontalAlignment.Stretch)\n            .Background(n.Id == selectedId ? <span class=\"hljs-string\">\"#E5F1FB\"</span> : <span class=\"hljs-string\">\"#FFFFFF\"</span>)\n    ).ToArray()\n).Width(<span class=\"hljs-number\">200</span>).Padding(<span class=\"hljs-number\">8</span>);\n\nElement detail = selected <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>\n    ? TextBlock(<span class=\"hljs-string\">\"No selection\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>).Padding(<span class=\"hljs-number\">20</span>)\n    : VStack(<span class=\"hljs-number\">8</span>,\n        Heading(selected.Title),\n        TextBlock(selected.Body).Opacity(<span class=\"hljs-number\">0.8</span>)\n    ).Padding(<span class=\"hljs-number\">20</span>);\n\n<span class=\"hljs-keyword\">return</span> HStack(<span class=\"hljs-number\">0</span>, list, detail);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-master-detail/layout.png\" alt=\"Master-detail layout\"></p>\n<p>The list is a <code>VStack</code> of full-width buttons; the selected row gets a\ndistinct background. The detail pane is conditional — <code>selected is null</code>\nrenders the empty state, otherwise the title + body. Both sides are\nplain elements; no intermediate component is needed.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Lift the selection into context only when a third component needs\nit.</strong> A list and a detail pane in the same <code>Render</code> share the\nselection through a local — no <a href=\"/context/\"><code>UseContext</code></a> needed\nuntil a third pane (toolbar, status bar) wants to read or write.</p>\n<p><strong>Pre-resolve the selected record once.</strong> A <code>FirstOrDefault</code> is fine\nfor short lists; for large catalogs put the records in a\n<code>Dictionary&#x3C;int, Note></code> so the lookup is O(1).</p>\n<p><strong>Don't reach for a <code>ListView&#x3C;T></code> for three rows.</strong> The full\ncollection control earns its weight at 50+ rows. A <code>VStack</code> of buttons\nis fine for the recipe-sized case and trivial to read.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Promote to <code>ListView&#x3C;T></code> when\nthe row count grows or each row gets non-trivial.</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — When the detail belongs on its\nown page rather than a sibling pane.</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — Pull the data layer behind an\n<code>IDataSource&#x3C;T></code> once it's a network source.</li>\n<li><strong><a href=\"/recipes/login/\">Recipe: Login</a></strong> — Sibling recipe — adjacent shape but\ndifferent validation surface.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Master-detail # Master-detail in Microsoft.UI.Reactor (Reactor) is the canonical multi-pane shape: a list of records on one side, the selected record's details on the other. The whole thing is one UseState for the selected id and two slots in an HStack . Primitives # Slot API Selection state UseState&#x3C;int?> List render VStack + Select(...).ToArray() Selected highlight Per-row .Background(...) Detail branch Element typed local for null case Layout split HStack(0, list, detail) record Note ( int Id , string Title , string Body ); class NoteBrowser : Component { private static readonly Note[] Notes = new [] { new Note( 1 , \"Project plan\" , \"Draft the milestone sequence; ship before Friday.\" ), new Note( 2 , \"Grocery list\" , \"Bread, olive oil, lemons, parsley, two limes.\" ), new Note( 3 , \"Bug triage\" , \"Refocus on the persistence regression; defer the WinForms host.\" ), }; The data layer is plain C#. A real app pulls notes from IDataSource&#x3C;T> or an async-resources source; the shape stays the same. Selection state # // Single source of truth for \"which note is selected\" — the list // writes to it via the button click; the detail pane reads from it. // Re-renders are scoped to slots that actually changed. var (selectedId, setSelectedId) = UseState&#x3C; int ?>( 1 ); var selected = Notes.FirstOrDefault(n => n.Id == selectedId); One UseState&#x3C;int?> holds the id; the list writes via a button click, the detail reads via FirstOrDefault . Both slots re-render only when the selection actually changes. Layout # var list = VStack( 2 , Notes.Select(n => Button(n.Title, () => setSelectedId(n.Id)) .HAlign(Microsoft.UI.Xaml.HorizontalAlignment.Stretch) .Background(n.Id == selectedId ? \"#E5F1FB\" : \"#FFFFFF\" ) ).ToArray() ).Width( 200 ).Padding( 8 ); Element detail = selected is null ? TextBlock( \"No selection\" ).Opacity( 0.6 ).Padding( 20 ) : VStack( 8 , Heading(selected.Title), TextBlock(selected.Body).Opacity( 0.8 ) ).Padding( 20 ); return HStack( 0 , list, detail); The list is a VStack of full-width buttons; the selected row gets a distinct background. The detail pane is conditional — selected is null renders the empty state, otherwise the title + body. Both sides are plain elements; no intermediate component is needed. Tips # Lift the selection into context only when a third component needs it. A list and a detail pane in the same Render share the selection through a local — no UseContext needed until a third pane (toolbar, status bar) wants to read or write. Pre-resolve the selected record once. A FirstOrDefault is fine for short lists; for large catalogs put the records in a Dictionary&#x3C;int, Note> so the lookup is O(1). Don't reach for a ListView&#x3C;T> for three rows. The full collection control earns its weight at 50+ rows. A VStack of buttons is fine for the recipe-sized case and trivial to read. Next Steps # Collections — Promote to ListView&#x3C;T> when the row count grows or each row gets non-trivial. Navigation — When the detail belongs on its own page rather than a sibling pane. Data System — Pull the data layer behind an IDataSource&#x3C;T> once it's a network source. Recipe: Login — Sibling recipe — adjacent shape but different validation surface. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/master-detail.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/master-detail.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "selection-state",
          "text": "Selection state"
        },
        {
          "depth": 3,
          "id": "layout",
          "text": "Layout"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Modal Dialog",
      "path": "recipes/modal-dialog.md",
      "slug": "/recipes/modal-dialog/",
      "html": "<h1 id=\"recipe-modal-dialog\">Recipe: Modal dialog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-modal-dialog\">#</a></h1>\n<p>The modal is an element you return conditionally. There is no special\n\"open dialog\" API — when <code>open</code> is true the tree includes the modal\npanel layered over the page; when it's false the modal element is\nabsent and the page renders alone.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Open/closed state</td><td><code>UseState&#x3C;bool></code></td></tr><tr><td>Conditional layer</td><td><code>open ? Group(page, modal) : page</code></td></tr><tr><td>Scrim</td><td>Outer <code>Border</code> with semi-transparent fill</td></tr><tr><td>Buttons</td><td><code>Button(label, onClick)</code></td></tr><tr><td>Production wrapper</td><td><a href=\"/dialogs-and-flyouts/\"><code>ContentDialog</code></a></td></tr></tbody></table>\n<h3 id=\"state\">State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n<span class=\"hljs-keyword\">var</span> (deleted, setDeleted) = UseState(<span class=\"hljs-literal\">false</span>);\n</code></pre>\n<p>Two booleans — one for the open/closed flag, one for the recorded\noutcome. The Delete branch flips both; the Cancel branch flips only\n<code>open</code>.</p>\n<h3 id=\"page\">Page<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#page\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The page renders normally; the modal is just another element</span>\n<span class=\"hljs-comment\">// returned conditionally based on `open`.</span>\n<span class=\"hljs-keyword\">var</span> page = VStack(<span class=\"hljs-number\">12</span>,\n    TextBlock(deleted ? <span class=\"hljs-string\">\"Item deleted.\"</span> : <span class=\"hljs-string\">\"1 item selected.\"</span>),\n    Button(<span class=\"hljs-string\">\"Delete…\"</span>, () => setOpen(<span class=\"hljs-literal\">true</span>))\n).Padding(<span class=\"hljs-number\">20</span>);\n</code></pre>\n<p>The page renders normally. The button updates <code>open</code>, which triggers\na re-render that includes the modal.</p>\n<h3 id=\"modal-panel\">Modal panel<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#modal-panel\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Pair the dialog with a scrim (SmokeFill) so clicks outside the</span>\n<span class=\"hljs-comment\">// dialog don't reach the page underneath. The focus trap lives on</span>\n<span class=\"hljs-comment\">// the modal Border.</span>\nElement modal = Border(\n    VStack(<span class=\"hljs-number\">16</span>,\n        Heading(<span class=\"hljs-string\">\"Delete this item?\"</span>),\n        TextBlock(<span class=\"hljs-string\">\"This action cannot be undone.\"</span>).Opacity(<span class=\"hljs-number\">0.8</span>),\n        HStack(<span class=\"hljs-number\">8</span>,\n            Button(<span class=\"hljs-string\">\"Cancel\"</span>, () => setOpen(<span class=\"hljs-literal\">false</span>)),\n            Button(<span class=\"hljs-string\">\"Delete\"</span>, () => { setDeleted(<span class=\"hljs-literal\">true</span>); setOpen(<span class=\"hljs-literal\">false</span>); })\n        ).HAlign(Microsoft.UI.Xaml.HorizontalAlignment.Right)\n    ).Padding(<span class=\"hljs-number\">20</span>).Background(<span class=\"hljs-string\">\"#FFFFFF\"</span>).CornerRadius(<span class=\"hljs-number\">8</span>)\n).Background(<span class=\"hljs-string\">\"#80000000\"</span>).Padding(<span class=\"hljs-number\">40</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-modal-dialog/confirm.png\" alt=\"Delete confirmation modal\"></p>\n<p>The panel is a <code>Border</code> with a colored scrim background. The two\nbuttons close over the same <code>setOpen</code> and <code>setDeleted</code> setters, so\n\"Cancel\" reverts cleanly and \"Delete\" commits the action and dismisses\nthe modal in the same render.</p>\n<p>For a real app, swap the <code>Border</code> for a <code>ContentDialog</code> from\n<a href=\"/dialogs-and-flyouts/\">dialogs-and-flyouts</a> so the focus trap,\nescape-to-cancel, and screen-reader semantics come for free; the\nrecipe above is the same shape with explicit primitives so the\ncomposition is visible.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Render the modal as a sibling, not a child of the trigger.</strong>\nPutting the modal under the button gives it the button's bounding box\nfor layout; rendering it via <code>Group(page, modal)</code> lets the modal\nspan the full host window the way modals are expected to.</p>\n<p><strong>Trap focus on the modal Border in real apps.</strong> The recipe above\nomits focus management for readability — production code should call\n<code>.FocusTrap(...)</code> on the panel so Tab cycles within the dialog. See\n<a href=\"/accessibility/\">accessibility</a> for the full pattern.</p>\n<p><strong>Don't conflate \"open\" with \"saving\".</strong> A confirm + commit modal has\nthree states: closed, open, committing. Add a third <code>UseState&#x3C;bool></code>\nfor the in-flight bit when the commit is async — pattern is the same\nas the <a href=\"/recipes/login/\">login recipe</a>.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/dialogs-and-flyouts/\">Dialogs &#x26; Flyouts</a></strong> — Production\n<code>ContentDialog</code> / <code>MenuFlyout</code> controls.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Focus traps and the\nscreen-reader contract for modals.</li>\n<li><strong><a href=\"/commanding/\">Commanding</a></strong> — Wire the Delete branch to a\nreusable <code>Command&#x3C;T></code> shared with menu and keyboard.</li>\n<li><strong><a href=\"/recipes/login/\">Recipe: Login</a></strong> — Sibling recipe — same pattern for\nin-flight state.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Modal dialog # The modal is an element you return conditionally. There is no special \"open dialog\" API — when open is true the tree includes the modal panel layered over the page; when it's false the modal element is absent and the page renders alone. Primitives # Concern API Open/closed state UseState&#x3C;bool> Conditional layer open ? Group(page, modal) : page Scrim Outer Border with semi-transparent fill Buttons Button(label, onClick) Production wrapper ContentDialog State # var (open, setOpen) = UseState( false ); var (deleted, setDeleted) = UseState( false ); Two booleans — one for the open/closed flag, one for the recorded outcome. The Delete branch flips both; the Cancel branch flips only open . Page # // The page renders normally; the modal is just another element // returned conditionally based on `open`. var page = VStack( 12 , TextBlock(deleted ? \"Item deleted.\" : \"1 item selected.\" ), Button( \"Delete…\" , () => setOpen( true )) ).Padding( 20 ); The page renders normally. The button updates open , which triggers a re-render that includes the modal. Modal panel # // Pair the dialog with a scrim (SmokeFill) so clicks outside the // dialog don't reach the page underneath. The focus trap lives on // the modal Border. Element modal = Border( VStack( 16 , Heading( \"Delete this item?\" ), TextBlock( \"This action cannot be undone.\" ).Opacity( 0.8 ), HStack( 8 , Button( \"Cancel\" , () => setOpen( false )), Button( \"Delete\" , () => { setDeleted( true ); setOpen( false ); }) ).HAlign(Microsoft.UI.Xaml.HorizontalAlignment.Right) ).Padding( 20 ).Background( \"#FFFFFF\" ).CornerRadius( 8 ) ).Background( \"#80000000\" ).Padding( 40 ); The panel is a Border with a colored scrim background. The two buttons close over the same setOpen and setDeleted setters, so \"Cancel\" reverts cleanly and \"Delete\" commits the action and dismisses the modal in the same render. For a real app, swap the Border for a ContentDialog from dialogs-and-flyouts so the focus trap, escape-to-cancel, and screen-reader semantics come for free; the recipe above is the same shape with explicit primitives so the composition is visible. Tips # Render the modal as a sibling, not a child of the trigger. Putting the modal under the button gives it the button's bounding box for layout; rendering it via Group(page, modal) lets the modal span the full host window the way modals are expected to. Trap focus on the modal Border in real apps. The recipe above omits focus management for readability — production code should call .FocusTrap(...) on the panel so Tab cycles within the dialog. See accessibility for the full pattern. Don't conflate \"open\" with \"saving\". A confirm + commit modal has three states: closed, open, committing. Add a third UseState&#x3C;bool> for the in-flight bit when the commit is async — pattern is the same as the login recipe . Next Steps # Dialogs &#x26; Flyouts — Production ContentDialog / MenuFlyout controls. Accessibility — Focus traps and the screen-reader contract for modals. Commanding — Wire the Delete branch to a reusable Command&#x3C;T> shared with menu and keyboard. Recipe: Login — Sibling recipe — same pattern for in-flight state. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/modal-dialog.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/modal-dialog.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "state",
          "text": "State"
        },
        {
          "depth": 3,
          "id": "page",
          "text": "Page"
        },
        {
          "depth": 3,
          "id": "modal-panel",
          "text": "Modal panel"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Multi-step Form",
      "path": "recipes/multi-step-form.md",
      "slug": "/recipes/multi-step-form/",
      "html": "<h1 id=\"recipe-multi-step-form\">Recipe: Multi-Step Form<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-multi-step-form\">#</a></h1>\n<p>A wizard form is a step-index <a href=\"/hooks/\"><code>UseState</code></a> plus a switch\non which step renders. The field state lives in the same component as\nthe step index, so moving forward and back never loses what the user\ntyped — Microsoft.UI.Reactor (Reactor) doesn't unmount the surrounding component when the\nvisible slot changes.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Step index</td><td><code>UseState&#x3C;int></code></td></tr><tr><td>Per-field state</td><td><code>UseState&#x3C;string></code> / <code>UseState&#x3C;int></code> / <code>UseState&#x3C;bool></code></td></tr><tr><td>Step branching</td><td><code>switch</code> on the step index returning <code>Element</code></td></tr><tr><td>Advance gating</td><td><code>.IsEnabled(canAdvance)</code> on the Next button</td></tr><tr><td>Input controls</td><td><a href=\"/forms/\"><code>TextBox</code></a>, <a href=\"/forms/\"><code>RadioButtons</code></a>, <a href=\"/forms/\"><code>CheckBox</code></a></td></tr></tbody></table>\n<h3 id=\"state\">State<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// One UseState for the step index plus one per field. The fields</span>\n<span class=\"hljs-comment\">// are declared at the top of Render so they survive every step</span>\n<span class=\"hljs-comment\">// transition — Reactor never unmounts Wizard, so the hooks keep</span>\n<span class=\"hljs-comment\">// their values as the user moves forward and back.</span>\n<span class=\"hljs-keyword\">var</span> (step, setStep) = UseState(<span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> (email, setEmail) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> (role, setRole) = UseState(<span class=\"hljs-number\">-1</span>);\n<span class=\"hljs-keyword\">var</span> (newsletter, setNewsletter) = UseState(<span class=\"hljs-literal\">false</span>);\n</code></pre>\n<p>The step index and every field hook live at the top of <code>Render</code>. None\nof them get torn down when the visible step changes; only the slot the\n<code>switch</code> returns is swapped. That's why the user can move from step 2\nback to step 1, edit a field, and step 2's selections are still there\nwhen they come forward again.</p>\n<h3 id=\"per-step-validation\">Per-step validation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#per-step-validation\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// canAdvance is a pure function of the step index plus the</span>\n<span class=\"hljs-comment\">// current field values. The Next button binds to it directly;</span>\n<span class=\"hljs-comment\">// no debounce, no separate validation pass.</span>\n<span class=\"hljs-built_in\">bool</span> canAdvance = step <span class=\"hljs-keyword\">switch</span>\n{\n    <span class=\"hljs-number\">0</span> => name.Trim().Length >= <span class=\"hljs-number\">2</span>\n         &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'@'</span>) &#x26;&#x26; email.Contains(<span class=\"hljs-string\">'.'</span>),\n    <span class=\"hljs-number\">1</span> => role >= <span class=\"hljs-number\">0</span>,\n    _ => <span class=\"hljs-literal\">true</span>,\n};\n</code></pre>\n<p><code>canAdvance</code> is a pure function of the current step plus the field\nvalues. It runs on every render — no debounce, no validation pass —\nand the Next button binds to it directly via <code>.IsEnabled(canAdvance)</code>.\nStep 0 wants a name and a plausible email; step 1 wants a role\nselected; step 2 (summary) is always advanceable since the only\nforward action is Submit.</p>\n<h3 id=\"step-bodies\">Step bodies<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-bodies\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\">Element <span class=\"hljs-title\">StepAccount</span>()</span> => VStack(<span class=\"hljs-number\">10</span>,\n    SubHeading(<span class=\"hljs-string\">\"Step 1 of 3 — Account\"</span>),\n    TextBox(name, setName, placeholderText: <span class=\"hljs-string\">\"Your name\"</span>,\n        header: <span class=\"hljs-string\">\"Name\"</span>).Width(<span class=\"hljs-number\">340</span>),\n    TextBox(email, setEmail, placeholderText: <span class=\"hljs-string\">\"you@example.com\"</span>,\n        header: <span class=\"hljs-string\">\"Email\"</span>).Width(<span class=\"hljs-number\">340</span>)\n);\n</code></pre>\n<p>Each step is a local function returning an <code>Element</code>. Account collects\nthe text fields; the second step collects the radio + checkbox:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\">Element <span class=\"hljs-title\">StepPreferences</span>()</span> => VStack(<span class=\"hljs-number\">10</span>,\n    SubHeading(<span class=\"hljs-string\">\"Step 2 of 3 — Preferences\"</span>),\n    TextBlock(<span class=\"hljs-string\">\"Pick the role that fits best.\"</span>).Opacity(<span class=\"hljs-number\">0.7</span>),\n    RadioButtons(<span class=\"hljs-keyword\">new</span>[] { <span class=\"hljs-string\">\"Engineer\"</span>, <span class=\"hljs-string\">\"Designer\"</span>, <span class=\"hljs-string\">\"Manager\"</span> },\n        role, setRole),\n    CheckBox(newsletter, setNewsletter,\n        label: <span class=\"hljs-string\">\"Send me product updates\"</span>)\n);\n</code></pre>\n<p>The locals close over the hook setters at the top of <code>Render</code>, so the\ncontrolled-input contract from <a href=\"/forms/\">Forms</a> just works — value\nin, setter out, same shape as a single-page form.</p>\n<h3 id=\"orchestrator\">Orchestrator<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#orchestrator\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The orchestrator picks which step renders, then lays the</span>\n<span class=\"hljs-comment\">// Back / Next buttons under it. Back is disabled on step 0;</span>\n<span class=\"hljs-comment\">// Next is disabled until canAdvance; the last step swaps the</span>\n<span class=\"hljs-comment\">// Next label to \"Submit\".</span>\nElement body = step <span class=\"hljs-keyword\">switch</span>\n{\n    <span class=\"hljs-number\">0</span> => StepAccount(),\n    <span class=\"hljs-number\">1</span> => StepPreferences(),\n    _ => StepSummary(),\n};\n\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n    Heading(<span class=\"hljs-string\">\"Create your account\"</span>),\n    body,\n    HStack(<span class=\"hljs-number\">8</span>,\n        Button(<span class=\"hljs-string\">\"Back\"</span>, () => setStep(step - <span class=\"hljs-number\">1</span>)).IsEnabled(step != <span class=\"hljs-number\">0</span>),\n        Button(step == <span class=\"hljs-number\">2</span> ? <span class=\"hljs-string\">\"Submit\"</span> : <span class=\"hljs-string\">\"Next\"</span>,\n            () => setStep(step + <span class=\"hljs-number\">1</span>)).IsEnabled(canAdvance &#x26;&#x26; step != <span class=\"hljs-number\">2</span>)\n    )\n).Padding(<span class=\"hljs-number\">20</span>).Width(<span class=\"hljs-number\">380</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-multi-step-form/step1.png\" alt=\"Multi-step form, step one\"></p>\n<p>The orchestrator is a <code>switch</code> over <code>step</code> plus a <code>Back</code> / <code>Next</code> row.\nThe Back button is disabled on step 0; the Next button is disabled\nuntil <code>canAdvance</code>; on the last step the Next button swaps to\n\"Submit\" and grounds out (a real app would call into its commit path\nhere).</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The step index is the whole state machine.</strong> A wizard doesn't need\na routing library, doesn't need a per-step <code>Component</code>, doesn't need\na <code>WizardController</code>. A <code>UseState&#x3C;int></code> plus a <code>switch</code> is the\ncanonical shape; everything else is decoration.</p>\n<p><strong>Declare every field hook at the top, not inside the step.</strong> Hooks\nmust be called in the same order on every render\n(<a href=\"/hooks/\">Hooks rules</a>). Putting <code>UseState</code> inside a step branch\nwould skip the hook on renders where that step isn't active and break\nthe hook ordering. Top-level declarations also guarantee the values\nsurvive step transitions.</p>\n<p><strong>Gate advancement at the button, not inside the handler.</strong> A\ndisabled Next button is a single render check the user can see;\nguarding inside the click handler runs after they already pressed\nsomething the UI made look ready. Disabled-on-render is the\nload-bearing layer.</p>\n<p><strong>Don't add real <a href=\"/navigation/\"><code>Navigation</code></a> for a 3-step\nwizard.</strong> Navigation routes between top-level surfaces (pages,\ndialogs); a wizard is one surface that mutates internally. Reach for\nreal routing only when a step needs its own URL, its own back-stack\nentry, or its own deep link.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — The full input + validation surface each\nstep pulls from.</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — Promote the wizard to real\nrouted pages when a step needs its own URL or back-stack entry.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Why declaring every <code>UseState</code> at the top\nof <code>Render</code> is the load-bearing rule that lets wizard state survive\nstep transitions.</li>\n<li><strong><a href=\"/recipes/login/\">Recipe: Login</a></strong> — Single-step sibling recipe with the\nsame controlled-input contract.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Multi-Step Form # A wizard form is a step-index UseState plus a switch on which step renders. The field state lives in the same component as the step index, so moving forward and back never loses what the user typed — Microsoft.UI.Reactor (Reactor) doesn't unmount the surrounding component when the visible slot changes. Primitives # Concern API Step index UseState&#x3C;int> Per-field state UseState&#x3C;string> / UseState&#x3C;int> / UseState&#x3C;bool> Step branching switch on the step index returning Element Advance gating .IsEnabled(canAdvance) on the Next button Input controls TextBox , RadioButtons , CheckBox State # // One UseState for the step index plus one per field. The fields // are declared at the top of Render so they survive every step // transition — Reactor never unmounts Wizard, so the hooks keep // their values as the user moves forward and back. var (step, setStep) = UseState( 0 ); var (name, setName) = UseState( \"\" ); var (email, setEmail) = UseState( \"\" ); var (role, setRole) = UseState( -1 ); var (newsletter, setNewsletter) = UseState( false ); The step index and every field hook live at the top of Render . None of them get torn down when the visible step changes; only the slot the switch returns is swapped. That's why the user can move from step 2 back to step 1, edit a field, and step 2's selections are still there when they come forward again. Per-step validation # // canAdvance is a pure function of the step index plus the // current field values. The Next button binds to it directly; // no debounce, no separate validation pass. bool canAdvance = step switch { 0 => name.Trim().Length >= 2 &#x26;&#x26; email.Contains( '@' ) &#x26;&#x26; email.Contains( '.' ), 1 => role >= 0 , _ => true , }; canAdvance is a pure function of the current step plus the field values. It runs on every render — no debounce, no validation pass — and the Next button binds to it directly via .IsEnabled(canAdvance) . Step 0 wants a name and a plausible email; step 1 wants a role selected; step 2 (summary) is always advanceable since the only forward action is Submit. Step bodies # Element StepAccount () => VStack( 10 , SubHeading( \"Step 1 of 3 — Account\" ), TextBox(name, setName, placeholderText: \"Your name\" , header: \"Name\" ).Width( 340 ), TextBox(email, setEmail, placeholderText: \"you@example.com\" , header: \"Email\" ).Width( 340 ) ); Each step is a local function returning an Element . Account collects the text fields; the second step collects the radio + checkbox: Element StepPreferences () => VStack( 10 , SubHeading( \"Step 2 of 3 — Preferences\" ), TextBlock( \"Pick the role that fits best.\" ).Opacity( 0.7 ), RadioButtons( new [] { \"Engineer\" , \"Designer\" , \"Manager\" }, role, setRole), CheckBox(newsletter, setNewsletter, label: \"Send me product updates\" ) ); The locals close over the hook setters at the top of Render , so the controlled-input contract from Forms just works — value in, setter out, same shape as a single-page form. Orchestrator # // The orchestrator picks which step renders, then lays the // Back / Next buttons under it. Back is disabled on step 0; // Next is disabled until canAdvance; the last step swaps the // Next label to \"Submit\". Element body = step switch { 0 => StepAccount(), 1 => StepPreferences(), _ => StepSummary(), }; return VStack( 16 , Heading( \"Create your account\" ), body, HStack( 8 , Button( \"Back\" , () => setStep(step - 1 )).IsEnabled(step != 0 ), Button(step == 2 ? \"Submit\" : \"Next\" , () => setStep(step + 1 )).IsEnabled(canAdvance &#x26;&#x26; step != 2 ) ) ).Padding( 20 ).Width( 380 ); The orchestrator is a switch over step plus a Back / Next row. The Back button is disabled on step 0; the Next button is disabled until canAdvance ; on the last step the Next button swaps to \"Submit\" and grounds out (a real app would call into its commit path here). Tips # The step index is the whole state machine. A wizard doesn't need a routing library, doesn't need a per-step Component , doesn't need a WizardController . A UseState&#x3C;int> plus a switch is the canonical shape; everything else is decoration. Declare every field hook at the top, not inside the step. Hooks must be called in the same order on every render ( Hooks rules ). Putting UseState inside a step branch would skip the hook on renders where that step isn't active and break the hook ordering. Top-level declarations also guarantee the values survive step transitions. Gate advancement at the button, not inside the handler. A disabled Next button is a single render check the user can see; guarding inside the click handler runs after they already pressed something the UI made look ready. Disabled-on-render is the load-bearing layer. Don't add real Navigation for a 3-step wizard. Navigation routes between top-level surfaces (pages, dialogs); a wizard is one surface that mutates internally. Reach for real routing only when a step needs its own URL, its own back-stack entry, or its own deep link. Next Steps # Forms — The full input + validation surface each step pulls from. Navigation — Promote the wizard to real routed pages when a step needs its own URL or back-stack entry. Hooks — Why declaring every UseState at the top of Render is the load-bearing rule that lets wizard state survive step transitions. Recipe: Login — Single-step sibling recipe with the same controlled-input contract. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/multi-step-form.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/multi-step-form.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "state",
          "text": "State"
        },
        {
          "depth": 3,
          "id": "per-step-validation",
          "text": "Per-step validation"
        },
        {
          "depth": 3,
          "id": "step-bodies",
          "text": "Step bodies"
        },
        {
          "depth": 3,
          "id": "orchestrator",
          "text": "Orchestrator"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Paginated List",
      "path": "recipes/paginated-list.md",
      "slug": "/recipes/paginated-list/",
      "html": "<h1 id=\"recipe-paginated-list\">Recipe: Paginated List<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-paginated-list\">#</a></h1>\n<p>A paginated list is four states sharing one source of truth: an initial\nload, a populated list, a \"load the next page\" affordance, and a terminal\n\"no more results.\" <a href=\"/async-resources/\"><code>UseInfiniteResource</code></a> owns\nall four in Microsoft.UI.Reactor (Reactor) — the component below pattern-matches on <code>LoadState</code> and reads\n<code>Items</code> directly; there is no local <code>UseState</code> for loading or errors.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Cursor-paginated fetch</td><td><a href=\"/async-resources/\"><code>UseInfiniteResource</code></a></td></tr><tr><td>Page payload + cursor</td><td><a href=\"/async-resources/\"><code>Page&#x3C;TItem, TCursor></code></a></td></tr><tr><td>Lifecycle discriminator</td><td><code>LoadState.Loading / Idle / EndOfList / Error</code></td></tr><tr><td>Flat sparse view</td><td><code>InfiniteResource.Items</code> (nulls are unloaded slots)</td></tr><tr><td>Manual page advance</td><td><code>commits.FetchNext()</code> / <code>commits.Retry()</code></td></tr></tbody></table>\n<h3 id=\"data--fetcher\">Data + fetcher<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data--fetcher\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The recipe is API-shape-agnostic: the fetcher returns a Page&#x3C;TItem, TCursor></span>\n<span class=\"hljs-comment\">// regardless of whether the backend is REST, gRPC, or — as here — an in-process</span>\n<span class=\"hljs-comment\">// fake. The cursor is whatever the server hands back; null signals end-of-list.</span>\n<span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Commit</span>(<span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Sha</span>, <span class=\"hljs-title\">string</span> <span class=\"hljs-title\">Message</span>);\n\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">FakeApi</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Commit[] All = Enumerable.Range(<span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">23</span>)\n        .Select(i => <span class=\"hljs-keyword\">new</span> Commit(<span class=\"hljs-string\">$\"sha-<span class=\"hljs-subst\">{i:<span class=\"hljs-number\">000</span>}</span>\"</span>, <span class=\"hljs-string\">$\"Refactor module <span class=\"hljs-subst\">{i}</span>\"</span>))\n        .ToArray();\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">async</span> Task&#x3C;Page&#x3C;Commit, <span class=\"hljs-built_in\">string</span>>> GetCommitsAsync(<span class=\"hljs-built_in\">string</span>? cursor, CancellationToken ct)\n    {\n        <span class=\"hljs-keyword\">await</span> Task.Delay(<span class=\"hljs-number\">450</span>, ct);              <span class=\"hljs-comment\">// simulate network latency</span>\n        <span class=\"hljs-keyword\">const</span> <span class=\"hljs-built_in\">int</span> pageSize = <span class=\"hljs-number\">5</span>;\n        <span class=\"hljs-built_in\">int</span> offset = cursor <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> ? <span class=\"hljs-number\">0</span> : <span class=\"hljs-built_in\">int</span>.Parse(cursor);\n        <span class=\"hljs-keyword\">var</span> slice = All.Skip(offset).Take(pageSize).ToArray();\n        <span class=\"hljs-built_in\">int</span> next = offset + slice.Length;\n        <span class=\"hljs-built_in\">string</span>? nextCursor = next >= All.Length ? <span class=\"hljs-literal\">null</span> : next.ToString();\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> Page&#x3C;Commit, <span class=\"hljs-built_in\">string</span>>(slice, nextCursor, TotalCount: All.Length);\n    }\n}\n</code></pre>\n<p>The fetcher's only contract is <code>(cursor, ct) -> Task&#x3C;Page&#x3C;TItem, TCursor>></code>.\nThe cursor is opaque to Reactor — pass back whatever your server speaks\n(an offset, an opaque continuation string, a record id). A null\n<code>NextCursor</code> is how you signal end-of-list; that's the only way <code>LoadState</code>\ntransitions to <code>EndOfList</code>.</p>\n<h3 id=\"one-hook-call\">One hook call<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#one-hook-call\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// UseInfiniteResource owns the fetch lifecycle: cancellation on deps-change,</span>\n<span class=\"hljs-comment\">// dedup of in-flight pages, a flat sparse `Items` list (null = unloaded slot),</span>\n<span class=\"hljs-comment\">// and a `LoadState` discriminator the UI pattern-matches against.</span>\n<span class=\"hljs-keyword\">var</span> commits = UseInfiniteResource&#x3C;Commit, <span class=\"hljs-built_in\">string</span>>(\n    fetchPage: (cursor, ct) => FakeApi.GetCommitsAsync(cursor, ct),\n    deps: <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">object</span>[] { <span class=\"hljs-string\">\"commits\"</span> });\n</code></pre>\n<p><code>UseInfiniteResource</code> registers the fetcher, owns the cancellation token,\nand survives re-renders. The <code>deps</code> array is the cache key — change it\n(e.g. when a filter flips) and the hook cancels in-flight pages, drops\nthe page table, and refetches from page 0. See\n<a href=\"/hooks/\"><code>UseState</code></a> when you need a sibling piece of UI state\n(filter, sort) — it pairs naturally because the setter triggers a\nre-render and <code>deps</code> then drives the restart.</p>\n<h3 id=\"derive-ui-state-from-the-resource\">Derive UI state from the resource<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#derive-ui-state-from-the-resource\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// The hook exposes three observable signals the UI cares about:</span>\n<span class=\"hljs-comment\">//   - LoadState  — Loading / Idle / EndOfList / Error</span>\n<span class=\"hljs-comment\">//   - Items      — sparse flat list (null entries are in-flight or unloaded)</span>\n<span class=\"hljs-comment\">//   - HasMore    — false once the server reported a null NextCursor</span>\n<span class=\"hljs-comment\">// Everything below derives from these — no local UseState for \"is loading\"</span>\n<span class=\"hljs-comment\">// or \"did it fail\"; the hook is the source of truth.</span>\n<span class=\"hljs-keyword\">var</span> loadedItems = commits.Items.OfType&#x3C;Commit>().ToArray();\n<span class=\"hljs-keyword\">var</span> isInitialLoad = commits.LoadState <span class=\"hljs-keyword\">is</span> LoadState.Loading &#x26;&#x26; loadedItems.Length == <span class=\"hljs-number\">0</span>;\n<span class=\"hljs-keyword\">var</span> error = commits.LoadState <span class=\"hljs-keyword\">as</span> LoadState.Error;\n<span class=\"hljs-keyword\">var</span> atEnd = commits.LoadState <span class=\"hljs-keyword\">is</span> LoadState.EndOfList;\n<span class=\"hljs-keyword\">var</span> loadingMore = commits.LoadState <span class=\"hljs-keyword\">is</span> LoadState.Loading &#x26;&#x26; loadedItems.Length > <span class=\"hljs-number\">0</span>;\n</code></pre>\n<p>There is no <code>(loading, setLoading)</code> and no <code>(error, setError)</code> in the\ncomponent. <code>LoadState</code> is the discriminator; <code>Items.Count</code> is the\nloaded-anything-yet predicate. Deriving those locally on every render\nis fine — the work is pure C# and the reconciler skips slots that\ndidn't change.</p>\n<h3 id=\"render-the-four-states\">Render the four states<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render-the-four-states\">#</a></h3>\n<pre><code class=\"hljs language-csharp\">Element body;\n<span class=\"hljs-keyword\">if</span> (isInitialLoad)\n{\n    body = TextBlock(<span class=\"hljs-string\">\"Loading…\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>).Padding(<span class=\"hljs-number\">20</span>);\n}\n<span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (error <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> &#x26;&#x26; loadedItems.Length == <span class=\"hljs-number\">0</span>)\n{\n    body = VStack(<span class=\"hljs-number\">8</span>,\n        TextBlock(<span class=\"hljs-string\">$\"Couldn't load commits: <span class=\"hljs-subst\">{error.Exception.Message}</span>\"</span>)\n            .Foreground(<span class=\"hljs-string\">\"#C42B1C\"</span>),\n        Button(<span class=\"hljs-string\">\"Retry\"</span>, () => commits.Retry())\n    ).Padding(<span class=\"hljs-number\">20</span>);\n}\n<span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (loadedItems.Length == <span class=\"hljs-number\">0</span>)\n{\n    body = TextBlock(<span class=\"hljs-string\">\"No commits yet.\"</span>).Opacity(<span class=\"hljs-number\">0.6</span>).Padding(<span class=\"hljs-number\">20</span>);\n}\n<span class=\"hljs-keyword\">else</span>\n{\n    body = VStack(<span class=\"hljs-number\">2</span>,\n        loadedItems.Select(c =>\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBlock(c.Sha).Opacity(<span class=\"hljs-number\">0.5</span>).Width(<span class=\"hljs-number\">72</span>),\n                TextBlock(c.Message)\n            ).Padding(<span class=\"hljs-number\">6</span>)\n        ).ToArray()\n    );\n}\n\n<span class=\"hljs-comment\">// The footer is the load-more sentinel: a button while there's another page,</span>\n<span class=\"hljs-comment\">// a label once the server reported end-of-list, and a Retry on per-page error.</span>\nElement footer = atEnd\n    ? TextBlock(<span class=\"hljs-string\">\"— end of list —\"</span>).Opacity(<span class=\"hljs-number\">0.5</span>).Padding(<span class=\"hljs-number\">12</span>)\n    : error <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> &#x26;&#x26; loadedItems.Length > <span class=\"hljs-number\">0</span>\n        ? Button(<span class=\"hljs-string\">$\"Retry — <span class=\"hljs-subst\">{error.Exception.Message}</span>\"</span>, () => commits.Retry()).Padding(<span class=\"hljs-number\">8</span>)\n        : Button(\n            loadingMore ? <span class=\"hljs-string\">\"Loading more…\"</span> : <span class=\"hljs-string\">$\"Load more (<span class=\"hljs-subst\">{commits.EstimatedRemaining}</span> remaining)\"</span>,\n            () => commits.FetchNext()\n          ).IsEnabled(!loadingMore).Padding(<span class=\"hljs-number\">8</span>);\n\n<span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">0</span>,\n    Heading(<span class=\"hljs-string\">$\"Commits (<span class=\"hljs-subst\">{commits.TotalCount ?? loadedItems.Length}</span>)\"</span>).Padding(<span class=\"hljs-number\">20</span>),\n    body,\n    footer\n).Width(<span class=\"hljs-number\">400</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-paginated-list/loaded.png\" alt=\"Paginated list after two pages and a Load More button\"></p>\n<p>The body branches on three predicates: initial load (skeleton), first-page\nerror with zero loaded items (full-screen retry), or a populated list.\nThe footer is the sentinel — a <code>Button(\"Load more\")</code> while pages remain,\na \"— end of list —\" label once the server reported <code>NextCursor == null</code>,\nor a per-page retry when a follow-up page errored after a successful first\nload. Loaded items survive across re-renders because they live inside the\nhook's <code>Items</code> view, not in local state.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Don't reach for <code>UseState</code> to remember pages.</strong> The hook's <code>Items</code> is\nthe page store. Mirroring it into a local list duplicates state — and the\nlocal copy will go stale across the next deps-change refetch.</p>\n<p><strong>Cursor paging is serial; offset paging is parallel.</strong> Cursor mode\nchains fetches because page <em>N</em>'s cursor lives in page <em>N-1</em>. If your\nserver speaks offsets, use the <code>cursorFromPageIndex</code> parameter on\n<a href=\"/async-resources/\"><code>UseInfiniteResource</code></a> so deep scrolls fetch\npages concurrently.</p>\n<p><strong>Promote to a virtualizer at row counts that matter.</strong> A button-driven\nload-more is right for ~5-200 rows. Once the list grows past the\nviewport's worth of pages, drive fetches from a virtualizer's\n<code>ItemAt(i)</code> (see <a href=\"/collections/\">Collections</a>) — the same hook\nbacks both flows.</p>\n<p><strong>Distinguish first-page error from follow-up error.</strong> A failure on the\ninitial fetch should take the whole list area; a failure loading page 3\nshould only replace the footer button. The recipe branches on\n<code>loadedItems.Length</code> to choose between them.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — Full state-machine\nreference for <code>UseInfiniteResource</code>, including <code>Refresh()</code> and the\nPending fallback story.</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Promote the recipe's\n<code>VStack</code>-of-rows to a virtualized <code>ListView&#x3C;T></code> when the page count\noutpaces the viewport.</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — When the source already speaks\n<code>IDataSource&#x3C;T></code>, use <code>UseDataSource</code> to bridge onto this hook with\noffset-based parallel paging.</li>\n<li><strong><a href=\"/recipes/master-detail/\">Recipe: Master-detail</a></strong> — Sibling recipe — pair\nthis list with a detail pane by lifting <code>selectedId</code> into a parent\ncomponent.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Paginated List # A paginated list is four states sharing one source of truth: an initial load, a populated list, a \"load the next page\" affordance, and a terminal \"no more results.\" UseInfiniteResource owns all four in Microsoft.UI.Reactor (Reactor) — the component below pattern-matches on LoadState and reads Items directly; there is no local UseState for loading or errors. Primitives # Concern API Cursor-paginated fetch UseInfiniteResource Page payload + cursor Page&#x3C;TItem, TCursor> Lifecycle discriminator LoadState.Loading / Idle / EndOfList / Error Flat sparse view InfiniteResource.Items (nulls are unloaded slots) Manual page advance commits.FetchNext() / commits.Retry() Data + fetcher # // The recipe is API-shape-agnostic: the fetcher returns a Page&#x3C;TItem, TCursor> // regardless of whether the backend is REST, gRPC, or — as here — an in-process // fake. The cursor is whatever the server hands back; null signals end-of-list. record Commit ( string Sha , string Message ); static class FakeApi { private static readonly Commit[] All = Enumerable.Range( 0 , 23 ) .Select(i => new Commit( $\"sha- {i: 000 } \" , $\"Refactor module {i} \" )) .ToArray(); public static async Task&#x3C;Page&#x3C;Commit, string >> GetCommitsAsync( string ? cursor, CancellationToken ct) { await Task.Delay( 450 , ct); // simulate network latency const int pageSize = 5 ; int offset = cursor is null ? 0 : int .Parse(cursor); var slice = All.Skip(offset).Take(pageSize).ToArray(); int next = offset + slice.Length; string ? nextCursor = next >= All.Length ? null : next.ToString(); return new Page&#x3C;Commit, string >(slice, nextCursor, TotalCount: All.Length); } } The fetcher's only contract is (cursor, ct) -> Task&#x3C;Page&#x3C;TItem, TCursor>> . The cursor is opaque to Reactor — pass back whatever your server speaks (an offset, an opaque continuation string, a record id). A null NextCursor is how you signal end-of-list; that's the only way LoadState transitions to EndOfList . One hook call # // UseInfiniteResource owns the fetch lifecycle: cancellation on deps-change, // dedup of in-flight pages, a flat sparse `Items` list (null = unloaded slot), // and a `LoadState` discriminator the UI pattern-matches against. var commits = UseInfiniteResource&#x3C;Commit, string >( fetchPage: (cursor, ct) => FakeApi.GetCommitsAsync(cursor, ct), deps: new object [] { \"commits\" }); UseInfiniteResource registers the fetcher, owns the cancellation token, and survives re-renders. The deps array is the cache key — change it (e.g. when a filter flips) and the hook cancels in-flight pages, drops the page table, and refetches from page 0. See UseState when you need a sibling piece of UI state (filter, sort) — it pairs naturally because the setter triggers a re-render and deps then drives the restart. Derive UI state from the resource # // The hook exposes three observable signals the UI cares about: // - LoadState — Loading / Idle / EndOfList / Error // - Items — sparse flat list (null entries are in-flight or unloaded) // - HasMore — false once the server reported a null NextCursor // Everything below derives from these — no local UseState for \"is loading\" // or \"did it fail\"; the hook is the source of truth. var loadedItems = commits.Items.OfType&#x3C;Commit>().ToArray(); var isInitialLoad = commits.LoadState is LoadState.Loading &#x26;&#x26; loadedItems.Length == 0 ; var error = commits.LoadState as LoadState.Error; var atEnd = commits.LoadState is LoadState.EndOfList; var loadingMore = commits.LoadState is LoadState.Loading &#x26;&#x26; loadedItems.Length > 0 ; There is no (loading, setLoading) and no (error, setError) in the component. LoadState is the discriminator; Items.Count is the loaded-anything-yet predicate. Deriving those locally on every render is fine — the work is pure C# and the reconciler skips slots that didn't change. Render the four states # Element body; if (isInitialLoad) { body = TextBlock( \"Loading…\" ).Opacity( 0.6 ).Padding( 20 ); } else if (error is not null &#x26;&#x26; loadedItems.Length == 0 ) { body = VStack( 8 , TextBlock( $\"Couldn't load commits: {error.Exception.Message} \" ) .Foreground( \"#C42B1C\" ), Button( \"Retry\" , () => commits.Retry()) ).Padding( 20 ); } else if (loadedItems.Length == 0 ) { body = TextBlock( \"No commits yet.\" ).Opacity( 0.6 ).Padding( 20 ); } else { body = VStack( 2 , loadedItems.Select(c => HStack( 8 , TextBlock(c.Sha).Opacity( 0.5 ).Width( 72 ), TextBlock(c.Message) ).Padding( 6 ) ).ToArray() ); } // The footer is the load-more sentinel: a button while there's another page, // a label once the server reported end-of-list, and a Retry on per-page error. Element footer = atEnd ? TextBlock( \"— end of list —\" ).Opacity( 0.5 ).Padding( 12 ) : error is not null &#x26;&#x26; loadedItems.Length > 0 ? Button( $\"Retry — {error.Exception.Message} \" , () => commits.Retry()).Padding( 8 ) : Button( loadingMore ? \"Loading more…\" : $\"Load more ( {commits.EstimatedRemaining} remaining)\" , () => commits.FetchNext() ).IsEnabled(!loadingMore).Padding( 8 ); return VStack( 0 , Heading( $\"Commits ( {commits.TotalCount ?? loadedItems.Length} )\" ).Padding( 20 ), body, footer ).Width( 400 ); The body branches on three predicates: initial load (skeleton), first-page error with zero loaded items (full-screen retry), or a populated list. The footer is the sentinel — a Button(\"Load more\") while pages remain, a \"— end of list —\" label once the server reported NextCursor == null , or a per-page retry when a follow-up page errored after a successful first load. Loaded items survive across re-renders because they live inside the hook's Items view, not in local state. Tips # Don't reach for UseState to remember pages. The hook's Items is the page store. Mirroring it into a local list duplicates state — and the local copy will go stale across the next deps-change refetch. Cursor paging is serial; offset paging is parallel. Cursor mode chains fetches because page N 's cursor lives in page N-1 . If your server speaks offsets, use the cursorFromPageIndex parameter on UseInfiniteResource so deep scrolls fetch pages concurrently. Promote to a virtualizer at row counts that matter. A button-driven load-more is right for ~5-200 rows. Once the list grows past the viewport's worth of pages, drive fetches from a virtualizer's ItemAt(i) (see Collections ) — the same hook backs both flows. Distinguish first-page error from follow-up error. A failure on the initial fetch should take the whole list area; a failure loading page 3 should only replace the footer button. The recipe branches on loadedItems.Length to choose between them. Next Steps # Async Resources — Full state-machine reference for UseInfiniteResource , including Refresh() and the Pending fallback story. Collections — Promote the recipe's VStack -of-rows to a virtualized ListView&#x3C;T> when the page count outpaces the viewport. Data System — When the source already speaks IDataSource&#x3C;T> , use UseDataSource to bridge onto this hook with offset-based parallel paging. Recipe: Master-detail — Sibling recipe — pair this list with a detail pane by lifting selectedId into a parent component. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/paginated-list.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/paginated-list.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "data--fetcher",
          "text": "Data + fetcher"
        },
        {
          "depth": 3,
          "id": "one-hook-call",
          "text": "One hook call"
        },
        {
          "depth": 3,
          "id": "derive-ui-state-from-the-resource",
          "text": "Derive UI state from the resource"
        },
        {
          "depth": 3,
          "id": "render-the-four-states",
          "text": "Render the four states"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Search with Suggestions",
      "path": "recipes/search-with-suggestions.md",
      "slug": "/recipes/search-with-suggestions/",
      "html": "<h1 id=\"recipe-search-with-suggestions\">Recipe: Search with suggestions<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-search-with-suggestions\">#</a></h1>\n<p>The pattern is two slots: an input that owns the query string, and a\ndropdown that shows zero or more suggestions filtered against a\ncatalog. <code>UseMemo</code> keys the filter on the query, so a re-render\nthat didn't touch the query is free — important for the keyboard-up-\narrow-down-arrow loop where the suggestion index changes but the\nquery doesn't.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Query state</td><td><code>UseState&#x3C;string></code></td></tr><tr><td>Filter caching</td><td><code>UseMemo&#x3C;T[]></code> keyed on the query</td></tr><tr><td>Dropdown render</td><td><code>Border</code> wrapping <code>VStack</code></td></tr><tr><td>Production wrapper</td><td><a href=\"/forms/\"><code>AutoSuggestBox</code></a></td></tr><tr><td>Async source</td><td><a href=\"/async-resources/\"><code>async-resources</code></a></td></tr></tbody></table>\n<h3 id=\"catalog\">Catalog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#catalog\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SearchBox</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span>[] Catalog = <span class=\"hljs-keyword\">new</span>[] {\n        <span class=\"hljs-string\">\"Account settings\"</span>, <span class=\"hljs-string\">\"Accessibility\"</span>, <span class=\"hljs-string\">\"Animation\"</span>,\n        <span class=\"hljs-string\">\"Buttons\"</span>, <span class=\"hljs-string\">\"Backdrop\"</span>, <span class=\"hljs-string\">\"Charts\"</span>,\n        <span class=\"hljs-string\">\"Components\"</span>, <span class=\"hljs-string\">\"Commanding\"</span>, <span class=\"hljs-string\">\"Context\"</span>,\n        <span class=\"hljs-string\">\"Effects\"</span>, <span class=\"hljs-string\">\"Forms\"</span>, <span class=\"hljs-string\">\"Hooks\"</span>,\n        <span class=\"hljs-string\">\"Localization\"</span>, <span class=\"hljs-string\">\"Navigation\"</span>, <span class=\"hljs-string\">\"Persistence\"</span>,\n        <span class=\"hljs-string\">\"Styling\"</span>, <span class=\"hljs-string\">\"Testing\"</span>, <span class=\"hljs-string\">\"Theming tokens\"</span>,\n    };\n</code></pre>\n<p>A static array stands in for the data source. A real app reaches for\n<a href=\"/async-resources/\"><code>async-resources</code></a> once the catalog is on\nthe network.</p>\n<h3 id=\"filter\">Filter<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#filter\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// UseMemo on the dependency array means the filter runs only when</span>\n<span class=\"hljs-comment\">// query changes — typing fast doesn't refilter mid-keystroke, and</span>\n<span class=\"hljs-comment\">// a re-render that didn't touch the query is free.</span>\n<span class=\"hljs-keyword\">var</span> (query, setQuery) = UseState(<span class=\"hljs-string\">\"\"</span>);\n<span class=\"hljs-keyword\">var</span> suggestions = UseMemo(\n    () => <span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(query)\n        ? <span class=\"hljs-keyword\">new</span> <span class=\"hljs-built_in\">string</span>[<span class=\"hljs-number\">0</span>]\n        : Catalog.Where(c => c.Contains(query,\n            System.StringComparison.OrdinalIgnoreCase))\n            .Take(<span class=\"hljs-number\">5</span>).ToArray(),\n    query);\n</code></pre>\n<p><code>UseMemo</code> caches the filtered array between renders that don't change\n<code>query</code>. The empty-query branch returns an empty array so the\ndropdown closes — collapsing the suggestion surface when the user\nclears the input is essential.</p>\n<h3 id=\"render\">Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n    TextBox(query, setQuery, placeholderText: <span class=\"hljs-string\">\"Search topics…\"</span>).Width(<span class=\"hljs-number\">300</span>),\n    suggestions.Length == <span class=\"hljs-number\">0</span>\n        ? Empty()\n        : Border(\n            VStack(<span class=\"hljs-number\">2</span>,\n                suggestions.Select(s =>\n                    TextBlock(s).Padding(<span class=\"hljs-number\">8</span>)).ToArray()\n            ).Background(<span class=\"hljs-string\">\"#FFFFFF\"</span>)\n        ).WithBorder(<span class=\"hljs-string\">\"#E0E0E0\"</span>).Width(<span class=\"hljs-number\">300</span>)\n).Padding(<span class=\"hljs-number\">20</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-search-with-suggestions/search.png\" alt=\"Suggestion dropdown\"></p>\n<p>The dropdown is a <code>Border</code> wrapping a <code>VStack</code> — same conditional\nrender shape as the <a href=\"/recipes/modal-dialog/\">modal-dialog recipe</a>. When\n<code>suggestions.Length == 0</code>, the <code>Empty()</code> factory drops the dropdown\nout of the tree entirely.</p>\n<p>For a production search box, swap the manual dropdown for\n<a href=\"/forms/\"><code>AutoSuggestBox</code></a> — it wires keyboard navigation,\nselection events, and the screen-reader contract. The recipe shows\nthe primitives so the composition is visible.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Cap the suggestion count.</strong> <code>Take(5)</code> here; the catalog can be huge\nbut the dropdown only ever shows a handful. An open-ended list\nforces a <code>ScrollView</code>, which forces keyboard handling, which forces\nfocus management — all real work to avoid for the common case.</p>\n<p><strong><code>UseMemo</code> is the load-bearing call.</strong> Without it, a slow filter\nruns on every render — including renders triggered by the\nsuggestion-keyboard navigation that didn't change the query. With\nit, the filter runs only when the query actually changes.</p>\n<p><strong>Plan for async early.</strong> A client-side filter is one line; a network\nfilter needs cancellation, debounce, and an error pane. The\n<a href=\"/async-resources/\"><code>async-resources</code></a> page covers the upgrade\npath when the catalog moves off the local machine.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — Promote to <code>AutoSuggestBox</code> once you\nneed keyboard nav and the accessibility peer.</li>\n<li><strong><a href=\"/async-resources/\">Async Resources</a></strong> — Network-backed\nsuggestions with cancellation.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — <code>UseMemo</code> semantics — when the cache\ninvalidates vs. survives.</li>\n<li><strong><a href=\"/recipes/master-detail/\">Recipe: Master-detail</a></strong> — Pair the search box\nwith a master list of matched records.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Search with suggestions # The pattern is two slots: an input that owns the query string, and a dropdown that shows zero or more suggestions filtered against a catalog. UseMemo keys the filter on the query, so a re-render that didn't touch the query is free — important for the keyboard-up- arrow-down-arrow loop where the suggestion index changes but the query doesn't. Primitives # Concern API Query state UseState&#x3C;string> Filter caching UseMemo&#x3C;T[]> keyed on the query Dropdown render Border wrapping VStack Production wrapper AutoSuggestBox Async source async-resources Catalog # class SearchBox : Component { private static readonly string [] Catalog = new [] { \"Account settings\" , \"Accessibility\" , \"Animation\" , \"Buttons\" , \"Backdrop\" , \"Charts\" , \"Components\" , \"Commanding\" , \"Context\" , \"Effects\" , \"Forms\" , \"Hooks\" , \"Localization\" , \"Navigation\" , \"Persistence\" , \"Styling\" , \"Testing\" , \"Theming tokens\" , }; A static array stands in for the data source. A real app reaches for async-resources once the catalog is on the network. Filter # // UseMemo on the dependency array means the filter runs only when // query changes — typing fast doesn't refilter mid-keystroke, and // a re-render that didn't touch the query is free. var (query, setQuery) = UseState( \"\" ); var suggestions = UseMemo( () => string .IsNullOrWhiteSpace(query) ? new string [ 0 ] : Catalog.Where(c => c.Contains(query, System.StringComparison.OrdinalIgnoreCase)) .Take( 5 ).ToArray(), query); UseMemo caches the filtered array between renders that don't change query . The empty-query branch returns an empty array so the dropdown closes — collapsing the suggestion surface when the user clears the input is essential. Render # return VStack( 8 , TextBox(query, setQuery, placeholderText: \"Search topics…\" ).Width( 300 ), suggestions.Length == 0 ? Empty() : Border( VStack( 2 , suggestions.Select(s => TextBlock(s).Padding( 8 )).ToArray() ).Background( \"#FFFFFF\" ) ).WithBorder( \"#E0E0E0\" ).Width( 300 ) ).Padding( 20 ); The dropdown is a Border wrapping a VStack — same conditional render shape as the modal-dialog recipe . When suggestions.Length == 0 , the Empty() factory drops the dropdown out of the tree entirely. For a production search box, swap the manual dropdown for AutoSuggestBox — it wires keyboard navigation, selection events, and the screen-reader contract. The recipe shows the primitives so the composition is visible. Tips # Cap the suggestion count. Take(5) here; the catalog can be huge but the dropdown only ever shows a handful. An open-ended list forces a ScrollView , which forces keyboard handling, which forces focus management — all real work to avoid for the common case. UseMemo is the load-bearing call. Without it, a slow filter runs on every render — including renders triggered by the suggestion-keyboard navigation that didn't change the query. With it, the filter runs only when the query actually changes. Plan for async early. A client-side filter is one line; a network filter needs cancellation, debounce, and an error pane. The async-resources page covers the upgrade path when the catalog moves off the local machine. Next Steps # Forms — Promote to AutoSuggestBox once you need keyboard nav and the accessibility peer. Async Resources — Network-backed suggestions with cancellation. Hooks — UseMemo semantics — when the cache invalidates vs. survives. Recipe: Master-detail — Pair the search box with a master list of matched records. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/search-with-suggestions.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/search-with-suggestions.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "catalog",
          "text": "Catalog"
        },
        {
          "depth": 3,
          "id": "filter",
          "text": "Filter"
        },
        {
          "depth": 3,
          "id": "render",
          "text": "Render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Settings Page",
      "path": "recipes/settings-page.md",
      "slug": "/recipes/settings-page/",
      "html": "<h1 id=\"recipe-settings-page\">Recipe: Settings page<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#recipe-settings-page\">#</a></h1>\n<p>A settings page is many small persisted prefs side-by-side. Each\npreference is its own <code>UsePersisted</code> call with its own key — there's\nno central settings object that needs migration when one preference\nmoves. The row layout is a single helper.</p>\n<h2 id=\"primitives\">Primitives<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#primitives\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concern</th><th>API</th></tr></thead><tbody><tr><td>Per-pref storage</td><td><code>UsePersisted&#x3C;T>(key, initial, scope)</code></td></tr><tr><td>Scope</td><td><code>PersistedScope.Window</code> / <code>Application</code></td></tr><tr><td>Toggle</td><td><code>ToggleSwitch(isOn, setOn)</code></td></tr><tr><td>Choice</td><td><code>ComboBox(items, index, setIndex)</code></td></tr><tr><td>Range</td><td><code>Slider(value, min, max, setValue)</code></td></tr><tr><td>Row layout helper</td><td><code>static Element SettingsRow(...)</code></td></tr></tbody></table>\n<h3 id=\"persisted-state\">Persisted state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#persisted-state\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Each preference is a separate UsePersisted call with its own key.</span>\n<span class=\"hljs-comment\">// The window scope ties the values to the host's lifetime; flip to</span>\n<span class=\"hljs-comment\">// PersistedScope.Application when the prefs should survive across</span>\n<span class=\"hljs-comment\">// windows.</span>\n<span class=\"hljs-keyword\">var</span> (notify, setNotify) = UsePersisted(<span class=\"hljs-string\">\"prefs/notify\"</span>, <span class=\"hljs-literal\">true</span>,\n    PersistedScope.Window);\n<span class=\"hljs-keyword\">var</span> (theme, setTheme) = UsePersisted(<span class=\"hljs-string\">\"prefs/theme\"</span>, <span class=\"hljs-number\">0</span>,\n    PersistedScope.Window);\n<span class=\"hljs-keyword\">var</span> (volume, setVolume) = UsePersisted(<span class=\"hljs-string\">\"prefs/volume\"</span>, <span class=\"hljs-number\">60.0</span>,\n    PersistedScope.Window);\n</code></pre>\n<p>Three preferences, three keys. Each pref renders + persists\nindependently, so adding a fourth is one line in <code>Render()</code> + one\nmatching control. The <code>PersistedScope.Window</code> scope keeps the values\ninside the host window — flip to <code>PersistedScope.Application</code> for\nprocess-wide preferences (auth, locale, theme) per the\n<a href=\"/persistence/\">persistence</a> page.</p>\n<h3 id=\"render\">Render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n    Heading(<span class=\"hljs-string\">\"Settings\"</span>),\n    SettingsRow(<span class=\"hljs-string\">\"Notifications\"</span>,\n        ToggleSwitch(notify, setNotify)),\n    SettingsRow(<span class=\"hljs-string\">\"Theme\"</span>,\n        ComboBox([<span class=\"hljs-string\">\"System\"</span>, <span class=\"hljs-string\">\"Light\"</span>, <span class=\"hljs-string\">\"Dark\"</span>], theme, setTheme)),\n    SettingsRow(<span class=\"hljs-string\">\"Volume\"</span>,\n        Slider(volume, <span class=\"hljs-number\">0</span>, <span class=\"hljs-number\">100</span>, setVolume).Width(<span class=\"hljs-number\">200</span>))\n).Padding(<span class=\"hljs-number\">20</span>);\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/recipe-settings-page/settings.png\" alt=\"Settings page\"></p>\n<p>A <code>VStack</code> of <code>SettingsRow</code>s; each row is a label + control. Reactor\nre-renders only the row whose state changed — the slider doesn't\nre-mount when you toggle notifications.</p>\n<h3 id=\"row-helper\">Row helper<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#row-helper\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// A `SettingsRow` is a label + control — two slots in an HStack with a</span>\n<span class=\"hljs-comment\">// fixed-width label so the controls line up across rows.</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">SettingsRow</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> label, Element control</span>)</span> =>\n    HStack(<span class=\"hljs-number\">16</span>,\n        TextBlock(label).Width(<span class=\"hljs-number\">120</span>),\n        control\n    );\n</code></pre>\n<p>A fixed-width label keeps the controls aligned across rows. The helper\nis a private static method, not a <code>Component</code> — it has no state, so a\nfunction returning an <code>Element</code> is the right shape.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Use one key per pref, not one big record.</strong> The single-key approach\navoids the <a href=\"/persistence/\">versioned-shape migration</a> story for\nthe common case of adding a pref. If two prefs are genuinely\ncorrelated (e.g. <code>theme</code> + <code>accentColor</code>), keep them in one record;\notherwise let them live alone.</p>\n<p><strong>Don't reach for <code>UseReducer</code> here.</strong> The Redux-style reducer is the\nright shape for state with cross-field invariants; a settings page is\nthe opposite case — each pref is independent.</p>\n<p><strong>Promote to <code>Application</code> scope only when prefs need to outlive the\nwindow.</strong> Window scope is the safer default; an Application-scoped\nkey shared across windows is a coordination problem the\n<a href=\"/persistence/\">persistence</a> page covers in detail.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — Scope choice, migration story,\nand the disk-bridge pattern when prefs must survive a restart.</li>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — Validation surface to apply to settings\nwith constraints (port numbers, file paths).</li>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — Wire the \"Theme\" pref to actual theme\nswitching via <code>ApplicationTheme</code>.</li>\n<li><strong><a href=\"/recipes/login/\">Recipe: Login</a></strong> — Sibling recipe for input-driven\nforms.</li>\n<li><strong><a href=\"/recipes/\">Recipes index</a></strong> — Back to the gallery.</li>\n</ul>",
      "text": "Recipe: Settings page # A settings page is many small persisted prefs side-by-side. Each preference is its own UsePersisted call with its own key — there's no central settings object that needs migration when one preference moves. The row layout is a single helper. Primitives # Concern API Per-pref storage UsePersisted&#x3C;T>(key, initial, scope) Scope PersistedScope.Window / Application Toggle ToggleSwitch(isOn, setOn) Choice ComboBox(items, index, setIndex) Range Slider(value, min, max, setValue) Row layout helper static Element SettingsRow(...) Persisted state # // Each preference is a separate UsePersisted call with its own key. // The window scope ties the values to the host's lifetime; flip to // PersistedScope.Application when the prefs should survive across // windows. var (notify, setNotify) = UsePersisted( \"prefs/notify\" , true , PersistedScope.Window); var (theme, setTheme) = UsePersisted( \"prefs/theme\" , 0 , PersistedScope.Window); var (volume, setVolume) = UsePersisted( \"prefs/volume\" , 60.0 , PersistedScope.Window); Three preferences, three keys. Each pref renders + persists independently, so adding a fourth is one line in Render() + one matching control. The PersistedScope.Window scope keeps the values inside the host window — flip to PersistedScope.Application for process-wide preferences (auth, locale, theme) per the persistence page. Render # return VStack( 16 , Heading( \"Settings\" ), SettingsRow( \"Notifications\" , ToggleSwitch(notify, setNotify)), SettingsRow( \"Theme\" , ComboBox([ \"System\" , \"Light\" , \"Dark\" ], theme, setTheme)), SettingsRow( \"Volume\" , Slider(volume, 0 , 100 , setVolume).Width( 200 )) ).Padding( 20 ); A VStack of SettingsRow s; each row is a label + control. Reactor re-renders only the row whose state changed — the slider doesn't re-mount when you toggle notifications. Row helper # // A `SettingsRow` is a label + control — two slots in an HStack with a // fixed-width label so the controls line up across rows. private static Element SettingsRow ( string label, Element control ) => HStack( 16 , TextBlock(label).Width( 120 ), control ); A fixed-width label keeps the controls aligned across rows. The helper is a private static method, not a Component — it has no state, so a function returning an Element is the right shape. Tips # Use one key per pref, not one big record. The single-key approach avoids the versioned-shape migration story for the common case of adding a pref. If two prefs are genuinely correlated (e.g. theme + accentColor ), keep them in one record; otherwise let them live alone. Don't reach for UseReducer here. The Redux-style reducer is the right shape for state with cross-field invariants; a settings page is the opposite case — each pref is independent. Promote to Application scope only when prefs need to outlive the window. Window scope is the safer default; an Application-scoped key shared across windows is a coordination problem the persistence page covers in detail. Next Steps # Persistence — Scope choice, migration story, and the disk-bridge pattern when prefs must survive a restart. Forms — Validation surface to apply to settings with constraints (port numbers, file paths). Styling — Wire the \"Theme\" pref to actual theme switching via ApplicationTheme . Recipe: Login — Sibling recipe for input-driven forms. Recipes index — Back to the gallery.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/recipes/settings-page.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/recipes/settings-page.md",
      "headings": [
        {
          "depth": 2,
          "id": "primitives",
          "text": "Primitives"
        },
        {
          "depth": 3,
          "id": "persisted-state",
          "text": "Persisted state"
        },
        {
          "depth": 3,
          "id": "render",
          "text": "Render"
        },
        {
          "depth": 3,
          "id": "row-helper",
          "text": "Row helper"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Cheat Sheet",
      "path": "cheat-sheet.md",
      "slug": "/cheat-sheet/",
      "html": "<h1 id=\"cheat-sheet\">Cheat Sheet<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#cheat-sheet\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor) at a glance. Every row links to the page that covers it in\ndepth.</p>\n<h2 id=\"minimum-viable-app\">Minimum viable app<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#minimum-viable-app\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HelloVignette</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        VStack(<span class=\"hljs-number\">8</span>,\n            Heading(<span class=\"hljs-string\">\"Hello\"</span>),\n            Button(<span class=\"hljs-string\">\"Click\"</span>, () => { })\n        ).Padding(<span class=\"hljs-number\">20</span>);\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/cheat-sheet/vignettes.png\" alt=\"Cheat-sheet vignettes — Hello, State, Effect\"></p>\n<p>Three pieces: a <code>using static Microsoft.UI.Reactor.Factories</code> import,\na <code>Component</code> subclass, and a <code>ReactorApp.Run&#x3C;App>(title, ...)</code> call.\nAdd <code>#if DEBUG preview: true #endif</code> so <code>mur</code> can capture screenshots.</p>\n<h2 id=\"hooks\">Hooks<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Hook</th><th>Returns</th><th>Use when</th></tr></thead><tbody><tr><td><code>UseState&#x3C;T>(initial)</code></td><td><code>(T, Action&#x3C;T>)</code></td><td>Local state that drives re-render.</td></tr><tr><td><code>UseReducer&#x3C;T>(initial)</code></td><td><code>(T, Action&#x3C;Func&#x3C;T,T>>)</code></td><td>Functional updates from prev.</td></tr><tr><td><code>UseReducer&#x3C;S,A>(reducer, initial)</code></td><td><code>(S, Action&#x3C;A>)</code></td><td>Redux-style state.</td></tr><tr><td><code>UseEffect(setup, deps?)</code></td><td>void</td><td>Side effects after render; cleanup on re-run.</td></tr><tr><td><code>UseMemo&#x3C;T>(factory, deps)</code></td><td><code>T</code></td><td>Cache expensive computation across renders.</td></tr><tr><td><code>UseCallback(fn, deps)</code></td><td>stable delegate</td><td>Memoize a callback identity.</td></tr><tr><td><code>UseRef&#x3C;T>(initial)</code></td><td><code>Ref&#x3C;T></code></td><td>Mutable value that does <em>not</em> trigger re-render.</td></tr><tr><td><code>UseContext&#x3C;T>(ctx)</code></td><td><code>T</code></td><td>Read a <a href=\"/context/\">context</a> value.</td></tr><tr><td><code>UsePersisted&#x3C;T>(key, initial, scope)</code></td><td><code>(T, Action&#x3C;T>)</code></td><td>Survive re-mount via <a href=\"/persistence/\">LRU cache</a>.</td></tr><tr><td><code>UseColorScheme()</code></td><td><code>ColorScheme</code></td><td>Reactive light/dark/HighContrast.</td></tr><tr><td><code>UseAsync(factory, deps)</code></td><td><code>AsyncResource&#x3C;T></code></td><td><a href=\"/async-resources/\"><code>async-resources</code></a>.</td></tr><tr><td><code>UseFocusTrap()</code></td><td><code>FocusTrapHandle</code></td><td>Trap Tab inside a sub-tree.</td></tr><tr><td><code>UseAnnounce()</code></td><td><code>Action&#x3C;string></code></td><td>Push a screen-reader announcement.</td></tr><tr><td><code>UseDevtools()</code></td><td><code>DevtoolsApi</code></td><td><a href=\"/dev-tooling/\">Dev-tooling</a> hooks.</td></tr></tbody></table>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StateVignette</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">return</span> Button(<span class=\"hljs-string\">$\"clicked <span class=\"hljs-subst\">{count}</span>×\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>));\n    }\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">EffectVignette</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (tick, setTick) = UseState(<span class=\"hljs-number\">0</span>);\n        UseEffect(() =>\n        {\n            <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> System.Timers.Timer(<span class=\"hljs-number\">1000</span>);\n            timer.Elapsed += (_, _) => setTick(tick + <span class=\"hljs-number\">1</span>);\n            timer.Start();\n            <span class=\"hljs-keyword\">return</span> () => timer.Dispose();\n        });\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Tick: <span class=\"hljs-subst\">{tick}</span>\"</span>);\n    }\n}\n</code></pre>\n<p>Full coverage on <a href=\"/hooks/\">Hooks</a>.</p>\n<h2 id=\"common-factories\">Common factories<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-factories\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Factory</th><th>Notes</th></tr></thead><tbody><tr><td><code>TextBlock(s)</code> / <code>Heading(s)</code> / <code>SubHeading(s)</code> / <code>Caption(s)</code></td><td>Read-only text.</td></tr><tr><td><code>Button(label, onClick)</code></td><td>The click control.</td></tr><tr><td><code>TextBox(value, set, placeholder?, header?)</code></td><td>Single-line input.</td></tr><tr><td><code>PasswordBox(pwd, set, placeholderText?)</code></td><td>Obscured input.</td></tr><tr><td><code>NumberBox(value, set, header?)</code></td><td>Numeric input.</td></tr><tr><td><code>CheckBox(checked, set, label?)</code></td><td>Two-state checkbox.</td></tr><tr><td><code>ToggleSwitch(on, set, header?)</code></td><td>Two-state switch.</td></tr><tr><td><code>Slider(value, min, max, set)</code></td><td>Numeric range.</td></tr><tr><td><code>ComboBox(items, index, set)</code></td><td>Drop-down list.</td></tr><tr><td><code>RadioButtons(items, index, set)</code></td><td>Single-select radio group.</td></tr><tr><td><code>VStack(spacing, ...children)</code> / <code>HStack(spacing, ...children)</code></td><td>Linear stacks.</td></tr><tr><td><code>Grid(cols, rows, ...children)</code></td><td>Two-axis layout.</td></tr><tr><td><code>ScrollView(child)</code></td><td>Scroll container.</td></tr><tr><td><code>Border(child)</code></td><td>Border + corner radius wrapper.</td></tr><tr><td><code>Expander(header, content)</code></td><td>Collapsible section.</td></tr><tr><td><code>ListView&#x3C;T>(items, key, view)</code></td><td>Bound list.</td></tr><tr><td><code>VirtualList(count, render, ...)</code></td><td>Index-driven virtual list.</td></tr><tr><td><code>DataGrid&#x3C;T>(source, columns, ...)</code></td><td><a href=\"/data-system/\">Data-system</a> grid.</td></tr><tr><td><code>Empty()</code> / <code>Group(...children)</code> / <code>ForEach(items, render)</code></td><td>Tree helpers.</td></tr><tr><td><code>Memo(ctx => ...)</code> / <code>RenderEachTime(ctx => ...)</code></td><td>Function components.</td></tr><tr><td><code>Component&#x3C;TComp>()</code></td><td>Mount a class-based <a href=\"/components/\">Component</a>.</td></tr></tbody></table>\n<p>Full coverage on <a href=\"/controls/\">Controls</a>.</p>\n<h2 id=\"modifier-chains\">Modifier chains<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#modifier-chains\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Group</th><th>Methods</th></tr></thead><tbody><tr><td>Size</td><td><code>.Width(n)</code> <code>.Height(n)</code> <code>.Size(w,h)</code></td></tr><tr><td>Spacing</td><td><code>.Margin(n)</code> <code>.Padding(n)</code> <code>.HAlign(...)</code> <code>.VAlign(...)</code></td></tr><tr><td>Text</td><td><code>.FontSize(n)</code> <code>.Bold()</code> <code>.SemiBold()</code> <code>.Opacity(n)</code></td></tr><tr><td>Color</td><td><code>.Background(token)</code> <code>.Foreground(token)</code> <code>.WithBorder(token, thickness?)</code></td></tr><tr><td>Shape</td><td><code>.CornerRadius(n)</code></td></tr><tr><td>Behavior</td><td><code>.IsEnabled(bool)</code> <code>.IsVisible(bool)</code> <code>.ToolTip(s)</code></td></tr><tr><td>Keying</td><td><code>.WithKey(s)</code></td></tr><tr><td>Flex</td><td><code>.Flex(grow?, shrink?, basis?)</code></td></tr><tr><td>Themed</td><td><code>.RequestedTheme(ElementTheme)</code> <code>.Backdrop(BackdropKind)</code></td></tr><tr><td>Transition</td><td><code>.OpacityTransition()</code> <code>.ScaleTransition()</code> <code>.TranslationTransition()</code></td></tr><tr><td>Compositor anim</td><td><code>.Animate(Curve, prop?)</code> <code>.Transition(t, c?)</code> <code>.InteractionStates(b, c?)</code></td></tr><tr><td>Layout anim</td><td><code>.LayoutAnimation()</code> <code>.SpringLayoutAnimation()</code> <code>.ConnectedAnimation(key)</code></td></tr><tr><td>Escape hatch</td><td><code>.Set(ctrl => { /* raw WinUI */ })</code></td></tr></tbody></table>\n<p>Full coverage on <a href=\"/styling/\">Styling</a>, <a href=\"/animation/\">Animation</a>,\n<a href=\"/theming-tokens/\">Theming Tokens</a>.</p>\n<h2 id=\"app-entry--hosting\">App entry + hosting<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#app-entry--hosting\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>API</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ReactorApp.Run&#x3C;TComp>(title, width?, height?, preview?)</code></td><td>Single-window app.</td></tr><tr><td><code>ReactorWindow.Open&#x3C;TComp>(...)</code></td><td>Additional window from a Component.</td></tr><tr><td><code>ReactorHost</code> / <code>ReactorHostControl</code></td><td>Embed Reactor in XAML / WinForms.</td></tr><tr><td><code>NavigationHost(nav, routeMap)</code></td><td>Multi-page navigation.</td></tr><tr><td><code>DeepLinkMap&#x3C;TRoute></code></td><td>URI-pattern routing.</td></tr><tr><td><code>Command&#x3C;T></code> / <code>.Bind(button)</code></td><td><a href=\"/commanding/\">Commanding</a>.</td></tr><tr><td><code>IDataSource&#x3C;T></code> / <code>DataGrid&#x3C;T></code></td><td><a href=\"/data-system/\">Data system</a>.</td></tr><tr><td><code>ApplicationPersistedScope.Default</code></td><td>Process-wide <a href=\"/persistence/\">persistence</a> cache.</td></tr></tbody></table>\n<h2 id=\"themed-colors\">Themed colors<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#themed-colors\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>What it is</th></tr></thead><tbody><tr><td><code>Theme.Accent</code> / <code>Theme.AccentSecondary</code> / <code>Theme.AccentText</code></td><td>Accent fill + text.</td></tr><tr><td><code>Theme.PrimaryText</code> / <code>Theme.SecondaryText</code> / <code>Theme.DisabledText</code></td><td>Text levels.</td></tr><tr><td><code>Theme.CardBackground</code> / <code>Theme.SolidBackground</code> / <code>Theme.LayerFill</code></td><td>Surfaces.</td></tr><tr><td><code>Theme.ControlFill</code> / <code>Theme.ControlFillSecondary</code> / <code>Theme.ControlFillInputActive</code></td><td>Control states.</td></tr><tr><td><code>Theme.CardStroke</code> / <code>Theme.DividerStroke</code> / <code>Theme.ControlStroke</code></td><td>Borders.</td></tr><tr><td><code>Theme.SystemSuccess</code> / <code>Theme.SystemCaution</code> / <code>Theme.SystemCritical</code></td><td>Signal.</td></tr><tr><td><code>Theme.Ref(\"CustomKey\")</code></td><td>Escape hatch for an app-level key.</td></tr></tbody></table>\n<p>Full 35-token catalog on <a href=\"/theming-tokens/\">Theming Tokens</a>.</p>\n<h2 id=\"patterns-at-a-glance\">Patterns at a glance<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns-at-a-glance\">#</a></h2>\n<p><strong>Controlled input.</strong> <code>var (v, set) = UseState(\"\")</code> →\n<code>TextBox(v, set)</code>.</p>\n<p><strong>Effect with cleanup.</strong> Return a <code>Func&#x3C;void></code> from the effect lambda;\nReactor calls it before the next run and on unmount.</p>\n<p><strong>Conditional render.</strong> <code>cond ? Element1 : Empty()</code> keeps the element\nout of the tree when <code>cond</code> is false — see the\n<a href=\"/recipes/modal-dialog/\">modal-dialog recipe</a>.</p>\n<p><strong>List with stable keys.</strong> <code>ForEach(items, x => Card(x).WithKey(x.Id))</code>\nso the reconciler can move rows rather than rebuild them.</p>\n<p><strong>Memoized subtree.</strong> Wrap the expensive child in <code>Memo(ctx => …)</code>;\nthe function-component cache holds it across parent re-renders.</p>\n<p><strong>Disable submit during async work.</strong> A single <code>submitting</code> bool\ncovers both the button's <code>.IsEnabled(...)</code> and the spinner label.</p>\n<h2 id=\"rules\">Rules<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rules\">#</a></h2>\n<p>Five rules cover most analyzer findings (full list on\n<a href=\"/rules-of-reactor/\">Rules of Reactor</a>):</p>\n<ol>\n<li>Hooks at the top of <code>Render</code>, never in a loop or conditional.</li>\n<li>Setters returned by hooks are stable; you can capture them in\nclosures safely.</li>\n<li>Render functions are pure — side effects go in <code>UseEffect</code>.</li>\n<li>Lists need stable <code>.WithKey()</code> for any element that can reorder.</li>\n<li>Theme-aware modifiers (<code>.Background</code>, <code>.Foreground</code>, <code>.WithBorder</code>)\nwant a <code>Theme.*</code> token, not a hex literal.</li>\n</ol>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Skim the cheat sheet, then jump.</strong> Each row is a one-liner; the\ndeep-dive page is one click away. Don't try to learn Reactor from\nthis page — learn it from <a href=\"/getting-started/\">getting-started</a> and\n<a href=\"/components/\">components</a>, and use this card as the recall surface.</p>\n<p><strong>Bookmark the modifier table.</strong> Modifier chains are the easiest thing\nto forget mid-keystroke; the modifier table is the highest-traffic\nsection of this page.</p>\n<p><strong>Copy from the recipes for shape, not from the cheat sheet.</strong> The\nrecipes are the working compositions; the cheat sheet is the catalog.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — Hello world walkthrough.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Hook-by-hook deep dive.</li>\n<li><strong><a href=\"/controls/\">Controls</a></strong> — Full control catalog.</li>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — Modifiers in detail.</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — The full rule list\nwith analyzer codes.</li>\n</ul>",
      "text": "Cheat Sheet # Microsoft.UI.Reactor (Reactor) at a glance. Every row links to the page that covers it in depth. Minimum viable app # class HelloVignette : Component { public override Element Render () => VStack( 8 , Heading( \"Hello\" ), Button( \"Click\" , () => { }) ).Padding( 20 ); } Three pieces: a using static Microsoft.UI.Reactor.Factories import, a Component subclass, and a ReactorApp.Run&#x3C;App>(title, ...) call. Add #if DEBUG preview: true #endif so mur can capture screenshots. Hooks # Hook Returns Use when UseState&#x3C;T>(initial) (T, Action&#x3C;T>) Local state that drives re-render. UseReducer&#x3C;T>(initial) (T, Action&#x3C;Func&#x3C;T,T>>) Functional updates from prev. UseReducer&#x3C;S,A>(reducer, initial) (S, Action&#x3C;A>) Redux-style state. UseEffect(setup, deps?) void Side effects after render; cleanup on re-run. UseMemo&#x3C;T>(factory, deps) T Cache expensive computation across renders. UseCallback(fn, deps) stable delegate Memoize a callback identity. UseRef&#x3C;T>(initial) Ref&#x3C;T> Mutable value that does not trigger re-render. UseContext&#x3C;T>(ctx) T Read a context value. UsePersisted&#x3C;T>(key, initial, scope) (T, Action&#x3C;T>) Survive re-mount via LRU cache . UseColorScheme() ColorScheme Reactive light/dark/HighContrast. UseAsync(factory, deps) AsyncResource&#x3C;T> async-resources . UseFocusTrap() FocusTrapHandle Trap Tab inside a sub-tree. UseAnnounce() Action&#x3C;string> Push a screen-reader announcement. UseDevtools() DevtoolsApi Dev-tooling hooks. class StateVignette : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return Button( $\"clicked {count} ×\" , () => setCount(count + 1 )); } } class EffectVignette : Component { public override Element Render () { var (tick, setTick) = UseState( 0 ); UseEffect(() => { var timer = new System.Timers.Timer( 1000 ); timer.Elapsed += (_, _) => setTick(tick + 1 ); timer.Start(); return () => timer.Dispose(); }); return TextBlock( $\"Tick: {tick} \" ); } } Full coverage on Hooks . Common factories # Factory Notes TextBlock(s) / Heading(s) / SubHeading(s) / Caption(s) Read-only text. Button(label, onClick) The click control. TextBox(value, set, placeholder?, header?) Single-line input. PasswordBox(pwd, set, placeholderText?) Obscured input. NumberBox(value, set, header?) Numeric input. CheckBox(checked, set, label?) Two-state checkbox. ToggleSwitch(on, set, header?) Two-state switch. Slider(value, min, max, set) Numeric range. ComboBox(items, index, set) Drop-down list. RadioButtons(items, index, set) Single-select radio group. VStack(spacing, ...children) / HStack(spacing, ...children) Linear stacks. Grid(cols, rows, ...children) Two-axis layout. ScrollView(child) Scroll container. Border(child) Border + corner radius wrapper. Expander(header, content) Collapsible section. ListView&#x3C;T>(items, key, view) Bound list. VirtualList(count, render, ...) Index-driven virtual list. DataGrid&#x3C;T>(source, columns, ...) Data-system grid. Empty() / Group(...children) / ForEach(items, render) Tree helpers. Memo(ctx => ...) / RenderEachTime(ctx => ...) Function components. Component&#x3C;TComp>() Mount a class-based Component . Full coverage on Controls . Modifier chains # Group Methods Size .Width(n) .Height(n) .Size(w,h) Spacing .Margin(n) .Padding(n) .HAlign(...) .VAlign(...) Text .FontSize(n) .Bold() .SemiBold() .Opacity(n) Color .Background(token) .Foreground(token) .WithBorder(token, thickness?) Shape .CornerRadius(n) Behavior .IsEnabled(bool) .IsVisible(bool) .ToolTip(s) Keying .WithKey(s) Flex .Flex(grow?, shrink?, basis?) Themed .RequestedTheme(ElementTheme) .Backdrop(BackdropKind) Transition .OpacityTransition() .ScaleTransition() .TranslationTransition() Compositor anim .Animate(Curve, prop?) .Transition(t, c?) .InteractionStates(b, c?) Layout anim .LayoutAnimation() .SpringLayoutAnimation() .ConnectedAnimation(key) Escape hatch .Set(ctrl => { /* raw WinUI */ }) Full coverage on Styling , Animation , Theming Tokens . App entry + hosting # API Purpose ReactorApp.Run&#x3C;TComp>(title, width?, height?, preview?) Single-window app. ReactorWindow.Open&#x3C;TComp>(...) Additional window from a Component. ReactorHost / ReactorHostControl Embed Reactor in XAML / WinForms. NavigationHost(nav, routeMap) Multi-page navigation. DeepLinkMap&#x3C;TRoute> URI-pattern routing. Command&#x3C;T> / .Bind(button) Commanding . IDataSource&#x3C;T> / DataGrid&#x3C;T> Data system . ApplicationPersistedScope.Default Process-wide persistence cache. Themed colors # Token What it is Theme.Accent / Theme.AccentSecondary / Theme.AccentText Accent fill + text. Theme.PrimaryText / Theme.SecondaryText / Theme.DisabledText Text levels. Theme.CardBackground / Theme.SolidBackground / Theme.LayerFill Surfaces. Theme.ControlFill / Theme.ControlFillSecondary / Theme.ControlFillInputActive Control states. Theme.CardStroke / Theme.DividerStroke / Theme.ControlStroke Borders. Theme.SystemSuccess / Theme.SystemCaution / Theme.SystemCritical Signal. Theme.Ref(\"CustomKey\") Escape hatch for an app-level key. Full 35-token catalog on Theming Tokens . Patterns at a glance # Controlled input. var (v, set) = UseState(\"\") → TextBox(v, set) . Effect with cleanup. Return a Func&#x3C;void> from the effect lambda; Reactor calls it before the next run and on unmount. Conditional render. cond ? Element1 : Empty() keeps the element out of the tree when cond is false — see the modal-dialog recipe . List with stable keys. ForEach(items, x => Card(x).WithKey(x.Id)) so the reconciler can move rows rather than rebuild them. Memoized subtree. Wrap the expensive child in Memo(ctx => …) ; the function-component cache holds it across parent re-renders. Disable submit during async work. A single submitting bool covers both the button's .IsEnabled(...) and the spinner label. Rules # Five rules cover most analyzer findings (full list on Rules of Reactor ): Hooks at the top of Render , never in a loop or conditional. Setters returned by hooks are stable; you can capture them in closures safely. Render functions are pure — side effects go in UseEffect . Lists need stable .WithKey() for any element that can reorder. Theme-aware modifiers ( .Background , .Foreground , .WithBorder ) want a Theme.* token, not a hex literal. Tips # Skim the cheat sheet, then jump. Each row is a one-liner; the deep-dive page is one click away. Don't try to learn Reactor from this page — learn it from getting-started and components , and use this card as the recall surface. Bookmark the modifier table. Modifier chains are the easiest thing to forget mid-keystroke; the modifier table is the highest-traffic section of this page. Copy from the recipes for shape, not from the cheat sheet. The recipes are the working compositions; the cheat sheet is the catalog. Next Steps # Getting Started — Hello world walkthrough. Hooks — Hook-by-hook deep dive. Controls — Full control catalog. Styling — Modifiers in detail. Rules of Reactor — The full rule list with analyzer codes.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/cheat-sheet.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/cheat-sheet.md",
      "headings": [
        {
          "depth": 2,
          "id": "minimum-viable-app",
          "text": "Minimum viable app"
        },
        {
          "depth": 2,
          "id": "hooks",
          "text": "Hooks"
        },
        {
          "depth": 2,
          "id": "common-factories",
          "text": "Common factories"
        },
        {
          "depth": 2,
          "id": "modifier-chains",
          "text": "Modifier chains"
        },
        {
          "depth": 2,
          "id": "app-entry--hosting",
          "text": "App entry + hosting"
        },
        {
          "depth": 2,
          "id": "themed-colors",
          "text": "Themed colors"
        },
        {
          "depth": 2,
          "id": "patterns-at-a-glance",
          "text": "Patterns at a glance"
        },
        {
          "depth": 2,
          "id": "rules",
          "text": "Rules"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Rules of Reactor",
      "path": "rules-of-reactor.md",
      "slug": "/rules-of-reactor/",
      "html": "<h1 id=\"rules-of-reactor\">Rules of Reactor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rules-of-reactor\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor)'s render loop has a small set of invariants. Most are\nenforced by analyzers, so a violation surfaces at build time with a\nspecific code; the rest are conventions that the framework expects.\nThis page lists them, names the analyzer where one exists, and shows\nthe before/after pair so the catch is visible.</p>\n<p>The five core rules:</p>\n<ol>\n<li><a href=\"#1-hook-order-is-stable-across-renders\">Hook order is stable across renders.</a></li>\n<li><a href=\"#2-render-functions-are-pure\">Render functions are pure.</a></li>\n<li><a href=\"#3-lists-need-stable-keys\">Lists need stable keys.</a></li>\n<li><a href=\"#4-setters-are-stable-and-deps-must-be-stable-too\">Setters returned by hooks are stable; deps must be stable too.</a></li>\n<li><a href=\"#5-theme-aware-modifiers-want-a-token-not-a-literal\">Theme-aware modifiers want a token, not a literal.</a></li>\n</ol>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// REACTOR_HOOKS_001 — hooks must run unconditionally on every render.</span>\n<span class=\"hljs-comment\">// Wrapping a hook in `if` shifts the hook indices when the branch flips,</span>\n<span class=\"hljs-comment\">// and the next render reads slot N expecting `UseEffect` but finds</span>\n<span class=\"hljs-comment\">// `UseState`. The HookOrderException it raises is loud, but the bug</span>\n<span class=\"hljs-comment\">// can ship if the conditional is rarely true.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HookOrderBad</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">bool</span> ShouldCount;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (ShouldCount)\n        {\n            <span class=\"hljs-keyword\">var</span> (count, _) = UseState(<span class=\"hljs-number\">0</span>);             <span class=\"hljs-comment\">// REACTOR_HOOKS_001</span>\n            <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>);\n        }\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"No counter.\"</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/rules-of-reactor/ok.png\" alt=\"Trivial app — rules-of-reactor doc-app shell\"></p>\n<h2 id=\"rule-index\">Rule index<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rule-index\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Rule</th><th>Analyzer</th><th>Page</th></tr></thead><tbody><tr><td>Hook order is stable</td><td><code>REACTOR_HOOKS_001</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Hooks called from Render only</td><td><code>REACTOR_HOOKS_005</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Deps are stable</td><td><code>REACTOR_HOOKS_004</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Render is pure</td><td><em>(convention)</em></td><td><a href=\"/components/\">Components</a></td></tr><tr><td>Lists need keys</td><td><em>(convention)</em></td><td><a href=\"/collections/\">Collections</a></td></tr><tr><td>Theme tokens not literals</td><td><code>REACTOR_THEME_001</code></td><td><a href=\"/theming-tokens/\">Theming Tokens</a></td></tr><tr><td>Lightweight styling</td><td><code>REACTOR_THEME_002</code></td><td><a href=\"/styling/\">Styling</a></td></tr><tr><td>Accessible names</td><td><code>REACTOR_A11Y_001..003</code></td><td><a href=\"/accessibility/\">Accessibility</a></td></tr></tbody></table>\n<p>Each rule below covers one row in the index with the before / after\nshape and the analyzer's catch.</p>\n<h2 id=\"1-hook-order-is-stable-across-renders\">1. Hook order is stable across renders<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#1-hook-order-is-stable-across-renders\">#</a></h2>\n<p>Hooks store their state at a slot index in the component's hook list.\nThe reconciler walks the list by ordinal on every render — so if\n<code>UseState</code> was at slot 0 on render 1 and slot 1 on render 2, the\nstate migrates to the wrong slot and the value silently corrupts.</p>\n<p><strong>The rule:</strong> call every hook unconditionally at the top of <code>Render()</code>,\nin the same order, every time. No <code>if</code>, no <code>for</code>, no <code>try/catch</code>\naround a hook call, no early return.</p>\n<p><strong>Analyzer:</strong> <code>REACTOR_HOOKS_001</code> (conditional hook call),\n<code>REACTOR_HOOKS_005</code> (hook called outside a <code>Render()</code> override or a\n<code>Use*</code> helper).</p>\n<p>Before:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// REACTOR_HOOKS_001 — hooks must run unconditionally on every render.</span>\n<span class=\"hljs-comment\">// Wrapping a hook in `if` shifts the hook indices when the branch flips,</span>\n<span class=\"hljs-comment\">// and the next render reads slot N expecting `UseEffect` but finds</span>\n<span class=\"hljs-comment\">// `UseState`. The HookOrderException it raises is loud, but the bug</span>\n<span class=\"hljs-comment\">// can ship if the conditional is rarely true.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HookOrderBad</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">bool</span> ShouldCount;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (ShouldCount)\n        {\n            <span class=\"hljs-keyword\">var</span> (count, _) = UseState(<span class=\"hljs-number\">0</span>);             <span class=\"hljs-comment\">// REACTOR_HOOKS_001</span>\n            <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>);\n        }\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"No counter.\"</span>);\n    }\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">HookOrderGood</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">bool</span> ShouldCount;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// Hook always runs; the conditional moves into the render output.</span>\n        <span class=\"hljs-keyword\">var</span> (count, _) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">return</span> ShouldCount\n            ? TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>)\n            : TextBlock(<span class=\"hljs-string\">\"No counter.\"</span>);\n    }\n}\n</code></pre>\n<p>The hook runs unconditionally; the branch moves into the element tree\nreturned by <code>Render()</code>. Same UI, stable hook order.</p>\n<p>Full coverage on <a href=\"/hooks/\">Hooks</a>.</p>\n<h2 id=\"2-render-functions-are-pure\">2. Render functions are pure<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#2-render-functions-are-pure\">#</a></h2>\n<p><code>Render()</code> runs <em>every</em> time the component re-renders — which can be\nmany times per second under animation or input. A side effect inside\n<code>Render()</code> (writing to a static counter, calling a logger, opening a\nfile) fires on each render, including dev-mode double-renders that\ncatch the bug. The right place for a side effect is inside <code>UseEffect</code>,\nwhich runs once per render commit after the tree is materialized.</p>\n<p><strong>The rule:</strong> <code>Render</code> reads state and returns elements. It does not\nmutate, call I/O, or fire telemetry. Side effects go in <code>UseEffect</code>.</p>\n<p>Before:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Render must be pure. Side effects (file I/O, mutation of static state,</span>\n<span class=\"hljs-comment\">// timers) belong inside UseEffect, which runs after the render commits.</span>\n<span class=\"hljs-comment\">// A logger call inside Render mounts will fire on every re-render,</span>\n<span class=\"hljs-comment\">// including ones triggered by the debugger — and it makes snapshot tests</span>\n<span class=\"hljs-comment\">// flaky because the rendered output now depends on a side effect.</span>\n<span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TelemetryBad</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">int</span> CardRenders;\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CardBad</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        TelemetryBad.CardRenders++;                    <span class=\"hljs-comment\">// side effect in Render</span>\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"Card\"</span>);\n    }\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Telemetry</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">int</span> CardRenders;\n}\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">CardGood</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        UseEffect(() =>\n        {\n            Telemetry.CardRenders++;\n            <span class=\"hljs-keyword\">return</span> () => { };\n        });\n        <span class=\"hljs-keyword\">return</span> TextBlock(<span class=\"hljs-string\">\"Card\"</span>);\n    }\n}\n</code></pre>\n<p>The counter still increments on every render, but it does so inside\nthe effect — which means a snapshot test of <code>CardGood</code> doesn't have a\nside-effect-on-render bug; the effect fires after the test inspects\nthe tree. Full coverage on <a href=\"/effects/\">Effects</a> and\n<a href=\"/components/\">Components</a>.</p>\n<h2 id=\"3-lists-need-stable-keys\">3. Lists need stable keys<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#3-lists-need-stable-keys\">#</a></h2>\n<p>When the items in a <code>ForEach</code> / <code>ListView</code> / <code>Select(...).ToArray()</code>\nreorder, the reconciler diffs the old tree against the new one.\nWithout keys, it diffs positionally — and a row that swapped places\ngets the previous row's local state (focus, scroll offset, in-flight\nedit). With <code>.WithKey(id)</code>, the reconciler matches by key and moves\nthe right state to the right row.</p>\n<p><strong>The rule:</strong> every element produced inside a list-like construct gets\na <code>.WithKey(stableId)</code> whose value persists across re-renders. The id\nmust be the record's primary key, not the array index.</p>\n<p>Before:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// A list reorder without keys forces the reconciler to walk both lists in</span>\n<span class=\"hljs-comment\">// order and reuse slot 0 for whatever new item lands first. Local state</span>\n<span class=\"hljs-comment\">// (focus, scroll position, in-flight edits) gets attached to the wrong</span>\n<span class=\"hljs-comment\">// row. WithKey on each child binds state to identity rather than slot.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TodoListBad</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> TodoItem[] Items = System.Array.Empty&#x3C;TodoItem>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">4</span>,\n        Items.Select(i =>\n            <span class=\"hljs-comment\">// No .WithKey — reorder is destructive.</span>\n            TextBox(i.Title, _ => { }, header: i.Id.ToString())\n        ).ToArray()\n    );\n}\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">TodoItem</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> Id, <span class=\"hljs-built_in\">string</span> Title</span>)</span>;\n</code></pre>\n<p>After:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TodoListGood</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> TodoItem[] Items = System.Array.Empty&#x3C;TodoItem>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => VStack(<span class=\"hljs-number\">4</span>,\n        Items.Select(i =>\n            TextBox(i.Title, _ => { }, header: i.Id.ToString())\n                .WithKey(i.Id.ToString())                  <span class=\"hljs-comment\">// stable identity</span>\n        ).ToArray()\n    );\n}\n</code></pre>\n<p>Full coverage on <a href=\"/collections/\">Collections</a> and\n<a href=\"/reconciliation/\">Reconciliation</a>.</p>\n<h2 id=\"4-setters-are-stable-and-deps-must-be-stable-too\">4. Setters are stable, and deps must be stable too<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#4-setters-are-stable-and-deps-must-be-stable-too\">#</a></h2>\n<p>The <code>Action&#x3C;T></code> setter returned by <code>UseState</code>, <code>UseReducer</code>, and\n<code>UsePersisted</code> keeps the same delegate identity across renders. You\ncan safely close over it inside a <code>UseEffect</code> cleanup, a captured\nevent handler, or a <code>Task</code> — the captured reference is the live\nsetter.</p>\n<p>The same can't be said of dependency arrays. A freshly-allocated\narray or a freshly-allocated record passed as <code>deps</code> differs by\nreference on every render, so the effect re-fires every time:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Wrong:</span>\nUseEffect(Setup, <span class=\"hljs-keyword\">new</span>[] { name, version });    <span class=\"hljs-comment\">// freshly-allocated array</span>\n<span class=\"hljs-comment\">// REACTOR_HOOKS_004 flags this.</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Right:</span>\nUseEffect(Setup, name, version);              <span class=\"hljs-comment\">// params overload — items</span>\n                                              <span class=\"hljs-comment\">// compared by value.</span>\n</code></pre>\n<p><strong>The rule:</strong> pass <code>deps</code> via the <code>params</code> overload of <code>UseEffect</code>/\n<code>UseMemo</code>/<code>UseCallback</code>, not as a freshly-allocated array. The\nanalyzer catches the common shape; for cases the analyzer can't see\n(a <code>Tuple&#x3C;...></code> allocated inline), assign the deps to a local first.</p>\n<p><strong>Analyzer:</strong> <code>REACTOR_HOOKS_004</code> (unstable deps),\n<code>REACTOR_HOOKS_007</code> (<code>UseMemoCells</code> builder missing a captured\ndependency).</p>\n<p>Full coverage on <a href=\"/hooks/\">Hooks</a>.</p>\n<h2 id=\"5-theme-aware-modifiers-want-a-token-not-a-literal\">5. Theme-aware modifiers want a token, not a literal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#5-theme-aware-modifiers-want-a-token-not-a-literal\">#</a></h2>\n<p><code>.Background</code>, <code>.Foreground</code>, and <code>.WithBorder</code> take a brush. A hex\nliteral works for the demo but breaks the moment the user flips\nthemes — the literal is locked to the value you typed, so the\nbrand-blue button stays blue on a now-dark background and the\ncontrast collapses.</p>\n<p><strong>The rule:</strong> pass a <code>Theme.*</code> token (or <code>Theme.Ref(\"CustomKey\")</code> for\na custom XAML resource key) to any theme-aware modifier. Reserve hex\nliterals for the rare case where the color is <em>intentionally</em>\ntheme-invariant (a brand mark, a print preview) — and leave a comment\nsaying so.</p>\n<p><strong>Analyzer:</strong> <code>REACTOR_THEME_001</code> (hard-coded color string on a\ntheme-aware modifier).</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, () => { }).Background(<span class=\"hljs-string\">\"#0066CC\"</span>);   <span class=\"hljs-comment\">// REACTOR_THEME_001</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Do:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, () => { }).Background(Theme.Accent);\n</code></pre>\n<p>Full coverage on <a href=\"/theming-tokens/\">Theming Tokens</a>.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Rule</th><th>Analyzer</th><th>Where the page lives</th></tr></thead><tbody><tr><td>Hook order is stable</td><td><code>REACTOR_HOOKS_001</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Hooks called from <code>Render</code> only</td><td><code>REACTOR_HOOKS_005</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Deps must be stable</td><td><code>REACTOR_HOOKS_004</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>UseResource fetcher is idempotent</td><td><code>REACTOR_HOOKS_006</code></td><td><a href=\"/async-resources/\">Async Resources</a></td></tr><tr><td>UseMemoCells builder must close over deps</td><td><code>REACTOR_HOOKS_007</code></td><td><a href=\"/hooks/\">Hooks</a></td></tr><tr><td>Render must be pure</td><td><em>(convention — no analyzer)</em></td><td><a href=\"/components/\">Components</a>, <a href=\"/effects/\">Effects</a></td></tr><tr><td>Lists need keys</td><td><em>(convention — no analyzer)</em></td><td><a href=\"/collections/\">Collections</a>, <a href=\"/reconciliation/\">Reconciliation</a></td></tr><tr><td>Theme-aware modifiers want a token</td><td><code>REACTOR_THEME_001</code></td><td><a href=\"/theming-tokens/\">Theming Tokens</a></td></tr><tr><td>Lightweight styling, not implicit resources</td><td><code>REACTOR_THEME_002</code></td><td><a href=\"/styling/\">Styling</a></td></tr><tr><td><code>RequestedTheme</code> is a render input, not a setter</td><td><code>REACTOR_THEME_003</code></td><td><a href=\"/styling/\">Styling</a></td></tr><tr><td>XML docs on public APIs</td><td><code>REACTOR_DOC_001</code></td><td>(framework code)</td></tr><tr><td><code>&#x3C;see cref=\"...\"/></code> resolves</td><td><code>REACTOR_DOC_002</code></td><td>(framework code)</td></tr><tr><td>Accessible name on interactive elements</td><td><code>REACTOR_A11Y_001..003</code></td><td><a href=\"/accessibility/\">Accessibility</a></td></tr></tbody></table>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Treat analyzer warnings as build errors in CI.</strong> Every rule with an\nanalyzer has a known false-positive rate near zero; the cost of a\nsuppression is small, the cost of a regression that the analyzer\nwould have caught is large.</p>\n<p><strong>The \"pure render\" rule is conventional, not enforced.</strong> No\nanalyzer catches it; review and snapshot tests are the safety net.\nThe <a href=\"/testing/\">testing</a> page covers the snapshot pattern that makes\npurity visible.</p>\n<p><strong><code>.WithKey</code> is cheap; reach for it whenever a list reorders.</strong> Even\nwhen the analyzer doesn't flag the omission, a missing key is one of\nthe bugs most likely to ship to a customer (it's invisible at small\nlist sizes and devastating at scale).</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Where rules 1 and 4 are exercised most.</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Render purity in depth.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Why keys matter at the\nreconciler level.</li>\n<li><strong><a href=\"/theming-tokens/\">Theming Tokens</a></strong> — Token catalog for rule 5.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — The A11Y_001..003 rules.</li>\n</ul>",
      "text": "Rules of Reactor # Microsoft.UI.Reactor (Reactor)'s render loop has a small set of invariants. Most are enforced by analyzers, so a violation surfaces at build time with a specific code; the rest are conventions that the framework expects. This page lists them, names the analyzer where one exists, and shows the before/after pair so the catch is visible. The five core rules: Hook order is stable across renders. Render functions are pure. Lists need stable keys. Setters returned by hooks are stable; deps must be stable too. Theme-aware modifiers want a token, not a literal. // REACTOR_HOOKS_001 — hooks must run unconditionally on every render. // Wrapping a hook in `if` shifts the hook indices when the branch flips, // and the next render reads slot N expecting `UseEffect` but finds // `UseState`. The HookOrderException it raises is loud, but the bug // can ship if the conditional is rarely true. class HookOrderBad : Component { public bool ShouldCount; public override Element Render () { if (ShouldCount) { var (count, _) = UseState( 0 ); // REACTOR_HOOKS_001 return TextBlock( $\"Count: {count} \" ); } return TextBlock( \"No counter.\" ); } } Rule index # Rule Analyzer Page Hook order is stable REACTOR_HOOKS_001 Hooks Hooks called from Render only REACTOR_HOOKS_005 Hooks Deps are stable REACTOR_HOOKS_004 Hooks Render is pure (convention) Components Lists need keys (convention) Collections Theme tokens not literals REACTOR_THEME_001 Theming Tokens Lightweight styling REACTOR_THEME_002 Styling Accessible names REACTOR_A11Y_001..003 Accessibility Each rule below covers one row in the index with the before / after shape and the analyzer's catch. 1. Hook order is stable across renders # Hooks store their state at a slot index in the component's hook list. The reconciler walks the list by ordinal on every render — so if UseState was at slot 0 on render 1 and slot 1 on render 2, the state migrates to the wrong slot and the value silently corrupts. The rule: call every hook unconditionally at the top of Render() , in the same order, every time. No if , no for , no try/catch around a hook call, no early return. Analyzer: REACTOR_HOOKS_001 (conditional hook call), REACTOR_HOOKS_005 (hook called outside a Render() override or a Use* helper). Before: // REACTOR_HOOKS_001 — hooks must run unconditionally on every render. // Wrapping a hook in `if` shifts the hook indices when the branch flips, // and the next render reads slot N expecting `UseEffect` but finds // `UseState`. The HookOrderException it raises is loud, but the bug // can ship if the conditional is rarely true. class HookOrderBad : Component { public bool ShouldCount; public override Element Render () { if (ShouldCount) { var (count, _) = UseState( 0 ); // REACTOR_HOOKS_001 return TextBlock( $\"Count: {count} \" ); } return TextBlock( \"No counter.\" ); } } After: class HookOrderGood : Component { public bool ShouldCount; public override Element Render () { // Hook always runs; the conditional moves into the render output. var (count, _) = UseState( 0 ); return ShouldCount ? TextBlock( $\"Count: {count} \" ) : TextBlock( \"No counter.\" ); } } The hook runs unconditionally; the branch moves into the element tree returned by Render() . Same UI, stable hook order. Full coverage on Hooks . 2. Render functions are pure # Render() runs every time the component re-renders — which can be many times per second under animation or input. A side effect inside Render() (writing to a static counter, calling a logger, opening a file) fires on each render, including dev-mode double-renders that catch the bug. The right place for a side effect is inside UseEffect , which runs once per render commit after the tree is materialized. The rule: Render reads state and returns elements. It does not mutate, call I/O, or fire telemetry. Side effects go in UseEffect . Before: // Render must be pure. Side effects (file I/O, mutation of static state, // timers) belong inside UseEffect, which runs after the render commits. // A logger call inside Render mounts will fire on every re-render, // including ones triggered by the debugger — and it makes snapshot tests // flaky because the rendered output now depends on a side effect. static class TelemetryBad { public static int CardRenders; } class CardBad : Component { public override Element Render () { TelemetryBad.CardRenders++; // side effect in Render return TextBlock( \"Card\" ); } } After: static class Telemetry { public static int CardRenders; } class CardGood : Component { public override Element Render () { UseEffect(() => { Telemetry.CardRenders++; return () => { }; }); return TextBlock( \"Card\" ); } } The counter still increments on every render, but it does so inside the effect — which means a snapshot test of CardGood doesn't have a side-effect-on-render bug; the effect fires after the test inspects the tree. Full coverage on Effects and Components . 3. Lists need stable keys # When the items in a ForEach / ListView / Select(...).ToArray() reorder, the reconciler diffs the old tree against the new one. Without keys, it diffs positionally — and a row that swapped places gets the previous row's local state (focus, scroll offset, in-flight edit). With .WithKey(id) , the reconciler matches by key and moves the right state to the right row. The rule: every element produced inside a list-like construct gets a .WithKey(stableId) whose value persists across re-renders. The id must be the record's primary key, not the array index. Before: // A list reorder without keys forces the reconciler to walk both lists in // order and reuse slot 0 for whatever new item lands first. Local state // (focus, scroll position, in-flight edits) gets attached to the wrong // row. WithKey on each child binds state to identity rather than slot. class TodoListBad : Component { public TodoItem[] Items = System.Array.Empty&#x3C;TodoItem>(); public override Element Render () => VStack( 4 , Items.Select(i => // No .WithKey — reorder is destructive. TextBox(i.Title, _ => { }, header: i.Id.ToString()) ).ToArray() ); } public record TodoItem ( int Id, string Title ) ; After: class TodoListGood : Component { public TodoItem[] Items = System.Array.Empty&#x3C;TodoItem>(); public override Element Render () => VStack( 4 , Items.Select(i => TextBox(i.Title, _ => { }, header: i.Id.ToString()) .WithKey(i.Id.ToString()) // stable identity ).ToArray() ); } Full coverage on Collections and Reconciliation . 4. Setters are stable, and deps must be stable too # The Action&#x3C;T> setter returned by UseState , UseReducer , and UsePersisted keeps the same delegate identity across renders. You can safely close over it inside a UseEffect cleanup, a captured event handler, or a Task — the captured reference is the live setter. The same can't be said of dependency arrays. A freshly-allocated array or a freshly-allocated record passed as deps differs by reference on every render, so the effect re-fires every time: // Wrong: UseEffect(Setup, new [] { name, version }); // freshly-allocated array // REACTOR_HOOKS_004 flags this. // Right: UseEffect(Setup, name, version); // params overload — items // compared by value. The rule: pass deps via the params overload of UseEffect / UseMemo / UseCallback , not as a freshly-allocated array. The analyzer catches the common shape; for cases the analyzer can't see (a Tuple&#x3C;...> allocated inline), assign the deps to a local first. Analyzer: REACTOR_HOOKS_004 (unstable deps), REACTOR_HOOKS_007 ( UseMemoCells builder missing a captured dependency). Full coverage on Hooks . 5. Theme-aware modifiers want a token, not a literal # .Background , .Foreground , and .WithBorder take a brush. A hex literal works for the demo but breaks the moment the user flips themes — the literal is locked to the value you typed, so the brand-blue button stays blue on a now-dark background and the contrast collapses. The rule: pass a Theme.* token (or Theme.Ref(\"CustomKey\") for a custom XAML resource key) to any theme-aware modifier. Reserve hex literals for the rare case where the color is intentionally theme-invariant (a brand mark, a print preview) — and leave a comment saying so. Analyzer: REACTOR_THEME_001 (hard-coded color string on a theme-aware modifier). // Don't: Button( \"Save\" , () => { }).Background( \"#0066CC\" ); // REACTOR_THEME_001 // Do: Button( \"Save\" , () => { }).Background(Theme.Accent); Full coverage on Theming Tokens . Reference # Rule Analyzer Where the page lives Hook order is stable REACTOR_HOOKS_001 Hooks Hooks called from Render only REACTOR_HOOKS_005 Hooks Deps must be stable REACTOR_HOOKS_004 Hooks UseResource fetcher is idempotent REACTOR_HOOKS_006 Async Resources UseMemoCells builder must close over deps REACTOR_HOOKS_007 Hooks Render must be pure (convention — no analyzer) Components , Effects Lists need keys (convention — no analyzer) Collections , Reconciliation Theme-aware modifiers want a token REACTOR_THEME_001 Theming Tokens Lightweight styling, not implicit resources REACTOR_THEME_002 Styling RequestedTheme is a render input, not a setter REACTOR_THEME_003 Styling XML docs on public APIs REACTOR_DOC_001 (framework code) &#x3C;see cref=\"...\"/> resolves REACTOR_DOC_002 (framework code) Accessible name on interactive elements REACTOR_A11Y_001..003 Accessibility Tips # Treat analyzer warnings as build errors in CI. Every rule with an analyzer has a known false-positive rate near zero; the cost of a suppression is small, the cost of a regression that the analyzer would have caught is large. The \"pure render\" rule is conventional, not enforced. No analyzer catches it; review and snapshot tests are the safety net. The testing page covers the snapshot pattern that makes purity visible. .WithKey is cheap; reach for it whenever a list reorders. Even when the analyzer doesn't flag the omission, a missing key is one of the bugs most likely to ship to a customer (it's invisible at small list sizes and devastating at scale). Next Steps # Hooks — Where rules 1 and 4 are exercised most. Components — Render purity in depth. Reconciliation — Why keys matter at the reconciler level. Theming Tokens — Token catalog for rule 5. Accessibility — The A11Y_001..003 rules.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/rules-of-reactor.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/rules-of-reactor.md",
      "headings": [
        {
          "depth": 2,
          "id": "rule-index",
          "text": "Rule index"
        },
        {
          "depth": 2,
          "id": "1-hook-order-is-stable-across-renders",
          "text": "1. Hook order is stable across renders"
        },
        {
          "depth": 2,
          "id": "2-render-functions-are-pure",
          "text": "2. Render functions are pure"
        },
        {
          "depth": 2,
          "id": "3-lists-need-stable-keys",
          "text": "3. Lists need stable keys"
        },
        {
          "depth": 2,
          "id": "4-setters-are-stable-and-deps-must-be-stable-too",
          "text": "4. Setters are stable, and deps must be stable too"
        },
        {
          "depth": 2,
          "id": "5-theme-aware-modifiers-want-a-token-not-a-literal",
          "text": "5. Theme-aware modifiers want a token, not a literal"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Theming Tokens",
      "path": "theming-tokens.md",
      "slug": "/theming-tokens/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/design/style/colors\">Colors</a>.</p>\n</blockquote>\n<p>A Microsoft.UI.Reactor (Reactor) theme token is a name for a color that the system resolves at render\ntime. The same token (<code>Theme.Accent</code>) emits a different <code>Brush</code>\ndepending on whether the app is in light mode, dark mode, or high\ncontrast — and switches without a re-render when the user flips\nthemes. The alternative — a literal like <code>\"#0066CC\"</code> baked into a\nmodifier — locks the value to a single visual mode and silently\nbreaks dark theme the moment a user opens settings. Tokens exist so\nthe <a href=\"/styling/\">styling</a> layer can hand a brush to a control without\ncaring which theme is active. The most common mistake is reaching for\na literal because \"I'll just hardcode this one\"; the\n<a href=\"/rules-of-reactor/\">REACTOR_THEME_001</a> analyzer catches that at\nbuild time so it cannot ship.</p>\n<h1 id=\"theming-tokens\">Theming Tokens<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#theming-tokens\">#</a></h1>\n<p>Reactor exposes 35 named theme tokens on the static\n<a href=\"/styling/\"><code>Theme</code></a> class plus a <code>Theme.Ref(string)</code> escape hatch\nfor any XAML resource key. Tokens resolve through WinUI's resource\nsystem (<code>XamlControlsResources</code> + the app's <code>ThemeDictionaries</code>), so\nthe values they produce match the design system Microsoft ships in\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/design/style/colors\">WinUI Fluent colors</a>.</p>\n<h2 id=\"lead-snippet\">Lead snippet<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#lead-snippet\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SwatchGrid</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => ScrollView(\n        VStack(<span class=\"hljs-number\">16</span>,\n            Heading(<span class=\"hljs-string\">\"Theme tokens\"</span>),\n            SwatchSection(<span class=\"hljs-string\">\"Accent\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"Accent\"</span>, Theme.Accent),\n                (<span class=\"hljs-string\">\"AccentSecondary\"</span>, Theme.AccentSecondary),\n                (<span class=\"hljs-string\">\"AccentTertiary\"</span>, Theme.AccentTertiary),\n                (<span class=\"hljs-string\">\"AccentDisabled\"</span>, Theme.AccentDisabled),\n            }),\n            SwatchSection(<span class=\"hljs-string\">\"Text\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"PrimaryText\"</span>, Theme.PrimaryText),\n                (<span class=\"hljs-string\">\"SecondaryText\"</span>, Theme.SecondaryText),\n                (<span class=\"hljs-string\">\"TertiaryText\"</span>, Theme.TertiaryText),\n                (<span class=\"hljs-string\">\"DisabledText\"</span>, Theme.DisabledText),\n                (<span class=\"hljs-string\">\"AccentText\"</span>, Theme.AccentText),\n            }),\n            SwatchSection(<span class=\"hljs-string\">\"Surfaces\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"SolidBackground\"</span>, Theme.SolidBackground),\n                (<span class=\"hljs-string\">\"CardBackground\"</span>, Theme.CardBackground),\n                (<span class=\"hljs-string\">\"SmokeFill\"</span>, Theme.SmokeFill),\n                (<span class=\"hljs-string\">\"SubtleFill\"</span>, Theme.SubtleFill),\n                (<span class=\"hljs-string\">\"LayerFill\"</span>, Theme.LayerFill),\n            }),\n            SwatchSection(<span class=\"hljs-string\">\"Control fill\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"ControlFill\"</span>, Theme.ControlFill),\n                (<span class=\"hljs-string\">\"ControlFillSecondary\"</span>, Theme.ControlFillSecondary),\n                (<span class=\"hljs-string\">\"ControlFillTertiary\"</span>, Theme.ControlFillTertiary),\n                (<span class=\"hljs-string\">\"ControlFillDisabled\"</span>, Theme.ControlFillDisabled),\n                (<span class=\"hljs-string\">\"ControlFillInputActive\"</span>, Theme.ControlFillInputActive),\n            }),\n            SwatchSection(<span class=\"hljs-string\">\"Stroke\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"CardStroke\"</span>, Theme.CardStroke),\n                (<span class=\"hljs-string\">\"SurfaceStroke\"</span>, Theme.SurfaceStroke),\n                (<span class=\"hljs-string\">\"DividerStroke\"</span>, Theme.DividerStroke),\n                (<span class=\"hljs-string\">\"ControlStroke\"</span>, Theme.ControlStroke),\n                (<span class=\"hljs-string\">\"ControlStrokeSecondary\"</span>, Theme.ControlStrokeSecondary),\n            }),\n            SwatchSection(<span class=\"hljs-string\">\"Signal\"</span>, <span class=\"hljs-keyword\">new</span>[] {\n                (<span class=\"hljs-string\">\"SystemAttention\"</span>, Theme.SystemAttention),\n                (<span class=\"hljs-string\">\"SystemSuccess\"</span>, Theme.SystemSuccess),\n                (<span class=\"hljs-string\">\"SystemCaution\"</span>, Theme.SystemCaution),\n                (<span class=\"hljs-string\">\"SystemCritical\"</span>, Theme.SystemCritical),\n                (<span class=\"hljs-string\">\"SystemNeutral\"</span>, Theme.SystemNeutral),\n                (<span class=\"hljs-string\">\"SystemSolidNeutral\"</span>, Theme.SystemSolidNeutral),\n            })\n        ).Padding(<span class=\"hljs-number\">20</span>)\n    );\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">SwatchSection</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> title, (<span class=\"hljs-built_in\">string</span> Name, ThemeRef Ref</span>)[] tokens)</span> =>\n        VStack(<span class=\"hljs-number\">8</span>,\n            SubHeading(title),\n            VStack(<span class=\"hljs-number\">4</span>, tokens.Select(t => Row(t.Name, t.Ref)).ToArray())\n        );\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> Element <span class=\"hljs-title\">Row</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> name, ThemeRef token</span>)</span> => HStack(<span class=\"hljs-number\">12</span>,\n        <span class=\"hljs-keyword\">new</span> BorderElement(Empty())\n            .Background(token)\n            .Size(<span class=\"hljs-number\">40</span>, <span class=\"hljs-number\">24</span>)\n            .WithBorder(<span class=\"hljs-string\">\"#DDDDDD\"</span>),\n        TextBlock(name).Width(<span class=\"hljs-number\">220</span>),\n        TextBlock(token.ResourceKey).Opacity(<span class=\"hljs-number\">0.6</span>)\n    );\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/theming-tokens/swatch-grid-light.png\" alt=\"Token swatches — light theme\">\n<img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/theming-tokens/swatch-grid-dark.png\" alt=\"Token swatches — dark theme\"></p>\n<p>The same grid, both themes. Every cell is the result of <code>ThemeRef</code>\nresolution against the active <code>ThemeDictionary</code>; switching themes is a\nproperty update on the host, not a re-render of the component.</p>\n<blockquote>\n<p><strong>Caveat:</strong> A hardcoded color string in <code>.Background(\"#0066CC\")</code> skips the\ntheme swap entirely. The button stays blue when the user flips to\ndark mode, and white text on a now-still-blue background still\nsatisfies WCAG against light's neutral — but the dark-mode neutral\nbehind it makes the page look broken. Use <code>Theme.Accent</code> (or the\nmatching <code>Theme.Ref(key)</code>) on <code>.Background</code> / <code>.Foreground</code> /\n<code>.WithBorder</code>; <a href=\"/rules-of-reactor/\">REACTOR_THEME_001</a> flags the\nliteral at build time so the regression never reaches review.</p>\n</blockquote>\n<h2 id=\"how-resolution-works\">How resolution works<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#how-resolution-works\">#</a></h2>\n<p><a href=\"/styling/\"><code>ThemeRef</code></a> is a readonly record struct holding the\nresource key. When a modifier like <code>.Background(token)</code> reaches the\nreconciler, the framework walks from the materialized\n<a href=\"/components/\"><code>FrameworkElement</code></a> up the visual tree looking for the\nnearest <code>RequestedTheme</code>, falls back to <code>ActualTheme</code>, then\n<code>Application.RequestedTheme</code>. The brush for the chosen theme name\ncomes from <code>Application.Resources.ThemeDictionaries[name]</code> (or the\nnearest merged dictionary).</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemeRefGood</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        Button(<span class=\"hljs-string\">\"Click me\"</span>, () => { }).Background(Theme.Accent);\n}\n</code></pre>\n<p>When the user flips the system theme, WinUI raises\n<a href=\"/animation/\"><code>ActualThemeChanged</code></a>; Reactor subscribes and updates\nthe brush on every element that referenced a <code>ThemeRef</code>. Components\nthat branch on the scheme use <a href=\"/hooks/\"><code>UseColorScheme</code></a>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// UseColorScheme reads the current scheme (Light / Dark) reactively so a</span>\n<span class=\"hljs-comment\">// component can branch on the value without re-implementing the resolver.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">SchemeAwareBadge</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> scheme = UseColorScheme();\n        <span class=\"hljs-keyword\">var</span> label = scheme == ColorScheme.Dark ? <span class=\"hljs-string\">\"Dark mode\"</span> : <span class=\"hljs-string\">\"Light mode\"</span>;\n        <span class=\"hljs-keyword\">return</span> TextBlock(label).Foreground(Theme.PrimaryText);\n    }\n}\n</code></pre>\n<p><code>UseColorScheme()</code> returns the current <code>ColorScheme</code> (Light / Dark /\nHighContrast) and re-runs render when the value changes. Reach for it\nonly when the branch can't be expressed as a token (e.g. a\nnon-color decision like which icon to render).</p>\n<h2 id=\"token-catalog\">Token catalog<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#token-catalog\">#</a></h2>\n<p>35 named tokens across 6 groups, plus the <code>Theme.Ref(string)</code> escape\nhatch.</p>\n<!-- TODO Phase 4: auto-generate this table from src/Reactor/Core/Theme.cs\n     so a new token can never be added without the page updating. -->\n<h3 id=\"accent-4-tokens\">Accent (4 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accent-4-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.Accent</code></td><td><code>AccentFillColorDefaultBrush</code></td><td>Primary action button background.</td></tr><tr><td><code>Theme.AccentSecondary</code></td><td><code>AccentFillColorSecondaryBrush</code></td><td>Pressed / hover accent variants.</td></tr><tr><td><code>Theme.AccentTertiary</code></td><td><code>AccentFillColorTertiaryBrush</code></td><td>Subtle accent surfaces.</td></tr><tr><td><code>Theme.AccentDisabled</code></td><td><code>AccentFillColorDisabledBrush</code></td><td>Disabled state on an accent control.</td></tr></tbody></table>\n<h3 id=\"text-5-tokens\">Text (5 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#text-5-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.PrimaryText</code></td><td><code>TextFillColorPrimaryBrush</code></td><td>Headlines and body.</td></tr><tr><td><code>Theme.SecondaryText</code></td><td><code>TextFillColorSecondaryBrush</code></td><td>Supporting copy.</td></tr><tr><td><code>Theme.TertiaryText</code></td><td><code>TextFillColorTertiaryBrush</code></td><td>Captions and timestamps.</td></tr><tr><td><code>Theme.DisabledText</code></td><td><code>TextFillColorDisabledBrush</code></td><td>Disabled labels.</td></tr><tr><td><code>Theme.AccentText</code></td><td><code>AccentTextFillColorPrimaryBrush</code></td><td>Inline accent text (links, badges).</td></tr></tbody></table>\n<h3 id=\"surfaces-5-tokens\">Surfaces (5 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#surfaces-5-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.SolidBackground</code></td><td><code>SolidBackgroundFillColorBaseBrush</code></td><td>Page background.</td></tr><tr><td><code>Theme.CardBackground</code></td><td><code>CardBackgroundFillColorDefaultBrush</code></td><td>Card / panel container.</td></tr><tr><td><code>Theme.SmokeFill</code></td><td><code>SmokeFillColorDefaultBrush</code></td><td>Modal scrim.</td></tr><tr><td><code>Theme.SubtleFill</code></td><td><code>SubtleFillColorSecondaryBrush</code></td><td>Hover wash for list rows.</td></tr><tr><td><code>Theme.LayerFill</code></td><td><code>LayerFillColorDefaultBrush</code></td><td>Stacked-card layer.</td></tr></tbody></table>\n<h3 id=\"control-fill-5-tokens\">Control fill (5 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#control-fill-5-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.ControlFill</code></td><td><code>ControlFillColorDefaultBrush</code></td><td>Default neutral control surface.</td></tr><tr><td><code>Theme.ControlFillSecondary</code></td><td><code>ControlFillColorSecondaryBrush</code></td><td>Hover state.</td></tr><tr><td><code>Theme.ControlFillTertiary</code></td><td><code>ControlFillColorTertiaryBrush</code></td><td>Pressed state.</td></tr><tr><td><code>Theme.ControlFillDisabled</code></td><td><code>ControlFillColorDisabledBrush</code></td><td>Disabled.</td></tr><tr><td><code>Theme.ControlFillInputActive</code></td><td><code>ControlFillColorInputActiveBrush</code></td><td>Active TextBox / NumberBox fill.</td></tr></tbody></table>\n<h3 id=\"stroke-5-tokens\">Stroke (5 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stroke-5-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.CardStroke</code></td><td><code>CardStrokeColorDefaultBrush</code></td><td>Card outline.</td></tr><tr><td><code>Theme.SurfaceStroke</code></td><td><code>SurfaceStrokeColorDefaultBrush</code></td><td>Page-level dividers.</td></tr><tr><td><code>Theme.DividerStroke</code></td><td><code>DividerStrokeColorDefaultBrush</code></td><td>Inline list separators.</td></tr><tr><td><code>Theme.ControlStroke</code></td><td><code>ControlStrokeColorDefaultBrush</code></td><td>Default control border.</td></tr><tr><td><code>Theme.ControlStrokeSecondary</code></td><td><code>ControlStrokeColorSecondaryBrush</code></td><td>Focus / pressed border.</td></tr></tbody></table>\n<h3 id=\"signal-11-tokens\">Signal (11 tokens)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#signal-11-tokens\">#</a></h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Token</th><th>WinUI key</th><th>Where to use</th></tr></thead><tbody><tr><td><code>Theme.SystemAttention</code></td><td><code>SystemFillColorAttentionBrush</code></td><td>Attention call-out.</td></tr><tr><td><code>Theme.SystemSuccess</code></td><td><code>SystemFillColorSuccessBrush</code></td><td>Success state.</td></tr><tr><td><code>Theme.SystemCaution</code></td><td><code>SystemFillColorCautionBrush</code></td><td>Warning state.</td></tr><tr><td><code>Theme.SystemCritical</code></td><td><code>SystemFillColorCriticalBrush</code></td><td>Error state.</td></tr><tr><td><code>Theme.SystemNeutral</code></td><td><code>SystemFillColorNeutralBrush</code></td><td>Info / neutral.</td></tr><tr><td><code>Theme.SystemSolidNeutral</code></td><td><code>SystemFillColorSolidNeutralBrush</code></td><td>Solid neutral surface.</td></tr><tr><td><code>Theme.SystemAttentionBackground</code></td><td><code>SystemFillColorAttentionBackgroundBrush</code></td><td>Attention banner background.</td></tr><tr><td><code>Theme.SystemSuccessBackground</code></td><td><code>SystemFillColorSuccessBackgroundBrush</code></td><td>Success banner background.</td></tr><tr><td><code>Theme.SystemCautionBackground</code></td><td><code>SystemFillColorCautionBackgroundBrush</code></td><td>Warning banner background.</td></tr><tr><td><code>Theme.SystemCriticalBackground</code></td><td><code>SystemFillColorCriticalBackgroundBrush</code></td><td>Error banner background.</td></tr><tr><td><code>Theme.SystemNeutralBackground</code></td><td><code>SystemFillColorNeutralBackgroundBrush</code></td><td>Info banner background.</td></tr></tbody></table>\n<p>(<code>Theme.SystemSolidAttention</code> rounds it to 35 + the <code>Theme.Ref(key)</code>\nescape hatch — total surface ≥ 37 per spec §5.3.)</p>\n<h3 id=\"custom-keys\">Custom keys<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-keys\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Reference any XAML resource by string key — covers app-level overrides</span>\n<span class=\"hljs-comment\">// and any token Reactor doesn't surface as a typed accessor.</span>\nButton(<span class=\"hljs-string\">\"Custom\"</span>, () => { })\n    .Background(Theme.Ref(<span class=\"hljs-string\">\"MyAppTitleBarBackground\"</span>));\n</code></pre>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"theming-an-infobar-by-severity\">Theming an InfoBar by severity<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#theming-an-infobar-by-severity\">#</a></h3>\n<p>The signal-background tokens pair with the foreground signal tokens\nto produce inline status banners that read correctly in either theme.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\">Element <span class=\"hljs-title\">StatusBanner</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> text, Severity severity</span>)</span> => HStack(<span class=\"hljs-number\">8</span>,\n    TextBlock(text).Foreground(severity <span class=\"hljs-keyword\">switch</span> {\n        Severity.Success  => Theme.SystemSuccess,\n        Severity.Warning  => Theme.SystemCaution,\n        Severity.Critical => Theme.SystemCritical,\n        _                  => Theme.SystemNeutral,\n    })\n).Background(severity <span class=\"hljs-keyword\">switch</span> {\n    Severity.Success  => Theme.SystemSuccessBackground,\n    Severity.Warning  => Theme.SystemCautionBackground,\n    Severity.Critical => Theme.SystemCriticalBackground,\n    _                  => Theme.SystemNeutralBackground,\n}).Padding(<span class=\"hljs-number\">12</span>).CornerRadius(<span class=\"hljs-number\">4</span>);\n</code></pre>\n<p>Pair this with <a href=\"/commanding/\"><code>commanding</code></a> to render the same banner\non success / failure of a <code>Command&#x3C;T></code>.</p>\n<h3 id=\"app-level-token-override\">App-level token override<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#app-level-token-override\">#</a></h3>\n<p>When the design system needs a brand color not in the WinUI palette,\ndefine it once in <code>Application.Resources</code> (or a merged dictionary) and\nreach for it through <code>Theme.Ref</code>:</p>\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-comment\">&#x3C;!-- App.xaml --></span>\n<span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">Application.Resources</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ResourceDictionary</span>></span>\n    <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ResourceDictionary.ThemeDictionaries</span>></span>\n      <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ResourceDictionary</span> <span class=\"hljs-attr\">x:Key</span>=<span class=\"hljs-string\">\"Light\"</span>></span>\n        <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">SolidColorBrush</span> <span class=\"hljs-attr\">x:Key</span>=<span class=\"hljs-string\">\"BrandPrimaryBrush\"</span> <span class=\"hljs-attr\">Color</span>=<span class=\"hljs-string\">\"#005FB8\"</span> /></span>\n      <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ResourceDictionary</span>></span>\n      <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ResourceDictionary</span> <span class=\"hljs-attr\">x:Key</span>=<span class=\"hljs-string\">\"Dark\"</span>></span>\n        <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">SolidColorBrush</span> <span class=\"hljs-attr\">x:Key</span>=<span class=\"hljs-string\">\"BrandPrimaryBrush\"</span> <span class=\"hljs-attr\">Color</span>=<span class=\"hljs-string\">\"#60AAFA\"</span> /></span>\n      <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ResourceDictionary</span>></span>\n    <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ResourceDictionary.ThemeDictionaries</span>></span>\n  <span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ResourceDictionary</span>></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">Application.Resources</span>></span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\">Button(<span class=\"hljs-string\">\"Buy now\"</span>, BuyAction)\n    .Background(Theme.Ref(<span class=\"hljs-string\">\"BrandPrimaryBrush\"</span>));\n</code></pre>\n<p>The brand color participates in theme swap because the override has a\nlight and dark variant. Defining it without <code>ThemeDictionaries</code>\nfreezes it to one theme — exactly the trap <code>Theme.Ref</code> is built to\nprevent.</p>\n<h3 id=\"per-element-theme-override\">Per-element theme override<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#per-element-theme-override\">#</a></h3>\n<p>A single sub-tree can opt out of the app theme via\n<a href=\"/styling/\"><code>RequestedTheme</code></a> — useful for a video player surface or\na print-preview pane that should always look light:</p>\n<pre><code class=\"hljs language-csharp\">ScrollView(content).RequestedTheme(ElementTheme.Light);\n</code></pre>\n<p>The walk in <code>Theme.Resolve</code> picks up the local override first, so\nevery <code>ThemeRef</code> inside that sub-tree resolves against the light\ndictionary even when the rest of the app is dark.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"hardcoded-color-literal\">Hardcoded color literal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hardcoded-color-literal\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Save\"</span>, () => { }).Background(<span class=\"hljs-string\">\"#0066CC\"</span>);\n<span class=\"hljs-comment\">//                                    ^^^^^^^^^^</span>\n<span class=\"hljs-comment\">// REACTOR_THEME_001 — hardcoded color literal on a theme-aware modifier.</span>\n<span class=\"hljs-comment\">// The button stays blue when the user flips to dark mode.</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ThemeRefGood</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        Button(<span class=\"hljs-string\">\"Click me\"</span>, () => { }).Background(Theme.Accent);\n}\n</code></pre>\n<p>The literal locks the value to one theme; the token participates in\nthe swap. The analyzer surfaces this at build time so a regression\ncan't ship; treat the warning as an error in CI.</p>\n<h3 id=\"light-only-test-missing-the-dark-regression\">Light-only test missing the dark regression<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#light-only-test-missing-the-dark-regression\">#</a></h3>\n<p>A common review mistake: the screenshot in the PR is light-mode, the\nreviewer signs off, and the dark-mode breakage lands on main. The\n<a href=\"/testing/\">testing</a> page covers the fixture pattern that mounts the\nsame component twice — once with <code>RequestedTheme(Light)</code>, once with\n<code>(Dark)</code> — and snapshots both.</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Theory</span>]\n[<span class=\"hljs-meta\">InlineData(ElementTheme.Light)</span>]\n[<span class=\"hljs-meta\">InlineData(ElementTheme.Dark)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">StatusBanner_renders_in_both_themes</span>(<span class=\"hljs-params\">ElementTheme theme</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> rendered = Mount(<span class=\"hljs-keyword\">new</span> StatusBanner(<span class=\"hljs-string\">\"Saved\"</span>, Severity.Success));\n    rendered = rendered.WithModifier(m => m.RequestedTheme(theme));\n    Assert.Empty(AccessibilityScanner.Scan(rendered));\n}\n</code></pre>\n<h3 id=\"resolving-a-non-themed-key-as-a-themeref\">Resolving a non-themed key as a ThemeRef<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#resolving-a-non-themed-key-as-a-themeref\">#</a></h3>\n<p><code>Theme.Ref(\"MyAppHeaderBackground\")</code> against a resource that exists\nonly in <code>Application.Resources</code> (not in a <code>ThemeDictionaries</code> entry)\nreturns a single brush regardless of theme. The page renders, but the\ncolor doesn't swap. If the goal is theme-aware, the resource must live\nin <code>ThemeDictionaries</code>; otherwise hardcode the color in app code and\nmark it intentional with a comment.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Reach for a typed token first, the <code>Theme.Ref</code> escape hatch\nsecond.</strong> Typed tokens land with IntelliSense and a doc comment; the\nstring keys do not. If a color is missing from the typed surface and\nit's a stable WinUI brush, add it to <code>Theme.cs</code> rather than\nproliferating <code>Theme.Ref(\"…\")</code> calls.</p>\n<p><strong>Treat REACTOR_THEME_001 as an error in CI.</strong> The cost of fixing a\nhardcoded color is one line; the cost of a dark-theme regression\nlanding on main is a customer report.</p>\n<p><strong>Don't burn cycles re-resolving on every render.</strong> <code>ThemeRef</code> is a\nrecord struct — a stack allocation. The resolver is called once per\nmodifier application; <code>UseColorScheme</code> only re-runs render when the\nscheme actually changes.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Concept</th><th>API</th><th>Notes</th></tr></thead><tbody><tr><td>Token</td><td><code>Theme.&#x3C;Name></code></td><td>35 typed accessors in <code>src/Reactor/Core/Theme.cs</code>.</td></tr><tr><td>Escape hatch</td><td><code>Theme.Ref(key)</code></td><td>Any XAML resource key, including app-level overrides.</td></tr><tr><td>Reactive read</td><td><code>UseColorScheme()</code></td><td>Returns <code>ColorScheme</code>; triggers re-render on swap.</td></tr><tr><td>Per-element override</td><td><code>.RequestedTheme(ElementTheme)</code></td><td>Walks visual tree, resolves nearest override.</td></tr><tr><td>Analyzer</td><td><code>REACTOR_THEME_001</code></td><td>Flags hardcoded literals on theme-aware modifiers.</td></tr><tr><td>WinUI surface</td><td><code>XamlControlsResources</code></td><td>Owning ThemeDictionary for every named token.</td></tr></tbody></table>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — Previous in the styling chain: the\nmodifier surface that consumes <code>ThemeRef</code> values.</li>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — Next: animating between themed\nvalues; how the brush transitions through a theme swap.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — Contrast budgets that\ndetermine which token to reach for in each surface.</li>\n<li><strong><a href=\"/devtools-internals/\">Devtools Internals</a></strong> — How the dev-menu's\ntheme-swap toggle drives the same resolver this page documents.</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — Where the\nREACTOR_THEME_001 analyzer is listed alongside the rest of the\nenforced idioms.</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Colors . A Microsoft.UI.Reactor (Reactor) theme token is a name for a color that the system resolves at render time. The same token ( Theme.Accent ) emits a different Brush depending on whether the app is in light mode, dark mode, or high contrast — and switches without a re-render when the user flips themes. The alternative — a literal like \"#0066CC\" baked into a modifier — locks the value to a single visual mode and silently breaks dark theme the moment a user opens settings. Tokens exist so the styling layer can hand a brush to a control without caring which theme is active. The most common mistake is reaching for a literal because \"I'll just hardcode this one\"; the REACTOR_THEME_001 analyzer catches that at build time so it cannot ship. Theming Tokens # Reactor exposes 35 named theme tokens on the static Theme class plus a Theme.Ref(string) escape hatch for any XAML resource key. Tokens resolve through WinUI's resource system ( XamlControlsResources + the app's ThemeDictionaries ), so the values they produce match the design system Microsoft ships in WinUI Fluent colors . Lead snippet # class SwatchGrid : Component { public override Element Render () => ScrollView( VStack( 16 , Heading( \"Theme tokens\" ), SwatchSection( \"Accent\" , new [] { ( \"Accent\" , Theme.Accent), ( \"AccentSecondary\" , Theme.AccentSecondary), ( \"AccentTertiary\" , Theme.AccentTertiary), ( \"AccentDisabled\" , Theme.AccentDisabled), }), SwatchSection( \"Text\" , new [] { ( \"PrimaryText\" , Theme.PrimaryText), ( \"SecondaryText\" , Theme.SecondaryText), ( \"TertiaryText\" , Theme.TertiaryText), ( \"DisabledText\" , Theme.DisabledText), ( \"AccentText\" , Theme.AccentText), }), SwatchSection( \"Surfaces\" , new [] { ( \"SolidBackground\" , Theme.SolidBackground), ( \"CardBackground\" , Theme.CardBackground), ( \"SmokeFill\" , Theme.SmokeFill), ( \"SubtleFill\" , Theme.SubtleFill), ( \"LayerFill\" , Theme.LayerFill), }), SwatchSection( \"Control fill\" , new [] { ( \"ControlFill\" , Theme.ControlFill), ( \"ControlFillSecondary\" , Theme.ControlFillSecondary), ( \"ControlFillTertiary\" , Theme.ControlFillTertiary), ( \"ControlFillDisabled\" , Theme.ControlFillDisabled), ( \"ControlFillInputActive\" , Theme.ControlFillInputActive), }), SwatchSection( \"Stroke\" , new [] { ( \"CardStroke\" , Theme.CardStroke), ( \"SurfaceStroke\" , Theme.SurfaceStroke), ( \"DividerStroke\" , Theme.DividerStroke), ( \"ControlStroke\" , Theme.ControlStroke), ( \"ControlStrokeSecondary\" , Theme.ControlStrokeSecondary), }), SwatchSection( \"Signal\" , new [] { ( \"SystemAttention\" , Theme.SystemAttention), ( \"SystemSuccess\" , Theme.SystemSuccess), ( \"SystemCaution\" , Theme.SystemCaution), ( \"SystemCritical\" , Theme.SystemCritical), ( \"SystemNeutral\" , Theme.SystemNeutral), ( \"SystemSolidNeutral\" , Theme.SystemSolidNeutral), }) ).Padding( 20 ) ); private static Element SwatchSection ( string title, ( string Name, ThemeRef Ref )[] tokens) => VStack( 8 , SubHeading(title), VStack( 4 , tokens.Select(t => Row(t.Name, t.Ref)).ToArray()) ); private static Element Row ( string name, ThemeRef token ) => HStack( 12 , new BorderElement(Empty()) .Background(token) .Size( 40 , 24 ) .WithBorder( \"#DDDDDD\" ), TextBlock(name).Width( 220 ), TextBlock(token.ResourceKey).Opacity( 0.6 ) ); } The same grid, both themes. Every cell is the result of ThemeRef resolution against the active ThemeDictionary ; switching themes is a property update on the host, not a re-render of the component. Caveat: A hardcoded color string in .Background(\"#0066CC\") skips the theme swap entirely. The button stays blue when the user flips to dark mode, and white text on a now-still-blue background still satisfies WCAG against light's neutral — but the dark-mode neutral behind it makes the page look broken. Use Theme.Accent (or the matching Theme.Ref(key) ) on .Background / .Foreground / .WithBorder ; REACTOR_THEME_001 flags the literal at build time so the regression never reaches review. How resolution works # ThemeRef is a readonly record struct holding the resource key. When a modifier like .Background(token) reaches the reconciler, the framework walks from the materialized FrameworkElement up the visual tree looking for the nearest RequestedTheme , falls back to ActualTheme , then Application.RequestedTheme . The brush for the chosen theme name comes from Application.Resources.ThemeDictionaries[name] (or the nearest merged dictionary). class ThemeRefGood : Component { public override Element Render () => Button( \"Click me\" , () => { }).Background(Theme.Accent); } When the user flips the system theme, WinUI raises ActualThemeChanged ; Reactor subscribes and updates the brush on every element that referenced a ThemeRef . Components that branch on the scheme use UseColorScheme : // UseColorScheme reads the current scheme (Light / Dark) reactively so a // component can branch on the value without re-implementing the resolver. class SchemeAwareBadge : Component { public override Element Render () { var scheme = UseColorScheme(); var label = scheme == ColorScheme.Dark ? \"Dark mode\" : \"Light mode\" ; return TextBlock(label).Foreground(Theme.PrimaryText); } } UseColorScheme() returns the current ColorScheme (Light / Dark / HighContrast) and re-runs render when the value changes. Reach for it only when the branch can't be expressed as a token (e.g. a non-color decision like which icon to render). Token catalog # 35 named tokens across 6 groups, plus the Theme.Ref(string) escape hatch. Accent (4 tokens) # Token WinUI key Where to use Theme.Accent AccentFillColorDefaultBrush Primary action button background. Theme.AccentSecondary AccentFillColorSecondaryBrush Pressed / hover accent variants. Theme.AccentTertiary AccentFillColorTertiaryBrush Subtle accent surfaces. Theme.AccentDisabled AccentFillColorDisabledBrush Disabled state on an accent control. Text (5 tokens) # Token WinUI key Where to use Theme.PrimaryText TextFillColorPrimaryBrush Headlines and body. Theme.SecondaryText TextFillColorSecondaryBrush Supporting copy. Theme.TertiaryText TextFillColorTertiaryBrush Captions and timestamps. Theme.DisabledText TextFillColorDisabledBrush Disabled labels. Theme.AccentText AccentTextFillColorPrimaryBrush Inline accent text (links, badges). Surfaces (5 tokens) # Token WinUI key Where to use Theme.SolidBackground SolidBackgroundFillColorBaseBrush Page background. Theme.CardBackground CardBackgroundFillColorDefaultBrush Card / panel container. Theme.SmokeFill SmokeFillColorDefaultBrush Modal scrim. Theme.SubtleFill SubtleFillColorSecondaryBrush Hover wash for list rows. Theme.LayerFill LayerFillColorDefaultBrush Stacked-card layer. Control fill (5 tokens) # Token WinUI key Where to use Theme.ControlFill ControlFillColorDefaultBrush Default neutral control surface. Theme.ControlFillSecondary ControlFillColorSecondaryBrush Hover state. Theme.ControlFillTertiary ControlFillColorTertiaryBrush Pressed state. Theme.ControlFillDisabled ControlFillColorDisabledBrush Disabled. Theme.ControlFillInputActive ControlFillColorInputActiveBrush Active TextBox / NumberBox fill. Stroke (5 tokens) # Token WinUI key Where to use Theme.CardStroke CardStrokeColorDefaultBrush Card outline. Theme.SurfaceStroke SurfaceStrokeColorDefaultBrush Page-level dividers. Theme.DividerStroke DividerStrokeColorDefaultBrush Inline list separators. Theme.ControlStroke ControlStrokeColorDefaultBrush Default control border. Theme.ControlStrokeSecondary ControlStrokeColorSecondaryBrush Focus / pressed border. Signal (11 tokens) # Token WinUI key Where to use Theme.SystemAttention SystemFillColorAttentionBrush Attention call-out. Theme.SystemSuccess SystemFillColorSuccessBrush Success state. Theme.SystemCaution SystemFillColorCautionBrush Warning state. Theme.SystemCritical SystemFillColorCriticalBrush Error state. Theme.SystemNeutral SystemFillColorNeutralBrush Info / neutral. Theme.SystemSolidNeutral SystemFillColorSolidNeutralBrush Solid neutral surface. Theme.SystemAttentionBackground SystemFillColorAttentionBackgroundBrush Attention banner background. Theme.SystemSuccessBackground SystemFillColorSuccessBackgroundBrush Success banner background. Theme.SystemCautionBackground SystemFillColorCautionBackgroundBrush Warning banner background. Theme.SystemCriticalBackground SystemFillColorCriticalBackgroundBrush Error banner background. Theme.SystemNeutralBackground SystemFillColorNeutralBackgroundBrush Info banner background. ( Theme.SystemSolidAttention rounds it to 35 + the Theme.Ref(key) escape hatch — total surface ≥ 37 per spec §5.3.) Custom keys # // Reference any XAML resource by string key — covers app-level overrides // and any token Reactor doesn't surface as a typed accessor. Button( \"Custom\" , () => { }) .Background(Theme.Ref( \"MyAppTitleBarBackground\" )); Patterns # Theming an InfoBar by severity # The signal-background tokens pair with the foreground signal tokens to produce inline status banners that read correctly in either theme. Element StatusBanner ( string text, Severity severity ) => HStack( 8 , TextBlock(text).Foreground(severity switch { Severity.Success => Theme.SystemSuccess, Severity.Warning => Theme.SystemCaution, Severity.Critical => Theme.SystemCritical, _ => Theme.SystemNeutral, }) ).Background(severity switch { Severity.Success => Theme.SystemSuccessBackground, Severity.Warning => Theme.SystemCautionBackground, Severity.Critical => Theme.SystemCriticalBackground, _ => Theme.SystemNeutralBackground, }).Padding( 12 ).CornerRadius( 4 ); Pair this with commanding to render the same banner on success / failure of a Command&#x3C;T> . App-level token override # When the design system needs a brand color not in the WinUI palette, define it once in Application.Resources (or a merged dictionary) and reach for it through Theme.Ref : &#x3C;!-- App.xaml --> &#x3C; Application.Resources > &#x3C; ResourceDictionary > &#x3C; ResourceDictionary.ThemeDictionaries > &#x3C; ResourceDictionary x:Key = \"Light\" > &#x3C; SolidColorBrush x:Key = \"BrandPrimaryBrush\" Color = \"#005FB8\" /> &#x3C;/ ResourceDictionary > &#x3C; ResourceDictionary x:Key = \"Dark\" > &#x3C; SolidColorBrush x:Key = \"BrandPrimaryBrush\" Color = \"#60AAFA\" /> &#x3C;/ ResourceDictionary > &#x3C;/ ResourceDictionary.ThemeDictionaries > &#x3C;/ ResourceDictionary > &#x3C;/ Application.Resources > Button( \"Buy now\" , BuyAction) .Background(Theme.Ref( \"BrandPrimaryBrush\" )); The brand color participates in theme swap because the override has a light and dark variant. Defining it without ThemeDictionaries freezes it to one theme — exactly the trap Theme.Ref is built to prevent. Per-element theme override # A single sub-tree can opt out of the app theme via RequestedTheme — useful for a video player surface or a print-preview pane that should always look light: ScrollView(content).RequestedTheme(ElementTheme.Light); The walk in Theme.Resolve picks up the local override first, so every ThemeRef inside that sub-tree resolves against the light dictionary even when the rest of the app is dark. Common Mistakes # Hardcoded color literal # // Don't: Button( \"Save\" , () => { }).Background( \"#0066CC\" ); // ^^^^^^^^^^ // REACTOR_THEME_001 — hardcoded color literal on a theme-aware modifier. // The button stays blue when the user flips to dark mode. class ThemeRefGood : Component { public override Element Render () => Button( \"Click me\" , () => { }).Background(Theme.Accent); } The literal locks the value to one theme; the token participates in the swap. The analyzer surfaces this at build time so a regression can't ship; treat the warning as an error in CI. Light-only test missing the dark regression # A common review mistake: the screenshot in the PR is light-mode, the reviewer signs off, and the dark-mode breakage lands on main. The testing page covers the fixture pattern that mounts the same component twice — once with RequestedTheme(Light) , once with (Dark) — and snapshots both. [ Theory ] [ InlineData(ElementTheme.Light) ] [ InlineData(ElementTheme.Dark) ] public void StatusBanner_renders_in_both_themes ( ElementTheme theme ) { var rendered = Mount( new StatusBanner( \"Saved\" , Severity.Success)); rendered = rendered.WithModifier(m => m.RequestedTheme(theme)); Assert.Empty(AccessibilityScanner.Scan(rendered)); } Resolving a non-themed key as a ThemeRef # Theme.Ref(\"MyAppHeaderBackground\") against a resource that exists only in Application.Resources (not in a ThemeDictionaries entry) returns a single brush regardless of theme. The page renders, but the color doesn't swap. If the goal is theme-aware, the resource must live in ThemeDictionaries ; otherwise hardcode the color in app code and mark it intentional with a comment. Tips # Reach for a typed token first, the Theme.Ref escape hatch second. Typed tokens land with IntelliSense and a doc comment; the string keys do not. If a color is missing from the typed surface and it's a stable WinUI brush, add it to Theme.cs rather than proliferating Theme.Ref(\"…\") calls. Treat REACTOR_THEME_001 as an error in CI. The cost of fixing a hardcoded color is one line; the cost of a dark-theme regression landing on main is a customer report. Don't burn cycles re-resolving on every render. ThemeRef is a record struct — a stack allocation. The resolver is called once per modifier application; UseColorScheme only re-runs render when the scheme actually changes. Reference # Concept API Notes Token Theme.&#x3C;Name> 35 typed accessors in src/Reactor/Core/Theme.cs . Escape hatch Theme.Ref(key) Any XAML resource key, including app-level overrides. Reactive read UseColorScheme() Returns ColorScheme ; triggers re-render on swap. Per-element override .RequestedTheme(ElementTheme) Walks visual tree, resolves nearest override. Analyzer REACTOR_THEME_001 Flags hardcoded literals on theme-aware modifiers. WinUI surface XamlControlsResources Owning ThemeDictionary for every named token. Next Steps # Styling — Previous in the styling chain: the modifier surface that consumes ThemeRef values. Animation — Next: animating between themed values; how the brush transitions through a theme swap. Accessibility — Contrast budgets that determine which token to reach for in each surface. Devtools Internals — How the dev-menu's theme-swap toggle drives the same resolver this page documents. Rules of Reactor — Where the REACTOR_THEME_001 analyzer is listed alongside the rest of the enforced idioms.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/theming-tokens.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/theming-tokens.md",
      "headings": [
        {
          "depth": 2,
          "id": "lead-snippet",
          "text": "Lead snippet"
        },
        {
          "depth": 2,
          "id": "how-resolution-works",
          "text": "How resolution works"
        },
        {
          "depth": 2,
          "id": "token-catalog",
          "text": "Token catalog"
        },
        {
          "depth": 3,
          "id": "accent-4-tokens",
          "text": "Accent (4 tokens)"
        },
        {
          "depth": 3,
          "id": "text-5-tokens",
          "text": "Text (5 tokens)"
        },
        {
          "depth": 3,
          "id": "surfaces-5-tokens",
          "text": "Surfaces (5 tokens)"
        },
        {
          "depth": 3,
          "id": "control-fill-5-tokens",
          "text": "Control fill (5 tokens)"
        },
        {
          "depth": 3,
          "id": "stroke-5-tokens",
          "text": "Stroke (5 tokens)"
        },
        {
          "depth": 3,
          "id": "signal-11-tokens",
          "text": "Signal (11 tokens)"
        },
        {
          "depth": 3,
          "id": "custom-keys",
          "text": "Custom keys"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "theming-an-infobar-by-severity",
          "text": "Theming an InfoBar by severity"
        },
        {
          "depth": 3,
          "id": "app-level-token-override",
          "text": "App-level token override"
        },
        {
          "depth": 3,
          "id": "per-element-theme-override",
          "text": "Per-element theme override"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "hardcoded-color-literal",
          "text": "Hardcoded color literal"
        },
        {
          "depth": 3,
          "id": "light-only-test-missing-the-dark-regression",
          "text": "Light-only test missing the dark regression"
        },
        {
          "depth": 3,
          "id": "resolving-a-non-themed-key-as-a-themeref",
          "text": "Resolving a non-themed key as a ThemeRef"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Dev Tooling",
      "path": "dev-tooling.md",
      "slug": "/dev-tooling/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s development workflow is built around fast feedback. The\nruntime is designed to surface the work it is doing — every render,\nevery reconcile, every effect, every dispatcher hop — through a small\nset of dedicated tools so you can see, attribute, and reason about\nwhat your app is doing without a profiler. The <code>mur</code> CLI, preview\nmode under <code>dotnet watch</code>, the MCP server, the VS Code panel, the\nin-app dev menu, and the reconcile-highlight / layout-cost overlays\nare all variants of the same idea: the runtime publishes its work,\nand the inner loop lets you read it. The cost is zero in retail —\nevery devtools entry point lives behind a build-time opt-in\n(<a href=\"/components/\"><code>devtools: true</code></a>) plus a session-time opt-in\n(<code>--devtools app</code>) so end users never see anything you don't intend\nto ship.</p>\n<h1 id=\"dev-tooling\">Dev Tooling<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dev-tooling\">#</a></h1>\n<p>Reactor's inner loop centers on <code>dotnet watch</code> and preview mode: you edit\ncode, save, and see changes in a running window — no manual restart, no\nstate-resetting class reload most of the time. Surrounding that loop are\nfive complementary surfaces — the <a href=\"#the-mur-cli\"><code>mur</code> CLI</a>, the\n<a href=\"#mcp-server\">MCP server</a>, the <a href=\"#vs-code-panel\">VS Code panel</a>, the\n<a href=\"#in-app-dev-menu\">in-app dev menu</a>, and the\n<a href=\"#runtime-overlays\">runtime overlays</a> — each of which lights up only\nwhen you ask for it.</p>\n<h2 id=\"preview-mode\">Preview Mode<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#preview-mode\">#</a></h2>\n<p>Pass <code>preview: true</code> to <code>ReactorApp.Run</code> to enable preview mode. This\nconnects your app to the Reactor hot reload pipeline:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Program entry point — this is the entire App.cs file:</span>\n<span class=\"hljs-comment\">// ReactorApp.Run&#x3C;DevToolingApp>(\"Dev Tooling Demo\",</span>\n<span class=\"hljs-comment\">//     width: 600, height: 450</span>\n<span class=\"hljs-comment\">// #if DEBUG</span>\n<span class=\"hljs-comment\">//     , preview: true</span>\n<span class=\"hljs-comment\">// #endif</span>\n<span class=\"hljs-comment\">// );</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// The preview flag enables hot reload via dotnet watch.</span>\n<span class=\"hljs-comment\">// In Release builds, preview is omitted entirely.</span>\n</code></pre>\n<p>The <code>#if DEBUG</code> guard ensures preview mode is only active in debug builds.\nRelease builds skip it entirely — no overhead, no extra dependencies. Inside\na <a href=\"/components/\">function component</a>, the same flag is forwarded by\n<code>ReactorApp.Run(\"Title\", ctx => ...)</code>.</p>\n<h2 id=\"running-with-hot-reload\">Running with Hot Reload<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#running-with-hot-reload\">#</a></h2>\n<p>Start your app with <code>dotnet watch</code>:</p>\n<!-- ai:lock -->\n<pre><code>dotnet watch run\n</code></pre>\n<!-- /ai:lock -->\n<p>This launches the app and watches your <code>.cs</code> files for changes. When you\nsave, <code>dotnet watch</code> recompiles and Reactor reloads the component tree.\nYour app state resets, but the window stays open.</p>\n<p>Here's what the preview app looks like running:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">DevToolingApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">var</span> (message, setMessage) = UseState(<span class=\"hljs-string\">\"Edit this code and save!\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            Heading(<span class=\"hljs-string\">\"Preview Mode Demo\"</span>),\n            TextBlock(message).FontSize(<span class=\"hljs-number\">16</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Click me\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>)),\n                TextBlock(<span class=\"hljs-string\">$\"Clicked <span class=\"hljs-subst\">{count}</span> times\"</span>).SemiBold()\n            ),\n            TextBox(message, setMessage, placeholderText: <span class=\"hljs-string\">\"Type something\"</span>)\n                .Width(<span class=\"hljs-number\">300</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dev-tooling/preview-app.png\" alt=\"Preview mode demo\"></p>\n<p>Edit the <code>message</code> default value or add a new element, save the file, and\nwatch the window update.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Hot reload <strong>resets state on every recompile</strong>. <code>UseState</code>, <code>UseReducer</code>,\nand <code>UsePersisted</code> (Application scope) all reinitialize. For state you\nwant to survive a reload, use <code>UsePersisted</code> with <code>PersistedScope.Window</code>\nor store the value in an <code>Observable&#x3C;T></code> field — see\n<a href=\"/persistence/\">persistence</a>. Avoid wiring critical app state through a\nplain <code>UseState</code> if your loop depends on the value staying put across\nsaves.</p>\n</blockquote>\n<h2 id=\"function-component-entry-point\">Function Component Entry Point<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#function-component-entry-point\">#</a></h2>\n<p>For quick experiments, skip the class entirely. Pass a lambda to\n<a href=\"/components/\"><code>ReactorApp.Run</code></a>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Alternative: inline function component, no class needed</span>\n<span class=\"hljs-comment\">// ReactorApp.Run(\"Quick Test\", ctx =></span>\n<span class=\"hljs-comment\">// {</span>\n<span class=\"hljs-comment\">//     var (n, setN) = ctx.UseState(0);</span>\n<span class=\"hljs-comment\">//     return VStack(12,</span>\n<span class=\"hljs-comment\">//         TextBlock($\"Count: {n}\").FontSize(20),</span>\n<span class=\"hljs-comment\">//         Button(\"+1\", () => setN(n + 1))</span>\n<span class=\"hljs-comment\">//     ).Padding(24);</span>\n<span class=\"hljs-comment\">// }, width: 400, height: 300);</span>\n</code></pre>\n<p>This is useful for throwaway prototypes or testing a single interaction.\nYou get the same hot-reload behavior — edit the lambda, save, see the\nresult. The <a href=\"/recipes/\">recipes folder</a> collects more elaborate\nexamples that fit this shape.</p>\n<h2 id=\"the-mur-cli\">The <code>mur</code> CLI<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-mur-cli\">#</a></h2>\n<p><code>mur</code> is Reactor's repo-aware command-line tool. It is the canonical\nentry point for the doc pipeline, the localization workflow, the\ndevtools server, and a handful of repo-maintenance utilities. The\nsubcommands map one-to-one to the workflows below.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Subcommand</th><th>Purpose</th><th>Common invocation</th></tr></thead><tbody><tr><td><code>mur docs</code></td><td>Compile the docset (templates + doc apps → <code>docs/guide/</code>)</td><td><code>mur docs compile</code></td></tr><tr><td><code>mur docs render-diagrams</code></td><td>Render <code>.mmd</code> Mermaid diagrams to <code>.svg</code> for fast inner-loop iteration</td><td><code>mur docs render-diagrams --topic architecture-overview</code></td></tr><tr><td><code>mur docs new-diagram &#x3C;topic> &#x3C;id></code></td><td>Scaffold a new Mermaid <code>.mmd</code> for a topic</td><td><code>mur docs new-diagram hooks slot-table</code></td></tr><tr><td><code>mur loc</code></td><td>Run the localization pipeline (extract strings, validate <code>.resw</code>, generate manifests)</td><td><code>mur loc extract</code></td></tr><tr><td><code>mur devtools</code></td><td>Start the MCP server for VS Code or agent integration</td><td><code>mur devtools serve</code></td></tr><tr><td><code>mur check</code></td><td>Repo-health checks (cref validity, namespace policy, \"did you mean\" suggestions)</td><td><code>mur check</code></td></tr><tr><td><code>mur pack-local</code> / <code>mur clean-local</code></td><td>Package / clean the local NuGet feed for samples that consume <code>Microsoft.UI.Reactor</code> as a package</td><td><code>mur pack-local</code></td></tr></tbody></table>\n<p><code>mur docs compile</code> is the workflow you reach for most often. See\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/contributing/doc-pipeline.md\">the doc-pipeline contributor guide</a>\nfor the full surface and the <code>--validate-only</code>, <code>--skip-screenshots</code>,\n<code>--skip-diagrams</code>, <code>--skip-reference</code>, and <code>--tier=&#x3C;stub|solid|comprehensive></code>\nflags that make the inner loop fast.</p>\n<h2 id=\"mcp-server\">MCP Server<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mcp-server\">#</a></h2>\n<p><code>mur devtools serve</code> starts a <a href=\"https://modelcontextprotocol.io/\">Model Context Protocol</a>\nserver that surfaces a small inventory of the running Reactor universe to\nan external agent or editor: the doc topic list, the API reference index,\ndiagnostic-rule descriptions, and a <code>compile</code> action that wraps\n<code>mur docs compile</code>. The server is <strong>not</strong> auto-started by <code>dotnet watch</code> —\nyou launch it explicitly when you want agent integration.</p>\n<p>The deeper protocol surface lives in\n<a href=\"/devtools-internals/\">DevTools Internals</a>. The\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/contributing/devtools.md\"><code>docs/contributing/devtools.md</code></a> guide is\nthe operational reference (how to plug it into Claude Desktop / VS Code).</p>\n<h2 id=\"vs-code-panel\">VS Code Panel<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#vs-code-panel\">#</a></h2>\n<p>The Reactor VS Code extension lives in <code>src/vscode-reactor/</code> and provides\na side panel that:</p>\n<ul>\n<li>Lists the running app's component tree.</li>\n<li>Highlights the source line for any element you click in the tree.</li>\n<li>Renders the latest screenshot capture next to your editor (the same\nPNGs the doc pipeline produces).</li>\n<li>Exposes a \"compile docs\" button that shells out to <code>mur docs compile</code>.</li>\n</ul>\n<p>The extension talks to the MCP server (above), so a panel session is\njust a long-lived <code>mur devtools serve</code> plus a UI on top. Reactor has\nno special editor requirement beyond the standard C# Dev Kit — the\npanel is additive.</p>\n<h2 id=\"in-app-dev-menu\">In-App Dev Menu<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#in-app-dev-menu\">#</a></h2>\n<p>For the dev surface that lives <strong>inside</strong> a running app — a \"Dev\" item\nin the titlebar, debug overlays you can toggle, one-off commands —\nReactor exposes three small primitives: <code>UseDevtools()</code>,\n<code>DevtoolsMenu(...)</code>, and <code>Observable&#x3C;T></code>. Two independent signals\ncombine:</p>\n<ol>\n<li>\n<p><strong>Build-time opt-in</strong> — pass <code>devtools: true</code> to <code>ReactorApp.Run</code>:</p>\n<pre><code class=\"hljs language-csharp\">ReactorApp.Run&#x3C;App>(<span class=\"hljs-string\">\"My App\"</span>, devtools: <span class=\"hljs-literal\">true</span>);\n</code></pre>\n<p>This is a capability gate — it does <strong>not</strong> by itself show any dev\nUI. Ship retail binaries with <code>devtools: true</code> safely; end users\ndon't see anything unless they ask for it.</p>\n</li>\n<li>\n<p><strong>Session opt-in</strong> — run the app with <code>--devtools app</code>:</p>\n<pre><code>myapp.exe --devtools app\n</code></pre>\n</li>\n</ol>\n<p><code>UseDevtools()</code> returns <code>true</code> only when <strong>both</strong> are present. Gate\nany dev-only element with a ternary in <code>Render()</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> dev = ctx.UseDevtools();\n    <span class=\"hljs-keyword\">return</span> VStack(\n        MainContent(),\n        dev ? DebugOverlay() : <span class=\"hljs-literal\">null</span>\n    );\n}\n</code></pre>\n<p><code>DebugOverlay()</code> is only <strong>constructed</strong> when <code>dev</code> is true. In retail\nthat line costs one bool read plus one branch — no element tree\nallocated, no children reconciled. The cost model carries over to\n<a href=\"/devtools-internals/\"><code>DevtoolsMenu</code></a>, which renders itself as a\ntitlebar item only when <code>UseDevtools()</code> is true:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">TitleBar</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => HStack(\n        Text(<span class=\"hljs-string\">\"My App\"</span>), Spacer(),\n        DevtoolsMenu(() => <span class=\"hljs-keyword\">new</span> MenuFlyoutItemBase[]\n        {\n            ToggleMenuItem(<span class=\"hljs-string\">\"Debug UI\"</span>,\n                AppFlags.DebugUI.Value,\n                v => AppFlags.DebugUI.Value = v),\n            MenuSeparator(),\n            MenuItem(<span class=\"hljs-string\">\"Clear cache\"</span>, () => CacheService.Clear()),\n            MenuItem(<span class=\"hljs-string\">\"Reload\"</span>,      () => Application.Current.Reload()),\n        })\n    );\n}\n</code></pre>\n<p><code>Observable&#x3C;T></code> is the lightweight INPC cell that backs flags like\n<code>AppFlags.DebugUI</code>. Declare them as <code>static readonly</code> fields:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AppFlags</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Observable&#x3C;<span class=\"hljs-built_in\">bool</span>> DebugUI   = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-literal\">false</span>);\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Observable&#x3C;<span class=\"hljs-built_in\">bool</span>> SlowMode  = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-literal\">false</span>);\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Observable&#x3C;<span class=\"hljs-built_in\">bool</span>> ForceDark = <span class=\"hljs-keyword\">new</span>(<span class=\"hljs-literal\">false</span>);\n}\n</code></pre>\n<p>Any component that wants to react to changes subscribes via\n<code>ctx.UseObservable(AppFlags.DebugUI).Value</code> — see\n<a href=\"/advanced/\">Advanced Patterns</a> for the broader observable-binding\nstory.</p>\n<h2 id=\"runtime-overlays\">Runtime Overlays<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#runtime-overlays\">#</a></h2>\n<p>Two overlays attach to a running app when devtools are on:</p>\n<ul>\n<li><strong>Reconcile-highlight overlay.</strong> Flashes a rectangle around any\nelement that the reconciler patched on the most recent render. Useful\nfor catching unintended re-renders (the <a href=\"/rules-of-reactor/\">Rules of Reactor</a>\npage covers the most common causes).</li>\n<li><strong>Layout-cost overlay.</strong> Tints elements by the cost of their last\nmeasure / arrange pass, sourced from ETW. The deeper attribution\nstory is in <a href=\"/perf-instrumentation/\">Perf Instrumentation</a>.</li>\n</ul>\n<p>Both overlays render under the <code>DevtoolsMenu</code> toggle group. They are\nzero-cost when off — they consume an ETW listener registration only\nwhen their flag is true.</p>\n<h2 id=\"the-iteration-cycle\">The Iteration Cycle<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-iteration-cycle\">#</a></h2>\n<p>The full inner loop:</p>\n<ol>\n<li><strong>Run</strong> <code>dotnet watch run</code> in your terminal.</li>\n<li><strong>Edit</strong> a component in your editor (with the VS Code panel open\nif you want screenshots and the component tree alongside).</li>\n<li><strong>Save</strong> the file — <code>dotnet watch</code> detects the change and recompiles.</li>\n<li><strong>See</strong> the updated UI in the running window.</li>\n</ol>\n<p>No build step to invoke manually. The app stays running and the window\nstays positioned where you left it.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">IterationDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (items, updateItems) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\n        <span class=\"hljs-keyword\">var</span> (input, setInput) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Heading(<span class=\"hljs-string\">\"Iteration Cycle Demo\"</span>),\n            TextBlock(<span class=\"hljs-string\">\"Add items, then edit this code and save to see hot reload.\"</span>),\n            HStack(<span class=\"hljs-number\">8</span>,\n                TextBox(input, setInput, placeholderText: <span class=\"hljs-string\">\"New item\"</span>)\n                    .Width(<span class=\"hljs-number\">200</span>),\n                Button(<span class=\"hljs-string\">\"Add\"</span>, () =>\n                {\n                    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrWhiteSpace(input))\n                    {\n                        updateItems(list =>\n                        {\n                            <span class=\"hljs-keyword\">var</span> next = <span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>(list) { input };\n                            <span class=\"hljs-keyword\">return</span> next;\n                        });\n                        setInput(<span class=\"hljs-string\">\"\"</span>);\n                    }\n                })\n            ),\n            ForEach(items, item => TextBlock(<span class=\"hljs-string\">$\"  - <span class=\"hljs-subst\">{item}</span>\"</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/dev-tooling/iteration-demo.png\" alt=\"Iteration demo\"></p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"per-feature-debug-flag-with-a-dev-menu-toggle\">Per-feature debug flag with a Dev menu toggle<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#per-feature-debug-flag-with-a-dev-menu-toggle\">#</a></h3>\n<p>Declare a flag as an <code>Observable&#x3C;bool></code> in a static class, expose it\nthrough the Dev menu's <code>ToggleMenuItem</code>, and read it from any component\nvia <code>ctx.UseObservable(...)</code>. Flipping the flag re-renders every\nsubscribed component — no event-wiring boilerplate. The pattern scales\nto a dozen flags without a config UI.</p>\n<h3 id=\"headless-screenshot-capture-for-docs\">Headless screenshot capture for docs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#headless-screenshot-capture-for-docs\">#</a></h3>\n<p>The same preview-mode plumbing the doc pipeline uses is available to\nyour own app: pass <code>preview: true</code> to <code>ReactorApp.Run</code> and the doc-\npipeline screenshot harness can capture the window. See\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/contributing/doc-pipeline.md\"><code>docs/contributing/doc-pipeline.md</code></a>\nfor the harness contract. This is how every page in the docset gets\nits screenshots automatically without an author launching the app by\nhand.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"shipping-with-devtools-true-and-no-session-gate\">Shipping with <code>devtools: true</code> and no session gate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#shipping-with-devtools-true-and-no-session-gate\">#</a></h3>\n<p>Forgetting that <code>devtools: true</code> is the <strong>capability</strong> gate and\n<code>--devtools app</code> is the <strong>activation</strong> gate is the most common\nmistake. A retail binary built with <code>devtools: true</code> shows no dev UI\nto a user — the session-time flag does. If you want dev tools off in\nretail, leave <code>devtools: true</code> in your <code>ReactorApp.Run</code> call; remove\nthe <code>--devtools app</code> flag from your shortcuts.</p>\n<h3 id=\"wiring-real-app-state-through-usestate-during-inner-loop-work\">Wiring real app state through <code>UseState</code> during inner-loop work<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#wiring-real-app-state-through-usestate-during-inner-loop-work\">#</a></h3>\n<p><code>UseState</code> resets on every hot reload. If your loop depends on a\n30-second login flow to reach the screen you are iterating on, store\nthe relevant state in <a href=\"/persistence/\"><code>UsePersisted</code></a> (Window scope)\nor in an <code>Observable&#x3C;T></code> field. The hot reload picks up the new code\nwithout resetting your state.</p>\n<h3 id=\"treating-the-in-app-dev-menu-as-the-only-devtools-surface\">Treating the in-app dev menu as the only devtools surface<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#treating-the-in-app-dev-menu-as-the-only-devtools-surface\">#</a></h3>\n<p>The <code>mur devtools serve</code> MCP server and the VS Code panel are\n<strong>separate</strong> surfaces from the in-app menu. They observe a running\napp from the outside; the in-app menu observes the runtime from\ninside the app itself. Build-time opt-in (<code>devtools: true</code>) is enough\nfor the in-app menu, but the MCP server requires a separate\n<code>mur devtools serve</code> invocation — don't expect it to start\nautomatically.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Keep dotnet watch running.</strong> Don't stop and restart it between\nedits. It handles recompilation and reconnection automatically.</p>\n<p><strong>Use small components.</strong> Smaller components reload faster because\nless of the tree needs to be rebuilt. Extract pieces early.</p>\n<p><strong>Check the terminal.</strong> When hot reload fails (usually a syntax\nerror), <code>dotnet watch</code> prints the error. Fix it and save — it retries.</p>\n<p><strong>Use <a href=\"/components/\">function components</a> for experiments.</strong>\n<code>ReactorApp.Run(\"Test\", ctx => ...)</code> is the fastest way to try an\nidea. No class boilerplate.</p>\n<p><strong>ARM64 builds for benchmarks.</strong> If you're on ARM64 hardware, build\nwith <code>dotnet run -r win-arm64</code> to get native performance numbers.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — Previous: create your first app and learn the basics</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Next: component classes, props, function components, composition</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Learn the state management primitives used inside components</li>\n<li><strong><a href=\"/testing/\">Testing</a></strong> — Headless renderer fixtures, snapshot tests, async test patterns</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — How the dev menu, overlays, and MCP server are implemented</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s development workflow is built around fast feedback. The runtime is designed to surface the work it is doing — every render, every reconcile, every effect, every dispatcher hop — through a small set of dedicated tools so you can see, attribute, and reason about what your app is doing without a profiler. The mur CLI, preview mode under dotnet watch , the MCP server, the VS Code panel, the in-app dev menu, and the reconcile-highlight / layout-cost overlays are all variants of the same idea: the runtime publishes its work, and the inner loop lets you read it. The cost is zero in retail — every devtools entry point lives behind a build-time opt-in ( devtools: true ) plus a session-time opt-in ( --devtools app ) so end users never see anything you don't intend to ship. Dev Tooling # Reactor's inner loop centers on dotnet watch and preview mode: you edit code, save, and see changes in a running window — no manual restart, no state-resetting class reload most of the time. Surrounding that loop are five complementary surfaces — the mur CLI , the MCP server , the VS Code panel , the in-app dev menu , and the runtime overlays — each of which lights up only when you ask for it. Preview Mode # Pass preview: true to ReactorApp.Run to enable preview mode. This connects your app to the Reactor hot reload pipeline: // Program entry point — this is the entire App.cs file: // ReactorApp.Run&#x3C;DevToolingApp>(\"Dev Tooling Demo\", // width: 600, height: 450 // #if DEBUG // , preview: true // #endif // ); // // The preview flag enables hot reload via dotnet watch. // In Release builds, preview is omitted entirely. The #if DEBUG guard ensures preview mode is only active in debug builds. Release builds skip it entirely — no overhead, no extra dependencies. Inside a function component , the same flag is forwarded by ReactorApp.Run(\"Title\", ctx => ...) . Running with Hot Reload # Start your app with dotnet watch : dotnet watch run This launches the app and watches your .cs files for changes. When you save, dotnet watch recompiles and Reactor reloads the component tree. Your app state resets, but the window stays open. Here's what the preview app looks like running: class DevToolingApp : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); var (message, setMessage) = UseState( \"Edit this code and save!\" ); return VStack( 16 , Heading( \"Preview Mode Demo\" ), TextBlock(message).FontSize( 16 ), HStack( 8 , Button( \"Click me\" , () => setCount(count + 1 )), TextBlock( $\"Clicked {count} times\" ).SemiBold() ), TextBox(message, setMessage, placeholderText: \"Type something\" ) .Width( 300 ) ).Padding( 24 ); } } Edit the message default value or add a new element, save the file, and watch the window update. Caveat: Hot reload resets state on every recompile . UseState , UseReducer , and UsePersisted (Application scope) all reinitialize. For state you want to survive a reload, use UsePersisted with PersistedScope.Window or store the value in an Observable&#x3C;T> field — see persistence . Avoid wiring critical app state through a plain UseState if your loop depends on the value staying put across saves. Function Component Entry Point # For quick experiments, skip the class entirely. Pass a lambda to ReactorApp.Run : // Alternative: inline function component, no class needed // ReactorApp.Run(\"Quick Test\", ctx => // { // var (n, setN) = ctx.UseState(0); // return VStack(12, // TextBlock($\"Count: {n}\").FontSize(20), // Button(\"+1\", () => setN(n + 1)) // ).Padding(24); // }, width: 400, height: 300); This is useful for throwaway prototypes or testing a single interaction. You get the same hot-reload behavior — edit the lambda, save, see the result. The recipes folder collects more elaborate examples that fit this shape. The mur CLI # mur is Reactor's repo-aware command-line tool. It is the canonical entry point for the doc pipeline, the localization workflow, the devtools server, and a handful of repo-maintenance utilities. The subcommands map one-to-one to the workflows below. Subcommand Purpose Common invocation mur docs Compile the docset (templates + doc apps → docs/guide/ ) mur docs compile mur docs render-diagrams Render .mmd Mermaid diagrams to .svg for fast inner-loop iteration mur docs render-diagrams --topic architecture-overview mur docs new-diagram &#x3C;topic> &#x3C;id> Scaffold a new Mermaid .mmd for a topic mur docs new-diagram hooks slot-table mur loc Run the localization pipeline (extract strings, validate .resw , generate manifests) mur loc extract mur devtools Start the MCP server for VS Code or agent integration mur devtools serve mur check Repo-health checks (cref validity, namespace policy, \"did you mean\" suggestions) mur check mur pack-local / mur clean-local Package / clean the local NuGet feed for samples that consume Microsoft.UI.Reactor as a package mur pack-local mur docs compile is the workflow you reach for most often. See the doc-pipeline contributor guide for the full surface and the --validate-only , --skip-screenshots , --skip-diagrams , --skip-reference , and --tier=&#x3C;stub|solid|comprehensive> flags that make the inner loop fast. MCP Server # mur devtools serve starts a Model Context Protocol server that surfaces a small inventory of the running Reactor universe to an external agent or editor: the doc topic list, the API reference index, diagnostic-rule descriptions, and a compile action that wraps mur docs compile . The server is not auto-started by dotnet watch — you launch it explicitly when you want agent integration. The deeper protocol surface lives in DevTools Internals . The docs/contributing/devtools.md guide is the operational reference (how to plug it into Claude Desktop / VS Code). VS Code Panel # The Reactor VS Code extension lives in src/vscode-reactor/ and provides a side panel that: Lists the running app's component tree. Highlights the source line for any element you click in the tree. Renders the latest screenshot capture next to your editor (the same PNGs the doc pipeline produces). Exposes a \"compile docs\" button that shells out to mur docs compile . The extension talks to the MCP server (above), so a panel session is just a long-lived mur devtools serve plus a UI on top. Reactor has no special editor requirement beyond the standard C# Dev Kit — the panel is additive. In-App Dev Menu # For the dev surface that lives inside a running app — a \"Dev\" item in the titlebar, debug overlays you can toggle, one-off commands — Reactor exposes three small primitives: UseDevtools() , DevtoolsMenu(...) , and Observable&#x3C;T> . Two independent signals combine: Build-time opt-in — pass devtools: true to ReactorApp.Run : ReactorApp.Run&#x3C;App>( \"My App\" , devtools: true ); This is a capability gate — it does not by itself show any dev UI. Ship retail binaries with devtools: true safely; end users don't see anything unless they ask for it. Session opt-in — run the app with --devtools app : myapp.exe --devtools app UseDevtools() returns true only when both are present. Gate any dev-only element with a ternary in Render() : public override Element Render () { var dev = ctx.UseDevtools(); return VStack( MainContent(), dev ? DebugOverlay() : null ); } DebugOverlay() is only constructed when dev is true. In retail that line costs one bool read plus one branch — no element tree allocated, no children reconciled. The cost model carries over to DevtoolsMenu , which renders itself as a titlebar item only when UseDevtools() is true: class TitleBar : Component { public override Element Render () => HStack( Text( \"My App\" ), Spacer(), DevtoolsMenu(() => new MenuFlyoutItemBase[] { ToggleMenuItem( \"Debug UI\" , AppFlags.DebugUI.Value, v => AppFlags.DebugUI.Value = v), MenuSeparator(), MenuItem( \"Clear cache\" , () => CacheService.Clear()), MenuItem( \"Reload\" , () => Application.Current.Reload()), }) ); } Observable&#x3C;T> is the lightweight INPC cell that backs flags like AppFlags.DebugUI . Declare them as static readonly fields: public static class AppFlags { public static readonly Observable&#x3C; bool > DebugUI = new ( false ); public static readonly Observable&#x3C; bool > SlowMode = new ( false ); public static readonly Observable&#x3C; bool > ForceDark = new ( false ); } Any component that wants to react to changes subscribes via ctx.UseObservable(AppFlags.DebugUI).Value — see Advanced Patterns for the broader observable-binding story. Runtime Overlays # Two overlays attach to a running app when devtools are on: Reconcile-highlight overlay. Flashes a rectangle around any element that the reconciler patched on the most recent render. Useful for catching unintended re-renders (the Rules of Reactor page covers the most common causes). Layout-cost overlay. Tints elements by the cost of their last measure / arrange pass, sourced from ETW. The deeper attribution story is in Perf Instrumentation . Both overlays render under the DevtoolsMenu toggle group. They are zero-cost when off — they consume an ETW listener registration only when their flag is true. The Iteration Cycle # The full inner loop: Run dotnet watch run in your terminal. Edit a component in your editor (with the VS Code panel open if you want screenshots and the component tree alongside). Save the file — dotnet watch detects the change and recompiles. See the updated UI in the running window. No build step to invoke manually. The app stays running and the window stays positioned where you left it. class IterationDemo : Component { public override Element Render () { var (items, updateItems) = UseReducer( new List&#x3C; string >()); var (input, setInput) = UseState( \"\" ); return VStack( 12 , Heading( \"Iteration Cycle Demo\" ), TextBlock( \"Add items, then edit this code and save to see hot reload.\" ), HStack( 8 , TextBox(input, setInput, placeholderText: \"New item\" ) .Width( 200 ), Button( \"Add\" , () => { if (! string .IsNullOrWhiteSpace(input)) { updateItems(list => { var next = new List&#x3C; string >(list) { input }; return next; }); setInput( \"\" ); } }) ), ForEach(items, item => TextBlock( $\" - {item} \" )) ).Padding( 24 ); } } Patterns # Per-feature debug flag with a Dev menu toggle # Declare a flag as an Observable&#x3C;bool> in a static class, expose it through the Dev menu's ToggleMenuItem , and read it from any component via ctx.UseObservable(...) . Flipping the flag re-renders every subscribed component — no event-wiring boilerplate. The pattern scales to a dozen flags without a config UI. Headless screenshot capture for docs # The same preview-mode plumbing the doc pipeline uses is available to your own app: pass preview: true to ReactorApp.Run and the doc- pipeline screenshot harness can capture the window. See docs/contributing/doc-pipeline.md for the harness contract. This is how every page in the docset gets its screenshots automatically without an author launching the app by hand. Common Mistakes # Shipping with devtools: true and no session gate # Forgetting that devtools: true is the capability gate and --devtools app is the activation gate is the most common mistake. A retail binary built with devtools: true shows no dev UI to a user — the session-time flag does. If you want dev tools off in retail, leave devtools: true in your ReactorApp.Run call; remove the --devtools app flag from your shortcuts. Wiring real app state through UseState during inner-loop work # UseState resets on every hot reload. If your loop depends on a 30-second login flow to reach the screen you are iterating on, store the relevant state in UsePersisted (Window scope) or in an Observable&#x3C;T> field. The hot reload picks up the new code without resetting your state. Treating the in-app dev menu as the only devtools surface # The mur devtools serve MCP server and the VS Code panel are separate surfaces from the in-app menu. They observe a running app from the outside; the in-app menu observes the runtime from inside the app itself. Build-time opt-in ( devtools: true ) is enough for the in-app menu, but the MCP server requires a separate mur devtools serve invocation — don't expect it to start automatically. Tips # Keep dotnet watch running. Don't stop and restart it between edits. It handles recompilation and reconnection automatically. Use small components. Smaller components reload faster because less of the tree needs to be rebuilt. Extract pieces early. Check the terminal. When hot reload fails (usually a syntax error), dotnet watch prints the error. Fix it and save — it retries. Use function components for experiments. ReactorApp.Run(\"Test\", ctx => ...) is the fastest way to try an idea. No class boilerplate. ARM64 builds for benchmarks. If you're on ARM64 hardware, build with dotnet run -r win-arm64 to get native performance numbers. Next Steps # Getting Started — Previous: create your first app and learn the basics Components — Next: component classes, props, function components, composition Hooks — Learn the state management primitives used inside components Testing — Headless renderer fixtures, snapshot tests, async test patterns DevTools Internals — How the dev menu, overlays, and MCP server are implemented",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/dev-tooling.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/dev-tooling.md",
      "headings": [
        {
          "depth": 2,
          "id": "preview-mode",
          "text": "Preview Mode"
        },
        {
          "depth": 2,
          "id": "running-with-hot-reload",
          "text": "Running with Hot Reload"
        },
        {
          "depth": 2,
          "id": "function-component-entry-point",
          "text": "Function Component Entry Point"
        },
        {
          "depth": 2,
          "id": "the-mur-cli",
          "text": "The mur CLI"
        },
        {
          "depth": 2,
          "id": "mcp-server",
          "text": "MCP Server"
        },
        {
          "depth": 2,
          "id": "vs-code-panel",
          "text": "VS Code Panel"
        },
        {
          "depth": 2,
          "id": "in-app-dev-menu",
          "text": "In-App Dev Menu"
        },
        {
          "depth": 2,
          "id": "runtime-overlays",
          "text": "Runtime Overlays"
        },
        {
          "depth": 2,
          "id": "the-iteration-cycle",
          "text": "The Iteration Cycle"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "per-feature-debug-flag-with-a-dev-menu-toggle",
          "text": "Per-feature debug flag with a Dev menu toggle"
        },
        {
          "depth": 3,
          "id": "headless-screenshot-capture-for-docs",
          "text": "Headless screenshot capture for docs"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "shipping-with-devtools-true-and-no-session-gate",
          "text": "Shipping with devtools: true and no session gate"
        },
        {
          "depth": 3,
          "id": "wiring-real-app-state-through-usestate-during-inner-loop-work",
          "text": "Wiring real app state through UseState during inner-loop work"
        },
        {
          "depth": 3,
          "id": "treating-the-in-app-dev-menu-as-the-only-devtools-surface",
          "text": "Treating the in-app dev menu as the only devtools surface"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Testing",
      "path": "testing.md",
      "slug": "/testing/",
      "html": "<h1 id=\"testing\">Testing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#testing\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor)'s render loop is deterministic and synchronous. A <code>Component</code>\nmounted in a unit test renders, runs effects, accepts state updates,\nre-renders, and disposes through the same code path the WinUI host\nuses — minus the WinUI tree at the bottom. That makes the unit layer\nfast (an xUnit run for the framework's 800+ tests is seconds), and it\nkeeps test bodies focused on the component's behavior instead of the\nwindowing chrome.</p>\n<p>This page covers the four test layers Reactor uses internally:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Counter</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">8</span>,\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>).FontSize(<span class=\"hljs-number\">20</span>).Bold(),\n            Button(<span class=\"hljs-string\">\"Increment\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n        ).Padding(<span class=\"hljs-number\">16</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/testing/counter.png\" alt=\"Counter component used as the running fixture target\"></p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Test layer</th><th>Project</th><th>When to reach for it</th></tr></thead><tbody><tr><td>Unit</td><td><code>tests/Reactor.Tests/</code></td><td>Hook semantics, reducer logic, modifier chains, analyzer rules. Fastest loop.</td></tr><tr><td>Self-test</td><td><code>tests/Reactor.SelfTests/</code> + <code>Reactor.AppTests.Host</code></td><td>Component renders into a real WinUI tree; assertions via <code>VisualTreeHelper</code>.</td></tr><tr><td>App E2E</td><td><code>tests/Reactor.AppTests/</code></td><td>WinAppDriver-driven full apps; click, type, screenshot diff.</td></tr><tr><td>Doc</td><td><code>docs/_pipeline/apps/</code></td><td>Compile + snapshot capture for the docs pipeline.</td></tr></tbody></table>\n<h2 id=\"unit-level-fixtures\">Unit-level fixtures<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#unit-level-fixtures\">#</a></h2>\n<p>Reactor's component lifecycle (<code>BeginRender</code> → <code>Render</code> → <code>FlushEffects</code> → <code>RunCleanups</code>)\nis exposed through the framework's internal API and used directly by\n<code>ComponentModelIntegrationTests</code> in <code>tests/Reactor.Tests/</code>. The pattern\nis small enough to fit in a helper inside your test class:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> ContextScope _scope = <span class=\"hljs-keyword\">new</span>();   <span class=\"hljs-comment\">// internal</span>\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> Element <span class=\"hljs-title\">Mount</span>(<span class=\"hljs-params\">Component component,\n    Dictionary&#x3C;ContextBase, <span class=\"hljs-built_in\">object</span>?>? ctx = <span class=\"hljs-literal\">null</span></span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (ctx <span class=\"hljs-keyword\">is</span> { Count: > <span class=\"hljs-number\">0</span> }) _scope.Push(ctx);\n    component.Context.BeginRender(onReRender: () => { }, _scope);\n    <span class=\"hljs-keyword\">var</span> el = component.Render();\n    component.Context.FlushEffects();\n    <span class=\"hljs-keyword\">return</span> el;\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> Element <span class=\"hljs-title\">Rerender</span>(<span class=\"hljs-params\">Component c</span>)</span> => Mount(c);\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Unmount</span>(<span class=\"hljs-params\">Component c</span>)</span> => c.Context.RunCleanups();\n</code></pre>\n<p><code>Mount</code> returns the root element. The test then drives state via the\ncomponent's own public surface (a property the component exposes, or a\ncaptured setter from <code>UseState</code>), calls <code>Rerender</code>, and asserts.\n<code>Unmount</code> runs the cleanup chain so effects with <code>dispose</code> lambdas\nrelease their resources before the next test starts.</p>\n<p>The full pattern is in <code>ComponentModelIntegrationTests.cs</code> — that file\nmounts a component with state + context + effects, drives 5 distinct\nlifecycle transitions, and asserts the effect log after each. Use it\nas the template when adding a unit fixture for a new hook.</p>\n<h2 id=\"effect-aware-async-tests\">Effect-aware async tests<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#effect-aware-async-tests\">#</a></h2>\n<p><code>UseEffect</code> does not fire during render. It fires when the component's\ncontext flushes effects — which the unit <code>Mount</code> helper above does\ninline. Tests that exercise effect ordering must observe the log\nbetween mount and the next render, not during render:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Effect-aware component used as a fixture target. UseEffect fires on the</span>\n<span class=\"hljs-comment\">// next flush, not during render — tests must wait for the flush before</span>\n<span class=\"hljs-comment\">// observing the side effect's log entry (see testing.md, \"Async patterns\").</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">EffectfulCounter</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">public</span> List&#x3C;<span class=\"hljs-built_in\">string</span>> Log { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        UseEffect(() =>\n        {\n            Log.Add(<span class=\"hljs-string\">$\"effect:<span class=\"hljs-subst\">{count}</span>\"</span>);\n            <span class=\"hljs-keyword\">return</span> () => Log.Add(<span class=\"hljs-string\">$\"cleanup:<span class=\"hljs-subst\">{count}</span>\"</span>);\n        }, count);\n        <span class=\"hljs-keyword\">return</span> Button(<span class=\"hljs-string\">$\"count=<span class=\"hljs-subst\">{count}</span>\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>));\n    }\n}\n</code></pre>\n<p>A test for <code>EffectfulCounter</code> mounts the component, asserts\n<code>Log = [\"effect:0\"]</code>, increments state, re-renders, and asserts\n<code>Log = [\"effect:0\", \"cleanup:0\", \"effect:5\"]</code>. The cleanup from the\nprevious effect runs before the new effect's body — that's the\ncontract <code>tests/Reactor.Tests/ComponentModelIntegrationTests.cs</code> codifies.</p>\n<p>For genuinely async effects (timer fires, HTTP callback), use\n<code>UseAsync</code> with a <code>CancellationToken</code> so the test can deterministically\nflush by awaiting the completion of a task the component exposes — or\ninject a fake clock via <code>UseContext</code> of a clock interface and tick it\nforward by hand. Avoid <code>Thread.Sleep</code> in tests; it leaks wall-clock\ntime into the suite and makes CI flaky.</p>\n<h2 id=\"snapshot-tests\">Snapshot tests<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#snapshot-tests\">#</a></h2>\n<p>A snapshot test takes the rendered element tree, serializes it to a\ndeterministic string, and compares against a checked-in golden file.\nReactor's <code>Element</code> records implement <code>ToString()</code> for compact\ndescriptions, and the reconciler's <code>ElementDescription</code> helper produces\na stable JSON shape. Place goldens next to the test file as\n<code>*.snapshot.txt</code> and diff with <code>Assert.Equal</code>:</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Fact</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Card_renders_three_slots</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> card = <span class=\"hljs-keyword\">new</span> ProfileCard(<span class=\"hljs-string\">\"Ada\"</span>, <span class=\"hljs-string\">\"Lovelace\"</span>, <span class=\"hljs-number\">36</span>);\n    <span class=\"hljs-keyword\">var</span> rendered = Mount(card);\n    <span class=\"hljs-keyword\">var</span> actual = ElementDescription.Of(rendered);\n    <span class=\"hljs-keyword\">var</span> expected = File.ReadAllText(<span class=\"hljs-string\">\"ProfileCard.snapshot.txt\"</span>);\n    Assert.Equal(expected.Trim(), actual.Trim());\n}\n</code></pre>\n<p>Update goldens when an intended UI change makes them stale — the diff\nof the snapshot file is the review surface. Don't snapshot anything\nthat includes a timestamp, a randomly generated id, or a hash; either\nnormalize those fields before comparison or factor them out of the\ncomponent under test.</p>\n<h2 id=\"accessibility-scanner-integration\">Accessibility scanner integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibility-scanner-integration\">#</a></h2>\n<p><a href=\"/accessibility/\"><code>AccessibilityScanner.Scan(root)</code></a> walks the\nrendered tree and returns a <code>List&#x3C;A11yDiagnostic></code> with the WCAG\ncriterion, fix suggestion, and source context for each finding. In a\ntest, mount the component and assert the scan returns empty:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// AccessibilityScanner fixture target. The scanner walks the element tree</span>\n<span class=\"hljs-comment\">// post-render and returns one A11yDiagnostic per finding; an icon-only</span>\n<span class=\"hljs-comment\">// button without an accessible name is the canonical positive case.</span>\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">IconOnlyButton</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> =>\n        Button(<span class=\"hljs-string\">\"\"</span>, () => { });   <span class=\"hljs-comment\">// no accessible name → diagnostic</span>\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Fact</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">IconOnlyButton_flags_missing_accessible_name</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> rendered = Mount(<span class=\"hljs-keyword\">new</span> IconOnlyButton());\n    <span class=\"hljs-keyword\">var</span> diagnostics = AccessibilityScanner.Scan(rendered);\n\n    Assert.Contains(diagnostics, d => d.Rule == <span class=\"hljs-string\">\"ButtonName\"</span>);\n}\n\n[<span class=\"hljs-meta\">Fact</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">NamedButton_passes_a11y_scan</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> rendered = Mount(<span class=\"hljs-keyword\">new</span> NamedButton());\n    Assert.Empty(AccessibilityScanner.Scan(rendered));\n}\n</code></pre>\n<p>The same scanner backs the in-app dev menu's \"Run accessibility scan\"\nbutton, so a fixture that passes here is the same shape that passes in\nthe running app. Treat scanner-clean as the standing bar for every new\ncomponent you ship.</p>\n<h2 id=\"self-tests-real-winui-tree\">Self-tests (real WinUI tree)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#self-tests-real-winui-tree\">#</a></h2>\n<p><code>Reactor.SelfTests</code> is the layer between the unit suite (pure C#, no\nWinUI) and the full E2E suite (WinAppDriver). A self-test mounts a\nreal fixture into the <code>Reactor.AppTests.Host</code> window, walks the WinUI\nvisual tree, and emits TAP. The xUnit wrapper in <code>SelfTestBatch.cs</code>\nparses the TAP and surfaces one test method per fixture.</p>\n<p>To add a self-test:</p>\n<ol>\n<li>Add a new fixture file under\n<code>tests/Reactor.AppTests.Host/Fixtures/</code> returning the component\nunder test wrapped in a small assertion harness.</li>\n<li>Register the fixture name in <code>FixtureRegistry</code>.</li>\n<li>The xUnit wrapper picks it up at discovery time via\n<code>--list-fixtures</code>; no code change needed on the test runner side.</li>\n</ol>\n<p>Reach for a self-test when the unit layer can't see the answer — e.g.\nwhen the WinUI control's measured size affects the component's\nbehavior, or when an automation peer's role depends on the\nmaterialized XAML control class.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Don't drive the unit fixture from <code>Task.Delay</code>.</strong> If an effect\nschedules async work, expose its completion task so the test can\n<code>await</code> it. Wall-clock delays leak into the suite and make CI flaky.</p>\n<p><strong>Snapshot the tree, not the rendered pixels.</strong> <code>ElementDescription.Of</code>\ngives you a deterministic string; an actual rendered bitmap depends on\nfont rendering, DPI, and platform Composition — none of which belong\nin a unit test.</p>\n<p><strong>Run the accessibility scan in every fixture's teardown.</strong> It's\ncheap, it catches regressions the moment they land, and it puts the\nanalyzer's output next to the test that introduced the problem.</p>\n<p><strong>Use <code>Reactor.AppTests</code> only where xUnit and self-tests can't reach.</strong>\nWinAppDriver is the slow lane; reserve it for keyboard navigation,\nfocus order, and click sequences that depend on Composition or input\nrouting.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Previous in the learning path: the\nprimitives a fixture exercises.</li>\n<li><strong><a href=\"/effects/\">Effects</a></strong> — <code>UseEffect</code> lifecycle and cleanup,\nincluding the flush ordering tested above.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — The scanner's rules and how\nto extend it with project-specific checks.</li>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — <code>mur</code> CLI, preview mode, and the\ndoc-pipeline harness that powers the screenshots on this page.</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Render purity rules that make the\nunit layer worth the investment.</li>\n</ul>",
      "text": "Testing # Microsoft.UI.Reactor (Reactor)'s render loop is deterministic and synchronous. A Component mounted in a unit test renders, runs effects, accepts state updates, re-renders, and disposes through the same code path the WinUI host uses — minus the WinUI tree at the bottom. That makes the unit layer fast (an xUnit run for the framework's 800+ tests is seconds), and it keeps test bodies focused on the component's behavior instead of the windowing chrome. This page covers the four test layers Reactor uses internally: class Counter : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return VStack( 8 , TextBlock( $\"Count: {count} \" ).FontSize( 20 ).Bold(), Button( \"Increment\" , () => setCount(count + 1 )) ).Padding( 16 ); } } Reference # Test layer Project When to reach for it Unit tests/Reactor.Tests/ Hook semantics, reducer logic, modifier chains, analyzer rules. Fastest loop. Self-test tests/Reactor.SelfTests/ + Reactor.AppTests.Host Component renders into a real WinUI tree; assertions via VisualTreeHelper . App E2E tests/Reactor.AppTests/ WinAppDriver-driven full apps; click, type, screenshot diff. Doc docs/_pipeline/apps/ Compile + snapshot capture for the docs pipeline. Unit-level fixtures # Reactor's component lifecycle ( BeginRender → Render → FlushEffects → RunCleanups ) is exposed through the framework's internal API and used directly by ComponentModelIntegrationTests in tests/Reactor.Tests/ . The pattern is small enough to fit in a helper inside your test class: private readonly ContextScope _scope = new (); // internal private Element Mount ( Component component, Dictionary&#x3C;ContextBase, object ?>? ctx = null ) { if (ctx is { Count: > 0 }) _scope.Push(ctx); component.Context.BeginRender(onReRender: () => { }, _scope); var el = component.Render(); component.Context.FlushEffects(); return el; } private Element Rerender ( Component c ) => Mount(c); private void Unmount ( Component c ) => c.Context.RunCleanups(); Mount returns the root element. The test then drives state via the component's own public surface (a property the component exposes, or a captured setter from UseState ), calls Rerender , and asserts. Unmount runs the cleanup chain so effects with dispose lambdas release their resources before the next test starts. The full pattern is in ComponentModelIntegrationTests.cs — that file mounts a component with state + context + effects, drives 5 distinct lifecycle transitions, and asserts the effect log after each. Use it as the template when adding a unit fixture for a new hook. Effect-aware async tests # UseEffect does not fire during render. It fires when the component's context flushes effects — which the unit Mount helper above does inline. Tests that exercise effect ordering must observe the log between mount and the next render, not during render: // Effect-aware component used as a fixture target. UseEffect fires on the // next flush, not during render — tests must wait for the flush before // observing the side effect's log entry (see testing.md, \"Async patterns\"). class EffectfulCounter : Component { public List&#x3C; string > Log { get ; } = new (); public override Element Render () { var (count, setCount) = UseState( 0 ); UseEffect(() => { Log.Add( $\"effect: {count} \" ); return () => Log.Add( $\"cleanup: {count} \" ); }, count); return Button( $\"count= {count} \" , () => setCount(count + 1 )); } } A test for EffectfulCounter mounts the component, asserts Log = [\"effect:0\"] , increments state, re-renders, and asserts Log = [\"effect:0\", \"cleanup:0\", \"effect:5\"] . The cleanup from the previous effect runs before the new effect's body — that's the contract tests/Reactor.Tests/ComponentModelIntegrationTests.cs codifies. For genuinely async effects (timer fires, HTTP callback), use UseAsync with a CancellationToken so the test can deterministically flush by awaiting the completion of a task the component exposes — or inject a fake clock via UseContext of a clock interface and tick it forward by hand. Avoid Thread.Sleep in tests; it leaks wall-clock time into the suite and makes CI flaky. Snapshot tests # A snapshot test takes the rendered element tree, serializes it to a deterministic string, and compares against a checked-in golden file. Reactor's Element records implement ToString() for compact descriptions, and the reconciler's ElementDescription helper produces a stable JSON shape. Place goldens next to the test file as *.snapshot.txt and diff with Assert.Equal : [ Fact ] public void Card_renders_three_slots () { var card = new ProfileCard( \"Ada\" , \"Lovelace\" , 36 ); var rendered = Mount(card); var actual = ElementDescription.Of(rendered); var expected = File.ReadAllText( \"ProfileCard.snapshot.txt\" ); Assert.Equal(expected.Trim(), actual.Trim()); } Update goldens when an intended UI change makes them stale — the diff of the snapshot file is the review surface. Don't snapshot anything that includes a timestamp, a randomly generated id, or a hash; either normalize those fields before comparison or factor them out of the component under test. Accessibility scanner integration # AccessibilityScanner.Scan(root) walks the rendered tree and returns a List&#x3C;A11yDiagnostic> with the WCAG criterion, fix suggestion, and source context for each finding. In a test, mount the component and assert the scan returns empty: // AccessibilityScanner fixture target. The scanner walks the element tree // post-render and returns one A11yDiagnostic per finding; an icon-only // button without an accessible name is the canonical positive case. class IconOnlyButton : Component { public override Element Render () => Button( \"\" , () => { }); // no accessible name → diagnostic } [ Fact ] public void IconOnlyButton_flags_missing_accessible_name () { var rendered = Mount( new IconOnlyButton()); var diagnostics = AccessibilityScanner.Scan(rendered); Assert.Contains(diagnostics, d => d.Rule == \"ButtonName\" ); } [ Fact ] public void NamedButton_passes_a11y_scan () { var rendered = Mount( new NamedButton()); Assert.Empty(AccessibilityScanner.Scan(rendered)); } The same scanner backs the in-app dev menu's \"Run accessibility scan\" button, so a fixture that passes here is the same shape that passes in the running app. Treat scanner-clean as the standing bar for every new component you ship. Self-tests (real WinUI tree) # Reactor.SelfTests is the layer between the unit suite (pure C#, no WinUI) and the full E2E suite (WinAppDriver). A self-test mounts a real fixture into the Reactor.AppTests.Host window, walks the WinUI visual tree, and emits TAP. The xUnit wrapper in SelfTestBatch.cs parses the TAP and surfaces one test method per fixture. To add a self-test: Add a new fixture file under tests/Reactor.AppTests.Host/Fixtures/ returning the component under test wrapped in a small assertion harness. Register the fixture name in FixtureRegistry . The xUnit wrapper picks it up at discovery time via --list-fixtures ; no code change needed on the test runner side. Reach for a self-test when the unit layer can't see the answer — e.g. when the WinUI control's measured size affects the component's behavior, or when an automation peer's role depends on the materialized XAML control class. Tips # Don't drive the unit fixture from Task.Delay . If an effect schedules async work, expose its completion task so the test can await it. Wall-clock delays leak into the suite and make CI flaky. Snapshot the tree, not the rendered pixels. ElementDescription.Of gives you a deterministic string; an actual rendered bitmap depends on font rendering, DPI, and platform Composition — none of which belong in a unit test. Run the accessibility scan in every fixture's teardown. It's cheap, it catches regressions the moment they land, and it puts the analyzer's output next to the test that introduced the problem. Use Reactor.AppTests only where xUnit and self-tests can't reach. WinAppDriver is the slow lane; reserve it for keyboard navigation, focus order, and click sequences that depend on Composition or input routing. Next Steps # Hooks — Previous in the learning path: the primitives a fixture exercises. Effects — UseEffect lifecycle and cleanup, including the flush ordering tested above. Accessibility — The scanner's rules and how to extend it with project-specific checks. Dev Tooling — mur CLI, preview mode, and the doc-pipeline harness that powers the screenshots on this page. Components — Render purity rules that make the unit layer worth the investment.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/testing.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/testing.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "unit-level-fixtures",
          "text": "Unit-level fixtures"
        },
        {
          "depth": 2,
          "id": "effect-aware-async-tests",
          "text": "Effect-aware async tests"
        },
        {
          "depth": 2,
          "id": "snapshot-tests",
          "text": "Snapshot tests"
        },
        {
          "depth": 2,
          "id": "accessibility-scanner-integration",
          "text": "Accessibility scanner integration"
        },
        {
          "depth": 2,
          "id": "self-tests-real-winui-tree",
          "text": "Self-tests (real WinUI tree)"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Performance",
      "path": "performance.md",
      "slug": "/performance/",
      "html": "<h1 id=\"performance\">Performance<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#performance\">#</a></h1>\n<p>A Microsoft.UI.Reactor (Reactor) app emits a structured account of its own work — one event at\nthe start and end of every reconcile pass, every component render,\nevery effect flush, every state write, every WinUI routed-event hop —\nthrough one managed <code>EventSource</code> named <code>Microsoft-UI-Reactor</code>. You\nattach a profiler, mask in the keywords you care about, run the\nscenario, and read the resulting span hierarchy. This page walks the\ntop-down workflow: how to take a trace with <code>dotnet-trace</code> or PerfView,\nwhat each span means, how to find the outlier, and how to reproduce a\nbenchmark from <code>tests/perf_bench/</code> so a regression has a number behind\nit. The deep \"how does the pipeline emit these events\" story lives in\n<a href=\"/perf-instrumentation/\">Perf Instrumentation</a> — read that next if\nyou're adding a new event id, not before you take your first trace.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Tool</th><th>Source</th><th>What you read</th><th>Best for</th></tr></thead><tbody><tr><td><code>dotnet-trace</code></td><td>EventPipe</td><td><code>.nettrace</code> opened in speedscope.app or VS Profiler</td><td>Reactor-only timing on any OS, AOT-friendly</td></tr><tr><td>PerfView / xperf</td><td>Classic ETW</td><td><code>.etl</code> opened in PerfView's Events view or WPA</td><td>Frame stutter that may be Reactor <em>or</em> WinUI native</td></tr><tr><td><code>tests/perf_bench/PerfBench.*</code></td><td>In-process <code>BenchTracker</code></td><td><code>*.report.txt</code> next to the exe</td><td>Reproducing a regression with FPS / update-ms / GC numbers</td></tr><tr><td>Reconcile-highlight overlay</td><td>Dev menu</td><td>Flashing rectangle per re-render</td><td>Finding which component re-rendered without taking a trace</td></tr><tr><td>Layout-cost overlay</td><td>Dev menu</td><td>Heatmap tint by measure/arrange cost</td><td>Finding expensive panels without taking a trace</td></tr></tbody></table>\n<p>The reconcile-highlight and layout-cost overlays are documented in\n<a href=\"/dev-tooling/\">Dev Tooling</a>; reach for them first when \"something\nfeels slow\" is the only signal you have. A profile trace is the next\nstep up: it costs more to set up but gives you elapsed microseconds\nper span and a complete hierarchy you can sort.</p>\n<h2 id=\"the-provider-and-its-keywords\">The provider and its keywords<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-provider-and-its-keywords\">#</a></h2>\n<p>The Reactor provider's events split across seven keywords. A consumer\nmasks in only the ones it wants, so a \"just reconcile\" trace doesn't\npay for state writes, MCP traffic, or trampoline dispatches:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Keywords</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Reconcile = (EventKeywords)<span class=\"hljs-number\">0x1</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Render = (EventKeywords)<span class=\"hljs-number\">0x2</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords State = (EventKeywords)<span class=\"hljs-number\">0x4</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Mcp = (EventKeywords)<span class=\"hljs-number\">0x8</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Lifecycle = (EventKeywords)<span class=\"hljs-number\">0x10</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Errors = (EventKeywords)<span class=\"hljs-number\">0x20</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords EventDispatch = (EventKeywords)<span class=\"hljs-number\">0x40</span>;\n    <span class=\"hljs-comment\">// Spec 044 — subsystem coverage gaps. Each gets its own bit so a</span>\n    <span class=\"hljs-comment\">// consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can</span>\n    <span class=\"hljs-comment\">// pick exactly the area it cares about without paying for the rest.</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Hosting = (EventKeywords)<span class=\"hljs-number\">0x80</span>;       <span class=\"hljs-comment\">// Window/HWND/DPI/Backdrop</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Persistence = (EventKeywords)<span class=\"hljs-number\">0x100</span>;  <span class=\"hljs-comment\">// settings store, placement</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Navigation = (EventKeywords)<span class=\"hljs-number\">0x200</span>;   <span class=\"hljs-comment\">// route push, cache, transitions</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Intl = (EventKeywords)<span class=\"hljs-number\">0x400</span>;         <span class=\"hljs-comment\">// missing keys, fallback, format</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Theme = (EventKeywords)<span class=\"hljs-number\">0x800</span>;        <span class=\"hljs-comment\">// theme apply, bindings</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Shell = (EventKeywords)<span class=\"hljs-number\">0x1000</span>;       <span class=\"hljs-comment\">// JumpList/Tray/ThumbnailToolbar</span>\n}\n</code></pre>\n<p><code>Reconcile</code> covers reconcile-pass boundaries and the child-list diff,\n<code>Render</code> covers per-component render timing and the effect-flush\nboundary, <code>State</code> covers <code>UseState</code> writes, <code>Mcp</code> covers\n<a href=\"/dev-tooling/\">devtools</a> tool dispatch, <code>Lifecycle</code> covers mount and\nunmount, <code>Errors</code> is severity-Error events, and <code>EventDispatch</code> covers\nthe trampoline that fans WinUI routed events out to your Reactor\ncallbacks. The values are power-of-two flags on <code>EventKeywords</code>, so\n<code>Reconcile | Render</code> is <code>0x3</code> and <code>Reconcile | Render | EventDispatch</code>\nis <code>0x43</code>.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/performance/top-down-flow.svg\" alt=\"Top-down profile flow — from running app through dotnet-trace or PerfView into a Reactor span hierarchy\"></p>\n<h2 id=\"taking-a-reactor-only-trace\">Taking a Reactor-only trace<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#taking-a-reactor-only-trace\">#</a></h2>\n<p>For a Reactor-only workflow on any OS, <code>dotnet-trace</code> is the right\ntool. It writes a <code>.nettrace</code> file that speedscope.app, Visual\nStudio's profiler, and PerfView all open:</p>\n<pre><code>dotnet tool install --global dotnet-trace\ndotnet-trace collect --process-id &#x3C;pid> --providers Microsoft-UI-Reactor:0x3:4\n</code></pre>\n<p><code>0x3</code> masks <code>Reconcile | Render</code>, <code>:4</code> pins the level at\nInformational. Pinning the level matters: the default is <code>Verbose</code>,\nwhich means every <code>StateChange</code> event hits the session — a noisy app\ncan write millions of those per minute, and the trace grows from tens\nof megabytes to gigabytes. Add the <code>EventDispatch</code> keyword (<code>0x43</code>\ntotal) when you're chasing a routed-event regression; leave it out\notherwise.</p>\n<p>Open the resulting file in <a href=\"https://www.speedscope.app/\">speedscope.app</a>\nand pick the \"Time order\" view — that's the flame graph. Each\n<code>ReconcileStart</code>/<code>ReconcileStop</code> pair is one reconcile pass; the\nnested <code>ChildReconcileStart</code>/<code>ChildReconcileStop</code> band underneath is\nthe keyed child-list diff inside that pass.</p>\n<h2 id=\"taking-a-correlated-trace-reactor--winui\">Taking a correlated trace (Reactor + WinUI)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#taking-a-correlated-trace-reactor--winui\">#</a></h2>\n<p>When a frame stutter could be Reactor's, WinUI's, or the compositor's,\nyou need ETW because <code>Microsoft-Windows-XAML</code> is a native provider\nthat doesn't flow through EventPipe. PerfView is the easier of the\ntwo ETW tools to use:</p>\n<pre><code>perfview /providers=\"Microsoft-UI-Reactor:0x3:4,*Microsoft-Windows-XAML\" collect\n</code></pre>\n<p>The <code>*</code> prefix on <code>Microsoft-Windows-XAML</code> registers it by name\n(PerfView resolves the GUID for you). Stop the collection after your\nrepro, open the resulting <code>.etl</code>, and use the <strong>Events</strong> view — not\nthe call-stack view — to read Reactor's timing data. Filter to\nprovider <code>Microsoft-UI-Reactor</code>, sort by elapsed microseconds, and\nthe slowest pass floats to the top.</p>\n<h2 id=\"reading-a-span-the-reconciler-entry\">Reading a span: the reconciler entry<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-a-span-the-reconciler-entry\">#</a></h2>\n<p>Every reconcile pass starts here. The event-id pair is <code>1</code> / <code>2</code>\n(<code>ReconcileStart</code> / <code>ReconcileStop</code>) and the counters reported on\n<code>ReconcileStop</code> are the per-pass cost summary:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> UIElement? Reconcile(\n    Element? oldElement,\n    Element? newElement,\n    UIElement? existingControl,\n    Action requestRerender)\n{\n    <span class=\"hljs-comment\">// Trace only top-level reconcile passes (depth == 0) to avoid flooding</span>\n    <span class=\"hljs-comment\">// the provider with per-subtree entries; nested Reconcile() calls during</span>\n    <span class=\"hljs-comment\">// the same pass don't emit their own start/stop. Gate the depth counter</span>\n    <span class=\"hljs-comment\">// and Start emit on IsEnabled so the disabled path pays nothing extra.</span>\n    <span class=\"hljs-built_in\">bool</span> emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled(\n        <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Informational,\n        Diagnostics.ReactorEventSource.Keywords.Reconcile)\n        &#x26;&#x26; _reconcileTraceDepth++ == <span class=\"hljs-number\">0</span>;\n    <span class=\"hljs-keyword\">if</span> (emitTrace)\n    {\n        Diagnostics.ReactorEventSource.Log.ReconcileStart(\n            newElement?.GetType().Name ?? <span class=\"hljs-string\">\"null\"</span>);\n    }\n    <span class=\"hljs-keyword\">if</span> (_debugReconcileDepth++ == <span class=\"hljs-number\">0</span>)\n    {\n        DebugElementsDiffed = <span class=\"hljs-number\">0</span>;\n        DebugElementsSkipped = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsCreated = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsModified = <span class=\"hljs-number\">0</span>;\n        <span class=\"hljs-keyword\">if</span> (ReactorFeatureFlags.HighlightReconcileChanges)\n        {\n            (_highlightMounted ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n            (_highlightModified ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n        }\n        <span class=\"hljs-comment\">// Consume the hot-reload signal exactly once per top-level pass so</span>\n        <span class=\"hljs-comment\">// every component re-runs Render() even when props/deps are unchanged.</span>\n        _forceFullRenderActive = ForceFullRenderPending;\n        ForceFullRenderPending = <span class=\"hljs-literal\">false</span>;\n\n        <span class=\"hljs-comment\">// Build the dirty-ancestor path. For every component node</span>\n        <span class=\"hljs-comment\">// whose SelfTriggered is true, walk up the realized visual</span>\n        <span class=\"hljs-comment\">// tree and add each ancestor control. Consumed by Update's</span>\n        <span class=\"hljs-comment\">// shallow-equality short-circuit so the walk can reach the</span>\n        <span class=\"hljs-comment\">// self-triggered descendant even when its ancestor element</span>\n        <span class=\"hljs-comment\">// records are structurally unchanged.</span>\n        PopulateDirtyAncestorPath();\n    }\n    <span class=\"hljs-keyword\">try</span> {\n    <span class=\"hljs-keyword\">try</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (newElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement)\n        {\n            <span class=\"hljs-keyword\">if</span> (existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                Unmount(existingControl);\n            <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n        }\n\n        <span class=\"hljs-keyword\">if</span> (oldElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement || existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> Mount(newElement, requestRerender);\n\n        <span class=\"hljs-keyword\">return</span> ReconcileImperative(oldElement, newElement, existingControl, requestRerender);\n</code></pre>\n<p>In a flame-graph viewer you see a band whose top edge is\n<code>ReconcileStart</code> and whose bottom edge is <code>ReconcileStop</code>. The band's\nwidth is the wall-clock duration of the pass. Hover the band and the\npayload fields show up: <code>elementsDiffed</code> is the count of element\nrecords the reconciler walked, <code>elementsSkipped</code> is the count it\nbailed out of by <code>ShouldUpdate</code> or referential equality,\n<code>uiElementsCreated</code> is the count of WinUI <code>FrameworkElement</code>\ninstances allocated this pass, and <code>uiElementsModified</code> is the count\nof property writes against existing controls.</p>\n<p>High <code>elementsSkipped</code> with a short pass is the healthy case — the\nbail-out is working. Low <code>elementsSkipped</code> with a long pass usually\nmeans the <a href=\"/hooks/\"><code>UseMemo</code></a> deps array is unstable: a new array\nor a new closure as a dep makes every render look new, and the\nbail-out can't fire. The <a href=\"/dev-tooling/\">reconcile-highlight\noverlay</a> catches this cheaper than a trace — flick\nit on and watch which component flashes every time you type.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Debug builds disable several of the reconciler's bail-out optimizations\nto surface invariant violations. The same scenario on a Debug build\nshows 3–4× the <code>elementsDiffed</code> count and roughly 2× the elapsed\nmicroseconds vs. Release. Always profile against <code>-c Release</code>; a flame\ngraph from a Debug build is not a valid signal about Release\nperformance.</p>\n</blockquote>\n<h2 id=\"the-eventdispatch-keyword\">The EventDispatch keyword<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-eventdispatch-keyword\">#</a></h2>\n<p>The <code>EventDispatch</code> keyword covers the per-event trampoline that lets\nReactor swap your handler closure between renders without\ndetaching from the WinUI event:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">EnsureSizeChangedSubscribed</span>(<span class=\"hljs-params\">FrameworkElement fe, EventHandlerState state, Action&#x3C;<span class=\"hljs-built_in\">object</span>, SizeChangedEventArgs>? handler</span>)</span>\n{\n    state.CurrentSizeChanged = handler;\n    <span class=\"hljs-keyword\">if</span> (state.SizeChangedTrampoline <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> &#x26;&#x26; handler <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n    {\n        state.SizeChangedTrampoline = (s, e) =>\n        {\n            Diagnostics.ReactorEventSource.Log.EventTrampolineDispatch(<span class=\"hljs-string\">\"SizeChanged\"</span>);\n            state.CurrentSizeChanged?.Invoke(s!, e);\n        };\n        fe.SizeChanged += state.SizeChangedTrampoline;\n        Diagnostics.ReactorEventSource.Log.EventTrampolineAttached(<span class=\"hljs-string\">\"SizeChanged\"</span>, fe.GetType().Name);\n    }\n}\n</code></pre>\n<p>The pattern matters for performance because the trampoline is bound\n<strong>once</strong> per element lifetime — every subsequent render that hands\nthe element a new callback updates a field in <code>state</code>, not the WinUI\nevent subscription list. Without this trick, every render that\nchanges a handler would call <code>fe.SizeChanged -=</code> then <code>fe.SizeChanged +=</code>; under the cover, WinUI walks a linked list each time and you'd\nsee a measurable hit on lists with hundreds of subscribed elements.</p>\n<p>When you do see <code>EventTrampolineDispatch</code> events dominating a\nverbose-level trace, that's the trampoline doing its actual job —\nthe events fire on the UI thread for every routed event WinUI emits.\nMask out <code>EventDispatch</code> (<code>0x3</code> instead of <code>0x43</code>) unless you're\nspecifically debugging input or pointer flow.</p>\n<h2 id=\"reproducing-a-benchmark\">Reproducing a benchmark<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reproducing-a-benchmark\">#</a></h2>\n<p>A regression claim needs a number, and the bench harness under\n<code>tests/perf_bench/</code> is how you get one. Each bench has three sibling\nvariants — <code>*.Reactor</code>, <code>*.Direct</code> (raw WinUI), and <code>*.Bound</code>\n(WinUI + MVVM bindings) — so you can compare Reactor to its\nnon-declarative baselines. The entry point is the standard top-level\n<code>ReactorApp.Run</code> call with a CLI options parser bolted on:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> opts = BenchCliOptions.Parse(<span class=\"hljs-keyword\">args</span>);\n<span class=\"hljs-keyword\">if</span> (opts.Headless) ConsoleHelper.EnsureConsole();\nDirtyTrackingApp.Opts = opts;\nReactorApp.Run&#x3C;DirtyTrackingApp>(<span class=\"hljs-string\">\"EXP-1 DirtyTracking.Reactor\"</span>, fullScreen: <span class=\"hljs-literal\">true</span>);\n</code></pre>\n<p>Run with <code>--headless --duration 10</code> and the app writes a\n<code>*.report.txt</code> next to the exe, then exits. The report includes the\nmetrics the harness records each frame — FPS, update milliseconds,\nmemory, GC generation counts, longest frame block:</p>\n<pre><code class=\"hljs language-csharp\">sb.AppendLine(<span class=\"hljs-string\">$\"Avg FPS:       <span class=\"hljs-subst\">{_fpsSamples.Average():F1}</span>\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"Min FPS:       <span class=\"hljs-subst\">{_fpsSamples.Min():F1}</span>\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"Max FPS:       <span class=\"hljs-subst\">{_fpsSamples.Max():F1}</span>\"</span>);\n\n<span class=\"hljs-keyword\">if</span> (_updateTimeSamples.Count > <span class=\"hljs-number\">0</span>)\n{\n    sb.AppendLine(<span class=\"hljs-string\">$\"Avg Update:    <span class=\"hljs-subst\">{_updateTimeSamples.Average():F2}</span> ms\"</span>);\n    sb.AppendLine(<span class=\"hljs-string\">$\"Max Update:    <span class=\"hljs-subst\">{_updateTimeSamples.Max():F2}</span> ms\"</span>);\n}\n\nsb.AppendLine(<span class=\"hljs-string\">$\"Avg Memory:    <span class=\"hljs-subst\">{_memorySamples.Average() / (<span class=\"hljs-number\">1024</span> * <span class=\"hljs-number\">1024</span>):F1}</span> MB\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"Peak Memory:   <span class=\"hljs-subst\">{_memorySamples.Max() / (<span class=\"hljs-number\">1024</span> * <span class=\"hljs-number\">1024</span>):F1}</span> MB\"</span>);\n\n<span class=\"hljs-comment\">// GC</span>\n<span class=\"hljs-built_in\">int</span> gen0Total = GC.CollectionCount(<span class=\"hljs-number\">0</span>) - _baselineGen0;\n<span class=\"hljs-built_in\">int</span> gen1Total = GC.CollectionCount(<span class=\"hljs-number\">1</span>) - _baselineGen1;\n<span class=\"hljs-built_in\">int</span> gen2Total = GC.CollectionCount(<span class=\"hljs-number\">2</span>) - _baselineGen2;\nsb.AppendLine(<span class=\"hljs-string\">$\"GC Gen0:       <span class=\"hljs-subst\">{gen0Total}</span>\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"GC Gen1:       <span class=\"hljs-subst\">{gen1Total}</span>\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"GC Gen2:       <span class=\"hljs-subst\">{gen2Total}</span>\"</span>);\n\n<span class=\"hljs-comment\">// Frame jank</span>\nsb.AppendLine(<span class=\"hljs-string\">$\"Longest Block: <span class=\"hljs-subst\">{_longestFrameBlockMs:F1}</span> ms\"</span>);\nsb.AppendLine(<span class=\"hljs-string\">$\"Anim Drops:    <span class=\"hljs-subst\">{_animationDrops}</span>\"</span>);\n</code></pre>\n<p>To reproduce a regression locally:</p>\n<pre><code>dotnet build tests/perf_bench/PerfBench.DirtyTracking/DirtyTracking.Reactor -c Release\n./tests/perf_bench/PerfBench.DirtyTracking/DirtyTracking.Reactor/bin/&#x3C;plat>/Release/&#x3C;tfm>/DirtyTracking.Reactor.exe --headless --duration 30\ncat ./tests/.../bin/&#x3C;plat>/Release/&#x3C;tfm>/EXP1_DirtyTracking_Reactor.report.txt\n</code></pre>\n<p>Re-run the same command on <code>main</code> and on your branch; the deltas in\n<code>Avg Update</code>, <code>Anim Drops</code>, and <code>GC Gen0</code> are the regression signal.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pin the level when you collect.</strong> <code>dotnet-trace</code> defaults to\n<code>Verbose</code>, which captures every <code>StateChange</code> write. Pin to\n<code>Informational</code> (<code>:4</code>) for reconcile/render workflows and the trace\nshrinks by roughly an order of magnitude — a 30-second session that\nwas 1.2 GB becomes ~80 MB.</p>\n<p><strong>Reach for an overlay before a trace.</strong> The\n<a href=\"/dev-tooling/\">reconcile-highlight</a> and\n<a href=\"/dev-tooling/\">layout-cost</a> overlays cost nothing to enable and\nwill solve \"which component is re-rendering too much\" without you\never opening a profiler. A trace is the right tool when you need\nelapsed microseconds; the overlays are the right tool when you need\n\"which one\".</p>\n<p><strong>Compare against the matching <code>*.Direct</code> bench.</strong> A Reactor bench\nresult in isolation is hard to interpret. Run the sibling\n<code>PerfBench.*.Direct</code> variant on the same hardware and the delta\ntells you what declarative composition costs over raw WinUI — that's\nthe framework-overhead number, not the absolute number.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — Under-the-hood companion: why each event exists, the IsEnabled gate, how to add a new event id.</li>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — The reconcile-highlight and layout-cost overlays plus the rest of the <code>mur</code> CLI.</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — How the overlays read the same provider events the profilers do.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — What the <code>ReconcileStop</code> counters describe; how to read the keyed child-list diff.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Where <code>UseMemo</code> deps come from and what makes them unstable.</li>\n</ul>",
      "text": "Performance # A Microsoft.UI.Reactor (Reactor) app emits a structured account of its own work — one event at the start and end of every reconcile pass, every component render, every effect flush, every state write, every WinUI routed-event hop — through one managed EventSource named Microsoft-UI-Reactor . You attach a profiler, mask in the keywords you care about, run the scenario, and read the resulting span hierarchy. This page walks the top-down workflow: how to take a trace with dotnet-trace or PerfView, what each span means, how to find the outlier, and how to reproduce a benchmark from tests/perf_bench/ so a regression has a number behind it. The deep \"how does the pipeline emit these events\" story lives in Perf Instrumentation — read that next if you're adding a new event id, not before you take your first trace. Reference # Tool Source What you read Best for dotnet-trace EventPipe .nettrace opened in speedscope.app or VS Profiler Reactor-only timing on any OS, AOT-friendly PerfView / xperf Classic ETW .etl opened in PerfView's Events view or WPA Frame stutter that may be Reactor or WinUI native tests/perf_bench/PerfBench.* In-process BenchTracker *.report.txt next to the exe Reproducing a regression with FPS / update-ms / GC numbers Reconcile-highlight overlay Dev menu Flashing rectangle per re-render Finding which component re-rendered without taking a trace Layout-cost overlay Dev menu Heatmap tint by measure/arrange cost Finding expensive panels without taking a trace The reconcile-highlight and layout-cost overlays are documented in Dev Tooling ; reach for them first when \"something feels slow\" is the only signal you have. A profile trace is the next step up: it costs more to set up but gives you elapsed microseconds per span and a complete hierarchy you can sort. The provider and its keywords # The Reactor provider's events split across seven keywords. A consumer masks in only the ones it wants, so a \"just reconcile\" trace doesn't pay for state writes, MCP traffic, or trampoline dispatches: public static class Keywords { public const EventKeywords Reconcile = (EventKeywords) 0x1 ; public const EventKeywords Render = (EventKeywords) 0x2 ; public const EventKeywords State = (EventKeywords) 0x4 ; public const EventKeywords Mcp = (EventKeywords) 0x8 ; public const EventKeywords Lifecycle = (EventKeywords) 0x10 ; public const EventKeywords Errors = (EventKeywords) 0x20 ; public const EventKeywords EventDispatch = (EventKeywords) 0x40 ; // Spec 044 — subsystem coverage gaps. Each gets its own bit so a // consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can // pick exactly the area it cares about without paying for the rest. public const EventKeywords Hosting = (EventKeywords) 0x80 ; // Window/HWND/DPI/Backdrop public const EventKeywords Persistence = (EventKeywords) 0x100 ; // settings store, placement public const EventKeywords Navigation = (EventKeywords) 0x200 ; // route push, cache, transitions public const EventKeywords Intl = (EventKeywords) 0x400 ; // missing keys, fallback, format public const EventKeywords Theme = (EventKeywords) 0x800 ; // theme apply, bindings public const EventKeywords Shell = (EventKeywords) 0x1000 ; // JumpList/Tray/ThumbnailToolbar } Reconcile covers reconcile-pass boundaries and the child-list diff, Render covers per-component render timing and the effect-flush boundary, State covers UseState writes, Mcp covers devtools tool dispatch, Lifecycle covers mount and unmount, Errors is severity-Error events, and EventDispatch covers the trampoline that fans WinUI routed events out to your Reactor callbacks. The values are power-of-two flags on EventKeywords , so Reconcile | Render is 0x3 and Reconcile | Render | EventDispatch is 0x43 . Taking a Reactor-only trace # For a Reactor-only workflow on any OS, dotnet-trace is the right tool. It writes a .nettrace file that speedscope.app, Visual Studio's profiler, and PerfView all open: dotnet tool install --global dotnet-trace dotnet-trace collect --process-id &#x3C;pid> --providers Microsoft-UI-Reactor:0x3:4 0x3 masks Reconcile | Render , :4 pins the level at Informational. Pinning the level matters: the default is Verbose , which means every StateChange event hits the session — a noisy app can write millions of those per minute, and the trace grows from tens of megabytes to gigabytes. Add the EventDispatch keyword ( 0x43 total) when you're chasing a routed-event regression; leave it out otherwise. Open the resulting file in speedscope.app and pick the \"Time order\" view — that's the flame graph. Each ReconcileStart / ReconcileStop pair is one reconcile pass; the nested ChildReconcileStart / ChildReconcileStop band underneath is the keyed child-list diff inside that pass. Taking a correlated trace (Reactor + WinUI) # When a frame stutter could be Reactor's, WinUI's, or the compositor's, you need ETW because Microsoft-Windows-XAML is a native provider that doesn't flow through EventPipe. PerfView is the easier of the two ETW tools to use: perfview /providers=\"Microsoft-UI-Reactor:0x3:4,*Microsoft-Windows-XAML\" collect The * prefix on Microsoft-Windows-XAML registers it by name (PerfView resolves the GUID for you). Stop the collection after your repro, open the resulting .etl , and use the Events view — not the call-stack view — to read Reactor's timing data. Filter to provider Microsoft-UI-Reactor , sort by elapsed microseconds, and the slowest pass floats to the top. Reading a span: the reconciler entry # Every reconcile pass starts here. The event-id pair is 1 / 2 ( ReconcileStart / ReconcileStop ) and the counters reported on ReconcileStop are the per-pass cost summary: public UIElement? Reconcile( Element? oldElement, Element? newElement, UIElement? existingControl, Action requestRerender) { // Trace only top-level reconcile passes (depth == 0) to avoid flooding // the provider with per-subtree entries; nested Reconcile() calls during // the same pass don't emit their own start/stop. Gate the depth counter // and Start emit on IsEnabled so the disabled path pays nothing extra. bool emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Informational, Diagnostics.ReactorEventSource.Keywords.Reconcile) &#x26;&#x26; _reconcileTraceDepth++ == 0 ; if (emitTrace) { Diagnostics.ReactorEventSource.Log.ReconcileStart( newElement?.GetType().Name ?? \"null\" ); } if (_debugReconcileDepth++ == 0 ) { DebugElementsDiffed = 0 ; DebugElementsSkipped = 0 ; DebugUIElementsCreated = 0 ; DebugUIElementsModified = 0 ; if (ReactorFeatureFlags.HighlightReconcileChanges) { (_highlightMounted ??= new ()).Clear(); (_highlightModified ??= new ()).Clear(); } // Consume the hot-reload signal exactly once per top-level pass so // every component re-runs Render() even when props/deps are unchanged. _forceFullRenderActive = ForceFullRenderPending; ForceFullRenderPending = false ; // Build the dirty-ancestor path. For every component node // whose SelfTriggered is true, walk up the realized visual // tree and add each ancestor control. Consumed by Update's // shallow-equality short-circuit so the walk can reach the // self-triggered descendant even when its ancestor element // records are structurally unchanged. PopulateDirtyAncestorPath(); } try { try { if (newElement is null or EmptyElement) { if (existingControl is not null ) Unmount(existingControl); return null ; } if (oldElement is null or EmptyElement || existingControl is null ) return Mount(newElement, requestRerender); return ReconcileImperative(oldElement, newElement, existingControl, requestRerender); In a flame-graph viewer you see a band whose top edge is ReconcileStart and whose bottom edge is ReconcileStop . The band's width is the wall-clock duration of the pass. Hover the band and the payload fields show up: elementsDiffed is the count of element records the reconciler walked, elementsSkipped is the count it bailed out of by ShouldUpdate or referential equality, uiElementsCreated is the count of WinUI FrameworkElement instances allocated this pass, and uiElementsModified is the count of property writes against existing controls. High elementsSkipped with a short pass is the healthy case — the bail-out is working. Low elementsSkipped with a long pass usually means the UseMemo deps array is unstable: a new array or a new closure as a dep makes every render look new, and the bail-out can't fire. The reconcile-highlight overlay catches this cheaper than a trace — flick it on and watch which component flashes every time you type. Caveat: Debug builds disable several of the reconciler's bail-out optimizations to surface invariant violations. The same scenario on a Debug build shows 3–4× the elementsDiffed count and roughly 2× the elapsed microseconds vs. Release. Always profile against -c Release ; a flame graph from a Debug build is not a valid signal about Release performance. The EventDispatch keyword # The EventDispatch keyword covers the per-event trampoline that lets Reactor swap your handler closure between renders without detaching from the WinUI event: private static void EnsureSizeChangedSubscribed ( FrameworkElement fe, EventHandlerState state, Action&#x3C; object , SizeChangedEventArgs>? handler ) { state.CurrentSizeChanged = handler; if (state.SizeChangedTrampoline is null &#x26;&#x26; handler is not null ) { state.SizeChangedTrampoline = (s, e) => { Diagnostics.ReactorEventSource.Log.EventTrampolineDispatch( \"SizeChanged\" ); state.CurrentSizeChanged?.Invoke(s!, e); }; fe.SizeChanged += state.SizeChangedTrampoline; Diagnostics.ReactorEventSource.Log.EventTrampolineAttached( \"SizeChanged\" , fe.GetType().Name); } } The pattern matters for performance because the trampoline is bound once per element lifetime — every subsequent render that hands the element a new callback updates a field in state , not the WinUI event subscription list. Without this trick, every render that changes a handler would call fe.SizeChanged -= then fe.SizeChanged += ; under the cover, WinUI walks a linked list each time and you'd see a measurable hit on lists with hundreds of subscribed elements. When you do see EventTrampolineDispatch events dominating a verbose-level trace, that's the trampoline doing its actual job — the events fire on the UI thread for every routed event WinUI emits. Mask out EventDispatch ( 0x3 instead of 0x43 ) unless you're specifically debugging input or pointer flow. Reproducing a benchmark # A regression claim needs a number, and the bench harness under tests/perf_bench/ is how you get one. Each bench has three sibling variants — *.Reactor , *.Direct (raw WinUI), and *.Bound (WinUI + MVVM bindings) — so you can compare Reactor to its non-declarative baselines. The entry point is the standard top-level ReactorApp.Run call with a CLI options parser bolted on: var opts = BenchCliOptions.Parse( args ); if (opts.Headless) ConsoleHelper.EnsureConsole(); DirtyTrackingApp.Opts = opts; ReactorApp.Run&#x3C;DirtyTrackingApp>( \"EXP-1 DirtyTracking.Reactor\" , fullScreen: true ); Run with --headless --duration 10 and the app writes a *.report.txt next to the exe, then exits. The report includes the metrics the harness records each frame — FPS, update milliseconds, memory, GC generation counts, longest frame block: sb.AppendLine( $\"Avg FPS: {_fpsSamples.Average():F1} \" ); sb.AppendLine( $\"Min FPS: {_fpsSamples.Min():F1} \" ); sb.AppendLine( $\"Max FPS: {_fpsSamples.Max():F1} \" ); if (_updateTimeSamples.Count > 0 ) { sb.AppendLine( $\"Avg Update: {_updateTimeSamples.Average():F2} ms\" ); sb.AppendLine( $\"Max Update: {_updateTimeSamples.Max():F2} ms\" ); } sb.AppendLine( $\"Avg Memory: {_memorySamples.Average() / ( 1024 * 1024 ):F1} MB\" ); sb.AppendLine( $\"Peak Memory: {_memorySamples.Max() / ( 1024 * 1024 ):F1} MB\" ); // GC int gen0Total = GC.CollectionCount( 0 ) - _baselineGen0; int gen1Total = GC.CollectionCount( 1 ) - _baselineGen1; int gen2Total = GC.CollectionCount( 2 ) - _baselineGen2; sb.AppendLine( $\"GC Gen0: {gen0Total} \" ); sb.AppendLine( $\"GC Gen1: {gen1Total} \" ); sb.AppendLine( $\"GC Gen2: {gen2Total} \" ); // Frame jank sb.AppendLine( $\"Longest Block: {_longestFrameBlockMs:F1} ms\" ); sb.AppendLine( $\"Anim Drops: {_animationDrops} \" ); To reproduce a regression locally: dotnet build tests/perf_bench/PerfBench.DirtyTracking/DirtyTracking.Reactor -c Release ./tests/perf_bench/PerfBench.DirtyTracking/DirtyTracking.Reactor/bin/&#x3C;plat>/Release/&#x3C;tfm>/DirtyTracking.Reactor.exe --headless --duration 30 cat ./tests/.../bin/&#x3C;plat>/Release/&#x3C;tfm>/EXP1_DirtyTracking_Reactor.report.txt Re-run the same command on main and on your branch; the deltas in Avg Update , Anim Drops , and GC Gen0 are the regression signal. Tips # Pin the level when you collect. dotnet-trace defaults to Verbose , which captures every StateChange write. Pin to Informational ( :4 ) for reconcile/render workflows and the trace shrinks by roughly an order of magnitude — a 30-second session that was 1.2 GB becomes ~80 MB. Reach for an overlay before a trace. The reconcile-highlight and layout-cost overlays cost nothing to enable and will solve \"which component is re-rendering too much\" without you ever opening a profiler. A trace is the right tool when you need elapsed microseconds; the overlays are the right tool when you need \"which one\". Compare against the matching *.Direct bench. A Reactor bench result in isolation is hard to interpret. Run the sibling PerfBench.*.Direct variant on the same hardware and the delta tells you what declarative composition costs over raw WinUI — that's the framework-overhead number, not the absolute number. Next Steps # Perf Instrumentation — Under-the-hood companion: why each event exists, the IsEnabled gate, how to add a new event id. Dev Tooling — The reconcile-highlight and layout-cost overlays plus the rest of the mur CLI. DevTools Internals — How the overlays read the same provider events the profilers do. Reconciliation — What the ReconcileStop counters describe; how to read the keyed child-list diff. Hooks — Where UseMemo deps come from and what makes them unstable.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/performance.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/performance.md",
      "headings": [
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "the-provider-and-its-keywords",
          "text": "The provider and its keywords"
        },
        {
          "depth": 2,
          "id": "taking-a-reactor-only-trace",
          "text": "Taking a Reactor-only trace"
        },
        {
          "depth": 2,
          "id": "taking-a-correlated-trace-reactor--winui",
          "text": "Taking a correlated trace (Reactor + WinUI)"
        },
        {
          "depth": 2,
          "id": "reading-a-span-the-reconciler-entry",
          "text": "Reading a span: the reconciler entry"
        },
        {
          "depth": 2,
          "id": "the-eventdispatch-keyword",
          "text": "The EventDispatch keyword"
        },
        {
          "depth": 2,
          "id": "reproducing-a-benchmark",
          "text": "Reproducing a benchmark"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Packaging",
      "path": "packaging.md",
      "slug": "/packaging/",
      "html": "<h1 id=\"packaging\">Packaging<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#packaging\">#</a></h1>\n<p>A Microsoft.UI.Reactor (Reactor) app is a normal WinUI 3 / Windows App SDK executable —\n<code>dotnet publish</code> produces the deployable artifact and the framework\nitself adds nothing exotic to the project file. What you choose at\npublish time is the <strong>shape</strong> of that artifact: an unpackaged folder\n(the <a href=\"/getting-started/\"><code>dotnet new reactorapp</code></a> default), a signed\nMSIX, a single-file bundle, or a Native AOT native binary — each\ncombined with a <code>win-x64</code> or <code>win-arm64</code> runtime identifier. The\ntrade-offs are the same ones any WinUI 3 app faces; the\nReactor-specific notes on this page cover what changes when your\ncodebase leans on the reflection-driven pieces of the framework\n(<a href=\"/data-system/\"><code>AutoColumns&#x3C;T></code></a>, the devtools component\ndiscovery in <a href=\"/components/\"><code>ReactorApp.Run</code></a>, and the\n<code>UseObservableTree</code> INPC walker).</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Publish shape</th><th>Key properties</th><th>Runtime identifier</th><th>What you get</th></tr></thead><tbody><tr><td>Unpackaged (template default)</td><td><code>WindowsPackageType=None</code>, <code>WindowsAppSDKSelfContained=true</code></td><td><code>win-x64</code> / <code>win-arm64</code></td><td>A folder with <code>MyApp.exe</code> and the WinUI 3 runtime alongside it. Run from anywhere; ship as a zip.</td></tr><tr><td>MSIX</td><td><code>WindowsPackageType=MSIX</code>, <code>GenerateAppxPackageOnBuild=true</code>, signed via <code>PackageCertificateThumbprint</code> or <code>PackageCertificateKeyFile</code></td><td><code>win-x64</code> / <code>win-arm64</code></td><td>A signed <code>.msix</code>. Required for Microsoft Store; the cleanest sideload story for enterprise.</td></tr><tr><td>Single-file</td><td><code>PublishSingleFile=true</code>, <code>IncludeNativeLibrariesForSelfExtract=true</code></td><td><code>win-x64</code> / <code>win-arm64</code> (must be set)</td><td>One <code>.exe</code> that self-extracts the WinUI runtime to <code>%TEMP%/.net/</code> on first launch.</td></tr><tr><td>Native AOT</td><td><code>PublishAot=true</code>, <code>InvariantGlobalization=true</code> (recommended)</td><td><code>win-x64</code> / <code>win-arm64</code> (required)</td><td>A native binary with no JIT, no <code>Assembly.GetTypes()</code>, no <code>Reflection.Emit</code>. Fastest cold start; trim-only.</td></tr></tbody></table>\n<p>The four shapes are not mutually exclusive — MSIX wraps any of the\nthree publish outputs, and AOT layers on top of either an unpackaged\nfolder or an MSIX. The decision is usually distribution-channel-first\n(Store / sideload / direct download) and then performance-second.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/packaging/publish-pipeline.svg\" alt=\"Reactor publish pipeline: dotnet publish takes a Reactor CSPROJ to one of three output shapes (unpackaged folder, single-file bundle, signed MSIX), with Native AOT layered on top of either the unpackaged or the MSIX form\"></p>\n<h2 id=\"the-unpackaged-shape\">The unpackaged shape<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-unpackaged-shape\">#</a></h2>\n<p><code>dotnet new reactorapp</code> scaffolds an unpackaged WinUI 3 project — the\nshape every sample in this repo also uses:</p>\n<pre><code class=\"hljs language-xml\"></code></pre>\n<p>The load-bearing properties are <code>UseWinUI=true</code> (pulls the WinUI 3\nXAML runtime), <code>WindowsPackageType=None</code> (no MSIX wrapper —\n<code>MyApp.exe</code> runs straight from the publish folder), and the explicit\n<code>&#x3C;Platforms>x64;ARM64&#x3C;/Platforms></code> (Windows App SDK self-contained\nbuilds reject the AnyCPU default). The\n<a href=\"https://www.nuget.org/packages/Microsoft.WindowsAppSDK\"><code>Microsoft.WindowsAppSDK</code></a>\npackage reference pulls the WinUI runtime; the <code>WindowsAppSDKVersion</code>\nproperty comes from <code>Directory.Build.props</code> so every project in the\nrepo pins the same SDK build.</p>\n<p>To ship this shape, <code>dotnet publish -c Release -r win-x64</code>. The\npublish folder contains <code>MyApp.exe</code>, <code>Reactor.dll</code>, the WinUI runtime\n(<code>Microsoft.WindowsAppRuntime.Bootstrap.dll</code>, the XAML compiler\noutput <code>MyApp.xbf</code>, etc.), and the .NET runtime if\n<code>WindowsAppSDKSelfContained=true</code>. Zip it and you have a sideloadable\nbuild that runs on any matching-arch Windows 10 1809+ machine.</p>\n<h2 id=\"msix\">MSIX<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#msix\">#</a></h2>\n<p>For Microsoft Store distribution and most enterprise sideloading,\nwrap the same publish output in an MSIX. The single-project MSIX\nshape adds three properties on top of the unpackaged CSPROJ:</p>\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PropertyGroup</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">WindowsPackageType</span>></span>MSIX<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">WindowsPackageType</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">GenerateAppxPackageOnBuild</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">GenerateAppxPackageOnBuild</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">AppxPackageSigningEnabled</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">AppxPackageSigningEnabled</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PackageCertificateThumbprint</span>></span>...<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">PackageCertificateThumbprint</span>></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">PropertyGroup</span>></span>\n\n<span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">ItemGroup</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">AppxManifest</span> <span class=\"hljs-attr\">Include</span>=<span class=\"hljs-string\">\"Package.appxmanifest\"</span> /></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">ItemGroup</span>></span>\n</code></pre>\n<p><code>Package.appxmanifest</code> declares the package identity (Publisher,\nPackageFamilyName, capabilities, file-type associations). The\n<a href=\"https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/packaging/\">WinUI 3 packaging docs</a>\ncover the manifest surface in full. The signing certificate is\neither a Microsoft Store-issued cert (for Store submissions) or a\nself-signed cert imported into <code>Cert:\\CurrentUser\\My</code> (for\nsideloading). MSIX is the only shape that gives the app a package\nidentity — features like background tasks, share targets, and the\nnotifier APIs require it.</p>\n<h2 id=\"single-file-publish\">Single-file publish<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#single-file-publish\">#</a></h2>\n<p>Single-file collapses the publish folder into one <code>.exe</code> that\nself-extracts on launch. For a WinUI 3 app the native runtime bits\nare not in managed assemblies, so the bare <code>PublishSingleFile=true</code>\nleaves several DLLs alongside the binary — add\n<code>IncludeNativeLibrariesForSelfExtract</code> to fold those into the bundle:</p>\n<pre><code class=\"hljs language-xml\"><span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PropertyGroup</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">PublishSingleFile</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">PublishSingleFile</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">SelfContained</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">SelfContained</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">IncludeNativeLibrariesForSelfExtract</span>></span>true<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">IncludeNativeLibrariesForSelfExtract</span>></span>\n  <span class=\"hljs-tag\">&#x3C;<span class=\"hljs-name\">RuntimeIdentifier</span>></span>win-x64<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">RuntimeIdentifier</span>></span>\n<span class=\"hljs-tag\">&#x3C;/<span class=\"hljs-name\">PropertyGroup</span>></span>\n</code></pre>\n<p>The trade-off is first-launch latency — the runtime extracts the\nembedded assemblies to <code>%TEMP%\\.net\\</code> (or the directory in\n<code>DOTNET_BUNDLE_EXTRACT_BASE_DIR</code>) before the process starts. Two\nReactor-specific notes: <code>Assembly.Location</code> returns an empty string\ninside a single-file bundle, so any code path that builds paths next\nto the exe should use\n<a href=\"https://learn.microsoft.com/en-us/dotnet/api/system.appcontext.basedirectory\"><code>AppContext.BaseDirectory</code></a>\ninstead; and the\n<a href=\"/persistence/\"><code>UsePersisted</code></a> <code>Application</code> scope writes to\n<code>%LOCALAPPDATA%\\&#x3C;AssemblyName>\\</code> — single-file doesn't change that\nlocation, but trimming away the assembly name (via\n<code>&#x3C;AssemblyName></code> rename in a publish profile) does.</p>\n<h2 id=\"arm64\">ARM64<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#arm64\">#</a></h2>\n<p>ARM64 is a second runtime identifier on the same project — the\n<code>&#x3C;Platforms>x64;ARM64&#x3C;/Platforms></code> line in the project template\nexists so MSBuild accepts the per-platform restore. Build for both\nin CI by running publish twice:</p>\n<pre><code class=\"hljs language-powershell\">dotnet publish -c Release -r win-x64   -o out/x64\ndotnet publish -c Release -r win-arm64 -o out/arm64\n</code></pre>\n<p>There is no separate Reactor build for ARM64 — <code>Reactor.dll</code> is\nAnyCPU-equivalent managed code that compiles for both architectures\nfrom the same source. The native bits below it (the WinUI 3 runtime\nand any <code>System.Drawing.Common</code> / <code>TraceEvent</code> natives transitively\npulled in by Reactor) ship per-RID, which is why the runtime\nidentifier matters even for managed-only Reactor code. The repo's\nsample apps default to <code>&#x3C;Platforms>x64;ARM64&#x3C;/Platforms></code>; the\n<code>reactorapp</code> template adds <code>X86</code> to the list for parity with the\nWinUI 3 templates, but Reactor itself is only tested on x64 / ARM64.</p>\n<h2 id=\"native-aot\">Native AOT<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#native-aot\">#</a></h2>\n<p>Reactor's perf-bench projects publish under AOT and run cleanly —\nthe framework is built to be AOT-compatible on its hot path. The\nshape is the same as any other AOT publish, with <code>PublishAot=true</code>\nand a runtime identifier:</p>\n<pre><code class=\"hljs language-xml\"></code></pre>\n<p><code>dotnet publish -c Release -r win-x64</code> produces a native binary —\nno <code>coreclr.dll</code>, no JIT, ~50 ms cold start versus ~250 ms for the\nJIT-based build on the same hardware. The project template gates\nthe same shape behind a <code>NativeAot</code> parameter:</p>\n<pre><code class=\"hljs language-xml\"></code></pre>\n<p>Pass <code>dotnet new reactorapp --NativeAot true</code> to get the AOT-enabled\nvariant. <code>InvariantGlobalization=true</code> is paired with <code>PublishAot</code>\nbecause the alternative — shipping the full ICU data — pulls in\ntrim warnings that the AOT analyzer flags as actionable.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <strong><code>AutoColumns&#x3C;T></code> and <code>Assembly.GetTypes()</code> are the two reflection\nsurfaces to know about.</strong> <code>Factories.AutoColumns&#x3C;T>()</code> walks\n<code>typeof(T).GetProperties()</code> to build <code>FieldDescriptor</code>s for a\n<a href=\"/data-system/\"><code>DataGrid&#x3C;T></code></a>, so the generic argument carries\n<code>[DynamicallyAccessedMembers(PublicProperties | PublicConstructors)]</code>\n— call it from your component and the AOT analyzer threads the\nannotation back through your code. Hand-built <code>Column&#x3C;T>(...)</code>\ncolumns avoid the reflection entirely and are the safe choice for\ntrim-paranoid code. The devtools code path\n(<code>ReactorApp.Run(..., devtools: true)</code>) walks\n<code>Assembly.GetTypes()</code> to enumerate component types, which is\nincompatible with trimming and carries <code>[RequiresUnreferencedCode]</code>\non every method that touches it — the AOT publish will warn unless\nyou drop <code>devtools: true</code> from retail builds (the documented retail\nshape; see <a href=\"/dev-tooling/\">Dev Tooling</a>).</p>\n</blockquote>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pick distribution before performance.</strong> MSIX is Store-only-ish\nand gives you a real package identity; unpackaged + zip is the\neasiest direct-download story; single-file is the same shape as\nunpackaged with a slower first launch. AOT is independent of all\nthree — apply it once the distribution shape is settled.</p>\n<p><strong>Publish for both architectures in CI from day one.</strong> ARM64\nWindows on Snapdragon X is a real audience now; an x64-only build\nruns under emulation but pays a startup tax. The <code>&#x3C;Platforms></code> line\nin the template covers both — the missing piece is the second\n<code>dotnet publish -r win-arm64</code> invocation in the build pipeline.</p>\n<p><strong>Keep <code>devtools: true</code> out of retail.</strong> <a href=\"/dev-tooling/\"><code>devtools: true</code></a>\non <code>ReactorApp.Run</code> is a capability gate that nothing user-visible\ndepends on at runtime — but the code path it enables walks\n<code>Assembly.GetTypes()</code>. Removing it from the release build drops the\nlast AOT trim warning that the framework emits.</p>\n<p><strong><code>Reactor.dll</code> is in your publish output as a managed assembly,\nnot a tucked-away framework package.</strong> Reactor ships as\n<code>Microsoft.UI.Reactor</code> (see <a href=\"https://github.com/microsoft/microsoft-ui-reactor\">spec 022 / Phase 0 release-asset\nflow</a> — local\nbuilds get <code>0.0.0-local</code> via <code>mur pack-local</code>). Trim-friendly\ndeployments don't get any framework-side magic; the same trimmer\nconfiguration that works for any WinUI 3 app works here.</p>\n<p><strong><code>Microsoft.WindowsAppSDK</code> is a transitive dependency, not a\ndirect one in the template.</strong> The <code>dotnet new reactorapp</code> CSPROJ\nreferences <code>Microsoft.UI.Reactor</code> and lets the WinUI SDK flow\nthrough. Override it in your CSPROJ when you need a specific SDK\npatch — Reactor pins through <code>WindowsAppSDKVersion</code> in\n<code>Directory.Build.props</code> so an explicit override is the\nintentional knob.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — Previous: the inner-loop side of the build pipeline (<code>mur pack-local</code>, <code>dotnet watch</code>, hot reload).</li>\n<li><strong><a href=\"/getting-started/\">Getting Started</a></strong> — Where the <code>dotnet new reactorapp</code> template that produces the unpackaged shape comes from.</li>\n<li><strong><a href=\"/performance/\">Performance</a></strong> — When you should reach for AOT (cold-start budgets, startup-perf benchmarks).</li>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — The ETW / EventPipe pipeline that survives AOT publish unchanged.</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — Where <code>ReactorApp.Run</code> and the <code>devtools: true</code> capability gate live.</li>\n</ul>",
      "text": "Packaging # A Microsoft.UI.Reactor (Reactor) app is a normal WinUI 3 / Windows App SDK executable — dotnet publish produces the deployable artifact and the framework itself adds nothing exotic to the project file. What you choose at publish time is the shape of that artifact: an unpackaged folder (the dotnet new reactorapp default), a signed MSIX, a single-file bundle, or a Native AOT native binary — each combined with a win-x64 or win-arm64 runtime identifier. The trade-offs are the same ones any WinUI 3 app faces; the Reactor-specific notes on this page cover what changes when your codebase leans on the reflection-driven pieces of the framework ( AutoColumns&#x3C;T> , the devtools component discovery in ReactorApp.Run , and the UseObservableTree INPC walker). Publish shape Key properties Runtime identifier What you get Unpackaged (template default) WindowsPackageType=None , WindowsAppSDKSelfContained=true win-x64 / win-arm64 A folder with MyApp.exe and the WinUI 3 runtime alongside it. Run from anywhere; ship as a zip. MSIX WindowsPackageType=MSIX , GenerateAppxPackageOnBuild=true , signed via PackageCertificateThumbprint or PackageCertificateKeyFile win-x64 / win-arm64 A signed .msix . Required for Microsoft Store; the cleanest sideload story for enterprise. Single-file PublishSingleFile=true , IncludeNativeLibrariesForSelfExtract=true win-x64 / win-arm64 (must be set) One .exe that self-extracts the WinUI runtime to %TEMP%/.net/ on first launch. Native AOT PublishAot=true , InvariantGlobalization=true (recommended) win-x64 / win-arm64 (required) A native binary with no JIT, no Assembly.GetTypes() , no Reflection.Emit . Fastest cold start; trim-only. The four shapes are not mutually exclusive — MSIX wraps any of the three publish outputs, and AOT layers on top of either an unpackaged folder or an MSIX. The decision is usually distribution-channel-first (Store / sideload / direct download) and then performance-second. The unpackaged shape # dotnet new reactorapp scaffolds an unpackaged WinUI 3 project — the shape every sample in this repo also uses: The load-bearing properties are UseWinUI=true (pulls the WinUI 3 XAML runtime), WindowsPackageType=None (no MSIX wrapper — MyApp.exe runs straight from the publish folder), and the explicit &#x3C;Platforms>x64;ARM64&#x3C;/Platforms> (Windows App SDK self-contained builds reject the AnyCPU default). The Microsoft.WindowsAppSDK package reference pulls the WinUI runtime; the WindowsAppSDKVersion property comes from Directory.Build.props so every project in the repo pins the same SDK build. To ship this shape, dotnet publish -c Release -r win-x64 . The publish folder contains MyApp.exe , Reactor.dll , the WinUI runtime ( Microsoft.WindowsAppRuntime.Bootstrap.dll , the XAML compiler output MyApp.xbf , etc.), and the .NET runtime if WindowsAppSDKSelfContained=true . Zip it and you have a sideloadable build that runs on any matching-arch Windows 10 1809+ machine. MSIX # For Microsoft Store distribution and most enterprise sideloading, wrap the same publish output in an MSIX. The single-project MSIX shape adds three properties on top of the unpackaged CSPROJ: &#x3C; PropertyGroup > &#x3C; WindowsPackageType > MSIX &#x3C;/ WindowsPackageType > &#x3C; GenerateAppxPackageOnBuild > true &#x3C;/ GenerateAppxPackageOnBuild > &#x3C; AppxPackageSigningEnabled > true &#x3C;/ AppxPackageSigningEnabled > &#x3C; PackageCertificateThumbprint > ... &#x3C;/ PackageCertificateThumbprint > &#x3C;/ PropertyGroup > &#x3C; ItemGroup > &#x3C; AppxManifest Include = \"Package.appxmanifest\" /> &#x3C;/ ItemGroup > Package.appxmanifest declares the package identity (Publisher, PackageFamilyName, capabilities, file-type associations). The WinUI 3 packaging docs cover the manifest surface in full. The signing certificate is either a Microsoft Store-issued cert (for Store submissions) or a self-signed cert imported into Cert:\\CurrentUser\\My (for sideloading). MSIX is the only shape that gives the app a package identity — features like background tasks, share targets, and the notifier APIs require it. Single-file publish # Single-file collapses the publish folder into one .exe that self-extracts on launch. For a WinUI 3 app the native runtime bits are not in managed assemblies, so the bare PublishSingleFile=true leaves several DLLs alongside the binary — add IncludeNativeLibrariesForSelfExtract to fold those into the bundle: &#x3C; PropertyGroup > &#x3C; PublishSingleFile > true &#x3C;/ PublishSingleFile > &#x3C; SelfContained > true &#x3C;/ SelfContained > &#x3C; IncludeNativeLibrariesForSelfExtract > true &#x3C;/ IncludeNativeLibrariesForSelfExtract > &#x3C; RuntimeIdentifier > win-x64 &#x3C;/ RuntimeIdentifier > &#x3C;/ PropertyGroup > The trade-off is first-launch latency — the runtime extracts the embedded assemblies to %TEMP%\\.net\\ (or the directory in DOTNET_BUNDLE_EXTRACT_BASE_DIR ) before the process starts. Two Reactor-specific notes: Assembly.Location returns an empty string inside a single-file bundle, so any code path that builds paths next to the exe should use AppContext.BaseDirectory instead; and the UsePersisted Application scope writes to %LOCALAPPDATA%\\&#x3C;AssemblyName>\\ — single-file doesn't change that location, but trimming away the assembly name (via &#x3C;AssemblyName> rename in a publish profile) does. ARM64 # ARM64 is a second runtime identifier on the same project — the &#x3C;Platforms>x64;ARM64&#x3C;/Platforms> line in the project template exists so MSBuild accepts the per-platform restore. Build for both in CI by running publish twice: dotnet publish -c Release -r win-x64 -o out/x64 dotnet publish -c Release -r win-arm64 -o out/arm64 There is no separate Reactor build for ARM64 — Reactor.dll is AnyCPU-equivalent managed code that compiles for both architectures from the same source. The native bits below it (the WinUI 3 runtime and any System.Drawing.Common / TraceEvent natives transitively pulled in by Reactor) ship per-RID, which is why the runtime identifier matters even for managed-only Reactor code. The repo's sample apps default to &#x3C;Platforms>x64;ARM64&#x3C;/Platforms> ; the reactorapp template adds X86 to the list for parity with the WinUI 3 templates, but Reactor itself is only tested on x64 / ARM64. Native AOT # Reactor's perf-bench projects publish under AOT and run cleanly — the framework is built to be AOT-compatible on its hot path. The shape is the same as any other AOT publish, with PublishAot=true and a runtime identifier: dotnet publish -c Release -r win-x64 produces a native binary — no coreclr.dll , no JIT, ~50 ms cold start versus ~250 ms for the JIT-based build on the same hardware. The project template gates the same shape behind a NativeAot parameter: Pass dotnet new reactorapp --NativeAot true to get the AOT-enabled variant. InvariantGlobalization=true is paired with PublishAot because the alternative — shipping the full ICU data — pulls in trim warnings that the AOT analyzer flags as actionable. Caveat: AutoColumns&#x3C;T> and Assembly.GetTypes() are the two reflection surfaces to know about. Factories.AutoColumns&#x3C;T>() walks typeof(T).GetProperties() to build FieldDescriptor s for a DataGrid&#x3C;T> , so the generic argument carries [DynamicallyAccessedMembers(PublicProperties | PublicConstructors)] — call it from your component and the AOT analyzer threads the annotation back through your code. Hand-built Column&#x3C;T>(...) columns avoid the reflection entirely and are the safe choice for trim-paranoid code. The devtools code path ( ReactorApp.Run(..., devtools: true) ) walks Assembly.GetTypes() to enumerate component types, which is incompatible with trimming and carries [RequiresUnreferencedCode] on every method that touches it — the AOT publish will warn unless you drop devtools: true from retail builds (the documented retail shape; see Dev Tooling ). Tips # Pick distribution before performance. MSIX is Store-only-ish and gives you a real package identity; unpackaged + zip is the easiest direct-download story; single-file is the same shape as unpackaged with a slower first launch. AOT is independent of all three — apply it once the distribution shape is settled. Publish for both architectures in CI from day one. ARM64 Windows on Snapdragon X is a real audience now; an x64-only build runs under emulation but pays a startup tax. The &#x3C;Platforms> line in the template covers both — the missing piece is the second dotnet publish -r win-arm64 invocation in the build pipeline. Keep devtools: true out of retail. devtools: true on ReactorApp.Run is a capability gate that nothing user-visible depends on at runtime — but the code path it enables walks Assembly.GetTypes() . Removing it from the release build drops the last AOT trim warning that the framework emits. Reactor.dll is in your publish output as a managed assembly, not a tucked-away framework package. Reactor ships as Microsoft.UI.Reactor (see spec 022 / Phase 0 release-asset flow — local builds get 0.0.0-local via mur pack-local ). Trim-friendly deployments don't get any framework-side magic; the same trimmer configuration that works for any WinUI 3 app works here. Microsoft.WindowsAppSDK is a transitive dependency, not a direct one in the template. The dotnet new reactorapp CSPROJ references Microsoft.UI.Reactor and lets the WinUI SDK flow through. Override it in your CSPROJ when you need a specific SDK patch — Reactor pins through WindowsAppSDKVersion in Directory.Build.props so an explicit override is the intentional knob. Next Steps # Dev Tooling — Previous: the inner-loop side of the build pipeline ( mur pack-local , dotnet watch , hot reload). Getting Started — Where the dotnet new reactorapp template that produces the unpackaged shape comes from. Performance — When you should reach for AOT (cold-start budgets, startup-perf benchmarks). Perf Instrumentation — The ETW / EventPipe pipeline that survives AOT publish unchanged. Components — Where ReactorApp.Run and the devtools: true capability gate live.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/packaging.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/packaging.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-unpackaged-shape",
          "text": "The unpackaged shape"
        },
        {
          "depth": 2,
          "id": "msix",
          "text": "MSIX"
        },
        {
          "depth": 2,
          "id": "single-file-publish",
          "text": "Single-file publish"
        },
        {
          "depth": 2,
          "id": "arm64",
          "text": "ARM64"
        },
        {
          "depth": 2,
          "id": "native-aot",
          "text": "Native AOT"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Diagnostics",
      "path": "diagnostics.md",
      "slug": "/diagnostics/",
      "html": "<p>When Microsoft.UI.Reactor (Reactor) swallows an exception, returns past an HRESULT, or\notherwise chooses to continue rather than throw, the framework has\nhistorically dropped a <code>Debug.WriteLine</code> and moved on. That worked for\nthe contributor — the message landed in Visual Studio's Output window\nduring a Debug build — but it disappeared in Release, which is the\nconfiguration every shipped app runs. Spec 044 fixed that: error and\nHRESULT diagnostics now route through the <code>Microsoft-UI-Reactor</code>\n<code>EventSource</code> (release-visible, keyword-gated, zero-allocation when\nno consumer is listening), and the existing in-process devtools tool\n(<code>reactor.logs</code>) was extended so an MCP agent can read framework\nevents in the same call that returns stdout / stderr / debug output.</p>\n<h1 id=\"diagnostics\">Diagnostics<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#diagnostics\">#</a></h1>\n<p>This page is about reading Reactor's diagnostics from a real app —\nnot about adding new events. The emission pipeline (keywords,\nIsEnabled gate, event-id allocation, EventPipe vs ETW transport split)\nlives in <a href=\"/perf-instrumentation/\">perf-instrumentation.md</a>; start\nthere if you are extending the provider.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/diagnostics/flow.svg\" alt=\"Reactor diagnostics flow — a framework catch site delegates to DiagnosticLog, which emits to ReactorEventSource for the Release path and mirrors to Debug.WriteLine in Debug builds; four consumers read the event surface: env-var / dotnet-trace capture, Visual Studio Profiler, the in-process ReactorTrace.Subscribe helper, and the reactor.logs MCP tool\"></p>\n<h2 id=\"the-rule\">The rule<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-rule\">#</a></h2>\n<p>Audience, not severity, decides the channel. <code>Debug.WriteLine</code>\nexists for the contributor working on the framework itself; the\ntarget audience is \"someone reading the Output window in Visual\nStudio with their checkout open\". <code>ReactorEventSource</code> exists for\nthe app developer, the SRE, and the support engineer; the target\naudience is \"someone who runs the shipped binary and needs to know\nwhy a window failed to open\".</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Audience</th><th>Channel</th><th>Visible in Release?</th></tr></thead><tbody><tr><td>Framework contributor</td><td><code>Debug.WriteLine</code>, <code>Debug.Assert</code></td><td>No</td></tr><tr><td>App developer / SRE</td><td><code>Microsoft-UI-Reactor</code> <code>EventSource</code></td><td>Yes</td></tr><tr><td>Unreachable code</td><td><code>throw new UnreachableException(...)</code></td><td>Yes (as a crash)</td></tr></tbody></table>\n<p>The two channels are complementary, not redundant. A swallowed\nexception in <code>RenderContext</code> emits both — the typed event for the\napp developer and a richer <code>Debug.WriteLine</code> mirror (with the\nexception message) for the contributor running a Debug build. The\nmirror is <code>[Conditional(\"DEBUG\")]</code> and compiles out in Release.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Exception messages are PII-shaped and never reach the ETW payload.\n<code>ex.Message</code> can carry absolute paths, environment values, partial\nform values, and the bound user data that caused the failure. The\ntyped event payload carries the exception <em>type</em> only\n(<code>InvalidOperationException</code>, <code>COMException</code>); a same-UID\n<code>dotnet-trace</code> consumer sees the type and nothing else. If you need\nthe message in your own logs, attach an in-process subscriber (see\n<a href=\"#in-process-subscription\"><code>ReactorTrace.Subscribe</code></a> below) and\nforward to a sink under your own ACL.</p>\n</blockquote>\n<h2 id=\"whats-instrumented\">What's instrumented<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#whats-instrumented\">#</a></h2>\n<p>The provider's events split across a small set of keywords; spec 044\nadds six subsystem keywords on top of the seven the perf-instrumentation\npage documents. Pick the bits that match what you're triaging:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Keyword</th><th>Bit</th><th>Covers</th></tr></thead><tbody><tr><td><code>Errors</code></td><td><code>0x20</code></td><td>Generic <code>SwallowedError</code> / <code>HResultFailed</code>, plus <code>RenderError</code></td></tr><tr><td><code>Hosting</code></td><td><code>0x80</code></td><td><code>WindowOpened</code>, <code>WindowClosed</code>, <code>WindowDpiChanged</code>, <code>BackdropMaterializationFailed</code></td></tr><tr><td><code>Persistence</code></td><td><code>0x100</code></td><td><code>PersistenceRead</code>, <code>PersistenceWrite</code>, <code>PersistenceRejected</code></td></tr><tr><td><code>Navigation</code></td><td><code>0x200</code></td><td><code>NavigationRequested</code>, <code>NavigationCompleted</code>, <code>NavigationCancelled</code>, cache hit/miss/evict, transitions, deep-link</td></tr><tr><td><code>Intl</code></td><td><code>0x400</code></td><td><code>IntlMissingKey</code></td></tr><tr><td><code>Theme</code></td><td><code>0x800</code></td><td><code>ThemeApplyFailed</code></td></tr><tr><td><code>Shell</code></td><td><code>0x1000</code></td><td><code>JumpList*</code> / <code>ThumbnailToolbar*</code> / <code>Tray*</code> (planned)</td></tr></tbody></table>\n<p>Combine bits with bitwise-or. The most common capture-everything-\nunsurprising mask is <code>0x1FA0</code> (<code>Errors | Hosting | Persistence | Navigation | Intl | Theme</code>) — drops the verbose <code>State</code> and\n<code>EventDispatch</code> keywords that produce per-state-write spam.</p>\n<h2 id=\"capturing-a-trace\">Capturing a trace<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#capturing-a-trace\">#</a></h2>\n<p>There are four routes. Pick by where the consumer lives.</p>\n<h3 id=\"environment-variables--zero-code-file-output\">Environment variables — zero code, file output<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#environment-variables--zero-code-file-output\">#</a></h3>\n<p>For a quick local capture with no app changes, the .NET runtime can\nwrite an EventPipe <code>.nettrace</code> file driven entirely by environment\nvariables. This is the right tool when an issue reproduces only in a\nshipped build, on a different machine, or when you want to hand the\ntrace off to someone else:</p>\n<pre><code>set DOTNET_EnableEventPipe=1\nset DOTNET_EventPipeOutputPath=reactor.nettrace\nset DOTNET_EventPipeConfig=Microsoft-UI-Reactor:0x1FA0:5\nMyApp.exe\n</code></pre>\n<p>The third variable is <code>&#x3C;provider>:&#x3C;keywords>:&#x3C;level></code>. <code>0x1FA0</code> is\nthe everything-unsurprising mask above; level <code>5</code> is <code>Verbose</code>. Run\nthe app, reproduce the issue, exit cleanly (the runtime flushes the\nfile on shutdown). Open the resulting <code>.nettrace</code> in Visual Studio's\nPerformance Profiler → Events Viewer.</p>\n<h3 id=\"dotnet-trace--attach-to-a-running-process\"><code>dotnet-trace</code> — attach to a running process<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dotnet-trace--attach-to-a-running-process\">#</a></h3>\n<p><code>dotnet-trace</code> is the cross-platform CLI for EventPipe capture.\nUseful when the app is already running and you want to scope the\nwindow:</p>\n<pre><code>dotnet-trace collect ^\n    --process-id &#x3C;pid> ^\n    --providers Microsoft-UI-Reactor:0x1FA0:5 ^\n    --output reactor.nettrace\n</code></pre>\n<p><code>Ctrl+C</code> stops the session; the <code>.nettrace</code> lands in the working\ndirectory. Same file format as the env-var route — same Events\nViewer workflow.</p>\n<h3 id=\"visual-studio-performance-profiler\">Visual Studio Performance Profiler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#visual-studio-performance-profiler\">#</a></h3>\n<p>For a GUI workflow, the Profiler's Events Viewer accepts the same\nprovider:keyword:level format. Diagnostics → Performance Profiler\n→ Events Viewer → Settings → Custom Provider:</p>\n<pre><code>Microsoft-UI-Reactor:0x1FA0:5\n</code></pre>\n<p>The timeline ties each Reactor event to the CPU sample, GC, and\nnetwork views, so you can see a <code>NavigationCompleted</code> next to the\nallocation spike it caused.</p>\n<h3 id=\"in-process-subscription\">In-process subscription<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#in-process-subscription\">#</a></h3>\n<p>When the consumer is the app itself — a custom log sink, a devtools\noverlay, an in-app diagnostics page — <code>ReactorTrace.Subscribe</code>\nreturns an <code>IDisposable</code> token that fires the callback for each\nevent matching the filter:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> IDisposable <span class=\"hljs-title\">Subscribe</span>(<span class=\"hljs-params\">\n    Action&#x3C;ReactorEvent> onEvent,\n    EventLevel level = EventLevel.Verbose,\n    EventKeywords keywords = (EventKeywords</span>)(<span class=\"hljs-params\"><span class=\"hljs-number\">-1</span></span>))</span>\n{\n    ArgumentNullException.ThrowIfNull(onEvent);\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> Subscription(onEvent, level, keywords);\n}\n</code></pre>\n<p>Multiple concurrent subscribers are supported; each filter is\nindependently active until the token is disposed. The subscriber\ncallback runs on the emission thread (usually the UI dispatcher when\nthe event originates from reconcile / render), so keep the work\nminimal — the framework wraps the call in <code>try/catch</code> so a buggy\nsink can't propagate to <code>EventSource.WriteEvent</code>, but the dispatcher\nis still blocked for the duration. Forward to a queue if your sink\ndoes anything expensive.</p>\n<p><code>ReactorTrace.Subscribe</code> is <strong>not</strong> a file-capture API. It exists\nbecause in-process consumers (devtools, an <code>ILogger</code> adapter, a\ncustom diagnostics page) need access to the same events the env-var\nroute writes to disk. For a <code>.nettrace</code> file, use one of the three\nroutes above — they cost less and emit a richer format.</p>\n<h2 id=\"reactorlogs-sourceevent--mcp-integration\"><code>reactor.logs source=event</code> — MCP integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactorlogs-sourceevent--mcp-integration\">#</a></h2>\n<p>The Reactor in-process devtools (<code>mur devtools</code>) expose a <code>logs</code> MCP\ntool that drains the captured <code>Console.Out</code> / <code>Console.Error</code> /\n<code>Debug.WriteLine</code> ring buffer. Spec 044 extended the tool so the\nbuffer also captures <code>Microsoft-UI-Reactor</code> ETW events, surfaced\nunder a new <code>source=event</code> filter:</p>\n<pre><code class=\"hljs language-jsonc\"><span class=\"hljs-comment\">// Request</span>\n<span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"method\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"tools/call\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"params\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">{</span>\n    <span class=\"hljs-attr\">\"name\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"logs\"</span><span class=\"hljs-punctuation\">,</span>\n    <span class=\"hljs-attr\">\"arguments\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"source\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"event\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"tail\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">20</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"level\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"Warning\"</span> <span class=\"hljs-punctuation\">}</span>\n  <span class=\"hljs-punctuation\">}</span>\n<span class=\"hljs-punctuation\">}</span>\n\n<span class=\"hljs-comment\">// Response — each entry now carries eventName / eventId</span>\n<span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"entries\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span>\n    <span class=\"hljs-punctuation\">{</span>\n      <span class=\"hljs-attr\">\"seq\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">142</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"ts\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"2026-05-19T17:42:11.330Z\"</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"source\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"event\"</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"level\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"Warning\"</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"text\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"SwallowedError category=Hosting operation=AppWindow.Close exceptionType=COMException\"</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"eventName\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"SwallowedError\"</span><span class=\"hljs-punctuation\">,</span>\n      <span class=\"hljs-attr\">\"eventId\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">16</span>\n    <span class=\"hljs-punctuation\">}</span>\n  <span class=\"hljs-punctuation\">]</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"nextSeq\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">143</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"dropped\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">0</span>\n<span class=\"hljs-punctuation\">}</span>\n</code></pre>\n<p>Existing clients that don't pass <code>source=event</code> see zero behavior\nchange — the <code>stdout</code> / <code>stderr</code> / <code>debug</code> filters still return their\ndedicated streams. The <code>eventName</code> and <code>eventId</code> fields are present\non every entry but are <code>null</code> for non-event sources, so a client\nwritten before spec 044 ignores them safely.</p>\n<p>HR-style payload fields render in the same <code>0x{X8}</code> shape the\npre-migration <code>Debug.WriteLine</code> sites used (<code>HResultFailed category=Shell operation=JumpList.Begin hr=0x80004002</code>), so log\ngreps that matched the old shape continue to hit.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"reading-a-swallow-that-happened-in-production\">Reading a swallow that happened in production<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-a-swallow-that-happened-in-production\">#</a></h3>\n<p>A customer reports a window that won't close cleanly on a specific\nmachine. Capture with the env-var route, filter to the <code>Errors</code>\nkeyword (<code>0x20</code>) at <code>Warning</code>, and open the trace in Events Viewer.\nThe relevant entries will look like:</p>\n<pre><code>SwallowedError  category=Hosting  operation=AppWindow.Close  exceptionType=COMException\nHResultFailed   category=Hosting  operation=AppWindow.Close  hr=0x80004005\n</code></pre>\n<p>The operation label is stable and developer-authored — search the\nReactor source for <code>\"AppWindow.Close\"</code> and you land on the\n<code>DiagnosticLog.SwallowedError</code> call site. The exception type and HR\ntogether pin the failure class (in this case, <code>E_FAIL</code> from the\nWinUI AppWindow lifecycle) without ever leaking the user-visible\nwindow title.</p>\n<h3 id=\"wiring-reactortracesubscribe-to-a-per-window-debug-overlay\">Wiring <code>ReactorTrace.Subscribe</code> to a per-window debug overlay<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#wiring-reactortracesubscribe-to-a-per-window-debug-overlay\">#</a></h3>\n<p>A devtools overlay that wants to surface \"navigation event happening\nnow\" doesn't need a file capture — just an in-process subscription:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">NavigationOverlay</span> : <span class=\"hljs-title\">IDisposable</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> IDisposable _subscription;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Queue&#x3C;<span class=\"hljs-built_in\">string</span>> _ring = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">NavigationOverlay</span>()</span>\n    {\n        _subscription = ReactorTrace.Subscribe(\n            evt =>\n            {\n                <span class=\"hljs-keyword\">if</span> ((evt.Keywords &#x26; ReactorEventSource.Keywords.Navigation) == <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n                <span class=\"hljs-keyword\">var</span> line = <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{evt.EventName}</span> <span class=\"hljs-subst\">{<span class=\"hljs-built_in\">string</span>.Join(<span class=\"hljs-string\">' '</span>,\n                    Enumerable.Range(<span class=\"hljs-number\">0</span>, evt.Payload.Count)\n                        .Select(i => <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{evt.PayloadNames[i]}</span>=<span class=\"hljs-subst\">{evt.Payload[i]}</span>\"</span>))}</span>\"</span>;\n                <span class=\"hljs-keyword\">lock</span> (_ring)\n                {\n                    _ring.Enqueue(line);\n                    <span class=\"hljs-keyword\">while</span> (_ring.Count > <span class=\"hljs-number\">50</span>) _ring.Dequeue();\n                }\n            },\n            level: EventLevel.Verbose,\n            keywords: ReactorEventSource.Keywords.Navigation);\n    }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Dispose</span>()</span> => _subscription.Dispose();\n}\n</code></pre>\n<p>The callback runs on the dispatcher (most navigation events\noriginate there) — for a UI overlay that's actually what you want.\nIf the overlay forwards to a background sink instead, marshal off\nthe dispatcher before doing the I/O.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"treating-debugwriteline-as-a-release-diagnostic\">Treating <code>Debug.WriteLine</code> as a release diagnostic<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#treating-debugwriteline-as-a-release-diagnostic\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">try</span> { window.AppWindow.Close(); }\n<span class=\"hljs-keyword\">catch</span> (Exception ex)\n{\n    Debug.WriteLine(<span class=\"hljs-string\">$\"Close failed: <span class=\"hljs-subst\">{ex}</span>\"</span>);  <span class=\"hljs-comment\">// disappears in Release</span>\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Do:</span>\n<span class=\"hljs-keyword\">try</span> { window.AppWindow.Close(); }\n<span class=\"hljs-keyword\">catch</span> (COMException ex) <span class=\"hljs-keyword\">when</span> (ex.HResult <span class=\"hljs-keyword\">is</span> HResults.RPC_E_DISCONNECTED <span class=\"hljs-keyword\">or</span> HResults.E_FAIL)\n{\n    DiagnosticLog.SwallowedError(LogCategory.Hosting, <span class=\"hljs-string\">\"AppWindow.Close\"</span>, ex);\n}\n</code></pre>\n<p>The first form was invisible to every shipped app. The second form\nemits to <code>Microsoft-UI-Reactor</code> under <code>Keywords.Errors</code> at <code>Warning</code>\nin Release (zero allocation when no consumer is attached) <em>and</em>\nmirrors a richer line including <code>ex.Message</code> to <code>Debug.WriteLine</code>\nin Debug builds. The narrow <code>catch</code> filter is the deliberate part:\nspec 044 §6.7.2 calls for <code>catch (COMException) when (ex.HResult is HResults.X or HResults.Y)</code> — never a bare <code>catch (COMException)</code>,\nbecause the bug-class HRESULTs need to keep propagating.</p>\n<h3 id=\"capturing-without-pinning-the-level\">Capturing without pinning the level<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#capturing-without-pinning-the-level\">#</a></h3>\n<pre><code>DOTNET_EventPipeConfig=Microsoft-UI-Reactor\n</code></pre>\n<p>Defaults to <code>Verbose</code> plus all keywords. A typical 30-second session\non a busy app writes hundreds of megabytes — and <code>State</code> keyword\nevents fire once per <code>UseState</code> write, so a state-heavy screen\nbecomes the entire trace. Pin both:</p>\n<pre><code>DOTNET_EventPipeConfig=Microsoft-UI-Reactor:0x1FA0:5\n</code></pre>\n<p><code>0x1FA0</code> is <code>Errors | Hosting | Persistence | Navigation | Intl | Theme</code> — the everything-unsurprising mask. <code>:5</code> is <code>Verbose</code>. The\ntrace shrinks by an order of magnitude.</p>\n<h3 id=\"computing-the-diagnostic-payload-outside-the-isenabled-gate\">Computing the diagnostic payload outside the IsEnabled gate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#computing-the-diagnostic-payload-outside-the-isenabled-gate\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">SwallowedError</span>(<span class=\"hljs-params\">LogCategory category, <span class=\"hljs-built_in\">string</span>? operation, Exception? ex</span>)</span>\n{\n    <span class=\"hljs-comment\">// Cost-of-disabled: when no consumer enables Keywords.Errors at</span>\n    <span class=\"hljs-comment\">// Warning the entire branch is skipped — no enum-to-string, no</span>\n    <span class=\"hljs-comment\">// type-name materialization, no WriteEvent dispatch.</span>\n    <span class=\"hljs-keyword\">if</span> (ReactorEventSource.Log.IsEnabled(EventLevel.Warning, ReactorEventSource.Keywords.Errors))\n    {\n        ReactorEventSource.Log.SwallowedError(\n            category.ToString(),\n            operation ?? <span class=\"hljs-built_in\">string</span>.Empty,\n            ex?.GetType().Name ?? <span class=\"hljs-built_in\">string</span>.Empty);\n    }\n\n    DebugSwallowedError(category, operation, ex);\n}\n</code></pre>\n<p><code>DiagnosticLog.SwallowedError</code> does its <code>category.ToString()</code> and\n<code>ex.GetType().Name</code> work <em>inside</em> the\n<code>ReactorEventSource.Log.IsEnabled(...)</code> gate, not outside. The\ndistinction is the entire point of the \"zero-allocation when no\nconsumer is attached\" guarantee. If a future helper materializes\nthe payload first and gates second, the no-allocation regression\ntest (<code>DisabledKeyword_skips_ReactorEventSource_WriteEvent_payload_marshal</code>)\ncatches it. The companion <code>HResultFailed</code> event has the same shape:</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Event(17, Level = EventLevel.Warning, Keywords = Keywords.Errors,\n    Message = <span class=\"hljs-string\">\"HResult failed (category={category}, op={operation}, hr=0x{hr:X8})\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">HResultFailed</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> category, <span class=\"hljs-built_in\">string</span> operation, <span class=\"hljs-built_in\">int</span> hr</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Warning, Keywords.Errors))\n        WriteEvent(<span class=\"hljs-number\">17</span>, category ?? <span class=\"hljs-built_in\">string</span>.Empty, operation ?? <span class=\"hljs-built_in\">string</span>.Empty, hr);\n}\n</code></pre>\n<h3 id=\"forwarding-exmessage-through-reactortracesubscribe\">Forwarding <code>ex.Message</code> through <code>ReactorTrace.Subscribe</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#forwarding-exmessage-through-reactortracesubscribe\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't — capturing the exception and forwarding its message defeats the strip:</span>\nException? lastEx = <span class=\"hljs-literal\">null</span>;\n<span class=\"hljs-keyword\">try</span> { <span class=\"hljs-comment\">/* ... */</span> }\n<span class=\"hljs-keyword\">catch</span> (Exception caught)\n{\n    lastEx = caught;\n    DiagnosticLog.SwallowedError(LogCategory.Hosting, <span class=\"hljs-string\">\"MyOp\"</span>, caught);\n}\n\nReactorTrace.Subscribe(evt =>\n{\n    <span class=\"hljs-comment\">// BAD: re-injects PII that the ETW payload deliberately excluded.</span>\n    _logger.Warn(evt.EventName + <span class=\"hljs-string\">\": \"</span> + <span class=\"hljs-built_in\">string</span>.Join(<span class=\"hljs-string\">\",\"</span>, evt.Payload) + <span class=\"hljs-string\">\" \"</span> + lastEx?.Message);\n});\n</code></pre>\n<p>The framework already stripped <code>ex.Message</code> from the payload —\nre-adding it from a captured local is exactly the PII leak the strip\nprevented. If a sink needs the message, log it inside the <code>catch</code>\nblock (where the exception is in scope and the sink's ACL applies),\nnot at the <code>ReactorTrace.Subscribe</code> boundary.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong><code>reactor.logs source=event</code> is the fastest read.</strong> Inside a\ndevtools session, calling the MCP tool returns the last N events\ninstantly without spinning up a <code>dotnet-trace</code> capture. Use the\nenv-var route when you need to hand a file to someone else; use the\nMCP tool when you're sitting in front of the running app.</p>\n<p><strong>The keyword mask is the audience pre-filter.</strong> Subscribing on\n<code>Keywords.Errors</code> alone is dramatically cheaper than <code>(-1)</code> because\nthe framework's hot reconcile / render paths drop their <code>IsEnabled</code>\ncheck immediately. A long-lived broad subscription raises the cost\nof every hot-path call site on the framework for as long as it\nlives.</p>\n<p><strong>HR fields are <code>0x{X8}</code>-rendered in <code>reactor.logs</code> text.</strong> The MCP\ntool's text rendering recognizes payload field names <code>hr</code>,\n<code>hresult</code>, and <code>hwnd</code> and formats them as 8-digit uppercase hex.\nThis matches the pre-migration <code>Debug.WriteLine</code> shape so existing\nlog greps keep working.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — The emission pipeline, keyword design, and the IsEnabled gate. Read first if you're adding events.</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — The MCP server and <code>logs</code> tool plumbing the diagnostic events flow through.</li>\n<li><strong><a href=\"/persistence/\">Persistence</a></strong> — Where <code>PersistenceRead</code> / <code>PersistenceWrite</code> / <code>PersistenceRejected</code> fire from.</li>\n<li><strong><a href=\"/navigation/\">Navigation</a></strong> — The route lifecycle that emits the Navigation-keyword events.</li>\n</ul>",
      "text": "When Microsoft.UI.Reactor (Reactor) swallows an exception, returns past an HRESULT, or otherwise chooses to continue rather than throw, the framework has historically dropped a Debug.WriteLine and moved on. That worked for the contributor — the message landed in Visual Studio's Output window during a Debug build — but it disappeared in Release, which is the configuration every shipped app runs. Spec 044 fixed that: error and HRESULT diagnostics now route through the Microsoft-UI-Reactor EventSource (release-visible, keyword-gated, zero-allocation when no consumer is listening), and the existing in-process devtools tool ( reactor.logs ) was extended so an MCP agent can read framework events in the same call that returns stdout / stderr / debug output. Diagnostics # This page is about reading Reactor's diagnostics from a real app — not about adding new events. The emission pipeline (keywords, IsEnabled gate, event-id allocation, EventPipe vs ETW transport split) lives in perf-instrumentation.md ; start there if you are extending the provider. The rule # Audience, not severity, decides the channel. Debug.WriteLine exists for the contributor working on the framework itself; the target audience is \"someone reading the Output window in Visual Studio with their checkout open\". ReactorEventSource exists for the app developer, the SRE, and the support engineer; the target audience is \"someone who runs the shipped binary and needs to know why a window failed to open\". Audience Channel Visible in Release? Framework contributor Debug.WriteLine , Debug.Assert No App developer / SRE Microsoft-UI-Reactor EventSource Yes Unreachable code throw new UnreachableException(...) Yes (as a crash) The two channels are complementary, not redundant. A swallowed exception in RenderContext emits both — the typed event for the app developer and a richer Debug.WriteLine mirror (with the exception message) for the contributor running a Debug build. The mirror is [Conditional(\"DEBUG\")] and compiles out in Release. Caveat: Exception messages are PII-shaped and never reach the ETW payload. ex.Message can carry absolute paths, environment values, partial form values, and the bound user data that caused the failure. The typed event payload carries the exception type only ( InvalidOperationException , COMException ); a same-UID dotnet-trace consumer sees the type and nothing else. If you need the message in your own logs, attach an in-process subscriber (see ReactorTrace.Subscribe below) and forward to a sink under your own ACL. What's instrumented # The provider's events split across a small set of keywords; spec 044 adds six subsystem keywords on top of the seven the perf-instrumentation page documents. Pick the bits that match what you're triaging: Keyword Bit Covers Errors 0x20 Generic SwallowedError / HResultFailed , plus RenderError Hosting 0x80 WindowOpened , WindowClosed , WindowDpiChanged , BackdropMaterializationFailed Persistence 0x100 PersistenceRead , PersistenceWrite , PersistenceRejected Navigation 0x200 NavigationRequested , NavigationCompleted , NavigationCancelled , cache hit/miss/evict, transitions, deep-link Intl 0x400 IntlMissingKey Theme 0x800 ThemeApplyFailed Shell 0x1000 JumpList* / ThumbnailToolbar* / Tray* (planned) Combine bits with bitwise-or. The most common capture-everything- unsurprising mask is 0x1FA0 ( Errors | Hosting | Persistence | Navigation | Intl | Theme ) — drops the verbose State and EventDispatch keywords that produce per-state-write spam. Capturing a trace # There are four routes. Pick by where the consumer lives. Environment variables — zero code, file output # For a quick local capture with no app changes, the .NET runtime can write an EventPipe .nettrace file driven entirely by environment variables. This is the right tool when an issue reproduces only in a shipped build, on a different machine, or when you want to hand the trace off to someone else: set DOTNET_EnableEventPipe=1 set DOTNET_EventPipeOutputPath=reactor.nettrace set DOTNET_EventPipeConfig=Microsoft-UI-Reactor:0x1FA0:5 MyApp.exe The third variable is &#x3C;provider>:&#x3C;keywords>:&#x3C;level> . 0x1FA0 is the everything-unsurprising mask above; level 5 is Verbose . Run the app, reproduce the issue, exit cleanly (the runtime flushes the file on shutdown). Open the resulting .nettrace in Visual Studio's Performance Profiler → Events Viewer. dotnet-trace — attach to a running process # dotnet-trace is the cross-platform CLI for EventPipe capture. Useful when the app is already running and you want to scope the window: dotnet-trace collect ^ --process-id &#x3C;pid> ^ --providers Microsoft-UI-Reactor:0x1FA0:5 ^ --output reactor.nettrace Ctrl+C stops the session; the .nettrace lands in the working directory. Same file format as the env-var route — same Events Viewer workflow. Visual Studio Performance Profiler # For a GUI workflow, the Profiler's Events Viewer accepts the same provider:keyword:level format. Diagnostics → Performance Profiler → Events Viewer → Settings → Custom Provider: Microsoft-UI-Reactor:0x1FA0:5 The timeline ties each Reactor event to the CPU sample, GC, and network views, so you can see a NavigationCompleted next to the allocation spike it caused. In-process subscription # When the consumer is the app itself — a custom log sink, a devtools overlay, an in-app diagnostics page — ReactorTrace.Subscribe returns an IDisposable token that fires the callback for each event matching the filter: public static IDisposable Subscribe ( Action&#x3C;ReactorEvent> onEvent, EventLevel level = EventLevel.Verbose, EventKeywords keywords = (EventKeywords )( -1 )) { ArgumentNullException.ThrowIfNull(onEvent); return new Subscription(onEvent, level, keywords); } Multiple concurrent subscribers are supported; each filter is independently active until the token is disposed. The subscriber callback runs on the emission thread (usually the UI dispatcher when the event originates from reconcile / render), so keep the work minimal — the framework wraps the call in try/catch so a buggy sink can't propagate to EventSource.WriteEvent , but the dispatcher is still blocked for the duration. Forward to a queue if your sink does anything expensive. ReactorTrace.Subscribe is not a file-capture API. It exists because in-process consumers (devtools, an ILogger adapter, a custom diagnostics page) need access to the same events the env-var route writes to disk. For a .nettrace file, use one of the three routes above — they cost less and emit a richer format. reactor.logs source=event — MCP integration # The Reactor in-process devtools ( mur devtools ) expose a logs MCP tool that drains the captured Console.Out / Console.Error / Debug.WriteLine ring buffer. Spec 044 extended the tool so the buffer also captures Microsoft-UI-Reactor ETW events, surfaced under a new source=event filter: // Request { \"method\" : \"tools/call\" , \"params\" : { \"name\" : \"logs\" , \"arguments\" : { \"source\" : \"event\" , \"tail\" : 20 , \"level\" : \"Warning\" } } } // Response — each entry now carries eventName / eventId { \"entries\" : [ { \"seq\" : 142 , \"ts\" : \"2026-05-19T17:42:11.330Z\" , \"source\" : \"event\" , \"level\" : \"Warning\" , \"text\" : \"SwallowedError category=Hosting operation=AppWindow.Close exceptionType=COMException\" , \"eventName\" : \"SwallowedError\" , \"eventId\" : 16 } ] , \"nextSeq\" : 143 , \"dropped\" : 0 } Existing clients that don't pass source=event see zero behavior change — the stdout / stderr / debug filters still return their dedicated streams. The eventName and eventId fields are present on every entry but are null for non-event sources, so a client written before spec 044 ignores them safely. HR-style payload fields render in the same 0x{X8} shape the pre-migration Debug.WriteLine sites used ( HResultFailed category=Shell operation=JumpList.Begin hr=0x80004002 ), so log greps that matched the old shape continue to hit. Patterns # Reading a swallow that happened in production # A customer reports a window that won't close cleanly on a specific machine. Capture with the env-var route, filter to the Errors keyword ( 0x20 ) at Warning , and open the trace in Events Viewer. The relevant entries will look like: SwallowedError category=Hosting operation=AppWindow.Close exceptionType=COMException HResultFailed category=Hosting operation=AppWindow.Close hr=0x80004005 The operation label is stable and developer-authored — search the Reactor source for \"AppWindow.Close\" and you land on the DiagnosticLog.SwallowedError call site. The exception type and HR together pin the failure class (in this case, E_FAIL from the WinUI AppWindow lifecycle) without ever leaking the user-visible window title. Wiring ReactorTrace.Subscribe to a per-window debug overlay # A devtools overlay that wants to surface \"navigation event happening now\" doesn't need a file capture — just an in-process subscription: public sealed class NavigationOverlay : IDisposable { private readonly IDisposable _subscription; private readonly Queue&#x3C; string > _ring = new (); public NavigationOverlay () { _subscription = ReactorTrace.Subscribe( evt => { if ((evt.Keywords &#x26; ReactorEventSource.Keywords.Navigation) == 0 ) return ; var line = $\" {evt.EventName} { string .Join( ' ' , Enumerable.Range( 0 , evt.Payload.Count) .Select(i => $\" {evt.PayloadNames[i]} = {evt.Payload[i]} \" ))} \" ; lock (_ring) { _ring.Enqueue(line); while (_ring.Count > 50 ) _ring.Dequeue(); } }, level: EventLevel.Verbose, keywords: ReactorEventSource.Keywords.Navigation); } public void Dispose () => _subscription.Dispose(); } The callback runs on the dispatcher (most navigation events originate there) — for a UI overlay that's actually what you want. If the overlay forwards to a background sink instead, marshal off the dispatcher before doing the I/O. Common Mistakes # Treating Debug.WriteLine as a release diagnostic # // Don't: try { window.AppWindow.Close(); } catch (Exception ex) { Debug.WriteLine( $\"Close failed: {ex} \" ); // disappears in Release } // Do: try { window.AppWindow.Close(); } catch (COMException ex) when (ex.HResult is HResults.RPC_E_DISCONNECTED or HResults.E_FAIL) { DiagnosticLog.SwallowedError(LogCategory.Hosting, \"AppWindow.Close\" , ex); } The first form was invisible to every shipped app. The second form emits to Microsoft-UI-Reactor under Keywords.Errors at Warning in Release (zero allocation when no consumer is attached) and mirrors a richer line including ex.Message to Debug.WriteLine in Debug builds. The narrow catch filter is the deliberate part: spec 044 §6.7.2 calls for catch (COMException) when (ex.HResult is HResults.X or HResults.Y) — never a bare catch (COMException) , because the bug-class HRESULTs need to keep propagating. Capturing without pinning the level # DOTNET_EventPipeConfig=Microsoft-UI-Reactor Defaults to Verbose plus all keywords. A typical 30-second session on a busy app writes hundreds of megabytes — and State keyword events fire once per UseState write, so a state-heavy screen becomes the entire trace. Pin both: DOTNET_EventPipeConfig=Microsoft-UI-Reactor:0x1FA0:5 0x1FA0 is Errors | Hosting | Persistence | Navigation | Intl | Theme — the everything-unsurprising mask. :5 is Verbose . The trace shrinks by an order of magnitude. Computing the diagnostic payload outside the IsEnabled gate # public static void SwallowedError ( LogCategory category, string ? operation, Exception? ex ) { // Cost-of-disabled: when no consumer enables Keywords.Errors at // Warning the entire branch is skipped — no enum-to-string, no // type-name materialization, no WriteEvent dispatch. if (ReactorEventSource.Log.IsEnabled(EventLevel.Warning, ReactorEventSource.Keywords.Errors)) { ReactorEventSource.Log.SwallowedError( category.ToString(), operation ?? string .Empty, ex?.GetType().Name ?? string .Empty); } DebugSwallowedError(category, operation, ex); } DiagnosticLog.SwallowedError does its category.ToString() and ex.GetType().Name work inside the ReactorEventSource.Log.IsEnabled(...) gate, not outside. The distinction is the entire point of the \"zero-allocation when no consumer is attached\" guarantee. If a future helper materializes the payload first and gates second, the no-allocation regression test ( DisabledKeyword_skips_ReactorEventSource_WriteEvent_payload_marshal ) catches it. The companion HResultFailed event has the same shape: [ Event(17, Level = EventLevel.Warning, Keywords = Keywords.Errors, Message = \"HResult failed (category={category}, op={operation}, hr=0x{hr:X8})\" ) ] public void HResultFailed ( string category, string operation, int hr ) { if (IsEnabled(EventLevel.Warning, Keywords.Errors)) WriteEvent( 17 , category ?? string .Empty, operation ?? string .Empty, hr); } Forwarding ex.Message through ReactorTrace.Subscribe # // Don't — capturing the exception and forwarding its message defeats the strip: Exception? lastEx = null ; try { /* ... */ } catch (Exception caught) { lastEx = caught; DiagnosticLog.SwallowedError(LogCategory.Hosting, \"MyOp\" , caught); } ReactorTrace.Subscribe(evt => { // BAD: re-injects PII that the ETW payload deliberately excluded. _logger.Warn(evt.EventName + \": \" + string .Join( \",\" , evt.Payload) + \" \" + lastEx?.Message); }); The framework already stripped ex.Message from the payload — re-adding it from a captured local is exactly the PII leak the strip prevented. If a sink needs the message, log it inside the catch block (where the exception is in scope and the sink's ACL applies), not at the ReactorTrace.Subscribe boundary. Tips # reactor.logs source=event is the fastest read. Inside a devtools session, calling the MCP tool returns the last N events instantly without spinning up a dotnet-trace capture. Use the env-var route when you need to hand a file to someone else; use the MCP tool when you're sitting in front of the running app. The keyword mask is the audience pre-filter. Subscribing on Keywords.Errors alone is dramatically cheaper than (-1) because the framework's hot reconcile / render paths drop their IsEnabled check immediately. A long-lived broad subscription raises the cost of every hot-path call site on the framework for as long as it lives. HR fields are 0x{X8} -rendered in reactor.logs text. The MCP tool's text rendering recognizes payload field names hr , hresult , and hwnd and formats them as 8-digit uppercase hex. This matches the pre-migration Debug.WriteLine shape so existing log greps keep working. Next Steps # Perf Instrumentation — The emission pipeline, keyword design, and the IsEnabled gate. Read first if you're adding events. DevTools Internals — The MCP server and logs tool plumbing the diagnostic events flow through. Persistence — Where PersistenceRead / PersistenceWrite / PersistenceRejected fire from. Navigation — The route lifecycle that emits the Navigation-keyword events.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/diagnostics.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/diagnostics.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-rule",
          "text": "The rule"
        },
        {
          "depth": 2,
          "id": "whats-instrumented",
          "text": "What's instrumented"
        },
        {
          "depth": 2,
          "id": "capturing-a-trace",
          "text": "Capturing a trace"
        },
        {
          "depth": 3,
          "id": "environment-variables--zero-code-file-output",
          "text": "Environment variables — zero code, file output"
        },
        {
          "depth": 3,
          "id": "dotnet-trace--attach-to-a-running-process",
          "text": "dotnet-trace — attach to a running process"
        },
        {
          "depth": 3,
          "id": "visual-studio-performance-profiler",
          "text": "Visual Studio Performance Profiler"
        },
        {
          "depth": 3,
          "id": "in-process-subscription",
          "text": "In-process subscription"
        },
        {
          "depth": 2,
          "id": "reactorlogs-sourceevent--mcp-integration",
          "text": "reactor.logs source=event — MCP integration"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "reading-a-swallow-that-happened-in-production",
          "text": "Reading a swallow that happened in production"
        },
        {
          "depth": 3,
          "id": "wiring-reactortracesubscribe-to-a-per-window-debug-overlay",
          "text": "Wiring ReactorTrace.Subscribe to a per-window debug overlay"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "treating-debugwriteline-as-a-release-diagnostic",
          "text": "Treating Debug.WriteLine as a release diagnostic"
        },
        {
          "depth": 3,
          "id": "capturing-without-pinning-the-level",
          "text": "Capturing without pinning the level"
        },
        {
          "depth": 3,
          "id": "computing-the-diagnostic-payload-outside-the-isenabled-gate",
          "text": "Computing the diagnostic payload outside the IsEnabled gate"
        },
        {
          "depth": 3,
          "id": "forwarding-exmessage-through-reactortracesubscribe",
          "text": "Forwarding ex.Message through ReactorTrace.Subscribe"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "WinForms Interop",
      "path": "winforms-interop.md",
      "slug": "/winforms-interop/",
      "html": "<blockquote>\n<p><strong>WinUI reference:</strong> For the full property surface and design guidance, see <a href=\"https://learn.microsoft.com/en-us/windows/apps/desktop/modernize/xaml-islands\">Xaml Islands</a>.</p>\n</blockquote>\n<p><code>XamlIslandControl</code> is a single WinForms control that hosts a complete\nMicrosoft.UI.Reactor (Reactor) <a href=\"/components/\">component</a> tree. The rest of the form stays\nWinForms — labels, panels, menus, the existing message loop — and\nyour Reactor subtree renders into a <code>DesktopWindowXamlSource</code> rooted\nat the control's client area. Data flow across the boundary is\nexplicit: WinForms event handlers call into Reactor by setting a\ncomponent's props or calling a method on a <code>ReactorHostControl</code>\nhandle; Reactor calls back into WinForms by invoking a delegate\nyour form gave it at construction. There is no implicit two-way\nbinding, no shared <code>DataContext</code>, and no XAML loader — the same\ndeclarative model that holds inside a pure Reactor window applies,\njust nested inside <code>System.Windows.Forms.Form</code>. The threading model\nis the load-bearing detail: WinForms owns the message pump and\nWinUI runs against a <code>DispatcherQueue</code> the bootstrap installs on the\nsame thread, but the two are <em>not</em> synchronized for arbitrary\nbackground-thread callbacks. The caveat below names the specific\nfailure mode. This page covers bootstrap, control insertion, data\nflow patterns, threading constraints, designer integration,\nkeyboard and accessibility bridging, and the WPF sibling\n<a href=\"/wpf-interop/\"><code>wpf-interop</code></a> for projects mixing all three.</p>\n<h1 id=\"winforms-interop\">WinForms Interop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#winforms-interop\">#</a></h1>\n<p>Reactor components can run inside WinForms applications using XAML Islands.\nThe <code>Reactor.Interop.WinForms</code> package provides <code>XamlIslandControl</code> — a\nstandard WinForms control that hosts a Reactor component tree with full\nkeyboard, accessibility, and theming support.</p>\n<h2 id=\"bootstrap\">Bootstrap<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#bootstrap\">#</a></h2>\n<p>Every WinForms + Reactor app starts with <code>XamlIslandBootstrap.Run()</code>. This\ninitializes the WinAppSDK/WinUI runtime and then calls your callback to\nshow the WinForms UI:</p>\n<pre><code class=\"hljs language-csharp\">XamlIslandBootstrap.Run(() =>\n{\n    <span class=\"hljs-keyword\">var</span> form = <span class=\"hljs-keyword\">new</span> SWF.Form\n    {\n        Text = <span class=\"hljs-string\">\"My WinForms + Reactor App\"</span>,\n        Width = <span class=\"hljs-number\">800</span>,\n        Height = <span class=\"hljs-number\">500</span>\n    };\n\n    <span class=\"hljs-keyword\">var</span> island = <span class=\"hljs-keyword\">new</span> XamlIslandControl\n    {\n        ComponentType = <span class=\"hljs-keyword\">typeof</span>(WinFormsHostDemo),\n        Dock = SWF.DockStyle.Fill\n    };\n\n    form.Controls.Add(island);\n    form.Show();\n});\n</code></pre>\n<p>The bootstrap handles:</p>\n<ul>\n<li>DispatcherQueue creation for WinUI</li>\n<li>Theme resource loading</li>\n<li>Keyboard message filtering (so WinUI controls receive key events)</li>\n<li>DPI awareness configuration</li>\n<li>Clean shutdown when <code>Application.Exit()</code> is called</li>\n</ul>\n<p>Call <code>XamlIslandBootstrap.Run()</code> once, at the very start of your\napplication. WinForms owns the message loop — Reactor runs inside it.</p>\n<h2 id=\"xamlislandcontrol\">XamlIslandControl<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#xamlislandcontrol\">#</a></h2>\n<p><code>XamlIslandControl</code> is a <code>System.Windows.Forms.Control</code> that wraps a\n<code>DesktopWindowXamlSource</code>. Add it to any WinForms form or panel:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">WinFormsHostDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-comment\">// This component is hosted via XamlIslandControl.ComponentType</span>\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Heading(<span class=\"hljs-string\">\"Reactor in WinForms\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>).FontSize(<span class=\"hljs-number\">24</span>),\n            Button(<span class=\"hljs-string\">\"+1\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>).Background(SolidBackground);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/winforms-interop/host-architecture.svg\" alt=\"A WinForms form hosts a XamlIslandControl that stands on DesktopWindowXamlSource and a ReactorHostControl, which owns the Reactor element tree.\"></p>\n<p>There are three ways to set the content:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Property</th><th>Use case</th></tr></thead><tbody><tr><td><code>ComponentType</code></td><td>Set a Reactor <code>Component</code> type — works in the designer</td></tr><tr><td><code>ContentFactory</code></td><td>Provide a factory function for custom initialization</td></tr><tr><td><code>XamlContent</code></td><td>Set raw WinUI <code>UIElement</code> content directly</td></tr></tbody></table>\n<p><code>ComponentType</code> is the simplest path: set it to <code>typeof(MyComponent)</code> and\nthe control creates and hosts the component automatically.</p>\n<h2 id=\"designer-support\">Designer Support<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#designer-support\">#</a></h2>\n<p><code>XamlIslandControl</code> has full WinForms designer support. Drag it onto a\nform and set <code>ComponentType</code> from the Properties grid — the dropdown lists\nall concrete <code>Component</code> subclasses with parameterless constructors:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// In the form's Designer.cs file:</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// this.reactorIsland = new XamlIslandControl();</span>\n<span class=\"hljs-comment\">// this.reactorIsland.ComponentType = typeof(DashboardComponent);</span>\n<span class=\"hljs-comment\">// this.reactorIsland.Dock = DockStyle.Fill;</span>\n<span class=\"hljs-comment\">// this.panel1.Controls.Add(this.reactorIsland);</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// The Properties grid shows a dropdown of all Component subclasses</span>\n<span class=\"hljs-comment\">// with parameterless constructors. Select your component and the</span>\n<span class=\"hljs-comment\">// designer serializes it as typeof(DashboardComponent).</span>\n</code></pre>\n<p>At design time, the control renders a placeholder with a border showing the\ncomponent name. No WinUI objects are created until the app runs, so the\ndesigner stays lightweight.</p>\n<p>The <code>ReactorComponentTypeConverter</code> powers this integration. It converts\nbetween <code>Type</code> objects and type name strings, and enumerates available\ncomponents for the dropdown.</p>\n<h2 id=\"keyboard-and-tab-navigation\">Keyboard and Tab Navigation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyboard-and-tab-navigation\">#</a></h2>\n<p>XAML Islands need explicit keyboard bridging to interoperate with WinForms\ntab order. <code>XamlIslandControl</code> handles this automatically:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">KeyboardDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (text, setText) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-comment\">// Tab moves focus into this Reactor tree from WinForms controls.</span>\n        <span class=\"hljs-comment\">// Tab/Shift+Tab cycles through Reactor controls normally.</span>\n        <span class=\"hljs-comment\">// Tab out of the last Reactor control returns focus to WinForms.</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBox(text, setText, placeholderText: <span class=\"hljs-string\">\"Type here...\"</span>)\n                .TabIndex(<span class=\"hljs-number\">0</span>),\n            Button(<span class=\"hljs-string\">\"Submit\"</span>, () => { })\n                .TabIndex(<span class=\"hljs-number\">1</span>)\n                .AccessKey(<span class=\"hljs-string\">\"S\"</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>).Background(SolidBackground);\n    }\n}\n</code></pre>\n<ul>\n<li><strong>Tab/Shift+Tab</strong> moves focus between WinForms controls and into/out of\nthe Reactor component tree</li>\n<li><strong>Arrow keys, Enter, Escape</strong> are routed to WinUI controls inside the\nisland</li>\n<li><strong>Alt+key shortcuts</strong> work for both WinForms menus and Reactor <code>AccessKey</code>\nmodifiers</li>\n</ul>\n<p>The bootstrap's <code>ContentPreTranslateMessage</code> hook ensures keyboard messages\nreach WinUI controls. No additional configuration is needed.</p>\n<h2 id=\"accessibility\">Accessibility<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#accessibility\">#</a></h2>\n<p>Screen readers see Reactor components inside XAML Islands as part of the\nWinForms automation tree. The interop layer bridges:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">AccessibleIslandComponent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (name, setName) = UseState(<span class=\"hljs-string\">\"\"</span>);\n\n        <span class=\"hljs-comment\">// All accessibility modifiers work inside XAML Islands</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Heading(<span class=\"hljs-string\">\"Registration\"</span>)\n                .HeadingLevel(AutomationHeadingLevel.Level1),\n            TextBox(name, setName, header: <span class=\"hljs-string\">\"Full Name\"</span>)\n                .AutomationName(<span class=\"hljs-string\">\"Full name\"</span>)\n                .Required()\n                .TabIndex(<span class=\"hljs-number\">0</span>),\n            Button(<span class=\"hljs-string\">\"Register\"</span>, () => { })\n                .AutomationName(<span class=\"hljs-string\">\"Submit registration\"</span>)\n                .TabIndex(<span class=\"hljs-number\">1</span>)\n        ).Padding(<span class=\"hljs-number\">24</span>)\n         .Landmark(AutomationLandmarkType.Form)\n         .Background(SolidBackground);\n    }\n}\n</code></pre>\n<ul>\n<li><strong>AutomationName</strong>, <strong>HeadingLevel</strong>, and other <a href=\"/accessibility/\">accessibility</a>\nmodifiers work identically to a pure Reactor app</li>\n<li><a href=\"/reference/hooks/UseAnnounce/\"><code>UseAnnounce</code></a> live regions are forwarded through the island boundary</li>\n<li><strong>Tab order</strong> flows correctly between WinForms and Reactor controls</li>\n<li><strong>Focus trapping</strong> via <code>UseFocusTrap</code> works within the Reactor subtree</li>\n</ul>\n<p>See <a href=\"/accessibility/\">Accessibility</a> for the full modifier reference.</p>\n<h2 id=\"data-flow-across-the-boundary\">Data flow across the boundary<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#data-flow-across-the-boundary\">#</a></h2>\n<p>Data crossing the WinForms ⇄ Reactor boundary moves through three\nmechanisms — pick the one that matches direction and frequency:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Direction</th><th>Mechanism</th><th>Use when</th></tr></thead><tbody><tr><td>WinForms → Reactor (one-shot)</td><td><code>XamlIslandControl.ComponentType = typeof(MyComponent)</code> and recreate</td><td>Setting initial content once per form. Re-assigning rebuilds the subtree.</td></tr><tr><td>WinForms → Reactor (live)</td><td><code>ContentFactory</code> returning a <code>ReactorHostControl</code>; keep a reference to the host's root component handle</td><td>The host is constructed once; you call methods on the component from event handlers.</td></tr><tr><td>WinForms → Reactor (observable VM)</td><td>Pass an existing INPC view model into the component constructor; the component calls <a href=\"/advanced/\"><code>UseObservable</code></a> to subscribe</td><td>You already have a WinForms-shaped view model and want Reactor to re-render on its change events.</td></tr><tr><td>Reactor → WinForms</td><td>Inject a <code>Action&#x3C;T></code> callback into the component's props; component calls it from event handlers</td><td>Whenever the Reactor subtree needs to push a value back to the host.</td></tr></tbody></table>\n<p>The flow is explicit and one-directional unless you wire\n<a href=\"/advanced/\"><code>UseObservable</code></a> over a WinForms-side <code>INotifyPropertyChanged</code>\nsource. There is no implicit <code>DataContext</code> and no global event bus —\neach crossing is a method call you write.</p>\n<h2 id=\"threading-constraints\">Threading constraints<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#threading-constraints\">#</a></h2>\n<p>The bootstrap creates a <code>DispatcherQueue</code> on the WinForms UI thread\n(the thread <code>Application.Run</code> is dispatching on) and pins WinUI to\nthat thread. WinForms message-pump callbacks (<code>Click</code>, <code>Load</code>,\n<code>Paint</code>) are already on the right thread — calling\n<code>element.SetValue(...)</code> or invoking a setter from inside a <code>Click</code>\nhandler is safe and the auto-marshal path is a no-op.</p>\n<p>The danger is background threads. <code>BackgroundWorker.ProgressChanged</code>,\n<code>Task.Run(...)</code> continuations without <code>ConfigureAwait</code>, and\n<code>HttpClient</code> async callbacks land on the thread pool by default. If\nyour handler then tries to set a Reactor <code>UseState</code> directly,\nReactor's auto-marshal in <code>RenderContext.SetState</code> queues the write\nonto <code>ReactorApp.UIDispatcher</code> — which works inside a pure Reactor\nwindow but, inside a WinForms host, is only the right dispatcher if\nyou bridged it explicitly. The safe shape is to invoke\n<code>XamlIslandControl.Dispatcher.TryEnqueue(...)</code> from background\ncallbacks before touching Reactor state. The caveat below covers the\nspecific exception you'll see if you forget.</p>\n<blockquote>\n<p><strong>Caveat:</strong> WinForms <code>Application.Idle</code> and the WinUI <code>DispatcherQueue</code> are NOT\nsynchronized. Calling <code>Element.SetValue(...)</code> from a WinForms\n<code>Button.Click</code> handler is fine — the click already runs on the WinForms\nUI thread, which is the same thread the WinUI dispatcher targets, and\nReactor's auto-marshal sees no thread change. But calling it from a\n<code>BackgroundWorker.ProgressChanged</code> handler raised by a worker thread\nwill throw <code>COMException: 0x8001010E (RPC_E_WRONG_THREAD)</code> from the\nunderlying <code>IXamlObject</code> setter — Reactor's auto-marshal looks at\n<code>ReactorApp.UIDispatcher</code> which, inside a WinForms host, may not have\nbeen initialized at all (interop bootstrap installs the dispatcher on\nthe host control, not the global app). The fix is to marshal manually\nfirst: <code>xamlIsland.Dispatcher.TryEnqueue(() => setState(value))</code> from\nthe background handler. The corresponding analyzer is\n<code>REACTOR_INTEROP_001</code> (\"Reactor state setter called from non-UI thread\nin WinForms host\") — Warning-level — but it only fires when the\nanalyzer can prove the call site is on a non-UI thread, which it\ncannot do across <code>Task.Run</code> lambda boundaries.</p>\n</blockquote>\n<h2 id=\"background-and-sizing\">Background and Sizing<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#background-and-sizing\">#</a></h2>\n<p>XAML Islands do not provide an implicit background or stretch behavior.\nReactor components hosted in WinForms must manage their own background:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">BackgroundDemo</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n\n        <span class=\"hljs-comment\">// Always set an explicit background on root content.</span>\n        <span class=\"hljs-comment\">// XAML Islands have no default background — without this,</span>\n        <span class=\"hljs-comment\">// content renders on a transparent surface.</span>\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            TextBlock(<span class=\"hljs-string\">\"Theme-aware background\"</span>).Bold(),\n            TextBlock(<span class=\"hljs-string\">$\"Count: <span class=\"hljs-subst\">{count}</span>\"</span>),\n            Button(<span class=\"hljs-string\">\"Increment\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>).Background(SolidBackground);\n    }\n}\n</code></pre>\n<p>Wrap your component's root in <code>Grid(...)</code> with <code>.Background(...)</code> to fill\nthe island area. Without this, the component renders on a transparent\nbackground and may not stretch to fill the WinForms control bounds.</p>\n<h2 id=\"advanced-contentfactory\">Advanced: ContentFactory<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#advanced-contentfactory\">#</a></h2>\n<p>For components that need constructor parameters or custom host configuration,\nuse <code>ContentFactory</code> instead of <code>ComponentType</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Use ContentFactory for components needing parameters:</span>\n<span class=\"hljs-comment\">//</span>\n<span class=\"hljs-comment\">// var island = new XamlIslandControl</span>\n<span class=\"hljs-comment\">// {</span>\n<span class=\"hljs-comment\">//     ContentFactory = () =></span>\n<span class=\"hljs-comment\">//     {</span>\n<span class=\"hljs-comment\">//         var host = new ReactorHostControl();</span>\n<span class=\"hljs-comment\">//         host.SetComponent&#x3C;ConfigurableComponent>();</span>\n<span class=\"hljs-comment\">//         return host;</span>\n<span class=\"hljs-comment\">//     }</span>\n<span class=\"hljs-comment\">// };</span>\n\n<span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ConfigurableComponent</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">12</span>,\n            Heading(<span class=\"hljs-string\">\"Dashboard\"</span>),\n            TextBlock(<span class=\"hljs-string\">$\"Value: <span class=\"hljs-subst\">{count}</span>\"</span>),\n            Button(<span class=\"hljs-string\">\"+1\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>))\n        ).Padding(<span class=\"hljs-number\">24</span>).Background(SolidBackground);\n    }\n}\n</code></pre>\n<p>The factory function runs on the UI thread after the XAML Island is ready.\nReturn any <code>UIElement</code> — typically a <code>ReactorHostControl</code> wrapping your\ncomponent.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"bridge-a-winforms-vm-into-a-reactor-component-via-useobservable\">Bridge a WinForms VM into a Reactor component via <code>UseObservable</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#bridge-a-winforms-vm-into-a-reactor-component-via-useobservable\">#</a></h3>\n<p>When an existing WinForms app already has a view model implementing\n<code>INotifyPropertyChanged</code>, the migration path is to host a Reactor\ncomponent that subscribes to the same VM rather than rewriting\nstate into hooks. The Reactor component receives the VM via\nconstructor props and calls <a href=\"/advanced/\"><code>UseObservable</code></a> to bind\nthe subscription to its lifetime; the WinForms form keeps the same\nVM reference and reacts to its property changes through the same\nINPC pipeline it always used. Result: incremental adoption, one\nform at a time, no big-bang rewrite.</p>\n<h3 id=\"share-a-single-accessibility-tree-across-island-and-host\">Share a single accessibility tree across island and host<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#share-a-single-accessibility-tree-across-island-and-host\">#</a></h3>\n<p>The bootstrap wires the WinForms accessibility tree into the\n<a href=\"/accessibility/\"><code>UIAutomation</code></a> tree rooted at the island. Screen\nreaders see one logical app, not two: the form's <code>Label</code> controls,\nthe island's Reactor <code>Text</code> elements, and the rest of the form\nappear in a single Narrator pass. To make this work, set\n<code>AutomationName</code> on the WinForms surrounding chrome (labels,\ngroup boxes, the form itself) — without it, Narrator reads \"pane\"\nfor the WinForms half and the experience is disjoint.</p>\n<h3 id=\"round-trip-focus-on-tab\">Round-trip focus on Tab<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#round-trip-focus-on-tab\">#</a></h3>\n<p>Tab from a WinForms <code>TextBox</code> should enter the Reactor subtree at\nits first focusable element, Tab again should advance within Reactor,\nand Tab off the last Reactor element should return to the next\nWinForms control. <code>XamlIslandControl</code> handles this via WinForms\n<code>Control.PreviewKeyDown</code> and the bootstrap's <code>PreTranslateMessage</code>\nfilter; no extra wiring is needed for the simple case. For complex\ntab orders that mix island and host explicitly, set <code>TabIndex</code> on\nboth sides — WinForms <code>TabIndex</code> and Reactor <code>.TabIndex(n)</code> use the\nsame integer space.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"not-calling-xamlislandbootstraprun-first\">Not calling <code>XamlIslandBootstrap.Run()</code> first<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#not-calling-xamlislandbootstraprun-first\">#</a></h3>\n<p>The bootstrap initializes the WinAppSDK, installs the dispatcher,\nand registers the keyboard message filter. Without it,\n<code>XamlIslandControl.Load</code> either no-ops (the control renders empty)\nor throws <code>InvalidOperationException: 'WindowsAppSDK was not bootstrapped on this thread.'</code> — depending on which property\ntriggers the lazy initialization first. The fix is the canonical\nentry point shown in the bootstrap snippet above: every WinForms +\nReactor <code>Main</code> method opens with <code>XamlIslandBootstrap.Run(() => { … })</code>.</p>\n<h3 id=\"placing-the-island-inside-a-panel-with-doublebuffered--true\">Placing the island inside a <code>Panel</code> with <code>DoubleBuffered = true</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#placing-the-island-inside-a-panel-with-doublebuffered--true\">#</a></h3>\n<p>WinForms' double-buffering composites child controls into an\noff-screen bitmap on every paint. XAML Islands render via DirectX\nto a separate composition surface that doesn't participate in\nGDI double-buffering; the result is severe flicker, white flashes\nduring resize, and intermittent stale-frame artifacts. The fix is\nto remove <code>DoubleBuffered = true</code> from any container <em>above</em> the\nisland. Set it only on sibling WinForms controls that don't\ncontain an island.</p>\n<h3 id=\"ignoring-accessibility-on-the-winforms-surrounding-chrome\">Ignoring accessibility on the WinForms surrounding chrome<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#ignoring-accessibility-on-the-winforms-surrounding-chrome\">#</a></h3>\n<p>A Reactor subtree using <a href=\"/accessibility/\"><code>AutomationName</code></a> on\nevery Element is fine on its own. Drop it into a WinForms form\nwhose <code>Label</code> controls have no <code>AccessibleName</code> set and whose\n<code>GroupBox</code> headers are empty, and Narrator reads \"pane, pane,\npane\" before reaching the island. The full-app a11y story\nrequires both halves. Run the <a href=\"/accessibility/\">a11y scanner</a>\nfrom inside the island and <code>axe-windows</code> / Accessibility Insights\nacross the host form.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Call <code>XamlIslandBootstrap.Run()</code> first.</strong> Before any WinForms forms are\nshown. The bootstrap must initialize the WinUI runtime before any\n<code>XamlIslandControl</code> instances are created.</p>\n<p><strong>Use <code>ComponentType</code> for simple cases.</strong> It is designer-friendly and\nhandles lifecycle automatically. Reserve <code>ContentFactory</code> for components\nthat need parameters or custom host setup.</p>\n<p><strong>Set an explicit background on root components.</strong> XAML Islands have no\ndefault background. Use <code>.Background(SolidBackgroundFillColorBase)</code> for\ntheme-aware backgrounds.</p>\n<p><strong>Dock or anchor the island control.</strong> <code>XamlIslandControl</code> supports\nstandard WinForms layout: <code>Dock = DockStyle.Fill</code> to fill a panel,\nor anchoring for proportional resize.</p>\n<p><strong>Test Tab navigation end-to-end.</strong> Tab between WinForms controls and Reactor\ncontrols to verify focus flows correctly. The bridging handles most cases\nautomatically, but complex tab orders may need <code>.TabIndex()</code> hints.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/wpf-interop/\">WPF Interop</a></strong> — the WPF sibling: hosting Reactor in <code>System.Windows.Window</code> apps with the same XAML Islands surface</li>\n<li><strong><a href=\"/windows/\">Windows</a></strong> — next topic: opening, finding, and managing top-level windows + tray icons</li>\n<li><strong><a href=\"/data-system/\">Data System</a></strong> — previous topic: DataGrid with sort, filter, and editing</li>\n<li><strong><a href=\"/\">Reactor</a></strong> — back to the index: overview of the framework and full topic list</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — accessibility modifiers and screen reader support</li>\n<li><strong><a href=\"/advanced/\">Advanced Patterns</a></strong> — error boundaries, Memo, and escape hatches</li>\n<li><strong><a href=\"/components/\">Components</a></strong> — building reusable components to host in WinForms</li>\n</ul>",
      "text": "WinUI reference: For the full property surface and design guidance, see Xaml Islands . XamlIslandControl is a single WinForms control that hosts a complete Microsoft.UI.Reactor (Reactor) component tree. The rest of the form stays WinForms — labels, panels, menus, the existing message loop — and your Reactor subtree renders into a DesktopWindowXamlSource rooted at the control's client area. Data flow across the boundary is explicit: WinForms event handlers call into Reactor by setting a component's props or calling a method on a ReactorHostControl handle; Reactor calls back into WinForms by invoking a delegate your form gave it at construction. There is no implicit two-way binding, no shared DataContext , and no XAML loader — the same declarative model that holds inside a pure Reactor window applies, just nested inside System.Windows.Forms.Form . The threading model is the load-bearing detail: WinForms owns the message pump and WinUI runs against a DispatcherQueue the bootstrap installs on the same thread, but the two are not synchronized for arbitrary background-thread callbacks. The caveat below names the specific failure mode. This page covers bootstrap, control insertion, data flow patterns, threading constraints, designer integration, keyboard and accessibility bridging, and the WPF sibling wpf-interop for projects mixing all three. WinForms Interop # Reactor components can run inside WinForms applications using XAML Islands. The Reactor.Interop.WinForms package provides XamlIslandControl — a standard WinForms control that hosts a Reactor component tree with full keyboard, accessibility, and theming support. Bootstrap # Every WinForms + Reactor app starts with XamlIslandBootstrap.Run() . This initializes the WinAppSDK/WinUI runtime and then calls your callback to show the WinForms UI: XamlIslandBootstrap.Run(() => { var form = new SWF.Form { Text = \"My WinForms + Reactor App\" , Width = 800 , Height = 500 }; var island = new XamlIslandControl { ComponentType = typeof (WinFormsHostDemo), Dock = SWF.DockStyle.Fill }; form.Controls.Add(island); form.Show(); }); The bootstrap handles: DispatcherQueue creation for WinUI Theme resource loading Keyboard message filtering (so WinUI controls receive key events) DPI awareness configuration Clean shutdown when Application.Exit() is called Call XamlIslandBootstrap.Run() once, at the very start of your application. WinForms owns the message loop — Reactor runs inside it. XamlIslandControl # XamlIslandControl is a System.Windows.Forms.Control that wraps a DesktopWindowXamlSource . Add it to any WinForms form or panel: class WinFormsHostDemo : Component { public override Element Render () { // This component is hosted via XamlIslandControl.ComponentType var (count, setCount) = UseState( 0 ); return VStack( 12 , Heading( \"Reactor in WinForms\" ), TextBlock( $\"Count: {count} \" ).FontSize( 24 ), Button( \"+1\" , () => setCount(count + 1 )) ).Padding( 24 ).Background(SolidBackground); } } There are three ways to set the content: Property Use case ComponentType Set a Reactor Component type — works in the designer ContentFactory Provide a factory function for custom initialization XamlContent Set raw WinUI UIElement content directly ComponentType is the simplest path: set it to typeof(MyComponent) and the control creates and hosts the component automatically. Designer Support # XamlIslandControl has full WinForms designer support. Drag it onto a form and set ComponentType from the Properties grid — the dropdown lists all concrete Component subclasses with parameterless constructors: // In the form's Designer.cs file: // // this.reactorIsland = new XamlIslandControl(); // this.reactorIsland.ComponentType = typeof(DashboardComponent); // this.reactorIsland.Dock = DockStyle.Fill; // this.panel1.Controls.Add(this.reactorIsland); // // The Properties grid shows a dropdown of all Component subclasses // with parameterless constructors. Select your component and the // designer serializes it as typeof(DashboardComponent). At design time, the control renders a placeholder with a border showing the component name. No WinUI objects are created until the app runs, so the designer stays lightweight. The ReactorComponentTypeConverter powers this integration. It converts between Type objects and type name strings, and enumerates available components for the dropdown. Keyboard and Tab Navigation # XAML Islands need explicit keyboard bridging to interoperate with WinForms tab order. XamlIslandControl handles this automatically: class KeyboardDemo : Component { public override Element Render () { var (text, setText) = UseState( \"\" ); // Tab moves focus into this Reactor tree from WinForms controls. // Tab/Shift+Tab cycles through Reactor controls normally. // Tab out of the last Reactor control returns focus to WinForms. return VStack( 12 , TextBox(text, setText, placeholderText: \"Type here...\" ) .TabIndex( 0 ), Button( \"Submit\" , () => { }) .TabIndex( 1 ) .AccessKey( \"S\" ) ).Padding( 24 ).Background(SolidBackground); } } Tab/Shift+Tab moves focus between WinForms controls and into/out of the Reactor component tree Arrow keys, Enter, Escape are routed to WinUI controls inside the island Alt+key shortcuts work for both WinForms menus and Reactor AccessKey modifiers The bootstrap's ContentPreTranslateMessage hook ensures keyboard messages reach WinUI controls. No additional configuration is needed. Accessibility # Screen readers see Reactor components inside XAML Islands as part of the WinForms automation tree. The interop layer bridges: class AccessibleIslandComponent : Component { public override Element Render () { var (name, setName) = UseState( \"\" ); // All accessibility modifiers work inside XAML Islands return VStack( 12 , Heading( \"Registration\" ) .HeadingLevel(AutomationHeadingLevel.Level1), TextBox(name, setName, header: \"Full Name\" ) .AutomationName( \"Full name\" ) .Required() .TabIndex( 0 ), Button( \"Register\" , () => { }) .AutomationName( \"Submit registration\" ) .TabIndex( 1 ) ).Padding( 24 ) .Landmark(AutomationLandmarkType.Form) .Background(SolidBackground); } } AutomationName , HeadingLevel , and other accessibility modifiers work identically to a pure Reactor app UseAnnounce live regions are forwarded through the island boundary Tab order flows correctly between WinForms and Reactor controls Focus trapping via UseFocusTrap works within the Reactor subtree See Accessibility for the full modifier reference. Data flow across the boundary # Data crossing the WinForms ⇄ Reactor boundary moves through three mechanisms — pick the one that matches direction and frequency: Direction Mechanism Use when WinForms → Reactor (one-shot) XamlIslandControl.ComponentType = typeof(MyComponent) and recreate Setting initial content once per form. Re-assigning rebuilds the subtree. WinForms → Reactor (live) ContentFactory returning a ReactorHostControl ; keep a reference to the host's root component handle The host is constructed once; you call methods on the component from event handlers. WinForms → Reactor (observable VM) Pass an existing INPC view model into the component constructor; the component calls UseObservable to subscribe You already have a WinForms-shaped view model and want Reactor to re-render on its change events. Reactor → WinForms Inject a Action&#x3C;T> callback into the component's props; component calls it from event handlers Whenever the Reactor subtree needs to push a value back to the host. The flow is explicit and one-directional unless you wire UseObservable over a WinForms-side INotifyPropertyChanged source. There is no implicit DataContext and no global event bus — each crossing is a method call you write. Threading constraints # The bootstrap creates a DispatcherQueue on the WinForms UI thread (the thread Application.Run is dispatching on) and pins WinUI to that thread. WinForms message-pump callbacks ( Click , Load , Paint ) are already on the right thread — calling element.SetValue(...) or invoking a setter from inside a Click handler is safe and the auto-marshal path is a no-op. The danger is background threads. BackgroundWorker.ProgressChanged , Task.Run(...) continuations without ConfigureAwait , and HttpClient async callbacks land on the thread pool by default. If your handler then tries to set a Reactor UseState directly, Reactor's auto-marshal in RenderContext.SetState queues the write onto ReactorApp.UIDispatcher — which works inside a pure Reactor window but, inside a WinForms host, is only the right dispatcher if you bridged it explicitly. The safe shape is to invoke XamlIslandControl.Dispatcher.TryEnqueue(...) from background callbacks before touching Reactor state. The caveat below covers the specific exception you'll see if you forget. Caveat: WinForms Application.Idle and the WinUI DispatcherQueue are NOT synchronized. Calling Element.SetValue(...) from a WinForms Button.Click handler is fine — the click already runs on the WinForms UI thread, which is the same thread the WinUI dispatcher targets, and Reactor's auto-marshal sees no thread change. But calling it from a BackgroundWorker.ProgressChanged handler raised by a worker thread will throw COMException: 0x8001010E (RPC_E_WRONG_THREAD) from the underlying IXamlObject setter — Reactor's auto-marshal looks at ReactorApp.UIDispatcher which, inside a WinForms host, may not have been initialized at all (interop bootstrap installs the dispatcher on the host control, not the global app). The fix is to marshal manually first: xamlIsland.Dispatcher.TryEnqueue(() => setState(value)) from the background handler. The corresponding analyzer is REACTOR_INTEROP_001 (\"Reactor state setter called from non-UI thread in WinForms host\") — Warning-level — but it only fires when the analyzer can prove the call site is on a non-UI thread, which it cannot do across Task.Run lambda boundaries. Background and Sizing # XAML Islands do not provide an implicit background or stretch behavior. Reactor components hosted in WinForms must manage their own background: class BackgroundDemo : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); // Always set an explicit background on root content. // XAML Islands have no default background — without this, // content renders on a transparent surface. return VStack( 12 , TextBlock( \"Theme-aware background\" ).Bold(), TextBlock( $\"Count: {count} \" ), Button( \"Increment\" , () => setCount(count + 1 )) ).Padding( 24 ).Background(SolidBackground); } } Wrap your component's root in Grid(...) with .Background(...) to fill the island area. Without this, the component renders on a transparent background and may not stretch to fill the WinForms control bounds. Advanced: ContentFactory # For components that need constructor parameters or custom host configuration, use ContentFactory instead of ComponentType : // Use ContentFactory for components needing parameters: // // var island = new XamlIslandControl // { // ContentFactory = () => // { // var host = new ReactorHostControl(); // host.SetComponent&#x3C;ConfigurableComponent>(); // return host; // } // }; class ConfigurableComponent : Component { public override Element Render () { var (count, setCount) = UseState( 0 ); return VStack( 12 , Heading( \"Dashboard\" ), TextBlock( $\"Value: {count} \" ), Button( \"+1\" , () => setCount(count + 1 )) ).Padding( 24 ).Background(SolidBackground); } } The factory function runs on the UI thread after the XAML Island is ready. Return any UIElement — typically a ReactorHostControl wrapping your component. Patterns # Bridge a WinForms VM into a Reactor component via UseObservable # When an existing WinForms app already has a view model implementing INotifyPropertyChanged , the migration path is to host a Reactor component that subscribes to the same VM rather than rewriting state into hooks. The Reactor component receives the VM via constructor props and calls UseObservable to bind the subscription to its lifetime; the WinForms form keeps the same VM reference and reacts to its property changes through the same INPC pipeline it always used. Result: incremental adoption, one form at a time, no big-bang rewrite. Share a single accessibility tree across island and host # The bootstrap wires the WinForms accessibility tree into the UIAutomation tree rooted at the island. Screen readers see one logical app, not two: the form's Label controls, the island's Reactor Text elements, and the rest of the form appear in a single Narrator pass. To make this work, set AutomationName on the WinForms surrounding chrome (labels, group boxes, the form itself) — without it, Narrator reads \"pane\" for the WinForms half and the experience is disjoint. Round-trip focus on Tab # Tab from a WinForms TextBox should enter the Reactor subtree at its first focusable element, Tab again should advance within Reactor, and Tab off the last Reactor element should return to the next WinForms control. XamlIslandControl handles this via WinForms Control.PreviewKeyDown and the bootstrap's PreTranslateMessage filter; no extra wiring is needed for the simple case. For complex tab orders that mix island and host explicitly, set TabIndex on both sides — WinForms TabIndex and Reactor .TabIndex(n) use the same integer space. Common Mistakes # Not calling XamlIslandBootstrap.Run() first # The bootstrap initializes the WinAppSDK, installs the dispatcher, and registers the keyboard message filter. Without it, XamlIslandControl.Load either no-ops (the control renders empty) or throws InvalidOperationException: 'WindowsAppSDK was not bootstrapped on this thread.' — depending on which property triggers the lazy initialization first. The fix is the canonical entry point shown in the bootstrap snippet above: every WinForms + Reactor Main method opens with XamlIslandBootstrap.Run(() => { … }) . Placing the island inside a Panel with DoubleBuffered = true # WinForms' double-buffering composites child controls into an off-screen bitmap on every paint. XAML Islands render via DirectX to a separate composition surface that doesn't participate in GDI double-buffering; the result is severe flicker, white flashes during resize, and intermittent stale-frame artifacts. The fix is to remove DoubleBuffered = true from any container above the island. Set it only on sibling WinForms controls that don't contain an island. Ignoring accessibility on the WinForms surrounding chrome # A Reactor subtree using AutomationName on every Element is fine on its own. Drop it into a WinForms form whose Label controls have no AccessibleName set and whose GroupBox headers are empty, and Narrator reads \"pane, pane, pane\" before reaching the island. The full-app a11y story requires both halves. Run the a11y scanner from inside the island and axe-windows / Accessibility Insights across the host form. Tips # Call XamlIslandBootstrap.Run() first. Before any WinForms forms are shown. The bootstrap must initialize the WinUI runtime before any XamlIslandControl instances are created. Use ComponentType for simple cases. It is designer-friendly and handles lifecycle automatically. Reserve ContentFactory for components that need parameters or custom host setup. Set an explicit background on root components. XAML Islands have no default background. Use .Background(SolidBackgroundFillColorBase) for theme-aware backgrounds. Dock or anchor the island control. XamlIslandControl supports standard WinForms layout: Dock = DockStyle.Fill to fill a panel, or anchoring for proportional resize. Test Tab navigation end-to-end. Tab between WinForms controls and Reactor controls to verify focus flows correctly. The bridging handles most cases automatically, but complex tab orders may need .TabIndex() hints. Next Steps # WPF Interop — the WPF sibling: hosting Reactor in System.Windows.Window apps with the same XAML Islands surface Windows — next topic: opening, finding, and managing top-level windows + tray icons Data System — previous topic: DataGrid with sort, filter, and editing Reactor — back to the index: overview of the framework and full topic list Accessibility — accessibility modifiers and screen reader support Advanced Patterns — error boundaries, Memo, and escape hatches Components — building reusable components to host in WinForms",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/winforms-interop.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/winforms-interop.md",
      "headings": [
        {
          "depth": 2,
          "id": "bootstrap",
          "text": "Bootstrap"
        },
        {
          "depth": 2,
          "id": "xamlislandcontrol",
          "text": "XamlIslandControl"
        },
        {
          "depth": 2,
          "id": "designer-support",
          "text": "Designer Support"
        },
        {
          "depth": 2,
          "id": "keyboard-and-tab-navigation",
          "text": "Keyboard and Tab Navigation"
        },
        {
          "depth": 2,
          "id": "accessibility",
          "text": "Accessibility"
        },
        {
          "depth": 2,
          "id": "data-flow-across-the-boundary",
          "text": "Data flow across the boundary"
        },
        {
          "depth": 2,
          "id": "threading-constraints",
          "text": "Threading constraints"
        },
        {
          "depth": 2,
          "id": "background-and-sizing",
          "text": "Background and Sizing"
        },
        {
          "depth": 2,
          "id": "advanced-contentfactory",
          "text": "Advanced: ContentFactory"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "bridge-a-winforms-vm-into-a-reactor-component-via-useobservable",
          "text": "Bridge a WinForms VM into a Reactor component via UseObservable"
        },
        {
          "depth": 3,
          "id": "share-a-single-accessibility-tree-across-island-and-host",
          "text": "Share a single accessibility tree across island and host"
        },
        {
          "depth": 3,
          "id": "round-trip-focus-on-tab",
          "text": "Round-trip focus on Tab"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "not-calling-xamlislandbootstraprun-first",
          "text": "Not calling XamlIslandBootstrap.Run() first"
        },
        {
          "depth": 3,
          "id": "placing-the-island-inside-a-panel-with-doublebuffered--true",
          "text": "Placing the island inside a Panel with DoubleBuffered = true"
        },
        {
          "depth": 3,
          "id": "ignoring-accessibility-on-the-winforms-surrounding-chrome",
          "text": "Ignoring accessibility on the WinForms surrounding chrome"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "WPF Interop",
      "path": "wpf-interop.md",
      "slug": "/wpf-interop/",
      "html": "<h1 id=\"wpf-interop\">WPF Interop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#wpf-interop\">#</a></h1>\n<p><strong>Status: coming soon.</strong> A first-class Microsoft.UI.Reactor (Reactor) WPF host control\n(<code>Reactor.Interop.Wpf</code>) is on the roadmap but not yet shipped. The only\nhost wrapper in the box today is <code>Reactor.Interop.WinForms</code>.</p>\n<h2 id=\"workaround-for-today\">Workaround for today<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#workaround-for-today\">#</a></h2>\n<p>Host a Reactor component tree from WPF by embedding\n<a href=\"https://learn.microsoft.com/en-us/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.hosting.desktopwindowxamlsource\"><code>DesktopWindowXamlSource</code></a>\ndirectly — the same WinAppSDK primitive that\n<a href=\"/winforms-interop/\">WinForms Interop</a> wraps in <code>XamlIslandControl</code>.\nInside the island, mount a <code>ReactorHostControl</code> and assign it a\n<code>ComponentType</code>. The Reactor side of the boundary is identical to any\nother host: components, hooks, modifiers, and\n<a href=\"/hooks/\"><code>UseObservable&#x3C;T></code></a> for bridging an <code>INotifyPropertyChanged</code>\nview-model all work unchanged.</p>\n<p>The WPF <code>Dispatcher</code> and WinUI <code>DispatcherQueue</code> are distinct objects\non the same UI thread, so plain property writes from WPF event handlers\ninto Reactor setters work without marshalling — see\n<a href=\"/threading-and-dispatch/\">Threading and Dispatch</a> for the invariants\nthe WinUI side enforces.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/winforms-interop/\">WinForms Interop</a></strong> — The shipping parallel\nhost. Read this first; the WPF surface will mirror it.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — <code>UseObservable</code>, <code>UseObservableTree</code>, and\n<code>UseObservableProperty</code> for bridging <code>INotifyPropertyChanged</code>\nview-models from WPF.</li>\n<li><strong><a href=\"/threading-and-dispatch/\">Threading and Dispatch</a></strong> — How Reactor's\nhook setters auto-marshal across dispatchers.</li>\n<li><strong><a href=\"/xaml-developers/\">XAML Developers</a></strong> — Migration cookbook for\nWPF/XAML pages moving to Reactor's declarative shell.</li>\n</ul>",
      "text": "WPF Interop # Status: coming soon. A first-class Microsoft.UI.Reactor (Reactor) WPF host control ( Reactor.Interop.Wpf ) is on the roadmap but not yet shipped. The only host wrapper in the box today is Reactor.Interop.WinForms . Workaround for today # Host a Reactor component tree from WPF by embedding DesktopWindowXamlSource directly — the same WinAppSDK primitive that WinForms Interop wraps in XamlIslandControl . Inside the island, mount a ReactorHostControl and assign it a ComponentType . The Reactor side of the boundary is identical to any other host: components, hooks, modifiers, and UseObservable&#x3C;T> for bridging an INotifyPropertyChanged view-model all work unchanged. The WPF Dispatcher and WinUI DispatcherQueue are distinct objects on the same UI thread, so plain property writes from WPF event handlers into Reactor setters work without marshalling — see Threading and Dispatch for the invariants the WinUI side enforces. Next Steps # WinForms Interop — The shipping parallel host. Read this first; the WPF surface will mirror it. Hooks — UseObservable , UseObservableTree , and UseObservableProperty for bridging INotifyPropertyChanged view-models from WPF. Threading and Dispatch — How Reactor's hook setters auto-marshal across dispatchers. XAML Developers — Migration cookbook for WPF/XAML pages moving to Reactor's declarative shell.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/wpf-interop.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/wpf-interop.md",
      "headings": [
        {
          "depth": 2,
          "id": "workaround-for-today",
          "text": "Workaround for today"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Architecture Overview",
      "path": "architecture-overview.md",
      "slug": "/architecture-overview/",
      "html": "<p>Microsoft.UI.Reactor (Reactor) is a declarative shell over WinUI. You write a <code>Component</code> whose\n<code>Render()</code> method returns an immutable tree of element records — value\ntypes that describe what you want on screen, not the WinUI controls that\nimplement it. A single reconciler diffs the new tree against the previous\none and edits a long-lived WinUI control tree in place, renting and\nreturning controls through an element pool so the heavy classes\n(<a href=\"/text-and-media/\"><code>Button</code></a>, <a href=\"/navigation/\"><code>TabView</code></a>,\n<a href=\"/collections/\"><code>ItemsRepeater</code></a>) survive across renders. Hooks like\n<a href=\"/hooks/\"><code>UseState</code></a> hold the state that drives the loop, and effects\nfire after the reconciler has committed property writes, so the UI you\nobserve in an effect is the UI on screen. The most common mistake is\nreading this page as \"Reactor wraps XAML\" — the element tree is not a\nshadow XAML graph, it is a new authoring surface that happens to\nmaterialize WinUI.</p>\n<h1 id=\"architecture-overview\">Architecture Overview<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#architecture-overview\">#</a></h1>\n<p>This page maps Reactor's runtime end-to-end so the rest of the\nUnder-the-hood track has a shared diagram to point at. Read it once,\nthen jump to the section you care about: <a href=\"/reactivity-model/\">Reactivity\nModel</a>, <a href=\"/reconciliation/\">Reconciliation</a>,\n<a href=\"/hooks-internals/\">Hooks Internals</a>, or\n<a href=\"/effects-scheduling/\">Effects Scheduling</a>.</p>\n<h2 id=\"the-render-loop\">The render loop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-render-loop\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/architecture-overview/overview.svg\" alt=\"Reactor render loop: state setter, component render, element tree, reconciler, WinUI tree, composition, frame\"></p>\n<p>The flow is one-way. A hook setter writes a new value into a\n<a href=\"/hooks-internals/\"><code>RenderContext</code></a> slot, the context calls back into\nthe host with a re-render request, the component's <code>Render()</code> produces a\nnew element tree, the reconciler walks both trees in lockstep and writes\nthe differences to existing WinUI controls. Nothing pulls. There is no\nbinding expression listening to a property; the element tree is the\nsingle source of truth for the frame after a re-render, and the\nreconciler is the single mutator.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Subsystem</th><th>Files</th><th>What it owns</th></tr></thead><tbody><tr><td>Component shell</td><td><code>src/Reactor/Core/Component.cs</code></td><td><code>Render()</code> contract, <code>ShouldUpdate</code>, hook convenience methods</td></tr><tr><td>Hook table</td><td><code>src/Reactor/Core/RenderContext.cs</code></td><td>Slot table, hook implementations, UI-thread invariant</td></tr><tr><td>Elements</td><td><code>src/Reactor/Core/Element.cs</code>, <code>ElementFactory.cs</code></td><td>Immutable records, factory plumbing for ItemsRepeater</td></tr><tr><td>Reconciler</td><td><code>src/Reactor/Core/Reconciler*.cs</code>, <code>ChildReconciler.cs</code></td><td>Mount / Update / Unmount, keyed LIS diff</td></tr><tr><td>Pool</td><td><code>src/Reactor/Core/ElementPool.cs</code></td><td><code>TryRent</code> / <code>Return</code> for poolable leaf controls</td></tr><tr><td>Modifiers</td><td><code>src/Reactor/Elements/ElementExtensions*.cs</code></td><td>Fluent modifier fold</td></tr><tr><td>Hosting</td><td><code>src/Reactor/Hosting/</code></td><td>Window/app bootstrap, UI dispatcher capture</td></tr><tr><td>Diagnostics</td><td><code>src/Reactor/Core/Diagnostics/</code></td><td>ETW provider, overlay hooks</td></tr></tbody></table>\n<p>Every section below zooms into one row of that table.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">internal</span> RenderContext Context { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override to describe the UI. Use UseState, UseEffect, etc. from the context.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Must call hooks in the same order every render.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> Element <span class=\"hljs-title\">Render</span>()</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Controls whether this propless component should re-render when its parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default: false — propless components only re-render from their own state changes or context changes.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override and return true to always re-render when the parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">virtual</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>()</span> => <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p><code>Component</code> is the surface — a single <code>abstract Element Render()</code> plus\nthe <code>ShouldUpdate()</code> hook that lets a propless component opt out of\nparent-driven renders. Everything else is convenience wiring around the\n<a href=\"/hooks-internals/\"><code>RenderContext</code></a> that holds the hook slot table.\nFunction components (<code>Func(ctx => ...)</code>) skip the class declaration but\ngo through the same <code>RenderContext</code> machinery.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The reconciler never recreates the WinUI control tree on a re-render.\nIf you cache a control reference outside Reactor (e.g. stash a <code>Button</code>\nfrom a captured ref in a long-lived field) and the element it\nmaterialized later gets replaced because its type changed, your stash\npoints at an unmounted control still tagged with its old element. Use\n<a href=\"/hooks-internals/\"><code>UseElementRef</code></a> and read <code>.Current</code> inside an\neffect — the ref is the only thing the reconciler keeps current.</p>\n</blockquote>\n<h2 id=\"element-records--the-immutable-description\">Element records — the immutable description<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#element-records--the-immutable-description\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<p>Every node in the tree is a <code>record</code> deriving from <code>Element</code>. The shape\nmatters: records are value-like, structurally immutable, and cheap to\nallocate. A render produces a fresh tree on the heap; the previous\ntree's records become garbage as soon as the reconciler finishes the\npass. The <code>Key</code> slot is the identity primitive — set it via <code>.WithKey()</code>\non collection items so the <a href=\"/reconciliation/\">child reconciler</a> can\nmatch elements across reorderings.</p>\n<p>Modifiers ride inside <code>ElementModifiers</code>. A chain like\n<code>Text(\"hi\").FontSize(24).Margin(8)</code> produces one <code>TextElement</code> with a\nmerged <code>Modifiers</code> record, not three nested wrappers — the\n<a href=\"/modifier-system/\">modifier system</a> folds them so the reconciler only\nsees one shape per concept.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> UIElement <span class=\"hljs-title\">GetElement</span>(<span class=\"hljs-params\">ElementFactoryGetArgs <span class=\"hljs-keyword\">args</span></span>)</span>\n{\n    <span class=\"hljs-comment\">// Resolve the realized data → (key, dataIndex). Three paths:</span>\n    <span class=\"hljs-comment\">//   1. Spec 042: args.Data is ReactorRow — read both off the row.</span>\n    <span class=\"hljs-comment\">//   2. Legacy: args.Data is int — index directly, synthetic key.</span>\n    <span class=\"hljs-comment\">//   3. Fallback: unknown shape, treat as index 0.</span>\n    <span class=\"hljs-built_in\">string</span> key;\n    <span class=\"hljs-built_in\">int</span> index;\n    <span class=\"hljs-keyword\">switch</span> (<span class=\"hljs-keyword\">args</span>.Data)\n    {\n        <span class=\"hljs-keyword\">case</span> ReactorRow row:\n            key = row.Key;\n            index = row.Index;\n            <span class=\"hljs-keyword\">break</span>;\n        <span class=\"hljs-keyword\">case</span> <span class=\"hljs-built_in\">int</span> i:\n            index = i;\n            key = i.ToString(<span class=\"hljs-keyword\">global</span>::System.Globalization.CultureInfo.InvariantCulture);\n            <span class=\"hljs-keyword\">break</span>;\n        <span class=\"hljs-literal\">default</span>:\n            index = <span class=\"hljs-number\">0</span>;\n            key = <span class=\"hljs-string\">\"0\"</span>;\n            <span class=\"hljs-keyword\">break</span>;\n    }\n\n    <span class=\"hljs-keyword\">if</span> (index &#x3C; <span class=\"hljs-number\">0</span> || index >= _items.Count)\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> TextBlock { Text = <span class=\"hljs-string\">\"\"</span> };\n\n    <span class=\"hljs-keyword\">var</span> item = _items[index];\n    <span class=\"hljs-keyword\">var</span> element = BuildOrCache(key, item, index);\n\n    UIElement? control;\n    <span class=\"hljs-keyword\">if</span> (_recyclePool.Count > <span class=\"hljs-number\">0</span>)\n    {\n        <span class=\"hljs-comment\">// Reuse a previously-recycled container. The framework still has</span>\n        <span class=\"hljs-comment\">// it parented to the ItemsRepeater, so the ViewManager.cpp:866</span>\n        <span class=\"hljs-comment\">// Append-skip kicks in and the visual tree stays stable.</span>\n        <span class=\"hljs-keyword\">var</span> reused = _recyclePool.Pop();\n        <span class=\"hljs-keyword\">if</span> (_lastElementByControl.TryGetValue(reused, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> oldElement))\n        {\n            <span class=\"hljs-keyword\">var</span> replacement = _reconciler.Reconcile(oldElement, element, reused, _requestRerender);\n            <span class=\"hljs-keyword\">if</span> (replacement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> &#x26;&#x26; !ReferenceEquals(replacement, reused))\n            {\n                <span class=\"hljs-comment\">// Heterogeneous-row case: Reconcile decided the root</span>\n                <span class=\"hljs-comment\">// element type changed and built a fresh control.</span>\n                <span class=\"hljs-comment\">// `reused` is now unmounted but still parented to the</span>\n                <span class=\"hljs-comment\">// ItemsRepeater — detach so it doesn't sit there as</span>\n                <span class=\"hljs-comment\">// an orphan (the original leak shape we're fixing).</span>\n                <span class=\"hljs-comment\">// (PR #324 review)</span>\n                DetachFromParent(reused);\n                _lastElementByControl.Remove(reused);\n                control = replacement;\n            }\n            <span class=\"hljs-keyword\">else</span>\n            {\n                control = reused;\n            }\n        }\n        <span class=\"hljs-keyword\">else</span>\n        {\n            <span class=\"hljs-comment\">// Defensive: pool entry without a tracked oldElement should</span>\n            <span class=\"hljs-comment\">// not happen — fall back to re-mounting on top of it.</span>\n            control = _reconciler.Mount(element, _requestRerender);\n        }\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        control = _reconciler.Mount(element, _requestRerender);\n    }\n\n    _mountedElements[key] = element;\n    <span class=\"hljs-keyword\">if</span> (control <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n    {\n        _keyByControl[control] = key;\n        _lastElementByControl[control] = element;\n    }\n\n    <span class=\"hljs-keyword\">return</span> control ?? <span class=\"hljs-keyword\">new</span> TextBlock { Text = <span class=\"hljs-string\">\"\"</span> };\n}\n</code></pre>\n<p>Element factories show the shape from the other side: the\n<a href=\"/collections/\">collections</a> layer hands an <code>ItemsRepeater</code> an\n<code>ElementFactory</code> which, on demand, builds a new element for an item\nindex, mounts a control through the same reconciler, and hands the\ncontrol back. The <code>_viewBuilder</code> is the closure you passed to\n<code>ForEach(items, viewBuilder)</code>; the reconciler ensures that re-rendering\nre-uses the same control whenever the keyed identity matches.</p>\n<h2 id=\"reconciler--the-only-mutator\">Reconciler — the only mutator<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reconciler--the-only-mutator\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> UIElement? Reconcile(\n    Element? oldElement,\n    Element? newElement,\n    UIElement? existingControl,\n    Action requestRerender)\n{\n    <span class=\"hljs-comment\">// Trace only top-level reconcile passes (depth == 0) to avoid flooding</span>\n    <span class=\"hljs-comment\">// the provider with per-subtree entries; nested Reconcile() calls during</span>\n    <span class=\"hljs-comment\">// the same pass don't emit their own start/stop. Gate the depth counter</span>\n    <span class=\"hljs-comment\">// and Start emit on IsEnabled so the disabled path pays nothing extra.</span>\n    <span class=\"hljs-built_in\">bool</span> emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled(\n        <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Informational,\n        Diagnostics.ReactorEventSource.Keywords.Reconcile)\n        &#x26;&#x26; _reconcileTraceDepth++ == <span class=\"hljs-number\">0</span>;\n    <span class=\"hljs-keyword\">if</span> (emitTrace)\n    {\n        Diagnostics.ReactorEventSource.Log.ReconcileStart(\n            newElement?.GetType().Name ?? <span class=\"hljs-string\">\"null\"</span>);\n    }\n    <span class=\"hljs-keyword\">if</span> (_debugReconcileDepth++ == <span class=\"hljs-number\">0</span>)\n    {\n        DebugElementsDiffed = <span class=\"hljs-number\">0</span>;\n        DebugElementsSkipped = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsCreated = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsModified = <span class=\"hljs-number\">0</span>;\n        <span class=\"hljs-keyword\">if</span> (ReactorFeatureFlags.HighlightReconcileChanges)\n        {\n            (_highlightMounted ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n            (_highlightModified ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n        }\n        <span class=\"hljs-comment\">// Consume the hot-reload signal exactly once per top-level pass so</span>\n        <span class=\"hljs-comment\">// every component re-runs Render() even when props/deps are unchanged.</span>\n        _forceFullRenderActive = ForceFullRenderPending;\n        ForceFullRenderPending = <span class=\"hljs-literal\">false</span>;\n\n        <span class=\"hljs-comment\">// Build the dirty-ancestor path. For every component node</span>\n        <span class=\"hljs-comment\">// whose SelfTriggered is true, walk up the realized visual</span>\n        <span class=\"hljs-comment\">// tree and add each ancestor control. Consumed by Update's</span>\n        <span class=\"hljs-comment\">// shallow-equality short-circuit so the walk can reach the</span>\n        <span class=\"hljs-comment\">// self-triggered descendant even when its ancestor element</span>\n        <span class=\"hljs-comment\">// records are structurally unchanged.</span>\n        PopulateDirtyAncestorPath();\n    }\n    <span class=\"hljs-keyword\">try</span> {\n    <span class=\"hljs-keyword\">try</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (newElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement)\n        {\n            <span class=\"hljs-keyword\">if</span> (existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                Unmount(existingControl);\n            <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n        }\n\n        <span class=\"hljs-keyword\">if</span> (oldElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement || existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> Mount(newElement, requestRerender);\n\n        <span class=\"hljs-keyword\">return</span> ReconcileImperative(oldElement, newElement, existingControl, requestRerender);\n</code></pre>\n<p><code>Reconcile</code> is a tri-state dispatch: <code>(oldElement, newElement)</code> resolves\ninto Unmount, Mount, or <a href=\"/reconciliation/\">Update</a>. The Mount path\nwalks down the new tree allocating WinUI controls (or renting them from\nthe <a href=\"/element-pool/\">element pool</a>); the Update path uses\n<code>CanUpdate(old, new)</code> to decide whether to patch the existing control\nin place or unmount-and-remount. The Unmount path returns to the pool\nand walks effect cleanups in reverse slot order.</p>\n<p>The reconciler is split across partial files so the Mount and Update\nswitches stay tractable: <code>Reconciler.Mount.cs</code> holds the 40+ MountXxx\nhandlers, <code>Reconciler.Update.cs</code> holds the matching UpdateXxx, and\n<a href=\"/reconciliation/\"><code>ChildReconciler.cs</code></a> holds the keyed-vs-positional\nchild diff. There is one reconciler instance per host; component\nrecursion shares the dispatch.</p>\n<h2 id=\"hooks--slot-table-per-rendercontext\">Hooks — slot table per RenderContext<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks--slot-table-per-rendercontext\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>Each component instance owns a <a href=\"/hooks-internals/\"><code>RenderContext</code></a>\nwith a <code>List&#x3C;HookState></code> indexed by call order. <code>BeginRender</code> resets\n<code>_hookIndex</code> to zero; each hook call reads or creates the slot at the\ncurrent index and advances. Slot type mismatches throw\n<code>HookOrderException</code> immediately — this is why\n<a href=\"/rules-of-reactor/\">hook rules</a> (no conditional hooks, no hooks in\nloops with varying counts) exist. Without the positional invariant, a\nsetter on render N would write the wrong slot on render N+1.</p>\n<p>Hook setters are the entry point into the render loop. They do not\nmutate the UI directly; they store a value, equality-check against the\nprevious, and call <code>_requestRerender</code> on the host if anything changed.\nThe host coalesces requests through <code>DispatcherQueue.TryEnqueue</code>, so a\nsynchronous handler that calls three setters produces one re-render.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"tracing-a-render-end-to-end\">Tracing a render end-to-end<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tracing-a-render-end-to-end\">#</a></h3>\n<p>When you need to know what happened on a single frame, enable the\nreconcile overlay from the <a href=\"/dev-tooling/\">dev menu</a> and watch the ETW\n<code>ReactorEventSource</code> provider. The keywords are split so you can subscribe\nto <code>State</code>, <code>Render</code>, <code>Reconcile</code>, and <code>Effect</code> independently — each\nroughly corresponds to one box on the diagram above. The\n<a href=\"/perf-instrumentation/\">perf-instrumentation</a> page covers the\nattribution side; the <a href=\"/devtools-internals/\">devtools-internals</a> page\ncovers the overlay rendering.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">internal</span> RenderContext Context { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override to describe the UI. Use UseState, UseEffect, etc. from the context.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Must call hooks in the same order every render.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> Element <span class=\"hljs-title\">Render</span>()</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Controls whether this propless component should re-render when its parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default: false — propless components only re-render from their own state changes or context changes.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override and return true to always re-render when the parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">virtual</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>()</span> => <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p>Setting a state cell turns into a <code>StateChange</code> event, the request\nfires <code>RenderStart</code>, the reconciler emits <code>ReconcileStart</code> and\n<code>ReconcileStop</code> with elements-diffed / elements-skipped counters, and\neach effect emits its own <code>EffectStart</code>/<code>EffectStop</code>. Following the\nsequence in PerfView for a single frame is the fastest way to confirm\nthe runtime is doing what you expect.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"treating-the-element-tree-as-a-retained-graph\">Treating the element tree as a retained graph<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#treating-the-element-tree-as-a-retained-graph\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">private</span> TextElement _label = Text(<span class=\"hljs-string\">\"hello\"</span>);\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span> => _label;  <span class=\"hljs-comment\">// same record every render</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<p>The reconciler compares the new tree against the old to decide what to\npatch. Returning the same instance defeats <code>with</code> expressions on the\nmodifier chain and prevents any state-driven property update from\nreaching the WinUI control; the comparison short-circuits as\n\"unchanged\" and nothing flows. Always describe the UI freshly inside\n<code>Render()</code> and let element identity follow position (or <code>Key</code>) — the\nrecords are cheap.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The diagram is the API contract.</strong> When you read a new corner of\n<code>src/Reactor/</code>, locate it on the render-loop diagram first. The four\nboxes (Render, Reconcile, Effects, Composition) are mutually exclusive\nin time; anything that doesn't fit one of them is either hosting glue\nor diagnostics.</p>\n<p><strong><code>Reconciler.cs</code> is partial on purpose.</strong> Don't grep for a Mount\nimplementation in <code>Reconciler.cs</code> itself — the dispatch lives there,\nthe handlers live in <code>Reconciler.Mount.cs</code>. Same for Update / gestures\n/ drag-drop.</p>\n<p><strong>The element pool is opt-in by type.</strong> If a custom control needs\npooling, the type must be in <code>ElementPool.PoolableTypes</code> and survive a\n<code>Return</code> cleanup pass. Interactive controls (Button, TextBox) are\ndeliberately not pooled today; the <a href=\"/element-pool/\">element-pool</a> page\ndocuments the current set.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/reactor-vs-xaml/\">Reactor vs XAML</a></strong> — Same diagram, but mapped onto the XAML concepts you already know.</li>\n<li><strong><a href=\"/reactivity-model/\">Reactivity Model</a></strong> — Zooms into the state-setter → re-render arrow.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Zooms into the diff box.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Zooms into the slot table.</li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — Zooms into the effect-flush box and the async layer above it.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor) is a declarative shell over WinUI. You write a Component whose Render() method returns an immutable tree of element records — value types that describe what you want on screen, not the WinUI controls that implement it. A single reconciler diffs the new tree against the previous one and edits a long-lived WinUI control tree in place, renting and returning controls through an element pool so the heavy classes ( Button , TabView , ItemsRepeater ) survive across renders. Hooks like UseState hold the state that drives the loop, and effects fire after the reconciler has committed property writes, so the UI you observe in an effect is the UI on screen. The most common mistake is reading this page as \"Reactor wraps XAML\" — the element tree is not a shadow XAML graph, it is a new authoring surface that happens to materialize WinUI. Architecture Overview # This page maps Reactor's runtime end-to-end so the rest of the Under-the-hood track has a shared diagram to point at. Read it once, then jump to the section you care about: Reactivity Model , Reconciliation , Hooks Internals , or Effects Scheduling . The render loop # The flow is one-way. A hook setter writes a new value into a RenderContext slot, the context calls back into the host with a re-render request, the component's Render() produces a new element tree, the reconciler walks both trees in lockstep and writes the differences to existing WinUI controls. Nothing pulls. There is no binding expression listening to a property; the element tree is the single source of truth for the frame after a re-render, and the reconciler is the single mutator. Subsystem Files What it owns Component shell src/Reactor/Core/Component.cs Render() contract, ShouldUpdate , hook convenience methods Hook table src/Reactor/Core/RenderContext.cs Slot table, hook implementations, UI-thread invariant Elements src/Reactor/Core/Element.cs , ElementFactory.cs Immutable records, factory plumbing for ItemsRepeater Reconciler src/Reactor/Core/Reconciler*.cs , ChildReconciler.cs Mount / Update / Unmount, keyed LIS diff Pool src/Reactor/Core/ElementPool.cs TryRent / Return for poolable leaf controls Modifiers src/Reactor/Elements/ElementExtensions*.cs Fluent modifier fold Hosting src/Reactor/Hosting/ Window/app bootstrap, UI dispatcher capture Diagnostics src/Reactor/Core/Diagnostics/ ETW provider, overlay hooks Every section below zooms into one row of that table. public abstract class Component { internal RenderContext Context { get ; } = new (); /// &#x3C;summary> /// Override to describe the UI. Use UseState, UseEffect, etc. from the context. /// Must call hooks in the same order every render. /// &#x3C;/summary> public abstract Element Render () ; /// &#x3C;summary> /// Controls whether this propless component should re-render when its parent re-renders. /// Default: false — propless components only re-render from their own state changes or context changes. /// Override and return true to always re-render when the parent re-renders. /// &#x3C;/summary> protected internal virtual bool ShouldUpdate () => false ; Component is the surface — a single abstract Element Render() plus the ShouldUpdate() hook that lets a propless component opt out of parent-driven renders. Everything else is convenience wiring around the RenderContext that holds the hook slot table. Function components ( Func(ctx => ...) ) skip the class declaration but go through the same RenderContext machinery. Caveat: The reconciler never recreates the WinUI control tree on a re-render. If you cache a control reference outside Reactor (e.g. stash a Button from a captured ref in a long-lived field) and the element it materialized later gets replaced because its type changed, your stash points at an unmounted control still tagged with its old element. Use UseElementRef and read .Current inside an effect — the ref is the only thing the reconciler keeps current. Element records — the immutable description # public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } Every node in the tree is a record deriving from Element . The shape matters: records are value-like, structurally immutable, and cheap to allocate. A render produces a fresh tree on the heap; the previous tree's records become garbage as soon as the reconciler finishes the pass. The Key slot is the identity primitive — set it via .WithKey() on collection items so the child reconciler can match elements across reorderings. Modifiers ride inside ElementModifiers . A chain like Text(\"hi\").FontSize(24).Margin(8) produces one TextElement with a merged Modifiers record, not three nested wrappers — the modifier system folds them so the reconciler only sees one shape per concept. public UIElement GetElement ( ElementFactoryGetArgs args ) { // Resolve the realized data → (key, dataIndex). Three paths: // 1. Spec 042: args.Data is ReactorRow — read both off the row. // 2. Legacy: args.Data is int — index directly, synthetic key. // 3. Fallback: unknown shape, treat as index 0. string key; int index; switch ( args .Data) { case ReactorRow row: key = row.Key; index = row.Index; break ; case int i: index = i; key = i.ToString( global ::System.Globalization.CultureInfo.InvariantCulture); break ; default : index = 0 ; key = \"0\" ; break ; } if (index &#x3C; 0 || index >= _items.Count) return new TextBlock { Text = \"\" }; var item = _items[index]; var element = BuildOrCache(key, item, index); UIElement? control; if (_recyclePool.Count > 0 ) { // Reuse a previously-recycled container. The framework still has // it parented to the ItemsRepeater, so the ViewManager.cpp:866 // Append-skip kicks in and the visual tree stays stable. var reused = _recyclePool.Pop(); if (_lastElementByControl.TryGetValue(reused, out var oldElement)) { var replacement = _reconciler.Reconcile(oldElement, element, reused, _requestRerender); if (replacement is not null &#x26;&#x26; !ReferenceEquals(replacement, reused)) { // Heterogeneous-row case: Reconcile decided the root // element type changed and built a fresh control. // `reused` is now unmounted but still parented to the // ItemsRepeater — detach so it doesn't sit there as // an orphan (the original leak shape we're fixing). // (PR #324 review) DetachFromParent(reused); _lastElementByControl.Remove(reused); control = replacement; } else { control = reused; } } else { // Defensive: pool entry without a tracked oldElement should // not happen — fall back to re-mounting on top of it. control = _reconciler.Mount(element, _requestRerender); } } else { control = _reconciler.Mount(element, _requestRerender); } _mountedElements[key] = element; if (control is not null ) { _keyByControl[control] = key; _lastElementByControl[control] = element; } return control ?? new TextBlock { Text = \"\" }; } Element factories show the shape from the other side: the collections layer hands an ItemsRepeater an ElementFactory which, on demand, builds a new element for an item index, mounts a control through the same reconciler, and hands the control back. The _viewBuilder is the closure you passed to ForEach(items, viewBuilder) ; the reconciler ensures that re-rendering re-uses the same control whenever the keyed identity matches. Reconciler — the only mutator # public UIElement? Reconcile( Element? oldElement, Element? newElement, UIElement? existingControl, Action requestRerender) { // Trace only top-level reconcile passes (depth == 0) to avoid flooding // the provider with per-subtree entries; nested Reconcile() calls during // the same pass don't emit their own start/stop. Gate the depth counter // and Start emit on IsEnabled so the disabled path pays nothing extra. bool emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Informational, Diagnostics.ReactorEventSource.Keywords.Reconcile) &#x26;&#x26; _reconcileTraceDepth++ == 0 ; if (emitTrace) { Diagnostics.ReactorEventSource.Log.ReconcileStart( newElement?.GetType().Name ?? \"null\" ); } if (_debugReconcileDepth++ == 0 ) { DebugElementsDiffed = 0 ; DebugElementsSkipped = 0 ; DebugUIElementsCreated = 0 ; DebugUIElementsModified = 0 ; if (ReactorFeatureFlags.HighlightReconcileChanges) { (_highlightMounted ??= new ()).Clear(); (_highlightModified ??= new ()).Clear(); } // Consume the hot-reload signal exactly once per top-level pass so // every component re-runs Render() even when props/deps are unchanged. _forceFullRenderActive = ForceFullRenderPending; ForceFullRenderPending = false ; // Build the dirty-ancestor path. For every component node // whose SelfTriggered is true, walk up the realized visual // tree and add each ancestor control. Consumed by Update's // shallow-equality short-circuit so the walk can reach the // self-triggered descendant even when its ancestor element // records are structurally unchanged. PopulateDirtyAncestorPath(); } try { try { if (newElement is null or EmptyElement) { if (existingControl is not null ) Unmount(existingControl); return null ; } if (oldElement is null or EmptyElement || existingControl is null ) return Mount(newElement, requestRerender); return ReconcileImperative(oldElement, newElement, existingControl, requestRerender); Reconcile is a tri-state dispatch: (oldElement, newElement) resolves into Unmount, Mount, or Update . The Mount path walks down the new tree allocating WinUI controls (or renting them from the element pool ); the Update path uses CanUpdate(old, new) to decide whether to patch the existing control in place or unmount-and-remount. The Unmount path returns to the pool and walks effect cleanups in reverse slot order. The reconciler is split across partial files so the Mount and Update switches stay tractable: Reconciler.Mount.cs holds the 40+ MountXxx handlers, Reconciler.Update.cs holds the matching UpdateXxx, and ChildReconciler.cs holds the keyed-vs-positional child diff. There is one reconciler instance per host; component recursion shares the dispatch. Hooks — slot table per RenderContext # public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; Each component instance owns a RenderContext with a List&#x3C;HookState> indexed by call order. BeginRender resets _hookIndex to zero; each hook call reads or creates the slot at the current index and advances. Slot type mismatches throw HookOrderException immediately — this is why hook rules (no conditional hooks, no hooks in loops with varying counts) exist. Without the positional invariant, a setter on render N would write the wrong slot on render N+1. Hook setters are the entry point into the render loop. They do not mutate the UI directly; they store a value, equality-check against the previous, and call _requestRerender on the host if anything changed. The host coalesces requests through DispatcherQueue.TryEnqueue , so a synchronous handler that calls three setters produces one re-render. Patterns # Tracing a render end-to-end # When you need to know what happened on a single frame, enable the reconcile overlay from the dev menu and watch the ETW ReactorEventSource provider. The keywords are split so you can subscribe to State , Render , Reconcile , and Effect independently — each roughly corresponds to one box on the diagram above. The perf-instrumentation page covers the attribution side; the devtools-internals page covers the overlay rendering. public abstract class Component { internal RenderContext Context { get ; } = new (); /// &#x3C;summary> /// Override to describe the UI. Use UseState, UseEffect, etc. from the context. /// Must call hooks in the same order every render. /// &#x3C;/summary> public abstract Element Render () ; /// &#x3C;summary> /// Controls whether this propless component should re-render when its parent re-renders. /// Default: false — propless components only re-render from their own state changes or context changes. /// Override and return true to always re-render when the parent re-renders. /// &#x3C;/summary> protected internal virtual bool ShouldUpdate () => false ; Setting a state cell turns into a StateChange event, the request fires RenderStart , the reconciler emits ReconcileStart and ReconcileStop with elements-diffed / elements-skipped counters, and each effect emits its own EffectStart / EffectStop . Following the sequence in PerfView for a single frame is the fastest way to confirm the runtime is doing what you expect. Common Mistakes # Treating the element tree as a retained graph # // Don't: private TextElement _label = Text( \"hello\" ); public override Element Render () => _label; // same record every render public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } The reconciler compares the new tree against the old to decide what to patch. Returning the same instance defeats with expressions on the modifier chain and prevents any state-driven property update from reaching the WinUI control; the comparison short-circuits as \"unchanged\" and nothing flows. Always describe the UI freshly inside Render() and let element identity follow position (or Key ) — the records are cheap. Tips # The diagram is the API contract. When you read a new corner of src/Reactor/ , locate it on the render-loop diagram first. The four boxes (Render, Reconcile, Effects, Composition) are mutually exclusive in time; anything that doesn't fit one of them is either hosting glue or diagnostics. Reconciler.cs is partial on purpose. Don't grep for a Mount implementation in Reconciler.cs itself — the dispatch lives there, the handlers live in Reconciler.Mount.cs . Same for Update / gestures / drag-drop. The element pool is opt-in by type. If a custom control needs pooling, the type must be in ElementPool.PoolableTypes and survive a Return cleanup pass. Interactive controls (Button, TextBox) are deliberately not pooled today; the element-pool page documents the current set. Next Steps # Reactor vs XAML — Same diagram, but mapped onto the XAML concepts you already know. Reactivity Model — Zooms into the state-setter → re-render arrow. Reconciliation — Zooms into the diff box. Hooks Internals — Zooms into the slot table. Effects Scheduling — Zooms into the effect-flush box and the async layer above it.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/architecture-overview.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/architecture-overview.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-render-loop",
          "text": "The render loop"
        },
        {
          "depth": 2,
          "id": "element-records--the-immutable-description",
          "text": "Element records — the immutable description"
        },
        {
          "depth": 2,
          "id": "reconciler--the-only-mutator",
          "text": "Reconciler — the only mutator"
        },
        {
          "depth": 2,
          "id": "hooks--slot-table-per-rendercontext",
          "text": "Hooks — slot table per RenderContext"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "tracing-a-render-end-to-end",
          "text": "Tracing a render end-to-end"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "treating-the-element-tree-as-a-retained-graph",
          "text": "Treating the element tree as a retained graph"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Reactivity Model",
      "path": "reactivity-model.md",
      "slug": "/reactivity-model/",
      "html": "<p>Reactivity in Microsoft.UI.Reactor (Reactor) is one direction and one verb: a setter writes a\nslot, the framework asks the owning component to describe the next\ntree, the reconciler diffs and commits. There is no observer graph\nbetween properties, no dependency tracker watching reads, no binding\nexpression listening for <code>PropertyChanged</code>. The setter — the second\nelement of every <code>UseState</code> tuple — is the only thing the runtime\ntreats as a signal. Hold the value in a hook slot, change it through\nthe setter, and the component re-renders with the new value visible.\nHold the value in a plain field, mutate it directly, and nothing\nhappens — the render loop never finds out. This is the same trade-off\nReact made: the model is small and predictable, but every reactive\nedge is something you typed.</p>\n<h1 id=\"reactivity-model\">Reactivity Model<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reactivity-model\">#</a></h1>\n<p>This page is the <em>why</em> behind the <a href=\"/hooks/\">Hooks</a> surface. If you've\ncome from XAML and <code>INotifyPropertyChanged</code>, the change of model is\nlarger than the syntax. If you've come from SwiftUI's <code>@Observable</code> or\nVue's reactivity, the change is smaller, but Reactor is more explicit\nthan either.</p>\n<h2 id=\"what-flows-through-the-render-loop\">What flows through the render loop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#what-flows-through-the-render-loop\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/reactivity-model/state-flow.svg\" alt=\"Reactivity flow: setter call, equality check, slot write, rerender request, dispatcher coalesce, render, reconcile, flush effects\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Step</th><th>Code</th><th>Notes</th></tr></thead><tbody><tr><td>Setter call</td><td>closure returned from <code>UseState</code></td><td>Captures the hook slot index</td></tr><tr><td>UI-thread check</td><td><code>MarshalIfOffUIThread</code></td><td>Auto-marshal via <code>DispatcherQueue</code> from background</td></tr><tr><td>Equality</td><td><code>EqualityComparer&#x3C;T>.Default.Equals</code></td><td>No write, no rerender if equal</td></tr><tr><td>Slot write</td><td><code>ValueHookState&#x3C;T>.Value = newValue</code></td><td>Stays inside <code>RenderContext</code></td></tr><tr><td>Rerender request</td><td><code>_requestRerender?.Invoke()</code></td><td>Host queues a render</td></tr><tr><td>Coalesce</td><td><code>DispatcherQueue.TryEnqueue</code></td><td>Many setters → one render</td></tr><tr><td>Render</td><td><code>Component.Render()</code></td><td>Hooks read the new slot value</td></tr><tr><td>Diff</td><td><code>Reconciler.Reconcile</code></td><td>Property writes to existing WinUI controls</td></tr><tr><td>Flush</td><td><code>FlushEffects</code></td><td>Cleanups, then effect bodies</td></tr></tbody></table>\n<p>Every section below explains why one step in that table is shaped the\nway it is.</p>\n<h2 id=\"the-setter-is-the-signal\">The setter is the signal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-setter-is-the-signal\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p><code>UseState</code> returns <code>(value, setter)</code>. The <code>value</code> is just a read of\nthe slot at the current hook index. The <code>setter</code> is a closure over\nthat index. Calling the setter does not, by itself, \"react\". It walks\nto the slot, compares the new value against the old via\n<code>EqualityComparer&#x3C;T>.Default</code>, writes if different, and calls back\ninto the host with <code>_requestRerender</code>. The host decides when to render\nnext — typically on the next dispatcher tick — and once it does, the\ncomponent's <code>Render()</code> runs and the slot read returns the new value.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Setter</span>(<span class=\"hljs-params\">T newValue</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> h = (ValueHookState&#x3C;T>)_hooks[currentIndex];\n    <span class=\"hljs-built_in\">bool</span> changed;\n    <span class=\"hljs-keyword\">if</span> (h.ThreadSafe)\n    {\n        <span class=\"hljs-keyword\">lock</span> (h.Lock)\n        {\n            changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n            <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        }\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (MarshalIfOffUIThread(<span class=\"hljs-string\">\"UseState\"</span>, () => Setter(newValue))) <span class=\"hljs-keyword\">return</span>;\n        changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n        <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n}\n</code></pre>\n<p>A few details matter for understanding the model:</p>\n<ul>\n<li><strong>No subscribers.</strong> Nothing listens to the slot. The setter knows\none piece of mutable state (the slot) and one piece of mutable\nbehaviour (the host's render request). Components don't see other\ncomponents' state changes; they re-render when their own slots\nchange, when their parent re-renders, or when a context value they\nconsume changes — and not otherwise.</li>\n<li><strong>Equality short-circuits.</strong> If you set <code>count</code> to its current value,\nno rerender. This is why hook-driven re-renders compose cleanly:\npassing the same slot value through three parent renders does not\nmultiply work in the leaf.</li>\n<li><strong>Setters are stable.</strong> The closure captures the slot index and the\ncontext, both of which are fixed for the lifetime of the component.\nPassing <code>setCount</code> as a <a href=\"/effects/\"><code>UseEffect</code></a> dependency or\nthrough a child prop does not retrigger work.</li>\n</ul>\n<blockquote>\n<p><strong>Caveat:</strong> Mutating a reference-typed slot value in place does not trigger a\nrerender even if you call the setter. The equality check compares\n<code>newValue</code> against <code>hook.Value</code>, and after an in-place mutation\nthey're the same reference — so the equality check returns true and\nthe framework bails. If you need to \"change\" a <code>List&#x3C;T></code> or a custom\nclass, allocate a new instance (<code>new List&#x3C;T>(old) { newItem }</code>) and\nhand that to the setter, or use <a href=\"/hooks/\"><code>UseReducer</code></a> and return a\nnew value from the reducer.</p>\n</blockquote>\n<h2 id=\"why-not-inotifypropertychanged\">Why not INotifyPropertyChanged?<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#why-not-inotifypropertychanged\">#</a></h2>\n<p>XAML reactivity hangs off <code>INotifyPropertyChanged</code>. A view model\nimplements <code>INotifyPropertyChanged</code>, properties raise <code>PropertyChanged</code>\non assignment, <a href=\"/reactor-vs-xaml/\"><code>BindingExpression</code></a> instances\nlisten and pull the new value. The model is \"the property is the\nsignal\" — every property declared on a VM is a potential reactive\nedge, with a runtime cost (the event), an ergonomic cost (you write\nthe raise yourself or rely on a source generator), and a correctness\ncost (anything mutating the property without raising is a silent bug).</p>\n<p>Reactor moves the signal off the property and onto the setter. The\nvalue lives in a <a href=\"/hooks-internals/\">hook slot</a>, the only path to a\nwrite is the setter the framework hands you, and the only path to a\nread is calling the hook again in the next render. This eliminates\nthe silent-mutation class of bug: there is no way to update a\n<code>UseState</code> value without going through the setter, and the setter is\nthe thing the framework hooks. It also eliminates the per-property\nevent subscription cost — there are no subscriptions at all.</p>\n<p>The cost is that every piece of state you want to make reactive has to\ngo through a hook. A plain field can hold any value, but if you change\nit inside a render handler the UI won't update. The\n<a href=\"/rules-of-reactor/\">Rules of Reactor</a> document encodes this: every\nmutable thing the UI depends on lives in a hook, every mutation goes\nthrough that hook's setter.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Observable</span>&#x3C;<span class=\"hljs-title\">T</span>> : <span class=\"hljs-title\">INotifyPropertyChanged</span>\n{\n    <span class=\"hljs-keyword\">private</span> T _value;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>() : <span class=\"hljs-title\">this</span>(<span class=\"hljs-params\"><span class=\"hljs-literal\">default</span>!</span>)</span> { }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>(<span class=\"hljs-params\">T initial</span>)</span> => _value = initial;\n\n    <span class=\"hljs-keyword\">public</span> T Value\n    {\n        <span class=\"hljs-keyword\">get</span> => _value;\n        <span class=\"hljs-keyword\">set</span>\n        {\n            <span class=\"hljs-keyword\">if</span> (EqualityComparer&#x3C;T>.Default.Equals(_value, <span class=\"hljs-keyword\">value</span>)) <span class=\"hljs-keyword\">return</span>;\n            _value = <span class=\"hljs-keyword\">value</span>;\n            PropertyChanged?.Invoke(<span class=\"hljs-keyword\">this</span>, _valueChangedArgs);\n        }\n    }\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> PropertyChangedEventHandler? PropertyChanged;\n</code></pre>\n<p>There is a bridge. <code>Observable&#x3C;T></code> is a minimal INPC cell — one\nproperty, raises <code>PropertyChanged</code> when value differs — and\n<code>RenderContext.UseObservable</code> subscribes the current component to it.\nUse this when you have a piece of state shared across components (dev\nflags, the currently-signed-in user, light/dark mode) or when you're\ninteroperating with existing INPC view models. It's deliberately a\nsmall surface; full INPC view models with multiple properties and\ncomputed expressions stay in their own world, and a wrapper component\ntypically funnels their data into local hooks for the rendered slice.</p>\n<h2 id=\"why-not-auto-tracking\">Why not auto-tracking?<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#why-not-auto-tracking\">#</a></h2>\n<p>SwiftUI's <code>@Observable</code>, Vue's <code>ref</code>/<code>reactive</code>, MobX's <code>observable</code>,\nand Solid's signals all hide the dependency edge inside the read:\naccessing a tracked property during render registers the component as\na dependent of that property, and a write later notifies whoever\nregistered. The model is \"the framework figures out who needs to\nrerender\". The ergonomics are excellent when it works. The trade-offs\nare that the dependency graph is invisible (the dev tools become a\nmandatory part of debugging), the read-side becomes load-bearing\n(passing a tracked value to a memoizing helper can detach the\ndependency), and the proxy machinery on the reactive object has a\nruntime cost that's hard to predict.</p>\n<p>Reactor's positional hook table chooses the opposite trade-off. The\ndependency edge is the code you wrote: <code>UseState</code> is a slot, the\nsetter is the signal, re-render is the effect. The graph is the call\nstack. Debugging is reading source. This is the same reason React\npicked hooks over <code>observable</code> after the class-component era: when the\nreactive surface is small enough, an explicit setter beats invisible\ntracking.</p>\n<h2 id=\"shouldupdate--opting-out-of-parent-driven-re-renders\">ShouldUpdate — opting out of parent-driven re-renders<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#shouldupdate--opting-out-of-parent-driven-re-renders\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">internal</span> RenderContext Context { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override to describe the UI. Use UseState, UseEffect, etc. from the context.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Must call hooks in the same order every render.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> Element <span class=\"hljs-title\">Render</span>()</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Controls whether this propless component should re-render when its parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default: false — propless components only re-render from their own state changes or context changes.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override and return true to always re-render when the parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">virtual</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>()</span> => <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p>A parent re-render normally cascades to its children: the parent's\n<code>Render()</code> produces a new element tree, the reconciler walks it, and\neach child component's <code>Render()</code> is invoked because the parent's call\nto <code>Component(...)</code> is what allocated the child element. The default\n<code>ShouldUpdate</code> returns <code>false</code> for <em>propless</em> components — those have\nno inputs that could have changed, so a parent re-render with the same\nshape doesn't propagate. Components with props always re-render on\nparent change today; equality comparison on props is on the roadmap.</p>\n<p>Override <code>ShouldUpdate</code> to opt a propless component back in (rare —\nthe typical reason is a context value the component implicitly reads\nthat you want it to pick up). Don't reach for it to memoize: use\n<a href=\"/hooks/\"><code>UseMemo</code></a> on the expensive computation, or hoist state\nupward so the parent doesn't need to re-render for an unrelated\nchange.</p>\n<h2 id=\"state-batching\">State batching<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#state-batching\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Setter</span>(<span class=\"hljs-params\">T newValue</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> h = (ValueHookState&#x3C;T>)_hooks[currentIndex];\n    <span class=\"hljs-built_in\">bool</span> changed;\n    <span class=\"hljs-keyword\">if</span> (h.ThreadSafe)\n    {\n        <span class=\"hljs-keyword\">lock</span> (h.Lock)\n        {\n            changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n            <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        }\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (MarshalIfOffUIThread(<span class=\"hljs-string\">\"UseState\"</span>, () => Setter(newValue))) <span class=\"hljs-keyword\">return</span>;\n        changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n        <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n}\n</code></pre>\n<p>Three setters in the same synchronous block call <code>_requestRerender</code>\nthree times, and the host's request handler enqueues a render via\n<code>DispatcherQueue.TryEnqueue</code>. The dispatcher coalesces repeated\nenqueues on the same tick — only one render runs. This is the\nbatching contract: any sequence of setter calls in one event handler,\none effect body, one timer callback, etc., collapses into a single\nrender. The component sees all three new values at once on the next\nframe.</p>\n<p>Batching breaks if you <code>await</code> between setters and the continuation\nhops to a different dispatcher tick. The first render fires before\nthe second setter runs; the second setter triggers another render\nafterward. If you need them atomic, set them before the <code>await</code> or\nuse a single <a href=\"/hooks/\"><code>UseReducer</code></a> state that captures all the\nfields you're updating.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"bridging-an-existing-inpc-view-model\">Bridging an existing INPC view model<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#bridging-an-existing-inpc-view-model\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Observable</span>&#x3C;<span class=\"hljs-title\">T</span>> : <span class=\"hljs-title\">INotifyPropertyChanged</span>\n{\n    <span class=\"hljs-keyword\">private</span> T _value;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>() : <span class=\"hljs-title\">this</span>(<span class=\"hljs-params\"><span class=\"hljs-literal\">default</span>!</span>)</span> { }\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Observable</span>(<span class=\"hljs-params\">T initial</span>)</span> => _value = initial;\n\n    <span class=\"hljs-keyword\">public</span> T Value\n    {\n        <span class=\"hljs-keyword\">get</span> => _value;\n        <span class=\"hljs-keyword\">set</span>\n        {\n            <span class=\"hljs-keyword\">if</span> (EqualityComparer&#x3C;T>.Default.Equals(_value, <span class=\"hljs-keyword\">value</span>)) <span class=\"hljs-keyword\">return</span>;\n            _value = <span class=\"hljs-keyword\">value</span>;\n            PropertyChanged?.Invoke(<span class=\"hljs-keyword\">this</span>, _valueChangedArgs);\n        }\n    }\n\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> PropertyChangedEventHandler? PropertyChanged;\n</code></pre>\n<p>When you have an <code>INotifyPropertyChanged</code> view model you need to render\n— a port from an existing XAML app, a model exposed by a service —\nexpose the values you actually display via <code>UseObservable</code> for <code>Observable&#x3C;T></code>\ncells, or build a custom hook that subscribes to <code>PropertyChanged</code>\ninside an effect and stashes the latest value in <code>UseState</code>. Either\nway, the component's own state lives in hooks; the INPC cell is a\nsource the hook listens to.</p>\n<h3 id=\"atomic-multi-field-updates\">Atomic multi-field updates<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#atomic-multi-field-updates\">#</a></h3>\n<p>When two state values must change together, pick one of:</p>\n<ul>\n<li><strong>One <code>UseReducer</code></strong> holding both fields in a record. A single\n<code>update(prev => prev with { Foo = ..., Bar = ... })</code> writes both.</li>\n<li><strong>A computed slot.</strong> Hoist the second value into a <code>UseMemo</code>\nderived from the first.</li>\n</ul>\n<p>Don't write <code>setFoo(...); setBar(...)</code> after an <code>await</code> and hope —\nthe awaited continuation may have already let one render run.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"mutating-a-usestate-value-in-place\">Mutating a UseState value in place<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mutating-a-usestate-value-in-place\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> (items, setItems) = UseState(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\nitems.Add(<span class=\"hljs-string\">\"new\"</span>);        <span class=\"hljs-comment\">// mutates the same list the slot holds</span>\nsetItems(items);         <span class=\"hljs-comment\">// equality check sees same reference → no rerender</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>The setter's equality compares <code>newValue</code> against <code>hook.Value</code>. After\n<code>items.Add</code>, both refer to the same list, the comparison returns true,\nand nothing is written or re-rendered. Allocate a new list — or use\n<a href=\"/hooks/\"><code>UseReducer</code></a> so the framework forces you to return a new\nvalue from the reducer:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (items, update) = UseReducer(<span class=\"hljs-keyword\">new</span> List&#x3C;<span class=\"hljs-built_in\">string</span>>());\nupdate(prev => [.. prev, <span class=\"hljs-string\">\"new\"</span>]);\n</code></pre>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The setter is the contract.</strong> Anything that needs to be reactive\ngoes through a setter. Fields, properties, statics, and direct\nmutations are invisible to the render loop — they may exist in C#,\nbut the UI won't reflect them until something else triggers a render.</p>\n<p><strong>Equality is <code>EqualityComparer&#x3C;T>.Default</code>.</strong> Records are value-equal\nby default, primitives are value-equal, classes default to reference\nequality. If you store a custom class in <code>UseState</code>, either implement\n<code>IEquatable&#x3C;T></code> or replace the instance on every change so the\nequality check can decide correctly.</p>\n<p><strong>Don't reach for <code>UseObservable</code> for component-local state.</strong> It\nexists to bridge cross-component or external state. Local state lives\nin <code>UseState</code> / <code>UseReducer</code> so the slot table tracks it.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/architecture-overview/\">Architecture Overview</a></strong> — Previous: the full render loop.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Next: how the slot table is laid out.</li>\n<li><strong><a href=\"/reactor-vs-xaml/\">Reactor vs XAML</a></strong> — Same model contrasted with XAML's binding / INPC.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — What the diff phase does once <code>Render()</code> returns.</li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — When effects fire relative to the setter / render cycle.</li>\n</ul>",
      "text": "Reactivity in Microsoft.UI.Reactor (Reactor) is one direction and one verb: a setter writes a slot, the framework asks the owning component to describe the next tree, the reconciler diffs and commits. There is no observer graph between properties, no dependency tracker watching reads, no binding expression listening for PropertyChanged . The setter — the second element of every UseState tuple — is the only thing the runtime treats as a signal. Hold the value in a hook slot, change it through the setter, and the component re-renders with the new value visible. Hold the value in a plain field, mutate it directly, and nothing happens — the render loop never finds out. This is the same trade-off React made: the model is small and predictable, but every reactive edge is something you typed. Reactivity Model # This page is the why behind the Hooks surface. If you've come from XAML and INotifyPropertyChanged , the change of model is larger than the syntax. If you've come from SwiftUI's @Observable or Vue's reactivity, the change is smaller, but Reactor is more explicit than either. What flows through the render loop # Step Code Notes Setter call closure returned from UseState Captures the hook slot index UI-thread check MarshalIfOffUIThread Auto-marshal via DispatcherQueue from background Equality EqualityComparer&#x3C;T>.Default.Equals No write, no rerender if equal Slot write ValueHookState&#x3C;T>.Value = newValue Stays inside RenderContext Rerender request _requestRerender?.Invoke() Host queues a render Coalesce DispatcherQueue.TryEnqueue Many setters → one render Render Component.Render() Hooks read the new slot value Diff Reconciler.Reconcile Property writes to existing WinUI controls Flush FlushEffects Cleanups, then effect bodies Every section below explains why one step in that table is shaped the way it is. The setter is the signal # public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; UseState returns (value, setter) . The value is just a read of the slot at the current hook index. The setter is a closure over that index. Calling the setter does not, by itself, \"react\". It walks to the slot, compares the new value against the old via EqualityComparer&#x3C;T>.Default , writes if different, and calls back into the host with _requestRerender . The host decides when to render next — typically on the next dispatcher tick — and once it does, the component's Render() runs and the slot read returns the new value. void Setter ( T newValue ) { var h = (ValueHookState&#x3C;T>)_hooks[currentIndex]; bool changed; if (h.ThreadSafe) { lock (h.Lock) { changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; } if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } else { if (MarshalIfOffUIThread( \"UseState\" , () => Setter(newValue))) return ; changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } } A few details matter for understanding the model: No subscribers. Nothing listens to the slot. The setter knows one piece of mutable state (the slot) and one piece of mutable behaviour (the host's render request). Components don't see other components' state changes; they re-render when their own slots change, when their parent re-renders, or when a context value they consume changes — and not otherwise. Equality short-circuits. If you set count to its current value, no rerender. This is why hook-driven re-renders compose cleanly: passing the same slot value through three parent renders does not multiply work in the leaf. Setters are stable. The closure captures the slot index and the context, both of which are fixed for the lifetime of the component. Passing setCount as a UseEffect dependency or through a child prop does not retrigger work. Caveat: Mutating a reference-typed slot value in place does not trigger a rerender even if you call the setter. The equality check compares newValue against hook.Value , and after an in-place mutation they're the same reference — so the equality check returns true and the framework bails. If you need to \"change\" a List&#x3C;T> or a custom class, allocate a new instance ( new List&#x3C;T>(old) { newItem } ) and hand that to the setter, or use UseReducer and return a new value from the reducer. Why not INotifyPropertyChanged? # XAML reactivity hangs off INotifyPropertyChanged . A view model implements INotifyPropertyChanged , properties raise PropertyChanged on assignment, BindingExpression instances listen and pull the new value. The model is \"the property is the signal\" — every property declared on a VM is a potential reactive edge, with a runtime cost (the event), an ergonomic cost (you write the raise yourself or rely on a source generator), and a correctness cost (anything mutating the property without raising is a silent bug). Reactor moves the signal off the property and onto the setter. The value lives in a hook slot , the only path to a write is the setter the framework hands you, and the only path to a read is calling the hook again in the next render. This eliminates the silent-mutation class of bug: there is no way to update a UseState value without going through the setter, and the setter is the thing the framework hooks. It also eliminates the per-property event subscription cost — there are no subscriptions at all. The cost is that every piece of state you want to make reactive has to go through a hook. A plain field can hold any value, but if you change it inside a render handler the UI won't update. The Rules of Reactor document encodes this: every mutable thing the UI depends on lives in a hook, every mutation goes through that hook's setter. public sealed class Observable &#x3C; T > : INotifyPropertyChanged { private T _value; public Observable () : this ( default ! ) { } public Observable ( T initial ) => _value = initial; public T Value { get => _value; set { if (EqualityComparer&#x3C;T>.Default.Equals(_value, value )) return ; _value = value ; PropertyChanged?.Invoke( this , _valueChangedArgs); } } public event PropertyChangedEventHandler? PropertyChanged; There is a bridge. Observable&#x3C;T> is a minimal INPC cell — one property, raises PropertyChanged when value differs — and RenderContext.UseObservable subscribes the current component to it. Use this when you have a piece of state shared across components (dev flags, the currently-signed-in user, light/dark mode) or when you're interoperating with existing INPC view models. It's deliberately a small surface; full INPC view models with multiple properties and computed expressions stay in their own world, and a wrapper component typically funnels their data into local hooks for the rendered slice. Why not auto-tracking? # SwiftUI's @Observable , Vue's ref / reactive , MobX's observable , and Solid's signals all hide the dependency edge inside the read: accessing a tracked property during render registers the component as a dependent of that property, and a write later notifies whoever registered. The model is \"the framework figures out who needs to rerender\". The ergonomics are excellent when it works. The trade-offs are that the dependency graph is invisible (the dev tools become a mandatory part of debugging), the read-side becomes load-bearing (passing a tracked value to a memoizing helper can detach the dependency), and the proxy machinery on the reactive object has a runtime cost that's hard to predict. Reactor's positional hook table chooses the opposite trade-off. The dependency edge is the code you wrote: UseState is a slot, the setter is the signal, re-render is the effect. The graph is the call stack. Debugging is reading source. This is the same reason React picked hooks over observable after the class-component era: when the reactive surface is small enough, an explicit setter beats invisible tracking. ShouldUpdate — opting out of parent-driven re-renders # public abstract class Component { internal RenderContext Context { get ; } = new (); /// &#x3C;summary> /// Override to describe the UI. Use UseState, UseEffect, etc. from the context. /// Must call hooks in the same order every render. /// &#x3C;/summary> public abstract Element Render () ; /// &#x3C;summary> /// Controls whether this propless component should re-render when its parent re-renders. /// Default: false — propless components only re-render from their own state changes or context changes. /// Override and return true to always re-render when the parent re-renders. /// &#x3C;/summary> protected internal virtual bool ShouldUpdate () => false ; A parent re-render normally cascades to its children: the parent's Render() produces a new element tree, the reconciler walks it, and each child component's Render() is invoked because the parent's call to Component(...) is what allocated the child element. The default ShouldUpdate returns false for propless components — those have no inputs that could have changed, so a parent re-render with the same shape doesn't propagate. Components with props always re-render on parent change today; equality comparison on props is on the roadmap. Override ShouldUpdate to opt a propless component back in (rare — the typical reason is a context value the component implicitly reads that you want it to pick up). Don't reach for it to memoize: use UseMemo on the expensive computation, or hoist state upward so the parent doesn't need to re-render for an unrelated change. State batching # void Setter ( T newValue ) { var h = (ValueHookState&#x3C;T>)_hooks[currentIndex]; bool changed; if (h.ThreadSafe) { lock (h.Lock) { changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; } if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } else { if (MarshalIfOffUIThread( \"UseState\" , () => Setter(newValue))) return ; changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } } Three setters in the same synchronous block call _requestRerender three times, and the host's request handler enqueues a render via DispatcherQueue.TryEnqueue . The dispatcher coalesces repeated enqueues on the same tick — only one render runs. This is the batching contract: any sequence of setter calls in one event handler, one effect body, one timer callback, etc., collapses into a single render. The component sees all three new values at once on the next frame. Batching breaks if you await between setters and the continuation hops to a different dispatcher tick. The first render fires before the second setter runs; the second setter triggers another render afterward. If you need them atomic, set them before the await or use a single UseReducer state that captures all the fields you're updating. Patterns # Bridging an existing INPC view model # public sealed class Observable &#x3C; T > : INotifyPropertyChanged { private T _value; public Observable () : this ( default ! ) { } public Observable ( T initial ) => _value = initial; public T Value { get => _value; set { if (EqualityComparer&#x3C;T>.Default.Equals(_value, value )) return ; _value = value ; PropertyChanged?.Invoke( this , _valueChangedArgs); } } public event PropertyChangedEventHandler? PropertyChanged; When you have an INotifyPropertyChanged view model you need to render — a port from an existing XAML app, a model exposed by a service — expose the values you actually display via UseObservable for Observable&#x3C;T> cells, or build a custom hook that subscribes to PropertyChanged inside an effect and stashes the latest value in UseState . Either way, the component's own state lives in hooks; the INPC cell is a source the hook listens to. Atomic multi-field updates # When two state values must change together, pick one of: One UseReducer holding both fields in a record. A single update(prev => prev with { Foo = ..., Bar = ... }) writes both. A computed slot. Hoist the second value into a UseMemo derived from the first. Don't write setFoo(...); setBar(...) after an await and hope — the awaited continuation may have already let one render run. Common Mistakes # Mutating a UseState value in place # // Don't: var (items, setItems) = UseState( new List&#x3C; string >()); items.Add( \"new\" ); // mutates the same list the slot holds setItems(items); // equality check sees same reference → no rerender public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; The setter's equality compares newValue against hook.Value . After items.Add , both refer to the same list, the comparison returns true, and nothing is written or re-rendered. Allocate a new list — or use UseReducer so the framework forces you to return a new value from the reducer: var (items, update) = UseReducer( new List&#x3C; string >()); update(prev => [.. prev, \"new\" ]); Tips # The setter is the contract. Anything that needs to be reactive goes through a setter. Fields, properties, statics, and direct mutations are invisible to the render loop — they may exist in C#, but the UI won't reflect them until something else triggers a render. Equality is EqualityComparer&#x3C;T>.Default . Records are value-equal by default, primitives are value-equal, classes default to reference equality. If you store a custom class in UseState , either implement IEquatable&#x3C;T> or replace the instance on every change so the equality check can decide correctly. Don't reach for UseObservable for component-local state. It exists to bridge cross-component or external state. Local state lives in UseState / UseReducer so the slot table tracks it. Next Steps # Architecture Overview — Previous: the full render loop. Hooks Internals — Next: how the slot table is laid out. Reactor vs XAML — Same model contrasted with XAML's binding / INPC. Reconciliation — What the diff phase does once Render() returns. Effects Scheduling — When effects fire relative to the setter / render cycle.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/reactivity-model.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/reactivity-model.md",
      "headings": [
        {
          "depth": 2,
          "id": "what-flows-through-the-render-loop",
          "text": "What flows through the render loop"
        },
        {
          "depth": 2,
          "id": "the-setter-is-the-signal",
          "text": "The setter is the signal"
        },
        {
          "depth": 2,
          "id": "why-not-inotifypropertychanged",
          "text": "Why not INotifyPropertyChanged?"
        },
        {
          "depth": 2,
          "id": "why-not-auto-tracking",
          "text": "Why not auto-tracking?"
        },
        {
          "depth": 2,
          "id": "shouldupdate--opting-out-of-parent-driven-re-renders",
          "text": "ShouldUpdate — opting out of parent-driven re-renders"
        },
        {
          "depth": 2,
          "id": "state-batching",
          "text": "State batching"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "bridging-an-existing-inpc-view-model",
          "text": "Bridging an existing INPC view model"
        },
        {
          "depth": 3,
          "id": "atomic-multi-field-updates",
          "text": "Atomic multi-field updates"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "mutating-a-usestate-value-in-place",
          "text": "Mutating a UseState value in place"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Hooks Internals",
      "path": "hooks-internals.md",
      "slug": "/hooks-internals/",
      "html": "<p>A hook in Microsoft.UI.Reactor (Reactor) is a position. When <code>Render()</code> runs, the framework\ncounts each hook call in order — <code>UseState</code> is slot 0, then <code>UseEffect</code>\nis slot 1, then <code>UseMemo</code> is slot 2 — and stores whatever each hook\nneeds in a <code>List&#x3C;HookState></code> on the owning <code>RenderContext</code>. The next\nrender starts from <code>_hookIndex = 0</code> again and walks the same list in\nthe same order. The slot at position 0 had better still be the one\n<code>UseState</code> expects to find there, because the setter closure\n<code>UseState</code> returned on the first render captured the literal index\n<code>0</code> and writes back to that slot when you call it. This is the entire\nmechanical foundation of every hook rule you've ever read: \"same hooks\nin the same order every render\" is the runtime invariant the slot\ntable enforces. Get it wrong — a conditional <code>UseState</code> inside an\n<code>if</code>, a hook inside a loop whose iteration count changed — and the\ntype check on the slot throws <code>HookOrderException</code> because the slot\nthat used to hold <code>ValueHookState&#x3C;int></code> now sees a <code>EffectHookState</code>\nasking for its bucket.</p>\n<h1 id=\"hooks-internals\">Hooks Internals<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks-internals\">#</a></h1>\n<p>This page absorbs the legacy <code>docs/reference/state-and-hooks.md</code> and\nsits underneath the surface-level <a href=\"/hooks/\">Hooks</a> reference. The\ngoal is one description of the storage shape behind every hook —\nenough to debug a <code>HookOrderException</code>, predict whether <code>UseState</code>\nreturns a stable setter, and know when reaching for <code>UseRef</code> is\ncorrect.</p>\n<h2 id=\"the-slot-table\">The slot table<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-slot-table\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/hooks-internals/hook-slot-table.svg\" alt=\"Hook slot table: BeginRender resets _hookIndex, each hook advances, type mismatch throws HookOrderException\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Slot type</th><th>Holds</th><th>Created by</th></tr></thead><tbody><tr><td><code>ValueHookState&#x3C;T></code></td><td>Value, optional lock</td><td><code>UseState</code>, <code>UseReducer</code></td></tr><tr><td><code>EffectHookState</code></td><td>Deps, body, cleanup, pending flag</td><td><code>UseEffect</code></td></tr><tr><td><code>MemoHookState&#x3C;T></code></td><td>Deps, cached value</td><td><code>UseMemo</code>, <code>UseCallback</code>, <code>UseElementRef</code></td></tr><tr><td><code>RefHookState&#x3C;T></code></td><td>Mutable cell</td><td><code>UseRef</code></td></tr><tr><td><code>ResourceHookState&#x3C;T></code></td><td>Cache key, state, cleanup</td><td><code>UseResource</code>, <code>UseInfiniteResource</code></td></tr><tr><td><code>ContextHookState</code></td><td>Subscribed context keys</td><td><code>UseContext</code></td></tr></tbody></table>\n<p>Each subsection below covers one row of that table and the contract\nits hook surface depends on.</p>\n<h2 id=\"rendercontext-owns-the-table\">RenderContext owns the table<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#rendercontext-owns-the-table\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p><code>RenderContext</code> holds the <code>List&#x3C;HookState> _hooks</code> and an <code>int _hookIndex</code>. <code>BeginRender(requestRerender)</code> sets <code>_hookIndex = 0</code> and\ncaptures the UI thread ID; the component's <code>Render()</code> runs, hooks\nadvance <code>_hookIndex</code> as they go, and after <code>Render()</code> returns the\nreconciler reads <code>_hooks</code> to know which effects to flush. There is\none <code>RenderContext</code> per <code>Component</code> instance (and one per <code>Func(ctx => …)</code> function-component invocation — function components get their\nown context per parent's render, with state preserved across that\nparent's re-renders via positional matching just like any other\nelement).</p>\n<p>The slot-type check at the top of each hook is the runtime guard:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> ValueHookState&#x3C;T> hook)\n    <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> HookOrderException(...);\n</code></pre>\n<p>That check is the reason hook rules exist. A conditional hook\n(<code>if (cond) UseState(...)</code>) advances the slot table on some renders\nbut not others, so a later hook in the same <code>Render()</code> reads a slot\nholding a different state type, and the cast fails. The\n<a href=\"/rules-of-reactor/\">REACTOR_HOOKS_001..007</a> analyzer family flags\nthe most common shapes at build time so the runtime check stays an\nassertion of last resort.</p>\n<h2 id=\"usestate--the-canonical-slot\">UseState — the canonical slot<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usestate--the-canonical-slot\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p><code>UseState</code> allocates a <code>ValueHookState&#x3C;T></code> on first render (`_hookIndex</p>\n<blockquote>\n<p>= _hooks.Count<code>means \"this slot doesn't exist yet\"), captures</code>currentIndex = _hookIndex<code>, increments, and reads the value. The returned setter is a local function </code>Setter<code>closed over</code>currentIndex<code>and the context's</code>_hooks` field — the index never\nchanges for the lifetime of the component, which is why setter\nidentity is stable across renders.</p>\n</blockquote>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Setter</span>(<span class=\"hljs-params\">T newValue</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> h = (ValueHookState&#x3C;T>)_hooks[currentIndex];\n    <span class=\"hljs-built_in\">bool</span> changed;\n    <span class=\"hljs-keyword\">if</span> (h.ThreadSafe)\n    {\n        <span class=\"hljs-keyword\">lock</span> (h.Lock)\n        {\n            changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n            <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        }\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (MarshalIfOffUIThread(<span class=\"hljs-string\">\"UseState\"</span>, () => Setter(newValue))) <span class=\"hljs-keyword\">return</span>;\n        changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n        <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n}\n</code></pre>\n<p>The setter is also where the auto-marshal, the equality check, and\nthe rerender request live. Calling it on a background thread takes\nthe <code>MarshalIfOffUIThread</code> branch and re-enqueues the setter on the\nUI dispatcher; calling it from the UI thread runs the body inline.\nEither way, the slot is written with the new value, an ETW\n<code>StateChange</code> event fires if state-keyword tracing is enabled, and\n<code>_requestRerender</code> triggers the host to render again on the next\ndispatcher tick. The reactivity contract — equality short-circuit,\nsingle signal — is described end-to-end on the <a href=\"/reactivity-model/\">Reactivity\nModel</a> page; the implementation lives here.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The setter's closure captures the <code>currentIndex</code> <em>value</em>, not the\n<code>hookIndex</code> <em>variable</em>. That means it points at slot N forever, even\nif a future render adds hooks before it — adding a hook before this\none shifts every subsequent slot's intended type, and the next render\nwill throw <code>HookOrderException</code> from the type cast. There's no\nrecovery path; the component has to be unmounted (or the source fixed\nand hot-reload triggered, which remounts).</p>\n</blockquote>\n<h2 id=\"useeffect--the-slot-that-runs-later\">UseEffect — the slot that runs later<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useeffect--the-slot-that-runs-later\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">UseEffect</span>(<span class=\"hljs-params\">Action effect, <span class=\"hljs-keyword\">params</span> <span class=\"hljs-built_in\">object</span>[] dependencies</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> EffectHookState { Dependencies = <span class=\"hljs-literal\">null</span>, Effect = effect });\n    }\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[_hookIndex] <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> EffectHookState hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> HookOrderException(\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{_hookIndex}</span> is <span class=\"hljs-subst\">{_hooks[_hookIndex].GetType().Name}</span>, expected EffectHookState. \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span>);\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (hook.Dependencies <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> || !DepsEqual(hook.Dependencies, dependencies))\n    {\n        hook.PendingCleanup = hook.Cleanup;\n        hook.Cleanup = <span class=\"hljs-literal\">null</span>;\n        hook.Dependencies = dependencies.ToArray();\n        hook.Effect = effect;\n        hook.Pending = <span class=\"hljs-literal\">true</span>;\n    }\n}\n</code></pre>\n<p><code>UseEffect</code> doesn't run the body during <code>Render()</code>. It allocates an\n<code>EffectHookState</code> if needed, compares the dependencies against the\nprevious render's deps (<code>DepsEqual</code> uses <code>object.Equals</code> element-wise),\nand if they differ — or if this is the first render — it stashes the\nold cleanup as <code>PendingCleanup</code>, replaces the deps and body, and\nflips <code>Pending = true</code>. The reconciler calls <code>FlushEffectsTraced</code>\nafter commit; flush runs all <code>PendingCleanup</code> callbacks first, then\nall <code>Effect</code> bodies, both in slot order.</p>\n<p>The cleanup-before-body ordering is load-bearing: a <code>cts.Cancel()</code> /\n<code>timer.Dispose()</code> cleanup actually disposes the previous timer\nbefore the new body creates a new one. The\n<a href=\"/effects-scheduling/\">Effects Scheduling</a> page covers the timing\ncontract; this is the slot-shape that implements it.</p>\n<p>The reason <code>UseEffect(body, deps)</code> returns nothing and the cleanup\nmechanism uses a closure-returned <code>Func&#x3C;Action></code> (the overload\n<code>UseEffect(Func&#x3C;Action>)</code>) is to avoid eagerly walking through every\nhook's \"what if this cleans up\" plumbing. Effects that need cleanup\nopt in by returning an action; the slot holds a <code>null</code> cleanup\notherwise.</p>\n<h2 id=\"usememo--the-cached-value-slot\">UseMemo — the cached-value slot<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#usememo--the-cached-value-slot\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-title\">ElementRef</span>&#x3C;<span class=\"hljs-title\">T</span>> <span class=\"hljs-title\">UseElementRef</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> RenderContext ctx</span>)\n    <span class=\"hljs-keyword\">where</span> T : FrameworkElement</span>\n{\n    <span class=\"hljs-keyword\">if</span> (ctx <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentNullException(<span class=\"hljs-keyword\">nameof</span>(ctx));\n\n    <span class=\"hljs-comment\">// UseMemo with empty deps allocates the typed+inner ref on the first</span>\n    <span class=\"hljs-comment\">// render only; subsequent renders return the cached instance. Using</span>\n    <span class=\"hljs-comment\">// UseState here would eagerly evaluate `new ElementRef&#x3C;T>(new ElementRef())`</span>\n    <span class=\"hljs-comment\">// on every render and throw the result away (UseState only consults</span>\n    <span class=\"hljs-comment\">// the initial value on first call), which would defeat the point of a</span>\n    <span class=\"hljs-comment\">// cheap stable ref hook.</span>\n    <span class=\"hljs-keyword\">return</span> ctx.UseMemo(<span class=\"hljs-keyword\">static</span> () => <span class=\"hljs-keyword\">new</span> ElementRef&#x3C;T>(<span class=\"hljs-keyword\">new</span> ElementRef()), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n}\n</code></pre>\n<p><code>UseMemo&#x3C;T>(factory, deps)</code> allocates a <code>MemoHookState&#x3C;T></code> and stores\nthe computed value plus the deps it was computed against. Subsequent\nrenders compare deps; on miss, run the factory and store; on hit,\nreturn the cached value. The factory is <em>not</em> an effect — it runs\ninside <code>Render()</code>, on the render path, so it must be pure (no\nsetters, no side effects).</p>\n<p><code>UseElementRef</code> is the canonical example of building a hook out of\n<code>UseMemo</code> with empty deps. The static lambda <code>() => new ElementRef&#x3C;T>(new ElementRef())</code>\nruns on the first render only; every subsequent render returns the\nsame <code>ElementRef&#x3C;T></code> instance. Storing the ref in <code>UseState</code> would\nre-allocate on every render and throw the result away — <code>UseState</code>'s\ninitial value is consulted only on first call, but the allocation\nhappens unconditionally.</p>\n<p>This is the general pattern for custom hooks. Compose existing hooks\ninside a regular method; the slot table doesn't care whether the\nhook call comes from the component's <code>Render()</code> directly or from a\nhelper called by <code>Render()</code>, as long as call order is stable.</p>\n<h2 id=\"useref--the-mutable-cell\">UseRef — the mutable cell<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useref--the-mutable-cell\">#</a></h2>\n<p><code>UseRef&#x3C;T>(initial)</code> allocates a <code>RefHookState&#x3C;T></code> holding a <code>Ref&#x3C;T></code>\nwrapper. The wrapper exposes <code>Value</code> as a get/set property; mutating\nit does <em>not</em> schedule a re-render — that's the entire point.\nReach for <code>UseRef</code> when you need a value that persists across renders\nbut the UI doesn't depend on it: a counter you log, a debounce\ntimer ID, the previous value of a state cell, a stable target for an\nexternal library that wants a long-lived reference.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> renderCount = UseRef(<span class=\"hljs-number\">0</span>);\nrenderCount.Value++;  <span class=\"hljs-comment\">// does NOT re-render</span>\n\n<span class=\"hljs-keyword\">var</span> prev = UseRef(<span class=\"hljs-string\">\"\"</span>);\nUseEffect(() => { prev.Value = name; }, name);\n</code></pre>\n<p>If you find yourself wanting <code>UseRef</code> to trigger re-renders, you\nprobably want <code>UseState</code> instead. <code>UseRef</code>'s value is an escape hatch\nfor state that shouldn't drive UI.</p>\n<h2 id=\"useobservable-useresource-usecontext\">UseObservable, UseResource, UseContext<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#useobservable-useresource-usecontext\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PendingScope</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Dictionary&#x3C;<span class=\"hljs-built_in\">object</span>, <span class=\"hljs-built_in\">bool</span>> _loadingByToken = <span class=\"hljs-keyword\">new</span>(capacity: <span class=\"hljs-number\">4</span>);\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">object</span> _lock = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Fires when a resource joins, leaves, or changes its loading state.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> Action? Changed;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Start tracking <span class=\"hljs-doctag\">&#x3C;paramref name=\"token\"/></span> with the given initial <span class=\"hljs-doctag\">&#x3C;paramref name=\"isLoading\"/></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> state. A hook typically uses its own <span class=\"hljs-doctag\">&#x3C;c></span>this<span class=\"hljs-doctag\">&#x3C;/c></span>-equivalent as the token.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Register</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span> token, <span class=\"hljs-built_in\">bool</span> isLoading</span>)</span>\n    {\n        <span class=\"hljs-keyword\">lock</span> (_lock) _loadingByToken[token] = isLoading;\n        Changed?.Invoke();\n    }\n</code></pre>\n<p><code>UseObservable&#x3C;T></code> subscribes the current component to an\n<code>INotifyPropertyChanged</code> source. Internally it builds an effect that\nattaches <code>PropertyChanged += ...</code> and stores the latest value in a\nlocal <code>UseState</code>. Cleanup unsubscribes.</p>\n<p><code>UseResource&#x3C;T></code> (and <code>UseInfiniteResource</code>, <code>UseMutation</code>) walks a\nfuller slot shape — a <code>ResourceHookState&#x3C;T></code> carrying the cache key,\nthe latest value, the in-flight cancellation token, and a registration\nwith the nearest <a href=\"/async-resources/\"><code>PendingScope</code></a>\nancestor. The async-resource hooks are documented end-to-end in the\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/reference/async-system.md\">async-system reference</a>; the slot is\nthe same shape category — a thing the component renders against, with\ncleanup that releases external state on unmount.</p>\n<p><code>UseContext&#x3C;T></code> reads from the <a href=\"/context/\">Context</a> value the\nnearest ancestor pushed via a <code>.With(...)</code> modifier. The slot holds\nthe subscription so a change to the context value (via the publishing\nparent's re-render) marks the consuming component dirty.</p>\n<h2 id=\"function-components\">Function components<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#function-components\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">internal</span> RenderContext Context { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override to describe the UI. Use UseState, UseEffect, etc. from the context.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Must call hooks in the same order every render.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> Element <span class=\"hljs-title\">Render</span>()</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Controls whether this propless component should re-render when its parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default: false — propless components only re-render from their own state changes or context changes.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override and return true to always re-render when the parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">virtual</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>()</span> => <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p><code>Func(ctx => Render(ctx))</code> materializes as a function-component\nelement. The reconciler treats it like any other component — element\nrecord on the tree, mounted into a slot in the parent — but the\n\"component instance\" is an internal wrapper holding its own\n<code>RenderContext</code>. The wrapper instance is keyed by position in the\nparent's render output (or by <code>.WithKey(...)</code> if set), so its hook\nstate persists across the parent's re-renders the same way a <code>class MyComponent : Component</code> would.</p>\n<p>The implication is that hooks called inside the <code>ctx => ...</code> lambda\nmust follow the same rules as hooks in a class component's\n<code>Render()</code>. The lint and analyzer rules don't distinguish: same call\norder, no conditionals, no varying-count loops.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"building-a-custom-hook\">Building a custom hook<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#building-a-custom-hook\">#</a></h3>\n<p>A custom hook is a regular method that calls existing hooks. The\nslot table doesn't care; only call order matters. The convention is\nthe <code>Use</code> prefix so the analyzer can apply hook-rules at the call\nsite:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> (<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> Value, Action&#x3C;<span class=\"hljs-built_in\">string</span>> Set</span>) <span class=\"hljs-title\">UseDebouncedText</span>(<span class=\"hljs-params\">\n    <span class=\"hljs-keyword\">this</span> RenderContext ctx, <span class=\"hljs-built_in\">string</span> initial, TimeSpan delay</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> (<span class=\"hljs-keyword\">value</span>, setValue) = ctx.UseState(initial);\n    <span class=\"hljs-keyword\">var</span> (debounced, setDebounced) = ctx.UseState(initial);\n    ctx.UseEffect(() =>\n    {\n        <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n        _ = Task.Delay(delay, cts.Token).ContinueWith(\n            _ => setDebounced(<span class=\"hljs-keyword\">value</span>),\n            TaskContinuationOptions.OnlyOnRanToCompletion);\n        <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n    }, <span class=\"hljs-keyword\">value</span>);\n    <span class=\"hljs-keyword\">return</span> (debounced, setValue);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-title\">ElementRef</span>&#x3C;<span class=\"hljs-title\">T</span>> <span class=\"hljs-title\">UseElementRef</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> RenderContext ctx</span>)\n    <span class=\"hljs-keyword\">where</span> T : FrameworkElement</span>\n{\n    <span class=\"hljs-keyword\">if</span> (ctx <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentNullException(<span class=\"hljs-keyword\">nameof</span>(ctx));\n\n    <span class=\"hljs-comment\">// UseMemo with empty deps allocates the typed+inner ref on the first</span>\n    <span class=\"hljs-comment\">// render only; subsequent renders return the cached instance. Using</span>\n    <span class=\"hljs-comment\">// UseState here would eagerly evaluate `new ElementRef&#x3C;T>(new ElementRef())`</span>\n    <span class=\"hljs-comment\">// on every render and throw the result away (UseState only consults</span>\n    <span class=\"hljs-comment\">// the initial value on first call), which would defeat the point of a</span>\n    <span class=\"hljs-comment\">// cheap stable ref hook.</span>\n    <span class=\"hljs-keyword\">return</span> ctx.UseMemo(<span class=\"hljs-keyword\">static</span> () => <span class=\"hljs-keyword\">new</span> ElementRef&#x3C;T>(<span class=\"hljs-keyword\">new</span> ElementRef()), Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n}\n</code></pre>\n<p>Every call to <code>UseDebouncedText</code> consumes three slots\n(<code>UseState</code>, <code>UseState</code>, <code>UseEffect</code>). Calling it conditionally\nbreaks the same way an inline conditional <code>UseState</code> would.</p>\n<h3 id=\"reading-the-previous-value-of-a-state\">Reading the previous value of a state<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-the-previous-value-of-a-state\">#</a></h3>\n<p><code>UseRef</code> plus <code>UseEffect</code> gives you the previous render's value:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">var</span> prevCount = UseRef(<span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">var</span> previous = prevCount.Value;\nUseEffect(() => { prevCount.Value = count; }, count);\n</code></pre>\n<p><code>previous</code> carries the value <code>count</code> had before the most recent\nupdate. The mutation inside the effect doesn't re-render; the next\nrender reads the new value via the <code>Ref&#x3C;T></code> cell.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"conditional-hook\">Conditional hook<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#conditional-hook\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">if</span> (showCounter)\n    {\n        <span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);   <span class=\"hljs-comment\">// slot count varies per render</span>\n        <span class=\"hljs-keyword\">return</span> Button(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{count}</span>\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>));\n    }\n    <span class=\"hljs-keyword\">return</span> Text(<span class=\"hljs-string\">\"hidden\"</span>);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p>When <code>showCounter</code> flips from true to false, slot 0 (which had been\n<code>ValueHookState&#x3C;int></code>) is gone from the slot table on that render —\nor rather, the slot-table position 0 is now whatever hook the next\n<code>Render()</code> happens to call first. The runtime check throws\n<code>HookOrderException</code> on the first hook whose slot type no longer\nmatches. Hoist the <code>UseState</code> to unconditional, branch on the value\n<em>inside</em> the returned tree:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">var</span> (count, setCount) = UseState(<span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">return</span> showCounter ? Button(<span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{count}</span>\"</span>, () => setCount(count + <span class=\"hljs-number\">1</span>)) : Text(<span class=\"hljs-string\">\"hidden\"</span>);\n</code></pre>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong><code>HookOrderException</code> is a structural error.</strong> Read the message,\nfind the hook whose slot type the runtime saw — that hook's call\nsite is where the slot table went out of sync. Often the actual\noffender is an earlier hook that ran on one render and not on\nanother; the visible failure is downstream.</p>\n<p><strong>Custom hooks don't need framework cooperation.</strong> Any extension\nmethod on <code>RenderContext</code> (or <code>Component</code>) that calls existing hooks\nis a hook. The <code>Use</code> prefix is convention plus what the\n<a href=\"/rules-of-reactor/\">analyzer</a> recognizes; the framework itself\ntreats hook calls uniformly.</p>\n<p><strong>Setters and refs are identity-stable.</strong> Setter closures, <code>Ref&#x3C;T></code>\ninstances from <code>UseRef</code>, and the <code>ElementRef&#x3C;T></code> from <code>UseElementRef</code>\nall return the <em>same</em> instance across renders. Pass them as effect\ndependencies safely; they won't trigger a restart.</p>\n<p><strong><code>UseMemo</code> runs during render — it is not a deferred computation.</strong>\nIf the factory does I/O, sleeps, or sets state, you wrote an effect\nby mistake. Move it to <code>UseEffect</code>.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Previous: the public hook surface.</li>\n<li><strong><a href=\"/reactivity-model/\">Reactivity Model</a></strong> — Why the setter is the signal.</li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — Next: the FlushEffects pass after reconcile.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture Overview</a></strong> — The full render loop.</li>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — The lint-enforced hook-call-order rules.</li>\n</ul>",
      "text": "A hook in Microsoft.UI.Reactor (Reactor) is a position. When Render() runs, the framework counts each hook call in order — UseState is slot 0, then UseEffect is slot 1, then UseMemo is slot 2 — and stores whatever each hook needs in a List&#x3C;HookState> on the owning RenderContext . The next render starts from _hookIndex = 0 again and walks the same list in the same order. The slot at position 0 had better still be the one UseState expects to find there, because the setter closure UseState returned on the first render captured the literal index 0 and writes back to that slot when you call it. This is the entire mechanical foundation of every hook rule you've ever read: \"same hooks in the same order every render\" is the runtime invariant the slot table enforces. Get it wrong — a conditional UseState inside an if , a hook inside a loop whose iteration count changed — and the type check on the slot throws HookOrderException because the slot that used to hold ValueHookState&#x3C;int> now sees a EffectHookState asking for its bucket. Hooks Internals # This page absorbs the legacy docs/reference/state-and-hooks.md and sits underneath the surface-level Hooks reference. The goal is one description of the storage shape behind every hook — enough to debug a HookOrderException , predict whether UseState returns a stable setter, and know when reaching for UseRef is correct. The slot table # Slot type Holds Created by ValueHookState&#x3C;T> Value, optional lock UseState , UseReducer EffectHookState Deps, body, cleanup, pending flag UseEffect MemoHookState&#x3C;T> Deps, cached value UseMemo , UseCallback , UseElementRef RefHookState&#x3C;T> Mutable cell UseRef ResourceHookState&#x3C;T> Cache key, state, cleanup UseResource , UseInfiniteResource ContextHookState Subscribed context keys UseContext Each subsection below covers one row of that table and the contract its hook surface depends on. RenderContext owns the table # public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; RenderContext holds the List&#x3C;HookState> _hooks and an int _hookIndex . BeginRender(requestRerender) sets _hookIndex = 0 and captures the UI thread ID; the component's Render() runs, hooks advance _hookIndex as they go, and after Render() returns the reconciler reads _hooks to know which effects to flush. There is one RenderContext per Component instance (and one per Func(ctx => …) function-component invocation — function components get their own context per parent's render, with state preserved across that parent's re-renders via positional matching just like any other element). The slot-type check at the top of each hook is the runtime guard: if (_hooks[currentIndex] is not ValueHookState&#x3C;T> hook) throw new HookOrderException(...); That check is the reason hook rules exist. A conditional hook ( if (cond) UseState(...) ) advances the slot table on some renders but not others, so a later hook in the same Render() reads a slot holding a different state type, and the cast fails. The REACTOR_HOOKS_001..007 analyzer family flags the most common shapes at build time so the runtime check stays an assertion of last resort. UseState — the canonical slot # public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; UseState allocates a ValueHookState&#x3C;T> on first render (`_hookIndex = _hooks.Count means \"this slot doesn't exist yet\"), captures currentIndex = _hookIndex , increments, and reads the value. The returned setter is a local function Setter closed over currentIndex and the context's _hooks` field — the index never changes for the lifetime of the component, which is why setter identity is stable across renders. void Setter ( T newValue ) { var h = (ValueHookState&#x3C;T>)_hooks[currentIndex]; bool changed; if (h.ThreadSafe) { lock (h.Lock) { changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; } if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } else { if (MarshalIfOffUIThread( \"UseState\" , () => Setter(newValue))) return ; changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } } The setter is also where the auto-marshal, the equality check, and the rerender request live. Calling it on a background thread takes the MarshalIfOffUIThread branch and re-enqueues the setter on the UI dispatcher; calling it from the UI thread runs the body inline. Either way, the slot is written with the new value, an ETW StateChange event fires if state-keyword tracing is enabled, and _requestRerender triggers the host to render again on the next dispatcher tick. The reactivity contract — equality short-circuit, single signal — is described end-to-end on the Reactivity Model page; the implementation lives here. Caveat: The setter's closure captures the currentIndex value , not the hookIndex variable . That means it points at slot N forever, even if a future render adds hooks before it — adding a hook before this one shifts every subsequent slot's intended type, and the next render will throw HookOrderException from the type cast. There's no recovery path; the component has to be unmounted (or the source fixed and hot-reload triggered, which remounts). UseEffect — the slot that runs later # public void UseEffect ( Action effect, params object [] dependencies ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new EffectHookState { Dependencies = null , Effect = effect }); } if (_hooks[_hookIndex] is not EffectHookState hook) throw new HookOrderException( $\"Hook at index {_hookIndex} is {_hooks[_hookIndex].GetType().Name} , expected EffectHookState. \" + \"Hooks must be called in the same order every render.\" ); _hookIndex++; if (hook.Dependencies is null || !DepsEqual(hook.Dependencies, dependencies)) { hook.PendingCleanup = hook.Cleanup; hook.Cleanup = null ; hook.Dependencies = dependencies.ToArray(); hook.Effect = effect; hook.Pending = true ; } } UseEffect doesn't run the body during Render() . It allocates an EffectHookState if needed, compares the dependencies against the previous render's deps ( DepsEqual uses object.Equals element-wise), and if they differ — or if this is the first render — it stashes the old cleanup as PendingCleanup , replaces the deps and body, and flips Pending = true . The reconciler calls FlushEffectsTraced after commit; flush runs all PendingCleanup callbacks first, then all Effect bodies, both in slot order. The cleanup-before-body ordering is load-bearing: a cts.Cancel() / timer.Dispose() cleanup actually disposes the previous timer before the new body creates a new one. The Effects Scheduling page covers the timing contract; this is the slot-shape that implements it. The reason UseEffect(body, deps) returns nothing and the cleanup mechanism uses a closure-returned Func&#x3C;Action> (the overload UseEffect(Func&#x3C;Action>) ) is to avoid eagerly walking through every hook's \"what if this cleans up\" plumbing. Effects that need cleanup opt in by returning an action; the slot holds a null cleanup otherwise. UseMemo — the cached-value slot # public static ElementRef &#x3C; T > UseElementRef &#x3C; T >( this RenderContext ctx ) where T : FrameworkElement { if (ctx is null ) throw new ArgumentNullException( nameof (ctx)); // UseMemo with empty deps allocates the typed+inner ref on the first // render only; subsequent renders return the cached instance. Using // UseState here would eagerly evaluate `new ElementRef&#x3C;T>(new ElementRef())` // on every render and throw the result away (UseState only consults // the initial value on first call), which would defeat the point of a // cheap stable ref hook. return ctx.UseMemo( static () => new ElementRef&#x3C;T>( new ElementRef()), Array.Empty&#x3C; object >()); } UseMemo&#x3C;T>(factory, deps) allocates a MemoHookState&#x3C;T> and stores the computed value plus the deps it was computed against. Subsequent renders compare deps; on miss, run the factory and store; on hit, return the cached value. The factory is not an effect — it runs inside Render() , on the render path, so it must be pure (no setters, no side effects). UseElementRef is the canonical example of building a hook out of UseMemo with empty deps. The static lambda () => new ElementRef&#x3C;T>(new ElementRef()) runs on the first render only; every subsequent render returns the same ElementRef&#x3C;T> instance. Storing the ref in UseState would re-allocate on every render and throw the result away — UseState 's initial value is consulted only on first call, but the allocation happens unconditionally. This is the general pattern for custom hooks. Compose existing hooks inside a regular method; the slot table doesn't care whether the hook call comes from the component's Render() directly or from a helper called by Render() , as long as call order is stable. UseRef — the mutable cell # UseRef&#x3C;T>(initial) allocates a RefHookState&#x3C;T> holding a Ref&#x3C;T> wrapper. The wrapper exposes Value as a get/set property; mutating it does not schedule a re-render — that's the entire point. Reach for UseRef when you need a value that persists across renders but the UI doesn't depend on it: a counter you log, a debounce timer ID, the previous value of a state cell, a stable target for an external library that wants a long-lived reference. var renderCount = UseRef( 0 ); renderCount.Value++; // does NOT re-render var prev = UseRef( \"\" ); UseEffect(() => { prev.Value = name; }, name); If you find yourself wanting UseRef to trigger re-renders, you probably want UseState instead. UseRef 's value is an escape hatch for state that shouldn't drive UI. UseObservable, UseResource, UseContext # public sealed class PendingScope { private readonly Dictionary&#x3C; object , bool > _loadingByToken = new (capacity: 4 ); private readonly object _lock = new (); /// &#x3C;summary> Fires when a resource joins, leaves, or changes its loading state. &#x3C;/summary> public event Action? Changed; /// &#x3C;summary> /// Start tracking &#x3C;paramref name=\"token\"/> with the given initial &#x3C;paramref name=\"isLoading\"/> /// state. A hook typically uses its own &#x3C;c> this &#x3C;/c> -equivalent as the token. /// &#x3C;/summary> public void Register ( object token, bool isLoading ) { lock (_lock) _loadingByToken[token] = isLoading; Changed?.Invoke(); } UseObservable&#x3C;T> subscribes the current component to an INotifyPropertyChanged source. Internally it builds an effect that attaches PropertyChanged += ... and stores the latest value in a local UseState . Cleanup unsubscribes. UseResource&#x3C;T> (and UseInfiniteResource , UseMutation ) walks a fuller slot shape — a ResourceHookState&#x3C;T> carrying the cache key, the latest value, the in-flight cancellation token, and a registration with the nearest PendingScope ancestor. The async-resource hooks are documented end-to-end in the async-system reference ; the slot is the same shape category — a thing the component renders against, with cleanup that releases external state on unmount. UseContext&#x3C;T> reads from the Context value the nearest ancestor pushed via a .With(...) modifier. The slot holds the subscription so a change to the context value (via the publishing parent's re-render) marks the consuming component dirty. Function components # public abstract class Component { internal RenderContext Context { get ; } = new (); /// &#x3C;summary> /// Override to describe the UI. Use UseState, UseEffect, etc. from the context. /// Must call hooks in the same order every render. /// &#x3C;/summary> public abstract Element Render () ; /// &#x3C;summary> /// Controls whether this propless component should re-render when its parent re-renders. /// Default: false — propless components only re-render from their own state changes or context changes. /// Override and return true to always re-render when the parent re-renders. /// &#x3C;/summary> protected internal virtual bool ShouldUpdate () => false ; Func(ctx => Render(ctx)) materializes as a function-component element. The reconciler treats it like any other component — element record on the tree, mounted into a slot in the parent — but the \"component instance\" is an internal wrapper holding its own RenderContext . The wrapper instance is keyed by position in the parent's render output (or by .WithKey(...) if set), so its hook state persists across the parent's re-renders the same way a class MyComponent : Component would. The implication is that hooks called inside the ctx => ... lambda must follow the same rules as hooks in a class component's Render() . The lint and analyzer rules don't distinguish: same call order, no conditionals, no varying-count loops. Patterns # Building a custom hook # A custom hook is a regular method that calls existing hooks. The slot table doesn't care; only call order matters. The convention is the Use prefix so the analyzer can apply hook-rules at the call site: public static ( string Value, Action&#x3C; string > Set ) UseDebouncedText ( this RenderContext ctx, string initial, TimeSpan delay ) { var ( value , setValue) = ctx.UseState(initial); var (debounced, setDebounced) = ctx.UseState(initial); ctx.UseEffect(() => { var cts = new CancellationTokenSource(); _ = Task.Delay(delay, cts.Token).ContinueWith( _ => setDebounced( value ), TaskContinuationOptions.OnlyOnRanToCompletion); return () => cts.Cancel(); }, value ); return (debounced, setValue); } public static ElementRef &#x3C; T > UseElementRef &#x3C; T >( this RenderContext ctx ) where T : FrameworkElement { if (ctx is null ) throw new ArgumentNullException( nameof (ctx)); // UseMemo with empty deps allocates the typed+inner ref on the first // render only; subsequent renders return the cached instance. Using // UseState here would eagerly evaluate `new ElementRef&#x3C;T>(new ElementRef())` // on every render and throw the result away (UseState only consults // the initial value on first call), which would defeat the point of a // cheap stable ref hook. return ctx.UseMemo( static () => new ElementRef&#x3C;T>( new ElementRef()), Array.Empty&#x3C; object >()); } Every call to UseDebouncedText consumes three slots ( UseState , UseState , UseEffect ). Calling it conditionally breaks the same way an inline conditional UseState would. Reading the previous value of a state # UseRef plus UseEffect gives you the previous render's value: var (count, setCount) = UseState( 0 ); var prevCount = UseRef( 0 ); var previous = prevCount.Value; UseEffect(() => { prevCount.Value = count; }, count); previous carries the value count had before the most recent update. The mutation inside the effect doesn't re-render; the next render reads the new value via the Ref&#x3C;T> cell. Common Mistakes # Conditional hook # // Don't: public override Element Render () { if (showCounter) { var (count, setCount) = UseState( 0 ); // slot count varies per render return Button( $\" {count} \" , () => setCount(count + 1 )); } return Text( \"hidden\" ); } public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; When showCounter flips from true to false, slot 0 (which had been ValueHookState&#x3C;int> ) is gone from the slot table on that render — or rather, the slot-table position 0 is now whatever hook the next Render() happens to call first. The runtime check throws HookOrderException on the first hook whose slot type no longer matches. Hoist the UseState to unconditional, branch on the value inside the returned tree: var (count, setCount) = UseState( 0 ); return showCounter ? Button( $\" {count} \" , () => setCount(count + 1 )) : Text( \"hidden\" ); Tips # HookOrderException is a structural error. Read the message, find the hook whose slot type the runtime saw — that hook's call site is where the slot table went out of sync. Often the actual offender is an earlier hook that ran on one render and not on another; the visible failure is downstream. Custom hooks don't need framework cooperation. Any extension method on RenderContext (or Component ) that calls existing hooks is a hook. The Use prefix is convention plus what the analyzer recognizes; the framework itself treats hook calls uniformly. Setters and refs are identity-stable. Setter closures, Ref&#x3C;T> instances from UseRef , and the ElementRef&#x3C;T> from UseElementRef all return the same instance across renders. Pass them as effect dependencies safely; they won't trigger a restart. UseMemo runs during render — it is not a deferred computation. If the factory does I/O, sleeps, or sets state, you wrote an effect by mistake. Move it to UseEffect . Next Steps # Hooks — Previous: the public hook surface. Reactivity Model — Why the setter is the signal. Effects Scheduling — Next: the FlushEffects pass after reconcile. Architecture Overview — The full render loop. Rules of Reactor — The lint-enforced hook-call-order rules.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/hooks-internals.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/hooks-internals.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-slot-table",
          "text": "The slot table"
        },
        {
          "depth": 2,
          "id": "rendercontext-owns-the-table",
          "text": "RenderContext owns the table"
        },
        {
          "depth": 2,
          "id": "usestate--the-canonical-slot",
          "text": "UseState — the canonical slot"
        },
        {
          "depth": 2,
          "id": "useeffect--the-slot-that-runs-later",
          "text": "UseEffect — the slot that runs later"
        },
        {
          "depth": 2,
          "id": "usememo--the-cached-value-slot",
          "text": "UseMemo — the cached-value slot"
        },
        {
          "depth": 2,
          "id": "useref--the-mutable-cell",
          "text": "UseRef — the mutable cell"
        },
        {
          "depth": 2,
          "id": "useobservable-useresource-usecontext",
          "text": "UseObservable, UseResource, UseContext"
        },
        {
          "depth": 2,
          "id": "function-components",
          "text": "Function components"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "building-a-custom-hook",
          "text": "Building a custom hook"
        },
        {
          "depth": 3,
          "id": "reading-the-previous-value-of-a-state",
          "text": "Reading the previous value of a state"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "conditional-hook",
          "text": "Conditional hook"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Reconciliation",
      "path": "reconciliation.md",
      "slug": "/reconciliation/",
      "html": "<p>The reconciler is the only thing in Microsoft.UI.Reactor (Reactor) that writes to WinUI.\nEvery visual change you see on screen — a property update on a Button,\na new row in a list, a TabView item that just animated in — passes\nthrough one method, <code>Reconciler.Reconcile</code>, that takes the previous\nelement record, the new element record, and the existing WinUI control\n(if any), and decides among three outcomes: Mount a new control,\nUpdate the existing one in place, or Unmount and return the control to\nthe pool. The whole rest of Reactor exists to make those three\ndecisions cheap: immutable element records that can be compared\nfield-for-field, a hook system that re-renders only the components\nwhose state actually changed, an element pool that recycles the heavy\nWinUI classes, and a child-diff algorithm that handles list reorders\nin close to the theoretical minimum number of moves. Read this page\nbefore any of the others in the Under-the-hood track that mention\n\"the reconciler\" — and read it once, end-to-end, before reaching\nfor a debugger when something rerenders incorrectly.</p>\n<h1 id=\"reconciliation\">Reconciliation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reconciliation\">#</a></h1>\n<p>This page absorbs the legacy <code>docs/reference/reconciliation.md</code>. The\ngoal is one place to learn how Reactor diffs trees — the same\ninformation, in the structure the rest of the guide uses, plus the\nsource pointers that let you verify it.</p>\n<h2 id=\"tri-state-dispatch\">Tri-state dispatch<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tri-state-dispatch\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/reconciliation/mount-update-unmount.svg\" alt=\"Reconciler tri-state dispatch: Mount, Update, Unmount; pool rent on mount; pool return on unmount\"></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Inputs</th><th>Path</th><th>Outcome</th></tr></thead><tbody><tr><td><code>newEl</code> is null or <code>EmptyElement</code>, <code>existingControl</code> exists</td><td>Unmount</td><td><code>Unmount</code> runs effect cleanups, returns control to pool</td></tr><tr><td><code>oldEl</code> is null/Empty, <code>existingControl</code> is null</td><td>Mount</td><td><code>Mount</code> rents from pool or allocates, populates, returns new control</td></tr><tr><td>both elements present, <code>CanUpdate(old, new)</code> returns true</td><td>Update</td><td><code>UpdateXxx</code> patches changed properties only; child lists diff</td></tr><tr><td>both present, <code>CanUpdate</code> false (type changed)</td><td>Replace</td><td>Unmount then Mount; the new control replaces the old in the parent</td></tr></tbody></table>\n<p>Every section below explains how one of those rows is implemented.</p>\n<h2 id=\"reconcile--the-entry-point\">Reconcile — the entry point<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reconcile--the-entry-point\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> UIElement? Reconcile(\n    Element? oldElement,\n    Element? newElement,\n    UIElement? existingControl,\n    Action requestRerender)\n{\n    <span class=\"hljs-comment\">// Trace only top-level reconcile passes (depth == 0) to avoid flooding</span>\n    <span class=\"hljs-comment\">// the provider with per-subtree entries; nested Reconcile() calls during</span>\n    <span class=\"hljs-comment\">// the same pass don't emit their own start/stop. Gate the depth counter</span>\n    <span class=\"hljs-comment\">// and Start emit on IsEnabled so the disabled path pays nothing extra.</span>\n    <span class=\"hljs-built_in\">bool</span> emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled(\n        <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Informational,\n        Diagnostics.ReactorEventSource.Keywords.Reconcile)\n        &#x26;&#x26; _reconcileTraceDepth++ == <span class=\"hljs-number\">0</span>;\n    <span class=\"hljs-keyword\">if</span> (emitTrace)\n    {\n        Diagnostics.ReactorEventSource.Log.ReconcileStart(\n            newElement?.GetType().Name ?? <span class=\"hljs-string\">\"null\"</span>);\n    }\n    <span class=\"hljs-keyword\">if</span> (_debugReconcileDepth++ == <span class=\"hljs-number\">0</span>)\n    {\n        DebugElementsDiffed = <span class=\"hljs-number\">0</span>;\n        DebugElementsSkipped = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsCreated = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsModified = <span class=\"hljs-number\">0</span>;\n        <span class=\"hljs-keyword\">if</span> (ReactorFeatureFlags.HighlightReconcileChanges)\n        {\n            (_highlightMounted ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n            (_highlightModified ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n        }\n        <span class=\"hljs-comment\">// Consume the hot-reload signal exactly once per top-level pass so</span>\n        <span class=\"hljs-comment\">// every component re-runs Render() even when props/deps are unchanged.</span>\n        _forceFullRenderActive = ForceFullRenderPending;\n        ForceFullRenderPending = <span class=\"hljs-literal\">false</span>;\n\n        <span class=\"hljs-comment\">// Build the dirty-ancestor path. For every component node</span>\n        <span class=\"hljs-comment\">// whose SelfTriggered is true, walk up the realized visual</span>\n        <span class=\"hljs-comment\">// tree and add each ancestor control. Consumed by Update's</span>\n        <span class=\"hljs-comment\">// shallow-equality short-circuit so the walk can reach the</span>\n        <span class=\"hljs-comment\">// self-triggered descendant even when its ancestor element</span>\n        <span class=\"hljs-comment\">// records are structurally unchanged.</span>\n        PopulateDirtyAncestorPath();\n    }\n    <span class=\"hljs-keyword\">try</span> {\n    <span class=\"hljs-keyword\">try</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (newElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement)\n        {\n            <span class=\"hljs-keyword\">if</span> (existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                Unmount(existingControl);\n            <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n        }\n\n        <span class=\"hljs-keyword\">if</span> (oldElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement || existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> Mount(newElement, requestRerender);\n\n        <span class=\"hljs-keyword\">return</span> ReconcileImperative(oldElement, newElement, existingControl, requestRerender);\n</code></pre>\n<p><code>Reconcile</code> is short on purpose. The interesting work is in <code>Mount</code>,\n<code>Update</code>, and the child reconciler — this method just routes. The\nETW emit gate at the top is <code>IsEnabled</code>-guarded so the disabled path\ncosts a single read and branch. <code>_reconcileTraceDepth</code> makes sure\nnested <code>Reconcile</code> calls during the same pass don't emit their own\nstart/stop events; only the top-level pass logs.</p>\n<p>The reconciler holds debug counters (<code>DebugElementsDiffed</code>,\n<code>DebugElementsSkipped</code>, <code>DebugUIElementsCreated</code>,\n<code>DebugUIElementsModified</code>) reset at depth 0 and reported at the end of\nthe pass. The reconcile overlay in the <a href=\"/dev-tooling/\">dev menu</a>\nreads these.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The reconciler returns the <em>new</em> <code>UIElement</code> because Update can\nreplace the control rather than patching it: <code>CanUpdate</code> may return\ntrue for the type but the underlying WinUI control type may have\nchanged at runtime (e.g. a custom-type registry update), and the\nreconciler then signals \"replace me in your parent's <code>Children</code>\ncollection\". If you call <code>Reconcile</code> from a custom container and\nignore the return value, the parent keeps pointing at a control that's\nabout to be unmounted. The framework's own\n<a href=\"#child-reconciler\"><code>ChildReconciler</code></a> handles this; custom callers\nneed to mirror it.</p>\n</blockquote>\n<h2 id=\"mount--building-a-control-tree\">Mount — building a control tree<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#mount--building-a-control-tree\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> UIElement? Mount(Element element, Action requestRerender)\n{\n    <span class=\"hljs-comment\">// Unwrap legacy ModifiedElement (backward compat)</span>\n    ElementModifiers? modifiers = element.Modifiers;\n    <span class=\"hljs-keyword\">if</span> (element <span class=\"hljs-keyword\">is</span> ModifiedElement mod)\n    {\n        modifiers = mod.WrappedModifiers;\n        <span class=\"hljs-keyword\">if</span> (mod.Inner.Modifiers <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n            modifiers = modifiers.Merge(mod.Inner.Modifiers);\n        element = mod.Inner;\n    }\n</code></pre>\n<p><code>Mount</code> is a type-switch over <code>Element</code> subclasses. Each branch\n(<code>MountButton</code>, <code>MountText</code>, <code>MountStack</code>, …) allocates or rents a\nWinUI control, sets the properties the element specifies, recurses\ninto children, attaches event-trampoline handlers once at mount time,\nand returns the control. The 40+ MountXxx methods live in\n<code>Reconciler.Mount.cs</code> — see the <a href=\"/architecture-overview/\">Architecture\nOverview</a> source map for the structure.</p>\n<p>The reconciler maintains a context-value scope so children rendered\ninside <code>Mount</code> see the same <a href=\"/context/\">Context</a> values their\nparents pushed. Stagger scopes work the same way: a parent declaring\n<code>.WithStagger(delay)</code> pushes an index that descendants' enter\ntransitions consume.</p>\n<p><code>Mount</code> also threads through the <a href=\"/element-pool/\">element pool</a>. For\npoolable types, the framework tries to rent a recycled control before\nallocating — this is why long-running apps with lots of list churn\ndon't end up paying GC cost proportional to scroll velocity.</p>\n<h2 id=\"update--patching-in-place\">Update — patching in place<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#update--patching-in-place\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> UIElement? Reconcile(\n    Element? oldElement,\n    Element? newElement,\n    UIElement? existingControl,\n    Action requestRerender)\n{\n    <span class=\"hljs-comment\">// Trace only top-level reconcile passes (depth == 0) to avoid flooding</span>\n    <span class=\"hljs-comment\">// the provider with per-subtree entries; nested Reconcile() calls during</span>\n    <span class=\"hljs-comment\">// the same pass don't emit their own start/stop. Gate the depth counter</span>\n    <span class=\"hljs-comment\">// and Start emit on IsEnabled so the disabled path pays nothing extra.</span>\n    <span class=\"hljs-built_in\">bool</span> emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled(\n        <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Informational,\n        Diagnostics.ReactorEventSource.Keywords.Reconcile)\n        &#x26;&#x26; _reconcileTraceDepth++ == <span class=\"hljs-number\">0</span>;\n    <span class=\"hljs-keyword\">if</span> (emitTrace)\n    {\n        Diagnostics.ReactorEventSource.Log.ReconcileStart(\n            newElement?.GetType().Name ?? <span class=\"hljs-string\">\"null\"</span>);\n    }\n    <span class=\"hljs-keyword\">if</span> (_debugReconcileDepth++ == <span class=\"hljs-number\">0</span>)\n    {\n        DebugElementsDiffed = <span class=\"hljs-number\">0</span>;\n        DebugElementsSkipped = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsCreated = <span class=\"hljs-number\">0</span>;\n        DebugUIElementsModified = <span class=\"hljs-number\">0</span>;\n        <span class=\"hljs-keyword\">if</span> (ReactorFeatureFlags.HighlightReconcileChanges)\n        {\n            (_highlightMounted ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n            (_highlightModified ??= <span class=\"hljs-keyword\">new</span>()).Clear();\n        }\n        <span class=\"hljs-comment\">// Consume the hot-reload signal exactly once per top-level pass so</span>\n        <span class=\"hljs-comment\">// every component re-runs Render() even when props/deps are unchanged.</span>\n        _forceFullRenderActive = ForceFullRenderPending;\n        ForceFullRenderPending = <span class=\"hljs-literal\">false</span>;\n\n        <span class=\"hljs-comment\">// Build the dirty-ancestor path. For every component node</span>\n        <span class=\"hljs-comment\">// whose SelfTriggered is true, walk up the realized visual</span>\n        <span class=\"hljs-comment\">// tree and add each ancestor control. Consumed by Update's</span>\n        <span class=\"hljs-comment\">// shallow-equality short-circuit so the walk can reach the</span>\n        <span class=\"hljs-comment\">// self-triggered descendant even when its ancestor element</span>\n        <span class=\"hljs-comment\">// records are structurally unchanged.</span>\n        PopulateDirtyAncestorPath();\n    }\n    <span class=\"hljs-keyword\">try</span> {\n    <span class=\"hljs-keyword\">try</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (newElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement)\n        {\n            <span class=\"hljs-keyword\">if</span> (existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n                Unmount(existingControl);\n            <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n        }\n\n        <span class=\"hljs-keyword\">if</span> (oldElement <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> <span class=\"hljs-keyword\">or</span> EmptyElement || existingControl <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n            <span class=\"hljs-keyword\">return</span> Mount(newElement, requestRerender);\n\n        <span class=\"hljs-keyword\">return</span> ReconcileImperative(oldElement, newElement, existingControl, requestRerender);\n</code></pre>\n<p><code>Update</code> is the other type-switch. For each subclass, <code>UpdateXxx</code>\ncompares the old element's record fields against the new and writes\nonly the differences onto the existing WinUI control. <code>Button.Content</code>\nchanged? Update that DP. <code>IsEnabled</code> is the same? Skip. Modifiers like\nmargin and corner-radius diff the same way, just against the merged\n<code>ElementModifiers</code> records.</p>\n<p>The early-skip optimization is in two places. At the element level,\n<code>Element.CanSkipUpdate(oldEl, newEl)</code> returns true when the records\nare structurally identical (and have no theme bindings that need\nre-evaluation) — the reconciler can avoid the <code>children.Get(i)</code> COM\ncall entirely and just refresh the\n<a href=\"#tag-based-event-dispatch\">Tag</a> if the element carries callbacks. At\nthe property level, each <code>UpdateXxx</code> short-circuits per-property:\nunchanged property → no DP write.</p>\n<p><code>CanUpdate(old, new)</code> is the type-shape check: the same Element record\ntype, the same control-target type. Returns true for <code>(ButtonElement, ButtonElement)</code>; false for <code>(ButtonElement, ImageElement)</code> — type\nswap forces unmount + mount.</p>\n<h2 id=\"child-reconciler--keyed-vs-positional\">Child reconciler — keyed vs positional<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#child-reconciler--keyed-vs-positional\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Reconcile</span>(<span class=\"hljs-params\">\n    Element[] oldChildren,\n    Element[] newChildren,\n    IChildCollection children,\n    Reconciler reconciler,\n    Action requestRerender</span>)</span>\n{\n    <span class=\"hljs-comment\">// Filter out nulls and EmptyElements</span>\n    <span class=\"hljs-keyword\">var</span> oldFiltered = Filter(oldChildren);\n    <span class=\"hljs-keyword\">var</span> newFiltered = Filter(newChildren);\n\n    <span class=\"hljs-built_in\">bool</span> hasKeys = HasAnyKeys(oldFiltered) || HasAnyKeys(newFiltered);\n\n    <span class=\"hljs-comment\">// Spec 042 §6 — read the active Animations.Animate ambient once</span>\n    <span class=\"hljs-comment\">// per reconcile so insert / move / unmount paths can apply the</span>\n    <span class=\"hljs-comment\">// same kind without re-reading AsyncLocal for every child. Stays</span>\n    <span class=\"hljs-comment\">// null in the overwhelmingly common no-ambient case.</span>\n    <span class=\"hljs-keyword\">var</span> ambient = AnimationAmbient.Current;\n    AnimationKind? ambientKind = ambient <span class=\"hljs-keyword\">is</span> { HasEffect: <span class=\"hljs-literal\">true</span> } ? ambient.Kind : <span class=\"hljs-literal\">null</span>;\n\n    <span class=\"hljs-keyword\">if</span> (hasKeys)\n        ReconcileKeyed(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind);\n    <span class=\"hljs-keyword\">else</span>\n        ReconcilePositional(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind);\n}\n</code></pre>\n<p>A panel's <code>Children</code> is just a list. Diffing two lists isn't free; the\nnaive O(n²) \"compare every pair\" loses badly on large lists. Reactor\npicks one of two strategies based on whether any child carries a\n<code>Key</code>.</p>\n<p><strong>Positional.</strong> No keys → match by index. Walk both lists in parallel,\nupdate the common prefix in place, mount any extra elements at the\nend, unmount any leftover children. Cost: O(max(old, new)). The catch\nis that an insert at the front re-updates every element rather than\ndetecting the shift.</p>\n<p><strong>Keyed.</strong> Any child carries a key → run a four-phase algorithm:</p>\n<ol>\n<li>Strip the common prefix (children at the same index with the same\nkey + same shape).</li>\n<li>Strip the common suffix.</li>\n<li>The middle is the only part that can have moved. If it's a pure\ninsert (old middle empty) or pure remove (new middle empty),\nhandle it directly.</li>\n<li>Otherwise, run Longest Increasing Subsequence over the new\nmiddle's positions in the old middle. Elements in the LIS don't\nmove; everything else does. This is the minimum number of moves\nfor the given assignment.</li>\n</ol>\n<p>The keyed path is what makes <code>ForEach(items, item => Card(item).WithKey(item.Id))</code>\nbehave correctly when a row is inserted at the top of a hundred-row\nlist: one mount at index 0, no moves elsewhere — versus a hundred\nproperty writes in the positional path.</p>\n<h2 id=\"identity-and-key\">Identity and Key<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#identity-and-key\">#</a></h2>\n<p><code>Element.Key</code> is the identity primitive. Two elements with the same\nkey at different positions are treated as the same element by the\nkeyed child reconciler; two elements with different keys at the same\nposition are treated as different elements (force unmount + mount).\nWithout a key, identity follows position.</p>\n<pre><code class=\"hljs language-csharp\">ForEach(items, item => Card(item).WithKey(item.Id))\n</code></pre>\n<p>Use keys when:</p>\n<ul>\n<li>The list can reorder (sort, drag-reorder).</li>\n<li>Items can be inserted or removed in the middle.</li>\n<li>An item carries identity beyond its visual position (selection\nstate in a controlled-input pattern, focus, an in-flight async load).</li>\n</ul>\n<p>Don't use the array index as a key when the list can reorder — that\ndefeats the entire point. The <a href=\"/rules-of-reactor/\">REACTOR_DSL_001</a>\nanalyzer flags missing keys in <code>ForEach</code> and similar APIs.</p>\n<h2 id=\"tag-based-event-dispatch\">Tag-based event dispatch<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tag-based-event-dispatch\">#</a></h2>\n<p>The reconciler wires WinUI events (<code>Click</code>, <code>Tapped</code>, <code>TextChanged</code>,\n…) once at mount time. The handler doesn't capture the element's\ncallback directly — it would go stale after every re-render. Instead,\nthe reconciler stamps the current element onto the control via a\n<code>ReactorAttached</code> dependency property (the \"tag\"), and the handler\nreads the current element from <code>sender.Tag</code> and invokes whatever\ncallback is stored on the latest element.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Mount: wire once</span>\nbutton.Click += (sender, _) =>\n{\n    <span class=\"hljs-keyword\">var</span> el = Reconciler.GetElementTag&#x3C;ButtonElement>(sender);\n    el?.OnClick?.Invoke();\n};\n\n<span class=\"hljs-comment\">// Update: refresh the tag</span>\nReconciler.SetElementTag(button, newElement);\n</code></pre>\n<p>This is why <code>Element.HasCallbacks</code> is load-bearing in the early-skip\npath of the <a href=\"#child-reconciler-keyed-vs-positional\">child reconciler</a>\n— the skip short-circuits Update, so the tag has to be refreshed\nexplicitly or the trampoline keeps dispatching through the previous\nrender's closure (stale-state bug).</p>\n<h2 id=\"gap-nodes\">Gap nodes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#gap-nodes\">#</a></h2>\n<p>Some WinUI controls hold children in collections that aren't the\nfamiliar <code>Panel.Children</code>:</p>\n<ul>\n<li><strong>TabView</strong> — <code>TabItems</code></li>\n<li><strong>NavigationView</strong> — <code>MenuItems</code> plus <code>Content</code></li>\n<li><strong>TreeView</strong> — <code>RootNodes</code> with nested <code>Children</code></li>\n<li><strong>MenuBar / CommandBar</strong> — <code>Items</code> / <code>PrimaryCommands</code> / <code>SecondaryCommands</code></li>\n</ul>\n<p>The child reconciler walks <code>Panel.Children</code>-shaped containers\nuniformly. Gap nodes are handled imperatively in their respective\nMountXxx / UpdateXxx methods — the framework knows which collection\nto walk for each control type and runs a second-pass diff over it.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"stable-identity-through-state-changes\">Stable identity through state changes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#stable-identity-through-state-changes\">#</a></h3>\n<p>A keyed list whose items carry per-row state (selection, expansion,\nin-flight load) needs the key to stay stable across re-renders. The\ntypical mistake is to derive the key from a value that can change:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Stable: row identity persists across edits</span>\nForEach(rows, row => Card(row).WithKey(row.Id))\n\n<span class=\"hljs-comment\">// Unstable: changing the title changes the key, remounts the card,</span>\n<span class=\"hljs-comment\">// loses any state attached via UseState inside Card</span>\nForEach(rows, row => Card(row).WithKey(row.Title))\n</code></pre>\n<p>The reconciler isn't psychic. When the key changes, it unmounts the\nold element and mounts a new one — the per-row state, focus, and\nanimations restart from scratch. Use an immutable identifier.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<h3 id=\"custom-container-reconciliation\">Custom container reconciliation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#custom-container-reconciliation\">#</a></h3>\n<p>When you write a control that owns children (a custom panel, a\nthird-party container Reactor doesn't wrap), the integration point is\n<code>RegisterType</code> + a custom <code>ReconcileChildren</code> callback. The\n<a href=\"/architecture-overview/\">Architecture Overview</a> source-map row\n\"Reconciler\" lists the partial files where the type registry lives;\nthe third-party-controls test fixtures\n(<code>tests/Reactor.AppTests.ThirdPartyControls/</code>) are the canonical\nworked example.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"using-array-index-as-a-key\">Using array index as a key<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#using-array-index-as-a-key\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nForEach(items.Select((item, i) => (item, i)),\n        x => Card(x.item).WithKey(x.i.ToString()))\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Reconcile</span>(<span class=\"hljs-params\">\n    Element[] oldChildren,\n    Element[] newChildren,\n    IChildCollection children,\n    Reconciler reconciler,\n    Action requestRerender</span>)</span>\n{\n    <span class=\"hljs-comment\">// Filter out nulls and EmptyElements</span>\n    <span class=\"hljs-keyword\">var</span> oldFiltered = Filter(oldChildren);\n    <span class=\"hljs-keyword\">var</span> newFiltered = Filter(newChildren);\n\n    <span class=\"hljs-built_in\">bool</span> hasKeys = HasAnyKeys(oldFiltered) || HasAnyKeys(newFiltered);\n\n    <span class=\"hljs-comment\">// Spec 042 §6 — read the active Animations.Animate ambient once</span>\n    <span class=\"hljs-comment\">// per reconcile so insert / move / unmount paths can apply the</span>\n    <span class=\"hljs-comment\">// same kind without re-reading AsyncLocal for every child. Stays</span>\n    <span class=\"hljs-comment\">// null in the overwhelmingly common no-ambient case.</span>\n    <span class=\"hljs-keyword\">var</span> ambient = AnimationAmbient.Current;\n    AnimationKind? ambientKind = ambient <span class=\"hljs-keyword\">is</span> { HasEffect: <span class=\"hljs-literal\">true</span> } ? ambient.Kind : <span class=\"hljs-literal\">null</span>;\n\n    <span class=\"hljs-keyword\">if</span> (hasKeys)\n        ReconcileKeyed(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind);\n    <span class=\"hljs-keyword\">else</span>\n        ReconcilePositional(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind);\n}\n</code></pre>\n<p>An index-based key matches positions across renders, which is exactly\nwhat the <em>unkeyed</em> path already does. Worse, it positively asserts to\nthe reconciler that \"this element at index 3 is the same element as\nthe previous render's index 3\", so when an item is inserted at the\nfront, every keyed child shifts, every key mismatches its previous\nposition, and the keyed-path's LIS algorithm produces a worst-case\nmove list. Use the item's own identifier (database ID, GUID, content\nhash) or skip the key and let positional matching handle it.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Read <code>Reconciler.cs</code> as a routing function, not as the algorithm.</strong>\nThe interesting code is in <code>Reconciler.Mount.cs</code>,\n<code>Reconciler.Update.cs</code>, and <code>ChildReconciler.cs</code>. The orchestration\nfile is short; the per-type handlers are where the patching logic\nlives.</p>\n<p><strong>Keys aren't free — but they're cheap.</strong> Add a key whenever you can\npredict that the list will reorder or items will be inserted. Don't\nbother for static or append-only lists; positional matching is\noptimal there.</p>\n<p><strong><code>CanSkipUpdate</code> lives on <code>Element</code>.</strong> When you add a new element\ntype with callbacks, override <code>HasCallbacks</code> so the early-skip path\nrefreshes the tag. Forgetting this is the most common\nfresh-element-type bug; the symptom is \"the handler sees stale state\".</p>\n<p><strong>Reconciler overlays the truth.</strong> When something rerenders that\nshouldn't, enable the reconcile-highlight overlay\n(<a href=\"/dev-tooling/\">dev-tooling</a>) — it draws a border around every\ncontrol the reconciler touched on the last pass. If your \"unchanged\"\ncomponent lights up, the element-record comparison is finding a\ndifference somewhere; the overlay narrows the search to one row.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/reactivity-model/\">Reactivity Model</a></strong> — Previous: what brings the reconciler a new tree.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Where the state that drove this reconcile lives.</li>\n<li><strong><a href=\"/element-pool/\">Element Pool</a></strong> — Next: how Mount and Unmount talk to the pool.</li>\n<li><strong><a href=\"/effects-scheduling/\">Effects Scheduling</a></strong> — What runs after the reconciler commits.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture Overview</a></strong> — The full render-loop picture.</li>\n</ul>",
      "text": "The reconciler is the only thing in Microsoft.UI.Reactor (Reactor) that writes to WinUI. Every visual change you see on screen — a property update on a Button, a new row in a list, a TabView item that just animated in — passes through one method, Reconciler.Reconcile , that takes the previous element record, the new element record, and the existing WinUI control (if any), and decides among three outcomes: Mount a new control, Update the existing one in place, or Unmount and return the control to the pool. The whole rest of Reactor exists to make those three decisions cheap: immutable element records that can be compared field-for-field, a hook system that re-renders only the components whose state actually changed, an element pool that recycles the heavy WinUI classes, and a child-diff algorithm that handles list reorders in close to the theoretical minimum number of moves. Read this page before any of the others in the Under-the-hood track that mention \"the reconciler\" — and read it once, end-to-end, before reaching for a debugger when something rerenders incorrectly. Reconciliation # This page absorbs the legacy docs/reference/reconciliation.md . The goal is one place to learn how Reactor diffs trees — the same information, in the structure the rest of the guide uses, plus the source pointers that let you verify it. Tri-state dispatch # Inputs Path Outcome newEl is null or EmptyElement , existingControl exists Unmount Unmount runs effect cleanups, returns control to pool oldEl is null/Empty, existingControl is null Mount Mount rents from pool or allocates, populates, returns new control both elements present, CanUpdate(old, new) returns true Update UpdateXxx patches changed properties only; child lists diff both present, CanUpdate false (type changed) Replace Unmount then Mount; the new control replaces the old in the parent Every section below explains how one of those rows is implemented. Reconcile — the entry point # public UIElement? Reconcile( Element? oldElement, Element? newElement, UIElement? existingControl, Action requestRerender) { // Trace only top-level reconcile passes (depth == 0) to avoid flooding // the provider with per-subtree entries; nested Reconcile() calls during // the same pass don't emit their own start/stop. Gate the depth counter // and Start emit on IsEnabled so the disabled path pays nothing extra. bool emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Informational, Diagnostics.ReactorEventSource.Keywords.Reconcile) &#x26;&#x26; _reconcileTraceDepth++ == 0 ; if (emitTrace) { Diagnostics.ReactorEventSource.Log.ReconcileStart( newElement?.GetType().Name ?? \"null\" ); } if (_debugReconcileDepth++ == 0 ) { DebugElementsDiffed = 0 ; DebugElementsSkipped = 0 ; DebugUIElementsCreated = 0 ; DebugUIElementsModified = 0 ; if (ReactorFeatureFlags.HighlightReconcileChanges) { (_highlightMounted ??= new ()).Clear(); (_highlightModified ??= new ()).Clear(); } // Consume the hot-reload signal exactly once per top-level pass so // every component re-runs Render() even when props/deps are unchanged. _forceFullRenderActive = ForceFullRenderPending; ForceFullRenderPending = false ; // Build the dirty-ancestor path. For every component node // whose SelfTriggered is true, walk up the realized visual // tree and add each ancestor control. Consumed by Update's // shallow-equality short-circuit so the walk can reach the // self-triggered descendant even when its ancestor element // records are structurally unchanged. PopulateDirtyAncestorPath(); } try { try { if (newElement is null or EmptyElement) { if (existingControl is not null ) Unmount(existingControl); return null ; } if (oldElement is null or EmptyElement || existingControl is null ) return Mount(newElement, requestRerender); return ReconcileImperative(oldElement, newElement, existingControl, requestRerender); Reconcile is short on purpose. The interesting work is in Mount , Update , and the child reconciler — this method just routes. The ETW emit gate at the top is IsEnabled -guarded so the disabled path costs a single read and branch. _reconcileTraceDepth makes sure nested Reconcile calls during the same pass don't emit their own start/stop events; only the top-level pass logs. The reconciler holds debug counters ( DebugElementsDiffed , DebugElementsSkipped , DebugUIElementsCreated , DebugUIElementsModified ) reset at depth 0 and reported at the end of the pass. The reconcile overlay in the dev menu reads these. Caveat: The reconciler returns the new UIElement because Update can replace the control rather than patching it: CanUpdate may return true for the type but the underlying WinUI control type may have changed at runtime (e.g. a custom-type registry update), and the reconciler then signals \"replace me in your parent's Children collection\". If you call Reconcile from a custom container and ignore the return value, the parent keeps pointing at a control that's about to be unmounted. The framework's own ChildReconciler handles this; custom callers need to mirror it. Mount — building a control tree # public UIElement? Mount(Element element, Action requestRerender) { // Unwrap legacy ModifiedElement (backward compat) ElementModifiers? modifiers = element.Modifiers; if (element is ModifiedElement mod) { modifiers = mod.WrappedModifiers; if (mod.Inner.Modifiers is not null ) modifiers = modifiers.Merge(mod.Inner.Modifiers); element = mod.Inner; } Mount is a type-switch over Element subclasses. Each branch ( MountButton , MountText , MountStack , …) allocates or rents a WinUI control, sets the properties the element specifies, recurses into children, attaches event-trampoline handlers once at mount time, and returns the control. The 40+ MountXxx methods live in Reconciler.Mount.cs — see the Architecture Overview source map for the structure. The reconciler maintains a context-value scope so children rendered inside Mount see the same Context values their parents pushed. Stagger scopes work the same way: a parent declaring .WithStagger(delay) pushes an index that descendants' enter transitions consume. Mount also threads through the element pool . For poolable types, the framework tries to rent a recycled control before allocating — this is why long-running apps with lots of list churn don't end up paying GC cost proportional to scroll velocity. Update — patching in place # public UIElement? Reconcile( Element? oldElement, Element? newElement, UIElement? existingControl, Action requestRerender) { // Trace only top-level reconcile passes (depth == 0) to avoid flooding // the provider with per-subtree entries; nested Reconcile() calls during // the same pass don't emit their own start/stop. Gate the depth counter // and Start emit on IsEnabled so the disabled path pays nothing extra. bool emitTrace = Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Informational, Diagnostics.ReactorEventSource.Keywords.Reconcile) &#x26;&#x26; _reconcileTraceDepth++ == 0 ; if (emitTrace) { Diagnostics.ReactorEventSource.Log.ReconcileStart( newElement?.GetType().Name ?? \"null\" ); } if (_debugReconcileDepth++ == 0 ) { DebugElementsDiffed = 0 ; DebugElementsSkipped = 0 ; DebugUIElementsCreated = 0 ; DebugUIElementsModified = 0 ; if (ReactorFeatureFlags.HighlightReconcileChanges) { (_highlightMounted ??= new ()).Clear(); (_highlightModified ??= new ()).Clear(); } // Consume the hot-reload signal exactly once per top-level pass so // every component re-runs Render() even when props/deps are unchanged. _forceFullRenderActive = ForceFullRenderPending; ForceFullRenderPending = false ; // Build the dirty-ancestor path. For every component node // whose SelfTriggered is true, walk up the realized visual // tree and add each ancestor control. Consumed by Update's // shallow-equality short-circuit so the walk can reach the // self-triggered descendant even when its ancestor element // records are structurally unchanged. PopulateDirtyAncestorPath(); } try { try { if (newElement is null or EmptyElement) { if (existingControl is not null ) Unmount(existingControl); return null ; } if (oldElement is null or EmptyElement || existingControl is null ) return Mount(newElement, requestRerender); return ReconcileImperative(oldElement, newElement, existingControl, requestRerender); Update is the other type-switch. For each subclass, UpdateXxx compares the old element's record fields against the new and writes only the differences onto the existing WinUI control. Button.Content changed? Update that DP. IsEnabled is the same? Skip. Modifiers like margin and corner-radius diff the same way, just against the merged ElementModifiers records. The early-skip optimization is in two places. At the element level, Element.CanSkipUpdate(oldEl, newEl) returns true when the records are structurally identical (and have no theme bindings that need re-evaluation) — the reconciler can avoid the children.Get(i) COM call entirely and just refresh the Tag if the element carries callbacks. At the property level, each UpdateXxx short-circuits per-property: unchanged property → no DP write. CanUpdate(old, new) is the type-shape check: the same Element record type, the same control-target type. Returns true for (ButtonElement, ButtonElement) ; false for (ButtonElement, ImageElement) — type swap forces unmount + mount. Child reconciler — keyed vs positional # internal static void Reconcile ( Element[] oldChildren, Element[] newChildren, IChildCollection children, Reconciler reconciler, Action requestRerender ) { // Filter out nulls and EmptyElements var oldFiltered = Filter(oldChildren); var newFiltered = Filter(newChildren); bool hasKeys = HasAnyKeys(oldFiltered) || HasAnyKeys(newFiltered); // Spec 042 §6 — read the active Animations.Animate ambient once // per reconcile so insert / move / unmount paths can apply the // same kind without re-reading AsyncLocal for every child. Stays // null in the overwhelmingly common no-ambient case. var ambient = AnimationAmbient.Current; AnimationKind? ambientKind = ambient is { HasEffect: true } ? ambient.Kind : null ; if (hasKeys) ReconcileKeyed(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind); else ReconcilePositional(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind); } A panel's Children is just a list. Diffing two lists isn't free; the naive O(n²) \"compare every pair\" loses badly on large lists. Reactor picks one of two strategies based on whether any child carries a Key . Positional. No keys → match by index. Walk both lists in parallel, update the common prefix in place, mount any extra elements at the end, unmount any leftover children. Cost: O(max(old, new)). The catch is that an insert at the front re-updates every element rather than detecting the shift. Keyed. Any child carries a key → run a four-phase algorithm: Strip the common prefix (children at the same index with the same key + same shape). Strip the common suffix. The middle is the only part that can have moved. If it's a pure insert (old middle empty) or pure remove (new middle empty), handle it directly. Otherwise, run Longest Increasing Subsequence over the new middle's positions in the old middle. Elements in the LIS don't move; everything else does. This is the minimum number of moves for the given assignment. The keyed path is what makes ForEach(items, item => Card(item).WithKey(item.Id)) behave correctly when a row is inserted at the top of a hundred-row list: one mount at index 0, no moves elsewhere — versus a hundred property writes in the positional path. Identity and Key # Element.Key is the identity primitive. Two elements with the same key at different positions are treated as the same element by the keyed child reconciler; two elements with different keys at the same position are treated as different elements (force unmount + mount). Without a key, identity follows position. ForEach(items, item => Card(item).WithKey(item.Id)) Use keys when: The list can reorder (sort, drag-reorder). Items can be inserted or removed in the middle. An item carries identity beyond its visual position (selection state in a controlled-input pattern, focus, an in-flight async load). Don't use the array index as a key when the list can reorder — that defeats the entire point. The REACTOR_DSL_001 analyzer flags missing keys in ForEach and similar APIs. Tag-based event dispatch # The reconciler wires WinUI events ( Click , Tapped , TextChanged , …) once at mount time. The handler doesn't capture the element's callback directly — it would go stale after every re-render. Instead, the reconciler stamps the current element onto the control via a ReactorAttached dependency property (the \"tag\"), and the handler reads the current element from sender.Tag and invokes whatever callback is stored on the latest element. // Mount: wire once button.Click += (sender, _) => { var el = Reconciler.GetElementTag&#x3C;ButtonElement>(sender); el?.OnClick?.Invoke(); }; // Update: refresh the tag Reconciler.SetElementTag(button, newElement); This is why Element.HasCallbacks is load-bearing in the early-skip path of the child reconciler — the skip short-circuits Update, so the tag has to be refreshed explicitly or the trampoline keeps dispatching through the previous render's closure (stale-state bug). Gap nodes # Some WinUI controls hold children in collections that aren't the familiar Panel.Children : TabView — TabItems NavigationView — MenuItems plus Content TreeView — RootNodes with nested Children MenuBar / CommandBar — Items / PrimaryCommands / SecondaryCommands The child reconciler walks Panel.Children -shaped containers uniformly. Gap nodes are handled imperatively in their respective MountXxx / UpdateXxx methods — the framework knows which collection to walk for each control type and runs a second-pass diff over it. Patterns # Stable identity through state changes # A keyed list whose items carry per-row state (selection, expansion, in-flight load) needs the key to stay stable across re-renders. The typical mistake is to derive the key from a value that can change: // Stable: row identity persists across edits ForEach(rows, row => Card(row).WithKey(row.Id)) // Unstable: changing the title changes the key, remounts the card, // loses any state attached via UseState inside Card ForEach(rows, row => Card(row).WithKey(row.Title)) The reconciler isn't psychic. When the key changes, it unmounts the old element and mounts a new one — the per-row state, focus, and animations restart from scratch. Use an immutable identifier. public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } Custom container reconciliation # When you write a control that owns children (a custom panel, a third-party container Reactor doesn't wrap), the integration point is RegisterType + a custom ReconcileChildren callback. The Architecture Overview source-map row \"Reconciler\" lists the partial files where the type registry lives; the third-party-controls test fixtures ( tests/Reactor.AppTests.ThirdPartyControls/ ) are the canonical worked example. Common Mistakes # Using array index as a key # // Don't: ForEach(items.Select((item, i) => (item, i)), x => Card(x.item).WithKey(x.i.ToString())) internal static void Reconcile ( Element[] oldChildren, Element[] newChildren, IChildCollection children, Reconciler reconciler, Action requestRerender ) { // Filter out nulls and EmptyElements var oldFiltered = Filter(oldChildren); var newFiltered = Filter(newChildren); bool hasKeys = HasAnyKeys(oldFiltered) || HasAnyKeys(newFiltered); // Spec 042 §6 — read the active Animations.Animate ambient once // per reconcile so insert / move / unmount paths can apply the // same kind without re-reading AsyncLocal for every child. Stays // null in the overwhelmingly common no-ambient case. var ambient = AnimationAmbient.Current; AnimationKind? ambientKind = ambient is { HasEffect: true } ? ambient.Kind : null ; if (hasKeys) ReconcileKeyed(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind); else ReconcilePositional(oldFiltered, newFiltered, children, reconciler, requestRerender, ambientKind); } An index-based key matches positions across renders, which is exactly what the unkeyed path already does. Worse, it positively asserts to the reconciler that \"this element at index 3 is the same element as the previous render's index 3\", so when an item is inserted at the front, every keyed child shifts, every key mismatches its previous position, and the keyed-path's LIS algorithm produces a worst-case move list. Use the item's own identifier (database ID, GUID, content hash) or skip the key and let positional matching handle it. Tips # Read Reconciler.cs as a routing function, not as the algorithm. The interesting code is in Reconciler.Mount.cs , Reconciler.Update.cs , and ChildReconciler.cs . The orchestration file is short; the per-type handlers are where the patching logic lives. Keys aren't free — but they're cheap. Add a key whenever you can predict that the list will reorder or items will be inserted. Don't bother for static or append-only lists; positional matching is optimal there. CanSkipUpdate lives on Element . When you add a new element type with callbacks, override HasCallbacks so the early-skip path refreshes the tag. Forgetting this is the most common fresh-element-type bug; the symptom is \"the handler sees stale state\". Reconciler overlays the truth. When something rerenders that shouldn't, enable the reconcile-highlight overlay ( dev-tooling ) — it draws a border around every control the reconciler touched on the last pass. If your \"unchanged\" component lights up, the element-record comparison is finding a difference somewhere; the overlay narrows the search to one row. Next Steps # Reactivity Model — Previous: what brings the reconciler a new tree. Hooks Internals — Where the state that drove this reconcile lives. Element Pool — Next: how Mount and Unmount talk to the pool. Effects Scheduling — What runs after the reconciler commits. Architecture Overview — The full render-loop picture.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/reconciliation.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/reconciliation.md",
      "headings": [
        {
          "depth": 2,
          "id": "tri-state-dispatch",
          "text": "Tri-state dispatch"
        },
        {
          "depth": 2,
          "id": "reconcile--the-entry-point",
          "text": "Reconcile — the entry point"
        },
        {
          "depth": 2,
          "id": "mount--building-a-control-tree",
          "text": "Mount — building a control tree"
        },
        {
          "depth": 2,
          "id": "update--patching-in-place",
          "text": "Update — patching in place"
        },
        {
          "depth": 2,
          "id": "child-reconciler--keyed-vs-positional",
          "text": "Child reconciler — keyed vs positional"
        },
        {
          "depth": 2,
          "id": "identity-and-key",
          "text": "Identity and Key"
        },
        {
          "depth": 2,
          "id": "tag-based-event-dispatch",
          "text": "Tag-based event dispatch"
        },
        {
          "depth": 2,
          "id": "gap-nodes",
          "text": "Gap nodes"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "stable-identity-through-state-changes",
          "text": "Stable identity through state changes"
        },
        {
          "depth": 3,
          "id": "custom-container-reconciliation",
          "text": "Custom container reconciliation"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "using-array-index-as-a-key",
          "text": "Using array index as a key"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Control Reconciler Protocol",
      "path": "control-reconciler-protocol.md",
      "slug": "/control-reconciler-protocol/",
      "html": "<p>Microsoft.UI.Reactor (Reactor) draws the visible part of a frame by handing\neach immutable element record off to a per-control-type plug-in called a\n<strong>handler</strong>. The handler knows two things the <a href=\"/reconciliation/\">reconciler</a>\ndoesn't: which WinUI control to instantiate, and which of that control's\nproperties and events to wire to which element fields. The protocol is\ndesigned so authors do not pay for what they do not use — a handler that\ndeclares no children does no child dispatch, a controlled prop with no\ncallback does not subscribe its event trampoline, a one-way prop whose\nvalue did not change does not write — and so every built-in control and\nevery control you add yourself dispatches through the same tight loop.\nRead this page before adding a new native control or before reaching for\na debugger when an existing one mis-updates.</p>\n<h1 id=\"the-control-reconciler-protocol\">The Control Reconciler Protocol<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-control-reconciler-protocol\">#</a></h1>\n<p>This page documents the data path between\n<a href=\"/reconciliation/#reconcile--the-entry-point\"><code>Reconciler.Reconcile</code></a>\nand the WinUI control it edits. The audience is anyone extending\nReactor with a new native control, or anyone debugging why an\nexisting element does not update the way it should. The companion\n<a href=\"/extending-reactor-controls/\">Extending Reactor with Native Controls</a>\nwalks through adding a control end-to-end; this page is the\nreference under that recipe.</p>\n<h2 id=\"dispatch--one-handler-per-element-type\">Dispatch — one handler per element type<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dispatch--one-handler-per-element-type\">#</a></h2>\n<p>The reconciler holds a dictionary keyed by <code>System.Type</code> of the\nelement record. When a re-render passes a new element to\n<code>Reconcile</code>, the dictionary entry for that element's exact runtime\ntype is fetched, and that entry's <code>Mount</code> / <code>Update</code> / <code>Unmount</code>\nmethods run against the existing WinUI control. There is no base-class\nfallback for ordinary handlers — registering <code>ButtonElement</code> does\n<strong>not</strong> also catch a <code>MyButtonElement</code> that inherits from it; each\nconcrete element type has its own registration. (The one exception,\n<a href=\"#registration\"><code>RegisterHandlerForDerivedTypes</code></a>, exists for the\nT-erased templated-list family and is called out explicitly below.)</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> UIElement <span class=\"hljs-title\">Mount</span>(<span class=\"hljs-params\">Element element, Action requestRerender, Reconciler reconciler</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> typedEl = (TElement)element;\n    <span class=\"hljs-keyword\">var</span> ctx = <span class=\"hljs-keyword\">new</span> MountContext(reconciler, requestRerender);\n    <span class=\"hljs-keyword\">var</span> control = _handler.Mount(ctx, typedEl);\n\n    <span class=\"hljs-comment\">// Anchor element identity on the control via the attached state DP so</span>\n    <span class=\"hljs-comment\">// event trampolines can re-fetch the live element on each fire.</span>\n    <span class=\"hljs-keyword\">if</span> (control <span class=\"hljs-keyword\">is</span> FrameworkElement fe)\n        Reconciler.SetElementTag(fe, typedEl);\n\n    <span class=\"hljs-comment\">// Strategy dispatch — only when the handler declares a non-None Children strategy.</span>\n    <span class=\"hljs-keyword\">var</span> strategy = _handler.Children;\n    <span class=\"hljs-keyword\">if</span> (strategy <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n        DispatchChildrenMount(strategy, ctx, typedEl, control);\n\n    <span class=\"hljs-comment\">// Post-children mount hook. Fires after every child has mounted</span>\n    <span class=\"hljs-comment\">// (whether via the strategy switch above or an items-binder strategy</span>\n    <span class=\"hljs-comment\">// the handler dispatched inline before returning). Lets handlers</span>\n    <span class=\"hljs-comment\">// subscribe events that must wire after children-add. Default no-op</span>\n    <span class=\"hljs-comment\">// for handlers that don't override it.</span>\n    _handler.AfterChildrenMount(ctx, typedEl, control);\n\n    <span class=\"hljs-keyword\">return</span> control;\n}\n</code></pre>\n<p>The adapter shown above is the engine-side bridge between the typed\n<code>IElementHandler&#x3C;TElement, TControl></code> shape and the type-erased entry\nthe registry stores. The hot path is dictionary lookup → interface call\n→ a cast that the JIT folds at monomorphic call sites. The cost is\nmeasured at one or two nanoseconds per element on warmed paths and is\nthe reason the protocol does not expose a switch-on-type dispatcher to\nauthors.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Subsystem</th><th>File</th><th>What it owns</th></tr></thead><tbody><tr><td>Handler contract</td><td><code>src/Reactor/Core/V1Protocol/IElementHandler.cs</code></td><td>The <code>Mount</code> / <code>Update</code> / <code>Unmount</code> / <code>Children</code> surface</td></tr><tr><td>Contexts</td><td><code>src/Reactor/Core/V1Protocol/MountContext.cs</code></td><td>Ref-struct mount/update/unmount contexts</td></tr><tr><td>Adapter</td><td><code>src/Reactor/Core/V1Protocol/V1HandlerAdapter.cs</code></td><td>Type-erasure bridge into the registry</td></tr><tr><td>Children strategies</td><td><code>src/Reactor/Core/V1Protocol/ChildrenStrategy.cs</code></td><td>Declarative child-dispatch shapes</td></tr><tr><td>Bindings</td><td><code>src/Reactor/Core/V1Protocol/ReactorBindingT.cs</code></td><td>Event helpers + echo suppression</td></tr><tr><td>Descriptors</td><td><code>src/Reactor/Core/V1Protocol/Descriptor/</code></td><td>Declarative handlers + the interpreter</td></tr><tr><td>Pool</td><td><code>src/Reactor/Core/ElementPool.cs</code></td><td><code>TryRent</code> / <code>Return</code> for poolable controls</td></tr></tbody></table>\n<h2 id=\"the-handler-contract\">The handler contract<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-handler-contract\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">interface</span> <span class=\"hljs-title\">IElementHandler</span>&#x3C;<span class=\"hljs-title\">TElement</span>, <span class=\"hljs-title\">TControl</span>>\n    <span class=\"hljs-keyword\">where</span> <span class=\"hljs-title\">TElement</span> : <span class=\"hljs-title\">Element</span>\n    <span class=\"hljs-keyword\">where</span> <span class=\"hljs-title\">TControl</span> : <span class=\"hljs-title\">UIElement</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Create or rent the WinUI control, apply initial writes,</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> and subscribe event trampolines. Returns the control the engine</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> places into the parent's children collection.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\">TControl <span class=\"hljs-title\">Mount</span>(<span class=\"hljs-params\">MountContext ctx, TElement element</span>)</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Diff <span class=\"hljs-doctag\">&#x3C;paramref name=\"oldEl\"/></span> vs <span class=\"hljs-doctag\">&#x3C;paramref name=\"newEl\"/></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> and apply minimal writes to <span class=\"hljs-doctag\">&#x3C;paramref name=\"control\"/></span>. Returns void:</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the control identity is preserved (§13 Q12).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Update</span>(<span class=\"hljs-params\">UpdateContext ctx, TElement oldEl, TElement newEl, TControl control</span>)</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional override for handlers with explicit teardown</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> (e.g. control-side IDisposable resources). Default no-op — the</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> engine still runs the pool reset contract.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Unmount</span>(<span class=\"hljs-params\">UnmountContext ctx, TControl control</span>)</span> { }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional override for handlers that want to drive child</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> reconciliation manually instead of through</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"Children\"/></span>. Phase 1 strategy dispatch routes through</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"Children\"/></span>; this overload is retained for parity with</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the spec §4 surface and Phase 3 use.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ReconcileChildren</span>(<span class=\"hljs-params\">MountContext ctx, TElement oldEl, TElement newEl, TControl control</span>)</span> { }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional children strategy. Engine dispatches through it</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> when non-null (and not <span class=\"hljs-doctag\">&#x3C;see cref=\"None{TElement,TControl}\"/></span>);</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> otherwise the handler is a leaf for the purposes of child</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> reconciliation.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    ChildrenStrategy&#x3C;TElement, TControl>? Children => <span class=\"hljs-literal\">null</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Spec 047 §14 Phase 3 prelude (Engine A1) — optional hook the</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> engine invokes from <span class=\"hljs-doctag\">&#x3C;see cref=\"V1HandlerAdapter{TElement,TControl}\"/></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> after the <span class=\"hljs-doctag\">&#x3C;see cref=\"Children\"/></span> strategy has mounted/bound every child</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> (and after any items-binder strategy the handler dispatched inline).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default no-op.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;para></span>Override this to wire events whose subscription must happen</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> strictly after children-add — e.g. <span class=\"hljs-doctag\">&#x3C;c></span>TabView.SelectionChanged<span class=\"hljs-doctag\">&#x3C;/c></span>,</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> which WinUI fires spuriously while the first tab is being added if the</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> handler subscribes during the prop-apply phase. Subscribing here side-</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> steps that first-add echo.<span class=\"hljs-doctag\">&#x3C;/para></span><span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AfterChildrenMount</span>(<span class=\"hljs-params\">MountContext ctx, TElement element, TControl control</span>)</span> { }\n}\n</code></pre>\n<p><code>Mount</code> runs the first time the reconciler sees an element of this\ntype at a given slot. It rents a control from the pool (or allocates\none), applies initial property writes, subscribes the events it cares\nabout, and returns the control the reconciler will install in the\nparent's child collection. <code>Update</code> runs every subsequent render where\nthe same slot still holds an element of the same type — it diffs the\nold element against the new and applies the minimum writes needed.\n<code>Unmount</code> runs when the slot disappears or its element type changes;\nthe default body is a no-op because the engine handles pool return\nand child teardown for the standard case. The optional <code>Children</code>\nproperty declares how child elements are dispatched — see\n<a href=\"#children-strategies\">Children strategies</a> below.</p>\n<p>The contract has two deliberate constraints worth pointing out. First,\n<code>Update</code> returns <code>void</code> — handlers do <strong>not</strong> swap control instances\nacross renders. If a re-render genuinely needs a different control\ntype (a <code>Button</code> becoming a <code>HyperlinkButton</code>, for example), it is\nencoded as a different <code>Element</code> subclass, and the reconciler's\ntype-change path unmounts the old and mounts the new at that slot.\nSecond, <code>Mount</code> and <code>Update</code> run on the dispatcher thread that owns\nthe reconciler — handler bodies are free to touch control state\nwithout synchronization, and code that crosses threads is the\nauthor's responsibility (and a debug-build assert).</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Mount</code> is called exactly once per realized control, but the same\ncontrol may be passed back to a different handler instance through the\npool. If you stash per-control state in a field on the handler\ninstance, it survives pool rent — and that is almost certainly a bug.\nPer-control state lives on the <strong>control</strong>, either as an attached\nstate DP or as the typed event payload box\n(<a href=\"#echo-suppression-and-write-suppressed\"><code>Reconciler.GetOrCreateControlEventPayload</code></a>),\nboth of which the pool reset contract clears. The handler instance is\na stateless interpreter.</p>\n</blockquote>\n<h2 id=\"contexts-and-bindings\">Contexts and bindings<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#contexts-and-bindings\">#</a></h2>\n<p>The reconciler passes three contexts into a handler, one per lifecycle\nphase. Each is a <code>readonly ref struct</code>, so the context cannot escape\nthe call stack, cannot be captured by a closure, and does not allocate.</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-keyword\">ref</span> <span class=\"hljs-keyword\">struct</span> MountContext\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Reconciler _reconciler;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Action _requestRerender;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-title\">MountContext</span>(<span class=\"hljs-params\">Reconciler reconciler, Action requestRerender</span>)</span>\n    {\n        _reconciler = reconciler;\n        _requestRerender = requestRerender;\n    }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>The rerender callback for the owning component subtree.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Action RequestRerender => _requestRerender;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>The owning reconciler. Provided as an escape hatch for handlers</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> that need to forward a child mount through some non-strategy path.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Reconciler Reconciler => _reconciler;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Mount a child element through the reconciler. The returned</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"UIElement\"/></span> is whatever the reconciler decided to mount</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> (possibly null for <span class=\"hljs-doctag\">&#x3C;c></span>EmptyElement<span class=\"hljs-doctag\">&#x3C;/c></span>).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> UIElement? MountChild(Element child) => _reconciler.Mount(child, _requestRerender);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Apply a setter array to the control. Equivalent to the public</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"Reconciler.ApplySetters{T}(Action{T}[], T)\"/></span> helper; provided</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> on the context for symmetry with handler-authored mount bodies.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ApplySetters</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">Action&#x3C;T>[] setters, T control</span>) <span class=\"hljs-keyword\">where</span> T : <span class=\"hljs-keyword\">class</span></span>\n        => Reconciler.ApplySetters(setters, control);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Construct a per-binding event helper for the given control + element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> The returned binding closes over the control identity, not the element</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> — handlers re-fetch the live element via <span class=\"hljs-doctag\">&#x3C;c></span>ReactorState.Element<span class=\"hljs-doctag\">&#x3C;/c></span> on</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> each event fire, so the same binding survives element re-renders.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">ReactorBinding</span>&#x3C;<span class=\"hljs-title\">TElement</span>> <span class=\"hljs-title\">BindFor</span>&#x3C;<span class=\"hljs-title\">TElement</span>>(<span class=\"hljs-params\">FrameworkElement control, TElement element</span>)\n        <span class=\"hljs-keyword\">where</span> TElement : Element</span>\n        => <span class=\"hljs-keyword\">new</span> ReactorBinding&#x3C;TElement>(_reconciler, control, element);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Rent a control instance from the per-type pool, or allocate</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> a fresh one if the pool is empty / the policy opts out.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> T <span class=\"hljs-title\">RentControl</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">PoolPolicy&#x3C;T>? policy = <span class=\"hljs-literal\">null</span>, Func&#x3C;T>? factory = <span class=\"hljs-literal\">null</span></span>) <span class=\"hljs-keyword\">where</span> T : <span class=\"hljs-keyword\">class</span>, <span class=\"hljs-keyword\">new</span>()</span>\n        => _reconciler.RentControl(policy, factory);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Push a typed context value for the duration of the returned</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"IDisposable\"/></span>. Used by handlers that mount children which</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> must see an author-supplied context.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> IDisposable <span class=\"hljs-title\">PushContext</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">Context&#x3C;T> context, T <span class=\"hljs-keyword\">value</span></span>)</span> => _reconciler.PushContextDisposable(context, <span class=\"hljs-keyword\">value</span>);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Push a stagger scope; children mounted inside the scope</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> consume stagger indices for their enter transitions.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> IDisposable <span class=\"hljs-title\">PushStaggerScope</span>(<span class=\"hljs-params\">TimeSpan delay</span>)</span> => _reconciler.PushStaggerScopeDisposable(delay);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Escape hatch (Q11) — attach a raw routed handler directly to</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the control. Handlers should prefer the typed <span class=\"hljs-doctag\">&#x3C;c></span>On*<span class=\"hljs-doctag\">&#x3C;/c></span> family on</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"ReactorBinding{TElement}\"/></span>; use this only for events the</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> binding doesn't cover (e.g. <span class=\"hljs-doctag\">&#x3C;c></span>UIElement.PreviewKeyDown<span class=\"hljs-doctag\">&#x3C;/c></span> on a</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> non-FrameworkElement, or app-side custom routed events).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AddRawRoutedHandler</span>(<span class=\"hljs-params\">UIElement target, RoutedEvent re, Delegate h, <span class=\"hljs-built_in\">bool</span> handledEventsToo</span>)</span>\n        => target.AddHandler(re, h, handledEventsToo);\n}\n</code></pre>\n<p><code>MountContext.RentControl&#x3C;T>()</code> is how a handler obtains its WinUI\ncontrol. By default the engine pools every type that opts in via\n<code>PoolableTypes</code> — <code>Button</code>, <code>TextBlock</code>, <code>Border</code>, every common leaf —\nand <code>RentControl</code> returns a reset instance from the per-type stack\nwhen one is available. Authors can override the pool policy or supply\na custom <code>Func&#x3C;T></code> factory for controls that need constructor\narguments or that should never pool. <code>RentControl</code> is therefore the\nsingle place a handler crosses into native land, and the engine\nensures that every rented control reaches <code>ReturnControl</code> on unmount.</p>\n<p><code>MountContext.BindFor(control, element)</code> produces a\n<a href=\"source:src/Reactor/Core/V1Protocol/ReactorBindingT.cs\"><code>ReactorBinding&#x3C;TElement></code></a>\nthat wires events from the WinUI side back to element callbacks. The\nbinding has typed <code>On…</code> methods for the WinUI routed-event families\n(pointer, key, tap, focus) and a general <code>OnCustomEvent&#x3C;TArgs></code> for\nplain CLR events (<code>Click</code>, <code>ValueChanged</code>, <code>TextChanged</code>). The\ncritical property of every <code>On…</code> method is that the event trampoline\ncaptures the <strong>control</strong>, not the element — at fire time it re-reads\nthe live element from\n<a href=\"#element-identity-via-the-tag\"><code>Reconciler.GetElementTag(control)</code></a>.\nThe same subscription therefore survives every re-render without\nre-subscribing.</p>\n<p><code>UpdateContext</code> is the same surface as <code>MountContext</code> minus\n<code>RentControl</code> — update bodies run against an existing control, and\nallocating a new one is forbidden on this path. <code>UnmountContext</code> is\nnarrower still: it carries only <code>ReturnControl&#x3C;T></code> for the optional\npool-return contract.</p>\n<h2 id=\"the-mount-then-subscribe-invariant\">The mount-then-subscribe invariant<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-mount-then-subscribe-invariant\">#</a></h2>\n<p>A controlled property — a property whose value the framework writes\n<em>and</em> whose value the user can edit on the WinUI side — would race if\nthe handler subscribed to the change event before writing the initial\nvalue. The descriptor interpreter (and every hand-coded handler that\nfollows the same shape) sequences the work in two passes so the race\ncannot happen:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> TControl <span class=\"hljs-title\">Mount</span>(<span class=\"hljs-params\">MountContext ctx, TElement el</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> ctrl = ctx.RentControl(_descriptor.PoolPolicy, _descriptor.Factory);\n\n    <span class=\"hljs-comment\">// §14 Phase 3-final: when the descriptor declares an ItemsHost,</span>\n    <span class=\"hljs-comment\">// populate the items collection BEFORE the prop loop. Initial writes</span>\n    <span class=\"hljs-comment\">// for selection-tracking props (SelectedIndex/SelectedItem) need the</span>\n    <span class=\"hljs-comment\">// collection populated first — WinUI silently clamps selection</span>\n    <span class=\"hljs-comment\">// against an empty collection.</span>\n    <span class=\"hljs-keyword\">if</span> (_descriptor.Children <span class=\"hljs-keyword\">is</span> ItemsHost&#x3C;TElement, TControl> ih)\n        DispatchItemsHostMount(<span class=\"hljs-keyword\">in</span> ctx, el, ctrl, ih);\n    <span class=\"hljs-comment\">// §14 Phase 3 finish — consolidated dispatch arm: every items-</span>\n    <span class=\"hljs-comment\">// binder variant uses the same \"bind before prop loop\" ordering so</span>\n    <span class=\"hljs-comment\">// SelectedIndex initial writes land against a populated list.</span>\n    <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (_descriptor.Children <span class=\"hljs-keyword\">is</span> IItemsBinderStrategy binder &#x26;&#x26; ctrl <span class=\"hljs-keyword\">is</span> FrameworkElement feBinder)\n        binder.Bind(feBinder, oldElement: <span class=\"hljs-literal\">null</span>, el, ctx.Reconciler, ctx.RequestRerender, isMount: <span class=\"hljs-literal\">true</span>);\n\n    <span class=\"hljs-comment\">// Phase 1: all bare initial writes (no echo possible — subscriptions</span>\n    <span class=\"hljs-comment\">// not yet live). §14 Phase 3-final: dispatch through the</span>\n    <span class=\"hljs-comment\">// context-carrying overload so OneWayBridged entries can reach the</span>\n    <span class=\"hljs-comment\">// reconciler/rerender helpers; existing entries forward to the</span>\n    <span class=\"hljs-comment\">// parameterless overload via the virtual default on PropEntry.</span>\n    <span class=\"hljs-keyword\">var</span> props = _descriptor.Properties;\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-built_in\">int</span> i = <span class=\"hljs-number\">0</span>; i &#x3C; props.Count; i++)\n        props[i].Mount(<span class=\"hljs-keyword\">in</span> ctx, ctrl, el);\n\n    <span class=\"hljs-comment\">// Phase 2: subscribe controlled entries.</span>\n    <span class=\"hljs-keyword\">var</span> binding = ctx.BindFor(ctrl, el);\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-built_in\">int</span> i = <span class=\"hljs-number\">0</span>; i &#x3C; props.Count; i++)\n        props[i].EnsureSubscribed(binding, ctrl, el);\n\n    <span class=\"hljs-keyword\">var</span> getSetters = _descriptor.GetSetters;\n    <span class=\"hljs-keyword\">if</span> (getSetters <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n        ctx.ApplySetters(getSetters(el), ctrl);\n    <span class=\"hljs-keyword\">return</span> ctrl;\n}\n</code></pre>\n<p>The first loop runs the bare initial writes for every entry. The\nsecond loop wires the trampolines. Because no trampoline is listening\nduring the first loop, the synchronous change events WinUI raises when\nthe framework writes <code>IsOn = true</code>, <code>Value = 3.5</code>, or <code>Text = \"hi\"</code>\ngo nowhere — and the user's callback receives no spurious initial\nfire on Mount. On Update the equivalent property writes go through\n<code>ReactorBinding.WriteSuppressed</code>, which marks a per-control suppress\ncounter that the trampoline drains before invoking the callback. The\nnet effect is that the <strong>callback fires exactly when the user\ninteracted with the control</strong>, never when the framework wrote a value\nthe user did not type.</p>\n<p>The same invariant covers pool rent: a control returned to the pool\nkeeps its typed event-payload box, and re-mount checks the box's\n<code>Trampoline</code> slot to skip re-subscription. A pooled <code>RatingControl</code>\nthat gets rented out for a fresh <code>StarMeterElement</code> does not\ndouble-subscribe its <code>ValueChanged</code> event.</p>\n<h2 id=\"children-strategies\">Children strategies<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#children-strategies\">#</a></h2>\n<p>A handler's <code>Children</code> property declares <strong>how</strong> its child elements\nget dispatched. The strategy is a sealed record subclass; the\nadapter's switch over the strategy types runs after <code>Mount</code> / <code>Update</code>\nreturn, so the handler body itself never has to walk children.</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">ChildrenStrategy</span>&#x3C;<span class=\"hljs-title\">TElement</span>, <span class=\"hljs-title\">TControl</span>>\n    <span class=\"hljs-keyword\">where</span> TElement : Element\n    <span class=\"hljs-keyword\">where</span> TControl : UIElement</span>;\n\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Leaf — no children. Engine performs no dispatch beyond the</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> handler's Mount/Update body.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">None</span>&#x3C;<span class=\"hljs-title\">TElement</span>, <span class=\"hljs-title\">TControl</span>>() : ChildrenStrategy&#x3C;TElement, TControl>\n    <span class=\"hljs-keyword\">where</span> TElement : Element\n    <span class=\"hljs-keyword\">where</span> TControl : UIElement</span>;\n\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Single-content host (Border, ContentControl, Viewbox). The</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> engine mounts <span class=\"hljs-doctag\">&#x3C;paramref name=\"GetChild\"/></span>'s result and assigns it via</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;paramref name=\"SetChild\"/></span>.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;para></span><span class=\"hljs-doctag\">&#x3C;b></span>Structural reconcile:<span class=\"hljs-doctag\">&#x3C;/b></span> set <span class=\"hljs-doctag\">&#x3C;see cref=\"GetCurrentChild\"/></span> so</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the engine can read the existing slot value during <span class=\"hljs-doctag\">&#x3C;c></span>Update<span class=\"hljs-doctag\">&#x3C;/c></span> and</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> route through <span class=\"hljs-doctag\">&#x3C;c></span>Reconciler.ReconcileV1Child<span class=\"hljs-doctag\">&#x3C;/c></span> — that preserves</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> descendant component state across parent re-renders. When left null,</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the engine remounts the child on every update (only safe for slots that</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> are reset every render anyway). All built-in handlers set it.<span class=\"hljs-doctag\">&#x3C;/para></span><span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">SingleContent</span>&#x3C;<span class=\"hljs-title\">TElement</span>, <span class=\"hljs-title\">TControl</span>>(<span class=\"hljs-params\">\n    Func&#x3C;TElement, Element?> GetChild,\n    Action&#x3C;TControl, UIElement?> SetChild</span>) : ChildrenStrategy&#x3C;TElement, TControl>\n    <span class=\"hljs-keyword\">where</span> TElement : Element\n    <span class=\"hljs-keyword\">where</span> TControl : UIElement</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional: read the current child from the control. Required</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> for structural child reconciliation; if null the engine falls back to</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> remounting the child every Update.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Func&#x3C;TControl, UIElement?>? GetCurrentChild { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n}\n\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Panel host (StackPanel, Grid, Canvas). Engine mounts each</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> child and appends to the panel's <span class=\"hljs-doctag\">&#x3C;see cref=\"UIElementCollection\"/></span>.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;para></span><span class=\"hljs-doctag\">&#x3C;b></span>Phase 1 limitation:<span class=\"hljs-doctag\">&#x3C;/b></span> the dispatch is append-only; structural</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> diff against the previous render goes through the host's child</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> collection wholesale. Spec-042 keyed-reconcile integration is a</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Phase 3 follow-up.<span class=\"hljs-doctag\">&#x3C;/para></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;para></span><span class=\"hljs-doctag\">&#x3C;b></span>§14 Phase 3-final addition:<span class=\"hljs-doctag\">&#x3C;/b></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"PerChildAttached\"/></span> — optional callback invoked after each</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> child mount AND after each child Update. Receives the mounted</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"UIElement\"/></span> alongside the child element so the descriptor</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> can write WinUI attached DPs (e.g. <span class=\"hljs-doctag\">&#x3C;c></span>Grid.SetRow<span class=\"hljs-doctag\">&#x3C;/c></span>,</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;c></span>Canvas.SetLeft<span class=\"hljs-doctag\">&#x3C;/c></span>) based on attached-prop hints carried on the</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> child element. No-op by default.<span class=\"hljs-doctag\">&#x3C;/para></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;para></span><span class=\"hljs-doctag\">&#x3C;b></span>§14 Phase 3 close-out addition:<span class=\"hljs-doctag\">&#x3C;/b></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"PerChildAttachedAfterAll\"/></span> — optional two-pass callback</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> invoked once after every child has been mounted (Mount path) or</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> reconciled (Update path). Receives the full <span class=\"hljs-doctag\">&#x3C;c></span>(UIElement, Element)<span class=\"hljs-doctag\">&#x3C;/c></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> pair list in collection order so the descriptor can apply attached</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> DPs that reference OTHER children by name (e.g.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;c></span>RelativePanel.SetRightOf(b, a)<span class=\"hljs-doctag\">&#x3C;/c></span>). Distinct from</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;see cref=\"PerChildAttached\"/></span>, which fires per-child mid-pass and</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> cannot see siblings that haven't mounted yet. Most descriptors set</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> only one of the two; <span class=\"hljs-doctag\">&#x3C;c></span>RelativePanel<span class=\"hljs-doctag\">&#x3C;/c></span> is the canonical consumer</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> of the after-all shape.<span class=\"hljs-doctag\">&#x3C;/para></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n[<span class=\"hljs-meta\">Experimental(<span class=\"hljs-string\">\"REACTOR_V1_PREVIEW\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Panel</span>&#x3C;<span class=\"hljs-title\">TElement</span>, <span class=\"hljs-title\">TControl</span>>(<span class=\"hljs-params\">\n    Func&#x3C;TElement, IReadOnlyList&#x3C;Element>> GetChildren,\n    Func&#x3C;TControl, UIElementCollection> GetCollection</span>) : ChildrenStrategy&#x3C;TElement, TControl>\n    <span class=\"hljs-keyword\">where</span> TElement : Element\n    <span class=\"hljs-keyword\">where</span> TControl : UIElement</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional per-child attached-prop writer. Called by the</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> engine after each child is mounted (Mount path) AND after each child</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> is reconciled (Update path). The descriptor reads attached-prop</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> hints off the child element (via <span class=\"hljs-doctag\">&#x3C;c></span>Element.GetAttached&#x26;lt;T&#x26;gt;()<span class=\"hljs-doctag\">&#x3C;/c></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> or similar) and writes the corresponding WinUI attached DPs onto</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> the mounted <span class=\"hljs-doctag\">&#x3C;see cref=\"UIElement\"/></span>. Defaults to <span class=\"hljs-doctag\">&#x3C;c></span>null<span class=\"hljs-doctag\">&#x3C;/c></span> for</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> containers that don't carry per-child attached props</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> (e.g. <span class=\"hljs-doctag\">&#x3C;c></span>StackPanel<span class=\"hljs-doctag\">&#x3C;/c></span>).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">Action</span>&#x3C;<span class=\"hljs-title\">TControl</span>, <span class=\"hljs-title\">UIElement</span>, <span class=\"hljs-title\">Element</span>>? PerChildAttached</span> { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Optional two-pass callback fired once after every child has</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> been mounted/reconciled, receiving the full ordered list of</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;c></span>(UIElement, Element)<span class=\"hljs-doctag\">&#x3C;/c></span> pairs. Use for attached DPs that</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> reference siblings by name — e.g. <span class=\"hljs-doctag\">&#x3C;c></span>RelativePanel.SetRightOf<span class=\"hljs-doctag\">&#x3C;/c></span>.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Defaults to <span class=\"hljs-doctag\">&#x3C;c></span>null<span class=\"hljs-doctag\">&#x3C;/c></span>; only RelativePanel-shaped descriptors set</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> it.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Action&#x3C;TControl, IReadOnlyList&#x3C;(UIElement Mounted, Element ChildElement)>>? PerChildAttachedAfterAll { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n}\n</code></pre>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Strategy</th><th>Shape</th><th>Used by</th></tr></thead><tbody><tr><td><code>None</code></td><td>Leaf — no children</td><td><code>Button</code>, <code>TextBlock</code>, <code>Slider</code>, <code>RatingControl</code>, every leaf</td></tr><tr><td><code>SingleContent</code></td><td>One child slot, <code>GetCurrentChild</code> for structural reconcile</td><td><code>Border</code>, <code>ContentControl</code>, <code>Viewbox</code>, <code>Expander</code> body</td></tr><tr><td><code>Panel</code></td><td>Ordered child list against <code>UIElementCollection</code>, optional <code>PerChildAttached</code> for attached DPs</td><td><code>Grid</code>, <code>StackPanel</code>, <code>Canvas</code>, <code>RelativePanel</code></td></tr><tr><td><code>NamedSlots</code></td><td>Multiple named slots, each binding via a <code>NamedSlot</code> record</td><td><code>SplitView</code> (Pane + Content), <code>NavigationView</code> (Header + Content + PaneFooter)</td></tr><tr><td><code>ItemsHost</code></td><td>Flat items collection — <code>IReadOnlyList&#x3C;object></code> projected into the control's <code>Items</code> sink</td><td><code>ListBox</code>, <code>ComboBox</code>, <code>RadioButtons</code>, simple <code>FlipView</code></td></tr><tr><td><code>TemplatedItems&#x3C;TItem,…></code></td><td>Keyed templated list — routes through <code>BindKeyedItemsSource</code> for spec-042 keyed reconcile</td><td><code>ListView&#x3C;T></code>, <code>GridView&#x3C;T></code></td></tr><tr><td><code>TemplatedItemsErased</code></td><td>Same shape, T erased via <code>IKeyedItemSource</code> for typed-list base types</td><td><code>TemplatedListViewElement&#x3C;T></code> family</td></tr><tr><td><code>TreeChildren</code></td><td>Hierarchical <code>TreeViewNode</code> tree, optional per-node <code>ContentElement</code></td><td><code>TreeView</code></td></tr><tr><td><code>TabItemsHost</code></td><td>Heterogeneous items (<code>TabViewItem</code> / <code>PivotItem</code>) with in-place positional reconcile</td><td><code>TabView</code>, <code>Pivot</code></td></tr><tr><td><code>PreMountedItems</code></td><td>Pre-mounted items against a flat sink with no <code>ContainerContentChanging</code></td><td>Templated <code>FlipView</code></td></tr><tr><td><code>Imperative</code></td><td>Escape hatch — handler reconciles children itself</td><td>Custom containers that span multiple WinUI properties</td></tr></tbody></table>\n<p>The strategy switch is the engine's seam between the handler protocol\nand the rest of the reconciler — <code>SingleContent</code> and <code>NamedSlots</code>\nroute through\n<a href=\"/reconciliation/#child-reconciler\"><code>Reconciler.ReconcileV1Child</code></a>,\nwhich is the same diff path the rest of Reactor uses; <code>Panel</code>\nwalks the new and old child lists in lockstep and preserves descendant\ncomponent state across reorderings that do not change identity;\n<code>TemplatedItems</code> and <code>TemplatedItemsErased</code> route through\n<code>Reconciler.BindKeyedItemsSource</code>, which owns the spec-042\n<code>KeyedListDiff</code> realization plumbing — and so on. Adding a new\nstrategy is a Reactor-internals task; adding a new handler that\n<em>consumes</em> one of these strategies is what extension authors do.</p>\n<h2 id=\"pool-integration\">Pool integration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#pool-integration\">#</a></h2>\n<p>The descriptor's <code>PoolPolicy</code> (and the corresponding parameter to\n<code>MountContext.RentControl&#x3C;T></code>) determines whether the control type\npools at all and, if so, how to reset its mutable state when it goes\nback into the pool. Most leaf controls — <code>Button</code>, <code>TextBlock</code>,\n<code>Border</code>, <code>RatingControl</code>, every input the form catalog covers —\nopt in by default; the per-type entry in <code>PoolableTypes</code> declares the\nreset hook (clearing handlers, restoring default property values) so\na rented instance is observably indistinguishable from a fresh one.\nAuthors writing a new handler should generally accept the default and\nlet the engine pool the control; the policy escape hatch exists for\ncontrols whose state is too large or whose teardown is too expensive\nto be worth pooling.</p>\n<p><code>MountContext.RentControl&#x3C;T></code> is the single rent path. The engine\nguarantees that every rent reaches a <code>ReturnControl</code> on unmount — the\ndefault adapter body calls <code>Unmount</code> with a <code>V1UnmountDisposition</code>\nresult that opts into pool collection, the engine then walks the\nunmounted subtree and returns each control to its per-type stack. An\nauthor writing <code>Unmount</code> does not have to call <code>ReturnControl</code>\nthemselves unless they rented from the engine via the manual\n<code>RentControl</code> overload — the default lifecycle is fully managed.</p>\n<h2 id=\"echo-suppression-and-writesuppressed\">Echo suppression and <code>WriteSuppressed</code><a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#echo-suppression-and-writesuppressed\">#</a></h2>\n<p>When the framework writes the controlled prop, the WinUI control\nraises its change event synchronously. The event trampoline cannot\ndistinguish \"the user typed this\" from \"the framework just wrote\nthis\" without help, and forwarding the framework's own write to the\nelement callback would round-trip the same value through the user's\nstate setter and trigger a spurious re-render. <code>ReactorBinding</code>\nsuppresses that echo with a per-control counter:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>When to call</th><th>Effect</th></tr></thead><tbody><tr><td><code>binding.WriteSuppressed(mutate)</code></td><td>Inside <code>Update</code> when writing a controlled prop</td><td>Increments the suppress counter, runs the mutate, decrements</td></tr><tr><td><code>ChangeEchoSuppressor.ShouldSuppress(fe)</code></td><td>Inside an event trampoline</td><td>Drains a pending suppress count and returns true; trampoline short-circuits</td></tr></tbody></table>\n<p>The trampolines <code>ReactorBinding.OnCustomEvent&#x3C;TArgs></code> generates call\n<code>ShouldSuppress</code> on entry — every controlled prop in every built-in\nhandler is echo-safe without the handler author having to think\nabout it. Hand-authored trampolines (the ones that bypass\n<code>OnCustomEvent</code> for performance) follow the same shape; see\n<code>Reconciler.GetOrCreateControlEventPayload</code> for the per-control\npayload box that anchors the trampoline for the control's lifetime.</p>\n<h2 id=\"element-identity-via-the-tag\">Element identity via the tag<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#element-identity-via-the-tag\">#</a></h2>\n<p>Every framework element record is anchored on its mounted\n<code>FrameworkElement</code> via <code>Reconciler.SetElementTag(fe, element)</code>, an\nattached state DP. The tag is what makes event trampolines safe across\nre-renders: the trampoline closes over the control instance, calls\n<code>Reconciler.GetElementTag(control)</code> at fire time, casts the result to\nthe typed element, and invokes the callback. The element value is\nrefreshed by <code>Reconciler.SetElementTag</code> on every Update, so the\ntrampoline always sees the live element — including the live\ncallback, the live state captured by the user's component, and any\nintervening prop changes.</p>\n<p>Handler authors get the tag wiring for free — the adapter writes it\nafter <code>Mount</code> returns and after every <code>Update</code>. Hand-authored trampolines\nthat need to read the tag (e.g. the descriptor's static\n<code>ClickTrampoline</code> for <code>Button</code>) call <code>GetElementTag</code> and pattern-match\non the expected element type.</p>\n<h2 id=\"descriptors--declarative-handlers\">Descriptors — declarative handlers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#descriptors--declarative-handlers\">#</a></h2>\n<p><code>IElementHandler</code> is the imperative shape; <code>ControlDescriptor</code> is the\ndeclarative one. A descriptor is a list of <code>PropEntry</code> objects (each\nencoding one property's binding shape) plus an optional children\nstrategy, optional factory, and optional pool policy. The\n<code>DescriptorHandler&#x3C;TElement, TControl></code> interpreter is itself an\n<code>IElementHandler</code>, so the registry sees no difference between a\nhand-coded handler and a descriptor — the dispatch shell is identical,\nand any cost difference is the interpreter's iteration over a small\nlist of entries.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// A descriptor declares property bindings against the WinUI control the</span>\n<span class=\"hljs-comment\">// element targets. The framework's DescriptorHandler interprets the entries</span>\n<span class=\"hljs-comment\">// during Mount and Update — there is no per-element interpreter overhead</span>\n<span class=\"hljs-comment\">// beyond a dictionary lookup and the entry-loop iteration.</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LedIndicatorDescriptor</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> ControlDescriptor&#x3C;LedIndicatorElement, WinUI.Border> Descriptor =\n        <span class=\"hljs-keyword\">new</span> ControlDescriptor&#x3C;LedIndicatorElement, WinUI.Border>\n        {\n            <span class=\"hljs-comment\">// The descriptor's children strategy says \"no children\" — this is</span>\n            <span class=\"hljs-comment\">// a leaf control. See ChildrenStrategy survey in the prose.</span>\n            Children = <span class=\"hljs-keyword\">new</span> None&#x3C;LedIndicatorElement, WinUI.Border>(),\n        }\n        <span class=\"hljs-comment\">// OneWay: write on Mount, diff-and-write on Update. Equality skips</span>\n        <span class=\"hljs-comment\">// the write when the element value didn't change.</span>\n        .OneWay(\n            <span class=\"hljs-keyword\">get</span>: <span class=\"hljs-keyword\">static</span> e => e.Size,\n            <span class=\"hljs-keyword\">set</span>: <span class=\"hljs-keyword\">static</span> (c, v) => { c.Width = v; c.Height = v; c.CornerRadius = <span class=\"hljs-keyword\">new</span> Microsoft.UI.Xaml.CornerRadius(v / <span class=\"hljs-number\">2</span>); })\n        <span class=\"hljs-comment\">// The IsOn → Background mapping coerces both inputs onto one WinUI</span>\n        <span class=\"hljs-comment\">// property. A single OneWay entry observes (Color, IsOn) jointly</span>\n        <span class=\"hljs-comment\">// by reading both off the element in the get/set lambdas.</span>\n        .OneWay(\n            <span class=\"hljs-keyword\">get</span>: <span class=\"hljs-keyword\">static</span> e => (e.Color, e.IsOn),\n            <span class=\"hljs-keyword\">set</span>: <span class=\"hljs-keyword\">static</span> (c, v) =>\n                c.Background = <span class=\"hljs-keyword\">new</span> SolidColorBrush(\n                    v.IsOn ? v.Color : Color.FromArgb(<span class=\"hljs-number\">0x40</span>, v.Color.R, v.Color.G, v.Color.B)));\n}\n</code></pre>\n<p>The descriptor above declares a single one-way mapping from\n<code>(Color, IsOn)</code> onto <code>Border.Background</code>, plus a size hook that\nwrites <code>Width</code>, <code>Height</code>, and <code>CornerRadius</code> in one entry. It is a\ncomplete extension: every <code>LedIndicatorElement</code> the app renders\nflows through this descriptor, including pool rent, prop diff, and\nunmount.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/v1-protocol/led-indicator.png\" alt=\"Four LED indicators rendered by a Reactor app using the custom LedIndicatorElement descriptor\"></p>\n<h3 id=\"propentry-shapes--the-prop-binding-vocabulary\"><code>PropEntry</code> shapes — the prop-binding vocabulary<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#propentry-shapes--the-prop-binding-vocabulary\">#</a></h3>\n<p>The fluent builder methods on <code>ControlDescriptor</code> add one entry per\ncall. The full vocabulary covers the common cases plus the escape\nhatches a hand-coded handler would otherwise need:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Builder</th><th>When to reach for it</th></tr></thead><tbody><tr><td><code>.OneWay(get, set)</code></td><td>Plain one-way prop; write on Mount, diff-write on Update. No event subscription.</td></tr><tr><td><code>.OneWayConditional(get, set, shouldWrite)</code></td><td>Nullable / optional prop where leaving the control's default is the intent when the element didn't set a value.</td></tr><tr><td><code>.Initial(get, set)</code></td><td>Write once at Mount and never again. Use for seed-only props (<code>TextBox.InitialText</code>, etc.).</td></tr><tr><td><code>.Controlled&#x3C;TValue, TArgs>(get, set, subscribe, unsubscribe, callback, readBack)</code></td><td>Two-way prop. Framework writes with echo suppression; user input rounds back via the change event. Subscription is gated on <code>callback</code> returning non-null.</td></tr><tr><td><code>.CoercingOneWay(get, set, coercesController)</code></td><td>One-way prop whose write may coerce a sibling controlled value (e.g. <code>Slider.Minimum</code> re-clamping <code>Slider.Value</code>); writes through <code>WriteSuppressed</code> when the coercion fires.</td></tr><tr><td><code>.HandCodedControlled&#x3C;TPayload, TValue, TDelegate></code></td><td>Two-way prop with a multi-event control where the standard <code>Controlled</code> shape would collide on the per-control event payload (<code>TextBox.Text</code> alongside <code>SelectionChanged</code>).</td></tr><tr><td><code>.HandCodedEvent&#x3C;TPayload, TDelegate></code></td><td>Fire-and-forget event with no associated DP (<code>Button.Click</code>, <code>Image.ImageOpened</code>).</td></tr><tr><td><code>.CollectionDiffControlled&#x3C;…></code></td><td>Two-way prop whose value is an <code>IReadOnlyList&#x3C;TItem></code> round-tripped against a control-side <code>IList&#x3C;TItem></code> (<code>CalendarView.SelectedDates</code>).</td></tr><tr><td><code>.OneWayBridged(get, set, shouldWrite)</code></td><td>One-way prop whose <code>set</code> lambda needs access to the reconciler (used by the button-family flyout port).</td></tr><tr><td><code>.Imperative(mount, update)</code></td><td>Property-level escape hatch — the lambdas receive <code>(control, element)</code> and <code>(control, oldEl, newEl)</code> so an entry can express a diff the standard builders cannot.</td></tr><tr><td><code>.ImperativeBridged(mount, update)</code></td><td>Same shape with the <code>MountContext</code> / <code>UpdateContext</code> available — primary use is a secondary Element slot that needs structural reconcile via <code>ReconcileV1Child</code>.</td></tr></tbody></table>\n<p>The entries iterate as a flat list, in declaration order. The\ndescriptor's children strategy runs first when it is an items binder\n(so selection-tracking props see the populated collection at Mount);\notherwise the strategy fires after the entry loop. The mount-then-\nsubscribe ordering invariant is preserved by running every entry's\n<code>Mount</code> body before any entry's <code>EnsureSubscribed</code>.</p>\n<h2 id=\"registration\">Registration<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#registration\">#</a></h2>\n<p>Handlers are registered with a Reactor host's reconciler before the\nfirst render. The standard call is <code>Reconciler.RegisterHandler&#x3C;TElement, TControl>(handler)</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Registration is one call per Reactor host. RegisterDescriptor wraps</span>\n<span class=\"hljs-comment\">// RegisterHandler&#x3C;...>(new DescriptorHandler&#x3C;...>(descriptor)) — both shapes</span>\n<span class=\"hljs-comment\">// land on the same dispatch table. Duplicate registrations for the same</span>\n<span class=\"hljs-comment\">// element type throw.</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">LedIndicatorRegistration</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Register</span>(<span class=\"hljs-params\">Reconciler reconciler</span>)</span>\n    {\n        reconciler.RegisterHandler&#x3C;LedIndicatorElement, WinUI.Border>(\n            <span class=\"hljs-keyword\">new</span> DescriptorHandler&#x3C;LedIndicatorElement, WinUI.Border>(\n                LedIndicatorDescriptor.Descriptor));\n    }\n}\n</code></pre>\n<p>There are two variants worth knowing about:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Method</th><th>Use when</th></tr></thead><tbody><tr><td><code>RegisterHandler&#x3C;TElement, TControl>(handler)</code></td><td>Standard registration — exact-type dispatch on <code>element.GetType()</code>.</td></tr><tr><td><code>RegisterHandlerForDerivedTypes&#x3C;TBase, TControl>(handler)</code></td><td>T-erased registration on a non-generic base type — every closed-T variant whose chain reaches <code>TBase</code> routes to the same handler. Used by the typed templated-list family (<code>TemplatedListViewElement&#x3C;T></code>, etc.).</td></tr></tbody></table>\n<p>Exact-type registrations always win over a derived-type registration\nfor the same chain. Duplicate registration for the same element type\n<strong>throws</strong> — there is no last-writer-wins. If you need a different\nmapping for the same element type (e.g., a test harness substituting a\nfake control), build a separate <code>Reconciler</code> instance for the scope\nthat needs the override.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<p>A handful of recurring patterns cover the long tail of real handlers:</p>\n<p><strong>Map two element fields onto one control property.</strong> Use a single\n<code>.OneWay</code> entry whose <code>get</code> returns a tuple and whose <code>set</code> reads\nboth fields. The descriptor's value comparer treats the tuple\natomically, so the write fires only when either field changes. This\nis how the LED demo collapses <code>(Color, IsOn)</code> into a single\n<code>Background</code> update.</p>\n<p><strong>Map one element field onto multiple control properties.</strong> Same\nshape: one <code>.OneWay</code> entry whose <code>set</code> body writes all of them. The\ndiff-check still runs only against the element's source field.</p>\n<p><strong>Gate subscription on callback presence.</strong> Pass <code>e => e.OnXxx</code> as\nthe <code>callback</code> parameter to <code>.Controlled</code> or <code>.HandCodedEvent</code>. When\nthe element returns <code>null</code> the engine skips subscription entirely —\ncallback-less controls pay zero per-fire dispatch cost. This is the\nstandard idiom; do not gate manually inside a hand-authored\ntrampoline.</p>\n<p><strong>Survive across re-renders.</strong> Hand-authored trampolines that bypass\n<code>OnCustomEvent</code> must read the live element via\n<code>Reconciler.GetElementTag(control)</code> rather than capturing the element\nin the closure — the closure-captured element goes stale every render\nand the callback observes the wrong state. Every built-in\n<code>HandCodedEvent</code> trampoline follows this shape; copy it.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<p><strong>Subscribing inside <code>Update</code>.</strong> Each <code>Update</code> call runs against an\nexisting control whose subscriptions are already wired from <code>Mount</code>.\nSubscribing again in <code>Update</code> multiplies the trampolines across\nre-renders. The protocol's contract is: subscribe in <code>Mount</code> (or, for\ndescriptors, in <code>EnsureSubscribed</code> which the interpreter calls after\nthe Mount entry loop and lazily on <code>null → non-null</code> callback\ntransitions).</p>\n<p><strong>Writing without <code>WriteSuppressed</code> from <code>Update</code>.</strong> A controlled\nprop write from <code>Update</code> raises the change event synchronously and\nechoes through the trampoline back into the user's setter. Every\ncontrolled-shape descriptor entry already wraps the write; hand-coded\nhandlers should mirror this. Forgetting it is the most common cause\nof \"the value snaps back when I type\" bugs.</p>\n<p><strong>Capturing the element in a trampoline closure.</strong> Closures over the\nelement observe a stale value on the next render. Re-fetch via\n<code>Reconciler.GetElementTag(control)</code> on every fire instead.</p>\n<p><strong>Stashing state on the handler instance.</strong> Handler instances are\nregistered once per host and live as long as the reconciler. Per-\ncontrol state needs to live on the control (attached state DP, typed\nevent payload box), not on the handler. The handler is a stateless\ninterpreter.</p>\n<p><strong>Registering a handler against an open generic.</strong> The dispatch table\nkeys on <code>System.Type</code>, so an open generic like <code>typeof(DataGrid&#x3C;>)</code>\nhas no usable runtime type. Use <code>RegisterHandlerForDerivedTypes</code> with\na non-generic intermediate base when you need to catch every closed\nvariant of a generic element family.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Prefer descriptors for new ports.</strong> A descriptor is a declarative\nlist that the interpreter executes against the same surface a hand-\ncoded handler uses. The dispatch shape is identical; the descriptor\njust reads better. Reach for a hand-coded handler when the prop\ndiffing legitimately needs imperative logic (multi-source props,\ntemplate-part trampolines), and use the <code>.Imperative</code> /\n<code>.HandCoded…</code> escape hatches inside a descriptor before writing a\nfull hand-coded handler.</p>\n<p><strong>Read the <code>RatingControl</code> descriptor before writing your own.</strong> The\nbuilt-in descriptors in\n<code>src/Reactor/Core/V1Protocol/Descriptor/Descriptors/</code> are the\nauthoritative examples of every shape. Start from the closest match\nto your control and adapt; copy the comments along with the code so\nfuture maintainers see the rationale.</p>\n<p><strong>Let the engine handle pool integration.</strong> Default <code>PoolPolicy</code>\ncovers every leaf control already in <code>PoolableTypes</code>. Custom pool\npolicies exist for the cases where the default does not fit; do not\nreach for them as a first move.</p>\n<p><strong>Test the controlled-prop round trip with a real pointer driver.</strong>\nThe echo-suppression contract is invisible until two writes race. The\nexisting <code>SelfTests</code> fixtures (in <code>tests/Reactor.AppTests.Host/SelfTest/Fixtures/</code>)\nare the template — copy the pattern that drives the control via an\n<code>AutomationPeer</code> <code>IInvokeProvider</code>/<code>IValueProvider</code> and asserts the\ncallback fired exactly once.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><a href=\"/extending-reactor-controls/\">Extending Reactor with Native Controls</a> — the cookbook companion to this page.</li>\n<li><a href=\"/reconciliation/\">Reconciliation</a> — how the reconciler decides Mount vs Update vs Unmount, and where <code>Reconcile</code> calls into the handler.</li>\n<li><a href=\"/element-pool/\">Element Pool</a> — what <code>RentControl</code> and <code>ReturnControl</code> actually do.</li>\n<li><a href=\"/hooks-internals/\">Hooks Internals</a> — how the re-render side of the same loop works.</li>\n<li><a href=\"/modifier-system/\">Modifier System</a> — the <code>Setters</code> chain a descriptor applies after its own prop loop.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor) draws the visible part of a frame by handing each immutable element record off to a per-control-type plug-in called a handler . The handler knows two things the reconciler doesn't: which WinUI control to instantiate, and which of that control's properties and events to wire to which element fields. The protocol is designed so authors do not pay for what they do not use — a handler that declares no children does no child dispatch, a controlled prop with no callback does not subscribe its event trampoline, a one-way prop whose value did not change does not write — and so every built-in control and every control you add yourself dispatches through the same tight loop. Read this page before adding a new native control or before reaching for a debugger when an existing one mis-updates. The Control Reconciler Protocol # This page documents the data path between Reconciler.Reconcile and the WinUI control it edits. The audience is anyone extending Reactor with a new native control, or anyone debugging why an existing element does not update the way it should. The companion Extending Reactor with Native Controls walks through adding a control end-to-end; this page is the reference under that recipe. Dispatch — one handler per element type # The reconciler holds a dictionary keyed by System.Type of the element record. When a re-render passes a new element to Reconcile , the dictionary entry for that element's exact runtime type is fetched, and that entry's Mount / Update / Unmount methods run against the existing WinUI control. There is no base-class fallback for ordinary handlers — registering ButtonElement does not also catch a MyButtonElement that inherits from it; each concrete element type has its own registration. (The one exception, RegisterHandlerForDerivedTypes , exists for the T-erased templated-list family and is called out explicitly below.) public UIElement Mount ( Element element, Action requestRerender, Reconciler reconciler ) { var typedEl = (TElement)element; var ctx = new MountContext(reconciler, requestRerender); var control = _handler.Mount(ctx, typedEl); // Anchor element identity on the control via the attached state DP so // event trampolines can re-fetch the live element on each fire. if (control is FrameworkElement fe) Reconciler.SetElementTag(fe, typedEl); // Strategy dispatch — only when the handler declares a non-None Children strategy. var strategy = _handler.Children; if (strategy is not null ) DispatchChildrenMount(strategy, ctx, typedEl, control); // Post-children mount hook. Fires after every child has mounted // (whether via the strategy switch above or an items-binder strategy // the handler dispatched inline before returning). Lets handlers // subscribe events that must wire after children-add. Default no-op // for handlers that don't override it. _handler.AfterChildrenMount(ctx, typedEl, control); return control; } The adapter shown above is the engine-side bridge between the typed IElementHandler&#x3C;TElement, TControl> shape and the type-erased entry the registry stores. The hot path is dictionary lookup → interface call → a cast that the JIT folds at monomorphic call sites. The cost is measured at one or two nanoseconds per element on warmed paths and is the reason the protocol does not expose a switch-on-type dispatcher to authors. Subsystem File What it owns Handler contract src/Reactor/Core/V1Protocol/IElementHandler.cs The Mount / Update / Unmount / Children surface Contexts src/Reactor/Core/V1Protocol/MountContext.cs Ref-struct mount/update/unmount contexts Adapter src/Reactor/Core/V1Protocol/V1HandlerAdapter.cs Type-erasure bridge into the registry Children strategies src/Reactor/Core/V1Protocol/ChildrenStrategy.cs Declarative child-dispatch shapes Bindings src/Reactor/Core/V1Protocol/ReactorBindingT.cs Event helpers + echo suppression Descriptors src/Reactor/Core/V1Protocol/Descriptor/ Declarative handlers + the interpreter Pool src/Reactor/Core/ElementPool.cs TryRent / Return for poolable controls The handler contract # [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public interface IElementHandler &#x3C; TElement , TControl > where TElement : Element where TControl : UIElement { /// &#x3C;summary> Create or rent the WinUI control, apply initial writes, /// and subscribe event trampolines. Returns the control the engine /// places into the parent's children collection. &#x3C;/summary> TControl Mount ( MountContext ctx, TElement element ) ; /// &#x3C;summary> Diff &#x3C;paramref name=\"oldEl\"/> vs &#x3C;paramref name=\"newEl\"/> /// and apply minimal writes to &#x3C;paramref name=\"control\"/> . Returns void: /// the control identity is preserved (§13 Q12). &#x3C;/summary> void Update ( UpdateContext ctx, TElement oldEl, TElement newEl, TControl control ) ; /// &#x3C;summary> Optional override for handlers with explicit teardown /// (e.g. control-side IDisposable resources). Default no-op — the /// engine still runs the pool reset contract. &#x3C;/summary> void Unmount ( UnmountContext ctx, TControl control ) { } /// &#x3C;summary> Optional override for handlers that want to drive child /// reconciliation manually instead of through /// &#x3C;see cref=\"Children\"/> . Phase 1 strategy dispatch routes through /// &#x3C;see cref=\"Children\"/> ; this overload is retained for parity with /// the spec §4 surface and Phase 3 use. &#x3C;/summary> void ReconcileChildren ( MountContext ctx, TElement oldEl, TElement newEl, TControl control ) { } /// &#x3C;summary> Optional children strategy. Engine dispatches through it /// when non-null (and not &#x3C;see cref=\"None{TElement,TControl}\"/> ); /// otherwise the handler is a leaf for the purposes of child /// reconciliation. &#x3C;/summary> ChildrenStrategy&#x3C;TElement, TControl>? Children => null ; /// &#x3C;summary> Spec 047 §14 Phase 3 prelude (Engine A1) — optional hook the /// engine invokes from &#x3C;see cref=\"V1HandlerAdapter{TElement,TControl}\"/> /// after the &#x3C;see cref=\"Children\"/> strategy has mounted/bound every child /// (and after any items-binder strategy the handler dispatched inline). /// Default no-op. /// /// &#x3C;para> Override this to wire events whose subscription must happen /// strictly after children-add — e.g. &#x3C;c> TabView.SelectionChanged &#x3C;/c> , /// which WinUI fires spuriously while the first tab is being added if the /// handler subscribes during the prop-apply phase. Subscribing here side- /// steps that first-add echo. &#x3C;/para> &#x3C;/summary> void AfterChildrenMount ( MountContext ctx, TElement element, TControl control ) { } } Mount runs the first time the reconciler sees an element of this type at a given slot. It rents a control from the pool (or allocates one), applies initial property writes, subscribes the events it cares about, and returns the control the reconciler will install in the parent's child collection. Update runs every subsequent render where the same slot still holds an element of the same type — it diffs the old element against the new and applies the minimum writes needed. Unmount runs when the slot disappears or its element type changes; the default body is a no-op because the engine handles pool return and child teardown for the standard case. The optional Children property declares how child elements are dispatched — see Children strategies below. The contract has two deliberate constraints worth pointing out. First, Update returns void — handlers do not swap control instances across renders. If a re-render genuinely needs a different control type (a Button becoming a HyperlinkButton , for example), it is encoded as a different Element subclass, and the reconciler's type-change path unmounts the old and mounts the new at that slot. Second, Mount and Update run on the dispatcher thread that owns the reconciler — handler bodies are free to touch control state without synchronization, and code that crosses threads is the author's responsibility (and a debug-build assert). Caveat: Mount is called exactly once per realized control, but the same control may be passed back to a different handler instance through the pool. If you stash per-control state in a field on the handler instance, it survives pool rent — and that is almost certainly a bug. Per-control state lives on the control , either as an attached state DP or as the typed event payload box ( Reconciler.GetOrCreateControlEventPayload ), both of which the pool reset contract clears. The handler instance is a stateless interpreter. Contexts and bindings # The reconciler passes three contexts into a handler, one per lifecycle phase. Each is a readonly ref struct , so the context cannot escape the call stack, cannot be captured by a closure, and does not allocate. [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public readonly ref struct MountContext { private readonly Reconciler _reconciler; private readonly Action _requestRerender; internal MountContext ( Reconciler reconciler, Action requestRerender ) { _reconciler = reconciler; _requestRerender = requestRerender; } /// &#x3C;summary> The rerender callback for the owning component subtree. &#x3C;/summary> public Action RequestRerender => _requestRerender; /// &#x3C;summary> The owning reconciler. Provided as an escape hatch for handlers /// that need to forward a child mount through some non-strategy path. &#x3C;/summary> public Reconciler Reconciler => _reconciler; /// &#x3C;summary> Mount a child element through the reconciler. The returned /// &#x3C;see cref=\"UIElement\"/> is whatever the reconciler decided to mount /// (possibly null for &#x3C;c> EmptyElement &#x3C;/c> ). &#x3C;/summary> public UIElement? MountChild(Element child) => _reconciler.Mount(child, _requestRerender); /// &#x3C;summary> Apply a setter array to the control. Equivalent to the public /// &#x3C;see cref=\"Reconciler.ApplySetters{T}(Action{T}[], T)\"/> helper; provided /// on the context for symmetry with handler-authored mount bodies. &#x3C;/summary> public void ApplySetters &#x3C; T >( Action&#x3C;T>[] setters, T control ) where T : class => Reconciler.ApplySetters(setters, control); /// &#x3C;summary> Construct a per-binding event helper for the given control + element. /// The returned binding closes over the control identity, not the element /// — handlers re-fetch the live element via &#x3C;c> ReactorState.Element &#x3C;/c> on /// each event fire, so the same binding survives element re-renders. &#x3C;/summary> public ReactorBinding &#x3C; TElement > BindFor &#x3C; TElement >( FrameworkElement control, TElement element ) where TElement : Element => new ReactorBinding&#x3C;TElement>(_reconciler, control, element); /// &#x3C;summary> Rent a control instance from the per-type pool, or allocate /// a fresh one if the pool is empty / the policy opts out. &#x3C;/summary> public T RentControl &#x3C; T >( PoolPolicy&#x3C;T>? policy = null , Func&#x3C;T>? factory = null ) where T : class , new () => _reconciler.RentControl(policy, factory); /// &#x3C;summary> Push a typed context value for the duration of the returned /// &#x3C;see cref=\"IDisposable\"/> . Used by handlers that mount children which /// must see an author-supplied context. &#x3C;/summary> public IDisposable PushContext &#x3C; T >( Context&#x3C;T> context, T value ) => _reconciler.PushContextDisposable(context, value ); /// &#x3C;summary> Push a stagger scope; children mounted inside the scope /// consume stagger indices for their enter transitions. &#x3C;/summary> public IDisposable PushStaggerScope ( TimeSpan delay ) => _reconciler.PushStaggerScopeDisposable(delay); /// &#x3C;summary> Escape hatch (Q11) — attach a raw routed handler directly to /// the control. Handlers should prefer the typed &#x3C;c> On* &#x3C;/c> family on /// &#x3C;see cref=\"ReactorBinding{TElement}\"/> ; use this only for events the /// binding doesn't cover (e.g. &#x3C;c> UIElement.PreviewKeyDown &#x3C;/c> on a /// non-FrameworkElement, or app-side custom routed events). &#x3C;/summary> public void AddRawRoutedHandler ( UIElement target, RoutedEvent re, Delegate h, bool handledEventsToo ) => target.AddHandler(re, h, handledEventsToo); } MountContext.RentControl&#x3C;T>() is how a handler obtains its WinUI control. By default the engine pools every type that opts in via PoolableTypes — Button , TextBlock , Border , every common leaf — and RentControl returns a reset instance from the per-type stack when one is available. Authors can override the pool policy or supply a custom Func&#x3C;T> factory for controls that need constructor arguments or that should never pool. RentControl is therefore the single place a handler crosses into native land, and the engine ensures that every rented control reaches ReturnControl on unmount. MountContext.BindFor(control, element) produces a ReactorBinding&#x3C;TElement> that wires events from the WinUI side back to element callbacks. The binding has typed On… methods for the WinUI routed-event families (pointer, key, tap, focus) and a general OnCustomEvent&#x3C;TArgs> for plain CLR events ( Click , ValueChanged , TextChanged ). The critical property of every On… method is that the event trampoline captures the control , not the element — at fire time it re-reads the live element from Reconciler.GetElementTag(control) . The same subscription therefore survives every re-render without re-subscribing. UpdateContext is the same surface as MountContext minus RentControl — update bodies run against an existing control, and allocating a new one is forbidden on this path. UnmountContext is narrower still: it carries only ReturnControl&#x3C;T> for the optional pool-return contract. The mount-then-subscribe invariant # A controlled property — a property whose value the framework writes and whose value the user can edit on the WinUI side — would race if the handler subscribed to the change event before writing the initial value. The descriptor interpreter (and every hand-coded handler that follows the same shape) sequences the work in two passes so the race cannot happen: public TControl Mount ( MountContext ctx, TElement el ) { var ctrl = ctx.RentControl(_descriptor.PoolPolicy, _descriptor.Factory); // §14 Phase 3-final: when the descriptor declares an ItemsHost, // populate the items collection BEFORE the prop loop. Initial writes // for selection-tracking props (SelectedIndex/SelectedItem) need the // collection populated first — WinUI silently clamps selection // against an empty collection. if (_descriptor.Children is ItemsHost&#x3C;TElement, TControl> ih) DispatchItemsHostMount( in ctx, el, ctrl, ih); // §14 Phase 3 finish — consolidated dispatch arm: every items- // binder variant uses the same \"bind before prop loop\" ordering so // SelectedIndex initial writes land against a populated list. else if (_descriptor.Children is IItemsBinderStrategy binder &#x26;&#x26; ctrl is FrameworkElement feBinder) binder.Bind(feBinder, oldElement: null , el, ctx.Reconciler, ctx.RequestRerender, isMount: true ); // Phase 1: all bare initial writes (no echo possible — subscriptions // not yet live). §14 Phase 3-final: dispatch through the // context-carrying overload so OneWayBridged entries can reach the // reconciler/rerender helpers; existing entries forward to the // parameterless overload via the virtual default on PropEntry. var props = _descriptor.Properties; for ( int i = 0 ; i &#x3C; props.Count; i++) props[i].Mount( in ctx, ctrl, el); // Phase 2: subscribe controlled entries. var binding = ctx.BindFor(ctrl, el); for ( int i = 0 ; i &#x3C; props.Count; i++) props[i].EnsureSubscribed(binding, ctrl, el); var getSetters = _descriptor.GetSetters; if (getSetters is not null ) ctx.ApplySetters(getSetters(el), ctrl); return ctrl; } The first loop runs the bare initial writes for every entry. The second loop wires the trampolines. Because no trampoline is listening during the first loop, the synchronous change events WinUI raises when the framework writes IsOn = true , Value = 3.5 , or Text = \"hi\" go nowhere — and the user's callback receives no spurious initial fire on Mount. On Update the equivalent property writes go through ReactorBinding.WriteSuppressed , which marks a per-control suppress counter that the trampoline drains before invoking the callback. The net effect is that the callback fires exactly when the user interacted with the control , never when the framework wrote a value the user did not type. The same invariant covers pool rent: a control returned to the pool keeps its typed event-payload box, and re-mount checks the box's Trampoline slot to skip re-subscription. A pooled RatingControl that gets rented out for a fresh StarMeterElement does not double-subscribe its ValueChanged event. Children strategies # A handler's Children property declares how its child elements get dispatched. The strategy is a sealed record subclass; the adapter's switch over the strategy types runs after Mount / Update return, so the handler body itself never has to walk children. [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public abstract record ChildrenStrategy &#x3C; TElement , TControl > where TElement : Element where TControl : UIElement ; /// &#x3C;summary> Leaf — no children. Engine performs no dispatch beyond the /// handler's Mount/Update body. &#x3C;/summary> [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public sealed record None &#x3C; TElement , TControl >() : ChildrenStrategy&#x3C;TElement, TControl> where TElement : Element where TControl : UIElement ; /// &#x3C;summary> Single-content host (Border, ContentControl, Viewbox). The /// engine mounts &#x3C;paramref name=\"GetChild\"/> 's result and assigns it via /// &#x3C;paramref name=\"SetChild\"/> . /// /// &#x3C;para> &#x3C;b> Structural reconcile: &#x3C;/b> set &#x3C;see cref=\"GetCurrentChild\"/> so /// the engine can read the existing slot value during &#x3C;c> Update &#x3C;/c> and /// route through &#x3C;c> Reconciler.ReconcileV1Child &#x3C;/c> — that preserves /// descendant component state across parent re-renders. When left null, /// the engine remounts the child on every update (only safe for slots that /// are reset every render anyway). All built-in handlers set it. &#x3C;/para> &#x3C;/summary> [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public sealed record SingleContent &#x3C; TElement , TControl >( Func&#x3C;TElement, Element?> GetChild, Action&#x3C;TControl, UIElement?> SetChild ) : ChildrenStrategy&#x3C;TElement, TControl> where TElement : Element where TControl : UIElement { /// &#x3C;summary> Optional: read the current child from the control. Required /// for structural child reconciliation; if null the engine falls back to /// remounting the child every Update. &#x3C;/summary> public Func&#x3C;TControl, UIElement?>? GetCurrentChild { get ; init ; } } /// &#x3C;summary> Panel host (StackPanel, Grid, Canvas). Engine mounts each /// child and appends to the panel's &#x3C;see cref=\"UIElementCollection\"/> . /// /// &#x3C;para> &#x3C;b> Phase 1 limitation: &#x3C;/b> the dispatch is append-only; structural /// diff against the previous render goes through the host's child /// collection wholesale. Spec-042 keyed-reconcile integration is a /// Phase 3 follow-up. &#x3C;/para> /// /// &#x3C;para> &#x3C;b> §14 Phase 3-final addition: &#x3C;/b> /// &#x3C;see cref=\"PerChildAttached\"/> — optional callback invoked after each /// child mount AND after each child Update. Receives the mounted /// &#x3C;see cref=\"UIElement\"/> alongside the child element so the descriptor /// can write WinUI attached DPs (e.g. &#x3C;c> Grid.SetRow &#x3C;/c> , /// &#x3C;c> Canvas.SetLeft &#x3C;/c> ) based on attached-prop hints carried on the /// child element. No-op by default. &#x3C;/para> /// /// &#x3C;para> &#x3C;b> §14 Phase 3 close-out addition: &#x3C;/b> /// &#x3C;see cref=\"PerChildAttachedAfterAll\"/> — optional two-pass callback /// invoked once after every child has been mounted (Mount path) or /// reconciled (Update path). Receives the full &#x3C;c> (UIElement, Element) &#x3C;/c> /// pair list in collection order so the descriptor can apply attached /// DPs that reference OTHER children by name (e.g. /// &#x3C;c> RelativePanel.SetRightOf(b, a) &#x3C;/c> ). Distinct from /// &#x3C;see cref=\"PerChildAttached\"/> , which fires per-child mid-pass and /// cannot see siblings that haven't mounted yet. Most descriptors set /// only one of the two; &#x3C;c> RelativePanel &#x3C;/c> is the canonical consumer /// of the after-all shape. &#x3C;/para> /// &#x3C;/summary> [ Experimental( \"REACTOR_V1_PREVIEW\" ) ] public sealed record Panel &#x3C; TElement , TControl >( Func&#x3C;TElement, IReadOnlyList&#x3C;Element>> GetChildren, Func&#x3C;TControl, UIElementCollection> GetCollection ) : ChildrenStrategy&#x3C;TElement, TControl> where TElement : Element where TControl : UIElement { /// &#x3C;summary> Optional per-child attached-prop writer. Called by the /// engine after each child is mounted (Mount path) AND after each child /// is reconciled (Update path). The descriptor reads attached-prop /// hints off the child element (via &#x3C;c> Element.GetAttached&#x26;lt;T&#x26;gt;() &#x3C;/c> /// or similar) and writes the corresponding WinUI attached DPs onto /// the mounted &#x3C;see cref=\"UIElement\"/> . Defaults to &#x3C;c> null &#x3C;/c> for /// containers that don't carry per-child attached props /// (e.g. &#x3C;c> StackPanel &#x3C;/c> ). &#x3C;/summary> public Action &#x3C; TControl , UIElement , Element >? PerChildAttached { get ; init ; } /// &#x3C;summary> Optional two-pass callback fired once after every child has /// been mounted/reconciled, receiving the full ordered list of /// &#x3C;c> (UIElement, Element) &#x3C;/c> pairs. Use for attached DPs that /// reference siblings by name — e.g. &#x3C;c> RelativePanel.SetRightOf &#x3C;/c> . /// Defaults to &#x3C;c> null &#x3C;/c> ; only RelativePanel-shaped descriptors set /// it. &#x3C;/summary> public Action&#x3C;TControl, IReadOnlyList&#x3C;(UIElement Mounted, Element ChildElement)>>? PerChildAttachedAfterAll { get ; init ; } } Strategy Shape Used by None Leaf — no children Button , TextBlock , Slider , RatingControl , every leaf SingleContent One child slot, GetCurrentChild for structural reconcile Border , ContentControl , Viewbox , Expander body Panel Ordered child list against UIElementCollection , optional PerChildAttached for attached DPs Grid , StackPanel , Canvas , RelativePanel NamedSlots Multiple named slots, each binding via a NamedSlot record SplitView (Pane + Content), NavigationView (Header + Content + PaneFooter) ItemsHost Flat items collection — IReadOnlyList&#x3C;object> projected into the control's Items sink ListBox , ComboBox , RadioButtons , simple FlipView TemplatedItems&#x3C;TItem,…> Keyed templated list — routes through BindKeyedItemsSource for spec-042 keyed reconcile ListView&#x3C;T> , GridView&#x3C;T> TemplatedItemsErased Same shape, T erased via IKeyedItemSource for typed-list base types TemplatedListViewElement&#x3C;T> family TreeChildren Hierarchical TreeViewNode tree, optional per-node ContentElement TreeView TabItemsHost Heterogeneous items ( TabViewItem / PivotItem ) with in-place positional reconcile TabView , Pivot PreMountedItems Pre-mounted items against a flat sink with no ContainerContentChanging Templated FlipView Imperative Escape hatch — handler reconciles children itself Custom containers that span multiple WinUI properties The strategy switch is the engine's seam between the handler protocol and the rest of the reconciler — SingleContent and NamedSlots route through Reconciler.ReconcileV1Child , which is the same diff path the rest of Reactor uses; Panel walks the new and old child lists in lockstep and preserves descendant component state across reorderings that do not change identity; TemplatedItems and TemplatedItemsErased route through Reconciler.BindKeyedItemsSource , which owns the spec-042 KeyedListDiff realization plumbing — and so on. Adding a new strategy is a Reactor-internals task; adding a new handler that consumes one of these strategies is what extension authors do. Pool integration # The descriptor's PoolPolicy (and the corresponding parameter to MountContext.RentControl&#x3C;T> ) determines whether the control type pools at all and, if so, how to reset its mutable state when it goes back into the pool. Most leaf controls — Button , TextBlock , Border , RatingControl , every input the form catalog covers — opt in by default; the per-type entry in PoolableTypes declares the reset hook (clearing handlers, restoring default property values) so a rented instance is observably indistinguishable from a fresh one. Authors writing a new handler should generally accept the default and let the engine pool the control; the policy escape hatch exists for controls whose state is too large or whose teardown is too expensive to be worth pooling. MountContext.RentControl&#x3C;T> is the single rent path. The engine guarantees that every rent reaches a ReturnControl on unmount — the default adapter body calls Unmount with a V1UnmountDisposition result that opts into pool collection, the engine then walks the unmounted subtree and returns each control to its per-type stack. An author writing Unmount does not have to call ReturnControl themselves unless they rented from the engine via the manual RentControl overload — the default lifecycle is fully managed. Echo suppression and WriteSuppressed # When the framework writes the controlled prop, the WinUI control raises its change event synchronously. The event trampoline cannot distinguish \"the user typed this\" from \"the framework just wrote this\" without help, and forwarding the framework's own write to the element callback would round-trip the same value through the user's state setter and trigger a spurious re-render. ReactorBinding suppresses that echo with a per-control counter: Method When to call Effect binding.WriteSuppressed(mutate) Inside Update when writing a controlled prop Increments the suppress counter, runs the mutate, decrements ChangeEchoSuppressor.ShouldSuppress(fe) Inside an event trampoline Drains a pending suppress count and returns true; trampoline short-circuits The trampolines ReactorBinding.OnCustomEvent&#x3C;TArgs> generates call ShouldSuppress on entry — every controlled prop in every built-in handler is echo-safe without the handler author having to think about it. Hand-authored trampolines (the ones that bypass OnCustomEvent for performance) follow the same shape; see Reconciler.GetOrCreateControlEventPayload for the per-control payload box that anchors the trampoline for the control's lifetime. Element identity via the tag # Every framework element record is anchored on its mounted FrameworkElement via Reconciler.SetElementTag(fe, element) , an attached state DP. The tag is what makes event trampolines safe across re-renders: the trampoline closes over the control instance, calls Reconciler.GetElementTag(control) at fire time, casts the result to the typed element, and invokes the callback. The element value is refreshed by Reconciler.SetElementTag on every Update, so the trampoline always sees the live element — including the live callback, the live state captured by the user's component, and any intervening prop changes. Handler authors get the tag wiring for free — the adapter writes it after Mount returns and after every Update . Hand-authored trampolines that need to read the tag (e.g. the descriptor's static ClickTrampoline for Button ) call GetElementTag and pattern-match on the expected element type. Descriptors — declarative handlers # IElementHandler is the imperative shape; ControlDescriptor is the declarative one. A descriptor is a list of PropEntry objects (each encoding one property's binding shape) plus an optional children strategy, optional factory, and optional pool policy. The DescriptorHandler&#x3C;TElement, TControl> interpreter is itself an IElementHandler , so the registry sees no difference between a hand-coded handler and a descriptor — the dispatch shell is identical, and any cost difference is the interpreter's iteration over a small list of entries. // A descriptor declares property bindings against the WinUI control the // element targets. The framework's DescriptorHandler interprets the entries // during Mount and Update — there is no per-element interpreter overhead // beyond a dictionary lookup and the entry-loop iteration. public static class LedIndicatorDescriptor { public static readonly ControlDescriptor&#x3C;LedIndicatorElement, WinUI.Border> Descriptor = new ControlDescriptor&#x3C;LedIndicatorElement, WinUI.Border> { // The descriptor's children strategy says \"no children\" — this is // a leaf control. See ChildrenStrategy survey in the prose. Children = new None&#x3C;LedIndicatorElement, WinUI.Border>(), } // OneWay: write on Mount, diff-and-write on Update. Equality skips // the write when the element value didn't change. .OneWay( get : static e => e.Size, set : static (c, v) => { c.Width = v; c.Height = v; c.CornerRadius = new Microsoft.UI.Xaml.CornerRadius(v / 2 ); }) // The IsOn → Background mapping coerces both inputs onto one WinUI // property. A single OneWay entry observes (Color, IsOn) jointly // by reading both off the element in the get/set lambdas. .OneWay( get : static e => (e.Color, e.IsOn), set : static (c, v) => c.Background = new SolidColorBrush( v.IsOn ? v.Color : Color.FromArgb( 0x40 , v.Color.R, v.Color.G, v.Color.B))); } The descriptor above declares a single one-way mapping from (Color, IsOn) onto Border.Background , plus a size hook that writes Width , Height , and CornerRadius in one entry. It is a complete extension: every LedIndicatorElement the app renders flows through this descriptor, including pool rent, prop diff, and unmount. PropEntry shapes — the prop-binding vocabulary # The fluent builder methods on ControlDescriptor add one entry per call. The full vocabulary covers the common cases plus the escape hatches a hand-coded handler would otherwise need: Builder When to reach for it .OneWay(get, set) Plain one-way prop; write on Mount, diff-write on Update. No event subscription. .OneWayConditional(get, set, shouldWrite) Nullable / optional prop where leaving the control's default is the intent when the element didn't set a value. .Initial(get, set) Write once at Mount and never again. Use for seed-only props ( TextBox.InitialText , etc.). .Controlled&#x3C;TValue, TArgs>(get, set, subscribe, unsubscribe, callback, readBack) Two-way prop. Framework writes with echo suppression; user input rounds back via the change event. Subscription is gated on callback returning non-null. .CoercingOneWay(get, set, coercesController) One-way prop whose write may coerce a sibling controlled value (e.g. Slider.Minimum re-clamping Slider.Value ); writes through WriteSuppressed when the coercion fires. .HandCodedControlled&#x3C;TPayload, TValue, TDelegate> Two-way prop with a multi-event control where the standard Controlled shape would collide on the per-control event payload ( TextBox.Text alongside SelectionChanged ). .HandCodedEvent&#x3C;TPayload, TDelegate> Fire-and-forget event with no associated DP ( Button.Click , Image.ImageOpened ). .CollectionDiffControlled&#x3C;…> Two-way prop whose value is an IReadOnlyList&#x3C;TItem> round-tripped against a control-side IList&#x3C;TItem> ( CalendarView.SelectedDates ). .OneWayBridged(get, set, shouldWrite) One-way prop whose set lambda needs access to the reconciler (used by the button-family flyout port). .Imperative(mount, update) Property-level escape hatch — the lambdas receive (control, element) and (control, oldEl, newEl) so an entry can express a diff the standard builders cannot. .ImperativeBridged(mount, update) Same shape with the MountContext / UpdateContext available — primary use is a secondary Element slot that needs structural reconcile via ReconcileV1Child . The entries iterate as a flat list, in declaration order. The descriptor's children strategy runs first when it is an items binder (so selection-tracking props see the populated collection at Mount); otherwise the strategy fires after the entry loop. The mount-then- subscribe ordering invariant is preserved by running every entry's Mount body before any entry's EnsureSubscribed . Registration # Handlers are registered with a Reactor host's reconciler before the first render. The standard call is Reconciler.RegisterHandler&#x3C;TElement, TControl>(handler) : // Registration is one call per Reactor host. RegisterDescriptor wraps // RegisterHandler&#x3C;...>(new DescriptorHandler&#x3C;...>(descriptor)) — both shapes // land on the same dispatch table. Duplicate registrations for the same // element type throw. public sealed class LedIndicatorRegistration { public static void Register ( Reconciler reconciler ) { reconciler.RegisterHandler&#x3C;LedIndicatorElement, WinUI.Border>( new DescriptorHandler&#x3C;LedIndicatorElement, WinUI.Border>( LedIndicatorDescriptor.Descriptor)); } } There are two variants worth knowing about: Method Use when RegisterHandler&#x3C;TElement, TControl>(handler) Standard registration — exact-type dispatch on element.GetType() . RegisterHandlerForDerivedTypes&#x3C;TBase, TControl>(handler) T-erased registration on a non-generic base type — every closed-T variant whose chain reaches TBase routes to the same handler. Used by the typed templated-list family ( TemplatedListViewElement&#x3C;T> , etc.). Exact-type registrations always win over a derived-type registration for the same chain. Duplicate registration for the same element type throws — there is no last-writer-wins. If you need a different mapping for the same element type (e.g., a test harness substituting a fake control), build a separate Reconciler instance for the scope that needs the override. Patterns # A handful of recurring patterns cover the long tail of real handlers: Map two element fields onto one control property. Use a single .OneWay entry whose get returns a tuple and whose set reads both fields. The descriptor's value comparer treats the tuple atomically, so the write fires only when either field changes. This is how the LED demo collapses (Color, IsOn) into a single Background update. Map one element field onto multiple control properties. Same shape: one .OneWay entry whose set body writes all of them. The diff-check still runs only against the element's source field. Gate subscription on callback presence. Pass e => e.OnXxx as the callback parameter to .Controlled or .HandCodedEvent . When the element returns null the engine skips subscription entirely — callback-less controls pay zero per-fire dispatch cost. This is the standard idiom; do not gate manually inside a hand-authored trampoline. Survive across re-renders. Hand-authored trampolines that bypass OnCustomEvent must read the live element via Reconciler.GetElementTag(control) rather than capturing the element in the closure — the closure-captured element goes stale every render and the callback observes the wrong state. Every built-in HandCodedEvent trampoline follows this shape; copy it. Common Mistakes # Subscribing inside Update . Each Update call runs against an existing control whose subscriptions are already wired from Mount . Subscribing again in Update multiplies the trampolines across re-renders. The protocol's contract is: subscribe in Mount (or, for descriptors, in EnsureSubscribed which the interpreter calls after the Mount entry loop and lazily on null → non-null callback transitions). Writing without WriteSuppressed from Update . A controlled prop write from Update raises the change event synchronously and echoes through the trampoline back into the user's setter. Every controlled-shape descriptor entry already wraps the write; hand-coded handlers should mirror this. Forgetting it is the most common cause of \"the value snaps back when I type\" bugs. Capturing the element in a trampoline closure. Closures over the element observe a stale value on the next render. Re-fetch via Reconciler.GetElementTag(control) on every fire instead. Stashing state on the handler instance. Handler instances are registered once per host and live as long as the reconciler. Per- control state needs to live on the control (attached state DP, typed event payload box), not on the handler. The handler is a stateless interpreter. Registering a handler against an open generic. The dispatch table keys on System.Type , so an open generic like typeof(DataGrid&#x3C;>) has no usable runtime type. Use RegisterHandlerForDerivedTypes with a non-generic intermediate base when you need to catch every closed variant of a generic element family. Tips # Prefer descriptors for new ports. A descriptor is a declarative list that the interpreter executes against the same surface a hand- coded handler uses. The dispatch shape is identical; the descriptor just reads better. Reach for a hand-coded handler when the prop diffing legitimately needs imperative logic (multi-source props, template-part trampolines), and use the .Imperative / .HandCoded… escape hatches inside a descriptor before writing a full hand-coded handler. Read the RatingControl descriptor before writing your own. The built-in descriptors in src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ are the authoritative examples of every shape. Start from the closest match to your control and adapt; copy the comments along with the code so future maintainers see the rationale. Let the engine handle pool integration. Default PoolPolicy covers every leaf control already in PoolableTypes . Custom pool policies exist for the cases where the default does not fit; do not reach for them as a first move. Test the controlled-prop round trip with a real pointer driver. The echo-suppression contract is invisible until two writes race. The existing SelfTests fixtures (in tests/Reactor.AppTests.Host/SelfTest/Fixtures/ ) are the template — copy the pattern that drives the control via an AutomationPeer IInvokeProvider / IValueProvider and asserts the callback fired exactly once. Next Steps # Extending Reactor with Native Controls — the cookbook companion to this page. Reconciliation — how the reconciler decides Mount vs Update vs Unmount, and where Reconcile calls into the handler. Element Pool — what RentControl and ReturnControl actually do. Hooks Internals — how the re-render side of the same loop works. Modifier System — the Setters chain a descriptor applies after its own prop loop.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/control-reconciler-protocol.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/control-reconciler-protocol.md",
      "headings": [
        {
          "depth": 2,
          "id": "dispatch--one-handler-per-element-type",
          "text": "Dispatch — one handler per element type"
        },
        {
          "depth": 2,
          "id": "the-handler-contract",
          "text": "The handler contract"
        },
        {
          "depth": 2,
          "id": "contexts-and-bindings",
          "text": "Contexts and bindings"
        },
        {
          "depth": 2,
          "id": "the-mount-then-subscribe-invariant",
          "text": "The mount-then-subscribe invariant"
        },
        {
          "depth": 2,
          "id": "children-strategies",
          "text": "Children strategies"
        },
        {
          "depth": 2,
          "id": "pool-integration",
          "text": "Pool integration"
        },
        {
          "depth": 2,
          "id": "echo-suppression-and-writesuppressed",
          "text": "Echo suppression and WriteSuppressed"
        },
        {
          "depth": 2,
          "id": "element-identity-via-the-tag",
          "text": "Element identity via the tag"
        },
        {
          "depth": 2,
          "id": "descriptors--declarative-handlers",
          "text": "Descriptors — declarative handlers"
        },
        {
          "depth": 3,
          "id": "propentry-shapes--the-prop-binding-vocabulary",
          "text": "PropEntry shapes — the prop-binding vocabulary"
        },
        {
          "depth": 2,
          "id": "registration",
          "text": "Registration"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Extending Reactor Controls",
      "path": "extending-reactor-controls.md",
      "slug": "/extending-reactor-controls/",
      "html": "<p>Microsoft.UI.Reactor (Reactor) ships handlers for the built-in WinUI\ncontrol gallery, but the protocol that drives them is the same surface\nauthors use to add their own native controls. The contract you wire\nup — an immutable element record, a descriptor or hand-coded handler,\na registration call against the reconciler — gives a custom control\nthe same lifecycle, the same diff-and-write efficiency, the same pool\nreuse, and the same echo-safe two-way binding as <code>Button</code>,\n<a href=\"/forms/\"><code>TextBox</code></a>, and <code>TabView</code>. This page is the cookbook for\nthat flow. By the end you will have added a <code>StarMeter</code> element to a\nReactor app, wired it to WinUI's <code>RatingControl</code>, and matched the\nbuilt-in performance bar without writing a line of imperative\ninterpreter code. Read the reference companion\n<a href=\"/control-reconciler-protocol/\">The Control Reconciler Protocol</a> for the model the steps here\nplug into.</p>\n<h1 id=\"extending-reactor-with-native-controls\">Extending Reactor with Native Controls<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#extending-reactor-with-native-controls\">#</a></h1>\n<p>The audience is anyone wrapping a native Windows control — a third-\nparty WinUI control, a custom <code>Control</code> subclass, or an existing WinUI\ncontrol that the built-in catalog has not yet covered. The walk-through\nports <code>Microsoft.UI.Xaml.Controls.RatingControl</code> end-to-end so the\nshape of every step is concrete; the patterns transfer to anything\nthat descends from <code>FrameworkElement</code>. Cross-reference the built-in\ndescriptors in <code>src/Reactor/Core/V1Protocol/Descriptor/Descriptors/</code>\nas you adapt the example — they are the production examples of every\nshape this page mentions.</p>\n<h2 id=\"the-five-step-playbook\">The five-step playbook<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-five-step-playbook\">#</a></h2>\n<p>The whole flow is five steps, plus an optional sixth for control\nfamilies that need a derived-type registration. The same five steps\ncover every leaf control in the built-in catalog; you can do them in\nany order, but the listing order is the order they typically come up.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Step</th><th>What you write</th><th>Where it lives</th></tr></thead><tbody><tr><td>1</td><td>An <code>Element</code> record subclass with one field per prop and one field per event callback</td><td>App code, next to the component that uses it</td></tr><tr><td>2</td><td>A <code>ControlDescriptor&#x3C;TElement, TControl></code> (or <code>IElementHandler&#x3C;TElement, TControl></code>) wiring each field to a WinUI property or event</td><td>A <code>static class</code> in the same file or a sibling</td></tr><tr><td>3</td><td>A <code>Register</code> extension method that calls <code>reconciler.RegisterHandler(...)</code></td><td>Co-located with the descriptor</td></tr><tr><td>4</td><td>A call to <code>Register</code> against your host's reconciler at startup</td><td>App bootstrap, before the first render</td></tr><tr><td>5</td><td>A factory (<code>new MyElement { ... }</code> or a <code>static Element</code> helper)</td><td>Anywhere — usage shape is up to you</td></tr></tbody></table>\n<p>A sixth step — <code>RegisterHandlerForDerivedTypes</code> against a non-generic\nintermediate base — lets one registration catch every closed-T\nvariant of a generic element family. It is what the typed templated-\nlist ports use; you only need it if you are adding a similar family.</p>\n<h2 id=\"step-1--define-the-element-record\">Step 1 — Define the Element record<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-1--define-the-element-record\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// An Element subclass with one controlled prop (Value), three one-way</span>\n<span class=\"hljs-comment\">// props (MaxRating, Caption, IsClearEnabled), and one callback (OnValueChanged).</span>\n<span class=\"hljs-comment\">// Records give the reconciler value-equality for free — two StarMeterElement</span>\n<span class=\"hljs-comment\">// instances with identical fields compare equal and Update becomes a no-op.</span>\n<span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">StarMeterElement</span> : <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">double</span> Value { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">int</span> MaxRating { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; } = <span class=\"hljs-number\">5</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Caption { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">bool</span> IsClearEnabled { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; } = <span class=\"hljs-literal\">true</span>;\n    <span class=\"hljs-keyword\">public</span> System.Action&#x3C;<span class=\"hljs-built_in\">double</span>>? OnValueChanged { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n}\n</code></pre>\n<p>The record encodes everything the element exposes — controlled props,\none-way props, the optional caption, and the user's\n<code>OnValueChanged</code> callback. The choice of <code>record</code> matters: the\nreconciler diffs elements with value equality, so two\n<code>StarMeterElement</code> instances with identical fields compare equal and\nthe descriptor's Update path becomes a no-op. Reach for <code>record class</code>\nwhen the element will be created millions of times per second (so the\nsealed-record allocation is the dominant cost); reach for <code>record struct</code> only after profiling shows the heap pressure matters — most\ncustom controls are well-served by the default sealed-record shape.</p>\n<p>The fields below are the only thing your component author touches.\nEvery named property maps to one descriptor entry in Step 2.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Field</th><th>Kind</th><th>Why</th></tr></thead><tbody><tr><td><code>Value</code></td><td>controlled <code>double</code></td><td>The user can edit it; the framework writes it; the callback echoes the user's edits.</td></tr><tr><td><code>MaxRating</code></td><td>one-way <code>int</code></td><td>Driven by the element, never edited on the WinUI side.</td></tr><tr><td><code>Caption</code></td><td>optional one-way <code>string?</code></td><td>Should leave the control's default when the element didn't supply one.</td></tr><tr><td><code>IsClearEnabled</code></td><td>one-way <code>bool</code></td><td>Same as MaxRating — declarative knob.</td></tr><tr><td><code>OnValueChanged</code></td><td>callback <code>Action&#x3C;double>?</code></td><td>Subscribes only when non-null; the descriptor gates the trampoline on it.</td></tr></tbody></table>\n<p>Inherit from <code>Element</code> (not <code>FrameworkElement</code>, not <code>Control</code>) — this\nis the Reactor record, not the WinUI control. The built-in fluent\nmodifier chain (<code>.Width(...)</code>, <code>.Margin(...)</code>, <code>.Foreground(...)</code>)\napplies to the built-in element types it was authored against; a\ncustom element does not get those modifiers for free. Authors who\nwant chainable modifiers on their own element add an\n<code>Action&#x3C;TControl>[] Setters</code> field and an extension-method namespace\nthat appends to it, then point the descriptor's <code>GetSetters</code> selector\nat the field. The\n<a href=\"/modifier-system/\">Modifier System</a> page walks through the chain\nshape; keeping the demo focused, the cookbook below omits it.</p>\n<h2 id=\"step-2--choose-a-shape-descriptor-vs-hand-coded\">Step 2 — Choose a shape (descriptor vs hand-coded)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-2--choose-a-shape-descriptor-vs-hand-coded\">#</a></h2>\n<p>Reactor's handler protocol has two surfaces — declarative\n(<a href=\"/control-reconciler-protocol/#descriptors--declarative-handlers\"><code>ControlDescriptor</code></a>)\nand imperative\n(<a href=\"/control-reconciler-protocol/#the-handler-contract\"><code>IElementHandler</code></a>) — that\nland on the same dispatch table. New controls should default to a\ndescriptor; reach for a hand-coded handler only when the descriptor\nescape hatches (<code>.Imperative</code>, <code>.HandCoded…</code>) still cannot express\nthe diff.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Use a descriptor when</th><th>Reach for a hand-coded handler when</th></tr></thead><tbody><tr><td>Props map 1:1 to WinUI properties, or jointly through a tuple <code>get</code></td><td>Mount has irreducible imperative logic (template-part lookups via <code>OnApplyTemplate</code>, multi-control composition)</td></tr><tr><td>Events follow <code>subscribe(handler) ↔ readBack(control)</code></td><td>Events route through non-CLR shapes (raw routed events, native callbacks)</td></tr><tr><td>The control fits one of the standard <code>ChildrenStrategy</code> shapes</td><td>The control has a children-realization model the strategy set does not cover (custom virtualization, mixed item types per slot)</td></tr><tr><td>You want the next reader to skim the spec, not the code</td><td>You are wrapping a control whose contract the framework cannot yet express declaratively, and adding a new entry shape would not pay back</td></tr></tbody></table>\n<p>Every built-in port in <code>src/Reactor/Core/V1Protocol/Descriptor/Descriptors/</code>\nis a descriptor. The few hand-coded <code>IElementHandler</code> implementations\nin <code>src/Reactor/Core/V1Protocol/Handlers/</code> are the ones whose diff\nshape genuinely does not fit a declarative entry list — read them as\nthe bar for \"complicated enough to write by hand.\"</p>\n<h2 id=\"step-3--wire-props\">Step 3 — Wire props<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-3--wire-props\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StarMeterDescriptor</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> ControlDescriptor&#x3C;StarMeterElement, WinUI.RatingControl> Descriptor =\n        <span class=\"hljs-keyword\">new</span> ControlDescriptor&#x3C;StarMeterElement, WinUI.RatingControl>\n        {\n            <span class=\"hljs-comment\">// Leaf control — no children. (See ChildrenStrategy survey for</span>\n            <span class=\"hljs-comment\">// the other shapes: SingleContent, Panel, NamedSlots, ItemsHost…)</span>\n            Children = <span class=\"hljs-keyword\">new</span> None&#x3C;StarMeterElement, WinUI.RatingControl>(),\n        }\n        <span class=\"hljs-comment\">// OneWay props: written on Mount, diff-and-written on Update.</span>\n        .OneWay(\n            <span class=\"hljs-keyword\">get</span>: <span class=\"hljs-keyword\">static</span> e => e.MaxRating,\n            <span class=\"hljs-keyword\">set</span>: <span class=\"hljs-keyword\">static</span> (c, v) => c.MaxRating = v)\n        .OneWay(\n            <span class=\"hljs-keyword\">get</span>: <span class=\"hljs-keyword\">static</span> e => e.IsClearEnabled,\n            <span class=\"hljs-keyword\">set</span>: <span class=\"hljs-keyword\">static</span> (c, v) => c.IsClearEnabled = v)\n        <span class=\"hljs-comment\">// OneWayConditional skips the write when the predicate is false —</span>\n        <span class=\"hljs-comment\">// leaves Caption at the control's default for elements that didn't</span>\n        <span class=\"hljs-comment\">// supply one, rather than forcing it to null and losing a style.</span>\n        .OneWayConditional(\n            <span class=\"hljs-keyword\">get</span>:         <span class=\"hljs-keyword\">static</span> e => e.Caption,\n            <span class=\"hljs-keyword\">set</span>:         <span class=\"hljs-keyword\">static</span> (c, v) => c.Caption = v!,\n            shouldWrite: <span class=\"hljs-keyword\">static</span> e => e.Caption <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n        <span class=\"hljs-comment\">// Controlled is the two-way binding shape: the framework writes the</span>\n        <span class=\"hljs-comment\">// element's value at Mount (and on diff), suppresses the echo when</span>\n        <span class=\"hljs-comment\">// the framework is the writer, and forwards user input back through</span>\n        <span class=\"hljs-comment\">// OnValueChanged. Subscription is gated on the callback being non-</span>\n        <span class=\"hljs-comment\">// null — if the caller didn't pass OnValueChanged, no trampoline</span>\n        <span class=\"hljs-comment\">// is wired and the per-fire dispatch cost stays at zero.</span>\n        .Controlled&#x3C;<span class=\"hljs-built_in\">double</span>, <span class=\"hljs-built_in\">object</span>>(\n            <span class=\"hljs-keyword\">get</span>:         <span class=\"hljs-keyword\">static</span> e => e.Value,\n            <span class=\"hljs-keyword\">set</span>:         <span class=\"hljs-keyword\">static</span> (c, v) => c.Value = v,\n            subscribe:   <span class=\"hljs-keyword\">static</span> (fe, h) => ((WinUI.RatingControl)fe).ValueChanged += (s, e) => h(s, e!),\n            unsubscribe: <span class=\"hljs-keyword\">static</span> (fe, h) => { <span class=\"hljs-comment\">/* trampoline anchored for control lifetime */</span> },\n            callback:    <span class=\"hljs-keyword\">static</span> e => e.OnValueChanged,\n            readBack:    <span class=\"hljs-keyword\">static</span> c => c.Value);\n}\n</code></pre>\n<p>Each fluent builder call adds one\n<a href=\"/control-reconciler-protocol/#propentry-shapes--the-prop-binding-vocabulary\"><code>PropEntry</code></a>\nto the descriptor. The interpreter iterates them in declaration order\nduring Mount and Update. The descriptor above uses three of the\ncommon shapes:</p>\n<p><code>.OneWay(get, set)</code> — <code>MaxRating</code> and <code>IsClearEnabled</code>. The\ninterpreter writes the value at Mount; on Update it compares the old\nelement's value against the new and writes only when the values\ndiffer. The default comparer is <code>EqualityComparer&#x3C;TValue>.Default</code>;\npass an explicit comparer to the optional <code>comparer</code> parameter for\nfloating-point tolerance or custom-type equality.</p>\n<p><code>.OneWayConditional(get, set, shouldWrite)</code> — <code>Caption</code>. When the\nelement does not set a caption, the predicate returns <code>false</code> and the\nwrite is skipped, leaving the control's default in place. This is the\nshape to reach for whenever \"no value\" is a meaningful state — every\nnullable / optional prop in the built-in catalog uses it.</p>\n<p><code>.Controlled&#x3C;TValue, TArgs>(...)</code> — <code>Value</code>. The framework writes\nthe element's value on Mount (bare, no echo possible because the\ntrampoline is not yet wired). On Update it writes through\n<code>ReactorBinding.WriteSuppressed</code> so the WinUI side's synchronous\n<code>ValueChanged</code> echo is dropped. On user interaction the trampoline\nreads the live element off the control via the tag, drains the\nsuppress counter (which is zero when the user is the writer), and\ncalls <code>OnValueChanged</code> with the new value. The <code>subscribe</code> lambda's\nshape is <code>(FrameworkElement, EventHandler&#x3C;TArgs>) → void</code>; the\nengine boxes the control identity through <code>FrameworkElement</code> to keep\nthe entry generic across closed-T tuples. <code>TArgs</code> is whatever the\nWinUI event carries — for <code>RatingControl.ValueChanged</code> the args type\nis <code>object</code> because WinUI authors the event with\n<code>TypedEventHandler&#x3C;RatingControl, object></code>. Subscription is <strong>gated\non <code>callback</code> returning non-null</strong> — if the element doesn't carry\n<code>OnValueChanged</code>, no trampoline is wired and the dispatch cost for\nthat prop stays at zero.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The <code>unsubscribe</code> lambda is intentionally a no-op body — descriptor\ntrampolines anchor to the control's lifetime, not the element's. The\ncontrol returns to the pool, the pool reset contract clears the\ntrampoline slot on the typed event-payload box, and the next Mount\nfinds an empty slot and re-subscribes. Writing a real unsubscribe\ninto the lambda double-frees the subscription when the control pools\nand is the most common source of \"the event stops firing after a\nre-render\" bugs. If your control genuinely needs explicit unsubscribe\non unmount, override <code>IElementHandler.Unmount</code> and tear down there.</p>\n</blockquote>\n<h2 id=\"step-4--wire-events-the-non-dp-case\">Step 4 — Wire events (the non-DP case)<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-4--wire-events-the-non-dp-case\">#</a></h2>\n<p><code>StarMeterElement.Value</code> is bound to a property and an event together,\nso it uses the <code>.Controlled</code> shape above. Events that <strong>do not</strong>\nround-trip a DP — <code>Button.Click</code>, <code>Image.ImageOpened</code>,\n<code>TextBox.SelectionChanged</code> — use a sibling builder:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Builder</th><th>Use when</th></tr></thead><tbody><tr><td><code>.HandCodedEvent&#x3C;TPayload, TDelegate>(subscribe, callbackPresent, trampoline, slotIsNull, setSlot)</code></td><td>Fire-and-forget event with no associated DP. The <code>trampoline</code> is a <code>static</code> delegate that closes over no per-instance state; per-control state goes on the <code>TPayload</code> slot.</td></tr><tr><td><code>binding.OnCustomEvent&#x3C;TArgs>(subscribe, unsubscribe, handler)</code> inside a hand-coded handler</td><td>When you have already chosen the hand-coded shape and want the simpler closure-based trampoline. Allocates a closure per Mount instead of reusing a static delegate.</td></tr></tbody></table>\n<p>The <code>ButtonDescriptor</code> source (in\n<code>src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ButtonDescriptor.cs</code>)\nis the canonical example of <code>.HandCodedEvent</code> — including the\nshort-circuit for <code>IsDisabledFocusable</code> and the\n<code>Reconciler.GetElementTag</code> tag refresh.</p>\n<h2 id=\"step-5--declare-a-child-strategy\">Step 5 — Declare a child strategy<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-5--declare-a-child-strategy\">#</a></h2>\n<p><code>StarMeterElement</code> is a leaf, so the descriptor declares\n<code>Children = new None&#x3C;…>()</code>. The dispatch surface is the same as the\n<a href=\"/control-reconciler-protocol/#children-strategies\">ChildrenStrategy survey</a>; the\nright strategy is whichever matches your WinUI control's structural\ncontract:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Your WinUI control's child shape</th><th>Strategy</th></tr></thead><tbody><tr><td>No children</td><td><code>None</code></td></tr><tr><td>One <code>Content</code> / <code>Child</code> slot</td><td><code>SingleContent(GetChild, SetChild) { GetCurrentChild = … }</code></td></tr><tr><td><code>Children</code> collection on a <code>Panel</code></td><td><code>Panel(GetChildren, GetCollection)</code></td></tr><tr><td>Named slots (<code>Header</code>, <code>Content</code>, <code>Footer</code>)</td><td><code>NamedSlots([NamedSlot(\"Header\", …), …])</code></td></tr><tr><td>Flat items collection (<code>Items</code>) of values or pre-built elements</td><td><code>ItemsHost(GetItems, GetCollection)</code></td></tr><tr><td>Typed templated list with keyed reconcile</td><td><code>TemplatedItems&#x3C;TItem, TElement, TControl>(GetItems, KeySelector, BuildItemView)</code></td></tr><tr><td>Hierarchical tree</td><td><code>TreeChildren(GetNodes)</code></td></tr><tr><td>Tabs / pivots with per-item containers</td><td><code>TabItemsHost(GetItems, GetCollection, GetContent, CreateContainer)</code></td></tr></tbody></table>\n<p>The standard strategies cover everything in the built-in catalog. The\n<code>Imperative&#x3C;TElement, TControl></code> escape hatch exists for the case\nwhere none of them fit — for example, a control whose children live\non three different non-collection properties that must reconcile\ntogether. Reach for it last; you give up the engine's keyed reconcile\nand lose descendant component state across re-renders that touch the\nimperative slot.</p>\n<h2 id=\"step-6--register-and-use\">Step 6 — Register and use<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#step-6--register-and-use\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">StarMeterInterop</span>\n{\n    <span class=\"hljs-comment\">// One call per Reactor host. RegisterHandler accepts any IElementHandler,</span>\n    <span class=\"hljs-comment\">// and DescriptorHandler&#x3C;TElement,TControl> is the canonical interpreter</span>\n    <span class=\"hljs-comment\">// for a ControlDescriptor. Duplicate registration for the same element</span>\n    <span class=\"hljs-comment\">// type throws — register exactly once on each host you mount.</span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Register</span>(<span class=\"hljs-params\">Reconciler reconciler</span>)</span> =>\n        reconciler.RegisterHandler&#x3C;StarMeterElement, WinUI.RatingControl>(\n            <span class=\"hljs-keyword\">new</span> DescriptorHandler&#x3C;StarMeterElement, WinUI.RatingControl>(\n                StarMeterDescriptor.Descriptor));\n}\n</code></pre>\n<p>The <code>Register</code> call is the single bridge between your descriptor and\nthe Reactor host. Call it once per <code>ReactorHost</code> instance, before the\nfirst render against that host — typically from your app bootstrap,\nnext to any other interop registrations\n(<code>DockingNativeInterop.Register</code>, <code>XamlInterop.Register</code>, etc.). The\ncall shape is the same whether you registered a descriptor or a hand-\ncoded handler; both land on the same dispatch table.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">ExtendingApp</span> : <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n    {\n        <span class=\"hljs-keyword\">var</span> (rating, setRating) = UseState(<span class=\"hljs-number\">3.5</span>);\n\n        <span class=\"hljs-keyword\">return</span> VStack(<span class=\"hljs-number\">16</span>,\n            TextBlock(<span class=\"hljs-string\">\"StarMeter — custom element wrapping WinUI RatingControl\"</span>)\n                .FontSize(<span class=\"hljs-number\">14</span>).SemiBold(),\n\n            <span class=\"hljs-keyword\">new</span> StarMeterElement\n            {\n                Value = rating,\n                MaxRating = <span class=\"hljs-number\">5</span>,\n                Caption = <span class=\"hljs-string\">\"Rate this page\"</span>,\n                OnValueChanged = setRating,\n            },\n\n            TextBlock(<span class=\"hljs-string\">$\"current rating: <span class=\"hljs-subst\">{rating:<span class=\"hljs-number\">0.0</span>}</span>\"</span>),\n\n            HStack(<span class=\"hljs-number\">8</span>,\n                Button(<span class=\"hljs-string\">\"Reset\"</span>, () => setRating(<span class=\"hljs-number\">0</span>)),\n                Button(<span class=\"hljs-string\">\"5 stars\"</span>, () => setRating(<span class=\"hljs-number\">5</span>)))\n        ).Padding(<span class=\"hljs-number\">20</span>);\n    }\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/extending-reactor-controls/star-meter.png\" alt=\"Custom StarMeter element rendered alongside built-in Reactor controls\"></p>\n<p>That is the entire path from <code>setRating(5)</code> to a frame. The\ncomponent sets state, the reconciler walks the new element tree, the\nregistered descriptor is invoked for <code>StarMeterElement</code>, the\ninterpreter diffs against the previous element (only <code>Value</code>\nchanged), writes through <code>WriteSuppressed</code>, the trampoline drains\nthe suppress counter on the next echo, and the loop ends with the\ncontrol at 5 stars. The rest of the app's controls dispatch through\nthe built-in handlers in lockstep.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<p>A few recurring shapes turn up across most custom-control ports:</p>\n<p><strong>Match a built-in's perf bar without thinking about it.</strong> Use a\ndescriptor; let <code>RentControl</code> pool the WinUI control; let <code>Controlled</code>\nsuppress its own echoes. The interpreter's per-entry diff is a single\nequality check against the previous element's value — unchanged\nprops pay one comparison and zero writes. Pool reuse, echo\nsuppression, and static trampolines are defaults of the protocol, not\nopt-ins.</p>\n<p><strong>Bundle related fields into one prop entry.</strong> A control that\nexposes <code>(Color, IsOn) → Background</code> is one entry, not two — the\n<code>get</code> returns a tuple and the <code>set</code> reads both fields. The diff fires\nexactly when either input changes. This is how the LED indicator in\n<a href=\"/control-reconciler-protocol/#descriptors--declarative-handlers\">the Control Reconciler Protocol reference</a>\ncollapses two element fields into one WinUI write.</p>\n<p><strong>Layer in a Reactor-friendly default for an awkward control.</strong> Many\nWinUI controls require an initial property write to \"behave\"\n(<code>TextBox.PlaceholderText</code>, <code>RatingControl.MaxRating</code>). Use\n<code>.Initial(get, set)</code> to seed those once at Mount without paying the\ndiff-and-write cost on Update. The interpreter ignores Initial entries\non the Update path.</p>\n<p><strong>Wrap a third-party control whose API is unstable.</strong> Put the\ndescriptor in a sealed <code>static class</code>, mark every reference to the\nthird-party type <code>internal</code>, and re-export an <code>Element</code>-typed\nfactory. Your component authors depend on the element, not the\ncontrol. When the third-party control breaks compatibility, you\nupdate one file; every component keeps compiling.</p>\n<p><strong>Pre-flight the perf bar with the bench harness.</strong> The Reactor repo\nincludes a <code>perf_bench</code> project (<code>tests/perf_bench/</code>) that exercises\ndescriptors against the M1 / M2 / M5 / M7 / M10 micro-benches the\nspec uses to validate the descriptor model. Drop your descriptor into\nthe harness and run the relevant bench before locking your port in.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<p><strong>Subscribing in <code>Update</code> instead of <code>Mount</code>.</strong> A descriptor's\n<code>Controlled</code> entry already gates subscription on the\nmount-then-subscribe ordering — the interpreter calls\n<code>EnsureSubscribed</code> once after the Mount loop and again on each Update\nto catch null→non-null callback transitions. If you are hand-coding a\nhandler and forget to centralize subscription in <code>Mount</code>, every\nre-render double-subscribes the event.</p>\n<p><strong>Forgetting <code>WriteSuppressed</code> in a hand-coded <code>Update</code>.</strong> A\ncontrolled prop write from <code>Update</code> echoes through the trampoline\nback into the user's state setter. The descriptor's <code>Controlled</code>\nentry wraps the write for you; a hand-coded handler must mirror it\nwith <code>binding.WriteSuppressed(() => ctrl.Value = newValue)</code>.\nForgetting it is the single most common cause of \"the value snaps\nback when I edit it\" bugs.</p>\n<p><strong>Stashing per-control state on the descriptor / handler.</strong> The\ndescriptor and the handler instance are registered once per host.\nPer-control state needs to live on the <strong>control</strong> — either on the\nattached state DP (use <code>Reconciler.SetElementTag</code> / <code>GetElementTag</code>)\nor on the typed event payload box (use\n<code>Reconciler.GetOrCreateControlEventPayload&#x3C;TPayload></code>). The pool\nreset contract clears both on <code>ReturnControl</code>; descriptor-level state\nsurvives pool rent and corrupts the next mount.</p>\n<p><strong>Registering against the wrong reconciler.</strong> If your app uses\nmultiple <code>ReactorHost</code> instances (a tray icon host, a settings\nwindow, an interop host inside XAML islands), each host has its own\nreconciler. Register on every host that will render your element.\nThe exact-type dispatch will throw <code>Mount</code> for an unregistered\nelement type — it does not fall back to a base type.</p>\n<p><strong>Capturing the element in a static trampoline.</strong> The static\ntrampoline pattern works because it reads the live element on every\nfire. Capturing the element in the static closure is a contradiction\nin terms — there is no closure on a static — but the equivalent\nmistake on a hand-coded trampoline that closes over the element\ninstance does exactly that, and the trampoline observes the element\nthat existed at Mount forever.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Start from the closest built-in descriptor.</strong> Copy the most\nsimilar descriptor in <code>src/Reactor/Core/V1Protocol/Descriptor/Descriptors/</code>,\nstrip what does not apply, and adapt. The built-ins encode the\nproduction patterns for every shape you are likely to need —\nincluding the coercion edge cases (<code>Slider.Min</code>, <code>NumberBox.Max</code>) and\nthe multi-event control patterns (<code>TextBox</code>'s <code>Text</code> +\n<code>SelectionChanged</code>).</p>\n<p><strong>Keep the element record minimal.</strong> Every field is a public\ncontract — once consumers depend on it, you cannot drop or rename it\nwithout churn. Add fields as components need them, not preemptively.</p>\n<p><strong>Test the round trip with the existing fixture harness.</strong> Add a\nfixture in <code>tests/Reactor.AppTests.Host/SelfTest/Fixtures/</code> that\ndrives your control via an automation peer\n(<code>IInvokeProvider</code>/<code>IValueProvider</code>) and asserts the callback fires\nthe right number of times for the right values. The pattern is\nestablished and the fixture runner gives you a stable stable cross-\nprocess test rig for free.</p>\n<p><strong>Document the perf shape inline.</strong> If your control has non-obvious\nperformance characteristics — pool opt-out, custom comparer for\nfloating-point tolerance, deliberately imperative entry — capture\nthe reason in a comment on the descriptor field, not on the calling\ncomponent. The descriptor is where future maintainers will look.</p>\n<p><strong>Read the Control Reconciler Protocol reference once end-to-end.</strong> Steps 1 through 6\nabove are the recipe; <a href=\"/control-reconciler-protocol/\">the protocol page</a> is the\nspecification it implements. The two pages together are the complete\ncontract — bookmark both.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><a href=\"/control-reconciler-protocol/\">The Control Reconciler Protocol</a> — the model the steps on this page plug into.</li>\n<li><a href=\"/reconciliation/\">Reconciliation</a> — what happens before the registered handler is invoked, and how Mount vs Update vs Unmount get chosen.</li>\n<li><a href=\"/element-pool/\">Element Pool</a> — what <code>RentControl</code> actually does, and how the per-type pool keeps <code>Mount</code> allocation-free.</li>\n<li><a href=\"/modifier-system/\">Modifier System</a> — how the <code>Setters</code> chain a descriptor applies after its own prop loop works.</li>\n<li><a href=\"/hooks-internals/\">Hooks Internals</a> — how state changes turn into Render calls, which is the upstream half of every loop the protocol on this page is the downstream half of.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor) ships handlers for the built-in WinUI control gallery, but the protocol that drives them is the same surface authors use to add their own native controls. The contract you wire up — an immutable element record, a descriptor or hand-coded handler, a registration call against the reconciler — gives a custom control the same lifecycle, the same diff-and-write efficiency, the same pool reuse, and the same echo-safe two-way binding as Button , TextBox , and TabView . This page is the cookbook for that flow. By the end you will have added a StarMeter element to a Reactor app, wired it to WinUI's RatingControl , and matched the built-in performance bar without writing a line of imperative interpreter code. Read the reference companion The Control Reconciler Protocol for the model the steps here plug into. Extending Reactor with Native Controls # The audience is anyone wrapping a native Windows control — a third- party WinUI control, a custom Control subclass, or an existing WinUI control that the built-in catalog has not yet covered. The walk-through ports Microsoft.UI.Xaml.Controls.RatingControl end-to-end so the shape of every step is concrete; the patterns transfer to anything that descends from FrameworkElement . Cross-reference the built-in descriptors in src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ as you adapt the example — they are the production examples of every shape this page mentions. The five-step playbook # The whole flow is five steps, plus an optional sixth for control families that need a derived-type registration. The same five steps cover every leaf control in the built-in catalog; you can do them in any order, but the listing order is the order they typically come up. Step What you write Where it lives 1 An Element record subclass with one field per prop and one field per event callback App code, next to the component that uses it 2 A ControlDescriptor&#x3C;TElement, TControl> (or IElementHandler&#x3C;TElement, TControl> ) wiring each field to a WinUI property or event A static class in the same file or a sibling 3 A Register extension method that calls reconciler.RegisterHandler(...) Co-located with the descriptor 4 A call to Register against your host's reconciler at startup App bootstrap, before the first render 5 A factory ( new MyElement { ... } or a static Element helper) Anywhere — usage shape is up to you A sixth step — RegisterHandlerForDerivedTypes against a non-generic intermediate base — lets one registration catch every closed-T variant of a generic element family. It is what the typed templated- list ports use; you only need it if you are adding a similar family. Step 1 — Define the Element record # // An Element subclass with one controlled prop (Value), three one-way // props (MaxRating, Caption, IsClearEnabled), and one callback (OnValueChanged). // Records give the reconciler value-equality for free — two StarMeterElement // instances with identical fields compare equal and Update becomes a no-op. public sealed record StarMeterElement : Element { public double Value { get ; init ; } public int MaxRating { get ; init ; } = 5 ; public string ? Caption { get ; init ; } public bool IsClearEnabled { get ; init ; } = true ; public System.Action&#x3C; double >? OnValueChanged { get ; init ; } } The record encodes everything the element exposes — controlled props, one-way props, the optional caption, and the user's OnValueChanged callback. The choice of record matters: the reconciler diffs elements with value equality, so two StarMeterElement instances with identical fields compare equal and the descriptor's Update path becomes a no-op. Reach for record class when the element will be created millions of times per second (so the sealed-record allocation is the dominant cost); reach for record struct only after profiling shows the heap pressure matters — most custom controls are well-served by the default sealed-record shape. The fields below are the only thing your component author touches. Every named property maps to one descriptor entry in Step 2. Field Kind Why Value controlled double The user can edit it; the framework writes it; the callback echoes the user's edits. MaxRating one-way int Driven by the element, never edited on the WinUI side. Caption optional one-way string? Should leave the control's default when the element didn't supply one. IsClearEnabled one-way bool Same as MaxRating — declarative knob. OnValueChanged callback Action&#x3C;double>? Subscribes only when non-null; the descriptor gates the trampoline on it. Inherit from Element (not FrameworkElement , not Control ) — this is the Reactor record, not the WinUI control. The built-in fluent modifier chain ( .Width(...) , .Margin(...) , .Foreground(...) ) applies to the built-in element types it was authored against; a custom element does not get those modifiers for free. Authors who want chainable modifiers on their own element add an Action&#x3C;TControl>[] Setters field and an extension-method namespace that appends to it, then point the descriptor's GetSetters selector at the field. The Modifier System page walks through the chain shape; keeping the demo focused, the cookbook below omits it. Step 2 — Choose a shape (descriptor vs hand-coded) # Reactor's handler protocol has two surfaces — declarative ( ControlDescriptor ) and imperative ( IElementHandler ) — that land on the same dispatch table. New controls should default to a descriptor; reach for a hand-coded handler only when the descriptor escape hatches ( .Imperative , .HandCoded… ) still cannot express the diff. Use a descriptor when Reach for a hand-coded handler when Props map 1:1 to WinUI properties, or jointly through a tuple get Mount has irreducible imperative logic (template-part lookups via OnApplyTemplate , multi-control composition) Events follow subscribe(handler) ↔ readBack(control) Events route through non-CLR shapes (raw routed events, native callbacks) The control fits one of the standard ChildrenStrategy shapes The control has a children-realization model the strategy set does not cover (custom virtualization, mixed item types per slot) You want the next reader to skim the spec, not the code You are wrapping a control whose contract the framework cannot yet express declaratively, and adding a new entry shape would not pay back Every built-in port in src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ is a descriptor. The few hand-coded IElementHandler implementations in src/Reactor/Core/V1Protocol/Handlers/ are the ones whose diff shape genuinely does not fit a declarative entry list — read them as the bar for \"complicated enough to write by hand.\" Step 3 — Wire props # public static class StarMeterDescriptor { public static readonly ControlDescriptor&#x3C;StarMeterElement, WinUI.RatingControl> Descriptor = new ControlDescriptor&#x3C;StarMeterElement, WinUI.RatingControl> { // Leaf control — no children. (See ChildrenStrategy survey for // the other shapes: SingleContent, Panel, NamedSlots, ItemsHost…) Children = new None&#x3C;StarMeterElement, WinUI.RatingControl>(), } // OneWay props: written on Mount, diff-and-written on Update. .OneWay( get : static e => e.MaxRating, set : static (c, v) => c.MaxRating = v) .OneWay( get : static e => e.IsClearEnabled, set : static (c, v) => c.IsClearEnabled = v) // OneWayConditional skips the write when the predicate is false — // leaves Caption at the control's default for elements that didn't // supply one, rather than forcing it to null and losing a style. .OneWayConditional( get : static e => e.Caption, set : static (c, v) => c.Caption = v!, shouldWrite: static e => e.Caption is not null ) // Controlled is the two-way binding shape: the framework writes the // element's value at Mount (and on diff), suppresses the echo when // the framework is the writer, and forwards user input back through // OnValueChanged. Subscription is gated on the callback being non- // null — if the caller didn't pass OnValueChanged, no trampoline // is wired and the per-fire dispatch cost stays at zero. .Controlled&#x3C; double , object >( get : static e => e.Value, set : static (c, v) => c.Value = v, subscribe: static (fe, h) => ((WinUI.RatingControl)fe).ValueChanged += (s, e) => h(s, e!), unsubscribe: static (fe, h) => { /* trampoline anchored for control lifetime */ }, callback: static e => e.OnValueChanged, readBack: static c => c.Value); } Each fluent builder call adds one PropEntry to the descriptor. The interpreter iterates them in declaration order during Mount and Update. The descriptor above uses three of the common shapes: .OneWay(get, set) — MaxRating and IsClearEnabled . The interpreter writes the value at Mount; on Update it compares the old element's value against the new and writes only when the values differ. The default comparer is EqualityComparer&#x3C;TValue>.Default ; pass an explicit comparer to the optional comparer parameter for floating-point tolerance or custom-type equality. .OneWayConditional(get, set, shouldWrite) — Caption . When the element does not set a caption, the predicate returns false and the write is skipped, leaving the control's default in place. This is the shape to reach for whenever \"no value\" is a meaningful state — every nullable / optional prop in the built-in catalog uses it. .Controlled&#x3C;TValue, TArgs>(...) — Value . The framework writes the element's value on Mount (bare, no echo possible because the trampoline is not yet wired). On Update it writes through ReactorBinding.WriteSuppressed so the WinUI side's synchronous ValueChanged echo is dropped. On user interaction the trampoline reads the live element off the control via the tag, drains the suppress counter (which is zero when the user is the writer), and calls OnValueChanged with the new value. The subscribe lambda's shape is (FrameworkElement, EventHandler&#x3C;TArgs>) → void ; the engine boxes the control identity through FrameworkElement to keep the entry generic across closed-T tuples. TArgs is whatever the WinUI event carries — for RatingControl.ValueChanged the args type is object because WinUI authors the event with TypedEventHandler&#x3C;RatingControl, object> . Subscription is gated on callback returning non-null — if the element doesn't carry OnValueChanged , no trampoline is wired and the dispatch cost for that prop stays at zero. Caveat: The unsubscribe lambda is intentionally a no-op body — descriptor trampolines anchor to the control's lifetime, not the element's. The control returns to the pool, the pool reset contract clears the trampoline slot on the typed event-payload box, and the next Mount finds an empty slot and re-subscribes. Writing a real unsubscribe into the lambda double-frees the subscription when the control pools and is the most common source of \"the event stops firing after a re-render\" bugs. If your control genuinely needs explicit unsubscribe on unmount, override IElementHandler.Unmount and tear down there. Step 4 — Wire events (the non-DP case) # StarMeterElement.Value is bound to a property and an event together, so it uses the .Controlled shape above. Events that do not round-trip a DP — Button.Click , Image.ImageOpened , TextBox.SelectionChanged — use a sibling builder: Builder Use when .HandCodedEvent&#x3C;TPayload, TDelegate>(subscribe, callbackPresent, trampoline, slotIsNull, setSlot) Fire-and-forget event with no associated DP. The trampoline is a static delegate that closes over no per-instance state; per-control state goes on the TPayload slot. binding.OnCustomEvent&#x3C;TArgs>(subscribe, unsubscribe, handler) inside a hand-coded handler When you have already chosen the hand-coded shape and want the simpler closure-based trampoline. Allocates a closure per Mount instead of reusing a static delegate. The ButtonDescriptor source (in src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ButtonDescriptor.cs ) is the canonical example of .HandCodedEvent — including the short-circuit for IsDisabledFocusable and the Reconciler.GetElementTag tag refresh. Step 5 — Declare a child strategy # StarMeterElement is a leaf, so the descriptor declares Children = new None&#x3C;…>() . The dispatch surface is the same as the ChildrenStrategy survey ; the right strategy is whichever matches your WinUI control's structural contract: Your WinUI control's child shape Strategy No children None One Content / Child slot SingleContent(GetChild, SetChild) { GetCurrentChild = … } Children collection on a Panel Panel(GetChildren, GetCollection) Named slots ( Header , Content , Footer ) NamedSlots([NamedSlot(\"Header\", …), …]) Flat items collection ( Items ) of values or pre-built elements ItemsHost(GetItems, GetCollection) Typed templated list with keyed reconcile TemplatedItems&#x3C;TItem, TElement, TControl>(GetItems, KeySelector, BuildItemView) Hierarchical tree TreeChildren(GetNodes) Tabs / pivots with per-item containers TabItemsHost(GetItems, GetCollection, GetContent, CreateContainer) The standard strategies cover everything in the built-in catalog. The Imperative&#x3C;TElement, TControl> escape hatch exists for the case where none of them fit — for example, a control whose children live on three different non-collection properties that must reconcile together. Reach for it last; you give up the engine's keyed reconcile and lose descendant component state across re-renders that touch the imperative slot. Step 6 — Register and use # public static class StarMeterInterop { // One call per Reactor host. RegisterHandler accepts any IElementHandler, // and DescriptorHandler&#x3C;TElement,TControl> is the canonical interpreter // for a ControlDescriptor. Duplicate registration for the same element // type throws — register exactly once on each host you mount. public static void Register ( Reconciler reconciler ) => reconciler.RegisterHandler&#x3C;StarMeterElement, WinUI.RatingControl>( new DescriptorHandler&#x3C;StarMeterElement, WinUI.RatingControl>( StarMeterDescriptor.Descriptor)); } The Register call is the single bridge between your descriptor and the Reactor host. Call it once per ReactorHost instance, before the first render against that host — typically from your app bootstrap, next to any other interop registrations ( DockingNativeInterop.Register , XamlInterop.Register , etc.). The call shape is the same whether you registered a descriptor or a hand- coded handler; both land on the same dispatch table. class ExtendingApp : Component { public override Element Render () { var (rating, setRating) = UseState( 3.5 ); return VStack( 16 , TextBlock( \"StarMeter — custom element wrapping WinUI RatingControl\" ) .FontSize( 14 ).SemiBold(), new StarMeterElement { Value = rating, MaxRating = 5 , Caption = \"Rate this page\" , OnValueChanged = setRating, }, TextBlock( $\"current rating: {rating: 0.0 } \" ), HStack( 8 , Button( \"Reset\" , () => setRating( 0 )), Button( \"5 stars\" , () => setRating( 5 ))) ).Padding( 20 ); } } That is the entire path from setRating(5) to a frame. The component sets state, the reconciler walks the new element tree, the registered descriptor is invoked for StarMeterElement , the interpreter diffs against the previous element (only Value changed), writes through WriteSuppressed , the trampoline drains the suppress counter on the next echo, and the loop ends with the control at 5 stars. The rest of the app's controls dispatch through the built-in handlers in lockstep. Patterns # A few recurring shapes turn up across most custom-control ports: Match a built-in's perf bar without thinking about it. Use a descriptor; let RentControl pool the WinUI control; let Controlled suppress its own echoes. The interpreter's per-entry diff is a single equality check against the previous element's value — unchanged props pay one comparison and zero writes. Pool reuse, echo suppression, and static trampolines are defaults of the protocol, not opt-ins. Bundle related fields into one prop entry. A control that exposes (Color, IsOn) → Background is one entry, not two — the get returns a tuple and the set reads both fields. The diff fires exactly when either input changes. This is how the LED indicator in the Control Reconciler Protocol reference collapses two element fields into one WinUI write. Layer in a Reactor-friendly default for an awkward control. Many WinUI controls require an initial property write to \"behave\" ( TextBox.PlaceholderText , RatingControl.MaxRating ). Use .Initial(get, set) to seed those once at Mount without paying the diff-and-write cost on Update. The interpreter ignores Initial entries on the Update path. Wrap a third-party control whose API is unstable. Put the descriptor in a sealed static class , mark every reference to the third-party type internal , and re-export an Element -typed factory. Your component authors depend on the element, not the control. When the third-party control breaks compatibility, you update one file; every component keeps compiling. Pre-flight the perf bar with the bench harness. The Reactor repo includes a perf_bench project ( tests/perf_bench/ ) that exercises descriptors against the M1 / M2 / M5 / M7 / M10 micro-benches the spec uses to validate the descriptor model. Drop your descriptor into the harness and run the relevant bench before locking your port in. Common Mistakes # Subscribing in Update instead of Mount . A descriptor's Controlled entry already gates subscription on the mount-then-subscribe ordering — the interpreter calls EnsureSubscribed once after the Mount loop and again on each Update to catch null→non-null callback transitions. If you are hand-coding a handler and forget to centralize subscription in Mount , every re-render double-subscribes the event. Forgetting WriteSuppressed in a hand-coded Update . A controlled prop write from Update echoes through the trampoline back into the user's state setter. The descriptor's Controlled entry wraps the write for you; a hand-coded handler must mirror it with binding.WriteSuppressed(() => ctrl.Value = newValue) . Forgetting it is the single most common cause of \"the value snaps back when I edit it\" bugs. Stashing per-control state on the descriptor / handler. The descriptor and the handler instance are registered once per host. Per-control state needs to live on the control — either on the attached state DP (use Reconciler.SetElementTag / GetElementTag ) or on the typed event payload box (use Reconciler.GetOrCreateControlEventPayload&#x3C;TPayload> ). The pool reset contract clears both on ReturnControl ; descriptor-level state survives pool rent and corrupts the next mount. Registering against the wrong reconciler. If your app uses multiple ReactorHost instances (a tray icon host, a settings window, an interop host inside XAML islands), each host has its own reconciler. Register on every host that will render your element. The exact-type dispatch will throw Mount for an unregistered element type — it does not fall back to a base type. Capturing the element in a static trampoline. The static trampoline pattern works because it reads the live element on every fire. Capturing the element in the static closure is a contradiction in terms — there is no closure on a static — but the equivalent mistake on a hand-coded trampoline that closes over the element instance does exactly that, and the trampoline observes the element that existed at Mount forever. Tips # Start from the closest built-in descriptor. Copy the most similar descriptor in src/Reactor/Core/V1Protocol/Descriptor/Descriptors/ , strip what does not apply, and adapt. The built-ins encode the production patterns for every shape you are likely to need — including the coercion edge cases ( Slider.Min , NumberBox.Max ) and the multi-event control patterns ( TextBox 's Text + SelectionChanged ). Keep the element record minimal. Every field is a public contract — once consumers depend on it, you cannot drop or rename it without churn. Add fields as components need them, not preemptively. Test the round trip with the existing fixture harness. Add a fixture in tests/Reactor.AppTests.Host/SelfTest/Fixtures/ that drives your control via an automation peer ( IInvokeProvider / IValueProvider ) and asserts the callback fires the right number of times for the right values. The pattern is established and the fixture runner gives you a stable stable cross- process test rig for free. Document the perf shape inline. If your control has non-obvious performance characteristics — pool opt-out, custom comparer for floating-point tolerance, deliberately imperative entry — capture the reason in a comment on the descriptor field, not on the calling component. The descriptor is where future maintainers will look. Read the Control Reconciler Protocol reference once end-to-end. Steps 1 through 6 above are the recipe; the protocol page is the specification it implements. The two pages together are the complete contract — bookmark both. Next Steps # The Control Reconciler Protocol — the model the steps on this page plug into. Reconciliation — what happens before the registered handler is invoked, and how Mount vs Update vs Unmount get chosen. Element Pool — what RentControl actually does, and how the per-type pool keeps Mount allocation-free. Modifier System — how the Setters chain a descriptor applies after its own prop loop works. Hooks Internals — how state changes turn into Render calls, which is the upstream half of every loop the protocol on this page is the downstream half of.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/extending-reactor-controls.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/extending-reactor-controls.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-five-step-playbook",
          "text": "The five-step playbook"
        },
        {
          "depth": 2,
          "id": "step-1--define-the-element-record",
          "text": "Step 1 — Define the Element record"
        },
        {
          "depth": 2,
          "id": "step-2--choose-a-shape-descriptor-vs-hand-coded",
          "text": "Step 2 — Choose a shape (descriptor vs hand-coded)"
        },
        {
          "depth": 2,
          "id": "step-3--wire-props",
          "text": "Step 3 — Wire props"
        },
        {
          "depth": 2,
          "id": "step-4--wire-events-the-non-dp-case",
          "text": "Step 4 — Wire events (the non-DP case)"
        },
        {
          "depth": 2,
          "id": "step-5--declare-a-child-strategy",
          "text": "Step 5 — Declare a child strategy"
        },
        {
          "depth": 2,
          "id": "step-6--register-and-use",
          "text": "Step 6 — Register and use"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Element Pool",
      "path": "element-pool.md",
      "slug": "/element-pool/",
      "html": "<h1 id=\"element-pool\">Element Pool<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#element-pool\">#</a></h1>\n<p>The Microsoft.UI.Reactor (Reactor) element pool recycles unmounted WinUI controls so a scroll-heavy\nlist or fast-mounting subtree doesn't allocate a fresh <code>TextBlock</code> /\n<code>Button</code> / <code>Border</code> on every render. Each WinUI type gets its own\nfixed-size stack (cap 32) inside <code>ElementPool</code>; the\n<a href=\"/reconciliation/\">reconciler</a> calls <code>TryRent(typeof(T))</code> on mount\nand <code>Return(element)</code> on unmount. This page documents the rental\ncontract, the cleanup pass that resets a returned element, and the\nexclusions that keep pooling safe — composition-tainted visuals,\nnon-poolable types, and parent-state corner cases.</p>\n<h2 id=\"renting-on-mount\">Renting on mount<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#renting-on-mount\">#</a></h2>\n<p>Every <code>MountXxx</code> handler in <code>Reconciler.Mount.cs</code> follows the same\nshape: try the pool first, allocate fresh on miss.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> FrameworkElement? TryRent(Type type)\n{\n    <span class=\"hljs-keyword\">if</span> (!Enabled) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n    <span class=\"hljs-keyword\">if</span> (!PoolableTypes.Contains(type)) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n    <span class=\"hljs-keyword\">if</span> (!_pools.TryGetValue(type, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> stack) || stack.Count == <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">null</span>;\n    <span class=\"hljs-keyword\">var</span> item = stack.Pop();\n    <span class=\"hljs-keyword\">return</span> item;\n}\n</code></pre>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/element-pool/rental-states.svg\" alt=\"Element-pool state machine: a control starts unpooled, transitions to Rented on Mount, to Returning on Unmount, and back to Pooled after ForceDetach + CleanElement — unless the per-type cap is exceeded or the control became compositor-tainted, in which case it falls out of pooling.\"></p>\n<p><code>TryRent</code> is three checks and a pop: pool enabled, type in the\npoolable set, stack non-empty. Anything that fails returns <code>null</code> and\nthe caller falls through to <code>new TextBlock()</code> / <code>new WinUI.Button()</code>.\nThe exclusions are deliberate — the live-previewer can flip\n<code>Pool.Enabled = false</code> when recycled controls with stale property\nstate would cause visual glitches, and the poolable type set is a\nhardcoded <code>HashSet&#x3C;Type></code> because pooling a type the cleanup pass\ndoesn't know about would leak property state into the next user.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Type</th><th>Why it's poolable</th><th>Notes</th></tr></thead><tbody><tr><td><code>TextBlock</code>, <code>RichTextBlock</code></td><td>Static leaf; no event handlers; cheap reset</td><td>High-volume win in lists.</td></tr><tr><td><code>StackPanel</code>, <code>Grid</code>, <code>Border</code></td><td>Container shells; children cleared on return</td><td>Reused across most layouts.</td></tr><tr><td><code>ScrollViewer</code>, <code>Canvas</code>, <code>Viewbox</code></td><td>Single-<code>Content</code> controls; content nulled on return</td><td>Same shape.</td></tr><tr><td><code>ProgressBar</code>, <code>ProgressRing</code></td><td>Indeterminate flag + value reset to defaults</td><td>Stop-state restored on return.</td></tr><tr><td><code>Image</code>, <code>InfoBadge</code></td><td><code>Source</code> / <code>Value</code> nulled</td><td>Image source pooling is the largest win.</td></tr><tr><td><code>Button</code>, <code>TextBox</code>, <code>ToggleSwitch</code></td><td>Interactive — pooled via Tag-based event trampoline</td><td>See \"Why interactive controls are safe\" below.</td></tr></tbody></table>\n<p>Types not in this set are not pooled — every mount of a <code>ListView</code>,\n<code>NumberBox</code>, <code>DatePicker</code>, etc. allocates fresh. That's deliberate:\nthe cleanup pass has to know how to reset every property a Reactor\nmodifier or <code>.Set</code> callback might have written, and adding a type to\n<code>PoolableTypes</code> without a matching <code>CleanElement</code> case would leak\nproperty state into the next reader's mount.</p>\n<h2 id=\"returning-on-unmount\">Returning on unmount<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#returning-on-unmount\">#</a></h2>\n<p><code>Return</code> is the more interesting half. A control coming back to the\npool may carry hundreds of WinUI property values, a parent reference,\nevent handler subscriptions, and (on interactive controls) the\ntrampoline state that drives Reactor's event dispatch:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Return</span>(<span class=\"hljs-params\">FrameworkElement element</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (!Enabled) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">var</span> type = element.GetType();\n    <span class=\"hljs-keyword\">if</span> (!PoolableTypes.Contains(type)) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Don't pool elements that had GetElementVisual() called — they permanently</span>\n    <span class=\"hljs-comment\">// lose XAML implicit transition API access (OpacityTransition, etc.).</span>\n    <span class=\"hljs-keyword\">if</span> (IsCompositorTainted(element)) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (!_pools.TryGetValue(type, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> stack))\n    {\n        stack = <span class=\"hljs-keyword\">new</span> Stack&#x3C;FrameworkElement>();\n        _pools[type] = stack;\n    }\n\n    <span class=\"hljs-keyword\">if</span> (stack.Count >= MaxPerType) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p>The path runs in five steps after the type check: composition-taint\ncheck (a visual that has had <code>GetElementVisual()</code> called permanently\nloses access to XAML implicit transitions like <code>OpacityTransition</code>,\nso it must not be pooled), per-type stack lookup, per-type cap check\n(stop accepting at 32 instances), then — past the snippet boundary —\nthe detach-from-parent dance and <code>CleanElement(...)</code> reset before the\ncontrol finally pushes onto the stack. The scratch-panel round-trip\nin <code>ForceDetach</code> is the load-bearing piece: WinUI's internal parent\ntracking can retain stale state that throws <code>COMException</code> when the\ncontrol is later re-parented, and <code>_scratchPanel.Children.Add</code> +\n<code>Remove</code> forces WinUI to clear it.</p>\n<p>The element record itself isn't pooled — only the realized WinUI\ncontrol on the other side:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<p>Records are cheap; the heavy classes are the WinUI controls that\nallocate template instances, brush caches, and visual state machines.\nPooling the records would save nothing because the reconciler creates\nfresh records every render by design.</p>\n<h2 id=\"why-interactive-controls-are-safe-to-pool\">Why interactive controls are safe to pool<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#why-interactive-controls-are-safe-to-pool\">#</a></h2>\n<p>A <code>Button.Click</code> handler that closes over the captured rerender\nclosure cannot be left dangling on a pooled control — the next user\nof that control would receive clicks targeting the wrong component.\nThe fix is the Tag-based event trampoline: the WinUI subscription\nattached at first mount stays attached, but it reads the <em>current</em>\nelement from the control's <code>Tag</code> at invocation time, so a recycled\ncontrol dispatching from its old subscription naturally routes to\nthe new element's <code>OnClick</code> after <code>Reconciler.SetElementTag</code> runs:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">MarshalIfOffUIThread</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> hookName, Action work</span>)</span>\n{\n    <span class=\"hljs-comment\">// Hot path — same thread that ran BeginRender. ~1ns TLS read + cmp + branch.</span>\n    <span class=\"hljs-keyword\">if</span> (Environment.CurrentManagedThreadId == _uiThreadId) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p><code>CleanElement</code> calls <code>ClearCurrentEventHandlers(fe)</code> on pool return,\nwhich nulls the per-mount user-handler delegates without touching the\nWinUI subscription. On the next mount, the trampoline finds null\nhandlers in the new Tag's element record, dispatches to nothing, and\ncosts essentially zero — until the user actually wires <code>.Click(...)</code>,\nat which point the trampoline routes correctly. TASK-060 in\n<code>Reconciler.cs</code> calls this out specifically: clearing the delegate\nlist on return is what stops a pooled control from firing the\nprevious component's captured closure into the next mount.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The pool is opt-in by type.</strong> Adding a custom control to the pool\nmeans adding it to <code>ElementPool.PoolableTypes</code> <em>and</em> extending\n<code>CleanElement</code> with a switch arm that resets every property the\ncontrol might have carried. Skipping the second step leaks state.</p>\n<p><strong><code>GetElementVisual()</code> is one-way.</strong> Once a control's composition\nvisual has been requested (typically for an <code>.Animate()</code> modifier),\nthe control can no longer use XAML implicit transitions and is\npermanently un-poolable. <code>ElementPool.MarkCompositorTainted</code> records\nthis; the next <code>Return</code> for that instance silently drops it. Reach for\n<code>.WithOpacityTransition()</code> and friends before <code>.Animate()</code> when both\nwork.</p>\n<p><strong>The cap is 32 per type.</strong> A burst of 100 list items mounts 32 from\nthe pool and 68 fresh; the next render returns 32 to the pool and\ndrops 36. That's fine for steady-state scrolling — the working set\nstabilizes around the visible window — but doesn't scale to giant\nflat lists. Reach for <a href=\"/collections/\"><code>VirtualList</code></a> for those.</p>\n<p><strong>Disable the pool when you need pristine state.</strong> The live previewer\nsets <code>Pool.Enabled = false</code> because hot-reloading new code into a\nrecycled control with old property state causes visible glitches.\nTest fixtures that snapshot-compare WinUI properties on mount should\ndo the same.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Previous: where Mount and Unmount call <code>TryRent</code> / <code>Return</code>.</li>\n<li><strong><a href=\"/collections/\">Collections</a></strong> — Next: the heaviest user of pooling (virtualized list items).</li>\n<li><strong><a href=\"/source-mapping/\">Source mapping</a></strong> — How pool reuse interacts with per-control source attribution.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture overview</a></strong> — Where the pool sits in the render loop.</li>\n</ul>",
      "text": "Element Pool # The Microsoft.UI.Reactor (Reactor) element pool recycles unmounted WinUI controls so a scroll-heavy list or fast-mounting subtree doesn't allocate a fresh TextBlock / Button / Border on every render. Each WinUI type gets its own fixed-size stack (cap 32) inside ElementPool ; the reconciler calls TryRent(typeof(T)) on mount and Return(element) on unmount. This page documents the rental contract, the cleanup pass that resets a returned element, and the exclusions that keep pooling safe — composition-tainted visuals, non-poolable types, and parent-state corner cases. Renting on mount # Every MountXxx handler in Reconciler.Mount.cs follows the same shape: try the pool first, allocate fresh on miss. public FrameworkElement? TryRent(Type type) { if (!Enabled) return null ; if (!PoolableTypes.Contains(type)) return null ; if (!_pools.TryGetValue(type, out var stack) || stack.Count == 0 ) return null ; var item = stack.Pop(); return item; } TryRent is three checks and a pop: pool enabled, type in the poolable set, stack non-empty. Anything that fails returns null and the caller falls through to new TextBlock() / new WinUI.Button() . The exclusions are deliberate — the live-previewer can flip Pool.Enabled = false when recycled controls with stale property state would cause visual glitches, and the poolable type set is a hardcoded HashSet&#x3C;Type> because pooling a type the cleanup pass doesn't know about would leak property state into the next user. Type Why it's poolable Notes TextBlock , RichTextBlock Static leaf; no event handlers; cheap reset High-volume win in lists. StackPanel , Grid , Border Container shells; children cleared on return Reused across most layouts. ScrollViewer , Canvas , Viewbox Single- Content controls; content nulled on return Same shape. ProgressBar , ProgressRing Indeterminate flag + value reset to defaults Stop-state restored on return. Image , InfoBadge Source / Value nulled Image source pooling is the largest win. Button , TextBox , ToggleSwitch Interactive — pooled via Tag-based event trampoline See \"Why interactive controls are safe\" below. Types not in this set are not pooled — every mount of a ListView , NumberBox , DatePicker , etc. allocates fresh. That's deliberate: the cleanup pass has to know how to reset every property a Reactor modifier or .Set callback might have written, and adding a type to PoolableTypes without a matching CleanElement case would leak property state into the next reader's mount. Returning on unmount # Return is the more interesting half. A control coming back to the pool may carry hundreds of WinUI property values, a parent reference, event handler subscriptions, and (on interactive controls) the trampoline state that drives Reactor's event dispatch: public void Return ( FrameworkElement element ) { if (!Enabled) return ; var type = element.GetType(); if (!PoolableTypes.Contains(type)) return ; // Don't pool elements that had GetElementVisual() called — they permanently // lose XAML implicit transition API access (OpacityTransition, etc.). if (IsCompositorTainted(element)) return ; if (!_pools.TryGetValue(type, out var stack)) { stack = new Stack&#x3C;FrameworkElement>(); _pools[type] = stack; } if (stack.Count >= MaxPerType) return ; The path runs in five steps after the type check: composition-taint check (a visual that has had GetElementVisual() called permanently loses access to XAML implicit transitions like OpacityTransition , so it must not be pooled), per-type stack lookup, per-type cap check (stop accepting at 32 instances), then — past the snippet boundary — the detach-from-parent dance and CleanElement(...) reset before the control finally pushes onto the stack. The scratch-panel round-trip in ForceDetach is the load-bearing piece: WinUI's internal parent tracking can retain stale state that throws COMException when the control is later re-parented, and _scratchPanel.Children.Add + Remove forces WinUI to clear it. The element record itself isn't pooled — only the realized WinUI control on the other side: public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } Records are cheap; the heavy classes are the WinUI controls that allocate template instances, brush caches, and visual state machines. Pooling the records would save nothing because the reconciler creates fresh records every render by design. Why interactive controls are safe to pool # A Button.Click handler that closes over the captured rerender closure cannot be left dangling on a pooled control — the next user of that control would receive clicks targeting the wrong component. The fix is the Tag-based event trampoline: the WinUI subscription attached at first mount stays attached, but it reads the current element from the control's Tag at invocation time, so a recycled control dispatching from its old subscription naturally routes to the new element's OnClick after Reconciler.SetElementTag runs: private bool MarshalIfOffUIThread ( string hookName, Action work ) { // Hot path — same thread that ran BeginRender. ~1ns TLS read + cmp + branch. if (Environment.CurrentManagedThreadId == _uiThreadId) return false ; CleanElement calls ClearCurrentEventHandlers(fe) on pool return, which nulls the per-mount user-handler delegates without touching the WinUI subscription. On the next mount, the trampoline finds null handlers in the new Tag's element record, dispatches to nothing, and costs essentially zero — until the user actually wires .Click(...) , at which point the trampoline routes correctly. TASK-060 in Reconciler.cs calls this out specifically: clearing the delegate list on return is what stops a pooled control from firing the previous component's captured closure into the next mount. Tips # The pool is opt-in by type. Adding a custom control to the pool means adding it to ElementPool.PoolableTypes and extending CleanElement with a switch arm that resets every property the control might have carried. Skipping the second step leaks state. GetElementVisual() is one-way. Once a control's composition visual has been requested (typically for an .Animate() modifier), the control can no longer use XAML implicit transitions and is permanently un-poolable. ElementPool.MarkCompositorTainted records this; the next Return for that instance silently drops it. Reach for .WithOpacityTransition() and friends before .Animate() when both work. The cap is 32 per type. A burst of 100 list items mounts 32 from the pool and 68 fresh; the next render returns 32 to the pool and drops 36. That's fine for steady-state scrolling — the working set stabilizes around the visible window — but doesn't scale to giant flat lists. Reach for VirtualList for those. Disable the pool when you need pristine state. The live previewer sets Pool.Enabled = false because hot-reloading new code into a recycled control with old property state causes visible glitches. Test fixtures that snapshot-compare WinUI properties on mount should do the same. Next Steps # Reconciliation — Previous: where Mount and Unmount call TryRent / Return . Collections — Next: the heaviest user of pooling (virtualized list items). Source mapping — How pool reuse interacts with per-control source attribution. Architecture overview — Where the pool sits in the render loop.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/element-pool.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/element-pool.md",
      "headings": [
        {
          "depth": 2,
          "id": "renting-on-mount",
          "text": "Renting on mount"
        },
        {
          "depth": 2,
          "id": "returning-on-unmount",
          "text": "Returning on unmount"
        },
        {
          "depth": 2,
          "id": "why-interactive-controls-are-safe-to-pool",
          "text": "Why interactive controls are safe to pool"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Effects Scheduling",
      "path": "effects-scheduling.md",
      "slug": "/effects-scheduling/",
      "html": "<p>The thing that trips up most readers in Microsoft.UI.Reactor (Reactor) is the order. <code>UseEffect</code> does\nnot run during render — it runs <em>after</em> the reconciler has finished\ncommitting the new tree to WinUI, on the UI thread, in hook-declaration\norder, with the cleanup from the previous round of the same effect\nrunning first. State you set inside an effect body schedules another\nrender; the next render's effects run after that one commits. This is\nthe same shape React's effect phase takes, and the trade-off is the\nsame: render stays a pure description of the next tree, side effects\nrun somewhere a write is safe, and you pay one extra render whenever an\neffect writes state. The piece worth holding in your head is that\n<a href=\"/reconciliation/\">reconciliation</a> and effect-flush are sequential —\nwhen your effect body runs, the WinUI controls you described in this\nrender are already mounted and visible, and the previous round's\ncleanup has already run.</p>\n<h1 id=\"effects-scheduling\">Effects Scheduling<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#effects-scheduling\">#</a></h1>\n<p>This page is about when <a href=\"/hooks/\"><code>UseEffect</code></a> fires and why.\nSurface-level usage is on the <a href=\"/effects/\">Effects</a> page; the\nasync-resource pipeline — <code>UseResource</code>, <code>UseInfiniteResource</code>,\n<code>Pending</code>, <code>QueryCache</code> — has its own deep walk in the\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/reference/async-system.md\">async-system reference</a>. What\nfollows is the render-loop view of the same callbacks.</p>\n<h2 id=\"the-render--commit--flush-sequence\">The render → commit → flush sequence<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-render--commit--flush-sequence\">#</a></h2>\n<p>A render pass runs in three phases. The first is pure description —\n<code>Render()</code> returns an <a href=\"/components/\"><code>Element</code></a> tree and never touches\nWinUI. The reconciler then walks the tree, mounts or updates real\ncontrols, and only after that does it flush queued effects.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">UseEffect</span>(<span class=\"hljs-params\">Action effect, <span class=\"hljs-keyword\">params</span> <span class=\"hljs-built_in\">object</span>[] dependencies</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> EffectHookState { Dependencies = <span class=\"hljs-literal\">null</span>, Effect = effect });\n    }\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[_hookIndex] <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> EffectHookState hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> HookOrderException(\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{_hookIndex}</span> is <span class=\"hljs-subst\">{_hooks[_hookIndex].GetType().Name}</span>, expected EffectHookState. \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span>);\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (hook.Dependencies <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> || !DepsEqual(hook.Dependencies, dependencies))\n    {\n        hook.PendingCleanup = hook.Cleanup;\n        hook.Cleanup = <span class=\"hljs-literal\">null</span>;\n        hook.Dependencies = dependencies.ToArray();\n        hook.Effect = effect;\n        hook.Pending = <span class=\"hljs-literal\">true</span>;\n    }\n}\n</code></pre>\n<p><code>UseEffect</code> does not call your callback. It checks the dependency\narray against the previous render's deps and, if they differ, marks\nthe hook <code>Pending = true</code> and stashes the previous cleanup. The\nreconciler's commit pass — see\n<a href=\"/reconciliation/\">Reconciliation</a> — calls\n<code>FlushEffectsTraced(ctx, …)</code> after every component's controls are in\nplace. Flush then runs all pending cleanups before any pending effect\nbodies, both in hook-declaration order.</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/effects-scheduling/effect-timing.svg\" alt=\"Render → commit → flush sequence: Component.Render returns an Element tree, Reconciler diffs and commits to WinUI, then FlushEffects runs prior cleanups before new effect bodies\"></p>\n<p>The cleanup-before-body ordering is the reason a <code>cts.Cancel()</code> /\n<code>timer.Dispose()</code> cleanup actually disposes the <em>previous</em> timer\nbefore a new one starts. If flush ran the body first and the cleanup\nsecond, the cleanup would dispose the timer the body just created.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Phase</th><th>What runs</th><th>Where in source</th></tr></thead><tbody><tr><td>Render</td><td><code>Component.Render()</code>, hook slots register</td><td><code>Component.cs</code> <code>Render</code> override</td></tr><tr><td>Reconcile</td><td>Diff against previous tree, mount/update controls</td><td><code>Reconciler.cs</code> <code>Reconcile</code></td></tr><tr><td>Commit</td><td>WinUI tree visible</td><td>end of <code>Reconciler.Mount</code> / <code>Update</code></td></tr><tr><td>Flush — phase 1</td><td>Run all <code>PendingCleanup</code> callbacks (in slot order)</td><td><code>RenderContext.FlushEffects</code></td></tr><tr><td>Flush — phase 2</td><td>Run pending effect bodies (in slot order)</td><td><code>RenderContext.FlushEffects</code></td></tr><tr><td>Unmount</td><td><code>Cleanup</code> runs once, then hook list is dropped</td><td><code>RenderContext.RunCleanups</code></td></tr></tbody></table>\n<blockquote>\n<p><strong>Caveat:</strong> The effect body sees the just-committed tree, not the tree it was\ndescribed against. If your effect inspects an\n<a href=\"/hooks/\"><code>ElementRef</code></a> or reads a DOM-equivalent property right after\nmount, the value reflects the WinUI control that came out of\n<a href=\"/reconciliation/\">reconciliation</a>, which can differ from what you\nexpected if the reconciler reused a pooled control or short-circuited\nthe update via <code>CanSkipUpdate</code>. Don't reason from the props you passed\nto the factory — reason from the control the reconciler actually\nattached.</p>\n</blockquote>\n<h2 id=\"dependency-equality-is-value-equality-with-one-trap\">Dependency equality is value equality, with one trap<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dependency-equality-is-value-equality-with-one-trap\">#</a></h2>\n<p>Reactor compares dependency arrays with <code>EqualityComparer&#x3C;T>.Default</code>.\nFor primitives and records that's value equality. For classes that\ndon't override <code>Equals</code>, it's reference equality — same identity\ntrap React's <code>useEffect</code> has.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, <span class=\"hljs-built_in\">bool</span> threadSafe = <span class=\"hljs-literal\">false</span>)\n{\n    <span class=\"hljs-keyword\">if</span> (_hookIndex >= _hooks.Count)\n    {\n        _hooks.Add(<span class=\"hljs-keyword\">new</span> ValueHookState&#x3C;T>(initialValue, threadSafe));\n    }\n\n    <span class=\"hljs-keyword\">var</span> currentIndex = _hookIndex;\n    _hookIndex++;\n\n    <span class=\"hljs-keyword\">if</span> (_hooks[currentIndex] <span class=\"hljs-function\"><span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-title\">ValueHookState</span>&#x3C;<span class=\"hljs-title\">T</span>> hook)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title\">HookOrderException</span>(<span class=\"hljs-params\">\n            <span class=\"hljs-string\">$\"Hook at index <span class=\"hljs-subst\">{currentIndex}</span> is <span class=\"hljs-subst\">{_hooks[currentIndex].GetType().Name}</span>, expected ValueHookState&#x3C;<span class=\"hljs-subst\">{<span class=\"hljs-keyword\">typeof</span>(T).Name}</span>> (UseState). \"</span> +\n            <span class=\"hljs-string\">\"Hooks must be called in the same order every render.\"</span></span>)</span>;\n</code></pre>\n<p><code>UseState</code> returns the <em>same</em> <code>Action&#x3C;T></code> setter instance across\nrenders for a given hook slot (the closure captures <code>currentIndex</code>,\nwhich doesn't change). A <code>var (count, setCount) = UseState(0)</code> setter\nis therefore safe to pass as a dependency — it compares equal between\nrenders and won't restart the effect.</p>\n<p>A freshly constructed object on every render isn't. <code>new Options { ... }</code>\nin a render body, passed as a dep, restarts the effect every render —\nthe object differs by reference even when its fields are identical.\nEither move it outside the component, wrap it in\n<a href=\"/hooks/\"><code>UseMemo</code></a>, or unpack the primitive fields and pass those.</p>\n<h2 id=\"cleanup-ordering-on-update-and-on-unmount\">Cleanup ordering on update and on unmount<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#cleanup-ordering-on-update-and-on-unmount\">#</a></h2>\n<p>A re-render with changed deps runs the <em>previous</em> effect's cleanup\nbefore the <em>new</em> effect body. An unmount runs cleanup with no\nfollowing body. The hook state carries both <code>Cleanup</code> (the live one)\nand <code>PendingCleanup</code> (the one queued for the next flush) so the two\npaths don't collide:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PendingScope</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Dictionary&#x3C;<span class=\"hljs-built_in\">object</span>, <span class=\"hljs-built_in\">bool</span>> _loadingByToken = <span class=\"hljs-keyword\">new</span>(capacity: <span class=\"hljs-number\">4</span>);\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">object</span> _lock = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Fires when a resource joins, leaves, or changes its loading state.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> Action? Changed;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Start tracking <span class=\"hljs-doctag\">&#x3C;paramref name=\"token\"/></span> with the given initial <span class=\"hljs-doctag\">&#x3C;paramref name=\"isLoading\"/></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> state. A hook typically uses its own <span class=\"hljs-doctag\">&#x3C;c></span>this<span class=\"hljs-doctag\">&#x3C;/c></span>-equivalent as the token.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Register</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span> token, <span class=\"hljs-built_in\">bool</span> isLoading</span>)</span>\n    {\n        <span class=\"hljs-keyword\">lock</span> (_lock) _loadingByToken[token] = isLoading;\n        Changed?.Invoke();\n    }\n</code></pre>\n<p><code>PendingScope</code> is one place the cleanup contract is load-bearing. A\n<code>UseResource</code> hook registers its token with the nearest ancestor\n<a href=\"/async-resources/\"><code>Pending</code></a> scope at mount, updates\nloading state through its lifecycle, and unregisters in cleanup.\nSkip the unregister and the <code>Pending</code> fallback never clears — the\nfallback subtree stays rendered after the resource resolves because\nthe scope still thinks something is loading. Always return a cleanup\nfrom any effect that registers state outside the component.</p>\n<h2 id=\"async-effects-and-cancellation\">Async effects and cancellation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#async-effects-and-cancellation\">#</a></h2>\n<p><code>UseEffect</code> takes an <code>Action</code>. Awaiting inside an <code>async void</code> lambda\nis a footgun — the await machinery decouples the body from the flush\nordering, so the cleanup runs against an effect that may not have\ncompleted. The shape that works is \"kick off a task, return a\ncleanup that cancels it\":</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nUseEffect(<span class=\"hljs-keyword\">async</span> () => {              <span class=\"hljs-comment\">// async void lambda</span>\n    <span class=\"hljs-keyword\">var</span> data = <span class=\"hljs-keyword\">await</span> Fetch(url);\n    setData(data);                   <span class=\"hljs-comment\">// may run after unmount → cancellation cliff</span>\n});\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">QueryCache</span> : <span class=\"hljs-title\">IDisposable</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>How often the eviction timer checks every slot for expiry. Default 1s.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> TimeSpan EvictionPollInterval { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">set</span>; } = TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Injected marshaller for <span class=\"hljs-doctag\">&#x3C;see cref=\"EntryChanged\"/></span>. Null → fires inline.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Action&#x3C;Action>? DispatcherPost { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">set</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Clock override for deterministic tests.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> Func&#x3C;DateTime> UtcNow { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">set</span>; } = () => DateTime.UtcNow;\n\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> ConcurrentDictionary&#x3C;<span class=\"hljs-built_in\">string</span>, Slot> _slots = <span class=\"hljs-keyword\">new</span>();\n    <span class=\"hljs-keyword\">private</span> Timer? _evictionTimer;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">object</span> _timerLock = <span class=\"hljs-keyword\">new</span>();\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">int</span> _disposed;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Fires when an entry is added, replaced, invalidated, or evicted.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> Action&#x3C;<span class=\"hljs-built_in\">string</span>>? EntryChanged;\n</code></pre>\n<p>The async-resource hooks (<code>UseResource</code>, <code>UseInfiniteResource</code>,\n<code>UseMutation</code>) wrap this pattern correctly — they own the cancellation\ntoken, marshal completions back to the UI thread, and clean up their\n<a href=\"/async-resources/\"><code>QueryCache</code></a> subscription on unmount.\nReach for them before writing async-effect glue by hand. When you do\nneed a raw <code>UseEffect</code> for an async operation, the cleanup function\nis your only chance to cancel the in-flight task — capture a\n<code>CancellationTokenSource</code>, return <code>() => cts.Cancel()</code>.</p>\n<h2 id=\"dependency-array-reference\">Dependency-array reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#dependency-array-reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Dep array shape</th><th>When the effect re-runs</th></tr></thead><tbody><tr><td>omitted (<code>UseEffect(body)</code>)</td><td>after every commit</td></tr><tr><td>empty (<code>UseEffect(body, Array.Empty&#x3C;object>())</code>)</td><td>once, after the first commit</td></tr><tr><td>primitives / records</td><td>when <code>EqualityComparer&#x3C;T>.Default.Equals</code> flips</td></tr><tr><td>reference-typed object built in render</td><td>every commit (almost never what you want)</td></tr><tr><td><code>setX</code> from <code>UseState</code></td><td>never re-triggers — setter identity is stable</td></tr></tbody></table>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"subscribe-in-mount-unsubscribe-in-cleanup\">Subscribe in mount, unsubscribe in cleanup<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#subscribe-in-mount-unsubscribe-in-cleanup\">#</a></h3>\n<p>The single most useful effect shape is \"talk to something outside the\ncomponent for as long as I'm on screen, then release it\". Reactor's\nown <a href=\"#\"><code>Observable.cs#observable-bridge</code></a> bridge follows this shape;\nso does any timer, event subscription, or background task.</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() =>\n{\n    <span class=\"hljs-keyword\">var</span> cts = <span class=\"hljs-keyword\">new</span> CancellationTokenSource();\n    _ = Task.Run(<span class=\"hljs-keyword\">async</span> () =>\n    {\n        <span class=\"hljs-keyword\">using</span> <span class=\"hljs-keyword\">var</span> timer = <span class=\"hljs-keyword\">new</span> PeriodicTimer(TimeSpan.FromSeconds(<span class=\"hljs-number\">1</span>));\n        <span class=\"hljs-keyword\">while</span> (<span class=\"hljs-keyword\">await</span> timer.WaitForNextTickAsync(cts.Token))\n            setSeconds(s => s + <span class=\"hljs-number\">1</span>);\n    });\n    <span class=\"hljs-keyword\">return</span> () => cts.Cancel();\n}, Array.Empty&#x3C;<span class=\"hljs-built_in\">object</span>>());\n</code></pre>\n<p>The setter <code>setSeconds</code> auto-marshals back to the UI thread (see\n<a href=\"/hooks/\">hooks</a> \"Updating state from background work\"). The cleanup\ncancels the token; the timer's <code>using</code> disposes when the\n<code>WaitForNextTickAsync</code> loop unblocks. No state leaks between mounts.</p>\n<h3 id=\"effect-that-observes-a-prop--state-change\">Effect that observes a prop / state change<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#effect-that-observes-a-prop--state-change\">#</a></h3>\n<p>When you need to react to a value flipping — open a dialog, fetch\nfresh data, broadcast a notification — pass the value as a dependency.\nThe cleanup runs the moment the value changes, before the new effect\nbody, so any work tied to the old value is torn down first.</p>\n<pre><code class=\"hljs language-csharp\">UseEffect(() =>\n{\n    <span class=\"hljs-keyword\">if</span> (!isOpen) <span class=\"hljs-keyword\">return</span> () => { };\n    LogTelemetry(<span class=\"hljs-string\">\"dialog_opened\"</span>);\n    <span class=\"hljs-keyword\">return</span> () => LogTelemetry(<span class=\"hljs-string\">\"dialog_closed\"</span>);\n}, isOpen);\n</code></pre>\n<p>Telemetry stays balanced because flush runs the previous body's\ncleanup (the \"closed\" log) before the new body's \"opened\" log — the\nevents arrive in the same order as the user's actions.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"setting-state-unconditionally-inside-an-effect\">Setting state unconditionally inside an effect<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#setting-state-unconditionally-inside-an-effect\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nUseEffect(() => {\n    setCount(c => c + <span class=\"hljs-number\">1</span>);   <span class=\"hljs-comment\">// no deps → runs every commit → infinite loop</span>\n});\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Setter</span>(<span class=\"hljs-params\">T newValue</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> h = (ValueHookState&#x3C;T>)_hooks[currentIndex];\n    <span class=\"hljs-built_in\">bool</span> changed;\n    <span class=\"hljs-keyword\">if</span> (h.ThreadSafe)\n    {\n        <span class=\"hljs-keyword\">lock</span> (h.Lock)\n        {\n            changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n            <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        }\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (MarshalIfOffUIThread(<span class=\"hljs-string\">\"UseState\"</span>, () => Setter(newValue))) <span class=\"hljs-keyword\">return</span>;\n        changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n        <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n}\n</code></pre>\n<p>Every state setter schedules a re-render. An effect with no deps\nre-runs after every commit, including the commit <em>its own setter\ncaused</em>, so the component renders in a tight loop until something else\nbreaks the cycle. Either gate the setter on a condition that can't\nre-trigger, or pass the value the setter depends on as a dep so the\neffect only fires when it actually changes.</p>\n<h3 id=\"building-a-dep-object-inside-render\">Building a dep object inside render<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#building-a-dep-object-inside-render\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">var</span> options = <span class=\"hljs-keyword\">new</span> ResourceOptions { Timeout = <span class=\"hljs-number\">5000</span> };\nUseEffect(() => Fetch(url, options), url, options);  <span class=\"hljs-comment\">// options is new every render</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">PendingScope</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> Dictionary&#x3C;<span class=\"hljs-built_in\">object</span>, <span class=\"hljs-built_in\">bool</span>> _loadingByToken = <span class=\"hljs-keyword\">new</span>(capacity: <span class=\"hljs-number\">4</span>);\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">object</span> _lock = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Fires when a resource joins, leaves, or changes its loading state.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">event</span> Action? Changed;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Start tracking <span class=\"hljs-doctag\">&#x3C;paramref name=\"token\"/></span> with the given initial <span class=\"hljs-doctag\">&#x3C;paramref name=\"isLoading\"/></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> state. A hook typically uses its own <span class=\"hljs-doctag\">&#x3C;c></span>this<span class=\"hljs-doctag\">&#x3C;/c></span>-equivalent as the token.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Register</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">object</span> token, <span class=\"hljs-built_in\">bool</span> isLoading</span>)</span>\n    {\n        <span class=\"hljs-keyword\">lock</span> (_lock) _loadingByToken[token] = isLoading;\n        Changed?.Invoke();\n    }\n</code></pre>\n<p><code>options</code> is a fresh allocation every render, so the dep array fails\n<code>EqualityComparer&#x3C;ResourceOptions>.Default.Equals</code> and the effect\nrestarts. Hoist the literal into a <code>static readonly</code> field, wrap it in\n<a href=\"/hooks/\"><code>UseMemo</code></a>, or pass its primitive fields directly:\n<code>UseEffect(..., url, 5000)</code>.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Match every \"subscribe\" with the cleanup that undoes it.</strong> Timer,\nevent handler, cache subscription, focus trap registration — the\neffect that creates one must return a cleanup that releases it, or\nthe next render leaks the resource.</p>\n<p><strong>Don't await directly in the lambda.</strong> <code>UseEffect(async () => ...)</code>\ndetaches from the flush ordering; capture a <code>CancellationTokenSource</code>,\nkick off a <code>Task.Run</code>, and cancel in cleanup. Or use\n<a href=\"/async-resources/\"><code>UseResource</code></a>, which already does this.</p>\n<p><strong>Treat the dep array as a value-equality contract.</strong> Records,\nprimitives, and stable setter identities are safe. Freshly-allocated\nclasses and <code>new int[] { … }</code> are not — they change every render and\nrestart the effect.</p>\n<p><strong>Read the source when timing surprises you.</strong> <code>FlushEffects</code> in\n<code>RenderContext.cs</code> is fifty lines; the two-phase cleanup-then-body\nordering is right there and explains every \"why did my cleanup run\nbefore my body did?\" question.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/effects/\">Effects</a></strong> — Previous: the surface lifecycle of <code>UseEffect</code>.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Next: the commit phase that flush runs after.</li>\n<li><strong><a href=\"/async-resources/\">Async resources</a></strong> — Hooks built on top of this scheduling.</li>\n<li><strong><a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/reference/async-system.md\">async-system reference</a></strong> — Deep walk through cache, refcount, and revalidation.</li>\n<li><strong><a href=\"/threading-and-dispatch/\">Threading and dispatch</a></strong> — Why effect bodies always run on the UI thread.</li>\n</ul>",
      "text": "The thing that trips up most readers in Microsoft.UI.Reactor (Reactor) is the order. UseEffect does not run during render — it runs after the reconciler has finished committing the new tree to WinUI, on the UI thread, in hook-declaration order, with the cleanup from the previous round of the same effect running first. State you set inside an effect body schedules another render; the next render's effects run after that one commits. This is the same shape React's effect phase takes, and the trade-off is the same: render stays a pure description of the next tree, side effects run somewhere a write is safe, and you pay one extra render whenever an effect writes state. The piece worth holding in your head is that reconciliation and effect-flush are sequential — when your effect body runs, the WinUI controls you described in this render are already mounted and visible, and the previous round's cleanup has already run. Effects Scheduling # This page is about when UseEffect fires and why. Surface-level usage is on the Effects page; the async-resource pipeline — UseResource , UseInfiniteResource , Pending , QueryCache — has its own deep walk in the async-system reference . What follows is the render-loop view of the same callbacks. The render → commit → flush sequence # A render pass runs in three phases. The first is pure description — Render() returns an Element tree and never touches WinUI. The reconciler then walks the tree, mounts or updates real controls, and only after that does it flush queued effects. public void UseEffect ( Action effect, params object [] dependencies ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new EffectHookState { Dependencies = null , Effect = effect }); } if (_hooks[_hookIndex] is not EffectHookState hook) throw new HookOrderException( $\"Hook at index {_hookIndex} is {_hooks[_hookIndex].GetType().Name} , expected EffectHookState. \" + \"Hooks must be called in the same order every render.\" ); _hookIndex++; if (hook.Dependencies is null || !DepsEqual(hook.Dependencies, dependencies)) { hook.PendingCleanup = hook.Cleanup; hook.Cleanup = null ; hook.Dependencies = dependencies.ToArray(); hook.Effect = effect; hook.Pending = true ; } } UseEffect does not call your callback. It checks the dependency array against the previous render's deps and, if they differ, marks the hook Pending = true and stashes the previous cleanup. The reconciler's commit pass — see Reconciliation — calls FlushEffectsTraced(ctx, …) after every component's controls are in place. Flush then runs all pending cleanups before any pending effect bodies, both in hook-declaration order. The cleanup-before-body ordering is the reason a cts.Cancel() / timer.Dispose() cleanup actually disposes the previous timer before a new one starts. If flush ran the body first and the cleanup second, the cleanup would dispose the timer the body just created. Phase What runs Where in source Render Component.Render() , hook slots register Component.cs Render override Reconcile Diff against previous tree, mount/update controls Reconciler.cs Reconcile Commit WinUI tree visible end of Reconciler.Mount / Update Flush — phase 1 Run all PendingCleanup callbacks (in slot order) RenderContext.FlushEffects Flush — phase 2 Run pending effect bodies (in slot order) RenderContext.FlushEffects Unmount Cleanup runs once, then hook list is dropped RenderContext.RunCleanups Caveat: The effect body sees the just-committed tree, not the tree it was described against. If your effect inspects an ElementRef or reads a DOM-equivalent property right after mount, the value reflects the WinUI control that came out of reconciliation , which can differ from what you expected if the reconciler reused a pooled control or short-circuited the update via CanSkipUpdate . Don't reason from the props you passed to the factory — reason from the control the reconciler actually attached. Dependency equality is value equality, with one trap # Reactor compares dependency arrays with EqualityComparer&#x3C;T>.Default . For primitives and records that's value equality. For classes that don't override Equals , it's reference equality — same identity trap React's useEffect has. public (T Value, Action&#x3C;T> Set) UseState&#x3C;T>(T initialValue, bool threadSafe = false ) { if (_hookIndex >= _hooks.Count) { _hooks.Add( new ValueHookState&#x3C;T>(initialValue, threadSafe)); } var currentIndex = _hookIndex; _hookIndex++; if (_hooks[currentIndex] is not ValueHookState &#x3C; T > hook) throw new HookOrderException ( $\"Hook at index {currentIndex} is {_hooks[currentIndex].GetType().Name} , expected ValueHookState&#x3C; { typeof (T).Name} > (UseState). \" + \"Hooks must be called in the same order every render.\" ) ; UseState returns the same Action&#x3C;T> setter instance across renders for a given hook slot (the closure captures currentIndex , which doesn't change). A var (count, setCount) = UseState(0) setter is therefore safe to pass as a dependency — it compares equal between renders and won't restart the effect. A freshly constructed object on every render isn't. new Options { ... } in a render body, passed as a dep, restarts the effect every render — the object differs by reference even when its fields are identical. Either move it outside the component, wrap it in UseMemo , or unpack the primitive fields and pass those. Cleanup ordering on update and on unmount # A re-render with changed deps runs the previous effect's cleanup before the new effect body. An unmount runs cleanup with no following body. The hook state carries both Cleanup (the live one) and PendingCleanup (the one queued for the next flush) so the two paths don't collide: public sealed class PendingScope { private readonly Dictionary&#x3C; object , bool > _loadingByToken = new (capacity: 4 ); private readonly object _lock = new (); /// &#x3C;summary> Fires when a resource joins, leaves, or changes its loading state. &#x3C;/summary> public event Action? Changed; /// &#x3C;summary> /// Start tracking &#x3C;paramref name=\"token\"/> with the given initial &#x3C;paramref name=\"isLoading\"/> /// state. A hook typically uses its own &#x3C;c> this &#x3C;/c> -equivalent as the token. /// &#x3C;/summary> public void Register ( object token, bool isLoading ) { lock (_lock) _loadingByToken[token] = isLoading; Changed?.Invoke(); } PendingScope is one place the cleanup contract is load-bearing. A UseResource hook registers its token with the nearest ancestor Pending scope at mount, updates loading state through its lifecycle, and unregisters in cleanup. Skip the unregister and the Pending fallback never clears — the fallback subtree stays rendered after the resource resolves because the scope still thinks something is loading. Always return a cleanup from any effect that registers state outside the component. Async effects and cancellation # UseEffect takes an Action . Awaiting inside an async void lambda is a footgun — the await machinery decouples the body from the flush ordering, so the cleanup runs against an effect that may not have completed. The shape that works is \"kick off a task, return a cleanup that cancels it\": // Don't: UseEffect( async () => { // async void lambda var data = await Fetch(url); setData(data); // may run after unmount → cancellation cliff }); public sealed class QueryCache : IDisposable { /// &#x3C;summary> How often the eviction timer checks every slot for expiry. Default 1s. &#x3C;/summary> public static TimeSpan EvictionPollInterval { get ; set ; } = TimeSpan.FromSeconds( 1 ); /// &#x3C;summary> Injected marshaller for &#x3C;see cref=\"EntryChanged\"/> . Null → fires inline. &#x3C;/summary> public Action&#x3C;Action>? DispatcherPost { get ; set ; } /// &#x3C;summary> Clock override for deterministic tests. &#x3C;/summary> public Func&#x3C;DateTime> UtcNow { get ; set ; } = () => DateTime.UtcNow; private readonly ConcurrentDictionary&#x3C; string , Slot> _slots = new (); private Timer? _evictionTimer; private readonly object _timerLock = new (); private int _disposed; /// &#x3C;summary> Fires when an entry is added, replaced, invalidated, or evicted. &#x3C;/summary> public event Action&#x3C; string >? EntryChanged; The async-resource hooks ( UseResource , UseInfiniteResource , UseMutation ) wrap this pattern correctly — they own the cancellation token, marshal completions back to the UI thread, and clean up their QueryCache subscription on unmount. Reach for them before writing async-effect glue by hand. When you do need a raw UseEffect for an async operation, the cleanup function is your only chance to cancel the in-flight task — capture a CancellationTokenSource , return () => cts.Cancel() . Dependency-array reference # Dep array shape When the effect re-runs omitted ( UseEffect(body) ) after every commit empty ( UseEffect(body, Array.Empty&#x3C;object>()) ) once, after the first commit primitives / records when EqualityComparer&#x3C;T>.Default.Equals flips reference-typed object built in render every commit (almost never what you want) setX from UseState never re-triggers — setter identity is stable Patterns # Subscribe in mount, unsubscribe in cleanup # The single most useful effect shape is \"talk to something outside the component for as long as I'm on screen, then release it\". Reactor's own Observable.cs#observable-bridge bridge follows this shape; so does any timer, event subscription, or background task. UseEffect(() => { var cts = new CancellationTokenSource(); _ = Task.Run( async () => { using var timer = new PeriodicTimer(TimeSpan.FromSeconds( 1 )); while ( await timer.WaitForNextTickAsync(cts.Token)) setSeconds(s => s + 1 ); }); return () => cts.Cancel(); }, Array.Empty&#x3C; object >()); The setter setSeconds auto-marshals back to the UI thread (see hooks \"Updating state from background work\"). The cleanup cancels the token; the timer's using disposes when the WaitForNextTickAsync loop unblocks. No state leaks between mounts. Effect that observes a prop / state change # When you need to react to a value flipping — open a dialog, fetch fresh data, broadcast a notification — pass the value as a dependency. The cleanup runs the moment the value changes, before the new effect body, so any work tied to the old value is torn down first. UseEffect(() => { if (!isOpen) return () => { }; LogTelemetry( \"dialog_opened\" ); return () => LogTelemetry( \"dialog_closed\" ); }, isOpen); Telemetry stays balanced because flush runs the previous body's cleanup (the \"closed\" log) before the new body's \"opened\" log — the events arrive in the same order as the user's actions. Common Mistakes # Setting state unconditionally inside an effect # // Don't: UseEffect(() => { setCount(c => c + 1 ); // no deps → runs every commit → infinite loop }); void Setter ( T newValue ) { var h = (ValueHookState&#x3C;T>)_hooks[currentIndex]; bool changed; if (h.ThreadSafe) { lock (h.Lock) { changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; } if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } else { if (MarshalIfOffUIThread( \"UseState\" , () => Setter(newValue))) return ; changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } } Every state setter schedules a re-render. An effect with no deps re-runs after every commit, including the commit its own setter caused , so the component renders in a tight loop until something else breaks the cycle. Either gate the setter on a condition that can't re-trigger, or pass the value the setter depends on as a dep so the effect only fires when it actually changes. Building a dep object inside render # // Don't: var options = new ResourceOptions { Timeout = 5000 }; UseEffect(() => Fetch(url, options), url, options); // options is new every render public sealed class PendingScope { private readonly Dictionary&#x3C; object , bool > _loadingByToken = new (capacity: 4 ); private readonly object _lock = new (); /// &#x3C;summary> Fires when a resource joins, leaves, or changes its loading state. &#x3C;/summary> public event Action? Changed; /// &#x3C;summary> /// Start tracking &#x3C;paramref name=\"token\"/> with the given initial &#x3C;paramref name=\"isLoading\"/> /// state. A hook typically uses its own &#x3C;c> this &#x3C;/c> -equivalent as the token. /// &#x3C;/summary> public void Register ( object token, bool isLoading ) { lock (_lock) _loadingByToken[token] = isLoading; Changed?.Invoke(); } options is a fresh allocation every render, so the dep array fails EqualityComparer&#x3C;ResourceOptions>.Default.Equals and the effect restarts. Hoist the literal into a static readonly field, wrap it in UseMemo , or pass its primitive fields directly: UseEffect(..., url, 5000) . Tips # Match every \"subscribe\" with the cleanup that undoes it. Timer, event handler, cache subscription, focus trap registration — the effect that creates one must return a cleanup that releases it, or the next render leaks the resource. Don't await directly in the lambda. UseEffect(async () => ...) detaches from the flush ordering; capture a CancellationTokenSource , kick off a Task.Run , and cancel in cleanup. Or use UseResource , which already does this. Treat the dep array as a value-equality contract. Records, primitives, and stable setter identities are safe. Freshly-allocated classes and new int[] { … } are not — they change every render and restart the effect. Read the source when timing surprises you. FlushEffects in RenderContext.cs is fifty lines; the two-phase cleanup-then-body ordering is right there and explains every \"why did my cleanup run before my body did?\" question. Next Steps # Effects — Previous: the surface lifecycle of UseEffect . Reconciliation — Next: the commit phase that flush runs after. Async resources — Hooks built on top of this scheduling. async-system reference — Deep walk through cache, refcount, and revalidation. Threading and dispatch — Why effect bodies always run on the UI thread.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/effects-scheduling.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/effects-scheduling.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-render--commit--flush-sequence",
          "text": "The render → commit → flush sequence"
        },
        {
          "depth": 2,
          "id": "dependency-equality-is-value-equality-with-one-trap",
          "text": "Dependency equality is value equality, with one trap"
        },
        {
          "depth": 2,
          "id": "cleanup-ordering-on-update-and-on-unmount",
          "text": "Cleanup ordering on update and on unmount"
        },
        {
          "depth": 2,
          "id": "async-effects-and-cancellation",
          "text": "Async effects and cancellation"
        },
        {
          "depth": 2,
          "id": "dependency-array-reference",
          "text": "Dependency-array reference"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "subscribe-in-mount-unsubscribe-in-cleanup",
          "text": "Subscribe in mount, unsubscribe in cleanup"
        },
        {
          "depth": 3,
          "id": "effect-that-observes-a-prop--state-change",
          "text": "Effect that observes a prop / state change"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "setting-state-unconditionally-inside-an-effect",
          "text": "Setting state unconditionally inside an effect"
        },
        {
          "depth": 3,
          "id": "building-a-dep-object-inside-render",
          "text": "Building a dep object inside render"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Threading and Dispatch",
      "path": "threading-and-dispatch.md",
      "slug": "/threading-and-dispatch/",
      "html": "<h1 id=\"threading-and-dispatch\">Threading and Dispatch<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#threading-and-dispatch\">#</a></h1>\n<p>Microsoft.UI.Reactor (Reactor) has one UI thread invariant: the reconciler, every hook setter,\nand every WinUI property write must happen on the thread that captured\nthe dispatcher at app bootstrap. When a setter is called from a\nbackground task, <a href=\"/hooks/\"><code>RenderContext</code></a> auto-marshals it onto that\ncaptured dispatcher; if you opt a hook into <code>threadSafe: true</code>, the\nwrite happens in place under a per-cell lock instead. This page covers\nwhich call sites enforce the invariant, how the marshal works, and the\ntwo error modes that bypass it loudly rather than silently.</p>\n<h2 id=\"the-ui-thread-invariant\">The UI-thread invariant<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-ui-thread-invariant\">#</a></h2>\n<p>The render context records <code>Environment.CurrentManagedThreadId</code> at the\nstart of every render pass. Every subsequent hook-setter call compares\nagainst that captured id and routes accordingly:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">MarshalIfOffUIThread</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> hookName, Action work</span>)</span>\n{\n    <span class=\"hljs-comment\">// Hot path — same thread that ran BeginRender. ~1ns TLS read + cmp + branch.</span>\n    <span class=\"hljs-keyword\">if</span> (Environment.CurrentManagedThreadId == _uiThreadId) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p>The hot path is <code>Environment.CurrentManagedThreadId == _uiThreadId</code> —\none TLS read, one compare, one branch. Same-thread callers pay\nessentially nothing; the marshal path only runs when the comparison\nfails. The same predicate guards hooks beyond setters: <code>Action</code>-typed\nhook callbacks, effect bodies, and reconciler mutations all assume the\nUI thread.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Call site</th><th>Thread invariant</th><th>What enforces it</th></tr></thead><tbody><tr><td><code>Component.Render()</code></td><td>UI thread</td><td><code>RenderContext.BeginRender</code> captures <code>_uiThreadId</code>; reconciler invokes on the dispatcher</td></tr><tr><td><code>setX(...)</code> / <code>updateX(...)</code> from <code>UseState</code> / <code>UseReducer</code></td><td>Any thread</td><td><code>MarshalIfOffUIThread</code> in <code>RenderContext.cs</code></td></tr><tr><td><code>setX(...)</code> with <code>threadSafe: true</code></td><td>Any thread</td><td>per-cell <code>lock</code> in <code>ValueHookState&#x3C;T></code>; no marshal hop</td></tr><tr><td><code>UseEffect</code> body</td><td>UI thread</td><td><code>RenderContext.FlushEffects</code> runs on the dispatcher</td></tr><tr><td><code>UseEffect</code> cleanup</td><td>UI thread</td><td>same dispatcher as flush</td></tr><tr><td><code>Reconciler.Reconcile</code></td><td>UI thread</td><td>host calls it from a dispatcher continuation</td></tr><tr><td><code>ReactorWindow</code> / tray-icon / <code>OpenWindow</code> mutators</td><td>UI thread</td><td><code>ThreadAffinity.ThrowIfNotOnUIThread</code></td></tr><tr><td><code>QueryCache</code> reads + writes</td><td>Any thread</td><td>per-slot lock + <code>ConcurrentDictionary</code></td></tr></tbody></table>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/threading-and-dispatch/dispatch-flow.svg\" alt=\"Cross-thread setter dispatch: a background task calls setSeconds, the marshal-guard sees a thread-id mismatch, TryEnqueue posts onto the captured UI dispatcher, the queued lambda re-enters the setter on the UI thread, RenderContext writes the slot, and the reconciler runs the resulting re-render.\"></p>\n<h2 id=\"cross-thread-setters-auto-marshal\">Cross-thread setters auto-marshal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#cross-thread-setters-auto-marshal\">#</a></h2>\n<p>Calling <code>setX(value)</code> from a <code>Task.Run</code>, a <code>PeriodicTimer</code> loop, or\nafter <code>await ... ConfigureAwait(false)</code> is correct by default — the\nsetter detects the off-thread call and queues itself back onto the\ncaptured dispatcher:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Setter</span>(<span class=\"hljs-params\">T newValue</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> h = (ValueHookState&#x3C;T>)_hooks[currentIndex];\n    <span class=\"hljs-built_in\">bool</span> changed;\n    <span class=\"hljs-keyword\">if</span> (h.ThreadSafe)\n    {\n        <span class=\"hljs-keyword\">lock</span> (h.Lock)\n        {\n            changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n            <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        }\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        <span class=\"hljs-keyword\">if</span> (MarshalIfOffUIThread(<span class=\"hljs-string\">\"UseState\"</span>, () => Setter(newValue))) <span class=\"hljs-keyword\">return</span>;\n        changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue);\n        <span class=\"hljs-keyword\">if</span> (changed) h.Value = newValue;\n        <span class=\"hljs-keyword\">if</span> (Diagnostics.ReactorEventSource.Log.IsEnabled(\n                <span class=\"hljs-keyword\">global</span>::System.Diagnostics.Tracing.EventLevel.Verbose,\n                Diagnostics.ReactorEventSource.Keywords.State))\n            Diagnostics.ReactorEventSource.Log.StateChange(<span class=\"hljs-string\">\"UseState\"</span>, <span class=\"hljs-keyword\">typeof</span>(T).Name, changed);\n        <span class=\"hljs-keyword\">if</span> (changed) _requestRerender?.Invoke();\n    }\n}\n</code></pre>\n<p>The marshalled lambda re-enters <code>Setter(newValue)</code> on the UI thread,\nwhich falls into the hot path the second time around. One\n<code>DispatcherQueue.TryEnqueue</code> per cross-thread call — microseconds, not\nfree, but vastly cheaper than the bugs you would hit writing the field\ndirectly from a worker.</p>\n<p>The two failure modes throw immediately rather than swallow:</p>\n<ul>\n<li><strong>No captured dispatcher.</strong> <code>ReactorApp.UIDispatcher is null</code> —\ntypical in unit tests, headless renders, or before bootstrap has\nfinished. The setter throws <code>InvalidOperationException</code> naming the\nthread and the captured UI thread id so the test failure points at\nthe missing host setup.</li>\n<li><strong>Dispatcher refused the enqueue.</strong> <code>TryEnqueue</code> returns false when\nthe dispatcher has begun shutting down. The setter throws rather\nthan dropping the update silently — that update was lost, and a\nloud throw beats a state cell that mysteriously stops advancing\nnear window close. Cancel background producers in your effect\ncleanup so they stop before the window closes.</li>\n</ul>\n<h2 id=\"the-trampoline-guard-for-non-setter-mutators\">The trampoline guard for non-setter mutators<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-trampoline-guard-for-non-setter-mutators\">#</a></h2>\n<p>Setters reach the UI thread via auto-marshal. Other mutators — opening\na window, changing tray-icon state, the spec-036 <a href=\"/navigation/\"><code>ReactorWindow</code></a>\nsurface — assert the UI-thread invariant explicitly via <code>ThreadAffinity</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ThrowIfNotOnUIThread</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> memberName</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> dispatcher = ReactorApp.UIDispatcher;\n    <span class=\"hljs-keyword\">if</span> (dispatcher <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (dispatcher.HasThreadAccess) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> InvalidOperationException(\n        <span class=\"hljs-string\">$\"<span class=\"hljs-subst\">{memberName}</span> must be called on the UI thread. \"</span> +\n        <span class=\"hljs-string\">\"Use ReactorApp.UIDispatcher.TryEnqueue(...) to marshal the call. (spec 036 §0.4)\"</span>);\n}\n</code></pre>\n<p><code>ThrowIfNotOnUIThread</code> is the assert; callers that need to be UI-thread\naffine call it at the top. When no dispatcher has been captured (early\nbootstrap, fixture setup), the assert is a no-op — those phases happen\non the bootstrap thread before there is a UI thread to gate against.\nOnce <code>UIDispatcher</code> is captured, the assert is mandatory: any\nnon-affine call site has to either <code>dispatcher.TryEnqueue(...)</code> or\nfail loudly.</p>\n<h2 id=\"threadsafe-true-skips-the-marshal\"><code>threadSafe: true</code> skips the marshal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#threadsafe-true-skips-the-marshal\">#</a></h2>\n<p><code>UseState&#x3C;T>(initial, threadSafe: true)</code> and the matching <code>UseReducer</code>\noverload route writes through a per-cell <code>lock</code> instead of the\ndispatcher. The trade-off is visibility: a non-thread-safe setter\nserializes through the UI tick, so the next read inside the same\nsynchronous reducer sees the post-write value only after the queued\ncall drains. Thread-safe setters serialize under a lock, so concurrent\nproducers settle deterministically before any of them returns, but the\nre-render still ends up on the UI thread because <code>_requestRerender</code> is\nitself dispatcher-affine. Reach for <code>threadSafe: true</code> when you have\nmany concurrent producers writing the same hook (ingest loops, sensor\ncallbacks); leave it off for the common case where one or two\nbackground tasks update state.</p>\n<h2 id=\"the-change-echo-suppressor\">The change-echo suppressor<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-change-echo-suppressor\">#</a></h2>\n<p>A separate UI-thread-only concern: programmatic writes to a control\nthat raise a <code>Changed</code> event would re-enter the user's <code>OnChanged</code>\ncallback with a value the framework just wrote. The reconciler\nsuppresses one such event per programmatic write:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">BeginSuppress</span>(<span class=\"hljs-params\">UIElement control</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (control <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> FrameworkElement fe) <span class=\"hljs-keyword\">return</span>;\n    Reconciler.GetOrCreateReactorState(fe).EchoSuppressCount++;\n}\n\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Returns <span class=\"hljs-doctag\">&#x3C;c></span>true<span class=\"hljs-doctag\">&#x3C;/c></span> if the current event fire should be suppressed (and</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> decrements the counter). Returns <span class=\"hljs-doctag\">&#x3C;c></span>false<span class=\"hljs-doctag\">&#x3C;/c></span> otherwise. Call at the top</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> of a change-event handler before invoking the user's OnChanged.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldSuppress</span>(<span class=\"hljs-params\">UIElement control</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (control <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> FrameworkElement fe) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">false</span>;\n    <span class=\"hljs-keyword\">if</span> (fe.GetValue(Reconciler.ReactorAttached.StateProperty) <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> Reconciler.ReactorState state)\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">false</span>;\n    <span class=\"hljs-comment\">// §8.2 — setter-suppression scope: drop the echo without consuming a</span>\n    <span class=\"hljs-comment\">// counter token. The scope wraps ApplySetters, where the engine can't</span>\n    <span class=\"hljs-comment\">// predict which value-bearing DPs the user's `.Set(...)` will write.</span>\n    <span class=\"hljs-keyword\">if</span> (state.EchoSuppressScopeDepth > <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">true</span>;\n    <span class=\"hljs-keyword\">if</span> (state.EchoSuppressCount > <span class=\"hljs-number\">0</span>)\n    {\n        state.EchoSuppressCount--;\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">true</span>;\n    }\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">false</span>;\n}\n</code></pre>\n<p><code>BeginSuppress</code> increments a counter attached to the WinUI control;\n<code>ShouldSuppress</code> checks-and-decrements at the top of the change-event\nhandler. The pair is one-for-one with the programmatic write — if a\nwrite doesn't actually change the value, suppress is balanced by an\nevent that never fires, so callers guard with an equality check first.\nThe counter lives on a <code>DependencyObject</code> attached property rather\nthan a <code>ConditionalWeakTable</code> because <a href=\"/reactor-vs-xaml/\">WinRT projection</a>\ncan hand the same native object back as different managed wrappers,\nand only the attached DP survives that round-trip.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Don't <code>Thread.Sleep</code> on the UI thread.</strong> Block the dispatcher and\nthe reconciler stops too — pending renders, queued effects, and any\nmarshalled setters from background tasks all stall together. Use\n<code>Task.Delay</code> from a worker and let the setter marshal back.</p>\n<p><strong>One captured dispatcher per process.</strong> <code>ReactorApp.UIDispatcher</code> is\nset during host bootstrap. Multi-window apps share the same dispatcher\n(spec 036). Background services that fan out to several windows post\nthrough the same <code>TryEnqueue</code> — no per-window threading.</p>\n<p><strong>Prefer auto-marshal over manual <code>TryEnqueue</code> in render code.</strong> The\nsetter already does the right thing; wrapping it in another\n<code>TryEnqueue</code> queues a no-op onto the queue and just costs an extra\nhop. Manual <code>TryEnqueue</code> is for non-setter UI mutations.</p>\n<p><strong>The throw is the point.</strong> If a setter throws because no dispatcher\nwas captured, you're in a unit-test or headless context that needs to\ndrive <code>RenderContext</code> directly. Don't catch it — the throw is your\nsignal to wire the test fixture (<code>ReactorHost</code>, <code>TestRenderHost</code>) so\nthe dispatcher is captured before the setter runs.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/effects-scheduling/\">Effects scheduling</a></strong> — Previous: where flush runs (always on this same UI thread).</li>\n<li><strong><a href=\"/element-pool/\">Element pool</a></strong> — Next: the per-thread pool the reconciler rents controls from.</li>\n<li><strong><a href=\"/hooks/\">Hooks</a></strong> — Surface for <code>UseState(threadSafe: true)</code>.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture overview</a></strong> — Where the dispatcher fits in the render loop.</li>\n</ul>",
      "text": "Threading and Dispatch # Microsoft.UI.Reactor (Reactor) has one UI thread invariant: the reconciler, every hook setter, and every WinUI property write must happen on the thread that captured the dispatcher at app bootstrap. When a setter is called from a background task, RenderContext auto-marshals it onto that captured dispatcher; if you opt a hook into threadSafe: true , the write happens in place under a per-cell lock instead. This page covers which call sites enforce the invariant, how the marshal works, and the two error modes that bypass it loudly rather than silently. The UI-thread invariant # The render context records Environment.CurrentManagedThreadId at the start of every render pass. Every subsequent hook-setter call compares against that captured id and routes accordingly: private bool MarshalIfOffUIThread ( string hookName, Action work ) { // Hot path — same thread that ran BeginRender. ~1ns TLS read + cmp + branch. if (Environment.CurrentManagedThreadId == _uiThreadId) return false ; The hot path is Environment.CurrentManagedThreadId == _uiThreadId — one TLS read, one compare, one branch. Same-thread callers pay essentially nothing; the marshal path only runs when the comparison fails. The same predicate guards hooks beyond setters: Action -typed hook callbacks, effect bodies, and reconciler mutations all assume the UI thread. Call site Thread invariant What enforces it Component.Render() UI thread RenderContext.BeginRender captures _uiThreadId ; reconciler invokes on the dispatcher setX(...) / updateX(...) from UseState / UseReducer Any thread MarshalIfOffUIThread in RenderContext.cs setX(...) with threadSafe: true Any thread per-cell lock in ValueHookState&#x3C;T> ; no marshal hop UseEffect body UI thread RenderContext.FlushEffects runs on the dispatcher UseEffect cleanup UI thread same dispatcher as flush Reconciler.Reconcile UI thread host calls it from a dispatcher continuation ReactorWindow / tray-icon / OpenWindow mutators UI thread ThreadAffinity.ThrowIfNotOnUIThread QueryCache reads + writes Any thread per-slot lock + ConcurrentDictionary Cross-thread setters auto-marshal # Calling setX(value) from a Task.Run , a PeriodicTimer loop, or after await ... ConfigureAwait(false) is correct by default — the setter detects the off-thread call and queues itself back onto the captured dispatcher: void Setter ( T newValue ) { var h = (ValueHookState&#x3C;T>)_hooks[currentIndex]; bool changed; if (h.ThreadSafe) { lock (h.Lock) { changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; } if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } else { if (MarshalIfOffUIThread( \"UseState\" , () => Setter(newValue))) return ; changed = !EqualityComparer&#x3C;T>.Default.Equals(h.Value, newValue); if (changed) h.Value = newValue; if (Diagnostics.ReactorEventSource.Log.IsEnabled( global ::System.Diagnostics.Tracing.EventLevel.Verbose, Diagnostics.ReactorEventSource.Keywords.State)) Diagnostics.ReactorEventSource.Log.StateChange( \"UseState\" , typeof (T).Name, changed); if (changed) _requestRerender?.Invoke(); } } The marshalled lambda re-enters Setter(newValue) on the UI thread, which falls into the hot path the second time around. One DispatcherQueue.TryEnqueue per cross-thread call — microseconds, not free, but vastly cheaper than the bugs you would hit writing the field directly from a worker. The two failure modes throw immediately rather than swallow: No captured dispatcher. ReactorApp.UIDispatcher is null — typical in unit tests, headless renders, or before bootstrap has finished. The setter throws InvalidOperationException naming the thread and the captured UI thread id so the test failure points at the missing host setup. Dispatcher refused the enqueue. TryEnqueue returns false when the dispatcher has begun shutting down. The setter throws rather than dropping the update silently — that update was lost, and a loud throw beats a state cell that mysteriously stops advancing near window close. Cancel background producers in your effect cleanup so they stop before the window closes. The trampoline guard for non-setter mutators # Setters reach the UI thread via auto-marshal. Other mutators — opening a window, changing tray-icon state, the spec-036 ReactorWindow surface — assert the UI-thread invariant explicitly via ThreadAffinity : public static void ThrowIfNotOnUIThread ( string memberName ) { var dispatcher = ReactorApp.UIDispatcher; if (dispatcher is null ) return ; if (dispatcher.HasThreadAccess) return ; throw new InvalidOperationException( $\" {memberName} must be called on the UI thread. \" + \"Use ReactorApp.UIDispatcher.TryEnqueue(...) to marshal the call. (spec 036 §0.4)\" ); } ThrowIfNotOnUIThread is the assert; callers that need to be UI-thread affine call it at the top. When no dispatcher has been captured (early bootstrap, fixture setup), the assert is a no-op — those phases happen on the bootstrap thread before there is a UI thread to gate against. Once UIDispatcher is captured, the assert is mandatory: any non-affine call site has to either dispatcher.TryEnqueue(...) or fail loudly. threadSafe: true skips the marshal # UseState&#x3C;T>(initial, threadSafe: true) and the matching UseReducer overload route writes through a per-cell lock instead of the dispatcher. The trade-off is visibility: a non-thread-safe setter serializes through the UI tick, so the next read inside the same synchronous reducer sees the post-write value only after the queued call drains. Thread-safe setters serialize under a lock, so concurrent producers settle deterministically before any of them returns, but the re-render still ends up on the UI thread because _requestRerender is itself dispatcher-affine. Reach for threadSafe: true when you have many concurrent producers writing the same hook (ingest loops, sensor callbacks); leave it off for the common case where one or two background tasks update state. The change-echo suppressor # A separate UI-thread-only concern: programmatic writes to a control that raise a Changed event would re-enter the user's OnChanged callback with a value the framework just wrote. The reconciler suppresses one such event per programmatic write: internal static void BeginSuppress ( UIElement control ) { if (control is not FrameworkElement fe) return ; Reconciler.GetOrCreateReactorState(fe).EchoSuppressCount++; } /// &#x3C;summary> /// Returns &#x3C;c> true &#x3C;/c> if the current event fire should be suppressed (and /// decrements the counter). Returns &#x3C;c> false &#x3C;/c> otherwise. Call at the top /// of a change-event handler before invoking the user's OnChanged. /// &#x3C;/summary> internal static bool ShouldSuppress ( UIElement control ) { if (control is not FrameworkElement fe) return false ; if (fe.GetValue(Reconciler.ReactorAttached.StateProperty) is not Reconciler.ReactorState state) return false ; // §8.2 — setter-suppression scope: drop the echo without consuming a // counter token. The scope wraps ApplySetters, where the engine can't // predict which value-bearing DPs the user's `.Set(...)` will write. if (state.EchoSuppressScopeDepth > 0 ) return true ; if (state.EchoSuppressCount > 0 ) { state.EchoSuppressCount--; return true ; } return false ; } BeginSuppress increments a counter attached to the WinUI control; ShouldSuppress checks-and-decrements at the top of the change-event handler. The pair is one-for-one with the programmatic write — if a write doesn't actually change the value, suppress is balanced by an event that never fires, so callers guard with an equality check first. The counter lives on a DependencyObject attached property rather than a ConditionalWeakTable because WinRT projection can hand the same native object back as different managed wrappers, and only the attached DP survives that round-trip. Tips # Don't Thread.Sleep on the UI thread. Block the dispatcher and the reconciler stops too — pending renders, queued effects, and any marshalled setters from background tasks all stall together. Use Task.Delay from a worker and let the setter marshal back. One captured dispatcher per process. ReactorApp.UIDispatcher is set during host bootstrap. Multi-window apps share the same dispatcher (spec 036). Background services that fan out to several windows post through the same TryEnqueue — no per-window threading. Prefer auto-marshal over manual TryEnqueue in render code. The setter already does the right thing; wrapping it in another TryEnqueue queues a no-op onto the queue and just costs an extra hop. Manual TryEnqueue is for non-setter UI mutations. The throw is the point. If a setter throws because no dispatcher was captured, you're in a unit-test or headless context that needs to drive RenderContext directly. Don't catch it — the throw is your signal to wire the test fixture ( ReactorHost , TestRenderHost ) so the dispatcher is captured before the setter runs. Next Steps # Effects scheduling — Previous: where flush runs (always on this same UI thread). Element pool — Next: the per-thread pool the reconciler rents controls from. Hooks — Surface for UseState(threadSafe: true) . Architecture overview — Where the dispatcher fits in the render loop.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/threading-and-dispatch.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/threading-and-dispatch.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-ui-thread-invariant",
          "text": "The UI-thread invariant"
        },
        {
          "depth": 2,
          "id": "cross-thread-setters-auto-marshal",
          "text": "Cross-thread setters auto-marshal"
        },
        {
          "depth": 2,
          "id": "the-trampoline-guard-for-non-setter-mutators",
          "text": "The trampoline guard for non-setter mutators"
        },
        {
          "depth": 2,
          "id": "threadsafe-true-skips-the-marshal",
          "text": "threadSafe: true skips the marshal"
        },
        {
          "depth": 2,
          "id": "the-change-echo-suppressor",
          "text": "The change-echo suppressor"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Source Mapping",
      "path": "source-mapping.md",
      "slug": "/source-mapping/",
      "html": "<h1 id=\"source-mapping\">Source Mapping<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#source-mapping\">#</a></h1>\n<p>\"Source mapping\" in Microsoft.UI.Reactor (Reactor) is the chain that ties a runtime artifact —\nan ETW event, a <code>--preview</code> overlay highlight, a thrown exception —\nback to the C# source that produced it. Today, attribution is at the\n<em>component</em> granularity: every render emits an ETW event carrying the\ncomponent's type name, and exceptions log with the same name plus the\nexception type. Spec 010 designs the next step — per-<em>element</em> source\ntagging via <code>[CallerFilePath]</code> / <code>[CallerLineNumber]</code> attributes on\nthe DSL factories, plumbed onto realized WinUI controls through an\nattached <code>DependencyProperty</code>. This page covers what's emitted now and\nhow the design layers on top of it.</p>\n<blockquote>\n<p><strong>Status.</strong> Per-element source tagging (<code>Element.Source</code>,\n<code>SourceInfo.LocationProperty</code>) is <strong>designed but not implemented</strong>.\nThe reference for the design is\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/010-source-mapping-design.md\">spec 010</a>. This page treats\nit as the next-step plan, not current behavior. Component-name\nattribution via ETW is shipping today and is documented below.</p>\n</blockquote>\n<h2 id=\"component-name-attribution-via-etw\">Component-name attribution via ETW<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#component-name-attribution-via-etw\">#</a></h2>\n<p>Every <code>ComponentRender</code> boundary emits an ETW event keyed by the\ncomponent's CLR type name. The keyword <code>Render</code> gates the events so\nconsumers can subscribe to just the render channel:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Keywords</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Reconcile = (EventKeywords)<span class=\"hljs-number\">0x1</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Render = (EventKeywords)<span class=\"hljs-number\">0x2</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords State = (EventKeywords)<span class=\"hljs-number\">0x4</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Mcp = (EventKeywords)<span class=\"hljs-number\">0x8</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Lifecycle = (EventKeywords)<span class=\"hljs-number\">0x10</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Errors = (EventKeywords)<span class=\"hljs-number\">0x20</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords EventDispatch = (EventKeywords)<span class=\"hljs-number\">0x40</span>;\n    <span class=\"hljs-comment\">// Spec 044 — subsystem coverage gaps. Each gets its own bit so a</span>\n    <span class=\"hljs-comment\">// consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can</span>\n    <span class=\"hljs-comment\">// pick exactly the area it cares about without paying for the rest.</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Hosting = (EventKeywords)<span class=\"hljs-number\">0x80</span>;       <span class=\"hljs-comment\">// Window/HWND/DPI/Backdrop</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Persistence = (EventKeywords)<span class=\"hljs-number\">0x100</span>;  <span class=\"hljs-comment\">// settings store, placement</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Navigation = (EventKeywords)<span class=\"hljs-number\">0x200</span>;   <span class=\"hljs-comment\">// route push, cache, transitions</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Intl = (EventKeywords)<span class=\"hljs-number\">0x400</span>;         <span class=\"hljs-comment\">// missing keys, fallback, format</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Theme = (EventKeywords)<span class=\"hljs-number\">0x800</span>;        <span class=\"hljs-comment\">// theme apply, bindings</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Shell = (EventKeywords)<span class=\"hljs-number\">0x1000</span>;       <span class=\"hljs-comment\">// JumpList/Tray/ThumbnailToolbar</span>\n}\n</code></pre>\n<p><code>ComponentRenderStart</code> / <code>ComponentRenderStop</code> fire with\n<code>componentName = node.Component?.GetType().Name</code>. That string is the\nattribution token that flows into PerfView / <code>dotnet-trace</code> /\n<code>xperf</code>, and it is the same string <a href=\"/devtools-internals/\"><code>devtools-internals</code></a>\nuses to label overlay frames. Per-component, not per-element — but\nsufficient for the common question \"which component is re-rendering\non every tick\".</p>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/source-mapping/attribution.svg\" alt=\"Source attribution today vs. spec 010 design: today the chain stops at Component.GetType().Name flowing into ETW; the design adds CallerInfo on the DSL factories, an Element.Source slot, and a WinUI attached DP that an inspector can read.\"></p>\n<h2 id=\"reconcile-pass-attribution\">Reconcile-pass attribution<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reconcile-pass-attribution\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Signal</th><th>Granularity</th><th>Where it surfaces</th></tr></thead><tbody><tr><td><code>ComponentRenderStart</code> / <code>Stop</code></td><td>Component CLR type name</td><td>ETW <code>Render</code> keyword</td></tr><tr><td><code>ReconcileStart</code> / <code>Stop</code></td><td>Root element type + diff counters</td><td>ETW <code>Reconcile</code> keyword</td></tr><tr><td><code>EffectsFlushStart</code> / <code>Stop</code></td><td>Component CLR type name</td><td>ETW <code>Render</code> keyword</td></tr><tr><td><code>StateChange</code></td><td>Hook kind + value type</td><td>ETW <code>State</code> keyword</td></tr><tr><td><code>RenderError</code></td><td>Component name + exception type only (message redacted)</td><td>ETW <code>Errors</code> keyword</td></tr><tr><td>Per-element file:line</td><td><strong>Designed (spec 010)</strong> — not shipping</td><td>Future: <code>Element.Source</code> + <code>SourceInfo.LocationProperty</code></td></tr></tbody></table>\n<p>The reconcile pass also emits a counter summary on stop:</p>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Event(2, Level = EventLevel.Informational, Keywords = Keywords.Reconcile,\n    Task = Tasks.Reconcile, Opcode = EventOpcode.Stop,\n    Message = <span class=\"hljs-string\">\"Reconcile stop (diffed={elementsDiffed}, skipped={elementsSkipped}, created={uiElementsCreated}, modified={uiElementsModified})\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ReconcileStop</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> elementsDiffed, <span class=\"hljs-built_in\">int</span> elementsSkipped, <span class=\"hljs-built_in\">int</span> uiElementsCreated, <span class=\"hljs-built_in\">int</span> uiElementsModified</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Informational, Keywords.Reconcile))\n        WriteEvent(<span class=\"hljs-number\">2</span>, elementsDiffed, elementsSkipped, uiElementsCreated, uiElementsModified);\n}\n</code></pre>\n<p><code>elementsDiffed</code> / <code>elementsSkipped</code> / <code>uiElementsCreated</code> /\n<code>uiElementsModified</code> give a frame-level view of how much actual work\nthe reconciler did. None of these carry a source location — they're\naggregate counters — but pairing them with the component start/stop\nevents tells you \"this component rendered, the reconciler touched N\nelements, and Y of them resulted in real WinUI writes.\"</p>\n<h2 id=\"why-etw-attribution-stops-at-the-component\">Why ETW attribution stops at the component<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#why-etw-attribution-stops-at-the-component\">#</a></h2>\n<p>The reconciler hand-rolls per-mount calls into <code>MountXxx</code> handlers,\neach of which constructs a fresh WinUI control. The handler doesn't\nknow which user line called <code>Text(\"hello\")</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Component</span>\n{\n    <span class=\"hljs-keyword\">internal</span> RenderContext Context { <span class=\"hljs-keyword\">get</span>; } = <span class=\"hljs-keyword\">new</span>();\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override to describe the UI. Use UseState, UseEffect, etc. from the context.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Must call hooks in the same order every render.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> Element <span class=\"hljs-title\">Render</span>()</span>;\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Controls whether this propless component should re-render when its parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Default: false — propless components only re-render from their own state changes or context changes.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Override and return true to always re-render when the parent re-renders.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">protected</span> <span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">virtual</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">ShouldUpdate</span>()</span> => <span class=\"hljs-literal\">false</span>;\n</code></pre>\n<p><code>Component.Render()</code> returns an <code>Element</code> tree the reconciler walks;\nthe element records currently carry <code>Key</code>, <code>Modifiers</code>, <code>Attached</code>,\nevent handlers, and metadata — but no source location. The component\ntype name is the closest available attribution because the reconciler\nhas the <code>Component</code> instance in hand; everything finer would require\nthe element to carry the location, which is exactly what spec 010\nadds.</p>\n<h2 id=\"the-spec-010-design-at-a-glance\">The spec 010 design at a glance<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-spec-010-design-at-a-glance\">#</a></h2>\n<p>Spec 010 settles on two coordinated pieces:</p>\n<ol>\n<li><strong>CallerInfo on the DSL.</strong> Every factory method\n(<code>Text(string)</code>, <code>Button(...)</code>, <code>VStack(...)</code>) gains optional\ntrailing <code>[CallerFilePath]</code> / <code>[CallerLineNumber]</code> parameters. C#\nbakes the values into IL as constants, so call sites stay\n<code>Text(\"Hello\")</code> and the runtime cost is zero. A <code>SourceLocation</code>\nvalue rides on the element record (<code>Element.Source</code>).</li>\n<li><strong>Attached property on the WinUI control.</strong> During reconcile, the\nlocation string is written to a custom attached\n<code>DependencyProperty</code> on the realized control, where the XAML Live\nVisual Tree, the <code>--preview</code> inspector, and the\n<a href=\"/devtools-internals/\">devtools overlay</a> can read it.</li>\n</ol>\n<p>The result is a runtime where right-clicking a <code>Button</code> in the\npreview inspector navigates to the <code>Button(\"Save\", …)</code> call in C#,\nthe same way Flutter's widget inspector navigates to a <code>Widget</code>\nconstructor. Until that ships, attribution stops at the component\nname.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>For now, lean on the component name.</strong> Wrap chunks of UI in\npurpose-named components — <code>&#x3C;UserCard></code>, <code>&#x3C;RegisterForm></code>,\n<code>&#x3C;NotificationBadge></code> — and the ETW events will identify them.\nInline anonymous <code>Func</code>-component lambdas show up as\n<code>FuncElement</code> in traces, which is almost never what you want.</p>\n<p><strong><code>RenderError</code> redacts the message on purpose.</strong> TASK-064 strips\n<code>ex.Message</code> from the ETW payload because exception messages can\ncarry absolute paths, env values, and form values. Apps that want\nricher diagnostics should log through their own pipeline (ETL/disk\nunder their own ACL) rather than the <code>Microsoft-UI-Reactor</code> provider.</p>\n<p><strong>PerfView gives you the full sequence.</strong> <code>Microsoft-UI-Reactor</code> is a\nmanaged <code>EventSource</code>, so it surfaces on both EventPipe\n(<code>dotnet-trace</code>) and classic ETW (PerfView / xperf / WPA). When you\nneed to correlate Reactor renders with native WinUI events\n(<code>Microsoft-Windows-XAML</code>), only ETW carries both — EventPipe doesn't\nflow native providers.</p>\n<p><strong>Watch the design before designing around it.</strong> Spec 010 owns the\nper-element story and several Phase 4 surfaces (preview inspector,\nlayout-cost overlay, reconcile-highlight) wait on it. Don't fork a\nparallel attribution scheme; track the spec.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/devtools-internals/\">Devtools internals</a></strong> — Where the preview inspector will consume <code>SourceLocation</code> once it lands.</li>\n<li><strong><a href=\"/perf-instrumentation/\">Perf instrumentation</a></strong> — Same ETW pipeline, focused on the timing axis.</li>\n<li><strong><a href=\"/architecture-overview/\">Architecture overview</a></strong> — How the render-loop produces the events documented here.</li>\n<li><strong><a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/010-source-mapping-design.md\">Spec 010 — Source mapping design</a></strong> — The full design reference.</li>\n</ul>",
      "text": "Source Mapping # \"Source mapping\" in Microsoft.UI.Reactor (Reactor) is the chain that ties a runtime artifact — an ETW event, a --preview overlay highlight, a thrown exception — back to the C# source that produced it. Today, attribution is at the component granularity: every render emits an ETW event carrying the component's type name, and exceptions log with the same name plus the exception type. Spec 010 designs the next step — per- element source tagging via [CallerFilePath] / [CallerLineNumber] attributes on the DSL factories, plumbed onto realized WinUI controls through an attached DependencyProperty . This page covers what's emitted now and how the design layers on top of it. Status. Per-element source tagging ( Element.Source , SourceInfo.LocationProperty ) is designed but not implemented . The reference for the design is spec 010 . This page treats it as the next-step plan, not current behavior. Component-name attribution via ETW is shipping today and is documented below. Component-name attribution via ETW # Every ComponentRender boundary emits an ETW event keyed by the component's CLR type name. The keyword Render gates the events so consumers can subscribe to just the render channel: public static class Keywords { public const EventKeywords Reconcile = (EventKeywords) 0x1 ; public const EventKeywords Render = (EventKeywords) 0x2 ; public const EventKeywords State = (EventKeywords) 0x4 ; public const EventKeywords Mcp = (EventKeywords) 0x8 ; public const EventKeywords Lifecycle = (EventKeywords) 0x10 ; public const EventKeywords Errors = (EventKeywords) 0x20 ; public const EventKeywords EventDispatch = (EventKeywords) 0x40 ; // Spec 044 — subsystem coverage gaps. Each gets its own bit so a // consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can // pick exactly the area it cares about without paying for the rest. public const EventKeywords Hosting = (EventKeywords) 0x80 ; // Window/HWND/DPI/Backdrop public const EventKeywords Persistence = (EventKeywords) 0x100 ; // settings store, placement public const EventKeywords Navigation = (EventKeywords) 0x200 ; // route push, cache, transitions public const EventKeywords Intl = (EventKeywords) 0x400 ; // missing keys, fallback, format public const EventKeywords Theme = (EventKeywords) 0x800 ; // theme apply, bindings public const EventKeywords Shell = (EventKeywords) 0x1000 ; // JumpList/Tray/ThumbnailToolbar } ComponentRenderStart / ComponentRenderStop fire with componentName = node.Component?.GetType().Name . That string is the attribution token that flows into PerfView / dotnet-trace / xperf , and it is the same string devtools-internals uses to label overlay frames. Per-component, not per-element — but sufficient for the common question \"which component is re-rendering on every tick\". Reconcile-pass attribution # Signal Granularity Where it surfaces ComponentRenderStart / Stop Component CLR type name ETW Render keyword ReconcileStart / Stop Root element type + diff counters ETW Reconcile keyword EffectsFlushStart / Stop Component CLR type name ETW Render keyword StateChange Hook kind + value type ETW State keyword RenderError Component name + exception type only (message redacted) ETW Errors keyword Per-element file:line Designed (spec 010) — not shipping Future: Element.Source + SourceInfo.LocationProperty The reconcile pass also emits a counter summary on stop: [ Event(2, Level = EventLevel.Informational, Keywords = Keywords.Reconcile, Task = Tasks.Reconcile, Opcode = EventOpcode.Stop, Message = \"Reconcile stop (diffed={elementsDiffed}, skipped={elementsSkipped}, created={uiElementsCreated}, modified={uiElementsModified})\" ) ] public void ReconcileStop ( int elementsDiffed, int elementsSkipped, int uiElementsCreated, int uiElementsModified ) { if (IsEnabled(EventLevel.Informational, Keywords.Reconcile)) WriteEvent( 2 , elementsDiffed, elementsSkipped, uiElementsCreated, uiElementsModified); } elementsDiffed / elementsSkipped / uiElementsCreated / uiElementsModified give a frame-level view of how much actual work the reconciler did. None of these carry a source location — they're aggregate counters — but pairing them with the component start/stop events tells you \"this component rendered, the reconciler touched N elements, and Y of them resulted in real WinUI writes.\" Why ETW attribution stops at the component # The reconciler hand-rolls per-mount calls into MountXxx handlers, each of which constructs a fresh WinUI control. The handler doesn't know which user line called Text(\"hello\") : public abstract class Component { internal RenderContext Context { get ; } = new (); /// &#x3C;summary> /// Override to describe the UI. Use UseState, UseEffect, etc. from the context. /// Must call hooks in the same order every render. /// &#x3C;/summary> public abstract Element Render () ; /// &#x3C;summary> /// Controls whether this propless component should re-render when its parent re-renders. /// Default: false — propless components only re-render from their own state changes or context changes. /// Override and return true to always re-render when the parent re-renders. /// &#x3C;/summary> protected internal virtual bool ShouldUpdate () => false ; Component.Render() returns an Element tree the reconciler walks; the element records currently carry Key , Modifiers , Attached , event handlers, and metadata — but no source location. The component type name is the closest available attribution because the reconciler has the Component instance in hand; everything finer would require the element to carry the location, which is exactly what spec 010 adds. The spec 010 design at a glance # Spec 010 settles on two coordinated pieces: CallerInfo on the DSL. Every factory method ( Text(string) , Button(...) , VStack(...) ) gains optional trailing [CallerFilePath] / [CallerLineNumber] parameters. C# bakes the values into IL as constants, so call sites stay Text(\"Hello\") and the runtime cost is zero. A SourceLocation value rides on the element record ( Element.Source ). Attached property on the WinUI control. During reconcile, the location string is written to a custom attached DependencyProperty on the realized control, where the XAML Live Visual Tree, the --preview inspector, and the devtools overlay can read it. The result is a runtime where right-clicking a Button in the preview inspector navigates to the Button(\"Save\", …) call in C#, the same way Flutter's widget inspector navigates to a Widget constructor. Until that ships, attribution stops at the component name. Tips # For now, lean on the component name. Wrap chunks of UI in purpose-named components — &#x3C;UserCard> , &#x3C;RegisterForm> , &#x3C;NotificationBadge> — and the ETW events will identify them. Inline anonymous Func -component lambdas show up as FuncElement in traces, which is almost never what you want. RenderError redacts the message on purpose. TASK-064 strips ex.Message from the ETW payload because exception messages can carry absolute paths, env values, and form values. Apps that want richer diagnostics should log through their own pipeline (ETL/disk under their own ACL) rather than the Microsoft-UI-Reactor provider. PerfView gives you the full sequence. Microsoft-UI-Reactor is a managed EventSource , so it surfaces on both EventPipe ( dotnet-trace ) and classic ETW (PerfView / xperf / WPA). When you need to correlate Reactor renders with native WinUI events ( Microsoft-Windows-XAML ), only ETW carries both — EventPipe doesn't flow native providers. Watch the design before designing around it. Spec 010 owns the per-element story and several Phase 4 surfaces (preview inspector, layout-cost overlay, reconcile-highlight) wait on it. Don't fork a parallel attribution scheme; track the spec. Next Steps # Devtools internals — Where the preview inspector will consume SourceLocation once it lands. Perf instrumentation — Same ETW pipeline, focused on the timing axis. Architecture overview — How the render-loop produces the events documented here. Spec 010 — Source mapping design — The full design reference.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/source-mapping.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/source-mapping.md",
      "headings": [
        {
          "depth": 2,
          "id": "component-name-attribution-via-etw",
          "text": "Component-name attribution via ETW"
        },
        {
          "depth": 2,
          "id": "reconcile-pass-attribution",
          "text": "Reconcile-pass attribution"
        },
        {
          "depth": 2,
          "id": "why-etw-attribution-stops-at-the-component",
          "text": "Why ETW attribution stops at the component"
        },
        {
          "depth": 2,
          "id": "the-spec-010-design-at-a-glance",
          "text": "The spec 010 design at a glance"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Modifier System",
      "path": "modifier-system.md",
      "slug": "/modifier-system/",
      "html": "<p>A modifier in Microsoft.UI.Reactor (Reactor) is not an operation — it is a value-bearing\nfunction <code>T -> T</code> that returns a new <a href=\"/components/\"><code>Element</code></a> record\nwith its <code>Modifiers</code> slot replaced. <code>.Bold().Margin(8).Background(Theme.Accent)</code>\nallocates one <code>TextBlockElement</code> and folds three small <code>ElementModifiers</code>\nrecords into it via <code>ElementModifiers.Merge</code>, then the reconciler reads\nthe merged record at commit time and writes WinUI properties in a single\nsweep. The trade-off is that you do not get the SwiftUI / Compose\n\"each modifier wraps the previous one\" semantics — most Reactor\nmodifiers are commutative because they live in a flat record, not on a\nstack — and the cases where order <em>does</em> matter (paint stacking,\nevent-handler null-clears, <code>.Set</code> callbacks that read other modifiers)\nare exactly the cases where the modifier writes to a property that\nalready carries semantics on its own. The most common mistake is\nexpecting <code>.Padding().Background()</code> to paint differently than\n<code>.Background().Padding()</code>; in Reactor it doesn't, because both end up\nin the same merged <code>Padding</code> / <code>Background</code> slots.</p>\n<h1 id=\"modifier-system\">Modifier System<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#modifier-system\">#</a></h1>\n<p>This page is the runtime view of the same fluent chain\n<a href=\"/styling/\">Styling</a> introduces from the surface. Both ride on top of\n<code>ElementExtensions</code> in <code>src/Reactor/Elements/</code>; the difference is which\nhalf of the call you care about — the API or the fold.</p>\n<h2 id=\"modifiers-are-flat-records-not-nested-wrappers\">Modifiers are flat records, not nested wrappers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#modifiers-are-flat-records-not-nested-wrappers\">#</a></h2>\n<p>A modifier extension method does exactly one thing: it produces a new\n<code>Element</code> whose <code>Modifiers</code> slot is the merge of the old one with a\nsingle-field <code>ElementModifiers</code> carrying the new value. The two\nhelpers that do all the work are <code>Modify</code> and <code>ModifyTheme</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Modify</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, ElementModifiers mods</span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    el <span class=\"hljs-keyword\">with</span> { Modifiers = el.Modifiers <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? el.Modifiers.Merge(mods) : mods };\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyA11y</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, AccessibilityModifiers a11y</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> existing = el.Modifiers?.Accessibility;\n    <span class=\"hljs-keyword\">var</span> merged = existing <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? existing.Merge(a11y) : a11y;\n    <span class=\"hljs-keyword\">return</span> Modify(el, <span class=\"hljs-keyword\">new</span> ElementModifiers { Accessibility = merged });\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyTheme</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, <span class=\"hljs-built_in\">string</span> property, ThemeRef theme</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> bindings = el.ThemeBindings <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>\n        ? <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef>(el.ThemeBindings) { [property] = theme }\n        : <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef> { [property] = theme };\n    <span class=\"hljs-keyword\">return</span> el <span class=\"hljs-keyword\">with</span> { ThemeBindings = bindings };\n}\n</code></pre>\n<p><code>Modify&#x3C;T>(el, mods)</code> is a <code>record with</code> expression on the concrete\nelement type — <code>T : Element</code>. That generic constraint is what lets\nthe call chain stay typed as <code>TextBlockElement</code> all the way through\n<code>.Bold().Margin(8).FontSize(24)</code>. SwiftUI returns\n<code>some View</code> and Compose returns <code>Modifier</code>; Reactor keeps the concrete\ntype so <code>.Set(tb => tb.TextWrapping = TextWrapping.Wrap)</code> two modifiers\nlater still binds to a <code>TextBlockElement</code> action — the WinUI control\nyou get inside the callback is a typed <code>TextBlock</code>, not a base\n<code>UIElement</code>.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Where the value lands</th><th>Slot on <code>Element</code></th><th>Set via</th></tr></thead><tbody><tr><td>Layout (margin, padding, size, alignment)</td><td><code>Modifiers.Layout</code></td><td><code>.Margin</code>, <code>.Padding</code>, <code>.Width</code>, <code>.HAlign</code>, etc.</td></tr><tr><td>Visual (font, foreground, opacity)</td><td><code>Modifiers.Visual</code></td><td><code>.FontSize</code>, <code>.Foreground</code>, <code>.Opacity</code>, etc.</td></tr><tr><td>Theme-bound brushes</td><td><code>ThemeBindings[propertyName]</code></td><td><code>.Background(Theme.Accent)</code>, <code>.Foreground(Theme.Ref(...))</code></td></tr><tr><td>Container-attached values</td><td><code>Attached[typeof(TAttached)]</code></td><td><code>.Grid(row: 1)</code>, <code>.Flex(grow: 1)</code>, <code>.Canvas(left: 8)</code></td></tr><tr><td>Event callbacks</td><td>typed <code>OnXxx</code> property on the leaf record</td><td><code>.Click(handler)</code>, <code>.TextChanged(handler)</code></td></tr><tr><td>Imperative escape hatch</td><td>element callback list</td><td><code>.Set(control => …)</code></td></tr></tbody></table>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/modifier-system/modifier-fold.svg\" alt=\"Modifier fold: a Text element gets .Bold, .Margin, .Background, .Grid, .Set in sequence; each call merges into one element with Modifiers / ThemeBindings / Attached slots populated, then the reconciler reads them on commit.\"></p>\n<p>The slots are independent — adding a <code>.Margin</code> after a <code>.Background</code>\ndoesn't displace the <code>.Background</code>, it just merges into the same\nrecord. That is why \"modifier order doesn't matter\" is true for almost\nevery Reactor modifier: the merge target is a field on a flat record,\nnot a position in a wrapper chain.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Modify</code> calls <code>el.Modifiers.Merge(mods)</code> with the <em>new</em> modifier as\nthe right-hand side. <code>Merge</code>'s rule for scalar properties is\n\"<code>other ?? self</code>\" — the later call wins when both set the same slot.\n<code>.FontSize(12).FontSize(16)</code> keeps 16, not 12, because the second call\nwent on top. This matters for <a href=\"/theming-tokens/\">theming</a>: a\n<code>.Background(\"#FF0000\")</code> literal placed <em>after</em> a\n<code>.Background(Theme.Accent)</code> overrides the theme binding entirely and\nsilently breaks dark-mode color swaps. The\n<a href=\"/rules-of-reactor/\"><code>REACTOR_THEME_001</code></a> analyzer flags the literal\nform so the override never ships.</p>\n</blockquote>\n<h2 id=\"attached-values-live-in-a-separate-slot\">Attached values live in a separate slot<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#attached-values-live-in-a-separate-slot\">#</a></h2>\n<p>Container-attached values (<code>Grid.Row</code>, <code>Canvas.Left</code>, <code>FlexPanel.Grow</code>)\ndon't go into <code>Modifiers</code> because they're metadata for a <em>parent</em>, not\nthe element itself. They ride in <code>Element.Attached</code> — a\n<code>Dictionary&#x3C;Type, object></code> keyed by the attached-data record:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Sets Grid attached properties (row, column, spans) on this element.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Only meaningful when the element is a child of a Grid.</span>\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Grid</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> T el, <span class=\"hljs-built_in\">int</span> row = <span class=\"hljs-number\">0</span>, <span class=\"hljs-built_in\">int</span> column = <span class=\"hljs-number\">0</span>, <span class=\"hljs-built_in\">int</span> rowSpan = <span class=\"hljs-number\">1</span>, <span class=\"hljs-built_in\">int</span> columnSpan = <span class=\"hljs-number\">1</span></span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    (T)el.SetAttached(<span class=\"hljs-keyword\">new</span> GridAttached(row, column, rowSpan, columnSpan));\n</code></pre>\n<p><code>.Grid(row: 1, column: 2)</code> creates a <code>GridAttached(1, 2, 1, 1)</code> record\nand calls <code>el.SetAttached(...)</code>. The dictionary is keyed by\n<code>typeof(GridAttached)</code>, so a later <code>.Grid(row: 3)</code> replaces the prior\nentry (not merges — attached records are atomic). A Reactor <code>Grid</code>\nasks each child element for its <code>GetAttached&#x3C;GridAttached>()</code> during\nmount, then writes <code>Grid.SetRow</code> / <code>Grid.SetColumn</code> on the WinUI\nchild. The same pattern wraps <code>Flex</code>:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Flex</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> T el,\n    <span class=\"hljs-built_in\">double</span> grow = <span class=\"hljs-number\">0</span>,\n    <span class=\"hljs-built_in\">double</span> shrink = <span class=\"hljs-number\">1</span>,\n    <span class=\"hljs-built_in\">double</span>? basis = <span class=\"hljs-literal\">null</span>,\n    <span class=\"hljs-built_in\">double</span>? minWidth = <span class=\"hljs-literal\">null</span>,\n    <span class=\"hljs-built_in\">double</span>? minHeight = <span class=\"hljs-literal\">null</span>,\n    FlexAlign? alignSelf = <span class=\"hljs-literal\">null</span>,\n    FlexPositionType position = FlexPositionType.Relative,\n    <span class=\"hljs-built_in\">double</span>? left = <span class=\"hljs-literal\">null</span>,\n    <span class=\"hljs-built_in\">double</span>? top = <span class=\"hljs-literal\">null</span>,\n    <span class=\"hljs-built_in\">double</span>? right = <span class=\"hljs-literal\">null</span>,\n    <span class=\"hljs-built_in\">double</span>? bottom = <span class=\"hljs-literal\">null</span>\n</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">if</span> (grow &#x3C; <span class=\"hljs-number\">0</span> || <span class=\"hljs-built_in\">double</span>.IsNaN(grow) || <span class=\"hljs-built_in\">double</span>.IsInfinity(grow))\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentOutOfRangeException(<span class=\"hljs-keyword\">nameof</span>(grow), <span class=\"hljs-string\">\"Grow must be a non-negative, finite value.\"</span>);\n\n    <span class=\"hljs-keyword\">if</span> (shrink &#x3C; <span class=\"hljs-number\">0</span> || <span class=\"hljs-built_in\">double</span>.IsNaN(shrink) || <span class=\"hljs-built_in\">double</span>.IsInfinity(shrink))\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentOutOfRangeException(<span class=\"hljs-keyword\">nameof</span>(shrink), <span class=\"hljs-string\">\"Shrink must be a non-negative, finite value.\"</span>);\n\n    <span class=\"hljs-keyword\">if</span> (minWidth <span class=\"hljs-keyword\">is</span> { } mw &#x26;&#x26; (mw &#x3C; <span class=\"hljs-number\">0</span> || <span class=\"hljs-built_in\">double</span>.IsNaN(mw) || <span class=\"hljs-built_in\">double</span>.IsInfinity(mw)))\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentOutOfRangeException(<span class=\"hljs-keyword\">nameof</span>(minWidth), <span class=\"hljs-string\">\"MinWidth must be a non-negative, finite value (or null for CSS `min-width: auto`).\"</span>);\n\n    <span class=\"hljs-keyword\">if</span> (minHeight <span class=\"hljs-keyword\">is</span> { } mh &#x26;&#x26; (mh &#x3C; <span class=\"hljs-number\">0</span> || <span class=\"hljs-built_in\">double</span>.IsNaN(mh) || <span class=\"hljs-built_in\">double</span>.IsInfinity(mh)))\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentOutOfRangeException(<span class=\"hljs-keyword\">nameof</span>(minHeight), <span class=\"hljs-string\">\"MinHeight must be a non-negative, finite value (or null for CSS `min-height: auto`).\"</span>);\n\n    <span class=\"hljs-keyword\">return</span> (T)el.SetAttached(<span class=\"hljs-keyword\">new</span> FlexAttached(grow, shrink, basis, minWidth, minHeight, alignSelf, position, left, top, right, bottom));\n}\n</code></pre>\n<p>Flex distinguishes itself with input validation — <code>grow</code> and <code>shrink</code>\nhave to be non-negative and finite — but otherwise lives in the same\n<code>Attached</code> dictionary, keyed by <code>typeof(FlexAttached)</code>. Reading two\nattached entries from the same child is the parent's call; a <code>Grid</code>\nignores <code>FlexAttached</code>, a <a href=\"/flex-layout/\"><code>FlexPanel</code></a> ignores\n<code>GridAttached</code>. Mixing them on one element is harmless but pointless —\nonly the parent's matching kind gets read.</p>\n<h2 id=\"event-callback-modifiers-are-typed-with-updates\">Event-callback modifiers are typed <code>with</code> updates<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#event-callback-modifiers-are-typed-with-updates\">#</a></h2>\n<p>Event handlers don't ride in <code>ElementModifiers</code> either. They are\ntyped properties on the concrete leaf record (<code>ButtonElement.OnClick</code>,\n<code>TextBoxElement.OnChanged</code>), and the <code>.Click(...)</code> /\n<code>.Changed(...)</code> extensions are thin <code>record with { }</code> updates:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Wires a click handler (sets <span class=\"hljs-doctag\">&#x3C;see cref=\"ButtonElement.OnClick\"/></span>). Passing <span class=\"hljs-doctag\">&#x3C;c></span>null<span class=\"hljs-doctag\">&#x3C;/c></span> clears any existing handler.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> ButtonElement <span class=\"hljs-title\">Click</span>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> ButtonElement el, Action? handler</span>)</span> =>\n    el <span class=\"hljs-keyword\">with</span> { OnClick = handler };\n\n<span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Wires a click handler (sets <span class=\"hljs-doctag\">&#x3C;see cref=\"HyperlinkButtonElement.OnClick\"/></span>). Passing <span class=\"hljs-doctag\">&#x3C;c></span>null<span class=\"hljs-doctag\">&#x3C;/c></span> clears.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> HyperlinkButtonElement <span class=\"hljs-title\">Click</span>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> HyperlinkButtonElement el, Action? handler</span>)</span> =>\n    el <span class=\"hljs-keyword\">with</span> { OnClick = handler };\n</code></pre>\n<p>Two reasons the design split this off from generic modifiers. First,\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-fluent-api-uplift.md\"><code>C# binds </code>el.OnClick(arg)<code> to delegate invocation before considering extension methods</code></a>, so a <code>.OnClick</code>\nextension method would be permanently unreachable — the WinUI XAML\nconvention (<code>Click=\"…\"</code>, drop the <code>On</code> prefix) avoids the clash.\nSecond, callback identity participates in\n<a href=\"/reconciliation/\">reconciliation</a> — <code>Element.HasCallbacks</code> is the\nsignal the reconciler uses to decide whether to refresh the event\ntrampoline's <code>Tag</code> after a skip fast-path. Storing callbacks in typed\nfields keeps that check cheap (one <code>is not null</code> per relevant slot)\ninstead of forcing the reconciler to walk a delegate dictionary.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"authoring-a-custom-modifier\">Authoring a custom modifier<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#authoring-a-custom-modifier\">#</a></h3>\n<p>If you find yourself writing <code>.Padding(8).WithBorder(\"#DDD\").Background(Theme.CardBackground)</code>\non every card, hoist it into a single extension that calls the existing\nmodifiers in order:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Card</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> T el</span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    el.Padding(<span class=\"hljs-number\">8</span>).WithBorder(<span class=\"hljs-string\">\"#DDD\"</span>).Background(Theme.CardBackground);\n</code></pre>\n<p>The generic constraint <code>T : Element</code> is the only ceremony — every\npiece of the chain preserves the concrete type, so the result still\nbinds to whatever record the caller passed in. There is no\n<code>CardModifier</code> record to define, no <code>ModifierBuilder</code>, no registration\nstep. The chain <em>is</em> the modifier.</p>\n<p>When you need a modifier that writes to a new slot (e.g., a custom\nattached property a custom container reads), the pattern is the same\ntwo helpers <code>Grid</code> and <code>Flex</code> use: define an attached-data record,\nexpose an extension that calls <code>el.SetAttached(...)</code>, and have the\nparent panel read it via <code>GetAttached&#x3C;TAttached>()</code> in its mount /\nupdate code.</p>\n<h3 id=\"reading-the-merged-modifier-in-a-custom-panel\">Reading the merged modifier in a custom panel<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reading-the-merged-modifier-in-a-custom-panel\">#</a></h3>\n<p>A custom layout panel that needs to know the user's\n<code>Modifiers.Layout.Margin</code> reads it off the element directly — the\nreconciler has already folded the chain by the time the panel's\nmount handler runs:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Element</span>\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Optional key for stable identity across re-renders (like React's key prop).</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> When set, the reconciler uses it to match elements across list reorderings.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">string</span>? Key { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span></span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Layout modifiers (margin, padding, size, alignment, etc.) applied to this element.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200)</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> Modifiers are stored inline so the concrete element type is preserved through chaining.</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> ElementModifiers? Modifiers { <span class=\"hljs-keyword\">get</span>; <span class=\"hljs-keyword\">init</span>; }\n</code></pre>\n<p>The record exposes <code>Modifiers</code> as a single nullable slot; if null, no\nmodifier touched the element; if non-null, every modifier the chain\napplied has been merged into it. There is no traversal cost — one\nfield read tells you everything the user said about the element's\nappearance.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"expecting-padding-and-background-to-stack-like-swiftui\">Expecting <code>Padding</code> and <code>Background</code> to stack like SwiftUI<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#expecting-padding-and-background-to-stack-like-swiftui\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't expect a red stripe inside the padding:</span>\nText(<span class=\"hljs-string\">\"hi\"</span>).Padding(<span class=\"hljs-number\">10</span>).Background(<span class=\"hljs-string\">\"#FF0000\"</span>);\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Modify</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, ElementModifiers mods</span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    el <span class=\"hljs-keyword\">with</span> { Modifiers = el.Modifiers <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? el.Modifiers.Merge(mods) : mods };\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyA11y</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, AccessibilityModifiers a11y</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> existing = el.Modifiers?.Accessibility;\n    <span class=\"hljs-keyword\">var</span> merged = existing <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? existing.Merge(a11y) : a11y;\n    <span class=\"hljs-keyword\">return</span> Modify(el, <span class=\"hljs-keyword\">new</span> ElementModifiers { Accessibility = merged });\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyTheme</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, <span class=\"hljs-built_in\">string</span> property, ThemeRef theme</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> bindings = el.ThemeBindings <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>\n        ? <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef>(el.ThemeBindings) { [property] = theme }\n        : <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef> { [property] = theme };\n    <span class=\"hljs-keyword\">return</span> el <span class=\"hljs-keyword\">with</span> { ThemeBindings = bindings };\n}\n</code></pre>\n<p>In SwiftUI, calling <code>.padding().background(Color.red)</code> paints the\nbackground <em>outside</em> the padding; reversing the order paints it\ninside. In Reactor, both calls land in the same <code>Modifiers.Layout.Padding</code>\nand <code>Modifiers.Visual.Background</code> slots, and the WinUI control gets\none padding and one background — the order doesn't change the\ngeometry. If you want the SwiftUI semantics, wrap the inner element in\na <a href=\"/styling/\"><code>Border</code></a> explicitly: <code>Border(Text(\"hi\").Padding(10)).Background(\"#FF0000\")</code>\ngives you the outer stripe; <code>Border(Text(\"hi\")).Padding(10).Background(\"#FF0000\")</code>\ngives you the inner.</p>\n<h3 id=\"re-setting-the-same-property-and-expecting-an-additive-effect\">Re-setting the same property and expecting an additive effect<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#re-setting-the-same-property-and-expecting-an-additive-effect\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nText(<span class=\"hljs-string\">\"hi\"</span>).Margin(<span class=\"hljs-number\">8</span>).Margin(left: <span class=\"hljs-number\">16</span>);   <span class=\"hljs-comment\">// last wins — margin is (16,8,8,8)? NO. it's (16,0,0,0)</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">Modify</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, ElementModifiers mods</span>) <span class=\"hljs-keyword\">where</span> T : Element</span> =>\n    el <span class=\"hljs-keyword\">with</span> { Modifiers = el.Modifiers <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? el.Modifiers.Merge(mods) : mods };\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyA11y</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, AccessibilityModifiers a11y</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> existing = el.Modifiers?.Accessibility;\n    <span class=\"hljs-keyword\">var</span> merged = existing <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span> ? existing.Merge(a11y) : a11y;\n    <span class=\"hljs-keyword\">return</span> Modify(el, <span class=\"hljs-keyword\">new</span> ElementModifiers { Accessibility = merged });\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> T <span class=\"hljs-title\">ModifyTheme</span>&#x3C;<span class=\"hljs-title\">T</span>>(<span class=\"hljs-params\">T el, <span class=\"hljs-built_in\">string</span> property, ThemeRef theme</span>) <span class=\"hljs-keyword\">where</span> T : Element</span>\n{\n    <span class=\"hljs-keyword\">var</span> bindings = el.ThemeBindings <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>\n        ? <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef>(el.ThemeBindings) { [property] = theme }\n        : <span class=\"hljs-keyword\">new</span> Dictionary&#x3C;<span class=\"hljs-built_in\">string</span>, ThemeRef> { [property] = theme };\n    <span class=\"hljs-keyword\">return</span> el <span class=\"hljs-keyword\">with</span> { ThemeBindings = bindings };\n}\n</code></pre>\n<p>The second <code>.Margin(left: 16)</code> builds a fresh <code>ElementModifiers</code> with\n<code>Margin = new Thickness(16, 0, 0, 0)</code> and hands it to <code>Merge</code>. <code>Merge</code>\ntreats the new <code>Margin</code> value as a single atomic field — there's no\nfield-level merge inside <code>Thickness</code>. The result is <code>(16, 0, 0, 0)</code>,\nnot <code>(16, 8, 8, 8)</code>. Either call the per-side overload once\n(<code>.Margin(left: 16, top: 8, right: 8, bottom: 8)</code>) or stick to the\nuniform shape if all sides are equal.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Generic constraint preserves type — chain freely.</strong> <code>.Bold()</code> returns\n<code>TextBlockElement</code>, <code>.Margin()</code> returns <code>TextBlockElement</code>, every\nmodifier preserves the type. <code>.Set(tb => ...)</code> at any point in the\nchain still gets the strongly-typed <code>TextBlock</code> callback.</p>\n<p><strong>Merge is \"later wins\" per slot.</strong> Repeating the same modifier\noverrides; it doesn't accumulate. The exception is event-callback\nparity, which the <a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/039-fluent-api-uplift.md\">Fluent API spec §15 Q2</a>\ndocuments — passing <code>null</code> to a <code>.Click(null)</code> clears the handler\nrather than queuing a no-op.</p>\n<p><strong>Attached values are atomic.</strong> <code>.Grid(row: 1).Grid(column: 2)</code>\nreplaces the first <code>GridAttached</code> with a fresh one — the column wins\nand the row resets to zero. Set all attached fields in one call:\n<code>.Grid(row: 1, column: 2)</code>.</p>\n<p><strong>Reach for <code>.Set</code> only when nothing else fits.</strong> If a modifier exists\n(<code>.Bold</code>, <code>.FontSize</code>, <code>.Foreground</code>), use it — the reconciler can\nbatch property writes for known modifiers and skip them under\n<a href=\"/reconciliation/\"><code>CanSkipUpdate</code></a>. <code>.Set</code> always runs.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/styling/\">Styling</a></strong> — Previous: the surface API for the chain documented here.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Next: where the merged modifier actually reaches WinUI.</li>\n<li><strong><a href=\"/theming-tokens/\">Theming tokens</a></strong> — <code>ThemeBindings</code> companion slot for brush properties.</li>\n<li><strong><a href=\"/flex-layout/\">Flex layout</a></strong> — Worked example of an attached-data record.</li>\n<li><strong><a href=\"/element-pool/\">Element pool</a></strong> — Why modifier cleanup matters when a control is recycled.</li>\n</ul>",
      "text": "A modifier in Microsoft.UI.Reactor (Reactor) is not an operation — it is a value-bearing function T -> T that returns a new Element record with its Modifiers slot replaced. .Bold().Margin(8).Background(Theme.Accent) allocates one TextBlockElement and folds three small ElementModifiers records into it via ElementModifiers.Merge , then the reconciler reads the merged record at commit time and writes WinUI properties in a single sweep. The trade-off is that you do not get the SwiftUI / Compose \"each modifier wraps the previous one\" semantics — most Reactor modifiers are commutative because they live in a flat record, not on a stack — and the cases where order does matter (paint stacking, event-handler null-clears, .Set callbacks that read other modifiers) are exactly the cases where the modifier writes to a property that already carries semantics on its own. The most common mistake is expecting .Padding().Background() to paint differently than .Background().Padding() ; in Reactor it doesn't, because both end up in the same merged Padding / Background slots. Modifier System # This page is the runtime view of the same fluent chain Styling introduces from the surface. Both ride on top of ElementExtensions in src/Reactor/Elements/ ; the difference is which half of the call you care about — the API or the fold. Modifiers are flat records, not nested wrappers # A modifier extension method does exactly one thing: it produces a new Element whose Modifiers slot is the merge of the old one with a single-field ElementModifiers carrying the new value. The two helpers that do all the work are Modify and ModifyTheme : private static T Modify &#x3C; T >( T el, ElementModifiers mods ) where T : Element => el with { Modifiers = el.Modifiers is not null ? el.Modifiers.Merge(mods) : mods }; private static T ModifyA11y &#x3C; T >( T el, AccessibilityModifiers a11y ) where T : Element { var existing = el.Modifiers?.Accessibility; var merged = existing is not null ? existing.Merge(a11y) : a11y; return Modify(el, new ElementModifiers { Accessibility = merged }); } private static T ModifyTheme &#x3C; T >( T el, string property, ThemeRef theme ) where T : Element { var bindings = el.ThemeBindings is not null ? new Dictionary&#x3C; string , ThemeRef>(el.ThemeBindings) { [property] = theme } : new Dictionary&#x3C; string , ThemeRef> { [property] = theme }; return el with { ThemeBindings = bindings }; } Modify&#x3C;T>(el, mods) is a record with expression on the concrete element type — T : Element . That generic constraint is what lets the call chain stay typed as TextBlockElement all the way through .Bold().Margin(8).FontSize(24) . SwiftUI returns some View and Compose returns Modifier ; Reactor keeps the concrete type so .Set(tb => tb.TextWrapping = TextWrapping.Wrap) two modifiers later still binds to a TextBlockElement action — the WinUI control you get inside the callback is a typed TextBlock , not a base UIElement . Where the value lands Slot on Element Set via Layout (margin, padding, size, alignment) Modifiers.Layout .Margin , .Padding , .Width , .HAlign , etc. Visual (font, foreground, opacity) Modifiers.Visual .FontSize , .Foreground , .Opacity , etc. Theme-bound brushes ThemeBindings[propertyName] .Background(Theme.Accent) , .Foreground(Theme.Ref(...)) Container-attached values Attached[typeof(TAttached)] .Grid(row: 1) , .Flex(grow: 1) , .Canvas(left: 8) Event callbacks typed OnXxx property on the leaf record .Click(handler) , .TextChanged(handler) Imperative escape hatch element callback list .Set(control => …) The slots are independent — adding a .Margin after a .Background doesn't displace the .Background , it just merges into the same record. That is why \"modifier order doesn't matter\" is true for almost every Reactor modifier: the merge target is a field on a flat record, not a position in a wrapper chain. Caveat: Modify calls el.Modifiers.Merge(mods) with the new modifier as the right-hand side. Merge 's rule for scalar properties is \" other ?? self \" — the later call wins when both set the same slot. .FontSize(12).FontSize(16) keeps 16, not 12, because the second call went on top. This matters for theming : a .Background(\"#FF0000\") literal placed after a .Background(Theme.Accent) overrides the theme binding entirely and silently breaks dark-mode color swaps. The REACTOR_THEME_001 analyzer flags the literal form so the override never ships. Attached values live in a separate slot # Container-attached values ( Grid.Row , Canvas.Left , FlexPanel.Grow ) don't go into Modifiers because they're metadata for a parent , not the element itself. They ride in Element.Attached — a Dictionary&#x3C;Type, object> keyed by the attached-data record: /// &#x3C;summary> /// Sets Grid attached properties (row, column, spans) on this element. /// Only meaningful when the element is a child of a Grid. /// &#x3C;/summary> public static T Grid &#x3C; T >( this T el, int row = 0 , int column = 0 , int rowSpan = 1 , int columnSpan = 1 ) where T : Element => (T)el.SetAttached( new GridAttached(row, column, rowSpan, columnSpan)); .Grid(row: 1, column: 2) creates a GridAttached(1, 2, 1, 1) record and calls el.SetAttached(...) . The dictionary is keyed by typeof(GridAttached) , so a later .Grid(row: 3) replaces the prior entry (not merges — attached records are atomic). A Reactor Grid asks each child element for its GetAttached&#x3C;GridAttached>() during mount, then writes Grid.SetRow / Grid.SetColumn on the WinUI child. The same pattern wraps Flex : public static T Flex &#x3C; T >( this T el, double grow = 0 , double shrink = 1 , double ? basis = null , double ? minWidth = null , double ? minHeight = null , FlexAlign? alignSelf = null , FlexPositionType position = FlexPositionType.Relative, double ? left = null , double ? top = null , double ? right = null , double ? bottom = null ) where T : Element { if (grow &#x3C; 0 || double .IsNaN(grow) || double .IsInfinity(grow)) throw new ArgumentOutOfRangeException( nameof (grow), \"Grow must be a non-negative, finite value.\" ); if (shrink &#x3C; 0 || double .IsNaN(shrink) || double .IsInfinity(shrink)) throw new ArgumentOutOfRangeException( nameof (shrink), \"Shrink must be a non-negative, finite value.\" ); if (minWidth is { } mw &#x26;&#x26; (mw &#x3C; 0 || double .IsNaN(mw) || double .IsInfinity(mw))) throw new ArgumentOutOfRangeException( nameof (minWidth), \"MinWidth must be a non-negative, finite value (or null for CSS `min-width: auto`).\" ); if (minHeight is { } mh &#x26;&#x26; (mh &#x3C; 0 || double .IsNaN(mh) || double .IsInfinity(mh))) throw new ArgumentOutOfRangeException( nameof (minHeight), \"MinHeight must be a non-negative, finite value (or null for CSS `min-height: auto`).\" ); return (T)el.SetAttached( new FlexAttached(grow, shrink, basis, minWidth, minHeight, alignSelf, position, left, top, right, bottom)); } Flex distinguishes itself with input validation — grow and shrink have to be non-negative and finite — but otherwise lives in the same Attached dictionary, keyed by typeof(FlexAttached) . Reading two attached entries from the same child is the parent's call; a Grid ignores FlexAttached , a FlexPanel ignores GridAttached . Mixing them on one element is harmless but pointless — only the parent's matching kind gets read. Event-callback modifiers are typed with updates # Event handlers don't ride in ElementModifiers either. They are typed properties on the concrete leaf record ( ButtonElement.OnClick , TextBoxElement.OnChanged ), and the .Click(...) / .Changed(...) extensions are thin record with { } updates: /// &#x3C;summary> Wires a click handler (sets &#x3C;see cref=\"ButtonElement.OnClick\"/> ). Passing &#x3C;c> null &#x3C;/c> clears any existing handler. &#x3C;/summary> public static ButtonElement Click ( this ButtonElement el, Action? handler ) => el with { OnClick = handler }; /// &#x3C;summary> Wires a click handler (sets &#x3C;see cref=\"HyperlinkButtonElement.OnClick\"/> ). Passing &#x3C;c> null &#x3C;/c> clears. &#x3C;/summary> public static HyperlinkButtonElement Click ( this HyperlinkButtonElement el, Action? handler ) => el with { OnClick = handler }; Two reasons the design split this off from generic modifiers. First, C# binds el.OnClick(arg) to delegate invocation before considering extension methods , so a .OnClick extension method would be permanently unreachable — the WinUI XAML convention ( Click=\"…\" , drop the On prefix) avoids the clash. Second, callback identity participates in reconciliation — Element.HasCallbacks is the signal the reconciler uses to decide whether to refresh the event trampoline's Tag after a skip fast-path. Storing callbacks in typed fields keeps that check cheap (one is not null per relevant slot) instead of forcing the reconciler to walk a delegate dictionary. Patterns # Authoring a custom modifier # If you find yourself writing .Padding(8).WithBorder(\"#DDD\").Background(Theme.CardBackground) on every card, hoist it into a single extension that calls the existing modifiers in order: public static T Card &#x3C; T >( this T el ) where T : Element => el.Padding( 8 ).WithBorder( \"#DDD\" ).Background(Theme.CardBackground); The generic constraint T : Element is the only ceremony — every piece of the chain preserves the concrete type, so the result still binds to whatever record the caller passed in. There is no CardModifier record to define, no ModifierBuilder , no registration step. The chain is the modifier. When you need a modifier that writes to a new slot (e.g., a custom attached property a custom container reads), the pattern is the same two helpers Grid and Flex use: define an attached-data record, expose an extension that calls el.SetAttached(...) , and have the parent panel read it via GetAttached&#x3C;TAttached>() in its mount / update code. Reading the merged modifier in a custom panel # A custom layout panel that needs to know the user's Modifiers.Layout.Margin reads it off the element directly — the reconciler has already folded the chain by the time the panel's mount handler runs: public abstract record Element { /// &#x3C;summary> /// Optional key for stable identity across re-renders (like React's key prop). /// When set, the reconciler uses it to match elements across list reorderings. /// &#x3C;/summary> public string ? Key { get ; init ; } /// &#x3C;summary> /// Layout modifiers (margin, padding, size, alignment, etc.) applied to this element. /// Set via fluent extension methods: Text(\"hi\").Margin(10).Width(200) /// Modifiers are stored inline so the concrete element type is preserved through chaining. /// &#x3C;/summary> public ElementModifiers? Modifiers { get ; init ; } The record exposes Modifiers as a single nullable slot; if null, no modifier touched the element; if non-null, every modifier the chain applied has been merged into it. There is no traversal cost — one field read tells you everything the user said about the element's appearance. Common Mistakes # Expecting Padding and Background to stack like SwiftUI # // Don't expect a red stripe inside the padding: Text( \"hi\" ).Padding( 10 ).Background( \"#FF0000\" ); private static T Modify &#x3C; T >( T el, ElementModifiers mods ) where T : Element => el with { Modifiers = el.Modifiers is not null ? el.Modifiers.Merge(mods) : mods }; private static T ModifyA11y &#x3C; T >( T el, AccessibilityModifiers a11y ) where T : Element { var existing = el.Modifiers?.Accessibility; var merged = existing is not null ? existing.Merge(a11y) : a11y; return Modify(el, new ElementModifiers { Accessibility = merged }); } private static T ModifyTheme &#x3C; T >( T el, string property, ThemeRef theme ) where T : Element { var bindings = el.ThemeBindings is not null ? new Dictionary&#x3C; string , ThemeRef>(el.ThemeBindings) { [property] = theme } : new Dictionary&#x3C; string , ThemeRef> { [property] = theme }; return el with { ThemeBindings = bindings }; } In SwiftUI, calling .padding().background(Color.red) paints the background outside the padding; reversing the order paints it inside. In Reactor, both calls land in the same Modifiers.Layout.Padding and Modifiers.Visual.Background slots, and the WinUI control gets one padding and one background — the order doesn't change the geometry. If you want the SwiftUI semantics, wrap the inner element in a Border explicitly: Border(Text(\"hi\").Padding(10)).Background(\"#FF0000\") gives you the outer stripe; Border(Text(\"hi\")).Padding(10).Background(\"#FF0000\") gives you the inner. Re-setting the same property and expecting an additive effect # // Don't: Text( \"hi\" ).Margin( 8 ).Margin(left: 16 ); // last wins — margin is (16,8,8,8)? NO. it's (16,0,0,0) private static T Modify &#x3C; T >( T el, ElementModifiers mods ) where T : Element => el with { Modifiers = el.Modifiers is not null ? el.Modifiers.Merge(mods) : mods }; private static T ModifyA11y &#x3C; T >( T el, AccessibilityModifiers a11y ) where T : Element { var existing = el.Modifiers?.Accessibility; var merged = existing is not null ? existing.Merge(a11y) : a11y; return Modify(el, new ElementModifiers { Accessibility = merged }); } private static T ModifyTheme &#x3C; T >( T el, string property, ThemeRef theme ) where T : Element { var bindings = el.ThemeBindings is not null ? new Dictionary&#x3C; string , ThemeRef>(el.ThemeBindings) { [property] = theme } : new Dictionary&#x3C; string , ThemeRef> { [property] = theme }; return el with { ThemeBindings = bindings }; } The second .Margin(left: 16) builds a fresh ElementModifiers with Margin = new Thickness(16, 0, 0, 0) and hands it to Merge . Merge treats the new Margin value as a single atomic field — there's no field-level merge inside Thickness . The result is (16, 0, 0, 0) , not (16, 8, 8, 8) . Either call the per-side overload once ( .Margin(left: 16, top: 8, right: 8, bottom: 8) ) or stick to the uniform shape if all sides are equal. Tips # Generic constraint preserves type — chain freely. .Bold() returns TextBlockElement , .Margin() returns TextBlockElement , every modifier preserves the type. .Set(tb => ...) at any point in the chain still gets the strongly-typed TextBlock callback. Merge is \"later wins\" per slot. Repeating the same modifier overrides; it doesn't accumulate. The exception is event-callback parity, which the Fluent API spec §15 Q2 documents — passing null to a .Click(null) clears the handler rather than queuing a no-op. Attached values are atomic. .Grid(row: 1).Grid(column: 2) replaces the first GridAttached with a fresh one — the column wins and the row resets to zero. Set all attached fields in one call: .Grid(row: 1, column: 2) . Reach for .Set only when nothing else fits. If a modifier exists ( .Bold , .FontSize , .Foreground ), use it — the reconciler can batch property writes for known modifiers and skip them under CanSkipUpdate . .Set always runs. Next Steps # Styling — Previous: the surface API for the chain documented here. Reconciliation — Next: where the merged modifier actually reaches WinUI. Theming tokens — ThemeBindings companion slot for brush properties. Flex layout — Worked example of an attached-data record. Element pool — Why modifier cleanup matters when a control is recycled.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/modifier-system.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/modifier-system.md",
      "headings": [
        {
          "depth": 2,
          "id": "modifiers-are-flat-records-not-nested-wrappers",
          "text": "Modifiers are flat records, not nested wrappers"
        },
        {
          "depth": 2,
          "id": "attached-values-live-in-a-separate-slot",
          "text": "Attached values live in a separate slot"
        },
        {
          "depth": 2,
          "id": "event-callback-modifiers-are-typed-with-updates",
          "text": "Event-callback modifiers are typed with updates"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "authoring-a-custom-modifier",
          "text": "Authoring a custom modifier"
        },
        {
          "depth": 3,
          "id": "reading-the-merged-modifier-in-a-custom-panel",
          "text": "Reading the merged modifier in a custom panel"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "expecting-padding-and-background-to-stack-like-swiftui",
          "text": "Expecting Padding and Background to stack like SwiftUI"
        },
        {
          "depth": 3,
          "id": "re-setting-the-same-property-and-expecting-an-additive-effect",
          "text": "Re-setting the same property and expecting an additive effect"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Analyzer Architecture",
      "path": "analyzer-architecture.md",
      "slug": "/analyzer-architecture/",
      "html": "<p>Microsoft.UI.Reactor (Reactor) ships a small Roslyn analyzer suite that catches mistakes the\ntype system can't — conditional hook calls, missing list keys,\nhardcoded colors, icon buttons with no accessible name, stale closure\ncaptures in <code>UseMemoCells</code>, and missing XML docs on public API. The\nanalyzers run inside the C# compiler on every build, so a violation\nshows up as a red squiggle in the editor before you ever run the app.\nThey're written to be cheap: each one starts with a syntactic gate\n(does this invocation even mention the method name we care about?)\nbefore consulting the <a href=\"/rules-of-reactor/\"><code>SemanticModel</code></a>, which is\nwhere the expensive type resolution lives. The most common mistake is\nauthoring a new analyzer that hits <code>SemanticModel.GetSymbolInfo</code> on\nevery <code>InvocationExpression</code> — that single change can double build\ntime on a large solution.</p>\n<h1 id=\"analyzer-architecture\">Analyzer Architecture<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#analyzer-architecture\">#</a></h1>\n<p>This page walks through the Reactor analyzer pipeline end-to-end:\nhow a diagnostic descriptor wires into the C# compilation, what\nhappens on each <code>InvocationExpression</code>, and how to add your own\nrule. The user-facing catalog of diagnostics lives on the\n<a href=\"/rules-of-reactor/\">Rules of Reactor</a> page; this is the contributor\nview. The analyzers sit in\n<a href=\"https://github.com/microsoft/reactor\"><code>src/Reactor.Analyzers/</code></a> and\nare pulled into every Reactor consumer through the SDK's\n<code>Analyzers</code> ItemGroup.</p>\n<h2 id=\"the-rule-pipeline\">The rule pipeline<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-rule-pipeline\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/analyzer-architecture/rule-pipeline.svg\" alt=\"Reactor analyzer rule pipeline — SyntaxNode → invocation filter → fast-path name check → semantic model → diagnostic / code fix\"></p>\n<p>Every analyzer is a <code>DiagnosticAnalyzer</code> that registers one or more\nsyntax-node actions in <code>Initialize</code>. The host (Roslyn) hands the\nanalyzer a <code>SyntaxNodeAnalysisContext</code> per matching node; the analyzer\ninspects the node, optionally walks the <a href=\"/rules-of-reactor/\"><code>SemanticModel</code></a>\nto resolve symbols, and calls <code>context.ReportDiagnostic</code> when it finds a\nviolation. Reactor's analyzers all match on <code>SyntaxKind.InvocationExpression</code>\nbecause every diagnostic in the catalog fires on a method call — a hook,\na factory, a modifier, a LINQ <code>Select</code>.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Initialize</span>(<span class=\"hljs-params\">AnalysisContext context</span>)</span>\n{\n    context.ConfigureGeneratedCodeAnalysis(GeneratedCodeAnalysisFlags.None);\n    context.EnableConcurrentExecution();\n    context.RegisterSyntaxNodeAction(AnalyzeInvocation, SyntaxKind.InvocationExpression);\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n    <span class=\"hljs-keyword\">var</span> methodName = GetInvokedMethodName(invocation);\n    <span class=\"hljs-keyword\">if</span> (methodName <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (!LooksLikeHook(methodName)) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (!IsLikelyReactorHook(context, invocation)) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p>The four cheap calls at the top do the heavy lifting on cost:\n<code>GetInvokedMethodName</code> reads the syntax tree only, <code>LooksLikeHook</code>\nmatches a <code>Use</code> prefix on the bare string, and <code>IsLikelyReactorHook</code>\nshort-circuits on <code>using</code> directives so a third-party method named\n<code>UseSomething</code> doesn't trip the rule. Only after all three pass does\nthe rule body run. The same fast-path / slow-path layering shows up in\nevery analyzer in the suite — it's the difference between a rule that\ncosts microseconds per file and one that re-walks the symbol graph on\nevery invocation.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>SemanticModel.GetSymbolInfo</code> is allocation-heavy and not free even on a\nsingle call. Don't reach for it inside the syntax callback unless a\ncheaper syntactic check has already filtered the node down to plausible\ncandidates. The <code>MissingWithKeyAnalyzer</code> is the extreme: it never calls\n<code>GetSymbolInfo</code> at all — a substring check for <code>.WithKey(</code> in the\nlambda body is enough because false positives only fire when the layout\ncontainer also turns out to be a Reactor factory, which the analyzer\nthen confirms by name list.</p>\n</blockquote>\n<h2 id=\"diagnostic-descriptors\">Diagnostic descriptors<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#diagnostic-descriptors\">#</a></h2>\n<p>Every diagnostic is described by a <code>DiagnosticDescriptor</code> — the id, the\ntitle, the message format, the category, the severity, and the help URL\n(when one exists). The descriptor is <code>static readonly</code> so Roslyn can\ndeduplicate it across analyzer instances.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n\n    <span class=\"hljs-comment\">// Match: Button(expr, action) as a factory call (IdentifierNameSyntax, not member access)</span>\n    <span class=\"hljs-keyword\">if</span> (invocation.Expression <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> IdentifierNameSyntax identifier)\n        <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (identifier.Identifier.Text != <span class=\"hljs-string\">\"Button\"</span>)\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> <span class=\"hljs-keyword\">args</span> = invocation.ArgumentList.Arguments;\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">args</span>.Count != <span class=\"hljs-number\">2</span>)\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// If the first argument is a string literal, it's a text button — no diagnostic needed</span>\n    <span class=\"hljs-keyword\">var</span> firstArg = <span class=\"hljs-keyword\">args</span>[<span class=\"hljs-number\">0</span>].Expression;\n    <span class=\"hljs-keyword\">if</span> (firstArg <span class=\"hljs-keyword\">is</span> LiteralExpressionSyntax literal\n        &#x26;&#x26; literal.IsKind(SyntaxKind.StringLiteralExpression))\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Check the fluent chain for .AutomationName()</span>\n    <span class=\"hljs-keyword\">if</span> (HasModifierInChain(invocation, <span class=\"hljs-string\">\"AutomationName\"</span>))\n        <span class=\"hljs-keyword\">return</span>;\n\n    context.ReportDiagnostic(Diagnostic.Create(\n        Rule,\n        invocation.GetLocation()));\n}\n</code></pre>\n<p><code>AnalyzeInvocation</code> here is the rule body for <a href=\"/rules-of-reactor/\"><code>REACTOR_A11Y_001</code></a>:\nit matches <code>Button(expr, action)</code> as an <code>IdentifierNameSyntax</code>-rooted\ncall (not a member access — that filter alone rejects most invocations\nin a typical codebase), then walks back up the fluent chain looking for\na <code>.AutomationName(...)</code> modifier. The walk caps at the enclosing\nstatement so a chain that wanders into another statement doesn't\nfalsely satisfy the rule. The <code>HasModifierInChain</code> helper is the same\nshape every analyzer that needs to look at trailing modifiers uses.</p>\n<h2 id=\"the-current-diagnostic-set\">The current diagnostic set<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-current-diagnostic-set\">#</a></h2>\n<p>These are the rules shipping out of\n<a href=\"https://github.com/microsoft/reactor\"><code>src/Reactor.Analyzers/</code></a> today.\nSeverity is the default; consumers can promote or suppress per project\nvia <code>.editorconfig</code>.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Id</th><th>Severity</th><th>Title</th><th>Source</th></tr></thead><tbody><tr><td><code>REACTOR_HOOKS_001</code></td><td>Warning</td><td>Hook called conditionally</td><td><code>HookRulesAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_HOOKS_004</code></td><td>Warning</td><td>Hook deps contains freshly allocated value</td><td><code>HookRulesAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_HOOKS_005</code></td><td>Warning</td><td>Hook called outside Render or a custom-hook method</td><td><code>HookRulesAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_HOOKS_006</code></td><td>Info</td><td>Resource fetcher looks non-idempotent</td><td><code>HookRulesAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_HOOKS_007</code></td><td>Warning</td><td>UseMemoCells builder captures variable not in deps</td><td><code>UseMemoCellsAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_A11Y_001</code></td><td>Warning</td><td>Icon-only button needs an accessible name</td><td><code>AccessibilityAnalyzers.cs</code></td></tr><tr><td><code>REACTOR_A11Y_002</code></td><td>Warning</td><td>Image needs alt text or AccessibilityHidden</td><td><code>AccessibilityAnalyzers.cs</code></td></tr><tr><td><code>REACTOR_A11Y_003</code></td><td>Warning</td><td>Form field needs a label</td><td><code>AccessibilityAnalyzers.cs</code></td></tr><tr><td><code>REACTOR_THEME_001</code></td><td>Warning</td><td>Use ThemeRef instead of hard-coded color</td><td><code>UseThemeRefAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_THEME_002</code></td><td>Warning</td><td>Prefer lightweight styling</td><td><code>UseLightweightStylingAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_THEME_003</code></td><td>Warning</td><td>RequestedTheme set on a non-root element</td><td><code>RequestedThemeSetAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_DSL_001</code></td><td>Warning</td><td>Dynamic list item missing .WithKey</td><td><code>MissingWithKeyAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_DOC_001</code></td><td>Warning</td><td>Public API missing XML doc summary</td><td><code>XmlDocSummaryAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_DOC_002</code></td><td>Warning</td><td>XML doc cref does not resolve</td><td><code>XmlDocCrefAnalyzer.cs</code></td></tr><tr><td><code>REACTOR_POOL_001</code></td><td>Warning</td><td><code>.Set</code> writes to a property that pool reset clears</td><td><code>PoolResetSetAnalyzer.cs</code></td></tr></tbody></table>\n<p><code>REACTOR_HOOKS_002</code> and <code>_003</code> are reserved for future control-flow /\ndata-flow analyses (variable hook counts across early returns, async\nboundaries inside <code>UseEffect</code>). They have descriptor slots but no\nimplementation today.</p>\n<h2 id=\"symbol-grounded-matching--the-withkey-case\">Symbol-grounded matching — the WithKey case<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#symbol-grounded-matching--the-withkey-case\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Analyze</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext ctx</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> inv = (InvocationExpressionSyntax)ctx.Node;\n\n    <span class=\"hljs-keyword\">if</span> (inv.Expression <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> MemberAccessExpressionSyntax member) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (member.Name.Identifier.ValueText != <span class=\"hljs-string\">\"Select\"</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Single lambda argument with an invocation body.</span>\n    <span class=\"hljs-keyword\">if</span> (inv.ArgumentList.Arguments.Count != <span class=\"hljs-number\">1</span>) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (inv.ArgumentList.Arguments[<span class=\"hljs-number\">0</span>].Expression <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> LambdaExpressionSyntax lambda) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> body = lambda.Body;\n    <span class=\"hljs-keyword\">if</span> (body <span class=\"hljs-keyword\">is</span> BlockSyntax block) body = ExtractReturnExpression(block) ?? body;\n    <span class=\"hljs-keyword\">if</span> (body <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> InvocationExpressionSyntax) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Cheap textual probe — analyzers run hot, so avoid full symbol resolution.</span>\n    <span class=\"hljs-comment\">// If the lambda body mentions \".WithKey(\" anywhere, assume it's keyed.</span>\n    <span class=\"hljs-keyword\">var</span> bodyText = body.ToString();\n    <span class=\"hljs-keyword\">if</span> (bodyText.Contains(<span class=\"hljs-string\">\".WithKey(\"</span>)) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p><a href=\"/rules-of-reactor/\"><code>REACTOR_DSL_001</code></a> is the loudest example of\nsyntactic-only matching done right. It fires on\n<code>items.Select(x => Row(x))</code> where <code>Row(...)</code> doesn't end in\n<code>.WithKey(...)</code>, and the entire decision is a substring check on\n<code>body.ToString()</code>. The trade-off is conservative: a <code>Select</code> projecting\nto a non-Reactor element type also gets the substring check, but the\nfollow-on <code>IsConsumedAsLayoutChildren</code> walk filters to\n<a href=\"/layout/\"><code>VStack</code></a> / <code>HStack</code> / <code>FlexRow</code> / <code>Grid</code> / <code>WrapGrid</code>\nparents by name. False positives require the user to be inside one of\nthose layout factories <em>and</em> projecting a method that happens not to\nend in <code>.WithKey</code> — rare enough that the syntactic-only approach is\ncorrect.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n\n    <span class=\"hljs-keyword\">if</span> (invocation.Expression <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> MemberAccessExpressionSyntax memberAccess)\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> methodName = memberAccess.Name.Identifier.Text;\n    <span class=\"hljs-keyword\">if</span> (!TargetMethods.Contains(methodName))\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> <span class=\"hljs-keyword\">args</span> = invocation.ArgumentList.Arguments;\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">args</span>.Count == <span class=\"hljs-number\">0</span>)\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Check if the first argument is a string literal</span>\n    <span class=\"hljs-keyword\">var</span> firstArg = <span class=\"hljs-keyword\">args</span>[<span class=\"hljs-number\">0</span>].Expression;\n    <span class=\"hljs-keyword\">if</span> (firstArg <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> LiteralExpressionSyntax literal)\n        <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (!literal.IsKind(SyntaxKind.StringLiteralExpression))\n        <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> colorValue = literal.Token.ValueText;\n</code></pre>\n<p><a href=\"/theming-tokens/\"><code>REACTOR_THEME_001</code></a> is a counterpoint: the rule\nneeds to read the string literal that follows\n<code>.Background(\"...\")</code> / <code>.Foreground(\"...\")</code> / <code>.WithBorder(\"...\")</code> and\nmap it to a suggested theme token. The descriptor message format has a\n<code>{0}</code> slot for the suggested token, which the analyzer looks up in\n<code>ColorToThemeToken</code> after the syntactic match. The diagnostic flows\nthrough to a paired <code>CodeFixProvider</code> that rewrites the literal to the\nmatching <code>Theme.Accent</code> / <code>Theme.PrimaryText</code> member.</p>\n<h2 id=\"property-bag-handoff-to-the-code-fix\">Property-bag handoff to the code fix<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#property-bag-handoff-to-the-code-fix\">#</a></h2>\n<p>Some rules carry data from the analyzer to the fix provider that isn't\ntrivial to recover from the diagnostic location alone. The\n<a href=\"/rules-of-reactor/\"><code>UseMemoCellsAnalyzer</code></a> does this — when the\nbuilder lambda closes over a variable that isn't in <code>deps</code>, the rule\nneeds to tell the code fix which variable name to add:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n    <span class=\"hljs-keyword\">var</span> name = GetInvokedMethodName(invocation);\n    <span class=\"hljs-keyword\">if</span> (name <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span> || !HookNames.Contains(name)) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">var</span> model = context.SemanticModel;\n    <span class=\"hljs-keyword\">var</span> symbol = model.GetSymbolInfo(invocation).Symbol <span class=\"hljs-keyword\">as</span> IMethodSymbol;\n    <span class=\"hljs-keyword\">if</span> (symbol <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Match by symbol so user-defined methods named UseMemoCells in</span>\n    <span class=\"hljs-comment\">// unrelated namespaces don't trip the analyzer.</span>\n    <span class=\"hljs-keyword\">if</span> (!IsReactorMemoCellsHook(symbol)) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p><code>CaptureNameProperty</code> is the contract. The rule body, after walking the\nlambda's body for unbound captures, writes the variable name into\n<code>Diagnostic.Properties</code> keyed by that string. The fix provider reads it\nback out and inserts the corresponding argument into the call's\n<code>deps</code> list. Round-tripping through the property bag is the supported\npattern — embedding the value in the message text would break the\nfix the moment the message changes or gets localized.</p>\n<h2 id=\"the-doc-system-analyzers\">The doc-system analyzers<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-doc-system-analyzers\">#</a></h2>\n<p>Phase 1.8 of <a href=\"https://github.com/microsoft/reactor\">spec 041</a> added\ntwo analyzers that don't fire on application code at all — they fire\non the Reactor SDK's own source to keep the auto-generated reference\ndocset honest. <code>REACTOR_DOC_001</code> flags any public type, method,\nproperty, or event without a <code>&#x3C;summary></code> XML doc comment;\n<code>REACTOR_DOC_002</code> flags <code>cref=\"X\"</code> attributes that don't resolve to a\nreal symbol. Both run during the <code>dotnet build</code> of <code>Reactor.csproj</code>\nitself; downstream consumers don't see them.</p>\n<p>The doc analyzers register a <code>SymbolAction</code> instead of a\n<code>SyntaxNodeAction</code> — the question they answer is per-symbol\n(is this symbol documented?) rather than per-syntax-node, so the\nsymbol-level callback is the cheaper and more accurate hook. This is\nthe second registration pattern in the codebase; everything else uses\nthe syntax-node pattern from the rule pipeline above. When you author a\nnew analyzer, the question to ask first is \"is this rule about a\nlocation in source, or about a thing in the type system\" — the answer\npicks the registration kind.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"authoring-a-new-analyzer\">Authoring a new analyzer<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#authoring-a-new-analyzer\">#</a></h3>\n<p>The minimum new-analyzer surface is one descriptor, one registration,\none rule body, and one entry in the project's <code>AnalyzerReleases.Shipped.md</code>\nwhen the rule moves out of preview. Start by writing the descriptor\nwith a stable id that follows the <code>REACTOR_&#x3C;CATEGORY>_&#x3C;NNN></code> convention:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> <span class=\"hljs-built_in\">string</span> DiagnosticId = <span class=\"hljs-string\">\"REACTOR_MYTHING_001\"</span>;\n<span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> DiagnosticDescriptor Rule = <span class=\"hljs-keyword\">new</span>(\n    DiagnosticId,\n    title: <span class=\"hljs-string\">\"One-line title\"</span>,\n    messageFormat: <span class=\"hljs-string\">\"Specific problem: {0}\"</span>,\n    category: <span class=\"hljs-string\">\"Reactor.MyThing\"</span>,\n    defaultSeverity: DiagnosticSeverity.Warning,\n    isEnabledByDefault: <span class=\"hljs-literal\">true</span>,\n    description: <span class=\"hljs-string\">\"Why this is wrong and what to do instead.\"</span>);\n</code></pre>\n<p>Then register the syntax-node action in <code>Initialize</code> and write the\nrule body. Mirror the layering from\n<a href=\"/rules-of-reactor/\"><code>HookRulesAnalyzer</code></a> — cheapest syntactic check\nfirst, name fast-path second, symbol resolution last, diagnostic\nreport only after all of those. If the rule wants a code fix, route\nthe relevant capture into <code>Diagnostic.Properties</code> and pair the\nanalyzer with a <code>CodeFixProvider</code> in the same assembly. Add a unit\ntest under <code>tests/Reactor.Analyzers.Tests/</code> for the positive case,\nthe negative case, and any edge that almost trips the syntactic\nfast path — those are the regressions that bite later.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"reaching-for-semanticmodel-on-every-node\">Reaching for SemanticModel on every node<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-semanticmodel-on-every-node\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n    <span class=\"hljs-keyword\">var</span> symbol = context.SemanticModel.GetSymbolInfo(invocation).Symbol;\n    <span class=\"hljs-keyword\">if</span> (symbol <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> IMethodSymbol m) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (m.Name != <span class=\"hljs-string\">\"Button\"</span>) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-comment\">// ...</span>\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Initialize</span>(<span class=\"hljs-params\">AnalysisContext context</span>)</span>\n{\n    context.ConfigureGeneratedCodeAnalysis(GeneratedCodeAnalysisFlags.None);\n    context.EnableConcurrentExecution();\n    context.RegisterSyntaxNodeAction(AnalyzeInvocation, SyntaxKind.InvocationExpression);\n}\n\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">AnalyzeInvocation</span>(<span class=\"hljs-params\">SyntaxNodeAnalysisContext context</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> invocation = (InvocationExpressionSyntax)context.Node;\n    <span class=\"hljs-keyword\">var</span> methodName = GetInvokedMethodName(invocation);\n    <span class=\"hljs-keyword\">if</span> (methodName <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (!LooksLikeHook(methodName)) <span class=\"hljs-keyword\">return</span>;\n    <span class=\"hljs-keyword\">if</span> (!IsLikelyReactorHook(context, invocation)) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p>The anti-pattern resolves a full symbol on every invocation in the\ncompilation — every <code>.ToString()</code>, every <code>string.Format</code>, every\nLINQ method, every method call in every test file. The correct shape\nchecks the method name syntactically first and only resolves the\nsymbol after the cheap check passes. On a large solution the\ndifference is seconds of build time per file.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Match <code>IdentifierNameSyntax</code> for factories, <code>MemberAccessExpressionSyntax</code>\nfor modifiers.</strong> Reactor factories are imported as\n<a href=\"/components/\"><code>using static</code></a> so they show up as bare identifiers\n(<code>Button(...)</code>, <code>VStack(...)</code>). Modifiers are always\n<code>.Method(...)</code> chains, which are member accesses. Splitting on the\nexpression kind early prunes most of the irrelevant invocations\nbefore any name check runs.</p>\n<p><strong>Use <code>Diagnostic.Properties</code> for the fix-provider handoff.</strong> Anything\nthe fix needs that isn't trivially recoverable from the diagnostic\n<code>Location</code> goes in the property bag. Re-parsing the message text\nbreaks the moment the message is touched.</p>\n<p><strong>Run the analyzer against <code>samples/</code>.</strong> The Reactor sample apps\ncollectively cover every modifier and every hook combination the test\nsuite cares about. A new analyzer that produces zero findings against\n<code>samples/</code> either over-fits its tests or has a syntactic gate that's\ntoo narrow — both warrant a look before merge.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/rules-of-reactor/\">Rules of Reactor</a></strong> — User-facing catalog of every diagnostic in the suite.</li>\n<li><strong><a href=\"/theming-tokens/\">Theming Tokens</a></strong> — How <code>REACTOR_THEME_001</code> plugs into the styling story.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — How the <code>REACTOR_A11Y_*</code> rules read code.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Why the hook rules exist; the slot-table invariant they protect.</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — Next under-the-hood page.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor) ships a small Roslyn analyzer suite that catches mistakes the type system can't — conditional hook calls, missing list keys, hardcoded colors, icon buttons with no accessible name, stale closure captures in UseMemoCells , and missing XML docs on public API. The analyzers run inside the C# compiler on every build, so a violation shows up as a red squiggle in the editor before you ever run the app. They're written to be cheap: each one starts with a syntactic gate (does this invocation even mention the method name we care about?) before consulting the SemanticModel , which is where the expensive type resolution lives. The most common mistake is authoring a new analyzer that hits SemanticModel.GetSymbolInfo on every InvocationExpression — that single change can double build time on a large solution. Analyzer Architecture # This page walks through the Reactor analyzer pipeline end-to-end: how a diagnostic descriptor wires into the C# compilation, what happens on each InvocationExpression , and how to add your own rule. The user-facing catalog of diagnostics lives on the Rules of Reactor page; this is the contributor view. The analyzers sit in src/Reactor.Analyzers/ and are pulled into every Reactor consumer through the SDK's Analyzers ItemGroup. The rule pipeline # Every analyzer is a DiagnosticAnalyzer that registers one or more syntax-node actions in Initialize . The host (Roslyn) hands the analyzer a SyntaxNodeAnalysisContext per matching node; the analyzer inspects the node, optionally walks the SemanticModel to resolve symbols, and calls context.ReportDiagnostic when it finds a violation. Reactor's analyzers all match on SyntaxKind.InvocationExpression because every diagnostic in the catalog fires on a method call — a hook, a factory, a modifier, a LINQ Select . public override void Initialize ( AnalysisContext context ) { context.ConfigureGeneratedCodeAnalysis(GeneratedCodeAnalysisFlags.None); context.EnableConcurrentExecution(); context.RegisterSyntaxNodeAction(AnalyzeInvocation, SyntaxKind.InvocationExpression); } private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; var methodName = GetInvokedMethodName(invocation); if (methodName is null ) return ; if (!LooksLikeHook(methodName)) return ; if (!IsLikelyReactorHook(context, invocation)) return ; The four cheap calls at the top do the heavy lifting on cost: GetInvokedMethodName reads the syntax tree only, LooksLikeHook matches a Use prefix on the bare string, and IsLikelyReactorHook short-circuits on using directives so a third-party method named UseSomething doesn't trip the rule. Only after all three pass does the rule body run. The same fast-path / slow-path layering shows up in every analyzer in the suite — it's the difference between a rule that costs microseconds per file and one that re-walks the symbol graph on every invocation. Caveat: SemanticModel.GetSymbolInfo is allocation-heavy and not free even on a single call. Don't reach for it inside the syntax callback unless a cheaper syntactic check has already filtered the node down to plausible candidates. The MissingWithKeyAnalyzer is the extreme: it never calls GetSymbolInfo at all — a substring check for .WithKey( in the lambda body is enough because false positives only fire when the layout container also turns out to be a Reactor factory, which the analyzer then confirms by name list. Diagnostic descriptors # Every diagnostic is described by a DiagnosticDescriptor — the id, the title, the message format, the category, the severity, and the help URL (when one exists). The descriptor is static readonly so Roslyn can deduplicate it across analyzer instances. private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; // Match: Button(expr, action) as a factory call (IdentifierNameSyntax, not member access) if (invocation.Expression is not IdentifierNameSyntax identifier) return ; if (identifier.Identifier.Text != \"Button\" ) return ; var args = invocation.ArgumentList.Arguments; if ( args .Count != 2 ) return ; // If the first argument is a string literal, it's a text button — no diagnostic needed var firstArg = args [ 0 ].Expression; if (firstArg is LiteralExpressionSyntax literal &#x26;&#x26; literal.IsKind(SyntaxKind.StringLiteralExpression)) return ; // Check the fluent chain for .AutomationName() if (HasModifierInChain(invocation, \"AutomationName\" )) return ; context.ReportDiagnostic(Diagnostic.Create( Rule, invocation.GetLocation())); } AnalyzeInvocation here is the rule body for REACTOR_A11Y_001 : it matches Button(expr, action) as an IdentifierNameSyntax -rooted call (not a member access — that filter alone rejects most invocations in a typical codebase), then walks back up the fluent chain looking for a .AutomationName(...) modifier. The walk caps at the enclosing statement so a chain that wanders into another statement doesn't falsely satisfy the rule. The HasModifierInChain helper is the same shape every analyzer that needs to look at trailing modifiers uses. The current diagnostic set # These are the rules shipping out of src/Reactor.Analyzers/ today. Severity is the default; consumers can promote or suppress per project via .editorconfig . Id Severity Title Source REACTOR_HOOKS_001 Warning Hook called conditionally HookRulesAnalyzer.cs REACTOR_HOOKS_004 Warning Hook deps contains freshly allocated value HookRulesAnalyzer.cs REACTOR_HOOKS_005 Warning Hook called outside Render or a custom-hook method HookRulesAnalyzer.cs REACTOR_HOOKS_006 Info Resource fetcher looks non-idempotent HookRulesAnalyzer.cs REACTOR_HOOKS_007 Warning UseMemoCells builder captures variable not in deps UseMemoCellsAnalyzer.cs REACTOR_A11Y_001 Warning Icon-only button needs an accessible name AccessibilityAnalyzers.cs REACTOR_A11Y_002 Warning Image needs alt text or AccessibilityHidden AccessibilityAnalyzers.cs REACTOR_A11Y_003 Warning Form field needs a label AccessibilityAnalyzers.cs REACTOR_THEME_001 Warning Use ThemeRef instead of hard-coded color UseThemeRefAnalyzer.cs REACTOR_THEME_002 Warning Prefer lightweight styling UseLightweightStylingAnalyzer.cs REACTOR_THEME_003 Warning RequestedTheme set on a non-root element RequestedThemeSetAnalyzer.cs REACTOR_DSL_001 Warning Dynamic list item missing .WithKey MissingWithKeyAnalyzer.cs REACTOR_DOC_001 Warning Public API missing XML doc summary XmlDocSummaryAnalyzer.cs REACTOR_DOC_002 Warning XML doc cref does not resolve XmlDocCrefAnalyzer.cs REACTOR_POOL_001 Warning .Set writes to a property that pool reset clears PoolResetSetAnalyzer.cs REACTOR_HOOKS_002 and _003 are reserved for future control-flow / data-flow analyses (variable hook counts across early returns, async boundaries inside UseEffect ). They have descriptor slots but no implementation today. Symbol-grounded matching — the WithKey case # static void Analyze ( SyntaxNodeAnalysisContext ctx ) { var inv = (InvocationExpressionSyntax)ctx.Node; if (inv.Expression is not MemberAccessExpressionSyntax member) return ; if (member.Name.Identifier.ValueText != \"Select\" ) return ; // Single lambda argument with an invocation body. if (inv.ArgumentList.Arguments.Count != 1 ) return ; if (inv.ArgumentList.Arguments[ 0 ].Expression is not LambdaExpressionSyntax lambda) return ; var body = lambda.Body; if (body is BlockSyntax block) body = ExtractReturnExpression(block) ?? body; if (body is not InvocationExpressionSyntax) return ; // Cheap textual probe — analyzers run hot, so avoid full symbol resolution. // If the lambda body mentions \".WithKey(\" anywhere, assume it's keyed. var bodyText = body.ToString(); if (bodyText.Contains( \".WithKey(\" )) return ; REACTOR_DSL_001 is the loudest example of syntactic-only matching done right. It fires on items.Select(x => Row(x)) where Row(...) doesn't end in .WithKey(...) , and the entire decision is a substring check on body.ToString() . The trade-off is conservative: a Select projecting to a non-Reactor element type also gets the substring check, but the follow-on IsConsumedAsLayoutChildren walk filters to VStack / HStack / FlexRow / Grid / WrapGrid parents by name. False positives require the user to be inside one of those layout factories and projecting a method that happens not to end in .WithKey — rare enough that the syntactic-only approach is correct. private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; if (invocation.Expression is not MemberAccessExpressionSyntax memberAccess) return ; var methodName = memberAccess.Name.Identifier.Text; if (!TargetMethods.Contains(methodName)) return ; var args = invocation.ArgumentList.Arguments; if ( args .Count == 0 ) return ; // Check if the first argument is a string literal var firstArg = args [ 0 ].Expression; if (firstArg is not LiteralExpressionSyntax literal) return ; if (!literal.IsKind(SyntaxKind.StringLiteralExpression)) return ; var colorValue = literal.Token.ValueText; REACTOR_THEME_001 is a counterpoint: the rule needs to read the string literal that follows .Background(\"...\") / .Foreground(\"...\") / .WithBorder(\"...\") and map it to a suggested theme token. The descriptor message format has a {0} slot for the suggested token, which the analyzer looks up in ColorToThemeToken after the syntactic match. The diagnostic flows through to a paired CodeFixProvider that rewrites the literal to the matching Theme.Accent / Theme.PrimaryText member. Property-bag handoff to the code fix # Some rules carry data from the analyzer to the fix provider that isn't trivial to recover from the diagnostic location alone. The UseMemoCellsAnalyzer does this — when the builder lambda closes over a variable that isn't in deps , the rule needs to tell the code fix which variable name to add: private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; var name = GetInvokedMethodName(invocation); if (name is null || !HookNames.Contains(name)) return ; var model = context.SemanticModel; var symbol = model.GetSymbolInfo(invocation).Symbol as IMethodSymbol; if (symbol is null ) return ; // Match by symbol so user-defined methods named UseMemoCells in // unrelated namespaces don't trip the analyzer. if (!IsReactorMemoCellsHook(symbol)) return ; CaptureNameProperty is the contract. The rule body, after walking the lambda's body for unbound captures, writes the variable name into Diagnostic.Properties keyed by that string. The fix provider reads it back out and inserts the corresponding argument into the call's deps list. Round-tripping through the property bag is the supported pattern — embedding the value in the message text would break the fix the moment the message changes or gets localized. The doc-system analyzers # Phase 1.8 of spec 041 added two analyzers that don't fire on application code at all — they fire on the Reactor SDK's own source to keep the auto-generated reference docset honest. REACTOR_DOC_001 flags any public type, method, property, or event without a &#x3C;summary> XML doc comment; REACTOR_DOC_002 flags cref=\"X\" attributes that don't resolve to a real symbol. Both run during the dotnet build of Reactor.csproj itself; downstream consumers don't see them. The doc analyzers register a SymbolAction instead of a SyntaxNodeAction — the question they answer is per-symbol (is this symbol documented?) rather than per-syntax-node, so the symbol-level callback is the cheaper and more accurate hook. This is the second registration pattern in the codebase; everything else uses the syntax-node pattern from the rule pipeline above. When you author a new analyzer, the question to ask first is \"is this rule about a location in source, or about a thing in the type system\" — the answer picks the registration kind. Patterns # Authoring a new analyzer # The minimum new-analyzer surface is one descriptor, one registration, one rule body, and one entry in the project's AnalyzerReleases.Shipped.md when the rule moves out of preview. Start by writing the descriptor with a stable id that follows the REACTOR_&#x3C;CATEGORY>_&#x3C;NNN> convention: public const string DiagnosticId = \"REACTOR_MYTHING_001\" ; private static readonly DiagnosticDescriptor Rule = new ( DiagnosticId, title: \"One-line title\" , messageFormat: \"Specific problem: {0}\" , category: \"Reactor.MyThing\" , defaultSeverity: DiagnosticSeverity.Warning, isEnabledByDefault: true , description: \"Why this is wrong and what to do instead.\" ); Then register the syntax-node action in Initialize and write the rule body. Mirror the layering from HookRulesAnalyzer — cheapest syntactic check first, name fast-path second, symbol resolution last, diagnostic report only after all of those. If the rule wants a code fix, route the relevant capture into Diagnostic.Properties and pair the analyzer with a CodeFixProvider in the same assembly. Add a unit test under tests/Reactor.Analyzers.Tests/ for the positive case, the negative case, and any edge that almost trips the syntactic fast path — those are the regressions that bite later. Common Mistakes # Reaching for SemanticModel on every node # // Don't: private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; var symbol = context.SemanticModel.GetSymbolInfo(invocation).Symbol; if (symbol is not IMethodSymbol m) return ; if (m.Name != \"Button\" ) return ; // ... } public override void Initialize ( AnalysisContext context ) { context.ConfigureGeneratedCodeAnalysis(GeneratedCodeAnalysisFlags.None); context.EnableConcurrentExecution(); context.RegisterSyntaxNodeAction(AnalyzeInvocation, SyntaxKind.InvocationExpression); } private static void AnalyzeInvocation ( SyntaxNodeAnalysisContext context ) { var invocation = (InvocationExpressionSyntax)context.Node; var methodName = GetInvokedMethodName(invocation); if (methodName is null ) return ; if (!LooksLikeHook(methodName)) return ; if (!IsLikelyReactorHook(context, invocation)) return ; The anti-pattern resolves a full symbol on every invocation in the compilation — every .ToString() , every string.Format , every LINQ method, every method call in every test file. The correct shape checks the method name syntactically first and only resolves the symbol after the cheap check passes. On a large solution the difference is seconds of build time per file. Tips # Match IdentifierNameSyntax for factories, MemberAccessExpressionSyntax for modifiers. Reactor factories are imported as using static so they show up as bare identifiers ( Button(...) , VStack(...) ). Modifiers are always .Method(...) chains, which are member accesses. Splitting on the expression kind early prunes most of the irrelevant invocations before any name check runs. Use Diagnostic.Properties for the fix-provider handoff. Anything the fix needs that isn't trivially recoverable from the diagnostic Location goes in the property bag. Re-parsing the message text breaks the moment the message is touched. Run the analyzer against samples/ . The Reactor sample apps collectively cover every modifier and every hook combination the test suite cares about. A new analyzer that produces zero findings against samples/ either over-fits its tests or has a syntactic gate that's too narrow — both warrant a look before merge. Next Steps # Rules of Reactor — User-facing catalog of every diagnostic in the suite. Theming Tokens — How REACTOR_THEME_001 plugs into the styling story. Accessibility — How the REACTOR_A11Y_* rules read code. Hooks Internals — Why the hook rules exist; the slot-table invariant they protect. DevTools Internals — Next under-the-hood page.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/analyzer-architecture.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/analyzer-architecture.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-rule-pipeline",
          "text": "The rule pipeline"
        },
        {
          "depth": 2,
          "id": "diagnostic-descriptors",
          "text": "Diagnostic descriptors"
        },
        {
          "depth": 2,
          "id": "the-current-diagnostic-set",
          "text": "The current diagnostic set"
        },
        {
          "depth": 2,
          "id": "symbol-grounded-matching--the-withkey-case",
          "text": "Symbol-grounded matching — the WithKey case"
        },
        {
          "depth": 2,
          "id": "property-bag-handoff-to-the-code-fix",
          "text": "Property-bag handoff to the code fix"
        },
        {
          "depth": 2,
          "id": "the-doc-system-analyzers",
          "text": "The doc-system analyzers"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "authoring-a-new-analyzer",
          "text": "Authoring a new analyzer"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "reaching-for-semanticmodel-on-every-node",
          "text": "Reaching for SemanticModel on every node"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "DevTools Internals",
      "path": "devtools-internals.md",
      "slug": "/devtools-internals/",
      "html": "<p>The Microsoft.UI.Reactor (Reactor) devtools surface is two subsystems that share a single\ngate. The MCP server speaks JSON-RPC over either HTTP loopback or\nstdio, exposing a fixed inventory of tools (<code>tools/list</code>, <code>tools/call</code>)\nthat an external CLI, editor, or agent harness drives against the\nrunning app. The in-app dev menu — <code>DevtoolsMenu(...)</code>, the keyboard\nshortcuts, the reconcile-highlight overlay, the layout-cost overlay —\nruns entirely in-process, observes the same component tree the user\nsees, and is gated by the same <code>UseDevtools()</code> flag. Both subsystems\nare zero-cost in retail: the gate evaluates a <code>static readonly bool</code>,\nand code paths gated behind it never construct elements or register\nETW listeners. The most common mistake is reaching for a devtools\nmodifier outside the gate; in retail it's still a <code>null</code>-returning\nfactory and contributes nothing visual, but the code path still\nallocates whatever the surrounding component constructed to pass to\nit. Gate the construction, not just the rendering.</p>\n<h1 id=\"devtools-internals\">DevTools Internals<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#devtools-internals\">#</a></h1>\n<p>This page covers the internals of Reactor's devtools — the MCP server\nthat external agents talk to, the in-app dev menu, the runtime\noverlays, and the gate that keeps the whole subsystem out of retail.\n<a href=\"/dev-tooling/\">Dev Tooling</a> is the user-facing companion; this is\nthe internals view.</p>\n<h2 id=\"the-mcp-loop\">The MCP loop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-mcp-loop\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/devtools-internals/mcp-loop.svg\" alt=\"DevTools MCP loop — CLI reads lockfile, POSTs JSON-RPC tools/call, server dispatches to McpDispatcher, hops to UI thread, tool runs against live tree, returns JSON-RPC response. In-app DevtoolsMenu observes the same UseDevtools gate\"></p>\n<p>The MCP server is a loopback <code>HttpListener</code> (or a stdio reader/writer\nloop) running inside the host process. On startup it picks a free\nport, generates a per-launch bearer token, and writes a lockfile under\n<code>%TEMP%/reactor-devtools/&#x3C;hash>.json</code> advertising the endpoint, the\nport, the pid, the build tag, and the token. The CLI reads that\nlockfile to find the running app — there's no service registry, no\nbroadcast discovery, just a file in tempdir keyed by the project's\ncanonicalized path. Any process running as the same user can read\nthe lockfile, present the token, and call tools.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Subsystem</th><th>Owner type</th><th>Source</th><th>Gate</th></tr></thead><tbody><tr><td>Gate hook</td><td><code>UseDevtools()</code></td><td><code>src/Reactor/Hooks/UseDevtools.cs</code></td><td>Build-time <code>devtools: true</code> AND session-time <code>--devtools app</code></td></tr><tr><td>MCP server</td><td><code>DevtoolsMcpServer</code></td><td><code>src/Reactor/Hosting/Devtools/DevtoolsMcpServer.cs</code></td><td>Bearer token + project lockfile</td></tr><tr><td>Tool registry</td><td><code>McpToolRegistry</code></td><td><code>src/Reactor/Hosting/Devtools/McpToolRegistry.cs</code></td><td>Per-tool input-schema validation</td></tr><tr><td>JSON-RPC dispatch</td><td><code>McpDispatcher</code></td><td><code>src/Reactor/Hosting/Devtools/McpDispatcher.cs</code></td><td>Method allowlist + <code>tools/call</code> routing</td></tr><tr><td>CLI client</td><td><code>McpCliClient</code></td><td><code>src/Reactor.Cli/Devtools/McpCliClient.cs</code></td><td>Reads lockfile, posts bearer-authed JSON-RPC</td></tr><tr><td>In-app menu</td><td><code>DevtoolsMenu(...)</code> factory</td><td><code>src/Reactor/Hosting/Devtools/DevtoolsMenuFactory.cs</code></td><td><code>UseDevtools()</code> — returns <code>Empty()</code> when off</td></tr><tr><td>Overlays</td><td><code>ReactorFeatureFlags</code></td><td><code>src/Reactor/Core/</code></td><td>Toggle binds an ETW listener only when on</td></tr></tbody></table>\n<h2 id=\"the-gate\">The gate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-gate\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">UseDevtools</span>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> RenderContext ctx</span>)</span> =>\n    ReactorApp.DevtoolsEnabled;\n</code></pre>\n<p><code>UseDevtools()</code> returns <code>ReactorApp.DevtoolsEnabled</code> — the AND of two\nindependent signals captured at process startup. The build-time signal\nis the <code>devtools: true</code> argument to <code>ReactorApp.Run&#x3C;TRoot>(...)</code>; ship\na release binary without it and the AND is false no matter what flags\nthe user passes. The session-time signal is the <code>--devtools app</code> (or\n<code>--devtools run</code>) command-line argument the user supplies. Both must\nhold for the gate to open.</p>\n<p>The value is frozen for the session and the hook deliberately does\nnot consume a slot in the <a href=\"/hooks-internals/\">hook table</a>. That's\nwhy <code>UseDevtools()</code> can be called from helpers that aren't part of\nthe render path — there's no positional invariant to violate. The\ntrade-off: the value can't change at runtime. Flipping\n<code>ReactorApp.DevtoolsEnabled</code> mid-session would produce a stale\ngate read in every component already rendered.</p>\n<blockquote>\n<p><strong>Caveat:</strong> Calling <code>UseDevtools()</code> does not opt the component into observing the\nflag — the flag is frozen for the session, so observation isn't\nneeded. But code inside the <code>dev ? DebugOverlay() : null</code> branch is\nstill evaluated when the gate is open, even if your overlay never\ndisplays anything user-visible. A <code>DebugOverlay()</code> factory that\nconstructs 200 children to display in a dev-only data panel is 200\nallocations per render whenever devtools are enabled, including\nduring automated UI testing that happens to launch with <code>--devtools app</code>. Gate construction, not just display.</p>\n</blockquote>\n<h2 id=\"the-cli-client\">The CLI client<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-cli-client\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">internal</span> <span class=\"hljs-keyword\">sealed</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">McpCliClient</span> : <span class=\"hljs-title\">IDisposable</span>\n{\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> HttpClient _http;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span> _endpoint;\n    <span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">readonly</span> <span class=\"hljs-built_in\">string</span>? _token;\n\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-title\">McpCliClient</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> endpoint, TimeSpan? timeout = <span class=\"hljs-literal\">null</span>, <span class=\"hljs-built_in\">string</span>? token = <span class=\"hljs-literal\">null</span></span>)</span>\n    {\n        _endpoint = endpoint;\n        _token = token;\n        _http = <span class=\"hljs-keyword\">new</span> HttpClient { Timeout = timeout ?? TimeSpan.FromSeconds(<span class=\"hljs-number\">30</span>) };\n        <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-built_in\">string</span>.IsNullOrEmpty(token))\n        {\n            _http.DefaultRequestHeaders.Authorization =\n                <span class=\"hljs-keyword\">new</span> <span class=\"hljs-keyword\">global</span>::System.Net.Http.Headers.AuthenticationHeaderValue(<span class=\"hljs-string\">\"Bearer\"</span>, token);\n        }\n    }\n</code></pre>\n<p><code>McpCliClient</code> is the CLI-side terminus of the MCP loop. It takes an\nendpoint URL, an optional bearer token, and an optional timeout, and\nexposes <code>InvokeTool</code> and <code>InvokeMethod</code> as the two entry points; the\nCLI's verb commands (<code>mur devtools components</code>, <code>mur devtools click</code>,\n…) all layer on top of <code>InvokeTool</code> with the tool name baked in. The\ntoken comes from the lockfile the server wrote on startup, so the\ndiscovery flow is open-the-lockfile → read endpoint + token →\nconstruct client. The <code>Authorization: Bearer &#x3C;token></code> header is the\nonly thing standing between an attacker on localhost and the\nrunning app's MCP surface — the lockfile sits in the user's tempdir\nso any process running as the user can read it, but processes\nrunning as other users can't.</p>\n<h2 id=\"endpoint-discovery\">Endpoint discovery<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#endpoint-discovery\">#</a></h2>\n<p>The lockfile under <code>%TEMP%/reactor-devtools/&#x3C;hash>.json</code> carries the\nendpoint URL, the per-launch bearer token, the pid (for liveness\nprobing), the build tag, the transport (<code>http</code> or <code>stdio</code>), and the\nproject path. The path hash is a truncated SHA-256 of the\ncanonicalized <code>.csproj</code> path — <code>C:\\foo\\bar.csproj</code> and\n<code>c:/foo/bar.csproj</code> collide deliberately, so re-launching the same\nproject from a different shell finds the existing session rather\nthan starting a second one. The CLI's <code>mur devtools serve</code> command\nchecks for a live lockfile before binding a new port; if one exists\nand the process is still alive, the second invocation exits with the\nrunning session's information instead of starting a new server.</p>\n<p>A lockfile is <em>stale</em> when the recorded pid doesn't match a running\nprocess. The CLI deletes stale lockfiles on read; the running server\ndoesn't proactively re-write its lockfile, but it also doesn't need\nto — the file's <code>pid</code> field is the only check that matters, and the\npid doesn't change for the life of the session.</p>\n<h2 id=\"tool-dispatch--the-ui-thread-hop\">Tool dispatch — the UI-thread hop<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tool-dispatch--the-ui-thread-hop\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-built_in\">object</span>? HandleCall(JsonElement? @params)\n{\n    <span class=\"hljs-keyword\">if</span> (@params <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> { } p || p.ValueKind != JsonValueKind.Object)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> McpToolException(<span class=\"hljs-string\">\"tools/call params must be an object with { name, arguments? }.\"</span>,\n            JsonRpcErrorCodes.InvalidParams);\n    <span class=\"hljs-keyword\">if</span> (!p.TryGetProperty(<span class=\"hljs-string\">\"name\"</span>, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> nameEl) || nameEl.ValueKind != JsonValueKind.String)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> McpToolException(<span class=\"hljs-string\">\"tools/call requires a string 'name' field.\"</span>, JsonRpcErrorCodes.InvalidParams);\n\n    <span class=\"hljs-keyword\">var</span> name = nameEl.GetString()!;\n    JsonElement? <span class=\"hljs-keyword\">args</span> = p.TryGetProperty(<span class=\"hljs-string\">\"arguments\"</span>, <span class=\"hljs-keyword\">out</span> <span class=\"hljs-keyword\">var</span> argsEl) ? argsEl : <span class=\"hljs-literal\">null</span>;\n    <span class=\"hljs-keyword\">return</span> Invoke(name, <span class=\"hljs-keyword\">args</span>);\n}\n</code></pre>\n<p>Tool handlers run on the UI dispatcher, not on the HTTP listener's\nworker thread. The MCP server takes the parsed request, looks up the\nhandler in the <a href=\"https://github.com/microsoft/reactor\"><code>McpToolRegistry</code></a>,\nand posts the handler call through <code>DispatcherQueue.TryEnqueue</code>. That\nhop is what makes it safe for a handler to query the live WinUI tree\n— <code>WindowRegistry.Snapshot()</code>, <code>host.Mount(new T())</code>, every property\nread on a <code>FrameworkElement</code> — all of which assume the calling\nthread is the one that owns the visual tree. Without the hop, the\nhandler would race against the UI thread and either throw\n<code>COMException</code> or produce a stale snapshot.</p>\n<p>The dispatch path is the same for HTTP and stdio. The two transports\ndiverge only at the read boundary (<code>HttpListenerContext</code> vs\n<code>StreamReader</code>) and the write boundary (<code>HttpListenerResponse</code> vs\n<code>StreamWriter</code>). Everything between — JSON-RPC parsing, the\n<code>McpDispatcher.Dispatch(body)</code> call, the dispatcher hop, the tool\nhandler — is shared code.</p>\n<h2 id=\"the-in-app-menu\">The in-app menu<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-in-app-menu\">#</a></h2>\n<p><code>DevtoolsMenu(...)</code> is the in-app counterpart to the MCP server: a\ntitlebar widget that shows up only when <code>UseDevtools()</code> is true and\nhosts a flyout of <code>MenuFlyoutItemBase</code> items the host app supplies.\nThe pattern is <code>static readonly Observable&#x3C;bool></code> fields in an\n<code>AppFlags</code> class, mutated by <code>ToggleMenuItem</code> callbacks, observed by\ncomponents via <code>ctx.UseObservable(AppFlags.DebugUI).Value</code>. The\n<code>DevtoolsMenu</code> factory also appends a built-in\n\"Highlight reconcile changes\" toggle that drives the\n<a href=\"#overlays\">overlay</a> subsystem.</p>\n<p>When the gate is closed, the factory returns <code>Empty()</code> <em>and</em> never\ninvokes the <code>items</code> lambda — every menu construction inside the\nlambda is skipped at retail cost of one bool check. That asymmetry\nmatters: callers can freely allocate <code>Observable&#x3C;bool></code> fields,\nconstruct <code>MenuItem</code> records, and read state from the surrounding\ncomponent without paying for any of it when the app ships without\nthe gate flipped.</p>\n<h2 id=\"overlays\">Overlays<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#overlays\">#</a></h2>\n<p>The reconcile-highlight overlay and the layout-cost overlay both\nattach to a running app when the gate is open. They're driven by\n<code>ReactorFeatureFlags.HighlightReconcileChanges</code> and\n<code>ReactorFeatureFlags.LayoutCostOverlay</code>, both of which are simple\n<code>static bool</code> properties the dev menu toggles. The overlays\nthemselves are stateless wrappers that subscribe to the\n<a href=\"/perf-instrumentation/\">ETW provider</a> only while their flag is\ntrue — the listener registration is the consuming cost. Toggle the\nflag back off and the subscription is dropped on the next render\ncycle.</p>\n<p>The reconcile overlay paints a brief flash on every element the\nreconciler patched on the most recent render; the layout-cost\noverlay tints elements by their last measure / arrange time\nsourced from the\n<a href=\"/perf-instrumentation/\"><code>Microsoft-UI-Reactor</code></a> ETW provider's\nreconcile / layout events. Neither one allocates per frame when\nits flag is off.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"adding-a-custom-mcp-tool\">Adding a custom MCP tool<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#adding-a-custom-mcp-tool\">#</a></h3>\n<p>The MCP tool surface is open for extension at host bring-up. Any\ncode with access to the <code>DevtoolsMcpServer</code> instance can call\n<code>server.Tools.Register(...)</code>. The shape is a descriptor (name,\ndescription, JSON input schema) plus a handler delegate that\nreceives the parsed <code>params</code> element and returns a JSON-serializable\nresult:</p>\n<pre><code class=\"hljs language-csharp\">server.Tools.Register(\n    <span class=\"hljs-keyword\">new</span> McpToolDescriptor(\n        Name: <span class=\"hljs-string\">\"appStats\"</span>,\n        Description: <span class=\"hljs-string\">\"Returns the running app's render and reconcile counters.\"</span>,\n        InputSchema: <span class=\"hljs-keyword\">new</span> { type = <span class=\"hljs-string\">\"object\"</span>, properties = <span class=\"hljs-keyword\">new</span> { }, additionalProperties = <span class=\"hljs-literal\">false</span> }),\n    _ => <span class=\"hljs-keyword\">new</span>\n    {\n        renders = AppStats.RenderCount,\n        reconciles = AppStats.ReconcileCount,\n        avgRenderUs = AppStats.AvgRenderMicroseconds(),\n    });\n</code></pre>\n<p>The tool shows up in <code>tools/list</code> on the next call, with the input\nschema echoed back to agents that introspect their tool inventory.\nHandlers that touch WinUI state must run inside a\n<code>DispatcherQueue.TryEnqueue</code> block — the registry's dispatch is\nalready on the UI thread, but the handler will run on the listener\nthread <em>if</em> the registration uses the synchronous shape. See the\nexisting <code>windows.activate</code> registration in\n<code>src/Reactor/Hosting/Devtools/DevtoolsTools.cs</code> for the dispatcher\ntrampoline pattern.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"reaching-for-usedevtools-to-gate-a-render-time-decision\">Reaching for UseDevtools to gate a render-time decision<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reaching-for-usedevtools-to-gate-a-render-time-decision\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (dev, _) = UseState(UseDevtools());  <span class=\"hljs-comment\">// captures once, never refreshes</span>\n    <span class=\"hljs-keyword\">return</span> dev ? VStack(...) : VStack(...);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-built_in\">bool</span> <span class=\"hljs-title\">UseDevtools</span>(<span class=\"hljs-params\"><span class=\"hljs-keyword\">this</span> RenderContext ctx</span>)</span> =>\n    ReactorApp.DevtoolsEnabled;\n</code></pre>\n<p><code>UseDevtools()</code> is already a static read; wrapping it in\n<a href=\"/hooks/\"><code>UseState</code></a> gains nothing and loses the contract: the\nstate cell only captures the value once and ignores subsequent\nsession changes. Just call <code>UseDevtools()</code> inline at the branch\npoint — it's a <code>static readonly bool</code> read, allocation-free, and\nthe value doesn't change for the life of the session anyway.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>The lockfile is the discovery contract.</strong> When an agent or CLI\ncan't find a running app, the first thing to check is whether the\nlockfile exists at <code>%TEMP%/reactor-devtools/</code>. Missing means the\nsession didn't pass the build-time gate. Present but stale means\nthe previous session crashed; deleting the file unblocks the next\nlaunch.</p>\n<p><strong><code>Observable&#x3C;bool></code> is the dev-flag pattern.</strong> Plain <code>static bool</code>\nfields don't notify, so toggling them from the dev menu doesn't\nre-render components that observe them. The\n<a href=\"/dev-tooling/\">devtools-menu</a> cookbook in <code>dev-tooling.md</code> uses\n<code>static readonly Observable&#x3C;bool></code> fields exactly so the\n<a href=\"/hooks/\"><code>UseObservable</code></a> hook can pick up changes.</p>\n<p><strong>Tools that mutate must hop the dispatcher.</strong> The MCP server\ndispatch lands on the UI thread by default through the\n<code>DispatcherQueue.TryEnqueue</code> call in the dispatcher, but custom\nhandlers registered via <code>server.Tools.Register</code> are responsible\nfor making sure any <a href=\"/reconciliation/\">reconciler</a>-touching\ncode path runs on the same thread.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — User-facing devtools API and the inner-loop story.</li>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — ETW provider the overlays consume.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Why <code>UseDevtools</code> doesn't consume a slot.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — How the reconcile-highlight overlay observes patches.</li>\n<li><strong><a href=\"/focus-and-input-internals/\">Focus and Input Internals</a></strong> — Previous under-the-hood page.</li>\n</ul>",
      "text": "The Microsoft.UI.Reactor (Reactor) devtools surface is two subsystems that share a single gate. The MCP server speaks JSON-RPC over either HTTP loopback or stdio, exposing a fixed inventory of tools ( tools/list , tools/call ) that an external CLI, editor, or agent harness drives against the running app. The in-app dev menu — DevtoolsMenu(...) , the keyboard shortcuts, the reconcile-highlight overlay, the layout-cost overlay — runs entirely in-process, observes the same component tree the user sees, and is gated by the same UseDevtools() flag. Both subsystems are zero-cost in retail: the gate evaluates a static readonly bool , and code paths gated behind it never construct elements or register ETW listeners. The most common mistake is reaching for a devtools modifier outside the gate; in retail it's still a null -returning factory and contributes nothing visual, but the code path still allocates whatever the surrounding component constructed to pass to it. Gate the construction, not just the rendering. DevTools Internals # This page covers the internals of Reactor's devtools — the MCP server that external agents talk to, the in-app dev menu, the runtime overlays, and the gate that keeps the whole subsystem out of retail. Dev Tooling is the user-facing companion; this is the internals view. The MCP loop # The MCP server is a loopback HttpListener (or a stdio reader/writer loop) running inside the host process. On startup it picks a free port, generates a per-launch bearer token, and writes a lockfile under %TEMP%/reactor-devtools/&#x3C;hash>.json advertising the endpoint, the port, the pid, the build tag, and the token. The CLI reads that lockfile to find the running app — there's no service registry, no broadcast discovery, just a file in tempdir keyed by the project's canonicalized path. Any process running as the same user can read the lockfile, present the token, and call tools. Reference # Subsystem Owner type Source Gate Gate hook UseDevtools() src/Reactor/Hooks/UseDevtools.cs Build-time devtools: true AND session-time --devtools app MCP server DevtoolsMcpServer src/Reactor/Hosting/Devtools/DevtoolsMcpServer.cs Bearer token + project lockfile Tool registry McpToolRegistry src/Reactor/Hosting/Devtools/McpToolRegistry.cs Per-tool input-schema validation JSON-RPC dispatch McpDispatcher src/Reactor/Hosting/Devtools/McpDispatcher.cs Method allowlist + tools/call routing CLI client McpCliClient src/Reactor.Cli/Devtools/McpCliClient.cs Reads lockfile, posts bearer-authed JSON-RPC In-app menu DevtoolsMenu(...) factory src/Reactor/Hosting/Devtools/DevtoolsMenuFactory.cs UseDevtools() — returns Empty() when off Overlays ReactorFeatureFlags src/Reactor/Core/ Toggle binds an ETW listener only when on The gate # public static bool UseDevtools ( this RenderContext ctx ) => ReactorApp.DevtoolsEnabled; UseDevtools() returns ReactorApp.DevtoolsEnabled — the AND of two independent signals captured at process startup. The build-time signal is the devtools: true argument to ReactorApp.Run&#x3C;TRoot>(...) ; ship a release binary without it and the AND is false no matter what flags the user passes. The session-time signal is the --devtools app (or --devtools run ) command-line argument the user supplies. Both must hold for the gate to open. The value is frozen for the session and the hook deliberately does not consume a slot in the hook table . That's why UseDevtools() can be called from helpers that aren't part of the render path — there's no positional invariant to violate. The trade-off: the value can't change at runtime. Flipping ReactorApp.DevtoolsEnabled mid-session would produce a stale gate read in every component already rendered. Caveat: Calling UseDevtools() does not opt the component into observing the flag — the flag is frozen for the session, so observation isn't needed. But code inside the dev ? DebugOverlay() : null branch is still evaluated when the gate is open, even if your overlay never displays anything user-visible. A DebugOverlay() factory that constructs 200 children to display in a dev-only data panel is 200 allocations per render whenever devtools are enabled, including during automated UI testing that happens to launch with --devtools app . Gate construction, not just display. The CLI client # internal sealed class McpCliClient : IDisposable { private readonly HttpClient _http; private readonly string _endpoint; private readonly string ? _token; public McpCliClient ( string endpoint, TimeSpan? timeout = null , string ? token = null ) { _endpoint = endpoint; _token = token; _http = new HttpClient { Timeout = timeout ?? TimeSpan.FromSeconds( 30 ) }; if (! string .IsNullOrEmpty(token)) { _http.DefaultRequestHeaders.Authorization = new global ::System.Net.Http.Headers.AuthenticationHeaderValue( \"Bearer\" , token); } } McpCliClient is the CLI-side terminus of the MCP loop. It takes an endpoint URL, an optional bearer token, and an optional timeout, and exposes InvokeTool and InvokeMethod as the two entry points; the CLI's verb commands ( mur devtools components , mur devtools click , …) all layer on top of InvokeTool with the tool name baked in. The token comes from the lockfile the server wrote on startup, so the discovery flow is open-the-lockfile → read endpoint + token → construct client. The Authorization: Bearer &#x3C;token> header is the only thing standing between an attacker on localhost and the running app's MCP surface — the lockfile sits in the user's tempdir so any process running as the user can read it, but processes running as other users can't. Endpoint discovery # The lockfile under %TEMP%/reactor-devtools/&#x3C;hash>.json carries the endpoint URL, the per-launch bearer token, the pid (for liveness probing), the build tag, the transport ( http or stdio ), and the project path. The path hash is a truncated SHA-256 of the canonicalized .csproj path — C:\\foo\\bar.csproj and c:/foo/bar.csproj collide deliberately, so re-launching the same project from a different shell finds the existing session rather than starting a second one. The CLI's mur devtools serve command checks for a live lockfile before binding a new port; if one exists and the process is still alive, the second invocation exits with the running session's information instead of starting a new server. A lockfile is stale when the recorded pid doesn't match a running process. The CLI deletes stale lockfiles on read; the running server doesn't proactively re-write its lockfile, but it also doesn't need to — the file's pid field is the only check that matters, and the pid doesn't change for the life of the session. Tool dispatch — the UI-thread hop # private object ? HandleCall(JsonElement? @params) { if (@params is not { } p || p.ValueKind != JsonValueKind.Object) throw new McpToolException( \"tools/call params must be an object with { name, arguments? }.\" , JsonRpcErrorCodes.InvalidParams); if (!p.TryGetProperty( \"name\" , out var nameEl) || nameEl.ValueKind != JsonValueKind.String) throw new McpToolException( \"tools/call requires a string 'name' field.\" , JsonRpcErrorCodes.InvalidParams); var name = nameEl.GetString()!; JsonElement? args = p.TryGetProperty( \"arguments\" , out var argsEl) ? argsEl : null ; return Invoke(name, args ); } Tool handlers run on the UI dispatcher, not on the HTTP listener's worker thread. The MCP server takes the parsed request, looks up the handler in the McpToolRegistry , and posts the handler call through DispatcherQueue.TryEnqueue . That hop is what makes it safe for a handler to query the live WinUI tree — WindowRegistry.Snapshot() , host.Mount(new T()) , every property read on a FrameworkElement — all of which assume the calling thread is the one that owns the visual tree. Without the hop, the handler would race against the UI thread and either throw COMException or produce a stale snapshot. The dispatch path is the same for HTTP and stdio. The two transports diverge only at the read boundary ( HttpListenerContext vs StreamReader ) and the write boundary ( HttpListenerResponse vs StreamWriter ). Everything between — JSON-RPC parsing, the McpDispatcher.Dispatch(body) call, the dispatcher hop, the tool handler — is shared code. The in-app menu # DevtoolsMenu(...) is the in-app counterpart to the MCP server: a titlebar widget that shows up only when UseDevtools() is true and hosts a flyout of MenuFlyoutItemBase items the host app supplies. The pattern is static readonly Observable&#x3C;bool> fields in an AppFlags class, mutated by ToggleMenuItem callbacks, observed by components via ctx.UseObservable(AppFlags.DebugUI).Value . The DevtoolsMenu factory also appends a built-in \"Highlight reconcile changes\" toggle that drives the overlay subsystem. When the gate is closed, the factory returns Empty() and never invokes the items lambda — every menu construction inside the lambda is skipped at retail cost of one bool check. That asymmetry matters: callers can freely allocate Observable&#x3C;bool> fields, construct MenuItem records, and read state from the surrounding component without paying for any of it when the app ships without the gate flipped. Overlays # The reconcile-highlight overlay and the layout-cost overlay both attach to a running app when the gate is open. They're driven by ReactorFeatureFlags.HighlightReconcileChanges and ReactorFeatureFlags.LayoutCostOverlay , both of which are simple static bool properties the dev menu toggles. The overlays themselves are stateless wrappers that subscribe to the ETW provider only while their flag is true — the listener registration is the consuming cost. Toggle the flag back off and the subscription is dropped on the next render cycle. The reconcile overlay paints a brief flash on every element the reconciler patched on the most recent render; the layout-cost overlay tints elements by their last measure / arrange time sourced from the Microsoft-UI-Reactor ETW provider's reconcile / layout events. Neither one allocates per frame when its flag is off. Patterns # Adding a custom MCP tool # The MCP tool surface is open for extension at host bring-up. Any code with access to the DevtoolsMcpServer instance can call server.Tools.Register(...) . The shape is a descriptor (name, description, JSON input schema) plus a handler delegate that receives the parsed params element and returns a JSON-serializable result: server.Tools.Register( new McpToolDescriptor( Name: \"appStats\" , Description: \"Returns the running app's render and reconcile counters.\" , InputSchema: new { type = \"object\" , properties = new { }, additionalProperties = false }), _ => new { renders = AppStats.RenderCount, reconciles = AppStats.ReconcileCount, avgRenderUs = AppStats.AvgRenderMicroseconds(), }); The tool shows up in tools/list on the next call, with the input schema echoed back to agents that introspect their tool inventory. Handlers that touch WinUI state must run inside a DispatcherQueue.TryEnqueue block — the registry's dispatch is already on the UI thread, but the handler will run on the listener thread if the registration uses the synchronous shape. See the existing windows.activate registration in src/Reactor/Hosting/Devtools/DevtoolsTools.cs for the dispatcher trampoline pattern. Common Mistakes # Reaching for UseDevtools to gate a render-time decision # // Don't: public override Element Render () { var (dev, _) = UseState(UseDevtools()); // captures once, never refreshes return dev ? VStack(...) : VStack(...); } public static bool UseDevtools ( this RenderContext ctx ) => ReactorApp.DevtoolsEnabled; UseDevtools() is already a static read; wrapping it in UseState gains nothing and loses the contract: the state cell only captures the value once and ignores subsequent session changes. Just call UseDevtools() inline at the branch point — it's a static readonly bool read, allocation-free, and the value doesn't change for the life of the session anyway. Tips # The lockfile is the discovery contract. When an agent or CLI can't find a running app, the first thing to check is whether the lockfile exists at %TEMP%/reactor-devtools/ . Missing means the session didn't pass the build-time gate. Present but stale means the previous session crashed; deleting the file unblocks the next launch. Observable&#x3C;bool> is the dev-flag pattern. Plain static bool fields don't notify, so toggling them from the dev menu doesn't re-render components that observe them. The devtools-menu cookbook in dev-tooling.md uses static readonly Observable&#x3C;bool> fields exactly so the UseObservable hook can pick up changes. Tools that mutate must hop the dispatcher. The MCP server dispatch lands on the UI thread by default through the DispatcherQueue.TryEnqueue call in the dispatcher, but custom handlers registered via server.Tools.Register are responsible for making sure any reconciler -touching code path runs on the same thread. Next Steps # Dev Tooling — User-facing devtools API and the inner-loop story. Perf Instrumentation — ETW provider the overlays consume. Hooks Internals — Why UseDevtools doesn't consume a slot. Reconciliation — How the reconcile-highlight overlay observes patches. Focus and Input Internals — Previous under-the-hood page.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/devtools-internals.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/devtools-internals.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-mcp-loop",
          "text": "The MCP loop"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "the-gate",
          "text": "The gate"
        },
        {
          "depth": 2,
          "id": "the-cli-client",
          "text": "The CLI client"
        },
        {
          "depth": 2,
          "id": "endpoint-discovery",
          "text": "Endpoint discovery"
        },
        {
          "depth": 2,
          "id": "tool-dispatch--the-ui-thread-hop",
          "text": "Tool dispatch — the UI-thread hop"
        },
        {
          "depth": 2,
          "id": "the-in-app-menu",
          "text": "The in-app menu"
        },
        {
          "depth": 2,
          "id": "overlays",
          "text": "Overlays"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "adding-a-custom-mcp-tool",
          "text": "Adding a custom MCP tool"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "reaching-for-usedevtools-to-gate-a-render-time-decision",
          "text": "Reaching for UseDevtools to gate a render-time decision"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Animation Pipeline",
      "path": "animation-pipeline.md",
      "slug": "/animation-pipeline/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s animation surface looks deceptively simple — set\n<code>.Opacity(visible ? 1 : 0)</code> and attach <code>.OpacityTransition()</code>, and the\nproperty change animates on next render. Underneath, four distinct\nsystems route into the same compositor layer: declaration-site implicit\nanimation via <code>.Animate(properties)</code>, an ambient\n<a href=\"/animation/\"><code>WithAnimation</code></a> scope that captures the reconciler's\nproperty writes and replays them through a curve, the\n<a href=\"/animation/\"><code>KeyframeBuilder</code></a> imperative timeline, and the\n<code>.Transition()</code> enter/exit machinery that runs when an element mounts\nor unmounts. All four converge on Windows Composition, which runs\nanimation interpolation on a dedicated compositor thread — separately\nfrom the UI thread, which is why a stalled <code>Render()</code> doesn't stutter a\nrunning animation. The most common mistake is reaching for an animation\non a layout property (Width, Height, Margin) and finding the animation\nstutters; those properties live in WinUI's layout system, not the\ncompositor's, so they animate at layout rate instead of frame rate.</p>\n<h1 id=\"animation-pipeline\">Animation Pipeline<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animation-pipeline\">#</a></h1>\n<p>This page maps the four authoring surfaces onto the underlying\ncompositor and explains the trade-offs that pick one surface over\nanother. The user-facing API is on the\n<a href=\"/animation/\">Animation</a> page; this is the internals view.</p>\n<h2 id=\"the-four-systems\">The four systems<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-four-systems\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/animation-pipeline/four-systems.svg\" alt=\"Reactor animation pipeline — implicit Animate, WithAnimation scope, KeyframeBuilder, and Transition enter/exit converging on Windows Composition\"></p>\n<p>Each system answers a different question. <code>.Animate(properties)</code> says\n\"any time these properties change on this element, smooth between old\nand new\"; the curve and duration are part of the element's\nconfiguration. <code>WithAnimation(curve, body)</code> says \"run <code>body</code> and route\nevery reconciler-driven property write into a transition with this\ncurve\"; the curve is ambient to the call, not attached to any element.\n<code>KeyframeBuilder</code> builds an explicit timeline you can name and trigger.\n<code>.Transition(...)</code> is for the mount / unmount edge, where the property\nchange isn't between two values at all — the element is appearing or\ndisappearing.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">WithAnimation</span>(<span class=\"hljs-params\">Curve? curve, Action action</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> prevCurve = _current;\n    <span class=\"hljs-keyword\">var</span> prevScope = _hasScope;\n    _current = curve;\n    _hasScope = <span class=\"hljs-literal\">true</span>;\n    <span class=\"hljs-keyword\">try</span> { action(); }\n    <span class=\"hljs-keyword\">finally</span> { _current = prevCurve; _hasScope = prevScope; }\n}\n</code></pre>\n<p><code>WithAnimation</code> is a <code>[ThreadStatic]</code> scope, not an element modifier.\nThat matters: it captures property writes made by <em>anything</em> running\ninside the scope, including code that runs through the\n<a href=\"/reconciliation/\">reconciler</a> on a setState callback. The\n<code>HasScope</code> flag carries through the <code>try / finally</code> so nested scopes\ncorrectly restore the outer curve when they exit. Reach for this when\nthe trigger for the animation is a state change rather than a property\ndelta — the reconciler's natural fire-after-state-change behavior is\nwhat routes the writes into the scope.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The compositor only animates a fixed set of properties cheaply:\n<code>Opacity</code>, <code>Offset</code> (translation), <code>Scale</code>, <code>Rotation</code>, and\n<code>CenterPoint</code>. Anything else — <code>Width</code>, <code>Height</code>, <code>Margin</code>,\n<code>Background</code>, <code>Foreground</code> — has to go through XAML's property system,\nwhich means a layout pass per frame. The\n<a href=\"/animation/\"><code>AnimateProperty</code></a> flag enum reflects exactly the\ncompositor-cheap set; if your animation isn't smooth, the property\nyou're animating probably isn't in it. Switch to a transform\n(scale a button down rather than animate its width) or accept that the\nanimation will hold up the UI thread.</p>\n</blockquote>\n<h2 id=\"transition-composition\">Transition composition<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#transition-composition\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">abstract</span> <span class=\"hljs-keyword\">record</span> <span class=\"hljs-title\">Transition</span>\n{\n    <span class=\"hljs-comment\">// -- Presets --</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">readonly</span> Transition Fade = <span class=\"hljs-keyword\">new</span> FadeTransition();\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-title\">Slide</span>(<span class=\"hljs-params\">Edge edge = Edge.Bottom</span>)</span> => <span class=\"hljs-keyword\">new</span> SlideTransition(edge);\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-title\">Scale</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">float</span> <span class=\"hljs-keyword\">from</span> = <span class=\"hljs-number\">0.85f</span></span>)</span> => <span class=\"hljs-keyword\">new</span> ScaleTransition(<span class=\"hljs-keyword\">from</span>);\n\n    <span class=\"hljs-comment\">// -- Asymmetric factory --</span>\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-title\">Enter</span>(<span class=\"hljs-params\">Transition enter</span>)</span> => <span class=\"hljs-keyword\">new</span> DirectionalTransition(enter, <span class=\"hljs-literal\">null</span>);\n    <span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-title\">Exit</span>(<span class=\"hljs-params\">Transition exit</span>)</span> => <span class=\"hljs-keyword\">new</span> DirectionalTransition(<span class=\"hljs-literal\">null</span>, exit);\n\n    <span class=\"hljs-comment\">// -- Combinators --</span>\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Combine two transitions to play in parallel (e.g., Fade + Slide).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-keyword\">operator</span> +(Transition a, Transition b) => <span class=\"hljs-keyword\">new</span> CombinedTransition(a, b);\n\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Asymmetric: left side is enter, right side is exit.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> Transition <span class=\"hljs-keyword\">operator</span> |(Transition enter, Transition exit)\n        => <span class=\"hljs-keyword\">new</span> AsymmetricTransition(enter, exit);\n}\n</code></pre>\n<p><code>Transition</code> is the enter/exit machinery — what runs when an element\nappears in or disappears from the tree on a re-render. The composition\noperators are the API: <code>Fade + Slide(Bottom)</code> runs both in parallel,\n<code>Fade | Scale(0.85f)</code> is asymmetric (<code>|</code> reads as \"enter | exit\"). The\ncombinators produce record types (<code>CombinedTransition</code>,\n<code>AsymmetricTransition</code>); the reconciler walks the record graph at\nmount and unmount time and translates each leaf into a\n<code>CompositionAnimation</code> that targets the underlying Visual.</p>\n<p>The mount path attaches <code>Fade.Enter</code> to the new element's Visual after\n<a href=\"/reconciliation/\">the reconciler</a> creates it, runs the animation, and\nclears the override once it completes. The unmount path is the inverse\nand is the trickier of the two: WinUI normally removes a child from\nits panel immediately on detach, which would kill the exit animation\nmid-flight. Reactor holds the element in a \"leaving\" pseudo-state\nuntil the exit transition's <code>CompositionScopedBatch</code> reports\ncompletion, then commits the detach. State changes that fire while an\nelement is leaving go to the new tree, never the leaving one — the\nleaving element is read-only.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Surface</th><th>Owner type</th><th>When to use</th><th>Compositor thread?</th></tr></thead><tbody><tr><td><code>.Animate(props, curve)</code></td><td><code>AnimationConfig</code></td><td>Stable element, animate a property delta on every change.</td><td>Yes (per-property triggers)</td></tr><tr><td><code>WithAnimation(curve, body)</code></td><td><code>AnimationScope</code></td><td>The change is driven by <code>setState</code>, animate whatever the reconciler writes.</td><td>Yes (writes route through scope)</td></tr><tr><td><code>Keyframes(name, b => ...)</code></td><td><code>KeyframeBuilder</code></td><td>Named, repeatable timeline triggered imperatively.</td><td>Yes (explicit <code>KeyFrameAnimation</code>)</td></tr><tr><td><code>.Transition(t)</code></td><td><code>ElementTransition</code></td><td>The element is appearing / disappearing on a render.</td><td>Yes (mount-time + unmount-leave hold)</td></tr></tbody></table>\n<p><code>AnimationScope.HasScope</code> distinguishes \"no scope\" from \"explicit\nsuppression\": passing <code>null</code> as the curve marks the scope active but\ndisables animation, useful for one-off explicit writes inside an outer\nanimated scope.</p>\n<h2 id=\"keyframe-authoring\">Keyframe authoring<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keyframe-authoring\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> KeyframeBuilder <span class=\"hljs-title\">At</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">float</span> progress,\n    <span class=\"hljs-built_in\">float</span>? opacity = <span class=\"hljs-literal\">null</span>, Vector3? scale = <span class=\"hljs-literal\">null</span>,\n    Vector3? translation = <span class=\"hljs-literal\">null</span>, <span class=\"hljs-built_in\">float</span>? rotation = <span class=\"hljs-literal\">null</span>,\n    Easing? easing = <span class=\"hljs-literal\">null</span></span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (progress &#x3C; <span class=\"hljs-number\">0f</span> || progress > <span class=\"hljs-number\">1f</span>)\n        <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> ArgumentOutOfRangeException(<span class=\"hljs-keyword\">nameof</span>(progress), progress, <span class=\"hljs-string\">\"Progress must be between 0.0 and 1.0.\"</span>);\n\n    _keyframes.Add(<span class=\"hljs-keyword\">new</span> KeyframeDef(progress)\n    {\n        Opacity = opacity,\n        Scale = scale,\n        Translation = translation,\n        Rotation = rotation,\n        Easing = easing,\n    });\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>;\n}\n</code></pre>\n<p><a href=\"/animation/\"><code>KeyframeBuilder.At(progress, ...)</code></a> is the per-frame\nentry point — <code>progress</code> is normalized 0.0 to 1.0 across the\nanimation's <code>Duration</code>. Each property argument is optional, so a\nkeyframe that only sets <code>Opacity</code> leaves <code>Scale</code> and the rest at their\ninterpolated values. The internal <code>KeyframeDef</code> record carries the\noptional easing override for that segment; when omitted, the segment\ninherits the animation's default curve.</p>\n<p><code>KeyframeBuilder</code> produces a <code>KeyframeAnimationDef</code> — pure data, no\ncompositor types. The reconciler is the layer that turns the def into a\n<code>KeyFrameAnimation</code> and calls <code>Visual.StartAnimation</code> on the materialized\nelement. Building the def in component code rather than building a\n<code>CompositionAnimation</code> directly means the same definition can flow\nthrough serialization, hot reload, or a future declarative\nkeyframe authoring API without touching the compositor binding.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"animating-an-entire-conditional-subtree\">Animating an entire conditional subtree<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animating-an-entire-conditional-subtree\">#</a></h3>\n<p>When a state flag toggles a chunk of UI, reach for <code>.Transition()</code>\non the wrapping element rather than per-property animations on each\nchild. The element's enter / exit drive the whole subtree's\nappearance through one transition spec:</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (show, setShow) = UseState(<span class=\"hljs-literal\">false</span>);\n    <span class=\"hljs-keyword\">return</span> VStack(\n        Button(show ? <span class=\"hljs-string\">\"Hide\"</span> : <span class=\"hljs-string\">\"Show\"</span>, () => setShow(!show)),\n        show\n            ? Card(Text(<span class=\"hljs-string\">\"Hello\"</span>)).Transition(Transition.Fade + Transition.Slide(Edge.Top))\n            : <span class=\"hljs-literal\">null</span>\n    );\n}\n</code></pre>\n<p>The reconciler treats the <code>null</code> branch as an unmount and the\n<code>Card(...)</code> branch as a mount; the <code>.Transition</code> modifier on the\n<code>Card</code> is what tells it to play the enter combination on mount and\nthe symmetric exit on unmount. The\n<a href=\"/reconciliation/\">reconciliation</a> page documents the\nmount / leave / unmount state machine in detail.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"animating-a-layout-property-and-expecting-compositor-smoothness\">Animating a layout property and expecting compositor smoothness<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#animating-a-layout-property-and-expecting-compositor-smoothness\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-keyword\">return</span> Border(child)\n    .Width(expanded ? <span class=\"hljs-number\">400</span> : <span class=\"hljs-number\">100</span>)        <span class=\"hljs-comment\">// layout property, not compositor</span>\n    .Animate(AnimateProperty.All);      <span class=\"hljs-comment\">// no effect on Width</span>\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">WithAnimation</span>(<span class=\"hljs-params\">Curve? curve, Action action</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> prevCurve = _current;\n    <span class=\"hljs-keyword\">var</span> prevScope = _hasScope;\n    _current = curve;\n    _hasScope = <span class=\"hljs-literal\">true</span>;\n    <span class=\"hljs-keyword\">try</span> { action(); }\n    <span class=\"hljs-keyword\">finally</span> { _current = prevCurve; _hasScope = prevScope; }\n}\n</code></pre>\n<p><code>.Animate(AnimateProperty.All)</code> only triggers implicit animations on\nthe five compositor properties (<code>Opacity</code>, <code>Offset</code>, <code>Scale</code>,\n<code>Rotation</code>, <code>CenterPoint</code>). <code>Width</code> is a XAML layout property — to\nanimate it, the panel measures and arranges every frame, on the UI\nthread. The correct shape is to express the size change as a scale\ntransform on a fixed-size child, or to gate the change behind a\n<code>WithAnimation</code> scope so at least the property write coalesces across\nthe dispatcher hop.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Five properties animate cheaply, the rest don't.</strong> The compositor\nanimates <code>Opacity</code>, <code>Offset</code>, <code>Scale</code>, <code>Rotation</code>, and <code>CenterPoint</code>\non its own thread — anything else takes the layout path. When picking\nbetween an authoring shape, prefer one that targets the cheap set\n(a scaled button instead of a resized button, a translated panel\ninstead of a margined panel).</p>\n<p><strong><code>Curve.Spring</code> is the Reactor default.</strong> A spring curve has no\nexplicit duration — it settles when the physics says it does, which\ntends to feel more responsive than a fixed-duration ease. Reach for\n<code>Curve.Ease(durationMs)</code> only when the duration is part of the\ndesign contract (e.g. matching a toast that times out after 400ms).</p>\n<p><strong>One scope per state cascade.</strong> When a single setState fires three\nproperty writes that should all animate together, wrap the work in\none <code>WithAnimation</code> scope rather than attaching three <code>.Animate(...)</code>\nmodifiers. The single scope keeps the writes coherent across the\n<a href=\"/reconciliation/\">reconciler's</a> update pass.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/animation/\">Animation</a></strong> — Surface-level animation API and ready-made transitions.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — How mount / leave / unmount drive transition lifecycle.</li>\n<li><strong><a href=\"/focus-and-input-internals/\">Focus and Input Internals</a></strong> — Companion under-the-hood page on input.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Why <code>setState</code> cascades animate consistently.</li>\n<li><strong><a href=\"/perf-instrumentation/\">Perf Instrumentation</a></strong> — Reading ETW events from animation frames.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s animation surface looks deceptively simple — set .Opacity(visible ? 1 : 0) and attach .OpacityTransition() , and the property change animates on next render. Underneath, four distinct systems route into the same compositor layer: declaration-site implicit animation via .Animate(properties) , an ambient WithAnimation scope that captures the reconciler's property writes and replays them through a curve, the KeyframeBuilder imperative timeline, and the .Transition() enter/exit machinery that runs when an element mounts or unmounts. All four converge on Windows Composition, which runs animation interpolation on a dedicated compositor thread — separately from the UI thread, which is why a stalled Render() doesn't stutter a running animation. The most common mistake is reaching for an animation on a layout property (Width, Height, Margin) and finding the animation stutters; those properties live in WinUI's layout system, not the compositor's, so they animate at layout rate instead of frame rate. Animation Pipeline # This page maps the four authoring surfaces onto the underlying compositor and explains the trade-offs that pick one surface over another. The user-facing API is on the Animation page; this is the internals view. The four systems # Each system answers a different question. .Animate(properties) says \"any time these properties change on this element, smooth between old and new\"; the curve and duration are part of the element's configuration. WithAnimation(curve, body) says \"run body and route every reconciler-driven property write into a transition with this curve\"; the curve is ambient to the call, not attached to any element. KeyframeBuilder builds an explicit timeline you can name and trigger. .Transition(...) is for the mount / unmount edge, where the property change isn't between two values at all — the element is appearing or disappearing. public static void WithAnimation ( Curve? curve, Action action ) { var prevCurve = _current; var prevScope = _hasScope; _current = curve; _hasScope = true ; try { action(); } finally { _current = prevCurve; _hasScope = prevScope; } } WithAnimation is a [ThreadStatic] scope, not an element modifier. That matters: it captures property writes made by anything running inside the scope, including code that runs through the reconciler on a setState callback. The HasScope flag carries through the try / finally so nested scopes correctly restore the outer curve when they exit. Reach for this when the trigger for the animation is a state change rather than a property delta — the reconciler's natural fire-after-state-change behavior is what routes the writes into the scope. Caveat: The compositor only animates a fixed set of properties cheaply: Opacity , Offset (translation), Scale , Rotation , and CenterPoint . Anything else — Width , Height , Margin , Background , Foreground — has to go through XAML's property system, which means a layout pass per frame. The AnimateProperty flag enum reflects exactly the compositor-cheap set; if your animation isn't smooth, the property you're animating probably isn't in it. Switch to a transform (scale a button down rather than animate its width) or accept that the animation will hold up the UI thread. Transition composition # public abstract record Transition { // -- Presets -- public static readonly Transition Fade = new FadeTransition(); public static Transition Slide ( Edge edge = Edge.Bottom ) => new SlideTransition(edge); public static Transition Scale ( float from = 0.85f ) => new ScaleTransition( from ); // -- Asymmetric factory -- public static Transition Enter ( Transition enter ) => new DirectionalTransition(enter, null ); public static Transition Exit ( Transition exit ) => new DirectionalTransition( null , exit); // -- Combinators -- /// &#x3C;summary> Combine two transitions to play in parallel (e.g., Fade + Slide). &#x3C;/summary> public static Transition operator +(Transition a, Transition b) => new CombinedTransition(a, b); /// &#x3C;summary> Asymmetric: left side is enter, right side is exit. &#x3C;/summary> public static Transition operator |(Transition enter, Transition exit) => new AsymmetricTransition(enter, exit); } Transition is the enter/exit machinery — what runs when an element appears in or disappears from the tree on a re-render. The composition operators are the API: Fade + Slide(Bottom) runs both in parallel, Fade | Scale(0.85f) is asymmetric ( | reads as \"enter | exit\"). The combinators produce record types ( CombinedTransition , AsymmetricTransition ); the reconciler walks the record graph at mount and unmount time and translates each leaf into a CompositionAnimation that targets the underlying Visual. The mount path attaches Fade.Enter to the new element's Visual after the reconciler creates it, runs the animation, and clears the override once it completes. The unmount path is the inverse and is the trickier of the two: WinUI normally removes a child from its panel immediately on detach, which would kill the exit animation mid-flight. Reactor holds the element in a \"leaving\" pseudo-state until the exit transition's CompositionScopedBatch reports completion, then commits the detach. State changes that fire while an element is leaving go to the new tree, never the leaving one — the leaving element is read-only. Reference # Surface Owner type When to use Compositor thread? .Animate(props, curve) AnimationConfig Stable element, animate a property delta on every change. Yes (per-property triggers) WithAnimation(curve, body) AnimationScope The change is driven by setState , animate whatever the reconciler writes. Yes (writes route through scope) Keyframes(name, b => ...) KeyframeBuilder Named, repeatable timeline triggered imperatively. Yes (explicit KeyFrameAnimation ) .Transition(t) ElementTransition The element is appearing / disappearing on a render. Yes (mount-time + unmount-leave hold) AnimationScope.HasScope distinguishes \"no scope\" from \"explicit suppression\": passing null as the curve marks the scope active but disables animation, useful for one-off explicit writes inside an outer animated scope. Keyframe authoring # public KeyframeBuilder At ( float progress, float ? opacity = null , Vector3? scale = null , Vector3? translation = null , float ? rotation = null , Easing? easing = null ) { if (progress &#x3C; 0f || progress > 1f ) throw new ArgumentOutOfRangeException( nameof (progress), progress, \"Progress must be between 0.0 and 1.0.\" ); _keyframes.Add( new KeyframeDef(progress) { Opacity = opacity, Scale = scale, Translation = translation, Rotation = rotation, Easing = easing, }); return this ; } KeyframeBuilder.At(progress, ...) is the per-frame entry point — progress is normalized 0.0 to 1.0 across the animation's Duration . Each property argument is optional, so a keyframe that only sets Opacity leaves Scale and the rest at their interpolated values. The internal KeyframeDef record carries the optional easing override for that segment; when omitted, the segment inherits the animation's default curve. KeyframeBuilder produces a KeyframeAnimationDef — pure data, no compositor types. The reconciler is the layer that turns the def into a KeyFrameAnimation and calls Visual.StartAnimation on the materialized element. Building the def in component code rather than building a CompositionAnimation directly means the same definition can flow through serialization, hot reload, or a future declarative keyframe authoring API without touching the compositor binding. Patterns # Animating an entire conditional subtree # When a state flag toggles a chunk of UI, reach for .Transition() on the wrapping element rather than per-property animations on each child. The element's enter / exit drive the whole subtree's appearance through one transition spec: public override Element Render () { var (show, setShow) = UseState( false ); return VStack( Button(show ? \"Hide\" : \"Show\" , () => setShow(!show)), show ? Card(Text( \"Hello\" )).Transition(Transition.Fade + Transition.Slide(Edge.Top)) : null ); } The reconciler treats the null branch as an unmount and the Card(...) branch as a mount; the .Transition modifier on the Card is what tells it to play the enter combination on mount and the symmetric exit on unmount. The reconciliation page documents the mount / leave / unmount state machine in detail. Common Mistakes # Animating a layout property and expecting compositor smoothness # // Don't: return Border(child) .Width(expanded ? 400 : 100 ) // layout property, not compositor .Animate(AnimateProperty.All); // no effect on Width public static void WithAnimation ( Curve? curve, Action action ) { var prevCurve = _current; var prevScope = _hasScope; _current = curve; _hasScope = true ; try { action(); } finally { _current = prevCurve; _hasScope = prevScope; } } .Animate(AnimateProperty.All) only triggers implicit animations on the five compositor properties ( Opacity , Offset , Scale , Rotation , CenterPoint ). Width is a XAML layout property — to animate it, the panel measures and arranges every frame, on the UI thread. The correct shape is to express the size change as a scale transform on a fixed-size child, or to gate the change behind a WithAnimation scope so at least the property write coalesces across the dispatcher hop. Tips # Five properties animate cheaply, the rest don't. The compositor animates Opacity , Offset , Scale , Rotation , and CenterPoint on its own thread — anything else takes the layout path. When picking between an authoring shape, prefer one that targets the cheap set (a scaled button instead of a resized button, a translated panel instead of a margined panel). Curve.Spring is the Reactor default. A spring curve has no explicit duration — it settles when the physics says it does, which tends to feel more responsive than a fixed-duration ease. Reach for Curve.Ease(durationMs) only when the duration is part of the design contract (e.g. matching a toast that times out after 400ms). One scope per state cascade. When a single setState fires three property writes that should all animate together, wrap the work in one WithAnimation scope rather than attaching three .Animate(...) modifiers. The single scope keeps the writes coherent across the reconciler's update pass. Next Steps # Animation — Surface-level animation API and ready-made transitions. Reconciliation — How mount / leave / unmount drive transition lifecycle. Focus and Input Internals — Companion under-the-hood page on input. Hooks Internals — Why setState cascades animate consistently. Perf Instrumentation — Reading ETW events from animation frames.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/animation-pipeline.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/animation-pipeline.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-four-systems",
          "text": "The four systems"
        },
        {
          "depth": 2,
          "id": "transition-composition",
          "text": "Transition composition"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "keyframe-authoring",
          "text": "Keyframe authoring"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "animating-an-entire-conditional-subtree",
          "text": "Animating an entire conditional subtree"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "animating-a-layout-property-and-expecting-compositor-smoothness",
          "text": "Animating a layout property and expecting compositor smoothness"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Focus and Input Internals",
      "path": "focus-and-input-internals.md",
      "slug": "/focus-and-input-internals/",
      "html": "<p>Microsoft.UI.Reactor (Reactor)'s focus and input surfaces are thin wrappers over WinUI's\nrouted-event system — every pointer event, every key press, every\ngesture starts in WinUI's hit-test, walks the visual tree as a routed\nevent, and lands at a Reactor element modifier like\n<a href=\"/input-and-gestures/\"><code>.OnTapped</code></a> or <a href=\"/input-and-gestures/\"><code>.OnKeyDown</code></a>.\nThree subsystems sit alongside the event flow. <code>UseFocusTrap</code> rides\non <code>LosingFocus</code> to cancel focus changes that would leave the trap\ncontainer. <code>FocusManager</code> registers fields and drives\n<a href=\"/forms/\">enter-to-advance</a> on form input. <code>UseAnnounce</code> takes a\nshortcut around focus entirely — it raises an automation-peer\nnotification, not a focus event, so the announcement reaches a screen\nreader even when nothing visible changes. The most common mistake is\ntreating focus as a property you set: <code>control.Focus(...)</code> <em>requests</em>\nfocus, and the request loses to anything else competing on the same\ndispatcher tick. Route programmatic focus through <code>FocusManager</code> so\nthe request runs at the right point in the lifecycle.</p>\n<h1 id=\"focus-and-input-internals\">Focus and Input Internals<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-and-input-internals\">#</a></h1>\n<p>This page covers the input subsystem from the routed-event boundary\nthrough the Reactor hooks that observe it. User-facing API is on\n<a href=\"/input-and-gestures/\">Input and Gestures</a> and\n<a href=\"/accessibility/\">Accessibility</a>; this is the internals view.</p>\n<h2 id=\"the-event-flow\">The event flow<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-event-flow\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/focus-and-input-internals/focus-flow.svg\" alt=\"Focus and input flow — pointer events through WinUI hit-test, routed-event delivery to element callbacks, focus-trap interception, programmatic FocusManager, and the UseAnnounce automation peer path\"></p>\n<p>WinUI owns the early stages: hit-testing under the pointer, the\ntunneling preview events, the bubbling action events. By the time a\nReactor modifier sees an event, WinUI has already picked the target\nand tunneled / bubbled the event through the visual tree. Reactor's\njob at that boundary is two things — wire a <code>RoutedEventHandler</code> to\nthe materialized control during the reconciler's mount pass, and\nattach the Reactor element's user callback to that handler. There's\nno parallel event tree; the routes you see in\n<a href=\"/input-and-gestures/\"><code>.OnPointerPressed</code></a> are the WinUI routes,\nplus a small layer that translates <code>RoutedEventArgs</code> into Reactor's\nevent-args records.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Surface</th><th>Hook / type</th><th>Wired to</th><th>Primary use</th></tr></thead><tbody><tr><td>Pointer event</td><td><code>.OnPointerPressed(...)</code></td><td>WinUI <code>PointerPressed</code> routed event</td><td>Low-level pointer (mouse / pen / touch)</td></tr><tr><td>Tap</td><td><code>.OnTapped(...)</code></td><td>WinUI <code>Tapped</code> routed event</td><td>Discrete click / tap with WinUI's gesture recognizer</td></tr><tr><td>Pan / pinch / rotate</td><td><code>.OnPan(...)</code></td><td>WinUI <code>Manipulation*</code> events</td><td>Continuous gestures with phase machine</td></tr><tr><td>Focus management</td><td><code>UseFocus()</code> → <code>FocusManager</code></td><td><code>control.Focus(Programmatic)</code></td><td>Form field ordering + enter-to-advance</td></tr><tr><td>Focus trap</td><td><code>UseFocusTrap(active)</code></td><td><code>UIElement.LosingFocus</code></td><td>Modal / flyout containment</td></tr><tr><td>Live region</td><td><code>UseAnnounce()</code></td><td><code>FrameworkElementAutomationPeer.RaiseNotificationEvent</code></td><td>Screen-reader announcements</td></tr><tr><td>Element focus</td><td><code>UseElementFocus()</code></td><td><code>Control.Focus()</code> via dispatcher</td><td>Imperative focus on a single element</td></tr><tr><td>Window-focus revalidation</td><td><code>FocusRevalidationService</code></td><td><code>Window.Activated</code></td><td>Refetch stale <a href=\"/hooks/\"><code>UseResource</code></a> data</td></tr></tbody></table>\n<h2 id=\"the-focusmanager\">The FocusManager<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-focusmanager\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">FocusNext</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span>? currentField = <span class=\"hljs-literal\">null</span></span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (_fieldOrder.Count == <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (currentField <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n    {\n        FocusField(_fieldOrder[<span class=\"hljs-number\">0</span>]);\n        <span class=\"hljs-keyword\">return</span>;\n    }\n\n    <span class=\"hljs-keyword\">var</span> index = _fieldOrder.IndexOf(currentField);\n    <span class=\"hljs-keyword\">if</span> (index &#x3C; <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (index + <span class=\"hljs-number\">1</span> &#x3C; _fieldOrder.Count)\n    {\n        FocusField(_fieldOrder[index + <span class=\"hljs-number\">1</span>]);\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        _onSubmit?.Invoke();\n    }\n}\n</code></pre>\n<p><a href=\"/forms/\"><code>FocusManager</code></a> is the form-side companion to WinUI's\nfocus system. Components call <code>useFocus.Register(name)</code> on each input\nduring render, and the manager keeps a <code>List&#x3C;string></code> of field names\nin order. <code>FocusNext</code> and <code>FocusPrevious</code> walk that list — the\ncommon pattern is <code>&#x3C;TextBox>.OnEnter(() => fm.FocusNext(name))</code> so\npressing Enter on a field advances to the next one, and Enter on the\nlast field calls the registered submit handler. The <code>_controls</code>\ndictionary maps each field name to the live WinUI <code>Control</code>; when\n<a href=\"/forms/\"><code>.Set(\"name\")</code></a> runs on mount, it stashes the control in\nthe manager. Programmatic focus calls\n<code>control.Focus(FocusState.Programmatic)</code>, which is the WinUI\nmechanism for \"I, the app, decided to move focus\" rather than a\nuser-initiated tab.</p>\n<blockquote>\n<p><strong>Caveat:</strong> <code>Register</code> is called in render order, on every render. That means\nadding a field at index 1 (between two existing fields) requires a\nclean re-register cycle — the manager's <code>Contains</code> check makes\nre-registration idempotent for existing names, but it can't reorder.\nFor dynamic forms that reorder fields, call <code>Clear()</code> at the top of\nthe render and re-register; for forms with conditional fields,\nprefer rendering all fields and toggling <code>.IsVisible</code> over removing\nthem from the tree, because removal also un-registers focus.</p>\n</blockquote>\n<h2 id=\"the-focus-trap\">The focus trap<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-focus-trap\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">private</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">OnLosingFocus</span>(<span class=\"hljs-params\">UIElement sender, LosingFocusEventArgs <span class=\"hljs-keyword\">args</span></span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (!_isActive || _container <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// SECURITY (TASK-100): refuse to trap when the container has fallen</span>\n    <span class=\"hljs-comment\">// out of the visual state where trapping is sensible. Without these</span>\n    <span class=\"hljs-comment\">// gates, a hidden / collapsed / unloaded container wedges keyboard</span>\n    <span class=\"hljs-comment\">// navigation: the user can't focus anything else and Esc / Tab</span>\n    <span class=\"hljs-comment\">// bounce back uselessly.</span>\n    <span class=\"hljs-keyword\">if</span> (_container <span class=\"hljs-keyword\">is</span> FrameworkElement fe)\n    {\n        <span class=\"hljs-keyword\">if</span> (!fe.IsLoaded) <span class=\"hljs-keyword\">return</span>;\n        <span class=\"hljs-keyword\">if</span> (fe.Visibility != Visibility.Visible) <span class=\"hljs-keyword\">return</span>;\n    }\n    <span class=\"hljs-keyword\">if</span> (!_container.IsHitTestVisible) <span class=\"hljs-keyword\">return</span>;\n</code></pre>\n<p><code>UseFocusTrap</code> lives on the <code>LosingFocus</code> routed event. When the\ntrap is active, the handler runs before WinUI commits the focus\nchange, walks from the proposed new-focus element back up the visual\ntree, and cancels the change if the target isn't a descendant of the\ntrap container. That's the trap. The four gates at the top of the\nsnippet are what make it safe — without them, a trap attached to a\ncontainer that has since become hidden, unloaded, or collapsed\nwedges the user's keyboard: every Tab keystroke loses focus, the\ntrap cancels every loss, and focus has no valid target inside the\ntrap either. Each guard names a specific failure mode the trap has\ncaused in the past.</p>\n<p>Cross-window navigation gets special treatment. A <code>LosingFocus</code> event\nwhose <code>NewFocusedElement</code> has a different <code>XamlRoot</code> is a different\nwindow entirely; the trap deliberately doesn't try to drag the user\nback. That matters for dialogs that open a child window — the trap\non the parent must not block focus moving into the child.</p>\n<h2 id=\"live-region-announcements\">Live-region announcements<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#live-region-announcements\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">Announce</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> message, <span class=\"hljs-built_in\">bool</span> assertive</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (_textBlock <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-comment\">// Primary path: RaiseNotificationEvent (WinUI 1.4+, best Narrator/NVDA support).</span>\n    <span class=\"hljs-keyword\">var</span> peer = FrameworkElementAutomationPeer.FromElement(_textBlock);\n    <span class=\"hljs-keyword\">if</span> (peer <span class=\"hljs-keyword\">is</span> <span class=\"hljs-keyword\">not</span> <span class=\"hljs-literal\">null</span>)\n    {\n        peer.RaiseNotificationEvent(\n            AutomationNotificationKind.ActionCompleted,\n            assertive\n                ? AutomationNotificationProcessing.ImportantAll\n                : AutomationNotificationProcessing.ImportantMostRecent,\n            message,\n            <span class=\"hljs-string\">\"ReactorAnnounce\"</span>);\n        <span class=\"hljs-keyword\">return</span>;\n    }\n\n    <span class=\"hljs-comment\">// Fallback: update the live-region TextBlock text. Screen readers that</span>\n    <span class=\"hljs-comment\">// monitor LiveSetting changes will pick this up.</span>\n    _textBlock.Text = message;\n}\n</code></pre>\n<p><a href=\"/accessibility/\"><code>UseAnnounce</code></a> goes around the focus path entirely.\nThe primary mechanism is <code>RaiseNotificationEvent</code> (WinUI 1.4+) on a\nhidden <code>TextBlock</code> that the handle keeps materialized. Screen readers\nhooked into UI Automation receive the notification with a processing\nhint — <code>ImportantAll</code> for assertive announcements that interrupt\ncurrent speech, <code>ImportantMostRecent</code> for polite announcements that\nqueue. The fallback path updates <code>TextBlock.Text</code> directly; screen\nreaders monitoring <code>LiveSetting</code> changes pick that up as a live-region\nupdate.</p>\n<p>The <code>Region</code> element the handle exposes is invisible, zero-sized, and\nmounts to a real <code>TextBlock</code>. Components include it once in their\ntree and call <code>handle.Announce(...)</code> from event handlers. The pattern\nmirrors WAI-ARIA's <code>aria-live</code> region: a stable element that exists\nsolely so assistive tech has a stable target for live updates.</p>\n<h2 id=\"continuous-gesture-phases\">Continuous gesture phases<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#continuous-gesture-phases\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-built_in\">enum</span> GesturePhase\n{\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Gesture has just become recognized and its parameters are stable.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    Began,\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Gesture is in progress; deltas are being reported.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    Changed,\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Gesture completed normally.<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    Ended,\n    <span class=\"hljs-comment\"><span class=\"hljs-doctag\">///</span> <span class=\"hljs-doctag\">&#x3C;summary></span>Gesture was cancelled (pointer capture lost, window focus lost, …).<span class=\"hljs-doctag\">&#x3C;/summary></span></span>\n    Cancelled,\n}\n</code></pre>\n<p>Pan, pinch, rotate, and long-press are continuous gestures — they\nfire once at <code>Began</code>, zero or more times at <code>Changed</code> as the user's\nfinger moves, and exactly once at either <code>Ended</code> (normal release) or\n<code>Cancelled</code> (pointer capture lost, escape, window deactivation). The\ncontract matters because component code routinely allocates state on\n<code>Began</code> (record the start position, snapshot the model under the\npointer) and tears it down on the terminal phase. A handler that\nskips <code>Cancelled</code> leaks the snapshot the next time the user is\nmid-gesture and switches windows.</p>\n<p>The four phases map directly onto WinUI's <code>ManipulationStarted</code> /\n<code>ManipulationDelta</code> / <code>ManipulationCompleted</code> cycle, with the\naddition of an explicit <code>Cancelled</code> phase that WinUI emits as a\ncompleted manipulation with <code>IsInertial = false</code> and a pointer\ncapture loss. The gesture wrappers\n<a href=\"/input-and-gestures/\"><code>.OnPan</code></a> / <code>.OnPinch</code> / <code>.OnRotate</code> are the\nshim that converts that pair of WinUI events into a clean Reactor\ncallback signature.</p>\n<h2 id=\"focus-revalidation\">Focus revalidation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#focus-revalidation\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> IReadOnlyList&#x3C;<span class=\"hljs-built_in\">string</span>> <span class=\"hljs-title\">RevalidateNow</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> now = UtcNow();\n    <span class=\"hljs-keyword\">lock</span> (_lock)\n    {\n        <span class=\"hljs-keyword\">if</span> (now - _lastSweepUtc &#x3C; ThrottleWindow)\n            <span class=\"hljs-keyword\">return</span> Array.Empty&#x3C;<span class=\"hljs-built_in\">string</span>>();\n        _lastSweepUtc = now;\n    }\n</code></pre>\n<p><a href=\"/async-resources/\"><code>FocusRevalidationService</code></a> is a different\nsense of \"focus\" — it's the window-focus event, not the keyboard one.\nWhen the user Alt-Tabs back to a Reactor app, any\n<a href=\"/hooks/\"><code>UseResource</code></a> hook that opted in via\n<code>ResourceOptions.RefetchOnWindowFocus = true</code> gets its cache entry\ninvalidated, which fires a re-fetch on next render. The 30-second\nthrottle is what keeps a quick Alt-Tab in and out from triggering a\nstorm of refetches across every enrolled resource — without it, an\napp open in five windows during a normal alt-tab cycle would refetch\nevery resource in every window each time the user crossed any of\nthem.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"trapping-focus-in-a-modal\">Trapping focus in a modal<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#trapping-focus-in-a-modal\">#</a></h3>\n<p>A modal dialog is the canonical use of <code>UseFocusTrap</code> and\n<a href=\"/accessibility/\"><code>UseAnnounce</code></a> together. The trap keeps Tab\ninside the dialog; the announcement tells the screen reader the\ndialog opened.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">override</span> Element <span class=\"hljs-title\">Render</span>()</span>\n{\n    <span class=\"hljs-keyword\">var</span> (open, setOpen) = UseState(<span class=\"hljs-literal\">false</span>);\n    <span class=\"hljs-keyword\">var</span> trap = UseFocusTrap(isActive: open);\n    <span class=\"hljs-keyword\">var</span> announce = UseAnnounce();\n\n    <span class=\"hljs-keyword\">return</span> Layer(\n        announce.Region,\n        MainContent(),\n        open\n            ? Dialog(\n                Text(<span class=\"hljs-string\">\"Are you sure?\"</span>),\n                HStack(\n                    Button(<span class=\"hljs-string\">\"Cancel\"</span>, () => setOpen(<span class=\"hljs-literal\">false</span>)),\n                    Button(<span class=\"hljs-string\">\"Delete\"</span>, () => { Delete(); setOpen(<span class=\"hljs-literal\">false</span>); })\n                )\n              )\n              .FocusTrap(trap)\n              .OnMount(() => announce.Announce(<span class=\"hljs-string\">\"Confirm delete dialog opened\"</span>, assertive: <span class=\"hljs-literal\">true</span>))\n            : <span class=\"hljs-literal\">null</span>\n    );\n}\n</code></pre>\n<p>The <code>UseFocusTrap(open)</code> call evaluates the active flag on every\nrender — flip <code>open</code> to false and the trap deactivates on the next\nreconcile pass; flip it back to true and the trap reattaches. The\n<a href=\"/reconciliation/\">reconciliation</a> page describes the mount /\nunmount machinery that drives the <code>OnMount</code> callback.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"calling-controlfocus-directly-from-an-event-handler\">Calling control.Focus() directly from an event handler<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#calling-controlfocus-directly-from-an-event-handler\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\nButton(<span class=\"hljs-string\">\"Edit\"</span>, () => {\n    setEditing(<span class=\"hljs-literal\">true</span>);\n    inputControl.Focus(FocusState.Programmatic);  <span class=\"hljs-comment\">// dispatcher race</span>\n});\n</code></pre>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">FocusNext</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span>? currentField = <span class=\"hljs-literal\">null</span></span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (_fieldOrder.Count == <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (currentField <span class=\"hljs-keyword\">is</span> <span class=\"hljs-literal\">null</span>)\n    {\n        FocusField(_fieldOrder[<span class=\"hljs-number\">0</span>]);\n        <span class=\"hljs-keyword\">return</span>;\n    }\n\n    <span class=\"hljs-keyword\">var</span> index = _fieldOrder.IndexOf(currentField);\n    <span class=\"hljs-keyword\">if</span> (index &#x3C; <span class=\"hljs-number\">0</span>) <span class=\"hljs-keyword\">return</span>;\n\n    <span class=\"hljs-keyword\">if</span> (index + <span class=\"hljs-number\">1</span> &#x3C; _fieldOrder.Count)\n    {\n        FocusField(_fieldOrder[index + <span class=\"hljs-number\">1</span>]);\n    }\n    <span class=\"hljs-keyword\">else</span>\n    {\n        _onSubmit?.Invoke();\n    }\n}\n</code></pre>\n<p>The setState call queues a re-render; the focus call runs\nimmediately. On the next dispatcher tick the reconciler may\nre-create the input control (if the conditional rendering paths\ndiffer enough), at which point the old reference is unmounted and\nthe new control hasn't been focused. Route programmatic focus\nthrough <code>UseFocus()</code> (which captures the control during the\nreconciler's set pass) or <a href=\"/hooks/\"><code>UseElementFocus()</code></a> (which\nschedules the focus call on the dispatcher after the render\ncommits) and the race goes away.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Set the trap's <code>active</code> flag, never call <code>Attach</code> / <code>Detach</code>.</strong>\nThe handle's <code>IsActive</code> setter is the only public mutation point. It\nruns the attach / detach internally and idempotently — calling it\nwith the same value is a no-op, calling it true / false on\nconsecutive renders correctly cycles the <code>LosingFocus</code> subscription.</p>\n<p><strong><code>UseElementFocus</code> is the dispatcher-safe focus call.</strong> When the\ntarget is one specific element rather than one of several form\nfields, prefer <code>UseElementFocus()</code> over storing a control reference.\nThe returned <code>RequestFocus</code> action schedules its call through the\ndispatcher trampoline so it runs after the current render commits.</p>\n<p><strong>Continuous gestures must handle <code>Cancelled</code>.</strong> Every snapshot\nallocated in <code>Began</code> must be released in either <code>Ended</code> or\n<code>Cancelled</code>. Match on <code>gesture.Phase</code> with all four arms — the\ncompiler won't warn on a missing case, and the leak only shows up\nwhen a user backgrounds the app mid-drag.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/input-and-gestures/\">Input and Gestures</a></strong> — Surface-level event API.</li>\n<li><strong><a href=\"/accessibility/\">Accessibility</a></strong> — User-facing accessibility hooks and conventions.</li>\n<li><strong><a href=\"/forms/\">Forms</a></strong> — How <code>UseFocus</code> plugs into the form authoring story.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — Mount / update lifecycle that drives <code>OnMount</code> callbacks.</li>\n<li><strong><a href=\"/animation-pipeline/\">Animation Pipeline</a></strong> — Previous under-the-hood page.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor)'s focus and input surfaces are thin wrappers over WinUI's routed-event system — every pointer event, every key press, every gesture starts in WinUI's hit-test, walks the visual tree as a routed event, and lands at a Reactor element modifier like .OnTapped or .OnKeyDown . Three subsystems sit alongside the event flow. UseFocusTrap rides on LosingFocus to cancel focus changes that would leave the trap container. FocusManager registers fields and drives enter-to-advance on form input. UseAnnounce takes a shortcut around focus entirely — it raises an automation-peer notification, not a focus event, so the announcement reaches a screen reader even when nothing visible changes. The most common mistake is treating focus as a property you set: control.Focus(...) requests focus, and the request loses to anything else competing on the same dispatcher tick. Route programmatic focus through FocusManager so the request runs at the right point in the lifecycle. Focus and Input Internals # This page covers the input subsystem from the routed-event boundary through the Reactor hooks that observe it. User-facing API is on Input and Gestures and Accessibility ; this is the internals view. The event flow # WinUI owns the early stages: hit-testing under the pointer, the tunneling preview events, the bubbling action events. By the time a Reactor modifier sees an event, WinUI has already picked the target and tunneled / bubbled the event through the visual tree. Reactor's job at that boundary is two things — wire a RoutedEventHandler to the materialized control during the reconciler's mount pass, and attach the Reactor element's user callback to that handler. There's no parallel event tree; the routes you see in .OnPointerPressed are the WinUI routes, plus a small layer that translates RoutedEventArgs into Reactor's event-args records. Reference # Surface Hook / type Wired to Primary use Pointer event .OnPointerPressed(...) WinUI PointerPressed routed event Low-level pointer (mouse / pen / touch) Tap .OnTapped(...) WinUI Tapped routed event Discrete click / tap with WinUI's gesture recognizer Pan / pinch / rotate .OnPan(...) WinUI Manipulation* events Continuous gestures with phase machine Focus management UseFocus() → FocusManager control.Focus(Programmatic) Form field ordering + enter-to-advance Focus trap UseFocusTrap(active) UIElement.LosingFocus Modal / flyout containment Live region UseAnnounce() FrameworkElementAutomationPeer.RaiseNotificationEvent Screen-reader announcements Element focus UseElementFocus() Control.Focus() via dispatcher Imperative focus on a single element Window-focus revalidation FocusRevalidationService Window.Activated Refetch stale UseResource data The FocusManager # public void FocusNext ( string ? currentField = null ) { if (_fieldOrder.Count == 0 ) return ; if (currentField is null ) { FocusField(_fieldOrder[ 0 ]); return ; } var index = _fieldOrder.IndexOf(currentField); if (index &#x3C; 0 ) return ; if (index + 1 &#x3C; _fieldOrder.Count) { FocusField(_fieldOrder[index + 1 ]); } else { _onSubmit?.Invoke(); } } FocusManager is the form-side companion to WinUI's focus system. Components call useFocus.Register(name) on each input during render, and the manager keeps a List&#x3C;string> of field names in order. FocusNext and FocusPrevious walk that list — the common pattern is &#x3C;TextBox>.OnEnter(() => fm.FocusNext(name)) so pressing Enter on a field advances to the next one, and Enter on the last field calls the registered submit handler. The _controls dictionary maps each field name to the live WinUI Control ; when .Set(\"name\") runs on mount, it stashes the control in the manager. Programmatic focus calls control.Focus(FocusState.Programmatic) , which is the WinUI mechanism for \"I, the app, decided to move focus\" rather than a user-initiated tab. Caveat: Register is called in render order, on every render. That means adding a field at index 1 (between two existing fields) requires a clean re-register cycle — the manager's Contains check makes re-registration idempotent for existing names, but it can't reorder. For dynamic forms that reorder fields, call Clear() at the top of the render and re-register; for forms with conditional fields, prefer rendering all fields and toggling .IsVisible over removing them from the tree, because removal also un-registers focus. The focus trap # private void OnLosingFocus ( UIElement sender, LosingFocusEventArgs args ) { if (!_isActive || _container is null ) return ; // SECURITY (TASK-100): refuse to trap when the container has fallen // out of the visual state where trapping is sensible. Without these // gates, a hidden / collapsed / unloaded container wedges keyboard // navigation: the user can't focus anything else and Esc / Tab // bounce back uselessly. if (_container is FrameworkElement fe) { if (!fe.IsLoaded) return ; if (fe.Visibility != Visibility.Visible) return ; } if (!_container.IsHitTestVisible) return ; UseFocusTrap lives on the LosingFocus routed event. When the trap is active, the handler runs before WinUI commits the focus change, walks from the proposed new-focus element back up the visual tree, and cancels the change if the target isn't a descendant of the trap container. That's the trap. The four gates at the top of the snippet are what make it safe — without them, a trap attached to a container that has since become hidden, unloaded, or collapsed wedges the user's keyboard: every Tab keystroke loses focus, the trap cancels every loss, and focus has no valid target inside the trap either. Each guard names a specific failure mode the trap has caused in the past. Cross-window navigation gets special treatment. A LosingFocus event whose NewFocusedElement has a different XamlRoot is a different window entirely; the trap deliberately doesn't try to drag the user back. That matters for dialogs that open a child window — the trap on the parent must not block focus moving into the child. Live-region announcements # public void Announce ( string message, bool assertive ) { if (_textBlock is null ) return ; // Primary path: RaiseNotificationEvent (WinUI 1.4+, best Narrator/NVDA support). var peer = FrameworkElementAutomationPeer.FromElement(_textBlock); if (peer is not null ) { peer.RaiseNotificationEvent( AutomationNotificationKind.ActionCompleted, assertive ? AutomationNotificationProcessing.ImportantAll : AutomationNotificationProcessing.ImportantMostRecent, message, \"ReactorAnnounce\" ); return ; } // Fallback: update the live-region TextBlock text. Screen readers that // monitor LiveSetting changes will pick this up. _textBlock.Text = message; } UseAnnounce goes around the focus path entirely. The primary mechanism is RaiseNotificationEvent (WinUI 1.4+) on a hidden TextBlock that the handle keeps materialized. Screen readers hooked into UI Automation receive the notification with a processing hint — ImportantAll for assertive announcements that interrupt current speech, ImportantMostRecent for polite announcements that queue. The fallback path updates TextBlock.Text directly; screen readers monitoring LiveSetting changes pick that up as a live-region update. The Region element the handle exposes is invisible, zero-sized, and mounts to a real TextBlock . Components include it once in their tree and call handle.Announce(...) from event handlers. The pattern mirrors WAI-ARIA's aria-live region: a stable element that exists solely so assistive tech has a stable target for live updates. Continuous gesture phases # public enum GesturePhase { /// &#x3C;summary> Gesture has just become recognized and its parameters are stable. &#x3C;/summary> Began, /// &#x3C;summary> Gesture is in progress; deltas are being reported. &#x3C;/summary> Changed, /// &#x3C;summary> Gesture completed normally. &#x3C;/summary> Ended, /// &#x3C;summary> Gesture was cancelled (pointer capture lost, window focus lost, …). &#x3C;/summary> Cancelled, } Pan, pinch, rotate, and long-press are continuous gestures — they fire once at Began , zero or more times at Changed as the user's finger moves, and exactly once at either Ended (normal release) or Cancelled (pointer capture lost, escape, window deactivation). The contract matters because component code routinely allocates state on Began (record the start position, snapshot the model under the pointer) and tears it down on the terminal phase. A handler that skips Cancelled leaks the snapshot the next time the user is mid-gesture and switches windows. The four phases map directly onto WinUI's ManipulationStarted / ManipulationDelta / ManipulationCompleted cycle, with the addition of an explicit Cancelled phase that WinUI emits as a completed manipulation with IsInertial = false and a pointer capture loss. The gesture wrappers .OnPan / .OnPinch / .OnRotate are the shim that converts that pair of WinUI events into a clean Reactor callback signature. Focus revalidation # public IReadOnlyList&#x3C; string > RevalidateNow () { var now = UtcNow(); lock (_lock) { if (now - _lastSweepUtc &#x3C; ThrottleWindow) return Array.Empty&#x3C; string >(); _lastSweepUtc = now; } FocusRevalidationService is a different sense of \"focus\" — it's the window-focus event, not the keyboard one. When the user Alt-Tabs back to a Reactor app, any UseResource hook that opted in via ResourceOptions.RefetchOnWindowFocus = true gets its cache entry invalidated, which fires a re-fetch on next render. The 30-second throttle is what keeps a quick Alt-Tab in and out from triggering a storm of refetches across every enrolled resource — without it, an app open in five windows during a normal alt-tab cycle would refetch every resource in every window each time the user crossed any of them. Patterns # Trapping focus in a modal # A modal dialog is the canonical use of UseFocusTrap and UseAnnounce together. The trap keeps Tab inside the dialog; the announcement tells the screen reader the dialog opened. public override Element Render () { var (open, setOpen) = UseState( false ); var trap = UseFocusTrap(isActive: open); var announce = UseAnnounce(); return Layer( announce.Region, MainContent(), open ? Dialog( Text( \"Are you sure?\" ), HStack( Button( \"Cancel\" , () => setOpen( false )), Button( \"Delete\" , () => { Delete(); setOpen( false ); }) ) ) .FocusTrap(trap) .OnMount(() => announce.Announce( \"Confirm delete dialog opened\" , assertive: true )) : null ); } The UseFocusTrap(open) call evaluates the active flag on every render — flip open to false and the trap deactivates on the next reconcile pass; flip it back to true and the trap reattaches. The reconciliation page describes the mount / unmount machinery that drives the OnMount callback. Common Mistakes # Calling control.Focus() directly from an event handler # // Don't: Button( \"Edit\" , () => { setEditing( true ); inputControl.Focus(FocusState.Programmatic); // dispatcher race }); public void FocusNext ( string ? currentField = null ) { if (_fieldOrder.Count == 0 ) return ; if (currentField is null ) { FocusField(_fieldOrder[ 0 ]); return ; } var index = _fieldOrder.IndexOf(currentField); if (index &#x3C; 0 ) return ; if (index + 1 &#x3C; _fieldOrder.Count) { FocusField(_fieldOrder[index + 1 ]); } else { _onSubmit?.Invoke(); } } The setState call queues a re-render; the focus call runs immediately. On the next dispatcher tick the reconciler may re-create the input control (if the conditional rendering paths differ enough), at which point the old reference is unmounted and the new control hasn't been focused. Route programmatic focus through UseFocus() (which captures the control during the reconciler's set pass) or UseElementFocus() (which schedules the focus call on the dispatcher after the render commits) and the race goes away. Tips # Set the trap's active flag, never call Attach / Detach . The handle's IsActive setter is the only public mutation point. It runs the attach / detach internally and idempotently — calling it with the same value is a no-op, calling it true / false on consecutive renders correctly cycles the LosingFocus subscription. UseElementFocus is the dispatcher-safe focus call. When the target is one specific element rather than one of several form fields, prefer UseElementFocus() over storing a control reference. The returned RequestFocus action schedules its call through the dispatcher trampoline so it runs after the current render commits. Continuous gestures must handle Cancelled . Every snapshot allocated in Began must be released in either Ended or Cancelled . Match on gesture.Phase with all four arms — the compiler won't warn on a missing case, and the leak only shows up when a user backgrounds the app mid-drag. Next Steps # Input and Gestures — Surface-level event API. Accessibility — User-facing accessibility hooks and conventions. Forms — How UseFocus plugs into the form authoring story. Reconciliation — Mount / update lifecycle that drives OnMount callbacks. Animation Pipeline — Previous under-the-hood page.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/focus-and-input-internals.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/focus-and-input-internals.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-event-flow",
          "text": "The event flow"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "the-focusmanager",
          "text": "The FocusManager"
        },
        {
          "depth": 2,
          "id": "the-focus-trap",
          "text": "The focus trap"
        },
        {
          "depth": 2,
          "id": "live-region-announcements",
          "text": "Live-region announcements"
        },
        {
          "depth": 2,
          "id": "continuous-gesture-phases",
          "text": "Continuous gesture phases"
        },
        {
          "depth": 2,
          "id": "focus-revalidation",
          "text": "Focus revalidation"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "trapping-focus-in-a-modal",
          "text": "Trapping focus in a modal"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "calling-controlfocus-directly-from-an-event-handler",
          "text": "Calling control.Focus() directly from an event handler"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Perf Instrumentation",
      "path": "perf-instrumentation.md",
      "slug": "/perf-instrumentation/",
      "html": "<p>Microsoft.UI.Reactor (Reactor) publishes a structured account of its internal work — every\nreconcile pass, every component render, every state write, every MCP\ntool call, every effect flush — through one managed\n<code>EventSource</code> named <code>Microsoft-UI-Reactor</code>. Consumers can subscribe to\nthe full firehose or to a subset by keyword (just reconcile events,\njust render events, just MCP traffic), and the emission path is\nkeyword-gated so retail with no consumer attached pays one branch\nand zero allocations per event site. The provider flows through both\nEventPipe (cross-platform, what <code>dotnet-trace</code> consumes) and classic\nETW (Windows-only, what PerfView and xperf consume). The most common\nmistake is reading \"EventSource\" and assuming it's a Windows-only or\nallocation-heavy mechanism; the managed EventSource lives in\n<code>System.Diagnostics.Tracing</code>, runs on every .NET platform, and emits\nthrough both pipes simultaneously with no per-event allocation when\nno consumer is listening.</p>\n<h1 id=\"perf-instrumentation\">Perf Instrumentation<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#perf-instrumentation\">#</a></h1>\n<p>This page covers what Reactor publishes, where to read it, and how to\nget a clean trace. The deeper sampling and attribution machinery\nlives in\n<a href=\"https://github.com/microsoft/reactor\">spec 031 (frame-aligned sampling)</a>\nand\n<a href=\"https://github.com/microsoft/reactor\">spec 032 (layout-cost attribution)</a>\n— this page links to those for the design rationale and focuses on\nwhat the runtime emits today.</p>\n<h2 id=\"the-emission-pipeline\">The emission pipeline<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-emission-pipeline\">#</a></h2>\n<p><img src=\"https://raw.githubusercontent.com/microsoft/microsoft-ui-reactor/main/docs/guide/images/perf-instrumentation/etw-flow.svg\" alt=\"ETW / EventPipe emission — hot paths through IsEnabled keyword gate to EventPipe + ETW, consumed by dotnet-trace or PerfView with optional Microsoft-Windows-XAML correlation\"></p>\n<p>A managed <code>EventSource</code> is a façade over two transports the .NET\nruntime maintains in parallel: EventPipe is the in-process, AOT-safe\nsession that <code>dotnet-trace</code> and <code>PerfCollect</code> consume; classic ETW is\nthe Windows kernel-level session that PerfView and xperf consume. The\nsame <code>WriteEvent</code> call writes to both. Choosing between the two is\nabout what <em>other</em> providers you need on the same timeline — Reactor\nplus the BCL is <code>dotnet-trace</code> territory, Reactor plus\n<code>Microsoft-Windows-XAML</code> (a native ETW provider WinUI emits from C++)\nrequires an ETW-based tool.</p>\n<h2 id=\"reference\">Reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#reference\">#</a></h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Provider</th><th>Type</th><th>Where it's consumed</th></tr></thead><tbody><tr><td><code>Microsoft-UI-Reactor</code></td><td>Managed <code>EventSource</code></td><td>EventPipe (<code>dotnet-trace</code>) + ETW (PerfView / xperf)</td></tr><tr><td><code>Microsoft-Windows-XAML</code></td><td>Native ETW (WinUI runtime)</td><td>ETW only — requires PerfView or xperf</td></tr><tr><td><code>System.Runtime</code></td><td>Built-in BCL <code>EventSource</code></td><td>Both pipes</td></tr><tr><td><code>Microsoft-Diagnostics-DiagnosticSource</code></td><td>DiagnosticSource bridge</td><td>EventPipe (<code>dotnet-trace</code>)</td></tr></tbody></table>\n<p>The Reactor provider's events split across a small set of keywords\nthat consumers can mask in or out independently. Keywords are\npower-of-two flags on <code>EventKeywords</code>, so a consumer can request\n\"reconcile + state\" with <code>0x5</code> (Reconcile=0x1 + State=0x4).</p>\n<h2 id=\"keywords\">Keywords<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#keywords\">#</a></h2>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">static</span> <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title\">Keywords</span>\n{\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Reconcile = (EventKeywords)<span class=\"hljs-number\">0x1</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Render = (EventKeywords)<span class=\"hljs-number\">0x2</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords State = (EventKeywords)<span class=\"hljs-number\">0x4</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Mcp = (EventKeywords)<span class=\"hljs-number\">0x8</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Lifecycle = (EventKeywords)<span class=\"hljs-number\">0x10</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Errors = (EventKeywords)<span class=\"hljs-number\">0x20</span>;\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords EventDispatch = (EventKeywords)<span class=\"hljs-number\">0x40</span>;\n    <span class=\"hljs-comment\">// Spec 044 — subsystem coverage gaps. Each gets its own bit so a</span>\n    <span class=\"hljs-comment\">// consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can</span>\n    <span class=\"hljs-comment\">// pick exactly the area it cares about without paying for the rest.</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Hosting = (EventKeywords)<span class=\"hljs-number\">0x80</span>;       <span class=\"hljs-comment\">// Window/HWND/DPI/Backdrop</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Persistence = (EventKeywords)<span class=\"hljs-number\">0x100</span>;  <span class=\"hljs-comment\">// settings store, placement</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Navigation = (EventKeywords)<span class=\"hljs-number\">0x200</span>;   <span class=\"hljs-comment\">// route push, cache, transitions</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Intl = (EventKeywords)<span class=\"hljs-number\">0x400</span>;         <span class=\"hljs-comment\">// missing keys, fallback, format</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Theme = (EventKeywords)<span class=\"hljs-number\">0x800</span>;        <span class=\"hljs-comment\">// theme apply, bindings</span>\n    <span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">const</span> EventKeywords Shell = (EventKeywords)<span class=\"hljs-number\">0x1000</span>;       <span class=\"hljs-comment\">// JumpList/Tray/ThumbnailToolbar</span>\n}\n</code></pre>\n<p>Seven keywords carve the event surface so a consumer can pay for\nexactly the events it wants. <code>Reconcile</code> covers the reconciler pass\nboundaries and the child-list diff; <code>Render</code> covers per-component\nrender timing and the effect-flush boundary; <code>State</code> covers\n<a href=\"/hooks-internals/\"><code>UseState</code></a> writes; <code>Mcp</code> covers\n<a href=\"/devtools-internals/\">devtools</a> tool dispatch; <code>Lifecycle</code> covers\nmount and unmount; <code>Errors</code> is severity-Error events; <code>EventDispatch</code>\ncovers the trampoline that fans WinUI routed events out to the\nReactor element callbacks. Adding a new event in any of those\ncategories is a matter of picking the right keyword and the right\nevent id; consumers already subscribed to the keyword pick the new\nevent up automatically.</p>\n<blockquote>\n<p><strong>Caveat:</strong> The default <code>EventLevel</code> for most events is <code>Informational</code> or\n<code>Verbose</code>. A <code>dotnet-trace</code> invocation that doesn't specify the level\ndefaults to <code>Verbose</code>, which means every <code>StateChange</code> event hits the\nsession — and a noisy app can write millions of those per minute.\nPass <code>--providers Microsoft-UI-Reactor:0x3:4</code> to scope to the\n<code>Reconcile | Render</code> keywords at <code>Informational</code> level only; the\ntrace size on a typical 30-second session drops from gigabytes to\ntens of megabytes.</p>\n</blockquote>\n<h2 id=\"the-isenabled-gate\">The IsEnabled gate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-isenabled-gate\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Event(1, Level = EventLevel.Informational, Keywords = Keywords.Reconcile,\n    Task = Tasks.Reconcile, Opcode = EventOpcode.Start,\n    Message = <span class=\"hljs-string\">\"Reconcile start (root={rootElementType})\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ReconcileStart</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> rootElementType</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Informational, Keywords.Reconcile))\n        WriteEvent(<span class=\"hljs-number\">1</span>, rootElementType ?? <span class=\"hljs-built_in\">string</span>.Empty);\n}\n</code></pre>\n<p>Every event method has the same shape: a single <code>IsEnabled</code> check\ngates the <code>WriteEvent</code> call. The pattern matters because\n<code>WriteEvent</code> with even one string argument allocates a per-call\nparameter array; the <code>IsEnabled</code> check is a static field read plus a\nmask compare. With no consumer attached the path costs the comparison\nand nothing else.</p>\n<p>For hot sites with computed payloads (a stringified type name, a\nformatted message, a timer reading), the call site does the\n<code>IsEnabled</code> check <em>before</em> computing the payload too — that's the\n\"defense in depth\" line in the <code>EventSource</code> xmldoc. Reactor's\nreconciler does this at its boundary call: it stops the\n<a href=\"https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.stopwatch\"><code>Stopwatch</code></a>,\nreads the elapsed microseconds, and stringifies the root element type\nall inside an outer <code>if (IsEnabled(...))</code> gate, so the payload\ncomputation is itself behind the gate.</p>\n<h2 id=\"the-reconcile-stop-event\">The reconcile stop event<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#the-reconcile-stop-event\">#</a></h2>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Event(2, Level = EventLevel.Informational, Keywords = Keywords.Reconcile,\n    Task = Tasks.Reconcile, Opcode = EventOpcode.Stop,\n    Message = <span class=\"hljs-string\">\"Reconcile stop (diffed={elementsDiffed}, skipped={elementsSkipped}, created={uiElementsCreated}, modified={uiElementsModified})\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ReconcileStop</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">int</span> elementsDiffed, <span class=\"hljs-built_in\">int</span> elementsSkipped, <span class=\"hljs-built_in\">int</span> uiElementsCreated, <span class=\"hljs-built_in\">int</span> uiElementsModified</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Informational, Keywords.Reconcile))\n        WriteEvent(<span class=\"hljs-number\">2</span>, elementsDiffed, elementsSkipped, uiElementsCreated, uiElementsModified);\n}\n</code></pre>\n<p><code>ReconcileStop</code> is the most-consumed event in the catalog — every\n<a href=\"/reconciliation/\">reconcile pass</a> emits exactly one of these, and\nthe four counters (elements diffed, elements skipped, UI elements\ncreated, UI elements modified) are the per-pass cost summary that\nthe <a href=\"/devtools-internals/\">layout-cost overlay</a> and the\n<code>perf</code> CLI verbs read. Pairing <code>ReconcileStart</code> (event id 1) with\n<code>ReconcileStop</code> (event id 2) over <code>Task = Tasks.Reconcile</code> with\n<code>Start</code> / <code>Stop</code> opcodes is what makes the pair show up as a\nduration band in PerfView's timeline view instead of as two\nunrelated points.</p>\n<p>The event ids are stable contract — never renumber. Adding a new\nevent always uses a new id; even when an event becomes obsolete it\nkeeps its slot so existing parsers don't shift. The same convention\napplies to the keyword bits.</p>\n<h2 id=\"taking-a-useful-trace\">Taking a useful trace<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#taking-a-useful-trace\">#</a></h2>\n<p>For Reactor-only profiling, <code>dotnet-trace</code> is the right tool — it's\ncross-platform, AOT-friendly, and writes a <code>.nettrace</code> file that\nVisual Studio's profiler and PerfView both open:</p>\n<pre><code>dotnet-trace collect --process-id &#x3C;pid> --providers Microsoft-UI-Reactor:0x3:4\n</code></pre>\n<p><code>0x3</code> masks <code>Reconcile | Render</code>, <code>:4</code> pins the level at\nInformational. For a session that needs to be correlated with the\nWinUI native render pipeline (when a frame stutter could be\nReactor's, WinUI's, or the compositor's), use PerfView or xperf\ninstead — those are the only consumers that see the native\n<code>Microsoft-Windows-XAML</code> provider. The\n<a href=\"https://github.com/microsoft/reactor\">ReactorEventSource.cs file header</a>\ncarries the exact xperf invocation; the GUIDs are stable across\nshipped versions.</p>\n<h2 id=\"spec-031-and-032--the-companion-specs\">Spec 031 and 032 — the companion specs<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#spec-031-and-032--the-companion-specs\">#</a></h2>\n<p>Two adjacent specs build on this provider rather than reshape it.\nSpec 031 — frame-aligned sampling — adds a per-frame heartbeat event\nso a consumer can correlate sample-based profiles (CPU hot spots)\nwith Reactor's frame boundaries. Spec 032 — layout-cost attribution\n— adds events at the WinUI panel measure / arrange boundary so an\nexpensive layout pass can be attributed back to the element that\ncaused it. Both are wired through the same <code>Microsoft-UI-Reactor</code>\nprovider with new event ids and new keywords; both leave the event\nshapes in this document untouched.</p>\n<h2 id=\"patterns\">Patterns<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#patterns\">#</a></h2>\n<h3 id=\"profiling-a-single-frame\">Profiling a single frame<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#profiling-a-single-frame\">#</a></h3>\n<p>A frame that looked janky in the <a href=\"/devtools-internals/\">reconcile-highlight\noverlay</a> is usually one of three things: an\nunintended re-render, a state write that fanned out to too many\nsubscribers, or an expensive layout pass. The ETW pair\n<code>ReconcileStart</code>/<code>ReconcileStop</code> plus the <code>ChildReconcileStart</code>/\n<code>ChildReconcileStop</code> band underneath gives the full picture for the\nfirst two. Open the trace in PerfView's \"Events\" view, filter to\n<code>Microsoft-UI-Reactor</code>, sort by elapsed microseconds; the outlier is\nthe frame that stuttered.</p>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// At the affected component, scope reconcile + render keywords only.</span>\n<span class=\"hljs-comment\">// dotnet-trace collect --process-id &#x3C;pid></span>\n<span class=\"hljs-comment\">//   --providers Microsoft-UI-Reactor:0x3:4   # Reconcile | Render</span>\n</code></pre>\n<p>The <code>elementsSkipped</code> counter in <code>ReconcileStop</code> is the cheapest\nindicator of <a href=\"/reactivity-model/\"><code>ShouldUpdate</code></a> wins — high\nskipped counts mean the bail-out is working; low skipped counts on\na long pass usually mean the <a href=\"/hooks/\"><code>UseMemo</code></a> deps array is\nunstable.</p>\n<h2 id=\"common-mistakes\">Common Mistakes<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#common-mistakes\">#</a></h2>\n<h3 id=\"computing-a-payload-outside-the-isenabled-gate\">Computing a payload outside the IsEnabled gate<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#computing-a-payload-outside-the-isenabled-gate\">#</a></h3>\n<pre><code class=\"hljs language-csharp\"><span class=\"hljs-comment\">// Don't:</span>\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ComponentRenderStop</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> componentName, Stopwatch sw</span>)</span>\n{\n    <span class=\"hljs-keyword\">var</span> elapsed = sw.Elapsed.TotalMicroseconds;  <span class=\"hljs-comment\">// computed even with no consumer</span>\n    <span class=\"hljs-keyword\">var</span> typeName = componentName ?? <span class=\"hljs-string\">\"&#x3C;anonymous>\"</span>;  <span class=\"hljs-comment\">// string alloc on null</span>\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Informational, Keywords.Render))\n        WriteEvent(<span class=\"hljs-number\">4</span>, typeName, (<span class=\"hljs-built_in\">long</span>)elapsed);\n}\n</code></pre>\n<pre><code class=\"hljs language-csharp\">[<span class=\"hljs-meta\">Event(1, Level = EventLevel.Informational, Keywords = Keywords.Reconcile,\n    Task = Tasks.Reconcile, Opcode = EventOpcode.Start,\n    Message = <span class=\"hljs-string\">\"Reconcile start (root={rootElementType})\"</span>)</span>]\n<span class=\"hljs-function\"><span class=\"hljs-keyword\">public</span> <span class=\"hljs-keyword\">void</span> <span class=\"hljs-title\">ReconcileStart</span>(<span class=\"hljs-params\"><span class=\"hljs-built_in\">string</span> rootElementType</span>)</span>\n{\n    <span class=\"hljs-keyword\">if</span> (IsEnabled(EventLevel.Informational, Keywords.Reconcile))\n        WriteEvent(<span class=\"hljs-number\">1</span>, rootElementType ?? <span class=\"hljs-built_in\">string</span>.Empty);\n}\n</code></pre>\n<p>The anti-pattern allocates and computes the payload every render\neven when no consumer is listening. With three hot events per\nrender pass and 60 renders per second, the per-second allocation\ncost is real — multiplied across an event surface this dense, it\nshows up on a flame graph. The correct shape gates <em>both</em> the\npayload computation and the <code>WriteEvent</code> call behind the\n<code>IsEnabled</code> check, so retail pays one branch and nothing else.</p>\n<h2 id=\"tips\">Tips<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#tips\">#</a></h2>\n<p><strong>Pin the level when you collect.</strong> <code>dotnet-trace</code> defaults to\n<code>Verbose</code>, which means every <code>StateChange</code> and every event-dispatch\nevent hits the session. Pin to <code>Informational</code> (<code>:4</code>) unless you\nactively want the verbose surface — the trace shrinks by an order\nof magnitude.</p>\n<p><strong><code>Microsoft-UI-Reactor</code> works with <code>Microsoft-Windows-XAML</code> only in\nETW tools.</strong> When you need Reactor and the WinUI native renderer on\nthe same timeline, that's PerfView or xperf. <code>dotnet-trace</code>\nemits an EventPipe-only session that the native provider doesn't\nflow into; you'd see Reactor's events in isolation, missing the\nWinUI side of every frame.</p>\n<p><strong>Use the <code>_reconcile-stop-event</code> band to spot the outlier.</strong> PerfView's\n\"Events\" view groups paired Start/Stop events into duration bands.\nSort by elapsed microseconds, scan for any band that's an order of\nmagnitude longer than the median, and the offending pass is named\nin the band's event payload.</p>\n<h2 id=\"next-steps\">Next Steps<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#next-steps\">#</a></h2>\n<ul>\n<li><strong><a href=\"/diagnostics/\">Diagnostics</a></strong> — How to capture, filter, and read the error / HR / subsystem events Reactor emits in Release builds, plus <code>ReactorTrace.Subscribe</code> and <code>reactor.logs source=event</code>.</li>\n<li><strong><a href=\"/devtools-internals/\">DevTools Internals</a></strong> — Where the overlays consume these events.</li>\n<li><strong><a href=\"/reconciliation/\">Reconciliation</a></strong> — What the reconcile-stop counters describe.</li>\n<li><strong><a href=\"/hooks-internals/\">Hooks Internals</a></strong> — Why <code>StateChange</code> fires from inside the slot table.</li>\n<li><strong><a href=\"/animation-pipeline/\">Animation Pipeline</a></strong> — Companion under-the-hood page on animation.</li>\n<li><strong><a href=\"/dev-tooling/\">Dev Tooling</a></strong> — User-facing surface for the perf tooling.</li>\n</ul>",
      "text": "Microsoft.UI.Reactor (Reactor) publishes a structured account of its internal work — every reconcile pass, every component render, every state write, every MCP tool call, every effect flush — through one managed EventSource named Microsoft-UI-Reactor . Consumers can subscribe to the full firehose or to a subset by keyword (just reconcile events, just render events, just MCP traffic), and the emission path is keyword-gated so retail with no consumer attached pays one branch and zero allocations per event site. The provider flows through both EventPipe (cross-platform, what dotnet-trace consumes) and classic ETW (Windows-only, what PerfView and xperf consume). The most common mistake is reading \"EventSource\" and assuming it's a Windows-only or allocation-heavy mechanism; the managed EventSource lives in System.Diagnostics.Tracing , runs on every .NET platform, and emits through both pipes simultaneously with no per-event allocation when no consumer is listening. Perf Instrumentation # This page covers what Reactor publishes, where to read it, and how to get a clean trace. The deeper sampling and attribution machinery lives in spec 031 (frame-aligned sampling) and spec 032 (layout-cost attribution) — this page links to those for the design rationale and focuses on what the runtime emits today. The emission pipeline # A managed EventSource is a façade over two transports the .NET runtime maintains in parallel: EventPipe is the in-process, AOT-safe session that dotnet-trace and PerfCollect consume; classic ETW is the Windows kernel-level session that PerfView and xperf consume. The same WriteEvent call writes to both. Choosing between the two is about what other providers you need on the same timeline — Reactor plus the BCL is dotnet-trace territory, Reactor plus Microsoft-Windows-XAML (a native ETW provider WinUI emits from C++) requires an ETW-based tool. Reference # Provider Type Where it's consumed Microsoft-UI-Reactor Managed EventSource EventPipe ( dotnet-trace ) + ETW (PerfView / xperf) Microsoft-Windows-XAML Native ETW (WinUI runtime) ETW only — requires PerfView or xperf System.Runtime Built-in BCL EventSource Both pipes Microsoft-Diagnostics-DiagnosticSource DiagnosticSource bridge EventPipe ( dotnet-trace ) The Reactor provider's events split across a small set of keywords that consumers can mask in or out independently. Keywords are power-of-two flags on EventKeywords , so a consumer can request \"reconcile + state\" with 0x5 (Reconcile=0x1 + State=0x4). Keywords # public static class Keywords { public const EventKeywords Reconcile = (EventKeywords) 0x1 ; public const EventKeywords Render = (EventKeywords) 0x2 ; public const EventKeywords State = (EventKeywords) 0x4 ; public const EventKeywords Mcp = (EventKeywords) 0x8 ; public const EventKeywords Lifecycle = (EventKeywords) 0x10 ; public const EventKeywords Errors = (EventKeywords) 0x20 ; public const EventKeywords EventDispatch = (EventKeywords) 0x40 ; // Spec 044 — subsystem coverage gaps. Each gets its own bit so a // consumer (dotnet-trace, EventListener, ReactorTrace.Subscribe) can // pick exactly the area it cares about without paying for the rest. public const EventKeywords Hosting = (EventKeywords) 0x80 ; // Window/HWND/DPI/Backdrop public const EventKeywords Persistence = (EventKeywords) 0x100 ; // settings store, placement public const EventKeywords Navigation = (EventKeywords) 0x200 ; // route push, cache, transitions public const EventKeywords Intl = (EventKeywords) 0x400 ; // missing keys, fallback, format public const EventKeywords Theme = (EventKeywords) 0x800 ; // theme apply, bindings public const EventKeywords Shell = (EventKeywords) 0x1000 ; // JumpList/Tray/ThumbnailToolbar } Seven keywords carve the event surface so a consumer can pay for exactly the events it wants. Reconcile covers the reconciler pass boundaries and the child-list diff; Render covers per-component render timing and the effect-flush boundary; State covers UseState writes; Mcp covers devtools tool dispatch; Lifecycle covers mount and unmount; Errors is severity-Error events; EventDispatch covers the trampoline that fans WinUI routed events out to the Reactor element callbacks. Adding a new event in any of those categories is a matter of picking the right keyword and the right event id; consumers already subscribed to the keyword pick the new event up automatically. Caveat: The default EventLevel for most events is Informational or Verbose . A dotnet-trace invocation that doesn't specify the level defaults to Verbose , which means every StateChange event hits the session — and a noisy app can write millions of those per minute. Pass --providers Microsoft-UI-Reactor:0x3:4 to scope to the Reconcile | Render keywords at Informational level only; the trace size on a typical 30-second session drops from gigabytes to tens of megabytes. The IsEnabled gate # [ Event(1, Level = EventLevel.Informational, Keywords = Keywords.Reconcile, Task = Tasks.Reconcile, Opcode = EventOpcode.Start, Message = \"Reconcile start (root={rootElementType})\" ) ] public void ReconcileStart ( string rootElementType ) { if (IsEnabled(EventLevel.Informational, Keywords.Reconcile)) WriteEvent( 1 , rootElementType ?? string .Empty); } Every event method has the same shape: a single IsEnabled check gates the WriteEvent call. The pattern matters because WriteEvent with even one string argument allocates a per-call parameter array; the IsEnabled check is a static field read plus a mask compare. With no consumer attached the path costs the comparison and nothing else. For hot sites with computed payloads (a stringified type name, a formatted message, a timer reading), the call site does the IsEnabled check before computing the payload too — that's the \"defense in depth\" line in the EventSource xmldoc. Reactor's reconciler does this at its boundary call: it stops the Stopwatch , reads the elapsed microseconds, and stringifies the root element type all inside an outer if (IsEnabled(...)) gate, so the payload computation is itself behind the gate. The reconcile stop event # [ Event(2, Level = EventLevel.Informational, Keywords = Keywords.Reconcile, Task = Tasks.Reconcile, Opcode = EventOpcode.Stop, Message = \"Reconcile stop (diffed={elementsDiffed}, skipped={elementsSkipped}, created={uiElementsCreated}, modified={uiElementsModified})\" ) ] public void ReconcileStop ( int elementsDiffed, int elementsSkipped, int uiElementsCreated, int uiElementsModified ) { if (IsEnabled(EventLevel.Informational, Keywords.Reconcile)) WriteEvent( 2 , elementsDiffed, elementsSkipped, uiElementsCreated, uiElementsModified); } ReconcileStop is the most-consumed event in the catalog — every reconcile pass emits exactly one of these, and the four counters (elements diffed, elements skipped, UI elements created, UI elements modified) are the per-pass cost summary that the layout-cost overlay and the perf CLI verbs read. Pairing ReconcileStart (event id 1) with ReconcileStop (event id 2) over Task = Tasks.Reconcile with Start / Stop opcodes is what makes the pair show up as a duration band in PerfView's timeline view instead of as two unrelated points. The event ids are stable contract — never renumber. Adding a new event always uses a new id; even when an event becomes obsolete it keeps its slot so existing parsers don't shift. The same convention applies to the keyword bits. Taking a useful trace # For Reactor-only profiling, dotnet-trace is the right tool — it's cross-platform, AOT-friendly, and writes a .nettrace file that Visual Studio's profiler and PerfView both open: dotnet-trace collect --process-id &#x3C;pid> --providers Microsoft-UI-Reactor:0x3:4 0x3 masks Reconcile | Render , :4 pins the level at Informational. For a session that needs to be correlated with the WinUI native render pipeline (when a frame stutter could be Reactor's, WinUI's, or the compositor's), use PerfView or xperf instead — those are the only consumers that see the native Microsoft-Windows-XAML provider. The ReactorEventSource.cs file header carries the exact xperf invocation; the GUIDs are stable across shipped versions. Spec 031 and 032 — the companion specs # Two adjacent specs build on this provider rather than reshape it. Spec 031 — frame-aligned sampling — adds a per-frame heartbeat event so a consumer can correlate sample-based profiles (CPU hot spots) with Reactor's frame boundaries. Spec 032 — layout-cost attribution — adds events at the WinUI panel measure / arrange boundary so an expensive layout pass can be attributed back to the element that caused it. Both are wired through the same Microsoft-UI-Reactor provider with new event ids and new keywords; both leave the event shapes in this document untouched. Patterns # Profiling a single frame # A frame that looked janky in the reconcile-highlight overlay is usually one of three things: an unintended re-render, a state write that fanned out to too many subscribers, or an expensive layout pass. The ETW pair ReconcileStart / ReconcileStop plus the ChildReconcileStart / ChildReconcileStop band underneath gives the full picture for the first two. Open the trace in PerfView's \"Events\" view, filter to Microsoft-UI-Reactor , sort by elapsed microseconds; the outlier is the frame that stuttered. // At the affected component, scope reconcile + render keywords only. // dotnet-trace collect --process-id &#x3C;pid> // --providers Microsoft-UI-Reactor:0x3:4 # Reconcile | Render The elementsSkipped counter in ReconcileStop is the cheapest indicator of ShouldUpdate wins — high skipped counts mean the bail-out is working; low skipped counts on a long pass usually mean the UseMemo deps array is unstable. Common Mistakes # Computing a payload outside the IsEnabled gate # // Don't: public void ComponentRenderStop ( string componentName, Stopwatch sw ) { var elapsed = sw.Elapsed.TotalMicroseconds; // computed even with no consumer var typeName = componentName ?? \"&#x3C;anonymous>\" ; // string alloc on null if (IsEnabled(EventLevel.Informational, Keywords.Render)) WriteEvent( 4 , typeName, ( long )elapsed); } [ Event(1, Level = EventLevel.Informational, Keywords = Keywords.Reconcile, Task = Tasks.Reconcile, Opcode = EventOpcode.Start, Message = \"Reconcile start (root={rootElementType})\" ) ] public void ReconcileStart ( string rootElementType ) { if (IsEnabled(EventLevel.Informational, Keywords.Reconcile)) WriteEvent( 1 , rootElementType ?? string .Empty); } The anti-pattern allocates and computes the payload every render even when no consumer is listening. With three hot events per render pass and 60 renders per second, the per-second allocation cost is real — multiplied across an event surface this dense, it shows up on a flame graph. The correct shape gates both the payload computation and the WriteEvent call behind the IsEnabled check, so retail pays one branch and nothing else. Tips # Pin the level when you collect. dotnet-trace defaults to Verbose , which means every StateChange and every event-dispatch event hits the session. Pin to Informational ( :4 ) unless you actively want the verbose surface — the trace shrinks by an order of magnitude. Microsoft-UI-Reactor works with Microsoft-Windows-XAML only in ETW tools. When you need Reactor and the WinUI native renderer on the same timeline, that's PerfView or xperf. dotnet-trace emits an EventPipe-only session that the native provider doesn't flow into; you'd see Reactor's events in isolation, missing the WinUI side of every frame. Use the _reconcile-stop-event band to spot the outlier. PerfView's \"Events\" view groups paired Start/Stop events into duration bands. Sort by elapsed microseconds, scan for any band that's an order of magnitude longer than the median, and the offending pass is named in the band's event payload. Next Steps # Diagnostics — How to capture, filter, and read the error / HR / subsystem events Reactor emits in Release builds, plus ReactorTrace.Subscribe and reactor.logs source=event . DevTools Internals — Where the overlays consume these events. Reconciliation — What the reconcile-stop counters describe. Hooks Internals — Why StateChange fires from inside the slot table. Animation Pipeline — Companion under-the-hood page on animation. Dev Tooling — User-facing surface for the perf tooling.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/perf-instrumentation.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/perf-instrumentation.md",
      "headings": [
        {
          "depth": 2,
          "id": "the-emission-pipeline",
          "text": "The emission pipeline"
        },
        {
          "depth": 2,
          "id": "reference",
          "text": "Reference"
        },
        {
          "depth": 2,
          "id": "keywords",
          "text": "Keywords"
        },
        {
          "depth": 2,
          "id": "the-isenabled-gate",
          "text": "The IsEnabled gate"
        },
        {
          "depth": 2,
          "id": "the-reconcile-stop-event",
          "text": "The reconcile stop event"
        },
        {
          "depth": 2,
          "id": "taking-a-useful-trace",
          "text": "Taking a useful trace"
        },
        {
          "depth": 2,
          "id": "spec-031-and-032--the-companion-specs",
          "text": "Spec 031 and 032 — the companion specs"
        },
        {
          "depth": 2,
          "id": "patterns",
          "text": "Patterns"
        },
        {
          "depth": 3,
          "id": "profiling-a-single-frame",
          "text": "Profiling a single frame"
        },
        {
          "depth": 2,
          "id": "common-mistakes",
          "text": "Common Mistakes"
        },
        {
          "depth": 3,
          "id": "computing-a-payload-outside-the-isenabled-gate",
          "text": "Computing a payload outside the IsEnabled gate"
        },
        {
          "depth": 2,
          "id": "tips",
          "text": "Tips"
        },
        {
          "depth": 2,
          "id": "next-steps",
          "text": "Next Steps"
        }
      ]
    },
    {
      "title": "Hooks",
      "path": "reference/hooks/index.md",
      "slug": "/reference/hooks/",
      "html": "<h1 id=\"hooks-reference\">Hooks reference<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#hooks-reference\">#</a></h1>\n<p>Hooks are how Reactor components subscribe to state, schedule effects,\nand reach into the framework's lifecycle from a function-shaped\n<code>Render</code> body. Each entry on this index is generated from the\n<code>&#x3C;summary></code> XML doc comments authored alongside the source in\n<code>src/Reactor/Hooks/</code> — the canonical surface — and is grouped by the\n<code>hooks</code> category in <code>docs/_pipeline/reference-map.yaml</code>.</p>\n<p>For the narrative walkthrough of how hooks compose with components,\ncontext, and effects, see the <a href=\"/hooks/\">Hooks</a> and\n<a href=\"/effects/\">Effects</a> guide pages. The reference leaves below\nare the exhaustive index.</p>\n<h2 id=\"generated-leaves\">Generated leaves<a class=\"heading-anchor\" aria-label=\"Link to section\" href=\"#generated-leaves\">#</a></h2>\n<p>The leaf pages in this folder are produced by <code>mur docs compile</code>'s\nreference-generation step (spec 041 §10.4). Each page covers one\npublic type / method / property / field / event in the Hooks\nnamespace, with sections for Summary, Parameters, Returns,\nDiscussion, Examples, Caveats, and See Also.</p>\n<blockquote>\n<p><strong>Heads up.</strong> This index is one of the few hand-authored files\nunder <code>docs/guide/reference/</code> — the per-member pages around it are\ncompiler output and should not be hand-edited. See\n<a href=\"https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/specs/041-docs-comprehensive-uplift.md\">spec 041 §10.4</a>\nfor the full rationale.</p>\n</blockquote>",
      "text": "Hooks reference # Hooks are how Reactor components subscribe to state, schedule effects, and reach into the framework's lifecycle from a function-shaped Render body. Each entry on this index is generated from the &#x3C;summary> XML doc comments authored alongside the source in src/Reactor/Hooks/ — the canonical surface — and is grouped by the hooks category in docs/_pipeline/reference-map.yaml . For the narrative walkthrough of how hooks compose with components, context, and effects, see the Hooks and Effects guide pages. The reference leaves below are the exhaustive index. Generated leaves # The leaf pages in this folder are produced by mur docs compile 's reference-generation step (spec 041 §10.4). Each page covers one public type / method / property / field / event in the Hooks namespace, with sections for Summary, Parameters, Returns, Discussion, Examples, Caveats, and See Also. Heads up. This index is one of the few hand-authored files under docs/guide/reference/ — the per-member pages around it are compiler output and should not be hand-edited. See spec 041 §10.4 for the full rationale.",
      "sourceUrl": "https://github.com/microsoft/microsoft-ui-reactor/blob/main/docs/guide/reference/hooks/index.md",
      "editUrl": "https://github.com/microsoft/microsoft-ui-reactor/edit/main/docs/guide/reference/hooks/index.md",
      "headings": [
        {
          "depth": 2,
          "id": "generated-leaves",
          "text": "Generated leaves"
        }
      ]
    }
  ],
  "routeToDoc": {
    "/": "index.md",
    "/getting-started/": "getting-started.md",
    "/thinking-in-reactor/": "thinking-in-reactor.md",
    "/xaml-developers/": "xaml-developers.md",
    "/reactor-vs-xaml/": "reactor-vs-xaml.md",
    "/components/": "components.md",
    "/hooks/": "hooks.md",
    "/effects/": "effects.md",
    "/context/": "context.md",
    "/commanding/": "commanding.md",
    "/advanced/": "advanced.md",
    "/layout/": "layout.md",
    "/flex-layout/": "flex-layout.md",
    "/styling/": "styling.md",
    "/animation/": "animation.md",
    "/input-and-gestures/": "input-and-gestures.md",
    "/controls/": "controls.md",
    "/forms/": "forms.md",
    "/collections/": "collections.md",
    "/text-and-media/": "text-and-media.md",
    "/status-and-info/": "status-and-info.md",
    "/dialogs-and-flyouts/": "dialogs-and-flyouts.md",
    "/data-system/": "data-system.md",
    "/charting/": "charting.md",
    "/navigation/": "navigation.md",
    "/windows/": "windows.md",
    "/docking/": "docking.md",
    "/async-resources/": "async-resources.md",
    "/persistence/": "persistence.md",
    "/localization/": "localization.md",
    "/accessibility/": "accessibility.md",
    "/recipes/": "recipes.md",
    "/recipes/command-palette/": "recipes/command-palette.md",
    "/recipes/drag-reorder/": "recipes/drag-reorder.md",
    "/recipes/login/": "recipes/login.md",
    "/recipes/master-detail/": "recipes/master-detail.md",
    "/recipes/modal-dialog/": "recipes/modal-dialog.md",
    "/recipes/multi-step-form/": "recipes/multi-step-form.md",
    "/recipes/paginated-list/": "recipes/paginated-list.md",
    "/recipes/search-with-suggestions/": "recipes/search-with-suggestions.md",
    "/recipes/settings-page/": "recipes/settings-page.md",
    "/cheat-sheet/": "cheat-sheet.md",
    "/rules-of-reactor/": "rules-of-reactor.md",
    "/theming-tokens/": "theming-tokens.md",
    "/dev-tooling/": "dev-tooling.md",
    "/testing/": "testing.md",
    "/performance/": "performance.md",
    "/packaging/": "packaging.md",
    "/diagnostics/": "diagnostics.md",
    "/winforms-interop/": "winforms-interop.md",
    "/wpf-interop/": "wpf-interop.md",
    "/architecture-overview/": "architecture-overview.md",
    "/reactivity-model/": "reactivity-model.md",
    "/hooks-internals/": "hooks-internals.md",
    "/reconciliation/": "reconciliation.md",
    "/control-reconciler-protocol/": "control-reconciler-protocol.md",
    "/extending-reactor-controls/": "extending-reactor-controls.md",
    "/element-pool/": "element-pool.md",
    "/effects-scheduling/": "effects-scheduling.md",
    "/threading-and-dispatch/": "threading-and-dispatch.md",
    "/source-mapping/": "source-mapping.md",
    "/modifier-system/": "modifier-system.md",
    "/analyzer-architecture/": "analyzer-architecture.md",
    "/devtools-internals/": "devtools-internals.md",
    "/animation-pipeline/": "animation-pipeline.md",
    "/focus-and-input-internals/": "focus-and-input-internals.md",
    "/perf-instrumentation/": "perf-instrumentation.md",
    "/reference/hooks/": "reference/hooks.md"
  }
} satisfies DocsManifest;
